node_modules/
.turbo/
dist/
.expo/
web-build/
*.log
.env
.env.local
.env.*.local
.DS_Store
# Supabase CLI local
supabase/.temp/
supabase/.branches/

# capturas de verify-i18n sin SCRATCH seteado (S56 — el default entra con D-351)
undefined/

# artefactos de build LOCAL (S78: la cuota EAS rebotó y el gate corrió
# sobre `eas build --local` — el APK queda en apps/<app>/ y jamás entra
# al repo; el artefacto canónico vive en EAS o en el dispositivo)
apps/*/build-*.apk

# Permisos locales de Claude Code — de ESTA máquina, jamás al repo
**/.claude/settings.local.json
**/.claude/settings.local.json.bak-*

# S106-C · el build/ del módulo nativo local (lo genera el gate en aislado)
packages/*/android/build/
