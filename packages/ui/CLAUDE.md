# packages/ui — Design system e-PetPlace

**Estado: S91 — 53 componentes, sistema exigible y consumido por los flujos reales de AMBOS apps.**

> **S91-B · NACE `ChipEntidad` — EL CHIP DE ENTIDAD, UNO SOLO EN TODA LA CASA (junta D-691).** Sube el **CHIP**, no el contenedor: `FiltroMascotas` es una hilera horizontal y el selector de raza del alta es una GRILLA, así que el seam correcto es la unidad — subir la hilera habría obligado a D a envolverla o clonarla. `FiltroMascotas` pasó a CONSUMIRLO (su copia murió, Ley 37 sobre los imports que quedaron muertos). **Contador re-medido: 52 → 53** (56 `.tsx` − 2 variantes `.web` − 1 infra), y R17 leído del propio lint: `exportaciones=76 · en-galería=67 · exentas-declaradas=9 · pendientes=0`.
> - **(a) «Crece un punto» (orden del founder), con números de la CASA y no de un ojo:** `compacto` es el de hoy (alto 44 · avatar `xs` 28 · texto `sm` 13) y `general` sube cada pieza UN PASO en su propia escala (avatar `sm` 40 · texto `base` 15), con el alto DERIVADO de conservar el aire (44−28 = 16 ⇒ 40+16 = **56**). **El alto no se eligió: es el que mantiene la respiración del chip firmado.** `compacto` es el default a propósito — los consumidores vivos no se mueven; el founder pidió crecer *para el uso nuevo*.
> - **(b) D-691 · el truncado NO nacía en esta pieza:** medido antes de tocar nada, la hilera no tenía `numberOfLines` ni `maxWidth`. Nace en **`SelectorOpcion:370`**, que en grilla fuerza `numberOfLines={1}` — por eso la cura es que D CONSUMA el chip, no que se parchee un ancho. De las cuatro salidas que D-691 deja abiertas se toma **envolver a dos líneas**: ensanchar rompe la grilla de dos columnas, bajar el cuerpo contradice la orden (a) que dice crecer, y truncar deja «Guacamayo Azul y Amarillo» (25) ilegible igual.
>
> **S91-B · NACE `sugerencias.ts` — EL TIPEO PREDICTIVO, y NO SUMA AL CONTADOR de componentes a propósito:** es un módulo `.ts` de matching puro, sin píxeles. Sube porque el patrón vivía INLINE en `hogar/bitacora.tsx` desde S65 y el alta de mascota lo necesita para la RAZA — **se generaliza, no se clona** (§6 del método). Lo que sube es la FORMA (el matching); el render y la voz quedan en cada casa, porque las dos interacciones NO son la misma: la bitácora propone chips que se marcan de a muchos, el alta propone UNA raza que llena un campo. **Sus dos perillas nacen con caso medido:** `minimoDeLetras` (el default 4 de la bitácora hace imposible buscar «lab»→Labrador) y `modo:'empieza'` (bajar el mínimo sin cambiar el modo sería peor: «lab» matchearía en el medio de cualquier palabra). **EXENTA DE R17 con su razón escrita** —mostrarla exigiría clonarle una interacción, y sería la de UNA de las dos casas—, y su gate es `scripts/verify-sugerencias.ts`, con brazo de REGRESIÓN contra la implementación vieja sobre el vocabulario VIVO: **no menos gate, otro gate.**
>
> ⚠️ **Y el hallazgo que salió de exentarla, medido y sin curar:** el parseo de R17 admite SOLO `PascalCase` y `useX`, así que **30 de los 105 exports identificadores del `index.ts` son INVISIBLES para la regla** —tokens, temas, infra de captura/archivos, y estas cinco funciones—. No están exentos: no entran al corpus. `recursosUi` ya estaba en la lista de exentos siendo invisible **sin decirlo**; `ALTO_PIE_CAMPO` sí lo dice. Ensanchar el filtro encendería 25 preexistentes de una vez ⇒ **es decisión de mesa, no de una tanda.**

> **RE-MEDIDO S88-B (L-141), mismo método auditable:** `ls src/components/*.tsx` = **55 archivos**, menos `MapaRecorrido.web.tsx` y `MapaZona.web.tsx` (variantes) y `capturaFoto.tsx` (infra) = **52 componentes**. Los **3** desde la medición de S85 (49): **TresNumeros** (S85-B26 — nació DESPUÉS de la medición de su propia sesión: la prosa se midió antes que el objeto terminara de crecer) · **CampoCodigo** (S88 — las cajas por dígito, lámina firmada) · **Badge** (S88 — el contador de novedades EXTRAÍDO de BarraTabs al ganar su segundo consumidor, la campana; con la variante `forma="huella"` de la lámina firmada). **El contador exigible (R17) leído del propio lint, no estimado: `exportaciones=75 · en-galería=66 · exentas-declaradas=9 · pendientes=0`** (decía 71 — creció con las piezas nuevas y sus hooks). **Glifos del registry: 42** — *RE-MEDIDO S91-B por las MISMAS dos vías, y las dos dan 42 coincidiendo en el CONJUNTO (cero nombres sin dibujo, cero dibujos sin nombre). Decía **41**: quedó viejo el día que S90-B depositó `receta` y su sesión no volvió a correr el contador. Es L-141 en su forma más barata —un glifo nuevo, un número sin actualizar— y por eso se re-mide al tocar el registry, no cuando alguien lo cite. La letra de S89 se conserva entera abajo porque su método sigue siendo el bueno:* **41** — *RE-MEDIDO S89-B al depositar `descargar`, y el número BAJÓ: decía **43**. Método, auditable y por DOS vías independientes: la union `IconoNombre` despojada de comentarios (L-170) da 41, y las claves del objeto de paths dan 41 — **y no coinciden solo en el número: coinciden en el CONJUNTO** (cero nombres en la union sin path, cero paths sin nombre en la union). Esa coincidencia es su propia prueba. El 43 de S88 no es reproducible por ninguna de las dos vías; se corrige acá porque quien toca el registry re-mide su contador (L-141), y un contador que declara DE MÁS es tan falso como uno que declara de menos.* (+`descargar` S89 — el hermano invertido de `compartir`, pedido de D). WCAG **178 / 0** corrido en vivo — cero pares nuevos en todo el lote.

> **RE-MEDIDO S85-B (L-141 — CUARTA vez que este contador se atrasa, y la cuarta se cuenta).** Método idéntico y auditable: `ls src/components/*.tsx` = **52 archivos**, menos `MapaRecorrido.web.tsx` y `MapaZona.web.tsx` (variantes de plataforma) y `capturaFoto.tsx` (infra) = **49 componentes**. Los **5** nacidos desde la medición de S82 (44): **MapaZona · FichaPrestador** (S84) · **FiltroPills · SelectorDia** (S85, PROMOVIDOS del cliente por la Regla de las Piezas) · y el crecimiento de `Insignia`, que no suma archivo pero sí familia (**`distincion`**, S85-B16). WCAG: **178 pares / 0 fallos**, corrido en vivo — las promovidas no traen pares nuevos (resuelven de slots) y la familia `distincion` reusa `capaBg`/`capaText`, que ya estaban medidos.
>
> **EL CONTADOR EXIGIBLE NO ES ÉSTE, y por eso éste se atrasa: es `verify:diseno` R17**, que cuenta las **exportaciones reales** de `index.ts` (**hoy 71**) y exige que cada una aparezca en la galería. Ése no envejece porque el lint se pone rojo. **Cuando los dos discrepen, gana R17 y esta línea se corrige** — jamás al revés. *(Cuatro escalas, todas correctas y midiendo cosas distintas: exportaciones (71, la exigible) · archivos-componente (49) · filas del índice de la skill · glifos del registry (**38**, contados sobre `Icono.tsx` despojado de comentarios — L-170).)*
>
> **Y la lección de que se haya atrasado cuatro veces, que vale más que los números:** esta prosa es **derivada**, y lo derivado decae mientras el objeto no. Se re-mide al tocarla, nunca "cuando haga falta" — porque el día que hace falta es el día en que alguien ya la citó.

**S59 — HeroMarca absorbe la safe area (cura en la primitiva, pedido S59-A1):** el techo pinta su fondo (gradiente / bg.card memorial) edge-to-edge hasta el borde físico y el CONTENIDO baja por `insets.top` (react-native-safe-area-context, mismo patrón que TechoTinta del prestador) — mismo lockup, misma curva 44/26, solo el fondo crece. Prop `techo` (default true); `techo={false}` SOLO para muestras fuera de posición (galería). Las pantallas consumidoras (Hogar, bienvenida) ya NO agregan `paddingTop: insets.top` por fuera — esa era la banda blanca sobre el techo. Barra de estado: íconos claros sobre el gradiente, wiring por pantalla con useFocusEffect (ui no conoce el foco — patrón BarraTabs); gate en dispositivo del founder PENDIENTE.

**S58 — los materiales de los acabados (D-358 + D-359 + D-360, gates founder en dispositivo PENDIENTES):** fondo claro = PAPEL ALGODÓN `#FAF9F7` (palette.light0; dark y memorial intactos) · tokens `src/tokens/elevacion.ts` (`theme.elevacion.reposo/elevada` — boxShadow DOS capas de tinta cálida rgb(31,27,22) en claro; dark/memorial = contacto mínimo; regla Chanel del marco cableada en Tarjeta: con elevación muere el hairline, el borde de tinte se conserva; Hoja porta `elevada`; `TarjetaElevacion` habla `plana|reposo|elevada` con 'sm'/'md' como alias DEPRECADOS) · **+ el 29: SelectorSegmentado** (Ley 19.3 — vistas exclusivas 2-3, riel bg.overlay hundido + activo apoyado con elevacion.reposo; se desliza la superficie, la sombra viaja; memorial sin deslizamiento; sin variante por dosis) · **+ el 30: CeldaNavegacion** (Ley 19.1 — relevamiento L-144: Celda de lista no cumplía la letra; ícono b′ tipado + detalle opcional + chevron + pressed 0.99; la regla S43 de Celda queda intacta). Gate WCAG: **154 pares / 0 fallos** (S58d Ley 22: 6 pares tonales ×3 registros ×2 temas) (S58b: papel/tinta ×3 + tealDark/tinta CAÍDO 2.86 reportado; S58c: accent.control — magentaDark porta blanco 8.25 + tint control ×2). **S58c — acento del cliente (firma founder):** `palette.magentaDark #8E1F68` = registro TRABAJADOR del magenta (el puro conserva su reserva) · `accent.control` en los 3 temas (claro magentaDark / dark violetText / memorial tinta) · LEY DE GEOMETRÍA (Ley 21): lo elegido = rectángulo `radius.suave` 10, lo que informa = píldora (Insignia intacta); SelectorOpcion migró a 10 y ganó `acento` capa/control/oficio (capa=verdeVital MUERE al paso) · SliderPrecio ganó `registro="control"` · CTA sigue en tinta · bg.tinta intocado.

## Qué hay

- **Tokens v4** (`src/tokens/`): palette canonizada desde los SVG de marca (pink #FF00AF · teal #28E8DA · verdeVital #2BE86B · menta/amarillo SOLO marca; extensiones v3.1; alphas precomputadas — nada se interpola en runtime), typography (DM Sans única UI + JetBrains Mono con REGLA DE VOZ), spacing/radius/motion (v3.1), shadows (RN objects; glow SEMÁNTICO solo-dark), opacity, dosis (prestador baja / dueño alta; isotipo = identidad, fuera de contabilidad).
- **Reglas estructurales**: DOS REGISTROS (puro=gráfica, *Dark AA=texto — gate programático), gradiente firma v2 (3 stops, violeta central, texto blanco, contextos cerrados; pinkVivo #DF00A1), paridad de tints perceptual (comunidad dark .09/.21).
- **Temas** (`src/themes/`): light (DEFAULT §7.3) / dark (opt-in) / memorial (M6 automático, sin gradiente, nada rebota) + capaText + accent.active (pink puro, UN elemento activo por vista).
- **49 componentes** (`src/components/`) — los 11 de S43, cada uno con su gate en dispositivo cerrado: Boton (5 variantes, loading 150ms sin layout shift, marca degrada en memorial) · Tarjeta (tintes, interactiva 0.99, sombra jamás animada) · Campo (nada se anima al tipear, slot de mensaje reservado) · Celda (jerarquía protegida, metadataMono fuerza la voz, pressed resalta jamás escala) · Separador · Insignia (jamás Pressable, dos familias, soloPunto con a11y forzada) · Encabezado (navegacion con centrado óptico / portada con la voz humana e isotipo gradiente default) · BarraTabs (pill accent.active por opacity, wiring expo-router en JSDoc, aria-selected directo) · Hoja (bottom sheet, gestos SM, back Android DOBLE VÍA — CERRADO, verificado en dispositivo por founder post-b0fae55) · Aviso (cola de-a-uno con ref síncrono) · EstadoVacio (dignidad, sin animación de entrada). **+ los 6 de S44**: CitaEnVivo (glow semántico dark / anillo 1.5+pill claro / memorial degrada; UNO por pantalla) · Esqueleto (+EsqueletoGrupo progressbar aria-busy; estático por ley) · AvatarMascota (foto→huella digna; `especie` reservada a D-288; nació el registro capaBg y violetText subió a #AE59FF) · Cronometro (mono display tabular-nums, por diferencia contra el server, pausadoEnMs congela) · EvidenciaFoto (.Capturar cámara directa/galería en Hoja/permiso con voz humana; .Thumbnail subiendo/subida/error — la foto jamás desaparece) · MapaRecorrido (nativo+web placeholder; EXIGE dev build — tiles de Google fuera de Expo Go desde SDK 53). **+ los 7 de S45**, cada uno con su gate en dispositivo: SelectorEspecie (grid 3×2 sobre catálogo real, borde capa.identidad sin accent.active) · CampoFecha (precisión honesta exacta/aproximada/estimada = espejo del CHECK de DB; su Hoja usa HojaScroll) · SelectorAvatar (la huella es cara válida; "Por ahora no" primera clase; captura vía `capturaFoto`) · HeroMarca (gradiente firma en contexto cerrado, memorial degrada a plano) · SelectorOpcion (chips selección única 2-4 opciones) · LineaDeVida (diccionario de voz CERRADO adentro, Ley 3) · VisorFoto (lightbox solo-fades, letterbox digno). **+ primitivas/infra que NO cuentan como componente**: HojaScroll (scrollable que GANA dentro de la Hoja — patrón SM block, L-132; vive en Hoja.tsx) y capturaFoto (infra de cámara/galería compartida EvidenciaFoto/SelectorAvatar — no duplicada). **+ el 1 de S47**: FichaVacuna (revisión del carnet de vacunas — presentacional pura, estados completa/dudosa/rechazada derivados de los datos con voz humana, tap→onEditar y "Esta no es"→onDescartar; gate en dispositivo cerrado por founder, cero tokens nuevos). **+ el 1 de S51**: FichaMascotaHogar (Zona 1 del Hogar — presentacional pura, tres voces del estado con semántica visual, texto vía riel i18n del app, tap→perfil; Ley 11 con espec gateada, cero tokens nuevos). **+ el 1 de S53**: Icono (set b′ de DIRECCION_ARTE — 6 firmados: paseo/veterinaria/grooming/refugio/despensa/coach; primitiva Huella canónica en brand/; registro capa/aa/tinta; BarraTabs con estadoPorHuella §2.6). Celda enmendada en S44: metadataMono + fin CONVIVEN apilados. **SelectorOpcion enmendado en S55-B4**: `disposicion` fila/tira/grilla (tira horizontal de días y grilla de inicios del CUÁNDO tipo Teams — mismo tratamiento de selección, radiogroup intacto). **+ los 5 de S58**: SelectorSegmentado, CeldaNavegacion, SliderPrecio, Interruptor y StepperCantidad (ver banda S58 arriba). **+ el 1 de S63**: ClipSesion (el clip corto de la sesión de adiestramiento — poster sereno + tap-para-reproducir con controles nativos de expo-video, JAMÁS autoplay en ningún tema; estados cargando/error con voz honesta del namespace ui; escalera: 0 clips = no se monta; espec aprobada por el arquitecto S63, primer consumidor el parte del dueño, segundo el Durante del prestador; gate founder en dispositivo pendiente — exige build con expo-video). Gate WCAG al cierre de S63: 154 pares / 0 fallos (ClipSesion no trae pares nuevos — tokens puros). **+ el 1 de S53 que faltaba en esta lista**: BarrasSemana (la tira de 7 días de los Vitales — presentacional pura y ESTÁTICA, día sin dato = barra base, sin ejes ni tooltips). **+ el 1 de S68**: VozComision (la voz del neto bajo un precio del taller, con el fee VIVO desde `fee_configs` — pagó D-412). **+ los 3 de S71**: Texto (las 5 variantes de la regla de voz — absorbió 10 `TituloBloque` + 4 `VozSecundaria` byte-idénticas; SIN prop `style`, deliberado) · FilaDato (etiqueta sobre valor, sin interacción — "si tocarlo no hace nada, es FilaDato") · PieRevelar (el control canónico de la Ley 19.6, con el NÚMERO en la etiqueta). **+ el 1 de S74**: LogoNegocio (el logo del negocio CONTENIDO, jamás recortado — la trampa del logo `MODELO_PRESENCIA` §2: caja radius.suave + bg.overlay + aire, contain; monograma de iniciales como fallback honesto, jamás huella; nace por la E2 de la vara A→B, Ley 11; gate founder en dispositivo PENDIENTE — con su primera pantalla consumidora, la ventana de equipo de B). **+ el 1 de S80**: FilaCita (una tarjeta = UNA cita en la jornada del prestador — el primer componente de DOMINIO; el canto vive ADENTRO, como borde del elemento que porta el radio, y la pantalla no elige color ni posición: EL CANTO DICE CATEGORÍA, EL GLIFO DICE SERVICIO, Ley 10). **+ los 3 de S81**: Entrada (§5, la aparición de la casa — escalón 45→120 FIRMADO; absorbió los `FadeInDown` artesanales) · CantoMarca · EvitaTeclado (PROMOVIDO — pagó D-498: la casa tiene UNO). **+ el 1 de S82**: PieReserva (el pie fijo de una reserva, segundo componente de DOMINIO — sube porque dos de sus cuatro pantallas lo tenían copiado a mano y la copia había perdido el PRECIO entero; `total = null` es legal y no dibuja vacío, el "desde" es del DATO y no del oficio, y el piso de la safe area lo pone la pieza. Su regla: `verify:diseno` R24). **+ los 2 de S84**: MapaZona (la ZONA aproximada — círculo sin pin, no interactivo; variante `.web`) · FichaPrestador (la vitrina del negocio, UNA sola vez: cliente + espejo del prestador). **+ los 2 de S85, los dos PROMOVIDOS del cliente por la Regla de las Piezas** (apareció el segundo consumidor, la portada del prestador): **FiltroPills · FiltroMascotas** (los chips con PATA — adoptaron la primitiva canónica `MarcaEleccion` en vez de su clon, que de paso curó una doble lectura de a11y) y **SelectorDia** (la rueda D3, con su física FIRMADA que viajó verbatim: el imán, y escala/opacidad/acento en un WORKLET porque durante el arrastre el estado de React llega tarde). **+ la tercera familia de `Insignia`** (`distincion`, S85-B16): no suma archivo pero sí vocabulario — la cohorte no es un `estado` (transitorio) ni una `capa` (dominio), y su `superficie="muro"` nació de medir que el tinte de capa sobre el muro da **1.03 en claro**. **Gate WCAG vigente: 178 pares / 0 fallos** (corrido en vivo S85: las promovidas resuelven de slots y no traen pares nuevos).
- **Marca**: `src/brand/Isotipo.tsx` (path oficial; tinta/blanco/gradiente) + `assets/brand/` (8 logos + 3 isotipos).
- **Galería**: `src/gallery/TokenGallery.tsx` — **NO es un tab visible en ninguna app** (prosa "tab Tokens en ambos apps" corregida en la transposición S73, L-161/L-141): en el **cliente** el tab "Tokens" NUNCA existió (`(tabs)/_layout.tsx` = Hogar·Explorar·Cuenta; la galería vive en `src/app/gallery.tsx`, alcanzable solo por URL) y en el **prestador** es `Tabs.Screen` oculta "viva por URL" (`/gallery`). La herramienta de la sesión jamás se presume superficie del founder (L-161). Incluye la pantalla raíz completa del prestador (el template que S44 volvió pantalla real) + secciones de los componentes de S44 y S45 con los 3 temas visibles (PanelTema).

## Gates

- WCAG: `pnpm verify:contrast` — **178 pares (re-medido S72, corrido en vivo), 0 fallos** (regla de peor punto del gradiente con exención de cola verificada; tab inactivo exento por espec con medición informativa).
- Browser: `node scripts/verify-gallery.mjs` (dev servers arriba).
- Dispositivo: OBLIGATORIO por componente (Ley 9) — Expo Go 57 APK + túnel (CLAUDE.md raíz). S43 corrió solo Android → D-285 (iOS antes del primer TestFlight).

## La ley

La skill **`epetplace-design-system`** (`.claude/skills/`) es obligatoria en toda tarea de UI: 13 leyes, ejemplos correcto/incorrecto reales, índice de exports. Componente faltante = Ley 11 (espec al founder → nace acá con el método completo → entra al índice). Peers nativos con rango del app, jamás `"*"`.

Skills que pesan acá: Software Mansion (código del motion), emil-design-eng (criterio), impeccable (vara visual). Tabla de autoridad: CLAUDE.md raíz.
