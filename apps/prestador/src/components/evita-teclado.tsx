/**
 * S81 — EvitaTeclado SUBIÓ a packages/ui (D-498: la casa tiene UNA).
 * Este archivo queda como FRONTERA de compatibilidad (patrón
 * MapaRecorrido.filtro): mismo path, mismo nombre, UNA implementación.
 * La letra completa vive con el código en ui.
 */
export { EvitaTeclado } from '@epetplace/ui';
