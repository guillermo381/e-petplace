# REVERSA de `20260815130000` — los cinco productores

Los cuatro `.sql` de al lado son las **definiciones VIVAS capturadas ANTES de
empalmar** (`pg_get_functiondef`), no reconstrucciones:

| archivo | función |
|---|---|
| `cpp` | `confirmar_pago_pedido` |
| `orv` | `otorgar_rol_vendedor` |
| `ccs` | `cancelar_cita_suelta` |
| `rdp` | `revisar_documento_prestador` |

**Y el ⑤, que no tiene par:**
```sql
DROP FUNCTION IF EXISTS public.barrer_solicitudes_expiradas();
SELECT cron.alter_job(
  job_id  => (SELECT jobid FROM cron.job WHERE jobname='expirar-citas-pendientes'),
  command => 'SELECT expirar_citas_pendientes();');
```

## 🔴 QUÉ NO DESHACE

- **Las intenciones ya encoladas QUEDAN** — son avisos legítimos de hechos que
  ocurrieron. Borrarlos sería quitarle a un vendedor la noticia de su venta.
- **Las 8 solicitudes materializadas como `expirada` NO vuelven a
  `pendiente`.** Y no deben: *ya estaban expiradas por el reloj; lo único que
  cambió es que ahora la fila lo dice.* Revertir eso sería devolverlas a un
  estado que el lector igual iba a contradecir.
- ⚠️ **Revertir el ⑤ devuelve la expiración a PEREZOSA** y con ella el hueco
  que la firma (a) vino a cerrar: el vet deja de enterarse sin mirar.
