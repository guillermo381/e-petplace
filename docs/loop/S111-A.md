# S111-A · EL PARTE — la fuente del handoff

> **Pista A de S111**: motor + conducción. Escritora única de DB y `docs/`.
> **Se lee ANTES que cualquier resumen.** Lo que vive sólo en la conversación
> muere con ella — este archivo existe para que nada de abajo dependa de eso.
>
> **Estado:** `main` `9b15bc7b`, cinco ramas dentro, árbol limpio, 5 typechecks
> en 0, `verify:diseno` verde con 62 reglas. **Los diez hallazgos cerrados y las tres
> piezas de la ronda del toque también. Todo por OTA (`L-467`).**

---

## ① 🔴 TRES MEDICIONES QUE VIVÍAN SÓLO EN LA CONVERSACIÓN

*Son la razón por la que este parte se escribió antes de compactar.*

### 1.1 · La bitácora de conductas YA EXISTE y sirve — **corrige una premisa de C**

C pidió *«el vocabulario de guardería como DATO y su escritor»*, y justificó **no
reusar el existente** diciendo que *«el vocabulario del adiestramiento describe
avances de un currículum»*. **Su disciplina fue correcta y su premisa no.**

**Medido contra el objeto:**

- `cat_conductas_bitacora` es **la bitácora UNIVERSAL de S91**, no la del
  adiestramiento. **25 conductas**, y son exactamente *«cómo se portó hoy»*:
  `durmio_tranquilo` · `convivio_bien` · `comio_normal` · `se_escondio` ·
  `miedo_ruidos` · `hizo_fuera_de_lugar` · `destrozo_objetos` ·
  `lloro_al_quedarse_solo` · `inquieto_en_casa` · `mas_carinoso` …
- **El currículum del adiestramiento vive en OTRAS DOS tablas**:
  `cat_objetivos_adiestramiento` y `cat_curriculum_adiestramiento`.
- El catálogo ya trae **`especies_aplicables`** y **`sujetos_aplicables`** ⇒ el
  recorte perro/gato de guardería **es un filtro de datos, no una decisión**.
- `registrar_bitacora_familia(p_mascota_id, p_texto, p_chips)` **no exige
  contexto de servicio** (ese guard murió en S91).

> ### ⇒ NO hay que inventar un vocabulario. **El que falta es el ESCRITOR del prestador**, y eso es técnico, no producto.

**Lo que sí cambia respecto de la bitácora de la familia:** el escritor del
prestador tiene que dejar **`procedencia = 'declarado_por_prestador'`** y
colgar del acto, no de la familia. *El vocabulario sirve; el escritor no.*

**⇒ ESTO NO SE ESTACIONA.** Es construcción de A, y está en la cola (§③).

### 1.2 · No existe wrapper de `obtener_tramo_vivo_de_mi_mascota`

Medido con control positivo (`marcar_a_bordo_guarderia` **sí** tiene wrapper):
en `packages/api/src/wrappers/` hay `registrarPuntoVivo` y `obtenerPuntoVivo`,
y **el del tramo NO**. Por eso C declaró un espejo en su pantalla en vez de
pedir el tramo al motor.

🔴 **Es la CUARTA vez en la sesión que dejo motor sin puerta.** Las tres
anteriores: los wrappers de la mensajería · el lector del hilo · el aviso del
durante.

### 1.3 · Las franjas existen y tienen datos

`guarderia_franjas` (`tipo · desde · hasta · dias_semana · zona_horaria`) está
**poblada**: recogida `07:00–09:00`, devolución `16:30–18:30`.
**`EstadiaDelDia` no proyecta ninguna** — sólo `aBordoEn`, `llegadaEn`,
`entregadaEn`, que son horas de **lo que ya pasó**. ⇒ el orden del día de ⑨ no
se puede construir hoy.

---

## ② EL GATE DEL FOUNDER — 🟢 **LOS DIEZ CERRADOS**

| # | qué | estado |
|---|---|---|
| ① | la foto que se subía bien y desaparecía | ✅ **cerrado entre dos pistas** — ver `L-466` |
| ②③④ | la obligatoriedad invertida | ✅ C |
| ⑤ | el carnet en devolución | ✅ **CERRADO, y eran TRES mitades.** A (motor) · B (la pieza) · C (la segunda puerta) |
| ⑥ | las no recogidas en el retorno | ✅ C, **derivando de la máquina** en vez de enumerar estados |
| ⑦ | el en vivo | ✅ pantalla · **espejo hasta que exista el wrapper del tramo** (§1.2) |
| ⑧ | el durante | ✅ media multi-destino · **chips: ver §1.1** · clip frenado (B) |
| ⑨ | el orden del día | ✅ reordenable · **falta la franja** (§1.3) |
| ⑩ | los avisos del durante | 🔴→✅ **lo declaré cerrado y NO lo estaba.** Ver §②ter |

---

## ②bis · ☠️ LA SEGUNDA PUERTA DE ⑤ — cerrada, y su historia se conserva

**Lo levantó B midiendo a su propio consumidor, y lo verifiqué yo contra el
archivo antes de escribirlo acá.**

`apps/cliente/src/app/guarderia/[estadiaId].tsx` compone el ítem del checklist
en la **línea 421** (`clave: 'carnet'`) **sin condicionar por dirección** — el
único `direccion` de ese bloque (línea **415**) es **la prop que se le pasa a la
pieza, no una condición**.

> ### ⇒ Ese lector **nunca produce un checklist vacío**, así que la cura de B no lo alcanza: en devolución **va a seguir dibujando la casilla del carnet**, que es exactamente lo que el founder rechazó.

✅ **CURADA por C** — el ítem quedó dentro de un spread condicional por
`acta.direccion === 'recogida'` (línea 435), **verificado por A contra el
archivo**, no contra el reporte.

🔴 **Y por qué esto retiene los APK:** *el modo de falla es silencioso.* No
rompe nada, **dibuja de más**, y se ve como si funcionara. **Un APK cortado con
⑤ a medias se lee como ⑤ cerrado** — y el founder lo encuentra en el aparato,
que es donde sale caro.

*Es la ley de S107 otra vez: **el censo casi siempre encuentra una segunda
puerta al mismo defecto**. Curar el síntoma reportado y no censar la clase es
media cura — y acá la otra mitad la encontró B midiendo a su propio consumidor,
no a su pieza.*

---

## ②ter · ☠️ ⑩ NO ESTABA CERRADO — corrección a la vista

**La firma del founder pidió el quinto acto (`retorno`) y, al volver, se midió
lo que el cinturón de ⑩ nunca había mirado.**

🔴 **Los cuatro avisos que declaré cerrados salían GENÉRICOS.**
`_voz_notificacion` no tenía rama para ninguno de los cuatro: caían en su `ELSE`
y devolvían `{}`. **Medido con control positivo** (`guarderia_media_resumen`
devuelve título y mensaje) ⇒ el vacío era del sujeto, no del instrumento.

Y **no salían mudos**: el despachador tiene fallback
—`'Tienes una novedad en e-PetPlace'`— así que **los cinco actos del día habrían
dicho lo mismo**, y el que cobra el día lo mismo que «va en camino».
*Un fallback convierte «no hay texto» en «hay un texto plausible», y ahí muere
la posibilidad de notarlo.* ⇒ **`L-468`**.

### Lo curado (migración `20260907640000`, cinturón verde con su rojo primero)

- **Las CINCO voces**, es/en, humanas y cortas, sin género (se usa el nombre o
  una forma neutra). El de no-recogida dice **la firma ② y nada más**: *«El día
  se cobró y no se repone»* — y el cinturón **censa lo prohibido**: aborta si el
  texto nombra mora, protocolo, refugio, abandono, plazo o retiro.
- **`guarderia_retorno`** entra al CHECK, al catálogo y al acto.
- **La `ruta` del toque** viaja en `datos` — mi mitad del deep link.

### Lo que se midió y NO había que tocar

- **Los dos canales ya se cumplían.** `in_app` es EL PISO, así que la campana
  siempre está y el «UNA sola entrega» de §7 es del **transporte**; `push` gana
  por orden con transporte vivo y `relacional`/`operacion` tienen
  `default_habilitada = true`. *La firma se cumple por construcción.*
- **El techo no muerde**: 20 cada 24 h contra cinco actos.
- ⚠️ Un usuario tiene `operacion/push = false` explícito. **Se respeta.**

### 🔴 Y un hallazgo que NO es de guardería: la push no abre ninguna pantalla

**Cero listeners de toque en el repo entero** (control positivo y negativo).
`despachar-push` manda el destino y **nadie lo lee** — `L-460`. Pedido
autocontenido a C en `docs/loop/buzon/S111-A-para-C-el-toque-de-la-push.md`,
con **dos caminos y mi voto**. No bloquea el gate: los avisos ya llegan bien.

### Un segundo error propio, declarado

El brazo de `L-140` de mi cinturón usó **`LIKE` sobre `proacl`** y tiró un
WARNING falso — **el error ② de S92, escrito en el canon, repetido por mí**.
Medido con `has_function_privilege`: las tres funciones dan `anon = false`.

---

## ③ LA COLA — 🟢 VACÍA. Las tres piezas cerradas.

> Las tres que quedaban eran mías y las tres están en `main`, cada una con su
> contrato publicado en el mismo acto.

**① El wrapper del tramo vivo** — `obtenerTramoVivoDeMiMascota`. Era la
**séptima cobranza** de *el contrato de una pieza de motor incluye su wrapper*,
y la cuarta de esta sesión mía. Su ausencia obligaba a C a mantener un **espejo**
de la lógica del servidor, *y un espejo diverge en silencio*. Cero migración: la
RPC ya existía con sus grants.

**② La bitácora del prestador** — `registrarBitacoraGuarderia`. **Sin vocabulario
nuevo y sin duplicarlo**: `cat_conductas_bitacora` es la bitácora universal de
S91 y sus 25 conductas ya son *«cómo se portó hoy»*. Reusa la tabla porque el
puente de chips tiene FK a ella — *una tabla paralela habría duplicado justo lo
que no hay que duplicar*. **La idempotencia sale de la FORMA**: una estadía es
un día, hay una fila por estadía, y la PK del puente vuelve el duplicado
**inexpresable**. Probado con **10 brazos, 5 rojos primero**, `ROLLBACK`,
residuo 0 — y el que vale es `vomito` rebotando sobre **Pepe, que es un ave**.

**③ La franja como orden base del día.** El lector ordenaba **alfabético**, y el
`aplicarOrden` de C dice *«lo nuevo cae por su ORDEN NATURAL»* ⇒ el orden natural
era el alfabético. Las franjas **existían y estaban pobladas**: el dato estaba y
nadie lo leía. Ahora el orden sale del **dato**, y los terminales caen al final
**por su NULL**, sin una rama que lo diga.

### 🟢 EL LOTE SALIÓ POR OTA — el discriminador dio los tres en NO

| app | group | runtime |
|---|---|---|
| **cliente** | `7890deea-76b5-4b42-8a41-2aefed56fd8b` | 1.0.7 · `preview` |
| **prestador** | `c492d2df-dae6-4326-af24-af910f466e65` | 1.0.7 · `preview` |

Ancla `3a5d244c`, árbol limpio, `verify-ota` **verde en las dos** con su rojo
producido primero.

🔴 **Y un límite del instrumento, medido y declarado:** el
`expo-updates fingerprint:generate` **local NO es comparable** con el que EAS
reporta en este monorepo pnpm (`L-134`). Compararlos daría un falso «cambió
algo». El veredicto salió del **diff**, que es evidencia directa: cero
dependencia nativa nueva (`expo-camera` ya estaba en prestador y `packages/ui`
lo inyecta **por slot** para no romper el cliente), diff **vacío** en app config
/ plugins / permisos / nativo, y runtime y canal sin cambio.


---

## ④ COMPROMISOS ABIERTOS

- **A → C:** el escritor de chips, la franja, el wrapper del tramo. Y **avisarle
  la corrección de §1.1**, que le ahorra estacionar algo que no hace falta.
- **A → B:** nada pendiente. **B cerró su mitad de ⑤** y además **levantó la
  segunda puerta**, que es lo que hoy retiene los APK. Tiene **una ficha sin
  número** esperando mi ronda (el semáforo que no puede servir a adopción,
  bloqueado **por tipo** — caso limpio de `D-976`; ya está depositada como
  `D-995`) y **un ítem de estacionamiento** (la salud del adoptable — ya está
  en §5.2).
- **C → A:** nada pendiente. **Los diez cerrados**, sha `3d31f9b1`.
- **La veda:** entera. **Cero publish** hasta la pasada completa y su «autorizo».

---

## ⑤ LO QUE HICE Y NO SE VE EN EL DIFF

- **Toqué `apps/prestador/src/lib/guarderia-cableado.ts`** (territorio de C)
  porque mi cambio de contrato del carnet rompió su compilación. Partí el
  payload; está declarado en un comentario **y C ya lo verificó y lo aceptó**.
- **Regeneré `router.d.ts` dos veces** (primario y worktree). Ver `L-465`: es
  generado, no viaja en el repo, **y lo regenera quien mergea**.
- **Las 5 estadías movidas, los 2 tramos, las 2 actas y las 3 fotos de la pasada
  del founder NO se tocaron.** Son evidencia del gate, no residuo.

---

## ⑥ LO CONSTRUIDO Y NO EJERCIDO

- **Nada de esta sesión se vio en un aparato**, salvo lo que el founder alcanzó
  a caminar (que se detuvo en el tramo de devolución).
- **El traspaso de adopción nunca corrió de verdad** — no puede, hasta que
  exista el acta. Sólo dentro de un arnés que la cargó y la deshizo.
- **La vidriera anónima nunca se abrió en un navegador sin sesión.**
- **Los cuatro avisos del durante** tienen su intención probada en el cinturón y
  **ninguno llegó todavía a un teléfono**.
