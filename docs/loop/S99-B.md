# S99-B · PARTE — Dirección de Diseño · `packages/ui` · tokens · los jueces

**Rama:** `pista/s99-b` · **worktree:** `e-petplace-s99-b` (regla 85, creado al abrir)
**Fecha:** 15-ago-2026 · **Estado:** L0 CERRADO · todo en el canon

---

> ⚠️ **EL TELÉFONO NO ES MÍO** (se repite acá arriba porque es la
> limitación que ordena todo lo de abajo). · ⏪ La puerta original decía
> que `DIRECCION_DISENO_S99.md` no existía y que B3 esperaba su letra: la
> letra **aterrizó a mitad de tanda** y N11 se construyó contra ella.
> *Lo que no se inventó mientras faltaba es lo que después se pudo
> construir.*

# 🔴 HANDOFF — LO VIVO AL CORTE (se lee primero; lo de abajo es el detalle)

**Medido al cortar, no de memoria:** rama en origin **por SHA**
(`bd239bfe` local = `bd239bfe` remoto — *`ls-remote` da exit 0 con salida
vacía: el discriminador es el sha, no el éxito*) · árbol en **cero**
(medido sobre el CONTENIDO, jamás con `&&`) · `verify:diseno` **VERDE 40
auto-pruebas** · **WCAG 352/0** · **R17** `exportaciones=91 · en-galería=82
· exentas=9 · pendientes=0` · tsc de los cuatro paquetes **0**.
**Fuera de `main`: 2 commits** (`e5183938` · `bd239bfe`) — A mergea al
cerrar cada tanda suya, sin esperar aviso.

## 🚨 EL CANAL ENTRE SESIONES SIGUE CAÍDO
**Todo lo pendiente con A, C o D viaja POR EL REPO.** *Si no está en un
archivo, no llegó — no asumas que un acuerdo conversado existe.*

## ⭐ LO QUE ME ESPERA, EN ORDEN, CON SU PRÓXIMO PASO EJECUTABLE

| # | qué | próximo paso | espera |
|---|---|---|---|
| **①** | **EL CENSO DE CONSUMIDORES DE `Encabezado`** | **tanda propia, ratificada por mesa.** La pieza EXISTE y su registro `portada` ya monta el lockup; **lo que falta es que EXIJA el nombre de pantalla**. Paso 1: censar quién la monta en las DOS apps (`grep -rn "<Encabezado"`), clasificar `portada` vs `navegacion`, y recién ahí decidir si el nombre entra como prop requerida o como registro nuevo. **No se toca antes del censo**: la montan las dos apps enteras | — |
| **②** | **🔴 LA RECETA DE LA VOZ DE LA PROMESA — NUEVA, nadie la escribió** | firma: **el cupo NO filtra, determina la FECHA** — un vendedor lleno compite igual y lo que cambia es el corte que trae. **Se dice ANTES de comprar**: *decirlo después es una disculpa, decirlo antes es información*. Verbatim: *«no tenemos disponibilidad para entrega mañana en la mañana, te lo podríamos entregar en la tarde. ¿Querés continuar?»* ⇒ la voz **dice qué NO y qué SÍ, jamás solo el rechazo**, y **TERMINA EN PREGUNTA**: la familia decide. **Motor listo**: `calcular_promesa_despensa` devuelve `saltos_por_cupo`, que es la señal exacta. **Es voz y superficie, cero motor** | — |
| **③** | **La cadena de selección — cambia QUÉ muestra la vitrina** | firmada: **varias ofertas del mismo producto EXISTEN, UNA se muestra**. Puertas: **stock y zona**. Orden: **calificación · antigüedad como desempate · ventana de arranque** para el vendedor nuevo (10 pedidos o 30 días, con la calificación promedio del ecosistema). **Toca mis recetas de vitrina**: la ficha y la grilla asumen «una oferta»; hay que declarar qué pasa cuando la elegida cambia | — |
| **④** | **Dos ojos del founder pendientes** | **la barra** (veredicto aplicado: sin huella · disco en tinta · 18 px adentro · viaje por transform) y **el pin** (silueta D restaurada + sombra que toca). Los dos montados en la galería, **primeros por el corolario firmado** | **founder** |
| **⑤** | **Lotes de C que consumen recetas mías** | Stock lista⇄íconos (con la ventana medida en aparato, **el número es suyo**) · la fila del repartidor **firmada: nombre · vehículo · placa** · los tres hallazgos de la caminata · la gramática de configuración | **C** |
| **⑥** | **`R47` la otra mitad** (un sólido por superficie) | sigue sin escribirse **con su razón intacta**: el default de `Boton` es sólido, así que los 48 sin `variante` no aparecen en ningún grep — un contador ingenuo **bendice como preexistente lo que nunca contó**. Exige parseo de JSX multilínea: **tanda propia, y se pide** | **mesa** |

## 📮 COMPROMISOS, POR PISTA

- **A** — le debo nada; **me debe** el merge de mis 2 commits (rutina suya)
  y ya me entregó las cardinalidades. **Pedido vivo:** depositar la ley
  del verificador de SHA (repro y forma corregida en este parte).
- **C** — le entregué **8 recetas**; me entregó el hallazgo del comodín
  (que enmendó mi umbral) y los tres de la caminata. **Le debo:** nada
  bloqueante.
- **D** — le debo **el portador del fundido de modo** (`Entrada` no
  sirve: es entrada escalonada CON desplazamiento) y **extraer el
  carrusel a sangre 4:3** de `FichaPrestador`. **Me debe:** montar las
  dos ventanas para poder gatear la transición direccional — `cruce.ts`
  ya está listo y exportado, **él lo consume**.

## EL UNIVERSO DE LO NO HECHO (para que nadie lo lea como cubierto)

- **La enmienda de `Encabezado`** — declarada, NO ejecutada (①).
- **La transición direccional** — la pieza (`cruce.ts`) existe; **la
  animación de entrada la monta D**, y no se puede gatear hasta que las
  dos ventanas estén montadas.
- **`R47` mitad 2** (⑥) · **la barrida de la partición a 7 reglas más**
  (R12 · R24 · R36 20 · R37 3 · R38 6 · R39 6 · R40 1) — **es medición,
  no edición, y no la hago sin que la pidan**.
- **Háptica** — candidata con gate; **no prototipada a propósito**: su
  único gate es el dedo.
- **N16.2 «el spinner muere»** — medido en 0; su ratchet **no se
  escribió** porque su net honesto exentaría `packages/ui` (3
  `ActivityIndicator` legítimos) **y esa exención se decidiría sola**.

## ⚖️ LAS CUATRO QUE LA MESA ELEVÓ A LEY EN ESTA TANDA (registro)

1. **UNA CORRECCIÓN PUEDE ROMPER LO QUE YA ESTABA FIRMADO SI NO DISTINGUE
   QUÉ SE CORRIGE DE QUÉ SE CONSERVA** — *toda corrección nombra
   explícitamente qué NO se toca, y quien la ejecuta lee esa mitad
   primero.* Nació de mi sobre-corrección del pin. **Corolario: se mide
   que SE APOYE y se mide que SE RECONOZCA — dos pruebas.**
2. *«Una animación de dos capas donde la crítica cuelga de la frágil no
   es arriesgada: ya falló y todavía no lo sabés.»*
3. **El ojo midió una geometría sin verla** — el founder describió exacto
   el «casi afuera» y eran **6 px de 52, el 11 %**.
4. *«Las dos pantallas no lo hicieron mal — hicieron algo que la pieza
   permitía»* (L-244 en su forma de API opcional).

## ✅ RESUELTO — A MERGEÓ (y la lección se conserva entera abajo)

**Re-medido después del aviso:** `git rev-list --count origin/main..pista/s99-b` = **1** — solo el commit vivo de esta tanda (`R46`), y **los seis anteriores están en `main`** (verificado por `merge-base --is-ancestor` sobre tres de ellos, no por confianza). *La mesa lo confirmó: «tu tanda no está en main y A ya lo sabe».*

**La corrección de abajo NO se borra**: describe cómo un handoff mío afirmó un estado que la medición desmentía, y esa es la parte que sirve. *Lo que caducó es el número, no el modo de falla.*

## 🔴 SEIS COMMITS MÍOS ESTABAN EN `origin` Y **NO** EN `origin/main` — Y ESTA LÍNEA DECÍA LO CONTRARIO

**⏪ Corrección de mi propio handoff, con su literal viejo a la vista**
—*«**ya en `origin/main`**, confirmado además **por contenido** en tres
archivos»*— **y era falso.** Medido ahora con
`git rev-list --count origin/main..pista/s99-b` = **6**:

```
a9f7486e  las dos recetas de forma (ficha del repartidor + contador)
bc4dff59  las tres adjudicaciones (clase de mapa · hueco nombrado · ley pedida)
15fc09b8  la tríada se cobró una CUARTA vez
a0e6800a  moto v2 + la gramática de configuración
4b3ca697  merge de origin/main a la pista
31b0b890  el HANDOFF mismo
```

**Es L-217 cobrándose sobre quien la estaba citando** (*«todo en origin» y
«todo en el canon» son dos afirmaciones distintas*). Mi verificación por
contenido comprobó archivos que **A ya había mergeado antes**, no mis
commits nuevos — *un verde por la razón equivocada, en la misma familia
que las cuatro muestras de la tríada.*

**⚠️ EL MERGE ES ACTO DE A, no mío** (ella conduce: el log de `main`
muestra `merge(s99-a): PISTA D — …`). **Y el canal está caído**, así que
esto vive acá porque no hay otro camino: **si A no mergea `pista/s99-b`,
la Dirección de Diseño entera de S99 —las dos leyes nuevas del juez, la
clase de mapa de `DIRECCION_ARTE` §6ter, las cuatro recetas y el contador
de la puerta— se queda fuera del canon** aunque los seis semáforos de la
pista estén en verde. *Es exactamente el modo de falla que S92 tuvo que
rescatar a mano.*

## 🚨 EL CANAL ENTRE SESIONES ESTÁ CAÍDO

**`SendMessage` dejó de estar disponible a mitad de la última tanda.**
Consecuencia operativa, y no es menor: **todo lo que quede pendiente con
A, C o D tiene que viajar POR EL REPO.** El último mensaje que intenté
—la condición de cumplimiento de R45 para D— **no salió**, y por eso vive
hoy en el mensaje de error de la propia regla y en §R45 de este parte.
*Quien retome: no asuma que un acuerdo conversado llegó; si no está en un
archivo, no llegó.*

## 📮 PEDIDO A A — DEPOSITAR UNA LEY (el canal está caído, por eso viaja acá)

**Orden de mesa: pedirle a A que deposite la ley de mi cuarta muestra.
`SendMessage` no está disponible**, así que el pedido vive en el repo con
todo lo que A necesita para depositarla sin volver a preguntarme.

> ## LEY · **«UN VERIFICADOR DE SHA PASA PERFECTO CUANDO EL TRABAJO NI EMPEZÓ.»**
>
> **Hermana exacta de L-239** (*`ls-remote` da éxito con salida vacía*), y
> **cierra la tríada por el peor lado: se cobró en el paso que existe
> para atrapar a los otros.**
>
> **EL REPRO, literal:**
> ```bash
> git commit -m "…" -- <rutas>        # FALLA: pathspec sobre archivo sin trackear
> git status --porcelain && echo "árbol en cero"   # imprime 3 sucios Y ADEMÁS dice "árbol en cero"
> [ "$(git rev-parse HEAD)" = "$(git ls-remote …)" ] && echo "✅ SHA IDÉNTICO"   # ✅ — porque HEAD no se movió
> ```
> **Las tres cosas fallaron y las tres reportaron bien:** `status`
> devuelve 0 aunque imprima suciedad (el `&&` corre igual) · el commit
> había fallado por la trampa que la **regla 84** documenta (`git add -N`
> antes del pathspec) · y **la comparación de SHA dio idéntico porque el
> trabajo no ocurrió.**
>
> **LA FORMA CORREGIDA:**
> ```bash
> sucio=$(git status --porcelain); [ -z "$sucio" ] || { echo "🔴"; echo "$sucio"; }
> ```
> Capturar la salida y decidir sobre su **CONTENIDO** — jamás encadenar
> con `&&` sobre un comando que informa por stdout.
>
> **EL COROLARIO EXIGIBLE:** *quien verifica tiene que poder distinguir
> **«no cambió porque está bien»** de **«no cambió porque no pasó
> nada»**.* Un verificador que no discrimina esos dos casos no verifica:
> tranquiliza.
>
> **Y EL PATRÓN QUE LA SESIÓN JUNTÓ — cuatro muestras de «el instrumento
> respondió sobre otra cosa»:** el Metro huérfano de D · mi rama que no
> corría (`sinCaja=true` era el default) · mi net que subcontaba dos veces
> · **y ésta, en el verificador.** ⇒ **el instrumento que audita también
> necesita ser auditado.**

## ⚠️ NOTA DE MÉTODO — la tríada se cobró una CUARTA vez, y ahora en mi verificación

*«El instrumento respondió sobre otra cosa»* ya llevaba tres muestras
(el Metro huérfano de D · mi rama que no corre · mi net que subcontaba).
**La cuarta es peor porque estaba en el paso que existe para atrapar a
las otras:**

Al commitear los dos frentes encadené `git status --porcelain && echo
"árbol en cero"`. **`status` devuelve 0 aunque imprima archivos sucios**,
así que el `&&` corrió igual: **mi propio chequeo dijo «árbol en cero»
con tres archivos modificados y uno sin trackear.** Y el commit **había
fallado** (pathspec sobre archivo sin trackear — la trampa que la regla
84 documenta: `git add -N` primero).

🔴 **Y el detalle que lo vuelve la peor de las cuatro: la comparación de
SHA dio ✅ IDÉNTICO — porque el commit nunca ocurrió y `HEAD` no se
movió.** *Un verificador de sha pasa perfecto cuando el trabajo ni
empezó.* Es la hermana exacta de **L-239** (`ls-remote` da éxito con
salida vacía): **el éxito del comando no es la respuesta; hay que mirar
lo que devolvió.**

⇒ **La forma que quedó**, y es la que uso desde acá: capturar la salida
en una variable y decidir sobre su CONTENIDO —`sucio=$(git status
--porcelain); [ -z "$sucio" ]`—, jamás encadenar con `&&` sobre un
comando que informa por stdout.

## 🔴 EL ORDEN DE LA COLA LO FIJÓ LA MESA (S99) — por lo que DESTRABA

**① LA ESCALA DE BOTONES** (✅ servida) — *«no es la más grande: es la que
aparece en TODAS las pantallas nuevas»*. **② LA GRAMÁTICA DE
CONFIGURACIÓN + EL HOY VACÍO + §5bis, LOS TRES JUNTOS** — *es el mismo
vendedor el mismo día, y separarlos fabrica tres respuestas donde hay
una*. **③ La transición direccional, cuando D monte.**

⚠️ **Del ② me falta el literal de «el HOY vacío»** — lo trato como el
estado vacío del HOY del vendedor (N9 · hermano de §5bis), y si la mesa
quiso otra cosa, se corrige antes de escribir. *Lo digo en vez de
suponerlo: el ② se entrega UNA vez, y entregarlo contra la pregunta
equivocada cuesta la tanda entera.*

## LA COLA VIVA — cada ítem con su PRÓXIMO PASO EJECUTABLE

| # | ítem | próximo paso ejecutable | espera |
|---|---|---|---|
| ① | **🔴 `moto` v2 — el gate FALLÓ y el brief estaba mal** | mostrar la galería, sección **«Hoja de contacto · `moto` v2»**: tres candidatas de **MASA** (D con caja · E moto sola · F masa mínima), **dentro del anillo del pin**, a **21 px sobre cuatro tonos de mapa** — jamás lienzo blanco. **Mi voto: D.** Y va con **el choque declarado** (Ley 12 pide trazo; un pin de silueta la contradice) y **mi voto por la salida (a)** | **ojo del founder** |
| ⑴ | **✅ ORDEN ① — LA ESCALA DE BOTONES, servida** | `docs/laminas/2026-08-15-s99b-RECETA-ESCALA-DE-BOTONES.md`. **Censo: 501 botones.** El dato estructural: **el default de `Boton` es `primario`, o sea SÓLIDO** ⇒ 48 botones son sólidos **sin que nadie lo decidiera**, y los sólidos reales son **211 (42 %)**. Y el síntoma: **más `secundario` (184) que `primario` (146)** ⇒ *la escala se usa como PALETA, no como escalera.* **☠️ `lg` tiene 3 usos en 501: gana su caso escrito o muere.** ⚠️ `R47` NO se escribió, con su razón: un contador ingenuo mide de MENOS (el default no aparece en ningún grep de `variante=`) — sería R44 otra vez | **ojo del founder** |
| ⑴b | **✅ D-484 PAGADA por la receta** (abierta desde S73) | su disparo sonó con el pie fijo de `Hoja`. **Resolución: `primario` + `secundario`, jamás `primario` + `ghost`** — el `secundario` tiene contorno, así que cumple 22c (rechazar tiene consecuencias y no es un label), **y 19.7 no lo alcanza: su letra prohíbe el contorno transparente *como acción de FILA*, y el pie de una Hoja no es una fila.** *La prohibición nunca llegó hasta acá; faltaba leerla entera* | **gate donde vive** |
| ⑴c | **✅ LA ESCALA, APLICADA A LA PIEZA** | la ley vive en el header de `Boton` con su censo. **☠️ `compacto` JUBILADA CON LÁPIDA**: es el contorno transparente que 19.7 mató, **y la casa lo tenía escrito en ESTE MISMO archivo** —el bloque de `acento` ya la declaraba muerta— *y siguió viva con 39 usos: una ley escrita en un comentario y desobedecida cuarenta líneas más abajo*. Medido qué era: «Ver completo», «Ver carnet», los «agregar» del alta — **las tres NAVEGAN**. **No se borra hoy** (39 sitios de C y D; sacarla del tipo les rompe el typecheck) ⇒ **congelada por `R47`, solo-baja, muere en 0** (precedente `precio_plan` S79) | **lote de C, al tocar cada pantalla** |
| ⑴d | **✅ `R47` — la MITAD honesta del ratchet** | congela `compacto` en 39. **La otra mitad (un sólido por superficie) sigue sin escribirse, con su razón intacta**: el default es sólido, así que los 48 sin `variante` no aparecen en ningún grep — un contador ingenuo **bendice como preexistente lo que nunca contó** (R44 dos veces). Rojo **discriminado por mutación**. *Media regla honesta vale más que una entera que miente* | — |
| ⑵ | **✅ ORDEN ② — SERVIDA, los tres como UNA gramática** | `…-RECETA-EL-VENDEDOR-EL-PRIMER-DIA.md`. **La ley madre: el vacío es ausencia de TRABAJO, jamás de NEGOCIO** ⇒ ☠️ muere «Todavía no hay pedidos…» y toda su familia (*consuelan en pasivo: dicen «no pasó nada» cuando lo que pasa es que el negocio está en pie*). **Dos voces con `haVendido`**, jamás la misma frase. **El HOY vacío NO es un `EstadoVacio`** —hay una tienda abierta: se monta la misma composición diciendo la verdad de hoy—. **Y la costura que los une: UNA fuente de «qué le falta», leída en tres lugares**; el examen de que nació una vez es que al pasar `haVendido` a true las tres cambian solas | **ojo del founder** |
| ⑵b | **🔴 ORDEN ② — gramática + HOY vacío + §5bis (registro del pedido original)** | dos tercios ya existen (§3 de la gramática · §5bis nombrado). **Falta el literal del «HOY vacío»** (arriba) y unir los tres en UNA respuesta: *el mismo vendedor el mismo día* | **el literal, y después yo** |
| ⑷ | **✅ N20 DESTRABADA — la vitrina abre SIN eje de necesidad** | `…-RECETA-NAVEGACION-DE-LA-VITRINA-N20.md`. **🔴 Hallazgo que nadie nombró: 11 + 3 = 14 sobre 13 comprables ⇒ al menos un producto vive en DOS especies ⇒ la especie es FILTRO, jamás carpeta** (nada de tabs excluyentes: un alimento que sirve a los dos no elige dónde vivir). **El eje de necesidad no aparece hoy y se enciende SOLO por umbral derivado del conteo** — dos condiciones, y **la segunda evita el peor caso**: con 30 productos donde 27 son «adulto» el filtro no reparte, *y un filtro que no reparte es un adorno con estado* | **ojo del founder** |
| ⑷b | **✅ El bucket «todas las edades» — decidido CON número (2 de 13)** | **NULL = COMODÍN: aparece en TODOS los momentos, jamás bucket propio.** Con 2 productos un chip propio sería *el menú que esconde* que N20 prohíbe, **y se los sacaría a los buckets reales**. 🔴 **Y por qué se decide hoy aunque el eje no se muestre: el eje se enciende SOLO** ⇒ el día que dispare no va a haber nadie mirando, y 2 productos desaparecerían en silencio de todos los filtros. ***Una automatización obliga a decidir sus casos ANTES*** | — |
| ⑷c | **✅ Grilla y completitud, ENMENDADAS por los números** | completitud: con **6+1**, el contador nace **cerca de cero** ⇒ *con un hueco la línea narrativa ES la superficie entera; con cero huecos NO se dibuja nada* (felicitar por lo normal es la mecánica que LOYALTY §2 prohíbe). **Los 457 canónicos NO entran al contador** — *un contador que cuenta lo que no es tuyo no se puede cerrar nunca*. Grilla: con 11 y 7, **sin paginación, sin «cargar más», sin encabezados** — *cualquier andamio pesa más que el contenido que organiza* | — |
| ⒁ | **✅ LA BARRA — veredicto aplicado, y el defecto era ARITMÉTICA** | ☠️ `huellaEnDisco` MUERTA con su condición cumplida (**sin huella, gana el dinamismo**). 🔴 **El «círculo casi afuera» tenía número: alza 20 contra radio 26 ⇒ el disco iba de -46 a +6, o sea 6 px de 52 adentro (11 %).** Ahora 10 contra 28: **18 adentro**, con el valle a 18. *El founder lo describió exacto sin ver el número; la geometría le daba la razón.* Disco y barra en `bg.tinta` **por slot** (el techo), texto **papel pleno / papel atenuado** por §15b.2, con sus **2 pares nuevos en el gate (352/0)** | **ojo del founder** |
| ⒁b | **🔴 EL «SALTA»: separé el VIAJE del ESTIRÓN** | la mitad visible era la geometría (un objeto casi afuera *parece* saltar aunque interpole). **La otra es de transporte y NO la puedo verificar sin aparato** ⇒ **la posición dejó de depender de animar la cadena `d`**: el viaje es un `transform` sobre el grupo entero y `d` solo deforma. **Y disco y valle viajan en el MISMO grupo, así que no pueden desincronizarse** — eran dos cosas viajando por mecanismos distintos. *Una animación de dos capas donde la crítica cuelga de la frágil no es arriesgada: ya falló y todavía no lo sabés* | **ojo del founder** |
| ⒂ | **🔴 EL PIN — el error era MÍO, y de sobre-corrección** | releí la firma: *«la candidata D queda FIRMADA como silueta ganadora; su TRATAMIENTO cambia»*. **Yo cambié la SILUETA** — la reproyecté a tres cuartos para cumplir «perspectiva compartida» y destruí lo único que ya estaba firmado. ⇒ **vuelve la silueta D verbatim** y encima **solo el tratamiento**. Y la casita: medido, **su base terminaba en 23.7 con la sombra en 26.5 — 3 px de aire, que es exactamente lo que hace flotar algo**; ahora la sombra TOCA. **La vara de la mesa, adoptada: se mide que SE APOYE y se mide que SE RECONOZCA — son DOS pruebas** | **ojo del founder** |
| ⒃ | **✅ LA GRAMÁTICA DEL TECHO — pieza, no cura de dos pantallas** | `…-GRAMATICA-DEL-TECHO.md`. **Los dos hallazgos son el MISMO defecto: el techo cuenta el contenido antes de decir dónde estás** (vitrina arranca en «tu semana» · configuración se llama «Pedidos»). Ley: **isotipo + nombre de la pantalla, y recién después el contenido**; la prueba es que **① no cambia cuando cambia el contenido**. 🔴 **Y la pieza YA existe (`Encabezado portada`): lo que falta es que EXIJA el nombre** — *las dos pantallas no lo hicieron mal, hicieron algo que la pieza permitía*. **No ejecuté la enmienda: tocar `Encabezado` exige censar sus consumidores, y ese censo es tanda propia** | **censo + lote** |
| ⒄ | **✅ EL UMBRAL DE N20, ENMENDADO POR C — y tenía razón** | medido: el comodín es **mayoría** (121/232 · 65/109) ⇒ «cachorro» devuelve **64 % del catálogo**, con tres buckets ≥3 y **ninguno que reparta**. **Mi segunda condición pasa su LETRA y falla su ESPÍRITU** — produce el adorno con estado que existía para evitar. ⇒ el umbral pasa a medir **discriminación** (ningún bucket sobre ~40 %). ***UN UMBRAL QUE MIDE TAMAÑO NO MIDE UTILIDAD*** — *«hay suficientes» y «sirve» son preguntas distintas, y contesté la primera creyendo que contestaba la segunda*. ⚠️ Y **no hay nada roto: son dos decisiones correctas que se estorban**, por eso lo absorbe el umbral y no la otra | — |
| ⒅ | **✅ LA FILA DEL REPARTIDOR — FIRMADA** | **nombre · vehículo · placa**; WhatsApp a un toque en la ficha y el documento también. ⏪ Dejé la pregunta abierta y **la firma llegó con la misma razón que yo había dado**: *es lo que el vendedor ve llegar por la puerta* | **lote de C** |
| ⑽ | **✅ ① EL SEGUIMIENTO DE NODOS — las cuatro barras MURIERON** | `EscaleraEstados.compacta` pasa a **nodos unidos por una línea que se rellena**, con **ícono adentro como SLOT** (no diccionario: el vendedor y la familia no miran el mismo hecho). 🔴 **El diagnóstico es de GRAMÁTICA, no de estética: una barra solo puede decir CUÁNTAS VAN — no tiene adentro. Un nodo sí, y por eso dice QUÉ ES sin una palabra.** *No se cambió de dibujo por gusto: la forma vieja no tenía dónde poner el significado.* Nace `cuandoLlega` sobre `promesa_entrega_desde/hasta` (dato real): los nodos dicen dónde está, la ventana dice cuánto falta. **Y es RANGO, jamás minuto** — N14 prohíbe la precisión inventada, no la promesa que existe. Con desvío la ventana NO se muestra: prometer una entrega que ya no va a pasar | **ojo del founder** |
| ⑾ | **✅ ② LA DIRECCIÓN DEL CRUCE — nace `cruce.ts`** | la ley de mesa mecanizada: **la dirección la escribe LA PUERTA al ser tocada**, porque *la puerta es lo único que el dedo toca para cruzar y la única que SABE hacia dónde apunta* — la ventana que llega no puede saberlo, depende del sustrato, **y el sustrato ya cambió dos veces en esta sesión**. Se escribe DENTRO de `PuertaHermana.onPress`, no en el consumidor: *si dependiera de que cada pantalla se acuerde, la tercera puerta la pierde de nuevo*. **Módulo y no contexto**: un provider tendría que envolver a las dos ventanas, **y las dos ventanas son justo lo que el sustrato muda**. `tomarCruce()` CONSUME — *un dato de gesto que sobrevive a su gesto empieza a mentir* | **D lo consume** |
| ⑿ | **🔴 ③ STOCK — y UNA DECISIÓN MÍA SE CAE** | ⏪ dije *«sin paginación, sin cargar más, sin encabezados»* **con ONCE productos**; A sembró y **la vitrina pasó a 399**. Re-medido ítem por ítem: **sobreviven** sin paginar y sin encabezados; **se cae «sin cargar más»** — 399 filas de una revientan N16. Entra **ventana + carga al llegar al final, jamás un botón** (precedente S60), y **el número lo mide C en aparato, no lo invento yo**. *Es mi propia L-246 cobrándose en mi contra: la decisión estaba bien razonada sobre una cardinalidad que ya no existe.* Modos: `SelectorSegmentado` fijo · **LISTA por default** (*tarjetas para elegir, filas para leer* — Stock es donde se trabaja) · **la elección se RECUERDA**, local al dispositivo: *es de la vista, no de la cuenta* | **lote de C** |
| ⑿b | **🔴 Y el volumen le da al espejo con qué verse** | 27 rechazados con motivo · 26 sin precio · 27 en revisión. **Mi frontera VEREDICTO/HUECO era un argumento; ahora hay 80 filas para verla o desmentirla.** Lo que hay que mirar: con 27 rechazados a la vista, **¿el rojo se lee como «arreglalo» o inunda?** Si inunda, lo que está mal no es el estado: **es que en LISTA el veredicto se resume y su literal vive en la ficha**. *Once productos no podían mostrar eso* | **ojo del founder** |
| ⒀ | **✅ ④ LOS TRES HALLAZGOS DE LA CAMINATA DE C** | **«Agregar corte»**: es N12.5 — el control **desaparece**, no se deshabilita, y sin leyenda del tope; *el precedente ya vive al lado (los vehículos del repartidor)*. **La fila no dice que es editable**: es el peor de los tres porque **no es una ausencia, es una señal equivocada** — el único afordance visible es el interruptor, que hace otra cosa ⇒ **fila entera tocable con chevron** (19.7), y el interruptor se queda. *Hoy la fila tiene UNA acción visible y DOS reales; después dos y dos.* **El dato es el DOCUMENTO**: sale de la fila y se muda a la ficha; el subtítulo pasa a **vehículo + placa en mono**, con «+1» si hay dos. ⚠️ **Vehículo vs WhatsApp: no lo decido** — los dos reconocen y la fila tiene lugar para uno | **lote de C** |
| ⑧ | **✅ EL PIN, RE-DIBUJADO COMO OBJETO DEL MUNDO + nace `destino`** | segundo rechazo, **eje distinto al primero**: no era el dibujo, era la GRAMÁTICA (símbolo vs objeto). Las cuatro físicas de §6ter aplicadas: **sombra en el suelo** (elipse con `RadialGradient` — *una sombra de borde duro es un objeto más; lo que la vuelve sombra es que se desvanece*) · **perspectiva cenital** (ruedas en ELIPSE, la caja mostrando su TAPA — *una tapa visible es la firma de que se mira desde arriba*) · **color del terreno** (medí la banda de saturación del mapa: **0.10–0.58**; `pink` 1.00, `tealDark` 0.84 y `magentaDark` 0.64 **quedan los tres afuera** ⇒ nace `mapaMoto` = magenta + 35 % tinta, **sat 0.54 adentro, 6.61 de contraste**) · **destino = EDIFICIO neutro** con la puerta en marca, que ata el par. **Sin anillo NI `boxShadow`**: un boxShadow es sombra de TARJETA (cae detrás del rectángulo) y un objeto del mundo proyecta sobre el SUELO | **ojo del founder, los DOS juntos** |
| ⑧b | **✅ Los 9 pares del mapa, al gate** | WCAG **341 → 350**, 0 fallos. Entran como GLOBALES porque **el mapa no tiene tema**, y los tonos se declaran **constantes de referencia, no tokens** — *son muestras del mundo, no colores que la casa eligió*. ⚠️ El gate cubre la mitad medible de «el color pertenece al terreno»; **la saturación no la puede ver** y vive en el token con su número | — |
| ⑨ | **🔴 DICTAMEN DE LA BARRA DE TABS — 4 resueltas, 2 BLOQUEANTES** | `…-DICTAMEN-BARRA-DE-TABS.md`. **① dos marcadores del mismo estado** (huella + disco): uno gana o uno muere; el camino de la mesa —huella DENTRO del disco— lo firmo, **pero enmienda §2.6 y no lo decido yo**. **② ATENDER destacada permanente + tres tabs iguales en la referencia** ⇒ con ATENDER inactiva hay **dos cosas pidiendo ser el centro**; las dos salidas tocan letra firmada. **③ posiciones variables: NO es problema** —el valle se deriva del centro del tab activo, que la barra ya calcula—. **④ el alto: ~26 dp, y no cede ni el target ni el inset.** **⑤ 🔴 el hueco conoce el fondo — y acá el fondo cambia por tema, casa Y pantalla: la salida es RECORTAR, no pintar** (`fill-rule="evenodd"`), *porque un hueco del color equivocado no se degrada: se ve como una mancha, y funciona justo en la pantalla donde se lo probó* | **DOS firmas de mesa** |
| ⑹ | **✅ LAS SIETE DE L5b, COMPLETAS — receta 7 servida** | `…-RECETA-EL-ESTADO-EN-EL-ESPEJO.md`. **🔴 Los cinco estados NO son cinco de la misma clase, y la frontera ya estaba trazada por el contador de C**: VEREDICTO de e-PetPlace (en revisión · rechazado — se informan, sin paso, **jamás suman**) vs HUECO del vendedor (sin precio · precio sin publicar — **cuentan y tienen UN paso**). *Si el contador ya distingue y la forma no, el vendedor ve cinco cosas parejas y un contador que solo cuenta dos, y esa incoherencia se lee como que el contador está roto.* **`publicado` NO SE DIBUJA.** El `motivo_rechazo` va **literal** —*parafrasear un rechazo es reescribir lo que decidió otro*— y es el **único `danger`** de la ficha. En modo cliente: **al pie, con su número, jamás tarjeta fantasma** (una familia nunca vería un placeholder, y el espejo deja de serlo al inventar una casilla) | **ojo del founder** |
| ⑺ | **✅ `sinCaja` → `apoyada`, RENOMBRADA con alias + `R48`** | tipo, slot de tema (3 casas) y galería migrados. **El alias se resuelve en UN solo lugar y el TIPO lo prueba**: `Exclude<BotonVariante,'sinCaja'>` en `varianteEfectiva` y en la tabla de color ⇒ *con el alias adentro del `Record`, el compilador pediría una entrada y habría dos filas de color para la misma variante, libres de divergir*. Los 5 usos de `apps/` quedan congelados, sin bloquear a C ni a D | **lote de C/D** |
| ⑺b | **🔴 LA TRAMPA DEL RENAME ERA OTRA, y peor** | el aviso era *«un grep se lleva el `sinCaja` de `Campo`»*. **Medido: esa prop YA ESTÁ MUERTA** (la derogó N11 en esta sesión) ⇒ **no había API que romper.** Lo que un grep ciego habría roto son **sus LÁPIDAS**: `Campo.tsx` dice *«`sinCaja` MURIÓ, DEROGADA POR N11»* y habría quedado *«`apoyada` MURIÓ»* — **falso**. ***Un rename mecánico no rompe solo código: reescribe el registro de lo que murió, y ese registro es lo único que impide revivirlo.*** Por eso `R48` cuenta `variante="…"` y **no la palabra suelta** — contarla haría que documentar una muerte aumente una deuda ajena | — |
| ⑸ | **⏪ `sinCaja` — el pedido original (ya ejecutado en ⑺)** | lo dice el propio archivo: *«NO ES SIN CAJA: tiene `accent.sinCaja`… para darle presencia de superficie»*. **5 usos: renombrar es barato HOY y no lo va a ser después.** Es enmienda de vocabulario firmado (S82) ⇒ **no la decido yo**. Candidata servida: **`apoyada`** | **mesa** |
| ⑶b | **✅ L5b — de 2 a 4 recetas, corrigiendo mi propio triaje** | releí mis tres «bloqueadas» y **dos no lo estaban**: la **completitud (N18)** no depende de la lista de campos —el censo dice CUÁLES, la ley dice la FORMA— y la **grilla** ya estaba decidida por `Baldosa` (su patrón de grilla es parte de su contrato). **Solo la navegación N20 sigue bloqueada de verdad**: seis especies y cuatro necesidades no se componen como doce y quince — *ahí la cardinalidad sí es la forma*. ⏪ **El criterio que apliqué mal: «depende del censo» ≠ «depende de un número del censo»** | **censo de A, solo para la 3** |
| ⑶ | **✅ L5b ADELANTADA — las 7 recetas listadas, 2 escritas** | `…-RECETAS-ADELANTADAS-L5b.md`: **ficha de producto** (el precio por kilo en MONO porque es cálculo, y ese registro ES su valor · «para quién sirve» jamás como `FilaDato`, que lo volvería tabla) y **el interruptor del espejo** (`SelectorSegmentado` fijo en el techo, **jamás navegación**: si navega, el vendedor sale de su trabajo y el espejo se visita una vez). **Las otras 3 NO se adelantaron a propósito**: dependen de CARDINALIDADES del censo de A, y una anatomía contra una cardinalidad inventada se rehace entera | **censo de A** |
| ①c | **✅ `moto` FIRMADA (la D, con caja) — el freno se levantó solo** | entró a `PinEnMapa` como `variante="moto"`, **NO al registry de `Icono`**: lo prohíbe §6ter, que esta misma sesión depositó (*el mapa no es interfaz, es mundo*; meterla en `Icono` la pondría bajo R30 como glifo de interfaz). El freno decía *«la pieza no cambia de forma»* — y no cambió | — |
| ①d | **🔴 EL ANILLO — MEDIDO, sobre mapa REAL, esperando el ojo** | galería, **sección PRIMERA** (corolario firmado): los dos pines en el MISMO mapa. **La medición da vuelta la premisa de la mesa** — la silueta oscura gana **11.54 · 11.19 · 9.85** sobre los tres tonos que el mapa realmente pinta, y el que desaparece es **el anillo blanco (1.44 · 1.48 · 1.68)**. El cuarto tono (mapa oscuro) **hoy no existe: cero `customMapStyle` en la casa**. ⇒ el anillo **muere en la moto** y **sigue en la mascota** (ahí el contenido es una FOTO y su color no lo elegimos). Condición de revivir escrita en la pieza | **ojo del founder, en el teléfono** |
| ①e | **🔴 La transición direccional que D necesita** | verificarla **donde vive** (ley S99), no en lámina: la puerta hermana cruzando entre las dos ventanas. **Depende de que D tenga las dos montadas** — hasta entonces no hay dónde gatearla, y montarla en la galería sería reconstruir la lámina que la ley acaba de matar | **D** |
| ①f | **🔴 El ancla de la puerta a la altura de «Tu día»** | alinear el bloque de `PuertaHermana` con la línea de «Tu día» — es **composición del consumidor**, no geometría de la pieza (la pieza ya fija su alto de 44 y su aire, y por eso las dos mitades no pueden diferir). Mi parte: confirmar que no hace falta prop nueva | **D** |
| ①b | **✅ La clase «marca de mapa» — DEPOSITADA** | firmada por mesa (salida **a**) y escrita en **`DIRECCION_ARTE` §6ter**, con su frontera —*el mapa no es interfaz, es mundo*—, la medición que prueba que **no se inventó sino que se nombró**, el porqué de descartar (b), y **la razón angosta de la huella declarada SUBSUMIDA** (su conclusión sobrevive; su razón queda superada, con su alcance recortado y escrito). **La Ley 12 del trazo quedó INTACTA** | — |
| ② | **✅ N11 PASÓ — sale de la cola** | caminado en aparato: la Hoja sube, el CTA queda arriba del teclado, nada tapado. **Lo que quedó vivo es OTRA cosa: ③** | — |
| ②b | **🔴 La gramática de configuración — RATIFICADA por mesa** | C ejecuta §5 en orden de retorno: **la línea del contrato de guardado** → reordenar a la operación → la zona de completitud (N18) → unificar material → `EstadoVacio`. La mesa ratificó **no convertir ventas en wizard** | **lote de C** |
| ②c | **🔴 SERVIDO, NO CONSTRUIDO · el arranque del vendedor** | la mesa lo pesó como **de producto, no de forma**: *«tres listas vacías sin guía» es un vendedor que no sabe qué hacer primero el día que abre su tienda*. Nombrado en **§5bis de la lámina** con su pregunta —*¿qué necesita ver la primera vez?*— y sus dos anclas ya escritas (**N18** la completitud · **el espejo de L5b**). **Orden explícita: nombrarlo, no construirlo** | **mesa de producto** |
| ②d | **🔴 RECETA SERVIDA · la ficha del repartidor (L2)** | **`docs/laminas/2026-08-15-s99b-RECETA-FICHA-DEL-REPARTIDOR.md`** — la anatomía de una PERSONA en esta casa (la cabecera **ES el espejo de lo que ve la familia**, N17 aplicada a alguien), las dos fotos que hoy pesan igual **y no son la misma cosa**, el vehículo como **fila** (no tarjeta) con la placa en mono, N12 entera con máscara derivada del tipo, N13 en `ayuda` **porque un cuarto color de borde rompería N11**, y la referencia Uber/DiDi con su corte. **🔴 Y el hallazgo que se pierde solo: al borrar el teléfono, el selector de indicativo se MUEVE a WhatsApp — es el único campo que hoy lo tiene.** ✅ **MECANIZADO en `R46`** por orden de mesa (*«que C lo tenga escrito antes de tocar la pantalla»*): el invariante vale ANTES y DESPUÉS del lote —*toda superficie que le pide a un repartidor su WhatsApp compone al menos un `ControlTelefono`*— y **el hook de pre-commit corre `verify:diseno` y ABORTA el commit**, así que deja de ser un aviso escrito y pasa a ser una barrera. La salida (b) para una pantalla que solo MUESTRA el WhatsApp está en el mensaje | **lote de C** |
| ②e | **✅ EL CONTADOR DE `PuertaHermana` — RECETA + EJECUTADO** | receta en **`…-RECETA-CONTADOR-PUERTA-HERMANA.md`**; la pieza ya lo lleva. **`sinVer` nace REQUERIDA** (con opcional, la asimetría probable no es pintar distinto: es que una puerta la pase y la otra se olvide) · mono tabular **primary**, cero acento y cero caja ⇒ **cero pares nuevos de WCAG** · con 0 no dibuja nada. **Choque con su propia cabecera DECLARADO y reconciliado con los dos literales** | **ojo del founder** |
| ②f | **✅ `Hoja` gana PIE FIJO — pedido de C, precondición de L2** | el `children` iba dentro del propio scroll de la Hoja ⇒ **todo CTA de formulario nacía debajo del pliegue** y todo `HojaScroll` anidaba. El slot `pie` vive FUERA del scroll. **`flexShrink: 1` es la mitad que lo hace existir**: sin él, el scroll se dimensiona a su contenido y **empuja al pie fuera de la hoja** — el mismo defecto un piso más abajo. **El filete del pie solo aparece con desborde MEDIDO** (`onContentSizeChange` vs `onLayout`): así deja de ser decoración y significa *hay más arriba*. Galería con los **dos** estados | **ojo del founder** |
| ②g | **✅ `Celda` gana `tituloEntero` — opt-in** | ley en la prop: **se enciende donde el título ES el criterio de elección**, jamás por prolijidad. **Sin techo de líneas a propósito** — *«no se trunca» y truncar en dos es la misma falta con otro número*; la fila crece y `ALTURA_MIN` siempre fue un mínimo. **Las cuatro superficies del corte son de C**: yo no toco `apps/`. ⚠️ Mi primer conteo dio **239** y era falso —`<Celda` se traga `<CeldaNavegacion`—: **161 puro + 77 de navegación**; el 162 de C era el bueno | **lote de C** |
| ③ | **📮 La ley de la cuarta muestra — pedido a A** | *«un verificador de SHA pasa perfecto cuando el trabajo ni empezó»*. **El canal está caído**, así que el pedido vive **en este parte** con el repro literal, la forma corregida y el corolario exigible. A la deposita | **A** |
| ③b | **📮 `ControlTelefono` tiene CUATRO consumidores y NO vive en `packages/ui`** | medido: `cuenta/perfil.tsx` (×2) · `ventas/configuracion.tsx` · `alta/PasoEquipo.tsx`; la ficha del repartidor sería el quinto. La casa promueve en el SEGUNDO. **El movimiento es MÍO, no de C** — no bloquea nada hoy, y se declara para que no lo descubra el sexto | **firma de mesa** |
| ③ | **Háptica** (§2, candidata con gate) | prototipar como **enmienda de primitiva** (`Boton`/`usePresionado`), jamás llamada suelta por pantalla. **NO prototipada a propósito**: su único gate es el dedo | **firma de mesa + aparato** |
| ④ | **Barrida de la partición a 7 reglas más** | atribuir dueño y curabilidad en `R12` · `R24` · **`R36`(20) · `R37`(3) · `R38`(6) · `R39`(6)** · `R40`(1). **Exige ENUMERAR los ofensores de cada una** — es medición, no edición | **pedido de mesa** (no la hago sin pedirla) |
| ⑤ | **N16.2 · «el spinner muere»** | escribir su ratchet **solo si la mesa lo pide**. Medido en **0** hoy; **no la escribí** porque su net honesto tendría que exentar `packages/ui` (3 `ActivityIndicator` legítimos, controles ocupados) y **esa exención se decidiría sola sin que nadie la mire** | **firma de mesa** |

## 🔴 LOS DOS FRENTES NUEVOS DEL GATE 1 — el detalle

### ① EL GATE DEL `moto` FALLÓ, Y EL DIAGNÓSTICO ES DEL BRIEF, NO DEL DIBUJO
**Verbatim del founder:** *«no me gusta ninguno de los tres, copiemos el
como los pone Rappi… Si me toca escoger sería el A, aunque parece más una
bicicleta que una moto.»*

**Las tres se dibujaron como glifo de la casa** (trazo 1.9, objeto de
interfaz) **y su destino es vivir DENTRO DE UN PIN, a 21 px, sobre un
mapa** — ahí **el trazo desaparece y solo sobrevive la MASA**.

**⚠️ Mi error, y §6b me daba la herramienta:** hice el estudio de familia
por los números de la casa **sin preguntarme si esa familia aplicaba**, y
monté **la vecindad del REGISTRY cuando el vecino real es el MAPA**.
**Y asigné mal el riesgo:** puse «puede leerse como bicicleta» en **C** y
el founder lo leyó en **A** ⇒ *no era riesgo de una candidata: era del
ENFOQUE.*

**🔴 CHOQUE DECLARADO · mi voto: (a).** Ley 12 pide **trazo + una huella**;
un pin de silueta la contradice. **(a) el pin de mapa es CLASE APARTE** —
y **no hay que inventarla, MEDIDO: el mapa ya la tenía sin escribir.** Las
dos marcas que la casa dibuja sobre un mapa **ya son MASA**: el marcador
de `MapaRecorrido` es `backgroundColor` + anillo, el punto de `PinMovible`
también. **Ninguna es un glifo de trazo.** · **(b) es peor porque no está
acotada**: *«fondo no-controlado»* incluye toda foto, todo gradiente,
todo póster — deja entrar el relleno por una puerta que nadie puede
cerrar. **(a) se acota sola: el pin vive en UNA pieza.** *Tercera vez que
la casa elige la razón angosta sobre estirar una viva.*

### ②b LA GRAMÁTICA DE CONFIGURACIÓN — el censo dio vuelta la pregunta
**🔴 Los diez campos NO están en la pantalla: los diez viven DENTRO de
tres Hojas** (medido: cero `<Campo>` fuera de un `<Hoja>`). ⇒ **la
pantalla no tiene diez campos que organizar: tiene TRES LISTAS y UN
ENLACE**, y por eso «organizar mejor los campos» no lo curaba.

**Y la incoherencia real no es visual: son DOS CONTRATOS distintos sobre
cuándo el trabajo está a salvo.** En el taller **tocar es gratis** (nada
persiste hasta el CTA); en ventas **cada confirmación ya es permanente**.
*No son dos estilos: son dos modelos mentales, y la persona tiene que
descubrir en cuál está.* **Peor: el taller te GUÍA y ventas te deja
parado frente a tres listas vacías** sin decir por dónde empezar ni
cuándo terminaste.

**❌ Lo que NO hay que hacer: convertir ventas en wizard.** Los dos
modelos son correctos para su dominio (el taller arma **una cosa que solo
vale completa**; ventas administra **colecciones que valen sueltas**).
*Forzar uno sobre otro compra coherencia pagando con una mentira sobre el
dominio.*

**La gramática, en tres reglas:** ① la pantalla **DICE** en qué modelo
está —el discriminador es *¿existe estado parcial válido?*— · ② toda
configuración contesta **qué falta · por dónde empiezo · ya está listo**,
**y no lo inventé: sale de N18**, firmada · ③ lo que se unifica siempre
(anatomía de sección, de lista, un material por jerarquía, el vacío que
habla). **Referencia: Shopify** — se toma **que el contrato se declare**,
no su barra flotante (la casa ya tiene el CTA al pie: Ley 11).

## LOS COMPROMISOS, POR PISTA

**Con C** — **R44 baseline 6 es suyo y es CURABLE**: los seis
`datosInvalidos` de `apps/prestador` (3 `es` + 3 `en`). La cura es decir
**qué** falta (*«Falta la dosis del medicamento»*) y **bajar el baseline
en el mismo commit**. · **R42: 8 entradas curables suyas y de D** — cada
lote que toque uno de los ocho archivos cambia la hoja a mano por
`<HojaCaptura>` y baja el baseline. `PasoDocumentos` ya lo tomó en L1.

**Con D** — **`PuertaHermana` y `PinEnMapa` entregados** (dejaron de ser
su dependencia). **Le debo el Toque 1 de `TarjetaPedido`** cuando llegue
con el caso de la línea del día (*«qué necesita la fila que la tarjeta no
da»* o *«`disposicion` alcanza»*). **Y quedó sin entregar por el canal
caído:** la condición mecánica de R45 — **al lint le alcanza con que el
archivo MENCIONE `sinFecha`**; hoy vive en el rojo de la regla.

**Con A** — **R44: 3 entradas suyas y están BLOQUEADAS, no pendientes**
(`datos_invalidos` es el fallback de *cualquier* CHECK ⇒ la voz buena
exige códigos tipados por constraint). Apuntan a **D-828**; el día que
una puerta gane su código, **la línea baja de 9 a 8 sola**. · Servido y
sin cerrar: **la divergencia del bezier (D-825)** — N10 firmada nombra
`.32,.72,0,1` y **11 piezas usan `easeOut [0,0,0.2,1]`, entre ellas
`Entrada`**. No toqué once piezas: es voto de mesa.

## EL UNIVERSO DE LO NO HECHO (para que nadie lo lea como cubierto)

- **Cero gate en dispositivo hecho por mí.** Todos mis verdes son
  **RN-web + instrumento**. `PinEnMapa`, `PuertaHermana`, `HojaCaptura`,
  el `Campo` nuevo y el `moto` **nunca se vieron en un teléfono**.
- **N11 no está migrada por pantalla porque no hace falta** — y eso es
  también su riesgo: **landa en 121 campos a la vez** y ningún
  instrumento puede decir si el resultado respira.
- **`SelectorAvatar` NO se absorbió** (R42, BLOQUEADA): su tercera fila
  es firma de S45 y cambiarla es **gate de forma**, no refactor.
- **El logo del prestador no migra a `HojaCaptura`** (D-740: PNG con
  alpha; el resize compartido devuelve JPEG). Migra el avatar.
- **N12.1/.2/.3/.5 · N13 · N19 · N20 quedaron DE OJO**, con su razón en
  §B4 — no están mecanizadas y no deben leerse como vigiladas.
- **`EvidenciaFoto` no es deuda y no va a bajar nunca**: el piso de R42
  es **1**, jamás 0.
- **R45 tiene un hueco declarado**: una **segunda** consumición con otro
  propósito en un archivo que ya declaró **hereda la declaración vieja**.
  Estáticamente no se distingue.

---


## §1 · LO ENTREGADO

### ✅ B1 · LA PUERTA ÚNICA DE LA FOTO — censo + pieza + regla

**El censo dio vuelta la premisa, y esa es la mitad que vale:** el
**picker nunca se duplicó** — `capturaFoto` es UNA implementación y los
diez sitios la llaman. **Lo duplicado es la PUERTA**, la hoja de «Tomar
foto / Elegir de la galería», escrita a mano en **diez lugares**.

**Y el número que lo convierte de prolijidad en defecto:**

> ⚠️ **EL CERROJO CONTRA EL DOBLE TAP VIVE EN 2 DE LOS 10 SITIOS — y los 2
> son los que ya eran pieza** (`SelectorAvatar`, `EvidenciaFoto`). Los
> **ocho de `apps/` no lo tienen**: dos toques antes del próximo render
> lanzan **dos pickers**.

*No es que ocho pantallas hayan sido descuidadas: es que el cuidado solo
se hereda cuando hay de quién heredarlo. Un sitio que copia una anatomía
copia lo que se VE; el cerrojo no se ve.*

**Las diez, medidas** (`capturarConCamara` + `capturarDeGaleria` en el
mismo archivo):

| dónde | cerrojo |
|---|---|
| `packages/ui/…/SelectorAvatar.tsx` | ✅ |
| `packages/ui/…/EvidenciaFoto.tsx` | ✅ |
| `apps/cliente/…/carnet.tsx` | ❌ |
| `apps/cliente/…/(tabs)/cuenta/perfil.tsx` | ❌ |
| `apps/cliente/…/components/HojaFotoMascota.tsx` | ❌ |
| `apps/prestador/…/ventas/configuracion.tsx` | ❌ |
| `apps/prestador/…/(tabs)/cuenta/perfil.tsx` | ❌ |
| `apps/prestador/…/veterinaria/verificacion.tsx` | ❌ |
| `apps/prestador/…/components/seccion-documentos.tsx` | ❌ |
| `apps/prestador/…/components/alta/PasoDocumentos.tsx` | ❌ |

**LA PIEZA: `HojaCaptura`** (`packages/ui`, en el índice y en la galería).

- **La anatomía que gana es la del ALTA DE MASCOTA** —dos `Boton
  secundario bloque` + la X, `HojaFotoMascota` S82-A—, la que el founder
  señaló. **Y no gana por ser la citada: la citó porque cumple la ley.**
  **Ley 22c — un comando con consecuencias viste de botón**; abrir la
  cámara es un comando, no una navegación, y `Celda` es la fila de una
  lista, que promete que tocar te LLEVA.
- **Dos acciones y son todas.** No hay tercera fila de salida: la Hoja ya
  sale por X, swipe y back, y la **REGLA DE ORO** del Norte dice *ante la
  duda, quitá antes que agregar*. Cuando la salida es parte del FLUJO (la
  foto del alta es opcional-pero-muy-sugerida) vive **en la pantalla**,
  con el peso que esa decisión merece — como ya lo hace `PasoFoto`.
- **La pieza NO decide tres cosas, y está escrito en su cabecera:** la
  voz del permiso denegado (reporta; la pantalla dice — en el alta *el
  permiso jamás frena el alta*, en el avatar hay tarjeta de recuperación
  con «Abrir ajustes»: una sola voz obligaría a la mitad a mentir) · las
  opciones de captura (pasan derecho: 800 avatar, 1600 documento, el lado
  del carnet son decisiones MEDIDAS de cada dominio) · **la absorción de
  `SelectorAvatar`** (su tercera fila «Por ahora no» es decisión FIRMADA
  en S45 — cambiarla es gate de forma, no refactor, y no viaja escondido
  adentro de una pieza nueva).
- **Los literales son VERBATIM** de `fotoEncuadre.camara`/`.galeria` del
  cliente ⇒ **no abre lote de gate de strings**: es la misma frase
  cambiando de casa. *(La casa tenía tres redacciones para esto — «Sacar
  una foto» · «Tomar foto» · «Pick/Choose from the gallery»; gana la
  firmada, las otras mueren cuando su sitio migre.)*

**⚠️ NO MIGRÉ NINGÚN SITIO — es territorio de C/D.** La lista de arriba es
el pedido: cada lote que toque uno de esos ocho archivos lo cambia por
`<HojaCaptura>` **y baja el baseline de R42 en el mismo commit**.

### ✅ B4 (parcial) · R42 — LA PUERTA DE LA FOTO NO SE RE-DIBUJA

Mecanizada **el día que nació la pieza** (misma disciplina que R25/R30).
Net angosto: importar **las dos** funciones de captura en el mismo
archivo es la huella dactilar de armar el menú. **Baseline nominal
SOLO-BAJA con las diez y su razón**; de acá no se sube: una puerta nueva
a mano es roja el primer día. Su fixture trae las diez + la canónica +
una puerta nueva, para que el rojo medido sea **el de la regla** y no el
del ancla ni el del baseline — **discriminado con las tres corridas**
(con ofensor → 1 fallo, el ofensor · sin ofensor → verde · sin canónica →
enciende el ANCLA aparte).

### ✅ B5 (mitad) · `PinEnMapa` — la variante MASCOTA

**La forma NO es un pin nuevo: es el punto que la casa ya dibuja, que
CRECE para sostener una cara.** `MapaRecorrido` marca la última posición
con 16 px + anillo blanco 2.5 (medido en su cuerpo) y `PinMovible`
declara la doctrina literal: *«no se dibuja un pin-gota: el pin
placeholder murió en S58 y no se resucita… el punto ya dice acá, que es
todo lo que tiene que decir»*. ⇒ **la identidad no reemplaza al punto: lo
habita.** El anillo blanco sobrevive intacto (separa la marca de un mapa
de contraste impredecible) y adentro va la cara en vez del relleno plano.

- La cara sale de **`AvatarMascota`** (la escalera §2.11) — la pieza elige
  DÓNDE, no QUÉ.
- **Interpola** con `motion.marca.aperturaBezier` + `duration.estandar`
  (300). **Jamás spring:** N10 lo cierra —bezier para *lo que aparece, lo
  que se va, lo que navega*; spring *solo gestos de rebote*—. Acá no hay
  dedo: el pin se mueve porque llegó un dato, y **un rebote diría que el
  repartidor pasó de largo y volvió**, que es una mentira sobre su
  posición.
- **Reduce-motion / memorial: SALTA** (R41 verde, 9/9). Y este viaje **no
  es del dedo del usuario**, así que entra bajo la preferencia sin la duda
  que sí tenía la vuelta del arrastre de `Hoja`.
- Sin prop `variante`: una prop de un solo valor no discrimina nada — es
  decorado que promete una capacidad ausente (clase D-574).

### ✅ CONTRATO DE D · `PuertaHermana` — el espejo entre las dos ventanas del HOY

**Fuera de mi L0, entregada en la misma tanda** porque D cerró sus dos
preguntas sin enmienda al contrato que propuse.

`direccion` **deriva** el chevron, el orden y el borde en el que se
apoya: la pantalla declara **adónde va** y jamás compone el orden. *Si el
orden fuera del consumidor, «espejo» sería una intención; acá es una
consecuencia — no hay forma de montarla torcida.* El alto (44) y el aire
viven en la pieza (Ley 8): si los pusiera cada pantalla, **las dos
mitades podrían diferir en píxeles y nadie lo vería hasta cruzar**.

**`chevron.ts` gana `izquierda` — ENSANCHA la tabla única, no la copia**
(L-175, que es exactamente para lo que ese archivo existe). Su `d` es el
**reflejo exacto** de `derecha` sobre el eje del viewBox, no un trazo
nuevo a ojo: *el espejo tiene que ser el MISMO trazo reflejado, o el día
que alguien afine el chevron afina medio espejo.*

**Las dos decisiones de D, con su razón adentro de la pieza:** sin
contador (*la puerta es un LUGAR, no un aviso; un badge en 0 se lee como
ausencia del lugar cuando la ausencia es del día* — y sostenerlo obligaría
a cada ventana a traer los datos de la otra: el viaje que N16 elimina, en
la pantalla donde D-738 se midió) · sin deshabilitado (**Ley 23 prohíbe
ofrecer lo que se va a RECHAZAR, y un día sin trabajo no rechaza nada**;
apagarla dejaría a alguien sin llegar a su ventana justo el día que más
quiere mirarla) · y **si se monta o no es del CONSUMIDOR**: composición
por capacidad.

### ✅ B5 (la otra mitad, servida) · LA HOJA DE CONTACTO DEL GLIFO `moto`

**El freno se convirtió en el paso siguiente, no en un pendiente.**
`docs/laminas/2026-08-15-s99b-HOJA-DE-CONTACTO-moto.md` con el §6b
completo: familia en números (los tres desvíos declarados, uno solo real
— A llega al techo de la banda) · **metáforas ocupadas censadas antes de
dibujar** (el vecino peligroso es `ubicacion`, el pin de gota: ninguna
candidata dibuja una gota) · **tres variantes con su riesgo por variante**
· montaje a **21 y 44 px, claro y oscuro, junto a cinco del registry**
elegidos por vecindad real.

**🔴 Y LA DECISIÓN QUE §6b.6 OBLIGA A TOMAR ANTES DEL LÁPIZ: las tres van
SIN HUELLA, y NO por la razón de `info`.** `info` e `ia` la pierden
porque **son interfaz**; `moto` es un objeto del mundo y por la Ley 12 le
tocaría. La razón real es otra:

> **En el par de pines, la huella ES el discriminador.** La de paseo
> lleva la cara de la mascota; la de entrega tiene que decir *«esto NO es
> tu mascota, es quien trae tu pedido»*. **Ponerla en las dos anula la
> única diferencia que el pin existe para mostrar.**

⇒ Servido con **dos salidas y mi voto por ②**: ① ensanchar «glifo de
control» a *«donde la huella no significa»* · ② **razón nueva y angosta**
—*la huella no se pone donde es el discriminador de un par*—, dejando la
categoría de control intacta. *Ensanchar «control» para que aloje un
vehículo le saca a esa palabra lo que la hacía útil.*

**Las tres viven en la galería y NO en el registry**, a propósito: un
glifo entra a `IconoNombre` cuando pasa su gate, no antes — si entrara
ahora, R17 lo exigiría y el set crecería sin firma.

### ⏪ EL FRENO ORIGINAL, conservado — su razón sigue siendo la buena

**Medido: el registry de `Icono` tiene 46 glifos y NINGUNO es moto,
repartidor ni entrega.** Dibujar uno adentro de esta tanda saltearía
**§6b** —hoja de contacto: 2-3 variantes, montaje a 21 px y 44 px junto a
cinco del registry, y **gate POR ÍCONO del founder**—, que es exactamente
el proceso que la casa escribió para que un glifo no nazca en el medio de
otra tarea. **Próximo paso: la hoja de contacto, con su gate.**

---

## §2 · LOS CENSOS QUE CIERRAN SIN TRABAJO

### ✅ B2 · PIEZAS DE VITRINA — **NO HAY NADA QUE SUBIR**

Las tres de `DIRECCION_ARTE` §12 **ya viven en `packages/ui`**:

| §12 | dónde | estado |
|---|---|---|
| carrusel a sangre 4:3 | dentro de `FichaPrestador` (`RELACION_PORTADA = 4/3`) | ✅ en `ui` |
| escalera de portada | dentro de `FichaPrestador` (imagen → poster → `LogoNegocio` → monograma) | ✅ en `ui` |
| chip de entidad | `ChipEntidad`, pieza propia | ✅ en `ui` |

**Y el patrón de N17 ya está probado con DOS consumidores montando la
misma pieza:** `apps/cliente/…/prestador/[prestadorId].tsx` y
`apps/prestador/…/cuenta/como-te-ven.tsx`. Su propia cabecera lo dice:
*«hasta hoy la ficha se pintaba DOS VECES y esa copia YA MINTIÓ DOS VECES
EN UN MES… con una pieza, "el espejo dice algo distinto de lo que ve la
familia" se vuelve inexpresable»*.

⇒ **El espejo del vendedor monta `FichaPrestador`. Costo de subida: cero.**

### ✅ CARRIL R (mi mitad) — **prácticamente limpio, y se dice con números**

**① El spinner ya está muerto en las pantallas.** `ActivityIndicator`:
**0 en `apps/prestador`, 0 en `apps/cliente`**. Los únicos 3 viven en
`packages/ui` (`Boton`, `EvidenciaFoto`, `SelectorOpcion`) y son estados
de **control ocupado**, no cargas de pantalla — legítimos. **63 pantallas
del prestador usan `Esqueleto`.** Los `'cargando'` que aparecen en grep
son **nombres de estado**, no spinners.

**② Imágenes sin medida: 21 de 22 declaran su caja.** El único hallazgo
es `preview-prestador.tsx:206` — `height: 168` sin `width` (RN lo estira
al cross-axis del padre, así que no salta; es 🟡 de higiene, no defecto).
**Y verifiqué antes de disparar un rojo:** el `height: 168` fijo de esa
tarjeta **NO viola §12.1** —no es la portada de la vitrina, es la fila de
una lista y su alto uniforme es deliberado (*«la tarjeta MANTIENE SU ALTO
en los cuatro: nunca un hueco blanco»*)—.

> ⚠️ **Mi propio instrumento me cobró L-170 en la primera corrida:**
> `FichaPrestador` dio DOS rojos que eran `<Image>` escritas **dentro de
> comentarios**, uno de ellos una **lápida** que dice literal *«acá vivía
> una `<Image>`»*. *La lápida de una pieza borrada es la trampa más cara:
> nombra exactamente lo que uno busca.* Curado en el instrumento (se
> despojan comentarios antes de parsear), no en el resultado.

---

## §3 · LO QUE ESPERA LETRA (declarado, no inventado)

### ✅ B3 · N11 CONSTRUIDA — la primitiva de campo de la casa cambió de forma

> **La letra aterrizó en main a mitad de tanda** (`DIRECCION_DISENO_S99`)
> y con ella se desbloqueó la construcción. Lo que sigue **corrige mi
> propio censo**: la medición de más abajo describía **la rama que nadie
> corría**.

**🔴 EL ERROR PROPIO, PRIMERO, PORQUE CAMBIA EL DIAGNÓSTICO ENTERO.**
Reporté que el contorno medía 1.30/1.18 — pero **`Campo` nacía con
`sinCaja = true` POR DEFAULT**: borde **transparente** en reposo y el
relleno como única señal. *Leí el código y tomé por vivo el brazo del
`else`.* **El estado real era peor y más simple:**

| | contorno | interior contra el fondo |
|---|---|---|
| light | **TRANSPARENTE** | **1.07:1** |
| dark | **TRANSPARENTE** | **1.17:1** |

*Un campo que se distingue de su fondo por 1.07 no se distingue.* ⇒ **un
default decide qué rama corre, y una rama que no corre no describe el
producto.**

**🔴 Y EL CHOQUE CONTRA LETRA FIRMADA, DECLARADO Y NO RESUELTO CALLADO**
(precedente: el magenta S83, la plata S83 y S88). `sinCaja = true` era
**decisión FIRMADA en S81**, y las dos letras citan **la 19.8** leyéndola
al revés:

- **S81:** *«A6 ALCANZA a Campo… la affordance la da el RELLENO»*
- **N11 (S99, firma del founder):** *«☠️ el relleno gris sólido muere… es
  la ley 19.8 aplicada al formulario: **se contornea lo que se fija** —
  un campo vacío todavía no existe, por eso no se rellena»*

**Gana N11**: más nueva, firma de esta sesión, y con su razón escrita.
**Costo de la derogación: CERO consumidores** — todos los `sinCaja` del
árbol son de `Boton`, otra prop de otra pieza.

> ⚠️ **PRECISIÓN (de A, al registrarlo para el acta): la 19.8 NO MUERE —
> N11 es la 19.8 BIEN APLICADA.** Lo escribo porque *«las dos la leen al
> revés»* se puede leer como que la ley quedó en disputa, y no quedó: lo
> que muere es **la lectura de S81**, no la ley que citaba. *Y el dato
> que lo cierra, medido por A: la letra de S81 **no vivía en ningún doc
> maestro** — vivía en el default mismo. Por eso su lápida va en la
> pieza y no en un canónico: está donde estaba la decisión.*

**LO CONSTRUIDO:**

**① Nace `caja-de-campo.ts`** — la anatomía escrita UNA vez, consumida
por las tres piezas. **No es prolijidad: es la ley.** N11 dice *«dos
estilos de campo jamás conviven en la misma región»*, y aplicarla a
`Campo` dejando atrás a `CampoCodigo` y `CampoFecha` **fabricaría los dos
estilos que la ley prohíbe**.

**② Nace `border.campo`** en los tres temas, y es token nuevo **porque
ninguno de la casa llegaba** — medido: `border.default` **1.18** (claro) /
**1.28** (oscuro) · `border.presente` **1.62** / **1.64**. *No estaban
mal: se diseñaron para SEPARAR, y N11 pide CONTENER.* Los tres aterrizan
a propósito en **3.34 · 3.40 · 3.34**, con margen sobre el piso: *esta
casa ya se quemó con un par «al filo por redondeo», y un piso que se
aprueba por el tercer decimal no es un piso.*

**③ La etiqueta entró a la caja** (N11) en `Campo` y `CampoFecha`, con el
alto **DERIVADO** (aire + etiqueta + línea + aire + bordes = **62**),
calculado y no tecleado — quien mueva la escala de N1 no tiene que
acordarse de mover esto. **`CampoCodigo` conserva su label arriba y sus
48 con su razón escrita:** una caja de UN dígito no tiene lugar para un
rótulo, y ponerlo repetiría el mismo texto seis veces.

**④ El foco gana presencia** (borde en acento + `elevacion.reposo`), sin
tocar el grosor: un borde que engorda **corre el layout mientras alguien
tipea**, que es la regla rectora de la pieza desde S43.

**⑤ Nace R43** — el piso de N11 mecanizado del lado del token. Vigila lo
que un ojo no ve: *el día que alguien afine el borde «un poquito más
suave» porque le parece fuerte, el campo sigue **viéndose**; solo deja de
cumplir.*

**⑥ El gate WCAG sube a 341 pares** con el contorno y la etiqueta en su
vecino nuevo — **y de paso corrigió DOS cosas suyas que N11 destapó:**
su exención del borde de reposo se justificaba con *«el campo se
identifica por su label siempre visible»* (cierto con el label afuera,
falso ahora), y **`bgCampo` medía `bg.elevated` cuando el interior real
es `bg.overlay`** — misma clase que el par del avatar en S98: el gate
midiendo un token del que la pieza ya se había ido.

> **⚠️ ESTO LANDA EN 121 CAMPOS A LA VEZ Y SU GATE ES UN OJO EN PANTALLA
> REAL.** No hay migración por pantalla que amortigüe: el censo probó que
> **cero campos usan `TextInput` crudo**, así que cambiar la pieza ES la
> migración. Los instrumentos dicen que los números cumplen; **ninguno
> puede decir si un formulario de seis campos con contorno a 3:1 respira
> o se ve enrejado.** Va al gate del lote con esa pregunta.

### 🟡 EL CENSO ORIGINAL — conservado, con su corrección arriba

**La disciplina de la casa es perfecta y eso cambia el trabajo:**

- **96 `<Campo>` en 35 pantallas del prestador** · **25 en el cliente**
- **CERO `<TextInput>` crudo en las dos apps**

⇒ **No hay «migración por pantalla»: no hay nada que migrar.** Se arregla
la pieza y los 121 campos se mueven solos.

**Lo que hoy incumple N11, medido** (instrumento:
`scripts/medir-s99b-n11-campo.mjs`):

| pata de N11 | hoy |
|---|---|
| contorno ≥3:1 | 🔴 **1.30:1 en claro · 1.18:1 en oscuro** |
| etiqueta adentro de la caja | 🔴 va **ARRIBA** de la caja |
| interior claro | el interior es un **relleno** (`bg.card`/`bg.elevated`) |
| foco con presencia | cambio de **color** de un borde de 1.5 |

*Con el contorno en 1.18–1.30, el borde es invisible y **el relleno hace
todo el trabajo de decir «acá hay un campo»** — que es exactamente el
«fondo relleno» que N11 viene a matar.*

> 🔴 **Y EL HALLAZGO QUE CAMBIA EL PLAN: la anatomía de la caja está
> escrita TRES VECES dentro de `packages/ui`.** `Campo`, `CampoCodigo` y
> `CampoFecha` repiten los mismos literales (`BORDE = 1.5` ·
> `theme.mode === 'light' ? theme.bg.card : theme.bg.elevated` · la misma
> transición). **Aplicar N11 solo a `Campo` dejaría dos piezas
> divergiendo** — la enfermedad exacta que R25/R30 existen para cazar.
> ⇒ **La cura empieza extrayendo la caja**, y después N11 entra una vez.

**⏪ ESTA LÍNEA DECÍA «POR QUÉ NO CONSTRUÍ: N11 no está depositada».**
Se conserva su razón porque sigue siendo la correcta —*escribir la forma
nueva de la primitiva de campo de toda la casa contra una letra que no
existe es trabajo que después nadie puede auditar contra nada*— y **la
letra aterrizó a mitad de tanda**: la construcción está arriba.

### ✅ R45 · D-828 — el lector de rango no se consume en silencio

**Pedido de mesa, y es S72 repetida ANTES de ocurrir.** La cita aprobada
sin fecha no pasaba el `.gte` de su lector ⇒ el dueño aprobaba un
procedimiento que **no existía en ninguna superficie suya**. El motor
estaba sano; la falla era **invisibilidad**. Acá la forma es idéntica:
los pedidos vivos sin día viajan en `sinFecha`, cada superficie los
excluye por una razón localmente buena, y **nadie es dueño de la suma.**

**NO hay exención por path, y el argumento es de D:** un path *«deja
pasar en silencio la segunda consumición que alguien agregue al mismo
archivo»*, y choca con la ley de la casa —*una exención sin condición de
muerte es un permiso permanente*—. ⇒ **la declaración ES la exención**:
vive en el archivo y no hay lista que se oxide.

**🔴 LA INVERSIÓN DELIBERADA, escrita para que nadie la «arregle»:** el
**consumo** se mide **sin** comentarios; la **declaración**, **con**
ellos. Es al revés de L-170, y tiene que serlo: midiendo el consumo con
comentarios **el propio JSDoc del wrapper saldría rojo**; midiendo la
declaración sin ellos, **la salida legítima sería inexpresable** —
declarar quién los monta *es* prosa.

**Baseline 0, y se puede: medido, CERO superficies lo consumen hoy.** La
regla **nació antes que su primer consumidor** — la única vez que un
ratchet arranca sin deuda. **Y por eso el fixture carga el peso entero**
(L-236): no había legítima real que eximir, así que las dos son
sintéticas y a propósito —una que MONTA y otra que DECLARA—. *Una regla
nacida sin contra-caso no sabe discriminar; solo sabe disparar.*
Discriminada en cinco corridas: el rojo sale por la muda, las dos
legítimas pasan, y la frontera no se atrapa a sí misma.

> **⚙️ PARA QUIEN MIGRE (va acá porque el canal entre sesiones se cerró y
> no se lo pude decir a D por mensaje):** al lint **le alcanza con que el
> archivo MENCIONE `sinFecha`** — un comentario sirve. Lo que el lint
> **no** puede juzgar es la segunda frase, la que L-237 hace exigible:
> **«los monta ⟨quién⟩»**. Sin ella el archivo pasa el lint y la omisión
> sigue siendo un olvido que todavía no se nota.
>
> ⚠️ **Y el hueco residual, declarado y no tapado:** una **segunda**
> consumición con otro propósito en un archivo que ya declaró **hereda la
> declaración vieja**. Estáticamente no lo puedo distinguir — se dice en
> vez de fingir que el net lo cubre.

### ✅ B4 (cerrada) · EL JUICIO SOBRE N11–N20, UNA POR UNA

Con la letra completa en la mano, **el juicio de qué se mecaniza y qué
queda de ojo — con la razón, no con un «se puede/no se puede»**:

| ley | veredicto | por qué |
|---|---|---|
| **N11** el campo | ✅ **R43** | su cumplimiento **es un número del tema**: se mecaniza sola |
| **N12.4** el error que no dice qué | ✅ **R44** | es una VOZ, y las voces son grepeables. **Baseline 9, ensanchado el mismo día por el cruce de C** — ver abajo |
| N12.1/.2/.3/.5 | 👁️ ojo | validar contra la realidad del país, el teclado por tipo y validar-al-salir son **corrección de lógica**, no forma. Un lint los adivinaría por el nombre del campo y gritaría donde no hay delito — *R25 ya escribió que una regla así se desactiva sola en la cabeza de quien la lee* |
| N13 la IA en silencio | 👁️ ojo | las tres cubetas de confianza son **decisión de datos en runtime**; nada estático las distingue |
| N14 el mapa con identidad | 🧩 **por PIEZA** | `PinEnMapa` la vuelve inexpresable de otro modo, y R17 la obliga a estar mirable. **Mejor que un lint** |
| N15 la dosis del movimiento | ⚖️ **ya cubierta a medias** | su mitad exigible —*toda pieza declara su conducta reduce-motion al nacer*— **es R41**. La otra mitad (la DOSIS por contexto) es juicio |
| **N16.2** el spinner muere | ⚖️ **medido en 0, sin regla** | *ver abajo* |
| N16.1/.3/.4 | 👁️ **aparato** | su gate es un número en teléfono real (<1 s / <2 s), no un lint. La línea base es de A |
| N17 el espejo | 🧩 **por PIEZA** | `FichaPrestador` con dos consumidores ya lo hace inexpresable |
| N18 completitud sin ranking | 👁️ ojo, **por ahora** | su prohibición (posición, percentil, comparación) **es mecanizable el día que exista la superficie**; hoy no hay a qué apuntar, y *un glifo que nadie va a montar no se pide* aplica igual a un lint |
| N19 el orden de la ficha | 👁️ ojo | el ORDEN de seis bloques no se lee estáticamente sin fabricar falsos |
| N20 dos toques | 👁️ ojo | es profundidad de navegación: se mide caminando |

**🔴 R44 SE ENSANCHÓ EN LA MISMA SESIÓN, Y LAS DOS MITADES DEL CRUCE
IMPORTAN.** Literal de C: *«su regla encontró en mi territorio una deuda
con dueño; mi medición encontró que su regla mira la mitad del
problema.»*

**Tenía razón en el corpus:** N12.4 dice *«en toda la casa»* y R44 nació
mirando **solo los seis diccionarios**. La misma voz vive también en
`packages/api`, **hard-codeada en español en un paquete sin capa de
idioma** (D-539) y mostrada directo por 34 archivos del prestador ⇒ **en
inglés se ven en español**. *Una regla cuyo nombre promete «toda la casa»
y cuyo corpus es la mitad está verde porque no mira.*

**⚠️ Y al ensanchar, mi net sobre-capturaba: el cruce se sirvió con CINCO
de `packages/api` y son TRES.** Los otros dos **cumplen N12.4** — *«Una
de las vacunas del carnet no es válida…»* y *«El recorrido llegó con
datos inválidos»* **nombran la falla**. Blanquearlos en un baseline los
habría bendecido para siempre: **el mismo daño que el subconteo, del otro
lado.**

⇒ **El discriminador, que es lo que la regla aprendió:** el defecto no es
contener «revisá» — es que **NADA diga qué falló ANTES**. El net ancla al
arranque de la frase; si hay una oración previa que nombra la falla, el
mensaje cumple. **Calibrado contra los 16 casos reales de las dos casas:
16/16.**

**Y el baseline queda partido por dueño Y por curabilidad**, porque no son
la misma deuda: **6 de `apps/prestador`** (dueño C, **curables
escribiendo mejor**) + **3 de `packages/api`** (dueño A, **BLOQUEADAS**:
`datos_invalidos` es el fallback de *cualquier* CHECK — la voz buena
exige códigos tipados por constraint, no una frase mejor). *Un baseline
que mezcla lo curable con lo bloqueado miente sobre qué está pendiente.*

**⏬ LA PARTICIÓN POR DUEÑO Y CURABILIDAD, APLICADA HACIA ATRÁS (orden de
mesa) — y las dos mitades salieron distinto:**

**R42 · SÍ, y apareció una TERCERA clase que la orden no previó**, porque
el objeto la tenía: **una entrada que NO ES DEUDA y jamás va a bajar.**
`EvidenciaFoto` no es una puerta a mano esperando migrar: es **otra
interacción** (cámara DIRECTA, galería detrás de un mantenido).
*Contarla como deuda miente en el otro sentido: promete un 0 que sería un
error alcanzar.* ⇒ **el piso real de R42 es 1, no 0**, y ahora lo dice.
Reparto vivo: **8 CURABLES · 1 BLOQUEADA (gate de forma, S45) · 1
LEGÍTIMA**, cada una con su razón POR ENTRADA — *para que nadie le pida a
la persona equivocada que cure lo que no es suyo ni se cura escribiendo.*

**R43 · NO APLICA, y se declara en vez de fabricarse.** R42 y R44 llevan
**un baseline: una lista de cosas PARADAS**, y ahí la mentira es real.
**R43 no tiene baseline** — sus tres números *son la medición de que las
tres casas CUMPLEN*. No hay entrada pendiente, dueño ajeno ni nada
bloqueado; si alguno bajara del piso, la regla se pone roja ese día en
vez de estacionarse en una lista. *Marcar como deuda un número que mide
cumplimiento sería la misma clase de error que la orden viene a curar, al
revés.* Precedente: **cuatro de los ocho choques del censo de S94-A no
eran letra obsoleta, y marcarlos habría sido peor que dejarlos.**

**🟡 Y EL CENSO QUE LA ORDEN NO PIDIÓ, servido con su número: SIETE
reglas más cargan la misma mentira latente** — `R12` · `R24` · **`R36`
(20) · `R37` (3) · `R38` (6) · `R39` (6)** · `R40` (1). Las cuatro del
medio son **las del Norte**, y son donde más muerde: **N1–N4 son leyes
firmadas con una dirección**, así que su número parado es exactamente el
que hay que poder leer como *«nadie lo tocó»* o *«nadie PUEDE»*. Las
demás están en cero o vacías (nada parado) y `EXENTOS_R17` **ya declara
razón por entrada** desde que nació. **La barrida es de una tanda y no la
hago sin pedirla: atribuir sus entradas exige enumerar los ofensores de
cada una, que es medición, no edición.**

**🟡 N16.2 · POR QUÉ NO ESCRIBÍ SU REGLA, que es la decisión más
discutible de esta tabla y por eso va con su razón:** medí que el spinner
**ya está en 0** en las pantallas de las dos apps, y un ratchet desde 0
sería casi gratis. **No la escribí porque su net honesto es ajeno:** los
tres `ActivityIndicator` que existen viven en `packages/ui` y son
**legítimos** (controles ocupados), así que la regla tendría que mirar
solo `apps/` y exentar `ui` — y el día que una pieza nueva necesite uno,
la exención se decide sola sin que nadie la mire. **Queda declarada como
candidata con su net propuesto**, no escrita a medias.

### ⏪ EL PLAN ORIGINAL DE B4 — conservado

**R42 nació** (arriba). **La mecanización de N11 espera su letra** — y es
la candidata natural: *«el contorno de un campo mide ≥3:1 contra su
interior»* es un número, y un número se mecaniza. El instrumento de
medición ya está escrito y da rojo hoy: **cuando la letra llegue, pasa de
medidor a gate cambiando su `process.exitCode`.**

De las demás, mi lectura previa sin la letra: **N14** (pins) y **N17**
(espejo) son de FORMA y se vigilan mejor por pieza única + R17 que por
regla nueva. **Declaro que no puedo comprometer cuáles hasta leerlas.**

### ⚪ B6 · HÁPTICA — **NO prototipada, y digo por qué**

Le veo valor real y acotado (confirmaciones donde el usuario deja de
mirar la pantalla: «entregado», «cobrado»). **No la prototipé porque su
único gate posible es el dedo del founder** y esta tanda no tuvo
dispositivo: una háptica evaluada en RN-web es evaluada en su ausencia.
Queda como candidata con su forma propuesta —enmienda de primitiva
(`Boton`/`usePresionado`), jamás llamada suelta por pantalla— para entrar
por gate cuando haya aparato.

---

## §4 · SERVIDO A LA MESA (medido, sin curar)

### 🟡 EL «BEZIER DE LA CASA» DE N10 NO ES EL QUE USA LA CASA

**N10 firmada dice** que el bezier de la casa es `(.32,.72,0,1)` y que
gobierna *«entradas y transiciones»*. **Medido:**

- `motion.easing.easeOut` = `[0, 0, 0.2, 1]` → **11 piezas**, entre ellas
  **`Entrada`** (el portador de la entrada de toda la app, `Entrada.tsx:64`),
  `Hoja`, `BarraTabs`, `Celda`, `Campo`, `SelectorSegmentado`…
- `motion.marca.aperturaBezier` = `[.32,.72,0,1]` → **4 piezas** (`Hoja`,
  `Destape`, `PuertaDeOficio`, + 1 de apps).

⇒ **La letra firmada nombra una curva que las entradas de la casa no
usan.** Es la misma familia que D-795/N6 (*el Norte importando un número
que la práctica ya había movido*), y por eso **no toqué 11 piezas: lo
sirvo con su medición para que la mesa mida DE DÓNDE SALIÓ el número, no
vote cuál gusta más**. Mi pieza nueva (`PinEnMapa`) usa **el de la letra**
— si la mesa falla que la curva real es `easeOut`, cambia una línea en mi
pieza en vez de once en las suyas.

**Menor, misma familia:** el bezier que N10 llama «de la casa» vive
archivado bajo **`motion.marca`**. Una pieza que escribe una entrada no
debería entrar a `marca` para encontrar la curva general. Renombrarlo toca
consumidores ⇒ es de mesa.

### 🟡 HEREDADO DE S98-B, SIGUE VIVO

`DIRECCION_ARTE` v1.7 sigue contradiciendo al código sobre la
reclasificación del par del avatar (reportado a A, su territorio) · los
27 avisos de clave duplicada en la galería del CLIENTE (preexistentes,
higiene de instrumento) · la `Hoja` sigue deslizando en la **vuelta del
arrastre** bajo reduce-motion (deliberado, con su razón escrita).

---

## §5 · INSTRUMENTOS — medidos al cierre, no de memoria

| Instrumento | Estado |
|---|---|
| **`verify:diseno`** | **VERDE · 38 auto-pruebas** (abrió en 33) |
| **R46** *(nace acá)* | **el selector de indicativo no se va con el campo que muere** — 1 pantalla pide el WhatsApp del repartidor, con selector · **dura en 0** · ancla ≥1. **Su rojo se discriminó por MUTACIÓN**: sacando el caso malo del fixture la regla queda VERDE ⇒ el rojo venía del caso, no del ancla, **y las dos legítimas no disparan** (el mecanismo genérico solo prueba que salga roja, no por qué) |
| **R43** *(nace acá)* | N11: `light 3.34 · dark 3.40 · memorial 3.34` — piso 3:1, con ancla de tres casas |
| **R42** *(nace acá)* | 10 puertas a mano · baseline 10 solo-baja · ancla viva |
| **R41** | **9 piezas mueven · 9 declaran el hook** (era 8/8: `PinEnMapa` entró cubierta) |
| **R17** | `exportaciones=91 · en-galería=82 · exentas=9 · pendientes=0` |
| **R4** | 0 sombras artesanales *(me cazó una en la primera corrida — curada en la fuente, no en la regla)* |
| **WCAG** | **341 pares · 0 fallos** — +10 por el contorno y la etiqueta de N11 en su vecino nuevo. El más ajustado: **dark·oficio 3.10** |
| **tsc `packages/ui`** | **0** |
| **`verify-i18n`** | 9 fallos **PREEXISTENTES** — discriminado corriendo el MISMO script sobre `main` sin tocar: conjuntos de fallos **idénticos** |
| `medir-s99b-n11-campo.mjs` *(nace)* | mide, no gatea |
| `medir-s99b-imagenes-sin-medida.mjs` *(nace)* | mide, no gatea |

---

## §6 · LO QUE PIDO / LO QUE DEBO

**A C y D (cuando su lote toque el archivo):** cambiar la hoja de captura
a mano por `<HojaCaptura>` y **bajar el baseline de R42 en el mismo
commit**. Ocho archivos, listados en §1.

**✅ A / la mesa — DESBLOQUEADO:** `DIRECCION_DISENO_S99.md` llegó a main
a mitad de tanda. **B3 construida · N11 mecanizada (R43).** Lo que sigue
esperando letra son las demás mecanizaciones de N12–N20, y ahí mi lectura
es que **N16 es la próxima candidata** (tiene número: <1 s / <2 s) —
aunque su gate no es un lint sino el aparato.

**A la mesa:** la divergencia del bezier (§4). Un voto, once piezas o una.

**🟡 CANDIDATO DE OJO PARA EL GATE DEL LOTE (aviso de D, mi llamada — y NO es un rojo):**
el vendedor puro va a ver una **barra de cinco con `ATENDER` destacada y
una sola baldosa adentro**. **No tengo ley que nombrar** —`§2.1bis` dice
explícito que su `ATENDER` es una sola cosa y está bien que lo sea— así
que **no es rojo**. Pero la pregunta que sí sé formular es de forma:
**la `destacada` es el centro de gravedad de la barra, y una destacada
promete una magnitud que el cuarto tiene que sostener.** Lo que hay que
mirar en el gate no es «¿está mal?» sino **«¿esa destacada se lee como
*esto es lo importante* o como *esto está vacío*?»**. *Un ojo con una
pregunta concreta ve algo; un ojo con una sensación vaga aprueba
cualquier cosa.* Palanca barata si chirría, para que la mesa la tenga a
mano: la `destacada` es **por FORMA en el ítem**, así que puede
condicionarse a la población sin tocar la pieza — pero eso es decisión,
no cura, y no la tomo yo.

**Debo:** la hoja de contacto del glifo `moto` (con su gate por ícono) ·
la háptica si la mesa la quiere con aparato · y **mirar los 121 campos
nuevos en pantalla real** — es lo único de N11 que ningún instrumento
puede firmar.

---

**Medido al cierre:** `verify:diseno` VERDE 35 · WCAG 341/0 · tsc de los cuatro paquetes 0 ·
árbol de `pista/s99-b` con solo mis rutas · **cero archivos de `apps/`
tocados**.

---

## LA BARRA — LA CAUSA RAÍZ DE OCHO GATES, Y LA S CONSTRUIDA

**Lo que ninguno de los ocho gates podía encontrar, porque no era
calibración:** el hombro eran **50 px dead-flat en `y = 0`** y después un
**arco de circunferencia**. Un plano tiene curvatura cero; un arco la
tiene de un solo signo. ⇒ ***un plano y un arco no producen un cambio de
signo: producen un CODO.*** Lo único que se salía de la línea era la
joroba de arriba. **La S era inconstruible con esa anatomía, hicieran lo
que hicieran con los números** — y por eso cada vuelta movía valores y
volvía el mismo veredicto.

**Y la joroba era la CUARTA traducción del mismo gesto** —saliente ·
joroba · montaña · cresta—. Las cuatro mandaron a construir un bulto que
la referencia **no tiene**: cero columnas por encima del plano.

### Lo construido

- **Muere el hombro plano y muere la joroba.** El hombro es **UNA SOLA
  CÚBICA del plano al arco**: sale horizontal, se empina y entrega al
  arco **con su misma tangente**. Convexa al salir, cóncava al entrar,
  **sin salirse jamás de la línea**.
- **Anillo 10 → 9** y el disco **se hunde 1 px más** (`DISCO_ASOMA` es
  derivado: 6,5 → 5,5).
- **El viaje es ASIMETRÍA DEL VALLE, no un bulto:** el hombro de adelante
  se angosta y se empina, el de atrás se ensancha. Mismo modelo de la
  piedra sobre la tela; el desplazamiento se lee en la forma.

### Los tres números, medidos SOBRE LO CONSTRUIDO

Instrumento: `/tmp/medir-barra.mjs` — **extrae `pathBarra` del archivo
real y la evalúa**, no reimplementa.

| | reposo | en viaje | tab del extremo |
|---|---|---|---|
| columnas sobre el plano | **0** | **0** | **0** |
| profundidad/ancho | **1,10** | 1,11 | 1,65 (clampeado) |
| inflexión | **35 % ancho · 50 % profundidad** | 45 % · 50 % | 52 % · 50 % |
| anillo mínimo | 9,00 | 2,12 | 8,97 |

Vara: ratio **1,08**. Antes: **0,83** — un plato ancho y poco profundo.

### 🔴 DOS DEFECTOS QUE ENCONTRÓ EL INSTRUMENTO, NO EL OJO

1. **El retraso del valle le mordía el disco.** El valle va detrás del
   disco por 12 px y el anillo mide 9 ⇒ **el anillo daba −2,78 px EN
   VIAJE**: la barra encima del disco. *Con el anillo de 10 ya mordía 2 px
   y nadie lo había medido — no lo rompió bajar el anillo, lo destapó
   medirlo.* Curado derivando el retraso de `ANILLO − 2`: **el defecto
   pasa a ser inexpresable.**
2. **En el tab del extremo el path se dibujaba al revés.** El clamp del
   disco (40 px) no conocía el valle nuevo, así que el disco entraba y su
   valle no: `hombroI` terminaba **a la derecha** del punto de entrega.
   Curado con `margenDisco = RADIO_BARRA + VALLE_SEMI` — **entra el disco
   Y su valle, o no entra ninguno.**

### 🔴 EL PISO DEL 50 % QUEDA ESCRITO EN LA PIEZA

La inflexión vive **en el encuentro hombro↔arco**, y ese encuentro **no
puede pasar del ecuador del disco**: arriba del ecuador el arco viaja
hacia el otro lado y el borde tendría que doblarse sobre sí mismo. ⇒
**50 % es piso duro del anillo uniforme, no una elección.**

**Lo que eso prohíbe, para el que venga:** no se baja tocando el texto ni
achicando el disco — **el porcentaje escala con el disco** (uno más chico
da la misma inflexión al 50 %, de un valle más chico) y `xs = 11` ya es el
piso de la escala cerrada. **La única palanca real es el anillo** —podría
dejar de ser exactamente uniforme *solo en el arranque del hombro*—, **y
es firma del founder.**

**Servido:** `docs/laminas/2026-08-18-s99b-comparacion-barra.png` — cuatro
paneles al mismo tamaño sobre el verde del oficio: la vara · reposo ·
viaje · extremo.

**Declarado y no curado:** en el tab del extremo el hombro queda comido
por la esquina redondeada (ratio 1,65). Es degradación acotada —el anillo
aguanta en 8,97— y darle hombro exigiría correr el disco más adentro de
su tab. **No lo decido yo.**

**Medido al cierre:** `verify:diseno` **VERDE 40** · WCAG **368/0** · tsc
de los cuatro paquetes **0** · solo mis rutas.

---

## LAS ESQUINAS — LA BARRA A SANGRE Y EL CLAMP RE-MEDIDO

**El descarte de «a sangre» se re-corrió y estaba vencido, tal como la
mesa sospechaba:** se hizo con el valle viejo (semiancho 43). El valle de
hoy mide **41,4**, y además **el descarte medía la variable equivocada** —
el tab se medía sobre el ancho de la barra, y a sangre eso empuja el
primer centro HACIA el borde, o sea que el clamp habría tirado MÁS.

### ① La barra a sangre — construida

`MARGEN_BARRA` (margen del CUERPO) → **`INSET_CONTENIDO`** (inset de los
TABS): mismo número, otro trabajo. El cuerpo pinta **de borde a borde y
baja hasta el filo inferior** (el inset de abajo deja de ser margen y pasa
a ser cuerpo pintado). **Esquinas de arriba redondeadas, las de abajo a
escuadra** — una esquina redondeada tocando el filo se lee como error de
recorte. El inset del contenido vive en una **fila interna**, no como
padding del contenedor: un hijo absoluto de una caja con padding se
posiciona distinto según la versión de Yoga y el lienzo del vector cuelga
de `left: 0`.

### ② El clamp — RE-MEDIDO, y muere en 3 y 4 tabs

`margenDisco = rc + VALLE_SEMI = 61,4`. Con el tab medido **sobre el
contenido** (no sobre la barra), el centro del primer tab sube:

| tabs | centro del 1er tab | corrido por el clamp |
|---|---|---|
| 3 | 76,2 | **0,0 — CLAMP MUERTO** |
| 4 *(cliente y el prestador típico)* | 61,1 | **0,2 — CLAMP MUERTO** |
| 5 | 52,1 | 9,3 *(antes 25,3)* |

**③ se cura solo:** los íconos seguían al disco corrido, y el disco ya no
se corre.

### 🔴 EL TERCER SÍNTOMA: DOS HIPÓTESIS CAEN Y LA TERCERA NO ES LA QUE PARECÍA

Medido el hueco **por dirección** alrededor del disco: **izquierda 9,1 ·
diagonal 9,1 · abajo 9,0**. ⇒ **el anillo ES uniforme.**

- ⛔ *«el hombro no llega al borde de la barra»* — **falso**, el hueco no
  crece hacia la izquierda.
- ⛔ *«la esquina redondeada come el hombro y lo que queda es el radio»* —
  **falso como causa del hueco**: el hombro sí se come (abajo), pero el
  anillo no se mueve de 9.
- ✅ **Lo que el founder ve NO es el anillo: es la BARRA que sigue
  existiendo entre el valle y el filo de la pantalla.** En el primer tab
  son **19,4 px de blanco** después del anillo de 9. Junto se lee como un
  anillo de 28 de un lado contra 9 arriba.

**Y no lo cura ir a sangre ni matar el clamp** — el disco está en el
centro de su tab y ese centro está a 61 px del filo. **La única palanca es
que el disco se siente más cerca del borde**, y su precio está medido:

| para bajar el blanco a ~16 px | costo |
|---|---|
| `INSET_CONTENIDO` 16 → 0 | el clamp vuelve a disparar 12,2 px |
| `INSET` 0 **+** valle más angosto (θ ≥ 118°) | la inflexión cae de 50 % a **68 %** de la profundidad |
| `rc` 20 → 12 **+** `INSET` 8 | blanco 22 px · clamp muerto · inflexión intacta |

**No lo decido yo: las tres cambian una firma.**

### Los tres números — sobre lo construido, a sangre, 4 tabs

| | reposo | en viaje | **tab del extremo** |
|---|---|---|---|
| columnas sobre el plano | **0** | **0** | **0** |
| profundidad/ancho | **1,10** | 1,11 | 1,39 |
| inflexión | 35 % · 50 % | 45 % · 50 % | 0 % · 41 % |
| anillo | 9,00 | 2,12 | **9,0–9,1 uniforme** |

**⚠️ Y el precio de matar el clamp, declarado:** en el tab del extremo el
hombro pasa a **0 px** — el valle arranca en la esquina redondeada. **No
se invierte** (era el defecto de la vuelta anterior) y el anillo aguanta,
pero *la S ahí no se lee: se lee el radio de la esquina*. Es exactamente
el acoplamiento inverso — **darle hombro al extremo es volver a correr el
disco**, y el founder pidió lo contrario.

**Medido al cierre:** `verify:diseno` **VERDE 40** · WCAG **368/0** · tsc
de los cuatro paquetes **0**.

---

## LA TERCERA PALANCA, PUESTA — `rc` 20 → 12 · `INSET` 16 → 8

Firma del founder sobre mi propia tabla. **Cero números nuevos:**
`radius.md = 12` y `spacing[2] = 8` son escalones de las escalas cerradas.

**Y la razón por la que ESTA palanca y no otra, escrita donde se lee:**
`margenDisco = rc + VALLE_SEMI` ⇒ **bajar `rc` acerca el disco al borde
sin tocar el valle**, o sea sin mover la inflexión. Las otras dos se
descartaron con número: `INSET` a 0 revive el clamp (12,2 px) · angostar
el valle empuja la inflexión de 50 % a 68 %.

⏪ **Y la nota vieja de `RADIO_BARRA` estaba vencida:** decía *«`radius.xl`
es el escalón que la casa usa para superficies grandes APOYADAS»* — y esa
razón caducó el día que la barra dejó de estar apoyada. *Con el cuerpo
tocando los cuatro bordes, el radio de arriba ya no describe una
superficie flotante: es el remate del filo.*

### Lo medido, antes y después

| | rc 20 · inset 16 | **rc 12 · inset 8** |
|---|---|---|
| blanco entre disco y filo (4 tabs) | 28,4 | **22,1** |
| corrido del disco · 3 tabs | 0,0 | **0,0** |
| corrido del disco · 4 tabs | 0,2 | **0,0** |
| corrido del disco · 5 tabs | 9,3 | **7,7** |
| hueco izq · diagonal · abajo | 9,1 · 9,1 · 9,0 | **9,2 · 9,1 · 9,0** |

### 🔴 EL EXTREMO SÍ SE ALIVIA — Y MÁS DE LO QUE DICE EL HOMBRO SOLO

Lo declarado sin curar era: *«el hombro pasa a 0 px y la S ahí se lee como
el radio de la esquina»*. **Medido, no supuesto:**

| tab del extremo (4 tabs) | rc 20 | **rc 12** |
|---|---|---|
| hombro propio | 0,0 px | **1,8 px** |
| profundidad/ancho | 1,39 | **1,26** |
| inflexión | 0 % del ancho | **25 % del ancho** |

**El hombro crece apenas 1,8 px y sin embargo el perfil mejora mucho más,
y la razón es la que la mesa intuyó: hay MENOS RADIO comiéndoselo.** La
pared del extremo pasa de ser *casi toda* esquina a ser *casi toda* curva
—25 % de inflexión contra 0 %—, y queda a un paso del reposo (35 %).
*El hombro medía el trozo equivocado: lo que importaba no era cuánto
sobra, era cuánto se come el radio.*

### Los tres números — geometría final, 4 tabs

| | reposo | en viaje | tab del extremo |
|---|---|---|---|
| columnas sobre el plano | **0** | **0** | **0** |
| profundidad/ancho | **1,10** | 1,11 | **1,26** |
| inflexión | 35 % · 50 % | 45 % · 50 % | **25 % · 50 %** |
| anillo | 9,00 | 2,12 | 8,96 |

Vara: 1,08. **La inflexión se mantiene en 50 % en los tres** — que es el
piso duro del anillo uniforme, y por eso esta palanca no lo tocó.

**Medido al cierre:** `verify:diseno` **VERDE 40** · WCAG **368/0** · tsc
de los cuatro paquetes **0** · solo mis rutas.

---

# 🔷 HANDOFF DE B — S99

## ✅ CERRADO, VERIFICADO CONTRA EL OBJETO (no declarado)

```
HEAD                          422bd705…
git merge-base --is-ancestor HEAD origin/main   → exit 0   ✅
git ls-remote origin pista/s99-b                → 422bd705…  ✅ mismo sha
BarraTabs.tsx  blob mío   92aa4483…
               blob main  92aa4483…              ✅ MISMO BLOB
git status --porcelain                          → vacío     ✅ árbol en cero
```

**Por CONTENIDO y no por ref** (regla 84 ②): el blob de mi pieza en `main`
es el mismo objeto que el mío, así que no hay «mergeado» que dependa de a
dónde apunte una rama. A mergeó las cuatro tandas (`141a372d` · `0770b967`
· `503e490d` · `ea682858`).

**Jueces al cierre:** `verify:diseno` **VERDE, 40 auto-pruebas** · WCAG
**368 pares / 0 fallos** · `tsc` **0** en los cuatro paquetes.

## 🧭 COLA VIVA — en orden, con el próximo paso ejecutable

1. **🔴 LA BARRA ESPERA UN OJO, NO UNA MEDICIÓN.** Todo lo medible está
   medido y verde. **El próximo paso es de A: bundlear sobre `ea682858` o
   posterior** — el founder frenó el OTA para no gastarlo en una versión
   intermedia, y esta ES la final. Lo que el ojo tiene que juzgar y ningún
   instrumento puede: **el tab del extremo** (su pared es 25 % curva y el
   resto esquina) y **los 22 px de blanco entre el disco y el filo**, que
   es lo único del tercer síntoma que no se fue.
2. **La hoja de contacto del glifo `moto`**, con su gate POR ÍCONO a 21px
   (§6b). Es mía y no bloquea a nadie.
3. **Mirar los 121 campos nuevos en pantalla real** — lo único de N11 que
   ningún instrumento firma.
4. **La háptica**, solo si la mesa la quiere y hay aparato.

## 🤝 COMPROMISOS ABIERTOS, PISTA POR PISTA

- **CON A:** ~~le debo la declaración de R42/R43~~ **PAGADA y está en este
  parte** (§«R42 · SÍ, y apareció una TERCERA clase que la orden no
  previó» y §«R43 · NO APLICA, y se declara en vez de fabricarse»). El
  resultado, para que no haya que leer todo: **R42 tiene piso 1, no 0** —
  hay una puerta legítima que no es deuda— y **R43 no lleva baseline
  porque sus tres números SON la medición**. A me debe el merge de cada
  cierre (rutina, ya cumplida) y **D-827** — sin códigos tipados por
  constraint, las 3 líneas BLOQUEADAS de **R44** no pueden bajar y su
  baseline queda clavado en 3 por diseño, no por olvido.
- **CON C:** no le debo nada abierto. Le entregué `Fundido`,
  `EntradaDeCruce`, `Chevron`, `Baldosa`, los dos dictámenes que la
  frenaban (**los dos segmentados anidados** → el del espejo pasa a
  `Interruptor`, y **la lista larga** → carga al llegar al final, contrato
  por cursor). **C me debe:** su declaración de si el `Interruptor` del
  espejo ya reemplazó al segmentado en «Tu tienda» — *la API de
  `InterruptorEspejo` no cambió, así que el cambio es invisible desde
  afuera y por eso hay que decirlo, no suponerlo.*
- **CON D:** le debo la receta de la ficha de producto en modo Administrar
  — **entregada**, y con ella el portador del fundido que había frenado
  bien. **D me debe:** confirmar que `EntradaDeCruce` resolvió su duda del
  `useIsFocused()`; mi lectura fue que **el problema puede no existir
  porque `tomarCruce()` CONSUME**, y eso se verifica volviendo de otro tab
  y viendo que NO anima.

## ⛔ FRENOS Y SUPUESTOS VIVOS

- **🔴 El piso del 50 % de la inflexión es GEOMETRÍA, no gusto**, y ya vive
  escrito en `ENCUENTRO_GRADOS`. **No se baja tocando el texto ni
  achicando el disco** — el porcentaje escala con el disco y `xs = 11` ya
  es el piso de la escala cerrada. La única palanca es **dejar el anillo
  no-uniforme solo en el arranque del hombro, y es firma del founder.**
- **⚠️ Los 22 px de blanco en la esquina NO se curaron** — bajaron de 28,4
  y ahí quedaron. Su causa está medida y **no es el anillo** (que es
  uniforme, 9,0-9,2 en las tres direcciones): es la barra que sigue
  existiendo entre el valle y el filo. Bajarlo más cuesta una de las dos
  palancas ya descartadas con número.
- **⚠️ El anillo en viaje baja a 2,12 px** — es el retraso del valle, y
  está acotado por construcción (`ANILLO − 2`) para que nunca muerda el
  disco. **Si alguien sube `ANILLO`, el retraso lo sigue solo.**
- **⚠️ Con 5 tabs el clamp todavía corre el disco 7,7 px.** Hoy no hay
  ninguna configuración de 5 tabs en el gate; **dispara si el prestador
  monta `atender` + `negocio` a la vez.**
- **Supuesto declarado:** todo lo de la barra está medido **por
  instrumento sobre el path construido**, jamás en aparato. *Un rasterizador
  propio prueba la geometría; no prueba cómo se ve.*
- **R47 (38, baseline 39) y R48 (5, baseline 5)** son solo-baja y **mueren
  al llegar a 0** — no son míos: bajan cuando C y D toquen esas pantallas.

## 🔧 FICHAS DE B A DEMANDA (no abren frente solas)

- **D-833** (galería: lo descartado con literal se retira; lo que espera
  firma va PRIMERO y no se toca) · **D-825** (el bezier viejo en piezas
  que nacieron antes de la letra vigente — **la mía nació curada**; las
  otras bajan al tocarlas, no en barrido).

## 🔧 OPERATIVO

- **Instrumentos que quedan y sirven** (viven en `/tmp`, se re-crean en
  dos minutos y su valor es el MÉTODO, no el archivo): extraen `pathBarra`
  **del archivo real y la evalúan** en vez de reimplementarla. *Ésa es la
  razón por la que encontraron dos defectos que ninguna lectura vio — el
  retraso que mordía el disco y el path invertido en el extremo.*
- **Cero secretos tocados.** Cero archivos de `apps/` tocados en toda la
  tanda de la barra: mi territorio fue `packages/ui` + `docs/`.
