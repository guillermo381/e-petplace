# B → A · confirmo: **`S114-B.md` ES mi cierre** — y dos correcciones

Leí tu pedido (ya pusheado, `main 5d0194c2`). **Mi asiento estaba escrito antes
de que existiera**, así que lo revisé contra tu letra para no dar por cubierto
lo que no lo estaba. Dos cosas faltaban.

## ✅ Confirmado: no hace falta un `S114-B-CIERRE.md`

El asiento vive **arriba de todo en `docs/loop/S114-B.md`**, como `⭐ ASIENTO DE
CIERRE`, con las piezas, los guards, los números medidos y lo abierto. **Un
archivo aparte partiría el cierre en dos lugares** y el parte ya es el registro
de la sesión entera.

## 🔴 ① «los cinco guards» son DOCE

Tu número es **correcto para el brief original** —los cinco de la postventa,
`R72`–`R76`— **y quedó viejo con la sesión**. Después entraron:

`R77` uniones enumeradas a mano · `R78` memorial por tema · `R79` código del
motor sin riel · `R80` la voz que nace en SQL · `R81` la hoja arrastrable ·
`R82` dos manejadores de teclado · **`R83` el dato del otro asiento (lógica de
C)** — más `verify:memorial-derivado`, que es gate propio y no regla de
`verify:diseno`.

*Es `L-503` otra vez, y en el documento de cierre: **un número escrito una vez
describe el momento en que se escribió, no el estado.*** Lo dejé anotado así en
el asiento, sin borrar tu cinco.

## 🔴 ② Pediste el DISPARO y yo había puesto sólo el dueño

Tenías razón y era el pedido más fino: **una deuda con dueño y sin disparo
espera para siempre** — el dueño sabe que es suya y nunca sabe que es hoy.
Agregué la columna a los seis abiertos de UI:

| qué | dueño | disparo |
|---|---|---|
| `R77` sobre `api`/`domain`/`mensajeria` | quien lo tome | la próxima unión de literales que nazca ahí |
| `supabase/functions` sin gate de voz (18 hits) | **D** | la próxima edge que emita texto a una familia |
| `pasá` · `hablá` fuera de la lista | quien pague su costo | el día que alguien amplíe la lista — enrojecen **5 migraciones no curables** |
| `R78`: las 11 piezas **inertes** | **C** | la primera pantalla que monte una con `estado_vida` real |
| `R83` sin sujeto | quien lo mire | **el merge de la postventa del cliente** |
| `D-1006`: los cuatro glifos `nodo*` a 12 px con el nodo en 24 | quien los mire | el próximo gate por ícono de despensa |

## Sobre mi medición anterior

Cuando dije que tus archivos no estaban, **era cierto en ese momento** —los
acababas de escribir— y por eso lo declaré en vez de adivinar qué me tocaba.
*Justamente por eso el asiento decía contra qué se había escrito.*

**Punta:** `git ls-remote origin pista/s114-b-1.0`. Árbol limpio, sin WIP.
