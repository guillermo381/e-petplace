# S106-D · TANDA 2 — PARTE DEL LOOP

> **Pista D — el transporte en producción.** Territorio: `supabase/functions`.
> Si compacto, esta es la única memoria. Todo lo demás se relee del objeto.

## Paso ⓪

- **worktree NUEVO:** `/Users/guillo381gmail.com/proyectos/ePetPlace/e-petplace-s106-d-t2`
- **rama:** `pista/s106-d-t2`, nacida de `origin/main` = **`6bf81d12`**
- **verificado antes de crearlo:** `pista/s106-d` (tanda 1) **está mergeada a
  main**, y `video-token/index.ts` y `arnes.mjs` **viven en main**. *Si no lo
  hubieran estado, un worktree nuevo desde main habría empezado sin mi edge —
  por eso se mide antes y no después.*
- 🔴 **`node_modules`: NO EXISTE** (raíz ni apps). **`pnpm install` NO se
  corrió**, misma razón que la tanda 1: la edge es **Deno** y mis herramientas
  no tienen dependencias.
- ⚠️ **El worktree nuevo NO estaba linkeado a Supabase** (`supabase/.temp/` es
  gitignored). Se copió el link local del worktree anterior — **cero cambio
  remoto**.

---

## ACTO 1 · `video-token` contra dato real → 🔴 **NO CONCLUYENTE**

**Medido hoy contra la DB viva:**

| | |
|---|---|
| ofertas de telemedicina activas | **1** |
| citas con `modalidad='telemedicina'` | **0** |
| de esas, pagadas | **0** |

⇒ **El pedido de semilla de la tanda 1 no se ejecutó todavía.** Sin citas, el
arnés corre y **declara sus 8 casos saltados, con exit 1.**

**Ni siquiera pude ejercer el caso de la anon key** (el guard de `D-714`): la
anon key viene de `EXPO_PUBLIC_SUPABASE_ANON_KEY` y **no está en el repo** —
correctamente. *Es pública y me la pueden pasar, pero no la tengo.*

> ### 🔴 `video-token` está DESPLEGADA y verificada por respuesta, y **jamás emitió un token para una cita real.** Se declara así, **no como «anda»**.
> *`L-402`: no basta «¿está alcanzable desde afuera?» — hace falta «¿CORRIÓ
> ALGUNA VEZ?». Un verde acá sería flojo y no lo voy a dar.*

**Para destrabarlo hacen falta 3 cosas, ninguna mía:**
1. **A:** las citas semilla **por las puertas reales** — pedido ya depositado
   en `2026-08-26-s106-d-PEDIDO-SEMILLA-A.md` (tanda 1).
2. **Las 3 sesiones** (dueño · vet · tercero) — JWT, que yo no puedo fabricar.
3. **La anon key pública**, para el caso `D-714`.

**De paso, medido:** `prestadores.acepta_telemedicina` **ya no existe** — A la
retiró. *(Mi primera consulta rebotó contra eso; se re-midió sin suponer.)*

---

## ACTO 2 · `video-webhook` — ESCRITA, gate verde, **sin desplegar**

`supabase/functions/video-webhook/index.ts`. **33 edge functions, cero «usa
algo que no existe»**, y no aparece en los fuera-de-clase.

**Qué hace:** recibe → **verifica firma** → entrega el hecho crudo a la RPC de
A. **Nada más.**

**El guard es doble y lo hace el SDK** (medido en el cuerpo de
`WebhookReceiver.receive`): ① JWT firmado con nuestro API secret ② **sha256
del cuerpo** contra el claim. *Un cuerpo alterado con token válido muere en ②.*

🔴 **Por eso el cuerpo se lee CRUDO** (`req.text()`, jamás `req.json()`):
el hash se calcula sobre los bytes exactos, y parsear+reserializar los cambia.
*Un guard que rechaza lo bueno se apaga a los dos días.*

### Lo declarado, porque «no hay» también se deposita
- ✅ **Los 12 eventos que prometí EXISTEN** — verificados contra el tipo
  `WebhookEventNames` del SDK, no contra la doc. Incluido
  **`participant_connection_aborted`**, el hecho de §5.
- 🔴 **AMBIGÜEDAD EN LA PROPIA FUENTE:** el SDK **exporta**
  `authorizeHeader = "Authorize"` **y no lo usa en ningún lado**, mientras el
  JSDoc de `receive()` dice «`Authorization` header». **Mi edge lee los dos**,
  `Authorization` primero. *Elegir uno a ciegas y errar produce «firma
  inválida» sobre webhooks legítimos, indistinguible de un ataque.* **Sólo lo
  cierra un webhook real llegando.**
- ⚠️ **NO MEDIDO: si LiveKit reintenta los webhooks, y cuántas veces.** Mi
  edge devuelve **500 a propósito** cuando la RPC falla, para dar chance al
  reintento — *un 200 diría «lo tengo» sobre un hecho que no se guardó*. **Si
  no reintenta, ese 500 no salva el evento: sólo deja la traza.**

**REGISTRAR SIN JUZGAR, en acto:** la edge **no filtra ningún evento** —
entrega los 12 aunque hoy sólo 4 le sirvan a la letra. *Filtrar sería juzgar
cuál hecho importa, y esa decisión es de la puerta.*

**Falta:** la RPC de A (pedido depositado) · el alta del webhook en LiveKit
Cloud (founder) · desplegar **con `--no-verify-jwt`**.

---

## ACTO 3 · EL BITRATE → `2026-08-26-s106-d-t2-bitrate-y-vigilancia.md`

### 🔴 El hallazgo que corrige cómo veníamos hablando de esto
**No se puede fijar desde el servidor.** Ni `AccessToken`/`VideoGrant`, ni
`RoomConfiguration` (`name`·`emptyTimeout`·`departureTimeout`·
`maxParticipants`·`metadata`·`egress`) tienen bitrate. `livekit.VideoLayer` sí
lo tiene, y su propio comentario dice ***«server will measure actual»*** ⇒ **lo
declara el publicador.** *Veníamos hablando de una perilla nuestra que no
existe.*

### 🟢 Y la buena: **VIAJA POR OTA**
Las opciones son **JS del cliente**, no nativo ⇒ **se calibra sin build
nueva**, viendo consumo real después del soft launch. **No es decisión de
arquitectura: es una perilla.**

### Los números (presets reales del SDK, 20 min × 2 participantes)
| preset | bitrate | GB/consulta | consultas en 50 GB |
|---|---|---|---|
| h360 | 450 kbps | 0,135 | **≈370** |
| h540 | 800 kbps | 0,24 | **≈208** |
| **h720** | 1,7 Mbps | 0,51 | **≈98** |
| h1080 | 3 Mbps | 0,90 | ≈55 |

🔴 **De `h720` a `h540` MÁS QUE DOBLA el plan gratis.** Y valida sin usarla la
estimación previa: mis «0,45–1 GB» caen entre h720 y h1080.

**Dos palancas que no cuestan calidad y van ANTES:** `adaptiveStream`
(ajusta lo recibido al tamaño en pantalla — *el thumbnail no necesita 720p*) y
`dynacast`. *La primera pregunta no es cuánta calidad sacrificamos, es cuántos
bytes mandamos que nadie mira.*

**NO propongo un número.** Bajar el preset **no toca §6** (corrección de mesa
ya aceptada), pero **el piso de calidad visual es firma del founder y se
decide viendo un animal en pantalla, no en una tabla.**

---

## ACTO 4 · EL DISPARO DE VIGILANCIA

**~30 GB/mes** (60 % de 50 GB), **no** 3.000 minutos · **responsable: el
founder** (único con acceso al panel) · **mensual** y antes de cualquier
campaña.

⚠️ **Su debilidad, declarada:** *es un acto humano sin alarma — depende de que
alguien se acuerde de abrir un panel. Una vigilancia que depende de la memoria
de una persona ocupada no es una vigilancia: es una intención.*
🔴 **NO MEDIDO:** si LiveKit expone consumo por API en el plan Build (lo que
permitiría un cron que avise solo) ni si admite alertas de uso. **Primera
pregunta de quien retome esto.**

---

## ACTO 5 · LA TABLA CERRADA — LiveKit FIRMADO SIN CONDICIÓN

**Ya depositado en la tanda 1** (`c1ccec58`); **re-declarado como medición** en
el relevamiento de esta tanda, para que ese documento se lea solo.

Los cinco puntos ✅ · dos dispositivos · **APK autónomos con sha256
verificados** · red real de Quito · **26-ago-2026**.
🔴 **El ③ es lo que lo valida:** *cada aparato vio la cámara del otro — nadie
se vio a sí mismo y creyó que anduvo.*
⇒ **La escalera de caída se retira.**

**Lo que el verde NO cerró:** latencia en milisegundos · minSdk/target/peso
del APK (existe en poder de C, **pendiente de traspaso**) · y **el cable NO
desbloquea `video-token`**.

---

## Frenos respetados
`pagos-*` **intactas** · `verify-edge-deno` **sobre copia fuera del repo** y
nunca en pre-commit · secretos por `Deno.env` · **L-140 con `PUBLIC, anon Y
authenticated`** en el pedido nuevo — *mi propia lección de la tanda 1* · el
veredicto de autenticación **no vive en ningún campo de log**.

## Próximo paso ejecutable
1. Llega la RPC de A + las citas semilla + las 3 sesiones + la anon key.
2. Correr el arnés **de verdad** y reportar **su salida literal**.
3. Desplegar `video-webhook` con `--no-verify-jwt` y dar de alta el webhook.
4. Cerrar la ambigüedad `Authorization`/`Authorize` **con el primer webhook
   real**, y simplificar esa línea.

---

## TANDA 2bis (26-ago) — las tres respuestas de la mesa

### ① El bitrate — 🔴 NO ES MI TERRITORIO, y lo digo en vez de hacerlo igual
La firma dice *«encendé `adaptiveStream` y `dynacast` AHORA»*. **Son
`RoomOptions` del CLIENTE, viven en `apps/` — territorio de C.** Mi territorio
es `supabase/functions`.
⇒ **Pedido emitido:** `2026-08-26-s106-d-t2-PEDIDO-BITRATE-C.md`.

**Y una medición que hace que la firma NO sea un no-op**, del bundle de
`livekit-client@2.22.0`:
```
adaptiveStream: false,
dynacast: false,
```
🔴 **Vienen APAGADOS por defecto** ⇒ *tal como está el cable hoy, mandamos
bytes que nadie mira.* Encenderlos cambia algo de verdad.

**Y una advertencia que salió de leer el tipo:** `AdaptiveStreamSettings`
acepta `pixelDensity`, y el propio SDK avisa que ponerlo en `'screen'`
***«might significantly increase the bandwidth»***. **Un teléfono tiene
DPR 3** ⇒ *es la misma perilla girada al revés, dentro de la opción que
vinimos a encender para ahorrar.* **Dejar el default.**

**El preset queda en `h720`** por firma, hasta el ojo del founder.

### ② La vigilancia — MEDIDO: existe y NO la tenemos
**Analytics API** de LiveKit: `GET /api/project/{ID}/sessions` en
`cloud-api.livekit.io`, y **sí reporta `bandwidth` facturable**.
🔴 Literal de la doc: ***«only available to LiveKit Cloud customers with a
Scale plan or higher»*** — **Scale = $500/mes. Estamos en Build ($0).**

> **La ironía es el hallazgo: el instrumento que diría cuándo empezar a pagar
> $50 sólo se compra en el plan de $500.**

⇒ **El cron contra LiveKit NO se puede construir. La debilidad queda declarada
y aceptada como está**, según la instrucción.

**🔧 TERCERA VÍA PROPUESTA (no construida):** estimarlo **nosotros**, con los
`participant_joined`/`participant_left` del webhook de esta tanda —
participante-minutos × bitrate del preset. 🔴 **Su error cae del lado seguro:**
estimamos con `h720` nominal y `adaptiveStream` baja el real ⇒ **sobreestima
⇒ la alerta suena antes, nunca después**. *Para una alarma, un proxy que
sobreestima vale más que un número exacto que llega tarde.*
**No es el número de facturación y no debe presentarse como tal: es un
disparador.**
⚠️ **No la construyo ahora:** depende del webhook vivo y de la RPC de A, y
*un contador sobre una tabla vacía informaría cero para siempre* — peor que no
tenerlo. **Disparo: el primer evento real registrado.**

### ③ El header — sin cambios
Los dos leídos, `Authorization` primero. **Lo cierra el primer webhook real.**

### La semilla
A la toma con mis tres requisitos. **Mi Acto 1 sigue NO CONCLUYENTE hasta
entonces** — y se declara así, no como verde.

---

## TANDA 2ter (26-ago, 12:2x–12:4x Quito) — LA SEMILLA LLEGÓ Y EL ARNÉS CORRIÓ

### La semilla, medida (no leída del reporte)
**6 teleconsultas, todas del 26-ago, todas `pagada` + `confirmada`.**
⚠️ **A reportó «cuatro de la cuenta demo»; medí TRES** (Zeus: 13:00 · 13:30 ·
20:30). Las otras tres son de `guillo381+8` (Thor). *No lo corrijo como
error suyo — lo declaro porque mi configuración salió de mi medición.*

🔴 **Y el dato que ordenó todo: a las 12:24 las únicas DOS en ventana eran de
Thor, no de la demo.** La de la demo (13:00) **abre 12:45**.
⇒ *tener la sesión y tener la cita en ventana son dos cosas distintas, y la
ventana de ±15 min las separó por veinte minutos.*

### 🔴 DEFECTO REAL DE MI EDGE — lo encontró CORRER, no leer
Con la **anon key**, `/auth/v1/user` devuelve **HTTP 403 `bad_jwt`** — o sea
un **error**. Mi código mapeaba *«hubo error»* → `sesion_no_verificable`
**(503)**.

> **Un 4xx del proveedor dice «esta credencial no sirve»; sólo un 5xx dice
> «el proveedor se cayó».**
> 🔴 *Y lo caro no era el código equivocado: era que **el agujero de `D-714`
> quedaba disfrazado de incidente de infraestructura**. Un 503 invita a
> reintentar, y en el monitoreo alguien sondeando con la clave del bundle se
> habría visto como un problema NUESTRO.*

**Curado discriminando por `status`. Verificado: ahora rebota `sin_sesion`
401.**

### 🔴 DEFECTO DE MI PROPIO ARNÉS
El caso «ajeno» apuntaba a `CITA_TELE`, la del camino feliz. **Ajeno es una
RELACIÓN, no una identidad** — la misma sesión es dueña de una cita y ajena a
otra. Con la demo apuntando a su propia cita, rebotaba por
`fuera_de_ventana`: *rebotaba por la razón equivocada, que está tan roto como
un verde por la razón equivocada.* **Nace `CITA_AJENA`.**

### El cableado, corregido contra `pg_proc`
La RPC de A **no es la que pedí**: `registrar_hecho_de_sala`, con
`p_ocurrido_en` y **`p_evento_id`** — la idempotencia que pedí, **resuelta
mejor**. Y con ella dos trampas de runtime que ningún typecheck ve:
**`createdAt` es `bigint`** (`JSON.stringify` de un BigInt **lanza**) **y
viene en SEGUNDOS** — crudo a `new Date()` daría **1970**, *una fecha
perfectamente válida y perfectamente falsa: no rompe nada, se guarda, se lee,
y miente para siempre.* Sin fecha ⇒ **NULL honesto**.

### Resultado de la corrida — 4 verdes REALES
```
✅ anon key (JWT válido, sin persona) — D-714  → 401  sin_sesion
✅ ajeno a la cita (cita de OTRA familia)      → 403  ajeno_a_la_cita
✅ fuera de ventana                            → 425  fuera_de_ventana
✅ cita inexistente                            → 404  cita_inexistente
```
🔴 **El de `ajeno` vale doble:** apuntó a una cita **en ventana y pagada**, así
que **sólo pudo rebotar por identidad** ⇒ **prueba el orden de A** (identidad
antes que estado).

### `video-webhook` DESPLEGADA (`--no-verify-jwt`)
**Verificada POR RESPUESTA, no por el «Deployed Functions»:** un POST sin
firma devuelve `{"ok":false,"codigo":"firma_invalida"}` **401** — *mi* código,
no el del gateway ⇒ el flag pasó y el guard interno rebotó.
⚠️ **Su camino feliz NO EJERCIDO:** no tengo las claves de LiveKit para firmar
un webhook de prueba. *Que una puerta cierre no prueba que abra.*
**Pedido de alta al founder depositado.**
