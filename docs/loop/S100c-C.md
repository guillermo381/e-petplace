# S100c-C — LA VITRINA NO DEJA VER LA MERCADERÍA

> **Pista C · rama `pista-c` · worktree `e-petplace-s100-c`.**
> Territorio: `apps/cliente` — vitrina de Despensa y ficha de producto.
> Hallazgos del gate del founder sobre la OTA `fbd7b6e0`: **C-01 · C-02 · C-03 · C-04 · C-05 · C-06**.

---

# 🔴 HANDOFF DE RELEVO — leer esto antes que el resto

> **Escrito ANTES del cierre y verificado contra el repo el 18-ago-2026, con el
> árbol en 0.** Nada de acá está de memoria: los archivos se cotejaron **por
> blob** contra `origin/main`, y **cada número trae su fecha al lado**.
>
> *Se escribe antes a propósito: en la vuelta pasada tres de los cuatro handoffs
> se escribieron DESPUÉS del push del bundle y no llegaron al canon hasta un
> merge extra. **Un handoff que no está en `main` no existe para quien abre
> después.***

## ⓪ DÓNDE ESTOY — el SHA se lee, no se copia

```bash
git ls-remote origin refs/heads/pista-c   # ← la verdad de dónde quedó C
git rev-parse HEAD                        # ← y que tu worktree coincide
git log --oneline origin/main..HEAD       # ← lo que quede acá son merges, no contenido
```

**No hay un SHA escrito a mano a propósito:** este archivo entra en el mismo
commit que lo registraría, así que **cualquier hash que escribiera sería el del
commit anterior al suyo**. *Un commit que anota su propio SHA lo cambia al
anotarlo.*

**✅ TODO MI CONTENIDO ESTÁ EN `main`, verificado por BLOB y no por ancestro**
(18-ago). Los nueve archivos que toqué dan hash idéntico entre `HEAD` y
`origin/main`:

```bash
for f in "apps/cliente/src/app/(tabs)/despensa/index.tsx" \
         "apps/cliente/src/app/(tabs)/despensa/producto/[productoId].tsx" \
         "apps/cliente/src/components/hoja-filtros-despensa.tsx" \
         "apps/cliente/src/i18n/es.ts" "apps/cliente/src/i18n/en.ts" \
         "packages/api/src/wrappers/despensa-catalogo.ts" \
         "scripts/medir-s100c-vitrina.mjs" "scripts/medir-s100c-busqueda.mjs" \
         "docs/loop/S100c-C.md"; do
  [ "$(git rev-parse HEAD:"$f")" = "$(git rev-parse origin/main:"$f")" ] \
    && echo "✓ $f" || echo "✗ $f"
done
```
**9 de 9 ✓.** Lo que queda adelante de `main` en mi rama son **dos merges de
integración, sin contenido propio**.

## ① LOS SEIS HALLAZGOS, CON SU ESTADO — y dónde DIFIERO del resumen publicado

> 🔴 **ANTES DE LA TABLA, UNA PRECISIÓN QUE CAMBIA CÓMO SE LEE: el cierre pidió
> cruzar contra «el veredicto numerado del founder (1-31)», y ESE DOCUMENTO NO
> ESTÁ EN EL REPO.** Medido el 18-ago, no supuesto:
> `docs/loop/S100-GATE-FOUNDER.md` es de **S100** —otra sesión, ancla
> `9baed84a`, OTA `5d7a2320`— y **numera cero hallazgos**; el único archivo que
> matchea un patrón «30/31» es **este mismo**, por su propia tabla.
>
> ⇒ **la columna «dónde» de abajo remite a los números del RESUMEN DE
> PUBLICACIÓN de A (§11-§15), no a una lista 1-31 del founder.** Se dice
> explícito para que nadie lea esta tabla como un cruce contra una vara que no
> existe. *Inventar la correspondencia habría sido más prolijo y falso.*
> **(Verificado de forma independiente por D, que llegó al mismo resultado y lo
> declaró igual en su handoff — dos pistas, una medición.)**

| # | hallazgo | estado | dónde |
|---|---|---|---|
| **C-01** | «proplan» no encuentra «Pro Plan» | ✅ **HECHO** | `despensa-catalogo.ts` · resumen §11 |
| **C-02** | los filtros se comen la pantalla | ✅ **HECHO** | `hoja-filtros-despensa.tsx` · resumen §12 |
| **C-03** | espacio muerto buscador → barra | ⚠️ **HECHO A MEDIAS: 48 de 74 dp** | resumen §13 · los otros 26 = **H-302**, de B |
| **C-04** | la ficha no tiene acordeones | ✅ **HECHO — y partido en dos por la medición** | `producto/[productoId].tsx` |
| **C-05** | el nombre queda muy alargado | 📋 **MEDIDO, NO HECHO — la cura es de B** | **H-303**, entregado con número |
| **C-06** | ~1 cm muerto en la ficha | 🔴 **CURADO POR B · NO VERIFICADO POR MÍ** | límite de mi vara, §④ |
| **H-301** | la búsqueda ignora la especie | ✅ **HECHO — firma del founder: ORDENA, no filtra** | ver el aviso de abajo |

### 🔴 LA ÚNICA DIFERENCIA CON EL RESUMEN DE PUBLICACIÓN, y hay que verla

**`docs/loop/S100c-RESUMEN-DE-PUBLICACION.md` §⑥② dice que H-301 «no se curó» y
«va al gate». Eso quedó VENCIDO: el founder firmó y la cura ENTRÓ.**

Verificable en el objeto, no en mi palabra:
```bash
git show origin/main:packages/api/src/wrappers/despensa-catalogo.ts | grep -c especiePrioritaria   # → 6
git show "origin/main:apps/cliente/src/app/(tabs)/despensa/index.tsx" | grep -c "mascota?.especie" # → 1
```
**No es un error de A: escribió su resumen cuando eso era cierto, y el mundo se
movió después.** *Es el mismo modo de falla que esta vuelta cobró cuatro veces
entre las pistas — **cierto al escribirse, falso al usarse**.* Se declara acá
porque **la sesión siguiente lee el resumen** y heredaría una deuda ya pagada.

## ② LO MEDIDO EN APARATO — con su número y su fecha

> **Todo con `scripts/medir-s100c-vitrina.mjs` y `medir-s100c-busqueda.mjs`,
> viewport 384×832, cuenta `+8`, RN-web en `:8093`, 18-ago-2026.** La base, con
> `supabase db query --linked` (solo lectura).
> **🔴 Esto es lo más caro de re-obtener: no se re-mide sin levantar Metro y el
> aparato.** Si el número que necesitás está acá, usalo; si no, corré el script.

| medición | número | fecha |
|---|---|---|
| cromo antes del primer producto — **al abrir** | **317 dp · 50,2 %** | 18-ago |
| cromo — **al cerrar** | **213 dp · 33,8 %** | 18-ago |
| ¿entra la primera tarjeta entera? | **NO (−18 dp) → SÍ** | 18-ago |
| **margen que le queda a la vitrina** | **42 dp** | 18-ago |
| espacio muerto buscador → barra de mascotas | **74 → 48 dp** | 18-ago |
| alto de la tarjeta de grilla (tras N24 de B) | **332 → 376 dp** | 18-ago |
| `proplan` · `royalcanin` · `tasteofthewild` | **0/0/0 → 20/50/27** | 18-ago |
| `hills` (correcto que siga en 0) | **0 → 0** | 18-ago |
| H-301 · resultados que cambian de posición | **19** | 18-ago |
| H-301 · resultados que **antes no estaban** | **31** | 18-ago |
| H-301 · de 3 leídos, cuántos son de otra especie | **3 de 3 → 0 de 3** | 18-ago |
| ficha · bloque de ingredientes | **213 car/120 dp → 111 car/72 dp** | 18-ago |
| tarjeta · nombre a **16/24 dp**, 2 líneas | **8 de 12** | 18-ago |
| borde superior del pie de la ficha (C-06) | **645 dp antes y después** | 18-ago |

**Del catálogo, contra la base viva (18-ago):** 470 productos · **20 de 105
marcas con espacio adentro, cubriendo 196 productos** · `descripcion` promedio
**10**, máximo **29** · `composicion_mercado` **vacía en 470 de 470** ·
`ingredientes_activos` en **202 de 470**, promedio **194** caracteres, máximo
**517** · `nombre` promedio **29**, máximo **81**, **183 sobre 30 caracteres** ·
**0 de 470** con `especies_aplicables` nula o vacía.

## ③ 🔴 LAS CURAS DE DOS MITADES — quitar una reintroduce el defecto SIN VERLO

**① H-301 = `especiePrioritaria` en el wrapper + `mascota?.especie` en la
pantalla.** Sin el argumento de la pantalla, el wrapper hace **un solo viaje y
orden alfabético**: la cura queda viva en el código y **muerta en la pantalla**,
sin un error. Y el orden **se hace en el SERVIDOR, en dos baldes**: quien lo
«simplifique» a un `.sort()` sobre lo traído **ordena el recorte y no el
catálogo** — los **31 productos** que hoy aparecen viven más allá del corte de
50 y volverían a ser invisibles con una pantalla que parece correcta.

**② El respaldo de búsqueda = comodín entre letras + PREFIJO DE 3 ANCLADO.**
Sacar el ancla parece «más resultados» y es lo contrario: medido, devolvía
**11 resultados para `hills` encabezados por «Higgins»**, con `Hill's` en
**cero productos** del catálogo ⇒ **convertía un vacío honesto en una lista
equivocada**. Y corre **SOLO cuando el exacto dio cero**: moverlo a correr
siempre vuelve difusa toda búsqueda, incluidas las que el founder aprobó.

**③ El `is.null` del segundo balde de especie.** Hoy **0 de 470** productos
tienen especie nula ⇒ parece cláusula muerta. En SQL `NOT (NULL @> …)` es NULL
y no TRUE, así que **el día que entre un producto sin especie declarada
desaparecería de la búsqueda ENTERA sin un error.** *Nadie ve al que falta.*

**④ El acordeón de la ficha = `PieRevelar` + el corte en 6 + la advertencia
FUERA del plegado.** Meter «Declara contener» dentro del revelado rompe letra
firmada (`MODELO_DESPENSA` §6/§10 y el límite de N22): **una advertencia de
salud jamás se pliega.**

## ④ LO QUE DECIDÍ **NO** HACER — con su razón, para que no se reintente

- **NO filtré la búsqueda por especie** — contradice `LETRA_RECORRIDO_DESPENSA_S96`
  §5.2. Se llevó al gate y **el founder firmó ORDENAR**. *Filtrar era más rápido
  y rompía letra firmada.*
- **NO reordené en memoria** lo ya traído: habría ordenado **el recorte**, y los
  31 productos de su especie más allá del corte seguirían invisibles.
- **NO construí la señal en la tarjeta** («para perros»): **aprobada por el
  founder y DIFERIDA** — la vitrina tiene **42 dp** y son todos (L-301).
- **NO apagué R29 ni monté un `PieDeCampo` decorativo** para quedarme con 26 dp.
  *Gamear el instrumento es peor que el defecto.*
- **NO curé el nombre de la tarjeta ni el hueco de la ficha:** son de
  `packages/ui`. Salieron como **H-303** y **C-06**, con su número.
- **NO monté el carrito flotante de N25** aunque la ley es de la vitrina: no
  estaba en mis seis, el glifo llegó sin gate de ícono y la barra ganó una
  quinta tab. *Montar una puerta nueva mientras se discute la gramática de la
  barra fabrica la segunda puerta que N25 vino a matar.*
- **NO toqué lo aprobado**: la imagen, el `+`, el precio, los chips de
  presentación, la composición.

## ⑤ DEUDAS VIVAS — con dueño y disparo

| # | qué | dueño | disparo |
|---|---|---|---|
| **el margen** 🔴 | **42 dp** es todo lo que le queda a la vitrina antes de cortar la primera tarjeta otra vez | **quien sume alto** | cualquier pieza nueva en ese eje (L-301) |
| **H-302** | los 26 dp del `PieDeCampo` en el buscador: que «este campo no tiene mensaje» sea **inexpresable**, **sin aflojar R29** y **censando consumidores antes de tipar** | **B** (aceptado) | su propia vuelta |
| **H-303** | nombre a **16/24 dp**, 2 líneas en **8 de 12**; bloque de **48 dp**. ⚠️ **Cuesta 48 y el margen son 42** — B lo frenó por eso | **B** | cuando el acreedor de alto esté resuelto |
| **C-06** | el hueco del pie de la ficha: **curado por B, NO verificado** | **B / founder en aparato** | el próximo gate en teléfono |
| **la señal de especie** ⏳ | «para perros» en un resultado de otra especie — **aprobada y diferida** | **founder / C** | cuando la vitrina recupere alto |
| **H-103** | **23 de 50** tarjetas sin foto; catálogo **161 de 470** con imagen | **founder / catálogo** | los seis comprables de octubre |
| **el resumen vencido** | `S100c-RESUMEN-DE-PUBLICACION.md` §⑥② da H-301 por no curado | **quien abra la vuelta** | leer §① de este handoff antes que el resumen |

## ⑥ LO QUE MI RELEVO TIENE QUE SABER DEL APARATO

```bash
node scripts/medir-s100c-vitrina.mjs  8 8093 <rotulo>   # cromo · filtros · ficha · nombre
node scripts/medir-s100c-busqueda.mjs 8 8093 <rotulo>   # C-01 y la especie de los resultados
```
Necesitan `apps/cliente/.env.local` (gitignored, se copia del worktree
principal), **Metro web en puerto propio — jamás 8081**, y la clave sale del
**keychain al momento** (R6). Para consultar la base hay que copiar
`supabase/.temp/` del worktree principal: **este worktree no nace linkeado.**

🔴 **LAS TRES TRAMPAS QUE ME MORDIERON, las tres de INSTRUMENTO:**

**① El selector que devuelve la respuesta que esperabas.** Mi primer detector de
filas de chips buscaba `[role="button"]` bajos y angostos y **devolvió los `+`
de la grilla**. Con él, el reporte decía *«no se dibuja la fila de especies»* —
**exactamente la conclusión a la que yo quería llegar**, y por eso casi la
publico. *Un fallo de selector se lee igual que el defecto que uno vino a
medir.* La versión buena **se ancla en una tarjeta real y sube al padre.**

**② El exit code leído del pipe, dos veces en un día.** `npx tsc --noEmit | head`
imprimió `EXIT=` vacío y me hizo dudar de un typecheck verde. **Se lee del
COMANDO** (`cmd > archivo; echo $?`) — L-191.

**③ Mi vara no ve los insets del sistema.** RN-web no los tiene, así que la cura
de C-06 es un **no-op medible en cero** acá. *Reportarlo verde porque no cambió
nada habría sido medir con la magnitud equivocada.*

**Y la de mi predecesora, que sigue viva:** `expo-router` conserva la pantalla
anterior en el DOM ⇒ **todo lo que se busque va acotado a
`[data-medicion-scroller]`.**

---

# ⓪ PRIMER ACTO — LOS HALLAZGOS AL REPO, ANTES DE CONSTRUIR

> **Depositados el 18-ago-2026, antes de tocar una línea de código.** Vienen del
> recorrido del founder sobre la OTA `fbd7b6e0` (ancla `f107eac9`) y hasta este
> commit **vivían solo en la mesa**. *Un hallazgo que no está en el repo se pierde* —
> y esta casa ya midió que la última acción de una pista es justo la que se queda
> afuera (L-217).
>
> **Lo de abajo es TRANSCRIPCIÓN, no diagnóstico.** El literal del founder va entre
> comillas y **no se interpreta acá**; la medición y la cura vienen después, en sus
> propias secciones, y se distinguen de esto a propósito.

## C-01 🔴 LA BÚSQUEDA NO ENCUENTRA «proplan» CUANDO EL PRODUCTO ES «Pro Plan»

**Literal del founder:** *«puse todo proplan, todo pegado… me la encontró solo cuando
pongo dos palabras»*.

**Lo que el hallazgo pide, sin resolverlo todavía:** normalizar espacios en **AMBOS
lados** de la comparación —como ya se hizo con los acentos en S100— y **probablemente
también curar los nombres del catálogo**. ⚠️ **Son dos mitades y hay que medirlas por
separado antes de elegir**: una cura de comparación y una cura de datos no se prueban
con el mismo instrumento, y curar la equivocada deja el defecto vivo con mejor cara.

## C-02 🔴 LOS FILTROS SE COMEN LA PANTALLA

**Literal del founder:** dos escalones de chips (categorías + especies) y *«con el
teclado desplegado no veo absolutamente nada más»*.

**FIRMA DE MESA (no es propuesta mía — llega firmada):**
- **La fila de ESPECIES MUERE cuando hay mascota elegida.** Ya está dicho arriba y hoy
  **se contradice**: en «Para Jack», que es gato, ofrecemos perros y conejos.
- **Las CATEGORÍAS pasan a UN CONTROL ÚNICO:** un botón «Filtrar» que abre una hoja con
  todo adentro (categoría, marca, precio, presentación) **mostrando cuántos filtros hay
  activos**.
- **La razón, que es lo que la vuelve firma y no gusto:** con **563 productos** los chips
  nunca van a alcanzar. Patrón de Instacart y Mercado Libre. **El alto vuelve a la
  mercadería.**

⚠️ **Choca contra una decisión declarada de mi predecesora**, y el choque se declara en
vez de absorberse: S100b decidió **NO fundir las dos filas de `FiltroPills`** porque
fundirlas daría *«un solo filtro activo a la vez»* — *no se recorta función para ganar
alto sin firma*. **Hoy la firma existe y va en la otra dirección**, y además la hoja
**no recorta función: la ensancha** (categoría + marca + precio + presentación, hoy
imposibles). *La decisión de S100b era correcta con la letra que tenía; cambió la letra,
no el razonamiento.*

## C-03 ESPACIO MUERTO ENTRE EL BUSCADOR Y LA BARRA DE MASCOTAS

**Literal:** *«se pierde mucho espacio»*. **Medilo y curalo** — el hallazgo llega sin
número y **el número es parte de la cura**, no de su reporte.

## C-04 LA FICHA NO TIENE ACORDEONES

**Literal:** *«en las imágenes de Laika tenían un más para los descriptores adicionales;
acá no los veo, están todos hacia abajo»*.

**🔴 Los DOS límites de letra que este hallazgo tiene encima, y ninguno es negociable:**
1. **El acordeón es SOLO PARA PROSA**, y **la advertencia de alérgeno JAMÁS se pliega**
   (`MODELO_DESPENSA` §6/§10 — *plegar una advertencia de salud la convierte en nota al
   pie*). **N22 lo confirma** en su propio límite.
2. **Mi predecesora midió que la descripción promedia 10 caracteres.** ⇒ **si el dato
   sigue siendo ése, se dice CON EL NÚMERO en vez de plegar aire.** *Un acordeón sobre
   diez caracteres es un control que promete contenido que no existe.*

## C-05 EL NOMBRE DEL PRODUCTO QUEDA MUY ALARGADO EN LA TARJETA

Evaluar tamaño de letra. **Literal del founder: *«en general se ve bien»*** ⇒ **es
afinamiento, no rojo.** Se anota con esa temperatura y no se le sube.

## C-06 ~1 cm MUERTO EN LA FICHA, ENTRE «Agregar» Y EL MENÚ

**B tiene el número (61 dp) y la cura vive EN LA PIEZA.** ⇒ **coordinar, jamás curar por
pantalla.** *Curar en la pantalla un defecto de la pieza lo esconde en un solo lugar y lo
deja vivo en todos los demás.*

## ✅ LO QUE EL FOUNDER APROBÓ Y NO SE TOCA

- **La imagen del producto** — *«la primera foto está mucho mejor definida»*.
- **El `+`.**
- **El precio.**
- **Los chips de presentación.**
- **La composición.**

> **Esta lista tiene el mismo peso que la de defectos.** *Cambiar lo que ya funciona no
> es celo: es una regresión que nadie pidió y que ningún instrumento va a cazar, porque
> lo que se rompe es una aprobación, no un build.*

---

## LA VARA DE ESTA VUELTA — N21–N25, y no se re-derivan

Depositadas por B el 18-ago-2026 en `docs/DIRECCION_DISENO_S99.md` **v1.1**, con su
medición en `docs/laminas/2026-08-18-s100c-MEDICION-SUPERFICIES.md`. Las que muerden en
mi territorio:

| ley | qué me obliga |
|---|---|
| **N21** superficies | un grupo de datos con rótulo propio va **en carta**; lo que ES la pantalla (encabezado, pie, CTA) **no**. Ante la duda: **quitar antes que agregar** |
| **N22** la «i» | lo que se necesita **para decidir** queda a la vista; lo que se necesita **para entender** va detrás de una «i». **Límite duro: una advertencia de salud jamás se pliega.** Es la letra que gobierna C-04 |
| **N23** color | el color marca **clase**, jamás **importancia**. Hoy se cumple por construcción (`Texto` no tiene `accent`): **no hay nada que curar, hay algo que impedir** |
| **N24** el control | un control que cambia de forma **reserva su espacio máximo desde el primer render**. Medido: la tarjeta salta **309,0 → 353,1 dp (+44,1)** al tocar el `+` |
| **N25** el carrito | **una sola puerta**, flotante abajo a la derecha, **y es de la VITRINA** (no del carrito/checkout/resumen). Se monta como `pie` de `PantallaConPie` — **a mano con `position:'absolute'` reintroduce H-105 y R53 muerde** |

---

# 🔴 LA LEY QUE ESTA VUELTA DEPOSITÓ EN EL CANON

> **UN ENCARGO DE MESA ES UNA HIPÓTESIS CON AUTORIDAD — se mide contra el
> objeto ANTES de ejecutarse, y quien lo recibe tiene el DEBER de falsarlo.**

**Nació de dos encargos a esta misma pista, los dos equivocados, los dos
cazados midiendo y ninguno razonando:**

① *«probablemente también curar los nombres del catálogo»* (C-01) —
**196 de 470 productos** caen bajo marcas con espacio adentro (`Pro Plan`,
`Royal Canin`, `Taste of the Wild`). **Curarlos habría significado
escribirlos MAL.** *La mesa mandó a arreglar el DATO cuando el defecto
estaba en el LECTOR.*

② *«la fila de especies… hoy se contradice»* (C-02) — **no se contradice**:
el guard existe desde S96-D y está en el ancla publicada. **El encargo
habría producido trabajo sobre un defecto inexistente**, y lo que el founder
vio —que era real— habría seguido vivo.

**Y la tercera, que es mía y va con ellas:** **convertir un vacío honesto en
una lista equivocada es peor que no encontrar nada, porque el que busca cree
que encontró.** (El respaldo de C-01 devolviendo «Higgins» para `hills`, con
`Hill's` medido en cero productos del catálogo.)

---

# ① LO MEDIDO — cada número con su fecha y su vara

> **Aparato propio, `scripts/medir-s100c-vitrina.mjs` y
> `scripts/medir-s100c-busqueda.mjs`**, viewport **384×832**, cuenta `+8`,
> RN-web en `:8093`, **18-ago-2026**. La base se consultó con
> `supabase db query --linked` (solo lectura).
>
> ⚠️ **EL LÍMITE DEL APARATO, repetido porque hay que repetirlo:** RN-web
> **no es el teléfono** — sin barra de tabs nativa, sin insets del sistema,
> **sin teclado**. Sirve para comparar un ANTES con un DESPUÉS con la misma
> vara, jamás para declarar cómo se ve en el aparato del founder.

## El cromo de la vitrina — antes y después

| | al abrir | al cerrar |
|---|---|---|
| cromo antes del primer producto | **317 dp** | **213 dp** |
| % del alto útil | **50,2 %** | **33,8 %** |
| ¿entra la primera tarjeta entera? | **NO — faltaban 18 dp** | **SÍ** |
| espacio muerto buscador → barra de mascotas | **74 dp** | **48 dp** |
| alto de la tarjeta de grilla | 332 dp | **376 dp** (+44 de N24, de B) |

## El catálogo, medido contra la base (no leído)

| | |
|---|---|
| productos | **470** |
| marcas distintas · con espacio adentro | **105 · 20** |
| productos bajo una marca con espacio | **196 (41,7 %)** |
| `descripcion`: promedio · máximo | **10 · 29 caracteres** |
| `composicion_mercado` vacía | **470 de 470** |
| `ingredientes_activos`: cuántos la tienen | **202 de 470** |
| ingredientes: promedio · mediana · máximo | **11 · 12 · 26** |
| texto de ingredientes: promedio · máximo | **194 · 517 caracteres** |
| `nombre`: promedio · máximo · sobre 30 car | **29 · 81 · 183 de 470** |

---

# ② LOS SEIS HALLAZGOS, UNO POR UNO

## C-01 ✅ CURADO — y **la mitad obvia de la cura era la que hacía daño**

El hallazgo llegaba con *«probablemente también curar los nombres del
catálogo»*. **Medido: el catálogo NO está sucio.** «Pro Plan» se escribe con
espacio porque **así se llama**, y **196 de 470 productos** caen bajo una
marca con espacio adentro ⇒ *«curar los nombres» habría significado
escribirlos MAL para que el buscador funcionara.*

**Y no es un caso, es una CLASE**, medida en pantalla:

| término | antes | después |
|---|---|---|
| `pro plan` | 20 | 20 |
| **`proplan`** | **0** | **20** |
| `royal canin` | 50 | 50 |
| **`royalcanin`** | **0** | **50** |
| `taste of the wild` | 27 | 27 |
| **`tasteofthewild`** | **0** | **27** |
| `hills` | 0 | **0** ← *y está bien: `Hill's` no existe en el catálogo* |

**La cura:** un respaldo que intercala el comodín entre las letras y **corre
SOLO cuando la búsqueda exacta devolvió cero** — donde no hay nada que
degradar, así que **no puede empeorar ninguna búsqueda que hoy funcione.**

🔴 **Y su primera versión estaba mal, la cazó el aparato, y esa historia es
la parte que vale:** intercalando entre TODAS las letras, `hills` devolvía
**11 resultados encabezados por «Higgins Vita Seed mezcla natural»**.
Medido: **`Hill's` tiene CERO productos** ⇒ *el respaldo convertía un vacío
HONESTO en once resultados equivocados, que es peor que no encontrar —
el vacío se entiende y la lista equivocada se cree.* Curado anclando las
**tres primeras letras pegadas** (`hil*l*s` ya no alcanza a «Higgins»), sin
perder un solo caso real: **ninguna marca parte sus tres primeras letras.**

## C-02 ✅ CURADO — 🔴 **pero la firma nombraba una cura que ya estaba hecha**

**Lo construido:** las dos tiras de chips (**132 dp = 42 % del cromo**) pasan
a **un control «Filtrar» con hoja**, con **cinco ejes independientes y
simultáneos** —categoría · especie · marca · presentación · precio—, de los
cuales **tres no existían**. *No recorta función: la ensancha*, que era la
razón por la que mi predecesora se había negado a fundir las dos filas.

🔴 **LA PREMISA DE LA FIRMA ES FALSA, MEDIDA:** *«la fila de ESPECIES muere
cuando hay mascota elegida — hoy se contradice»*. **No se contradice.** El
guard vive en `index.tsx:540` **desde S96-D**, **está en el ancla publicada
`f107eac9`** (verificado con `git show`) y **medido en aparato NO se
dibuja** con Jack elegido: 2 filas de chips sin mascota, **1 con mascota**.

🔴 **PERO LO QUE EL FOUNDER VIO ES REAL, Y ESTÁ EN OTRA SUPERFICIE ⇒ H-301.**
Con Jack (**gato**, verificado en la base) elegido y buscando «alimento»,
**los tres primeros resultados declaran, en su propia ficha, «Está pensado
para perros.»** La causa está en el código: la recomendación filtra por
especie (`despensa-catalogo.ts:980`) y **la búsqueda no recibe especie.**

> *Una observación correcta con una causa mal atribuida se cura en el lugar
> equivocado, y el defecto sigue vivo con mejor cara.* **Matar la fila de
> especies —la cura que la firma nombraba— no habría cambiado nada de lo
> que el founder vio.**

### ✅ H-301 CURADO — firma del founder, 18-ago-2026: **ORDENA, NO FILTRA**

**La firma:** *primero lo que ella puede comer, después el resto.* **No se
filtra** — `LETRA_RECORRIDO_DESPENSA_S96` §5.2 manda (*«se ofrece, no se
exige, y la compra sigue igual»*), y **ordenar respeta la letra sin esconder
nada**: una familia con un gato tiene que poder comprarle algo a un perro.

🔴 **EL ORDEN SE HACE EN EL SERVIDOR, EN DOS BALDES — y la medición prueba
que ESA decisión era la que importaba.** La búsqueda trae **50 de hasta 285**
coincidencias, así que reordenar en memoria lo ya traído habría reacomodado
la página y nada más: **el gato del puesto 51 alfabético nunca habría
aparecido.** Medido en pantalla, «alimento» con Jack (gato) elegido:

| | |
|---|---|
| resultados que **cambian de posición** | **19** |
| resultados que **antes no estaban** | **31** |
| de las 50 tarjetas, ¿cuántas quedaron en su lugar? | **ninguna** |
| los 3 primeros ANTES | `Active Mind 7+` ×3 → *«Está pensado para perros»* |
| los 3 primeros DESPUÉS | `Adulto Control PH Feline`… → *«para gatos»* |
| de 3 resultados leídos, cuántos NO son de su especie | **0** (antes: 3 de 3) |

⇒ *Un orden aplicado después del corte no ordena el catálogo: ordena el
recorte.* **Esos 31 son productos que el orden alfabético jamás alcanzaba.**

**REVERSIBLE EN UNA LÍNEA, declarado porque el founder lo pidió así:** quitar
el tercer argumento de `buscarProductosDespensa` devuelve el orden alfabético
único. **Sin estado nuevo y sin migración** — `undefined` = sin mascota = un
solo viaje.

**Y el `is.null` del segundo balde va aunque hoy no haga falta:** medido, **0
de 470** productos tienen `especies_aplicables` nula o vacía, pero en SQL
`NOT (NULL @> …)` es NULL y no TRUE ⇒ el día que entre un producto sin
especie declarada **desaparecería de la búsqueda ENTERA sin un solo error**.
*Un producto que se cae de una lista no tiene síntoma: nadie ve al que falta.*

**Las dos curas se componen:** buscar `proplan` con Jack elegido devuelve el
Pro Plan de gato primero.

⏳ **DIFERIDO CON SU RAZÓN, aprobado por el founder y NO construido:** la
señal en la tarjeta («para perros»). **La vitrina tiene 42 dp de presupuesto
vivo (L-301) y son todos.** Entra cuando haya margen.

## C-03 ✅ CURADO A MEDIAS, con la otra mitad declarada y con dueño

**74 dp muertos** entre la caja de texto (y 58) y la barra de mascotas
(y 132). **48 curados**: el aire estaba **pagado dos veces** — el
`Encabezado` portada ya trae `paddingBottom: spacing[5]`, y el `ScrollView`
sumaba otros 16 encima. Bajó a 8; quedan 28, que siguen siendo aire.

**Los otros 26 dp NO se curaron, y por qué ⇒ H-302 (dueño B).** Son el
`PieDeCampo` que `Campo` reserva SIEMPRE, en un buscador que **no tiene
`ayuda` y no puede tener `error`**. La prop `sinPie` existe, se probó, y
**`verify:diseno` la frenó con razón (R29)**: con `sinPie` el `Campo` sigue
pintando el borde de error y deja de renderizar el texto ⇒ *borde rojo sin
una palabra*. **No se apagó la regla y no se montó un `PieDeCampo`
decorativo para callarla** — *gamear el instrumento es peor que el defecto.*
B lo tomó con dos condiciones suyas (no aflojar R29 · censar consumidores
antes de tipar) y **acordamos que no entra en esta vuelta.**

## C-04 ✅ CURADO — **y el hallazgo se parte en dos, con una mitad que NO se construye**

| | medido | qué se hizo |
|---|---|---|
| `descripcion` | **10 car de promedio, máximo 29** | **NADA.** *Un acordeón sobre diez caracteres promete contenido que no existe* — el hallazgo pedía decirlo con el número, y éste es |
| `composicion_mercado` | **vacía en 470 de 470** | nada que plegar |
| `ingredientes_activos` | **202/470 · 194 car de promedio · hasta 517** | **se pliega** |

**Se usó `PieRevelar`** —la pieza que la casa ya tiene para esto (19.6)— en
vez de inventar un acordeón: cero componente nuevo (Ley 11), y encaja mejor
porque `n` es **cuántos ingredientes quedan**, un número verificable en vez
de un «ver más» mudo. **Con 6 o menos no se dibuja el control** (38 de 202).

🔴 **LA ADVERTENCIA DE ALÉRGENO QUEDA FUERA DEL PLEGADO**, y no es decisión
de la pantalla: `MODELO_DESPENSA` §6/§10 y el límite explícito de N22.
**Verificado después de curar: «Declara contener» sigue visible en
y 369–389**, con todas las secciones alcanzables.

Medido, mismo producto: **213 car / 120 dp → 111 car / 72 dp**, contenido de
la ficha **1136 → 1092 dp**.

## C-05 📋 MEDIDO, NO CURADO — la cura es de B ⇒ **H-303**

El nombre lo pinta `TarjetaProducto` (`packages/ui`) sobre `nombreCurado`:
**no es mi territorio.** Lo que entrego es el número:

- El nombre se pinta a **16/24 dp**; la marca a 14/20.
- **8 de las 12 primeras tarjetas parten el nombre en 2 líneas**, con **tope
  2** ⇒ *recorta, no desborda* — coherente con el *«en general se ve bien»*.
- El catálogo lo respalda: **183 de 470 nombres pasan de 30 caracteres.**
- El bloque del nombre son **48 dp de los 376** de la tarjeta.

**Temperatura conservada: es afinamiento, no rojo.** El founder aprobó la
tarjeta en general y **eso pesa igual que un defecto.**

## C-06 🔴 NO VERIFICABLE CON MI APARATO — se declara en vez de darle un verde

**B ya lo curó en `main` (`966c98d6`)**: `PantallaConPie` contaba el inset
del sistema **dos veces** adentro de `(tabs)`. **Medí antes y después: borde
superior del pie = 645 dp en las dos.** *No es que no funcione: **RN-web no
tiene insets del sistema**, así que la cura es un **no-op en mi vara por
construcción**.*

⇒ **C-06 queda SIN verificar por mí y su verificación es del aparato.**
*Reportarlo verde porque no cambió nada sería medir con la magnitud
equivocada.* Lo que sí confirmé por el camino real post-merge: **G-02 no
volvió** y **el scroll sigue vivo** (0 → 401) ⇒ la reserva derivada y el
`box-none` siguen haciendo su trabajo.

---

# ③ 🔴 EL CRUCE QUE SALIÓ BIEN POR CASUALIDAD — y el presupuesto que deja

**B midió su tarjeta (+44 dp por N24) y declaró su costo. Yo medí mi cromo
y declaré el mío. Las dos mediciones eran correctas y el defecto no vivía
en ninguno de los dos números.**

La aritmética, con el visor útil terminando en `y = 747`:

| | tarjeta va de … a … | ¿entra? |
|---|---|---|
| con mi cura (cromo 213) | 329 → **705** | **SÍ, sobran 42 dp** |
| con el cromo de la mañana (317) | 433 → **809** | **NO — se pasaba por 62 dp** |

⇒ *El que decide es la SUMA, y la suma no era de nadie.* Si el merge de B
hubiera entrado un día antes, el founder habría visto la primera tarjeta
cortada otra vez **y ninguno de los dos habría entendido por qué.**
B lo depositó como **L-301**.

🔴 **EL PRESUPUESTO QUE QUEDA, para que no se gaste dos veces: 42 dp.**
Es TODO el margen de la vitrina. **Cualquier cosa que agregue alto al cromo
o a la tarjeta lo consume.**

---

# ④ LO QUE DECIDÍ **NO** HACER — con su razón

- **NO filtré la búsqueda por la especie de la mascota**, que es la cura
  que H-301 pedía a primera vista. **Contradice letra firmada:**
  `LETRA_RECORRIDO_DESPENSA_S96` §5.2 — *«si se compra para una especie que
  no está en la familia… se ofrece, no se exige, y la compra sigue igual»*.
  ⇒ se llevó al gate en vez de resolverlo por criterio propio, **y el
  founder firmó ORDENAR**. *Haberlo filtrado habría sido más rápido y habría
  roto letra firmada.*
- **NO reordené en memoria los resultados ya traídos**, que era la cura de
  media hora. La búsqueda trae 50 de hasta 285 ⇒ **habría ordenado el
  recorte y no el catálogo**, y los **31 productos de su especie que el
  orden alfabético no alcanzaba** habrían seguido invisibles. *El número
  que lo probó salió de medirlo, no de razonarlo.*
- **NO curé el nombre de la tarjeta** ni el hueco de la ficha: los dos son
  de `packages/ui`. Salieron como H-303 y C-06 **con su número**.
- **NO apagué R29 ni monté un `PieDeCampo` decorativo** para quedarme con
  26 dp.
- **NO monté el carrito flotante de N25** aunque la ley es de la vitrina:
  **no está en mis seis hallazgos**, el glifo con ruedas recién llegó a
  `main` **sin gate de ícono**, y la barra podría ganar una quinta tab
  (pedido de D a B) — *montar una puerta nueva mientras se discute la
  gramática de la barra es fabricar una segunda puerta al mismo cuarto,
  que es justo lo que N25 vino a matar.* **Declarado para el gate.**
- **NO toqué lo que el founder aprobó**: la imagen, el `+`, el precio, los
  chips de presentación, la composición.

---

# ⑤ DEUDAS VIVAS — con dueño y disparo

| # | qué | dueño | disparo |
|---|---|---|---|
| ~~**H-301**~~ ✅ | **CERRADO por firma del founder 18-ago: la búsqueda ORDENA por la especie de la mascota, no filtra.** Medido: 19 resultados cambian de posición y **31 son productos que el orden alfabético no alcanzaba**; 0 de 3 leídos son de otra especie (antes 3 de 3). Reversible en una línea | — | cerrado |
| **la señal en la tarjeta** ⏳ | «para perros» en un resultado de otra especie: **APROBADA por el founder y DIFERIDA** — no hay margen de alto | **founder / C** | cuando la vitrina recupere presupuesto |
| **H-302** | los 26 dp del `PieDeCampo` en el buscador: `Campo` necesita que «este campo no tiene mensaje» sea **inexpresable**, sin aflojar R29 | **B** (aceptado, con censo previo) | su propia vuelta — acordado que no entra en ésta |
| **H-303** | el nombre a **16/24 dp** parte en 2 líneas en **8 de 12** tarjetas; **48 dp de los 376** | **B** | la próxima pasada de `packages/ui` |
| **C-06** | el hueco del pie de la ficha: **curado por B, NO verificado** — mi vara no puede verlo | **B / founder en aparato** | el publish |
| **H-103** | **23 de 50 tarjetas sin foto**; el catálogo tiene **161 de 470 con imagen** | **founder / catálogo** | los seis comprables de octubre |
| **el margen** | **42 dp** es todo lo que le queda a la vitrina antes de que la primera tarjeta se corte otra vez | **quien agregue alto** | cualquier pieza nueva en el eje |

---

# ⑥ LO QUE MI RELEVO TIENE QUE SABER

**Los dos aparatos son míos y se corren así:**
```
node scripts/medir-s100c-vitrina.mjs  8 8093 <rotulo>   # cromo · filtros · ficha · nombre
node scripts/medir-s100c-busqueda.mjs 8 8093 <rotulo>   # C-01 y la especie de los resultados
```
Necesitan `apps/cliente/.env.local` (gitignored, se copia del worktree
principal), Metro web **en puerto propio, jamás 8081**, y la clave sale del
**keychain al momento** (R6). Para consultar la base hace falta copiar
`supabase/.temp/` del worktree principal — este worktree no nace linkeado.

🔴 **LAS DOS TRAMPAS QUE ME MORDIERON, las dos de instrumento:**

**① El selector que devuelve la respuesta que esperabas.** Mi primer
detector de filas de chips buscaba `[role="button"]` bajos y angostos y
**devolvió los `+` de la grilla**. Con él, el reporte decía *«NO se dibuja
la fila de especies»* — **que es exactamente la conclusión a la que yo
quería llegar**, y por eso casi la publico. *Un fallo de selector se lee
igual que el defecto que uno vino a medir.* La versión buena **se ancla en
una tarjeta real y sube al padre**, sin adivinar cuántos envoltorios hay.

**② El exit code del pipe, dos veces en un día.** `npx tsc --noEmit | head`
imprimió `TSC_EXIT=` vacío y me hizo dudar de un typecheck que estaba
verde. **El exit se lee del COMANDO** (`cmd > archivo; echo $?`), jamás del
pipe — L-191, cobrada acá igual que la cobró mi predecesora.

**Y la de mi predecesora, que sigue viva:** `expo-router` conserva la
pantalla anterior en el DOM ⇒ **todo lo que se busque va acotado a
`[data-medicion-scroller]`**.

---
