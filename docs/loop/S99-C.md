# S99-C · PARTE DEL LOOP

**Pista C — prestador y ventas.** Rama `pista/s99-c`, worktree propio
(`e-petplace-s99-c`), árbol declarado abajo. **L0 = solo medición: no se
construyó ni una pantalla.** Todo lo de acá está medido contra el objeto (el
`.tsx`, la base viva, el aparato); lo que es propuesta lo dice con todas las
letras.

> **Leyenda:** 🔴 bloquea o cuesta plata · 🟡 hay que decidirlo antes de
> construir · ⚪ medido, sin acción hoy.

---

## ✅ L2 ESTÁ CAMINABLE — DISPARA EL GATE 2

**La ficha del repartidor camina en sus DOS entradas** (`nuevo` y `[id]`),
verificada DONDE VIVE (método nuevo del founder). Punta de rama:
**`18e9fb8b`**, en origin por sha, árbol en 0, `verify:diseno` VERDE 38.

| lo caminado | resultado |
|---|---|
| la anatomía de la receta, frase por frase | **8/8 presentes** |
| lo que NO puede aparecer (teléfono · «opcional» · voz genérica · el tope escrito) | **4/4 ausentes** |
| la EDICIÓN carga del motor con la MISMA pieza (D-791) | ✅ |
| errores de página, las dos entradas | **0** |

**Lo que encontró CAMINAR y ningún instrumento vio:** en la edición de un
repartidor sin foto el CTA quedaba apagado pidiéndola ⇒ **no se podía
corregir un documento mal tipeado sin subirle una foto** — justo lo que
D-791 vino a curar. Curado: la foto se exige en el ALTA, no al editar.

**Y un falso rojo que NO reporté:** leí la bandera como Colombia contra un
prefijo +593. Medí: `PAIS_DEFAULT='EC'`, y 🇪🇨 se ve casi igual que 🇨🇴 a
esa escala. *Era mi lectura, no la pantalla.*

### 📮 DOS PEDIDOS A A — ninguno bloquea el gate, los dos están declarados en la pantalla

1. **`listarRepartidores` no devuelve `correo`** (medido contra el tipo
   `Repartidor`). Por eso en edición el campo nace vacío, **solo viaja si
   se escribe algo** —mandarlo vacío lo BORRARÍA— y no se exige.
2. **No hay lector de viajes por repartidor.** El dato existe
   (`envios.repartidor_id` + `entregado_en`, medido); `misEntregasAsignadas`
   es del repartidor logueado, no del vendedor mirándolo. **Por eso ⑤ no se
   monta**: un cero fijo mentiría sobre alguien que sí entregó (L-139). Las
   voces ya están escritas para el día que llegue.

### 🛑 LO QUE FRENÉ, con su razón

- **La Hoja del RECURSO no gana pie fijo todavía**: tiene una advertencia de
  consecuencia («esto afecta N entregas») justo antes del CTA. Subir solo el
  botón la dejaría fuera de vista en el momento exacto en que hay que
  leerla; subir las dos mete contenido en un slot que la ley reserva al
  compromiso. **Pide mirarse en aparato.**

---

## 🚨 PARA A — EL MERGE QUE DISPARA EL OTA

**`pista/s99-c` está en origin, verificada por sha y por contenido.** Rango a
mergear a `main`, del más viejo al más nuevo:

| commit | qué lleva |
|---|---|
| **`c4e8ad12`** | 🔴 **LA CURA QUE DISPARA EL OTA** — el techo de Cuenta dejaba de llamar «No pudimos cargar tu negocio» al vendedor puro |
| `1b0cf673` | el parte con la ventana de aparato (solo `docs/`) |
| `ec4b52fb` | una sola entrada para el vendedor puro (③) + las 6 voces de R44 (④) |
| `d149a861` | R42 de 10 a 6 — las puertas de foto del prestador (⑤) |
| los de `docs/` | el parte; no tocan código |

**Las TRES curas del vendedor puro viajan juntas en este OTA** y ninguna se vio
corriendo todavía: el techo de Cuenta (`c4e8ad12`) · el HOY sin tarjeta roja y
«Casos» ausente (ya verificadas en aparato, pero desde mi worktree) · y la
entrada única de `ec4b52fb`. **`verify:diseno` VERDE 37 · typecheck VERDE** en
la punta de la rama.

---

# 🧭 HANDOFF — SE LEE PRIMERO Y NO SE RECONSTRUYE DE MEMORIA

> Depositado por la regla de compactación de `CLAUDE.md` (S99): *lo que vive
> solo en la conversación muere con ella.* **Todo dato de acá se re-mide
> contra el objeto antes de usarlo** (L-166): este bloque dice DÓNDE
> retomar, no qué es verdad hoy.

**Identidad y territorio.** Pista **C** — prestador y ventas. Worktree
`e-petplace-s99-c`, rama **`pista/s99-c`**. Míos (76h): `apps/prestador/**`
· `docs/loop/S99-C.md` · `scripts/s99/**` · `scripts/captura-s99c-l1.mjs` ·
`scripts/capturas/s99-c-l1/**`. **NO míos:** `packages/**` (B) · migraciones
y `packages/api` (A) · el cascarón de la barra y el HOY del dual (D).

**Estado al momento del depósito** — re-verificar, no creer: rama en origin
**sincronizada por sha** (`ls-remote` = `rev-parse`, jamás por código de
salida — L-239) · **NO mergeada a `main` todavía: la mergea A** · typecheck
prestador VERDE · `verify:diseno` VERDE 37 · árbol en 0.

### LA COLA VIVA, en orden, con el próximo paso ejecutable

| # | qué | próximo paso concreto |
|---|---|---|
| ① | 🔴 **EL CONTENIDO DEL HOY TARDA 1,5 s MÁS QUE ANTES** (§V ①) | **es lo primero de la próxima pasada**: 2,0–2,5 s → **3,8–4,0 s**, mismo método, misma ancla, misma cuenta. Bisecar por bundle (A/B/D entraron en el mismo OTA) o por olas |
| ② | **Las TRES curas necesitan OTA** (HOY del vendedor puro · «Casos» · el techo de Cuenta) | **no es mío publicar** (regla 82): A mergea y publica. Las dos primeras están VERDES en aparato; **la del techo de Cuenta es de esta ventana y NO se vio corriendo** |
| ③ | **El truncado del nombre de producto** (§V ②, clase de 4 superficies) | decidir la forma (dos líneas · dato a la izquierda · ancho mínimo) y curar en las cuatro de una |
| ④ | **Caminar lo que queda** (universo abajo, ya achicado) | con el OTA nuevo |
| ⑤ | **`FotoDelRepartidor` → `HojaCaptura`** | cura, no swap: cierra el doble disparo (8 puertas sin cerrojo, censo de B) |
| ⑥ | **Las voces de N12.4 (R44, baseline 6 mías)** | **con su condición**: Ley 23 primero (validar antes de mandar); los códigos por constraint se le piden a A contra **D-827** |
| ⑦ | **La `nota` de D-791** | la Hoja de recurso deja de calcular la ley del cambio y consume la del motor — **la copia muere EN EL MISMO LOTE** o difieren en silencio |

### 📵 EL UNIVERSO DE LO NO CAMINADO (L-238 — una ausencia sin universo no es medición)

**ACHICADO en la ventana del 15-ago** (§V): se caminaron Stock, Venta de
mostrador, Mis entregas, el detalle de un pedido, Configuración, la galería y
**tres Hojas con campos** (ajustar stock · registrar repartidor · agregar
producto), con el teclado abierto en las dos formas.

**Queda sin caminar:** la Hoja de **agregar corte** · la de **recurso** · las
internas de **Cuenta** (Seguridad · Preferencias · Tu histórico) · **las tres
acciones del repartidor EJERCIDAS** (no se tocaron: escriben en la base) · **y
todo el recorrido del EMPLEADO-VENDEDOR §0bis**, que no tiene cuenta sembrada
que yo sepa.

### 🟡 SERVIDO SIN CURAR (es del founder, o es dato, o es de otra pista)

- **`manana` sin ñ** como nombre de corte: **dato del fixture**, no cadena
  nuestra. Confirmado en aparato el 15-ago.
- **«Documentos — Cargando…»** quedó así en una captura. **NO lo declaro
  defecto: una sola observación no distingue lento de colgado**, y no volví a
  mirar. *Nombrarlo sin medirlo sería fabricar un rojo.*
- **`DATOS` del vendedor puro sigue sin su contenido de venta** (§2.0 lo deja
  abierto a propósito: sus specs finas no están firmadas).
- **La escalera pinta igual «hecho» y «actual»** — **NO es defecto**:
  `EscaleraEstados.tsx:174` lo dice literal, *«hecho y actual se llenan igual
  a propósito»*, con su razón escrita arriba (el paso actual preside por el
  PESO DEL TEXTO). Lo que se sirve es la **lectura en aparato**: con un pedido
  `en_reparto` la tarjeta muestra 4 de 4 segmentos llenos y la palabra
  «Entregado» — y en la tarjeta compacta **no hay peso de texto que
  desambigüe**. **Destinatario B.** *Es juicio de diseño y por eso no lo
  juzgo: traigo lo que se ve.*
- **El rótulo cortado por el título fijo de la Hoja** (§V ③) — de `Hoja` /
  `HojaScroll` de `packages/ui`. **Destinatario B.**
- **La duplicación HOY ↔ raíz `/ventas`** para el vendedor puro (§V ⑤): es la
  costura acordada con D, con su lápida y sus dos condiciones de muerte
  escritas. **No se cura unilateralmente.**

### COMPROMISOS ENTRE PISTAS — y el canal está CAÍDO

**El canal entre sesiones no está disponible: TODO viaja por repo.**
· **A** — le debo pedidos de código por constraint contra **D-827** cuando
L1 choque con cada puerta; me debe el merge a `main` y el OTA.
· **D** — la pieza `ventana-pedidos` está lista y su contrato **obliga**
`sinFecha` con `dia` (no compila si se olvida); nada suyo me bloquea.
· **B** — nada pendiente en ninguna dirección.

### SEGURIDAD, VERBATIM Y SIN EXCEPCIÓN

Las claves se leen del **keychain AL MOMENTO**
(`security find-generic-password -a <cuenta> -s <servicio> -w`) y **jamás**
quedan en chat, reporte, resumen ni repo — solo se publica su **FORMA**
(largo, clase de caracteres). **Verificación de una línea antes de usar el
PIN:** `… -w | grep -qE '^[0-9]+$'`. `service_role` jamás en el árbol; L-140
en toda función nueva.

### NUMERACIÓN

`D-NNN` · `L-NNN` · `R-NN` de este parte son **punteros**. El próximo número
libre **se re-verifica por grep contra `DEUDAS_CANONICAS.md`**, jamás se toma
de acá (L-141: la línea resumida decae, el objeto no).

---

## ⓪ · ARRANQUE Y FRENOS

**Hecho:** worktree propio desde `main` (`52d7ebee`, **mergeado a
`35e00011`** al llegar la tanda de A) · `supabase/.temp` copiado para poder
consultar la base desde el worktree · dispositivo `R5CY201ZDVL` conectado por
USB. **Tras el merge se re-verificaron los diez anclas `archivo:línea` que
cita este parte: los diez siguen apuntando a lo que dicen** — *un parte que
cita líneas envejece con el merge del vecino, y el que lo lee no tiene cómo
saberlo.*

### 🔴 LA TRAMPA DEL PUERTO — VERIFICADA POR MÍ, Y EL HUÉRFANO ERA MÍO

A avisó (hallazgo de D, enmienda a D-769). **No lo tomé por dicho: lo medí**
(`lsof -nP -iTCP:<p> -sTCP:LISTEN` + `cwd` del pid):

| puerto | cwd del Metro que lo servía | qué era |
|---|---|---|
| **`:8081`** | **`e-petplace-s97-c/apps/prestador`** | **MI worktree de S98, de una sesión CERRADA** |
| `:8082` | `e-petplace-s97-d/apps/prestador` | huérfano de D (no lo toco: no es mío) |
| `:8083` | `e-petplace/apps/cliente` | el primario |
| `:8084` | `e-petplace-s99-d/apps/prestador` | D, correcto |

**El de `:8081` arrancó el 14-ago 16:54 y seguía vivo.** ⇒ **cualquier
medición mía contra «mi» puerto por convención habría medido el árbol de
S98, con la app respondiendo normal.** Matado (`kill 92915`), `:8081`
verificado en **0 listeners**.

> *Es la forma exacta que este parte ya registra tres veces: el nombre decía
> una cosa (`:8081` = C) y la cosa era otra. Y acá el que dejó la trampa
> puesta fui yo, al cerrar S98 sin matar mi Metro.*

⚠️ **`:8082` sigue tomado por el huérfano de la worktree S97 de D** — se
reporta, no se mata: no es mi proceso.

### ⚠️ PRECONDICIÓN DE L1, MEDIDA AL INTENTAR UNA SONDA

Este worktree **no resuelve `@epetplace/api` ni `@epetplace/ui`**: un `tsc`
falla con `TS2307 Cannot find module` en **todos** los imports, no en los
míos. ⇒ **antes de construir en L1 hay que correr `CI=true pnpm install`**
(la receta que S98 dejó escrita). *Se declara porque una sonda de
compilación corrida acá da rojo por la razón equivocada — me pasó, y por eso
el hallazgo de §C-A abajo se sostiene en evidencia estática y no en ese
rojo.*

**Archivos que declaro (76h) — L0 no toca código de producto:**
`docs/loop/S99-C.md` · `scripts/s99/censo-viajes-pantalla.mjs`.

### 🔴 FRENO 1 — LA LLAVE DEL DISPOSITIVO NO ABRE EL DISPOSITIVO (bloquea C4)

La entrada de keychain que el acta S98 §5 documenta como el PIN del aparato
**no es un PIN**. Medido, sin imprimir el valor en ningún lado:

| qué se midió | resultado |
|---|---|
| `security find-generic-password -a pin -s epetplace-dispositivo-s97` | existe · **9 caracteres · 5 mayúsculas + 1 minúscula, cero dígitos** |
| comparación con `-a siembra -s epetplace-siembra-s97` | **IDÉNTICAS: la entrada del «PIN» guarda la clave de las cuentas** |
| la pantalla de bloqueo del aparato (dump de la jerarquía) | **teclado NUMÉRICO** — `keyguard_pin_view`, «Enter PIN», teclas 0–9 |

⇒ **una clave alfabética contra un teclado numérico: no hay forma de que
entre.** No se adivinó, no se reseteó, no se forzó (§6ter de S98 rige). El
aparato quedó **como se lo encontró**: en reposo, sin intentos fallidos
registrados —el `input text` con letras no deposita nada en un pad numérico,
y el dump muestra el botón OK todavía `Disabled`, o sea campo vacío— y el
residuo de la inspección (`/sdcard/ui.xml`, `ui2.xml`) borrado.

> *Nadie escribió mal el acta: **nadie abrió la entrada**. Es exactamente la
> forma que esta pista lleva cinco ventanas catalogando — medir la ETIQUETA
> («la entrada se llama pin») en vez de la COSA (lo que guarda). Y el costo
> se paga cuando alguien la necesita, que es hoy.*

**Lo que destraba C4, y es del founder:** el PIN real del aparato (o que
desbloquee y lo deje despierto). **Un comando, no una sesión.** Con eso, el
censo de tiempos corre entero con el método de §C4 abajo.

### ⚪ FRENO 2 — `DIRECCION_DISENO_S99.md` no existe todavía

La lectura mínima del arranque lo pide y **no está en el árbol** (`grep` en
cero sobre `docs/`). Leí en su lugar `DIRECCION_ARTE` §13 (el Norte N1–N10
con sus enmiendas). **Se declara para que nadie lea este parte como si
hubiera contrastado contra una letra que todavía no llegó.**

---

## C1 · EL DURANTE DEL VENDEDOR — QUÉ MUESTRA HOY, DEL OBJETO

**El founder dice que ve cuatro, y es verdad: son cuatro, y el corte vive en
un solo archivo.**

`apps/prestador/src/lib/escalera-pedido.ts:52-53` — las dos listas, literales:

```
PASOS_DESPACHO = preparado · empacado · despachado · entregado
PASOS_RETIRO   = preparado · empacado · facturado  · retirado
```

Sus dos consumidores: `ventas/index.tsx:187` (la fila del panel) y
`ventas/pedido/[pedidoId].tsx` (el detalle). **Ninguna pantalla lee el estado
crudo** — el corte es único, como `corte-agenda.ts` en el cliente.

### Lo que la fuente tiene detrás de esos cuatro

Medido contra `cat_estados_pedido`: **30 estados internos, 16 activos, 7
narrativas.** El mapeo (`situar()`, líneas 56-91) cubre **los 16 activos** —
14 explícitos y 2 (`creado`, `esperando_pago`) que caen al `default` **a
propósito**: sin escalera y sin botón, con la narrativa del catálogo hablando
sola. *No hay ningún estado activo sin tratamiento: eso está sano.*

**⚪ Y lo que conviene saber sin actuar hoy:** los **14 inactivos** también
caen al `default`. Correcto mientras estén apagados; **el día que alguien
active uno —`backorder`, `en_espera`, `esperando_courier`— el pedido pierde
la escalera sin que nada falle.** Es la clase «funciona mal sin fallar». No
se cura acá porque activar un estado es decisión de motor (A) y hoy no hay
ninguno prendido.

> ### ✅ ESTE ⚪ YA TIENE GUARD — y lo probé en rojo yo, no lo acepté verde
>
> A construyó **`scripts/verify-escalera-pedido.mjs`** (main `2d5b396b`)
> sobre el pedido que le hice al cerrar el cruce: *si el motor se mueve, mi
> archivo no se entera y nadie lo ve.*
>
> **Lo primero que verifiqué no fue que diera verde, sino que NO tuviera una
> copia del mapeo**: lee `escalera-pedido.ts` con `readFileSync` y parsea sus
> `case` (líneas 32-41). *Un guard con su propia copia del mapa no vigila
> nada: es una segunda verdad que da verde para siempre.*
>
> **Los tres brazos, cada uno con su rojo producido por mí y su restauración
> verificada (residuo 0 las dos veces):**
>
> | brazo | rojo que produje | qué gritó |
> |---|---|---|
> | R1 + R3 | apagué `case 'picking'` | *«estado ACTIVO sin mapear… picking»* + *«transición viva del vendedor desde picking»* |
> | R2 | agregué `case 'inventado'` | *«el mapeo nombra un estado que el catálogo ya no tiene»* |
>
> **Verde: 14 mapeados · 16 activos · 10 orígenes con botón** — y esos dos
> primeros números **coinciden con los que este parte midió por su cuenta**
> en §C1, por un camino distinto (yo leyendo `situar()` y consultando el
> catálogo; el guard parseando y consultando). *Dos métodos independientes
> dando el mismo número es lo más cerca que se puede estar de saberlo.*
>
> ⚠️ ~~**Nicety para A, no defecto:** su parser reconoce `case '[a-z_]+'`,
> así que un label con mayúscula —un typo— **no lo ve**.~~ **✅ CURADA el
> mismo día** (main `3df95f1e`, parser a `[a-zA-Z0-9_]+`). *Se descubrió sin
> buscarla: mi primer rojo renombró a `picking_APAGADO` y **R2 no disparó**,
> solo R1/R3.* **Re-verificada acá con mi repro exacta: ahora gritan LOS
> TRES** —R2 incluido, nombrando `picking_APAGADO`—, restaurado verde
> 14/16/10, residuo 0. *El hallazgo salió de un rojo mal hecho: rompí el caso
> de una forma que el instrumento no podía ver, y eso destapó el límite del
> instrumento en vez del de mi archivo.*

### 🟡 MI PROPUESTA — LOS CUATRO SE QUEDAN; LO QUE FALTA NO ES UN ESTADO

Bajo el criterio firmado (`LETRA_PANEL` §3 · `MODELO_DESPENSA` §8.6 v2.1:
*son cuatro escalones, no dos*), **la vista no necesita más estados: necesita
que el cuarto se lea como ajeno.** La letra del recorrido (§5) firma que
**el vendedor marca los TRES del local y el cuarto lo marca quien está en la
puerta** — y hoy la escalera pinta los cuatro con la misma tinta, así que el
vendedor ve un paso que él no puede dar y que ningún cartel le dice que no es
suyo.

⇒ Propuesta al founder, **una sola línea de diseño y cero motor**: los tres
del local se leen como acción y **el cuarto se lee como espera** (mismo
escalón, otra intensidad, con su voz: *«lo marca quien entrega»*). Se
contrasta contra N5 (un acento por pantalla) en el lote.

### ✅ EL CRUCE CON A1, HECHO — y achica L3 en vez de agrandarlo

A propone para la vista del vendedor **6 grupos** («entra el estado que cambia
lo que puede HACER o RESPONDER»,
`2026-08-15-s99a-L0-CENSOS.md` §A1). **No choca con los cuatro escalones
porque no es el mismo eje:** el escalón dice **dónde está**, el grupo dice
**qué puede hacer**.

🔴 **Y lo que hay que decir antes de que L3 lo construya: los seis grupos YA
EXISTEN, con nombre, en `escalera-pedido.ts`.** `situar()` no devuelve solo
`hechos`: devuelve **`accion`**, y el mapeo con la propuesta de A es **1 a 1**:

| grupo de A | `AccionPedido` que ya se computa |
|---|---|
| Nuevo | `preparar` |
| Preparando | `empacar` |
| Empacado | `facturar` |
| Listo para salir | `despachar` · `entregarRetiro` (retiro) |
| En la calle | `enManosRepartidor` · `reintentar` (con `desvio: 'noLlego'`) |
| Cerrado | `ninguna` |

Las dos únicas diferencias son **más finas, no contrarias**: yo saco
`entrega_fallida` del grupo y lo subo a **banda de desvío** (decisión ① de la
pieza), y parto «Listo para salir» en dos según el método de entrega.

⇒ **L3 no construye la agrupación: la consume.** *Si la re-implementa,
tenemos dos criterios contando el mismo hecho — el modo de falla que
`LA_CASA` §4.4 nombra como el más caro de esta clase de pantalla.*

---

## C2 · LA SECCIÓN «RECURSO» — DÓNDE VIVE Y QUÉ LE FALTA

**Dónde vive:** `apps/prestador/src/app/ventas/configuracion.tsx:951-992`
(la sección) + su Hoja en `1288-1343`. Entrada: `Ventas › Configuración`.

**Qué muestra hoy, campo por campo:** `nombre` (título) · `capacidad_por_dia`
(metadata mono) · el `activo` **solo como subtítulo, sin toggle**. Su Hoja
edita **dos cosas**: nombre —fijo al reabrir, es la llave del upsert (D-791)—
y capacidad. Nada más.

### 🔴 EL HALLAZGO: SON DOS ENTIDADES Y LOS DATOS DICEN QUE LA GENTE USA LA EQUIVOCADA

Medido contra la base viva:

| tabla | filas | qué es |
|---|---|---|
| `repartidores` | **5** | la PERSONA (nombre · documento · tipo · teléfono · whatsapp · 2 fotos · `user_id`) |
| `recursos_reparto` | **5** | la CAPACIDAD del negocio (nombre · capacidad/día · días) |
| `repartidor_vehiculos` | **0** | los vehículos de la persona (S98: techo de dos, inexpresable) |

**Los cinco recursos vivos se llaman «Moto de pruebas», «Moto de la Tienda
Pura», «Moto S97 de Pruebas», «Moto Todo S97», «Moto (demo S98)».** Y la
tabla de vehículos —construida en S98 con su techo de dos y su UNIQUE— **está
en cero.**

> ⇒ **Todo el que configuró reparto modeló su moto como RECURSO, no como
> vehículo de una persona.** No es una opinión de diseño: es lo que hicieron
> las cinco veces que alguien lo hizo, incluida la del gate de S98.

### 🔴 Y EL SEGUNDO, QUE ES PEOR: SE PUEDE DESPACHAR A QUIEN NO PUEDE VER EL ENVÍO

Medido: `repartidores.user_id` es NULLABLE · **2 de 5 lo tienen en NULL**, y
uno de esos dos es **Marco — el ÚNICO repartidor del ecosistema con la ficha
completa** (tipo de documento, WhatsApp y foto de la persona, el fixture que
A caminó en el aparato en S98).

- `despachar_pedido(p_pedido_id, p_repartidor_id)` **no menciona `user_id`
  en ninguna parte de su cuerpo** (4.131 caracteres, verificado por
  `pg_get_functiondef`).
- La pantalla del repartidor lee por `misEntregasAsignadas()`, o sea **por
  sesión**.
- El alta (`registrar_repartidor`) acepta `p_user_id` **y la pantalla nunca
  lo manda** — por eso Marco no tiene.

⇒ **El vendedor puede asignarle un envío a Marco, ver el pedido «despachado»,
y Marco no lo ve nunca.** Hoy no hay daño en curso (Marco tiene 0 envíos;
los 2 envíos vivos fueron a repartidores que sí tienen cuenta), pero **el
camino está abierto y es silencioso**: no falla, no avisa, entrega tarde.

### Qué le falta a la ficha que pide C2 — medido, no estimado

| lo que pide C2 | estado hoy |
|---|---|
| foto (persona) | ✅ `foto_path` · **obligatoria en el alta** (firma S98) |
| documento + tipo | ✅ `documento` · `tipo_documento` + foto del documento |
| nombre | ✅ |
| WhatsApp | ✅ `whatsapp` E.164 · obligatorio |
| vehículos (máx 2) | ✅ tabla + techo inexpresable — **y 0 filas: nunca se usó** |
| **correo-llave** | ❌ **NO EXISTE COLUMNA.** Lo más cercano es `user_id`, un uuid que ninguna pantalla llena |
| **viajes derivados** | ⚠️ **NO existe el lector, pero el dato SÍ: `envios.repartidor_id` existe.** Es un `count`, no una migración |

**⚠️ El comentario de cabecera de `configuracion.tsx:93-103` está VIEJO** —
dice *«el repartidor no se toca en esta ventana»* y *«`repartidores` no tiene
ninguna de esas columnas ni existe tabla de vehículos»*, y S98 construyó las
dos cosas justo debajo (`guardarRepartidor` en 580+ sube fotos y registra
vehículos). *Un comentario que describe un mundo que ya no existe se lee como
medición y manda a la sesión siguiente por el camino largo* (L-170).

**L2 (el renombre y la ficha) NO se toca hoy**, como ordena el arranque. Lo
que este censo agrega a L2: **son dos entidades y hay que decidir si se
funden** (el dato dice que la gente ya las fundió a mano), **y el
correo-llave es lo único que no existe en ninguna forma.**

---

## C3 · FORMULARIOS CANDIDATOS A IA — PANTALLA POR PANTALLA

### 🔴 EL HALLAZGO QUE REORDENA LA PRIMERA TANDA: EL MOTOR YA ESTÁ DESPLEGADO Y NO LO LLAMA NADIE

`supabase/functions/extract-documento/` existe, y **está ACTIVE en el
proyecto remoto** (`version 1`, `verify_jwt: true`, medido con
`functions list`, no leído del acta). Su cabecera declara el contrato exacto:
*«el alta del repartidor se pre-llena de una foto del documento; la persona
CORRIGE, no digita»*, con L-139 en el prompt y en el validador, y con la
cláusula que la vuelve segura: **no escribe en la base — devuelve lo leído y
la superficie decide.**

**Llamadores en todo el monorepo: CERO.** (`grep` sobre `apps/` y
`packages/`: ni una ocurrencia, ni wrapper en `packages/api`.)

⇒ **Motor sin puerta, y desplegado.** La primera tanda de IA no es
construcción desde cero: es **cablear una puerta** a algo que ya corre.

### El censo de los cuatro, con su dolor real

| formulario | dónde | campos que se tipean | dolor | frecuencia |
|---|---|---|---|---|
| **alta de repartidor** | `configuracion.tsx` Hoja 1074-1247 | **nombre · documento · teléfono · WhatsApp** (4 tipeados) + tipo de doc (selector) + país (Hoja) + **2 fotos** + hasta 2 vehículos (selector + placa) ⇒ **hasta 12 controles** | **alto y compuesto**: el teléfono y el WhatsApp piden el mismo número dos veces con indicativo; el documento tiene máscara que rebota; **y las dos fotos YA SE SUBEN** — la del documento se guarda y no se lee | **baja** (5 repartidores en todo el ecosistema) |
| **stock — carga** | no existe | — | — | — |
| **stock — ajuste** | `ventas/stock.tsx` Hoja 212-257 | dirección (selector) · cantidad · **motivo** (texto libre) | **bajo por ajuste, alto por repetición**: 3 controles, pero uno por SKU y por vez | **alta** (es la operación diaria) |
| **alta de mascota del mostrador** | `mostrador/nueva.tsx` 204-276 | nombre mascota · nombre cliente · email **o** teléfono (toggle) + especie (selector) ⇒ **4 controles** | **bajo**: ya es corto y el `q` de la búsqueda pre-llena el primer campo | media |
| **carnet** | **es del CLIENTE**, no del prestador (`extract-vacuna`, ACTIVE v34) | — | **ya resuelto por IA desde S48** | — |

**⚠️ Un hueco de dato, no de IA, que aparece de paso:** el alta del mostrador
**no pide raza**, y `LA_CASA_DEL_PRESTADOR` §6.1 firma *«nombre, especie,
raza»*. No es de esta tanda; se declara para que no se lea como omisión del
censo.

### 🟡 MI PROPUESTA DE PRIMERA TANDA (al founder)

**① El alta de repartidor, cableando `extract-documento`.** No porque sea el
más frecuente —es el menos frecuente de los cuatro— sino porque es **el único
donde el motor ya está desplegado, la foto ya se sube y la persona ya está
corrigiendo a mano lo que la máquina podría leer.** Es la tanda más barata
del mapa y la única que hoy paga infraestructura sin devolver nada.

**② La carga de stock por archivo** (`MODELO_DESPENSA` §8.6quater ②: PDF o
Excel que la IA lee y organiza). **Es la que cambia la escala** —un vendedor
real no carga cien productos a mano— y es la que su propia letra marca como
**posterior al 15-sep** (§14). *Entra al mapa con su fecha, no al lote.*

**③ Nada más.** El ajuste de stock y el alta del mostrador **no son
candidatos**: tres y cuatro controles no se arreglan con IA, se arreglan con
menos toques. *Poner IA sobre un formulario corto es cambiar un problema
chico por una dependencia grande.*

---

## C4 · CENSO DE TIEMPOS EN DISPOSITIVO — ✅ CORRIDO (ventana del founder, 15-ago)

> ⏪ **Este bloque nació 🔴 BLOQUEADO** (freno 1: el aparato no se podía
> desbloquear). El founder abrió la ventana y **el censo corrió entero**. Se
> conserva el encuadre porque el método que se había escrito a ciegas es el
> que se ejecutó, y eso es lo que lo vuelve repetible.

### 🔴 LO PRIMERO, Y CORRIGE LA PREMISA DEL PEDIDO: EL APARATO NO CORRE S99

**Antes de creerle a un número verifiqué sobre qué objeto respondía**, y no
era el que decía la orden de arranque:

| qué | medido |
|---|---|
| mi rama | `36b0b84c` · main `43f94546` **mergeado** · árbol 0 |
| APK instalado | `com.epetplace.prestador` **1.0.5**, `lastUpdateTime` **12-ago** |
| **OTA que CORRE** | **`01a00373-5155-78e5-a53e-69081b7c7a03`** · `embedded=false` · canal `preview` |
| cuenta | **`demo-vet` / Clínica Aurora** (leída del árbol de accesibilidad, no supuesta) |
| escalas de animación | las tres en **1.0** |

**Ese OTA es el de S98** — el acta S98 §5 lo nombra literal (`group 16908c27`
· ancla `d4613ce9`). ⇒ 🔴 **el aparato NO tiene nada de S99: ni N11, ni los
121 campos re-vestidos, ni la tanda de A.**

> **La orden de arranque decía «este baseline ya es post-N11 y hay que
> decirlo». Es al revés: es PRE-S99, y hay que decirlo.** *No es un detalle
> de prolijidad: si este «antes» se archiva como post-N11, toda comparación
> futura queda anclada a una premisa falsa — y el baseline existe justamente
> para eso.* **Es la quinta vez en esta jornada que el instrumento respondía
> sobre otro objeto, y la más cara de las cinco.**

### EL FRÍO — tres corridas, mediana (`scripts/s99/censo-tiempos-aparato.sh`)

| anclaje | c1 | c2 | c3 | **mediana** |
|---|---|---|---|---|
| **primer frame** (`am start -W`) | 352 | 334 | 327 | **334 ms** |
| **JS corriendo** (log `[update]`) | 289 | 272 | 262 | **272 ms** |
| **sesión resuelta** (log `[sesion] raíz prestador`) | 1468 | 1228 | 1224 | **1228 ms** |

⚠️ **Los 334 ms NO son «la app abre en un tercio de segundo».** `am start -W`
mide hasta el primer frame de la Activity — en React Native, el splash.
*Reportarlo solo sería el número bonito y falso.*

### EL CONTENIDO — bracket por offsets fijos, ±250 ms

**Ausente a +2,0 s · PRESENTE a +2,5 s** (ancla: `Hoy libre · 1 esta semana`,
un texto que **solo existe con datos cargados**). ⇒ **el día se ve entre 2,0
y 2,5 segundos** desde el toque.

> **El instrumento se corrigió a sí mismo, otra vez:** mi primera versión
> SONDEABA en bucle y cada `uiautomator dump` costaba **3,4 s** en este
> aparato ⇒ el «techo» salía 6,9–7,7 s y **medía mi propio sondeo, no la
> app**. Se descartó y se reemplazó por offsets fijos con arranque en frío
> por corrida. *Un instrumento cuyo costo domina la magnitud que mide no está
> midiendo.*

### EL CALIENTE — moverse entre tabs

**HOY→DATOS y HOY→NEGOCIO: contenido presente a +0,4 s**, el offset más chico
que muestreé ⇒ **por debajo de 400 ms**.

⚠️ **Su límite, declarado:** la tab conserva su contenido montado, así que
«presente» incluye datos de la visita anterior y el refresco corre detrás.
**Mide que la pantalla responde, no que el dato es fresco.**

### 🔴 EL CRUCE CON EL INSTRUMENTO DE RED DE A — Y ACÁ ESTÁ EL HALLAZGO

A midió el HOY en **28 peticiones / 13 olas ≈ 1.951 ms de pura red**. Mi
aparato dice **contenido a 2,0–2,5 s**, con la sesión resuelta a 1,23 s.

**Si las olas de A fueran seriales DESPUÉS del guard, el contenido llegaría a
~3,2 s. Llega antes.** ⇒ **la app SOLAPA entre 0,7 y 1,2 s de esa red**, y
*la suma ingenua de olas sobreestima el tiempo real.* **Se declara la
discrepancia, no se promedia** — el número de A sigue siendo correcto como
TOTAL de red; lo que no es, es una cadena.

**Y el reparto que el founder pidió — cuánto es red y cuánto es render:**

| tramo | tiempo |
|---|---|
| arranque nativo + JS | **~0,33 s** |
| resolver quién soy (guard) | **~0,9 s más** |
| traer el día | **~0,8–1,3 s más** |

> ### ⇒ **De los ~2,3 s hasta ver el día, ~1,9 s son ESPERAS DE RED y ~0,4 s son arranque y pintado.**
> **El «se siente lento» es RED, no render** — y sale de dos instrumentos
> independientes que se cruzan, no de la tesis heredada de S94-PERF.
> *Y el caliente lo confirma desde el otro lado: moverse entre tabs no se
> siente lento porque no vuelve a esperar la red para pintar.*

### ⚠️ EL ÚNICO HUECO DE C4, CON SU CAUSA Y SU COSTO

**El panel de ventas no se pudo medir.** La sesión del aparato es `demovet`
—prestador de servicios, **sin cuenta vendedora**— así que `/ventas` muestra
su estado honesto `sinCuenta` y **no hay contenido de panel que cronometrar**
(medido: el bracket dio «no» a +0,6 · +1,2 · +2,0 s, y la causa es ésa, no la
lentitud). **Exige la sesión `duenotodo`**, y cambiar de cuenta en la ventana
del founder no es medir: es tocarle la sesión. **Queda con su costo escrito:
un login.**

### Las diez, con su criterio declarado

**No salen de opinión.** ① las **cinco tabs**, que se abren por construcción
en toda sesión (`(tabs)/_layout.tsx:473-485`: `index` HOY · `mascotas` DATOS
· `atender` · `negocio` · `cuenta`) · ② las **cinco de ventas** a un toque de
ellas, que son el frente vivo de esta pista (`ventas/index` · `pedido/[id]` ·
`configuracion` · `stock` · `mostrador/index`).

### EL MÉTODO, para que el después sea comparable con el antes

1. **Aparato:** `R5CY201ZDVL` · 1080×2340 · densidad 450 · **las tres escalas
   de animación en 1.0** (verificado: `animator_duration_scale`,
   `window_animation_scale`, `transition_animation_scale`).
2. **Build:** la del OTA vigente, **con el id leído EN PANTALLA** (Cuenta ›
   pie) antes de medir nada — jamás de memoria (L-160).
3. **Cuenta:** `demovet` / Clínica Aurora para las cinco tabs · `duenotodo`
   para las cinco de ventas (matriz `2026-08-13-s97a-matriz-cuentas-prueba`).
4. **FRÍO** = primera apertura tras `am force-stop`, medida con
   `am start -W` (da `WaitTime`/`TotalTime` del propio sistema).
5. **CALIENTE** = con la app ya abierta, tocar la tab/celda y medir hasta que
   **el contenido está pintado** (no el esqueleto).
6. **Tres corridas por celda; se reporta la MEDIANA**, no la mejor. *Contra
   un tiempo variable, una corrida es una moneda al aire con forma de
   medición* (candidata #22, de esta pista).
7. **Se declara la red** (wifi/datos) y si el aparato está en carga.

**Costo:** ~20 minutos con el aparato desbloqueado. **Medido: eso costó.**

### 🔑 EL PROTOCOLO DE KEYCHAIN QUE HABRÍA EVITADO ESTO — para la próxima

*La entrada actual guarda la clave de las cuentas, no el PIN, y esa confusión
costó una jornada de las cuatro pistas sin gate. Lo que falta no es una
llave: es que la llave diga qué abre.*

**① DOS ENTRADAS, UNA POR COSA, con el nombre diciendo qué abre:**

| servicio | cuenta (`-a`) | qué guarda | quién lo usa |
|---|---|---|---|
| `epetplace-siembra-s97` | `siembra` | la **clave de las 13 cuentas de prueba** | login en la app |
| `epetplace-dispositivo-s97` | **`pin-desbloqueo`** | el **PIN numérico del aparato** | `adb` para desbloquear |

**② EL COMANDO DE LECTURA, siempre el mismo:**
`security find-generic-password -a <cuenta> -s <servicio> -w`

**③ LO QUE NO SE IMPRIME NUNCA — y esto es la mitad que importa:** el valor
**jamás** va a un `echo`, a un reporte, a un commit, a un mensaje entre
pistas ni al chat. Se canaliza **directo** al comando que lo consume
(`… | adb shell …`). **Lo único que se puede publicar es su FORMA** —largo,
clase de caracteres, si dos entradas coinciden—, que es exactamente lo que
permitió diagnosticar esto sin exponer nada.

**④ Y LA VERIFICACIÓN DE UNA LÍNEA que convierte el protocolo en garantía:**
antes de confiar en la entrada del PIN, **comprobar que es numérica**:
`security find-generic-password -a pin-desbloqueo -s … -w | grep -qE '^[0-9]+$'`
⇒ *si eso hubiera existido, el freno de hoy habría durado un segundo en vez
de una jornada, y habría dicho la causa exacta.*

### ⚪ LO QUE SÍ SE PUDO MEDIR SIN EL APARATO — Y ES EL DRIVER, NO UN SUSTITUTO

S94-PERF dejó medido que *no hay consultas que optimizar, hay viajes que
eliminar*: peaje ~150 ms por petición, y las pantallas **encadenan olas**.
Eso sí se puede contar del objeto. Nace
**`scripts/s99/censo-viajes-pantalla.mjs`** (solo lectura, con su método y
sus límites escritos adentro):

| pantalla | olas encadenadas al abrir | llamadas |
|---|---|---|
| **`(tabs)/index.tsx` (HOY)** | **15** ⚠ | **32** |
| `(tabs)/mascotas.tsx` | 3 | 6 |
| `(tabs)/cuenta/index.tsx` | 3 | 7 |
| `ventas/index.tsx` | 3 | 5 |
| `mostrador/index.tsx` | 3 | 3 |
| `(tabs)/atender.tsx` | 2 | 5 |
| `(tabs)/negocio.tsx` | 2 | 8 |
| `ventas/pedido/[id].tsx` | 2 | 4 |
| `ventas/configuracion.tsx` | 2 ⚠ | 8 |
| `ventas/stock.tsx` | 2 | 2 |

**El instrumento se calibró contra un caso ya medido en el aparato**, que es
lo que lo hace creíble: el HOY da **15 olas / 32 llamadas** contra las **12
olas / 28 peticiones** que S94-PERF midió con el teléfono. Mismo orden ⇒
sirve para **ORDENAR**, jamás para prometer milisegundos.

> 🔴 **Y el instrumento se corrigió a sí mismo dos veces antes de que yo
> escribiera un número acá, así que lo dejo escrito:** su primera versión
> daba **0 olas** en `configuracion.tsx` (no seguía la cadena cuando la carga
> vive en un `cargar()` extraído — justo las pantallas más grandes) y
> **26 olas** en el HOY (expandía dos veces el mismo cargador). *Un censo que
> devuelve cero donde hay siete llamadas no avisa: entrega una tabla
> prolija.* Las dos las cazó cotejar contra lo que yo mismo había leído a
> mano y contra el número del aparato de S94-PERF — ninguna, el razonamiento.
> El ⚠ de las dos filas queda a propósito: **el instrumento no puede
> distinguir «lo expandí dos veces» de «la pantalla pide dos veces», y decir
> cuál es sin medirlo en el aparato sería inventar.**

**Esto NO reemplaza a C4.** Es el driver del tiempo, no el tiempo.

---

## C5 · STOCK HOY — LO QUE HAY CONTRA LO QUE L5 PIDE

**Existe** `apps/prestador/src/app/ventas/stock.tsx` (260 líneas) y hace
**una sola cosa**: listar los SKUs y ajustar el saldo con motivo obligatorio.
Su lector es `listarSkusDelVendedor` → `producto_nombre` · `producto_marca` ·
`presentacion` · `stock_disponible` · `stock_reservado` · `estado` ·
`precio_publicado`.

### Contra §8.6quater, punto por punto

| lo que pide §8.6quater | estado medido |
|---|---|
| ① **espejo del cliente** («el vendedor ve SU vitrina como la ve la familia») | ❌ **hoy es una lista de filas** — exactamente la «tabla de inventario» que la letra dice que no |
| ② **carga por archivo (PDF/Excel con IA)** | ❌ no existe · *(y su propia letra la pone posterior al 15-sep)* |
| ③ **productos fuera del catálogo → propuesta** | ❌ no existe · el motor **rebota `producto_no_canonico`** (M21 vivo) |
| ④ **el toggle de venta en local vive acá** | ❌ no existe en esta pantalla |

### ⚪ LA BUENA NOTICIA, MEDIDA: EL ESPEJO NO SE CONSTRUYE DE CERO

`ProductoDeVitrina` —el tipo que ya alimenta la vitrina del cliente
(`packages/api/src/wrappers/despensa-catalogo.ts:75-110`)— **ya trae todo lo
que el espejo necesita**, incluida **`foto_url` con nulo honesto**, que es
justo lo único que el lector del vendedor NO tiene.

**Lo que le falta, y es exactamente un hueco:** `FiltrosVitrina` (línea 325)
acepta `familia_codigo`, `country_code` y `limite` — **y no acepta
`cuenta_comercial_id`**. `listarProductosDespensa` filtra por
`estado='publicada'` de **todos** los vendedores.

⇒ **El espejo es: el mismo lector, con un filtro más, más el stock encima.**
Un pedido chico a A, no un frente. *Y el borde que hay que decidir antes de
pedirlo:* la vitrina muestra **lo publicado**, y el vendedor tiene SKUs
`propuesto` que la familia todavía no ve — **el espejo tiene que decir esa
diferencia, o el vendedor va a creer que publicó algo que no publicó.** Eso
es 🟡 y es del founder, no mío.

---

## COSTURA CON D — DECLARADA, NO DESCUBIERTA AL MERGEAR

El cascarón del HOY del vendedor lo compone **D** (D-820) y lo lleno **yo**.
**La pieza de la ventana de pedidos se acuerda ANTES de escribirla.** Mi
insumo medido para ese acuerdo, servido ya:

- La ventana de pedidos **ya existe entera** en `ventas/index.tsx` y su fila
  es `TarjetaPedido` (de `packages/ui`) con **pasos + desvío + monto**.
- Su orden **no es por hora: es por trabajo pendiente** (`prioridad()`,
  líneas 78-99) — *lo que espera acción preside; lo entregado se apaga y
  baja*. **Si el HOY del vendedor reordena por hora, pierde su firma.**
- Cuesta **3 olas / 5 llamadas** al abrir (censo C4) — o sea que **meterla
  adentro del HOY sin compartir el contexto duplica viajes**: `contextoVentas`
  ya lo pide el HOY.

⇒ **Lo que le pido a D cuando abra:** que el cascarón exponga **dónde** entra
la ventana y **con qué contexto ya resuelto**, para que la pieza no vuelva a
pedir lo que el HOY ya trajo.

### ✅ ACORDADA EL 15-AGO — D preguntó antes de escribir, y acá queda la respuesta

D trajo la costura con su voto: **extraer la lista a una pieza sin fetch** y
**matar `ventas/index.tsx` como raíz**. Mi respuesta, medida:

**① La pieza: SÍ, y la escribo yo** — `apps/prestador/src/components/ventana-pedidos.tsx`
(precedente en el mismo directorio: `tarjeta-ventas.tsx`, ya con dos
consumidores). **Contrato con una corrección: son CINCO campos, no cuatro** —
`{ pedidos, extras, cupo, moneda, dia? }`. `moneda` no es opcional: la fila
formatea con `monto(total, moneda, idioma)`; sin ella, o la pieza vuelve a
pedir el contexto (y pierde el «sin fetch») o formatea a mano (clase D-448).
`tieneEntregas` **no entra**: es del grupo de celdas al pie, que es de la ruta.

**② Matar la raíz en L1: NO.** Medido — **`/ventas` tiene CUATRO entradas de
TRES poblaciones**, no una:

| entrada | población | estatuto |
|---|---|---|
| `_layout.tsx:256` Redirect | vendedor puro | **la mata D — correcto** |
| `(tabs)/index.tsx:2265` | **empleado-vendedor NO-GESTOR** | **§0bis, firma de mesa S96** |
| `negocio.tsx:563` baldosa «Tu tienda» | gestor | firmada |
| `negocio.tsx:439` | gestor en «negocio ajeno» | pantalla de estado |

🔴 **El que decide es el segundo, y no está en la tabla de cuentas de D
porque no es una cuenta dueña.** Por §2.0 el empleado **no lleva `NEGOCIO`**,
y su `ATENDER` compone la venta de mostrador — **no la lista de pedidos**.
⇒ con la raíz muerta, su puerta §0bis apunta a una ruta que no existe y se
queda **sin ninguna casa para su ventana**. *Es exactamente el actor que S96
encontró caminando —«el empleado-vendedor con puerta y sin camino»—: sería la
segunda vez que le sacamos el piso al mismo señor.*

**Y lo que disuelve la preocupación de D** (*«el día del dual habría que
elegir cuál muere igual, con la pieza ya duplicada»*): **con la pieza
extraída no hay nada duplicado.** La raíz pasa a ser **un consumidor más de
la misma pieza** ⇒ su costo baja de «dos casas que divergen» a **una ruta de
más**. La lápida, escrita para que L4 no la re-discuta:

> ☠️ La raíz muere cuando ① la pieza esté montada en el HOY del dual (L4)
> **y** ② las tres puertas tengan destino nuevo **por población**, incluida
> la de §0bis. Hasta entonces monta la MISMA pieza.

**Y NO se apunta la puerta de Negocio al tab de HOY en L1:** para el dual, el
HOY del tab es su día de CITAS y la fusión de la línea es L4 — mandarlo ahí
antes lo deja en una pantalla sin sus pedidos.

**③ El supuesto ④ de D (`esGestor: true` para el vendedor puro) cierra, con
una consecuencia medida que le devolví:** con eso el vendedor puro ve
`NEGOCIO`, y ahí vive la baldosa «Tu tienda» → `/ventas` (`negocio.tsx:563`).
⇒ **su supuesto y la muerte de la raíz son incompatibles en L1**: juntos le
dan un NEGOCIO con una baldosa rota. Con la raíz viva, el supuesto funciona
tal cual y además resuelve el puntero de facturación.

### ✅ CERRADA — D concedió el mismo día, y devolvió dos cosas

**El cascarón NO borra `/ventas` ni toca el bloque §0bis del HOY.** Muere
solo el `Redirect` de `_layout.tsx:256`, con lápida. D toma las dos
condiciones de muerte tal cual y las cita en su parte, así que **la lápida
vive en los dos lados** — que es lo que evita que L4 la re-discuta desde
cero.

#### 🔴 Y ME CORRIGIÓ UN NÚMERO: EL VIAJE DUPLICADO QUE YO TEMÍA NO EXISTE

Verifiqué su dato **contra el objeto y no contra su mensaje**
(`lib/cuenta-ventas.ts:104-115`): `contextoVentas()` **deduplica EN VUELO** —
`enVuelo` guarda la promesa mientras está en el aire y **la suelta al
resolver**; no es caché. ⇒ **si el tab monta la pieza en la MISMA ola, tres
consumidores comparten UN viaje y no se duplica nada.** Lo que sí cuesta es
montarla en una ola posterior.

*Y esa deduplicación nació de la misma clase de corrección que este parte
registra dos veces más: su propio comentario dice «el instrumento contó 3
consultas donde yo había declarado una ola de dos — mi número estaba mal y lo
dijo la corrida, no el razonamiento».*

⚠️ **Lo que sí conviene saber, y refuerza el punto de D:** `contextoVentas`
son **DOS olas por dentro** (`obtenerMiCuentaComercial()` esperado, y recién
después `Promise.all([roles, moneda])`). El censo de §C4 lo cuenta como UNA
llamada — es el límite que el propio instrumento declara en su cabecera. ⇒ el
costo real de las pantallas de ventas es **una ola más** de la que muestra la
tabla, y montar la pieza fuera de la primera ola cuesta las dos.

#### 📥 LO QUE ENTRA A MI COLA DE L1 POR ESTA COSTURA

Con la barra puesta, el vendedor puro entra a dos cuartos que **hoy nunca
visitó y lo reciben con un error genérico** (medido por D, en mi territorio):

| tab | línea | qué hace hoy sin prestador |
|---|---|---|
| `mascotas.tsx` (DATOS) | `:203` | `estado: 'error'` genérico — no distingue `sin_prestador` |
| `atender.tsx` | `:223` | `fase: 'error'` con el mensaje del wrapper |

(`negocio.tsx:273` y `cuenta/index.tsx:263` **ya lo distinguen bien**.)

**Es L-178 en carne: un dato faltante jamás se disfraza de permiso denegado.**
Y `atender.tsx` es el interesante: su mitad de tienda sale de
`contextoVentas()`, que **no necesita prestador** ⇒ para el vendedor puro ese
cuarto **puede funcionar de verdad**, con su única baldosa al mostrador —
§2.1bis al pie de la letra (*«su ATENDER es una sola cosa, y está bien que lo
sea»*).

🔴 **El lote L1 no se publica con la barra puesta y dos tabs mintiendo.**
Cierra cuando se juntan las dos mitades: el cascarón de D + estos dos cuartos
honestos. El orden se coordina al abrir L1.

---

## §C-A · 🔴 LO QUE MEDÍ DE LA TANDA DE A — DOS WRAPPERS SIN PUERTA

A avisa que ya están en `main` **`listarPedidosDelVendedorEnRango`** (el
lector del `dia?` de mi pieza) y **`definirReglaEnvioVendedor`**. Los dos
**existen** — y **ninguno de los dos se puede consumir desde las apps**:

| dónde | resultado |
|---|---|
| `packages/api/src/wrappers/despensa-vendedor.ts:102` y `:723` | ✅ existen y están `export`ados **del módulo** |
| `packages/api/src/index.ts` (el barrel) | ❌ **ninguno de los dos nombres aparece** |
| `export *` en el barrel | **cero** — es un barrel de nombres explícitos, así que el grep en cero es concluyente |

Las apps importan de `@epetplace/api` (la raíz), no del módulo. ⇒ **un
`import { listarPedidosDelVendedorEnRango } from '@epetplace/api'` no
compila.** Es **el precedente literal de S96-C**: *«el wrapper del track
existía y el re-export no — sin él, el cableo del GPS del reparto no
compila»*.

**Honestidad del método:** intenté probarlo **compilando** una sonda y **el
rojo salió por la razón equivocada** — este worktree no resuelve ningún
paquete del monorepo (ver precondición de L1 arriba), así que el mismo
`TS2307` aparecía en archivos que sí funcionan. *Un rojo por la razón
equivocada está tan roto como un verde por la razón equivocada*, así que la
sonda se descartó y se borró; **lo que sostiene el hallazgo es la evidencia
estática de arriba, no ese rojo.**

### ✅ CERRADO — y verificado contra el objeto, no contra el mensaje

A curó en `main` `02adea21`. **Comprobado acá tras el pull**, no dado por
hecho: `packages/api/src/index.ts:1196-1198` — `definirReglaEnvioVendedor` ·
`listarPedidosDelVendedorEnRango` · `type PedidoDelVendedorConDia`, **dentro
del bloque de `./wrappers/despensa-vendedor`** (un nombre en el bloque
equivocado no habría compilado). El comentario del barrel deja el precedente
escrito ahí mismo: *«un wrapper sin re-export es una puerta que no abre»*.

⇒ **Mi L1 no tiene nada de A bloqueándolo.** El `dia?` de la pieza y el
escritor de la regla de envío tienen puerta.

## §C-A2 · ⚠️ D-791 CIERRA EL MOTOR Y ME DEJA UNA SEGUNDA VERDAD EN LA PANTALLA

Con las tres puertas devolviendo `accion` + lo comprometido + `nota`, **la
«ley del cambio» pasa a tener dos autores**: el motor la dice, y mi Hoja de
recurso la dice a mano (`ventas/configuracion.tsx` — `cambio.recursoEntrega1`
/ `cambio.recursoEntregas`, computadas en la pantalla desde `cupoHoy`).
**Hoy coinciden; el día que difieran, gana la que el vendedor ve.** ⇒ entra a
L1/L2: **la pantalla consume la `nota` del motor y deja de calcularla**.

## §C-B · LA TANDA DE B, MEDIDA CONTRA MI TERRITORIO

### ✅ N11 re-viste mis campos solos — verificado, no aceptado

A avisa que *«tus 96 campos se re-visten solos: cambiar la pieza ES la
migración»*. **Lo comprobé en mi territorio antes de creerlo**, porque el
riesgo no es que la pieza cambie: es que **un campo hecho a mano NO cambie** y
quede distinto al lado de los demás. Medido en `apps/prestador/src`:

**96 `<Campo>` · 1 `<CampoCodigo>` · 2 `<CampoFecha>` — los 99 por la pieza de
`@epetplace/ui`, y CERO `TextInput` crudo.** ⇒ la afirmación de A **se
sostiene para mi territorio**: no tengo nada que tocar.

*(El único `TextInput` que devolvió el grep vive **adentro de un comentario**
de `recuperar.tsx` que explica cómo funciona `CampoCodigo` por dentro. Lo
persigo y lo declaro porque es, otra vez, medir el TEXTO en vez del CÓDIGO —
la forma que este parte ya registra cuatro veces.)*

### 🔴 `HojaCaptura` no me trae prolijidad: me trae un DEFECTO curado

El censo de B midió que el picker nunca se duplicó — **lo duplicado era la
PUERTA**, escrita a mano en diez lugares. Y el número que lo vuelve defecto:
**el cerrojo contra el doble tap vive en 2 de los 10, y los 8 de `apps/` no lo
tienen** ⇒ *dos toques antes del próximo render lanzan dos pickers*.

**Uno de esos ocho es mío:** `FotoDelRepartidor`
(`ventas/configuracion.tsx:264+`) — dos botones que llaman `void elegir(...)`
con **solo la bandera externa `deshabilitado`** (`guardando || subiendo`), sin
cerrojo por referencia. Es exactamente el patrón que B describe.

> *«Un sitio que copia la anatomía copia lo que se VE; el cerrojo no se ve.»*
> Es la misma frase que el Acto II de S98 escribió sobre esta misma pantalla
> —*«el alta de repartidor reinventó lo que la carga de foto de mascota ya
> hacía bien»*—, **y ahora tiene el número que le faltaba**: no era una queja
> de estética, eran ocho puertas sin cerrojo.

⇒ **A mi cola de L1: `FotoDelRepartidor` muere y sus dos usos pasan a
`HojaCaptura`.** No es swap cosmético: **cierra un defecto de doble disparo en
el alta con foto obligatoria.**

## §C-B2 · R44 — EL BASELINE ESTÁ BIEN APUNTADO, Y HAY CINCO MÁS AFUERA

**Las seis, listadas con el regex exacto del guard** (corrido aparte, sin
tocar el juez): son **tres voces × dos idiomas**, las tres con la misma clave
`datosInvalidos`, las seis en `apps/prestador` —

`es.ts:2285 · 2312 · 2336` («Revisa los datos e intenta de nuevo» · «…de la
nota» · «…del presupuesto») y sus gemelas `en.ts:1795 · 1820 · 1844`.

**El baseline de 6 es correcto y está bien apuntado**: las seis se muestran de
verdad — `lib/voz-error-vet.ts:94 · 119 · 143` mapea el código
`datos_invalidos` del wrapper a esas tres claves.

### 🔴 PERO R44 VERDE ≠ LA VOZ GENÉRICA MURIÓ — hay CINCO más fuera de su corpus

R44 mira **los seis diccionarios** (lo dice en su cabecera, así que su alcance
es honesto). **La misma voz vive además en `packages/api`, que no está en ese
corpus**, medido con el mismo criterio:

| dónde | voz |
|---|---|
| `veterinaria-presupuesto.ts:97` | «Revisá los datos del presupuesto.» |
| `veterinaria-nota-clinica.ts:194` | «Revisa los datos de la nota.» |
| `handshake-mostrador.ts:72` | **«Revisá los datos.»** — la más genérica de todas |
| `vacunas.ts:198` | «…no es válida. Revisá los datos…» |
| `_despensa-comun.ts:237` | «El recorrido llegó con datos inválidos.» |

**Y son PEORES que las seis, por dos razones medidas:** ① **34 archivos de
`apps/prestador` muestran `r.mensaje` directo**, o sea que esas cadenas SÍ
llegan a pantalla por los caminos que no pasan por `vozErrorVet`; ② viven
**hard-codeadas en español** y `packages/api` no tiene capa de idioma
(**D-539**, declarada en mi handoff de S98) ⇒ **en inglés se ven en español**.

### ⚠️ Y LA CURA NO ES UNA CADENA — lo mide el productor del código

`datos_invalidos` es **el fallback de una violación de CHECK**
(`veterinaria-presupuesto.ts:101` — `error.code === '23514'` → un solo código
para *cualquier* constraint). ⇒ **la pantalla no PUEDE decir «falta la dosis»
sin una de dos cosas:** ① validar **antes de mandar** (Ley 23: la puerta no
ofrece lo que va a rechazar) — y entonces la voz genérica queda como último
recurso real y honesto, o ② que el motor devuelva **un código por
constraint** (eso es de A).

*Prometer la voz buena sin resolver eso sería cambiar una frase genérica por
otra frase genérica mejor escrita.* ⇒ entra a L1 **con su condición**, no como
tarea de copy.

> ### ✅ EL CAMINO, ACORDADO CON A — para que L1 no lo re-derive
>
> **① Ley 23 primero:** valido **antes de mandar**, y la voz genérica queda
> como **último recurso honesto** en vez de como respuesta habitual.
> **② Donde la validación local NO pueda saberlo** —constraints que solo el
> servidor conoce: unicidad concurrente, ventanas, cupos— **A agrega el
> código tipado POR CONSTRAINT, a demanda y puerta por puerta**, cuando L1
> choque con cada una. *Se le pide con el constraint exacto y el literal del
> rebote que quiero poder dar.*
>
> **Y lo que A se comprometió a NO hacer, que es la mitad que protege:** un
> mapeo genérico de `23514` «mejor escrito». *Sería exactamente la frase
> genérica con mejor ropa que esta condición existe para prohibir.*

## §APARATO · LA CAMINATA COMPLETA (15-ago) — Y EL PUNTO ① TENÍA UN DEFECTO

**Premisa verificada ANTES de evaluar nada, por dos vías:** el pie de Cuenta
dice **`update 01a00717 · preview · 15/08 15:22`** y el logcat repite
`id=01a00717… · embedded=false · canal=preview` en **dos arranques
seguidos**. ⇒ el aparato corre el bundle publicado (ancla `7e6dc899`).

### 🔴 EL HALLAZGO: EL HOY DEL VENDEDOR PURO ERA UNA TARJETA ROJA — y era donde el founder se trabó

Entré desde cero como `duenodes` (sesión cerrada, login completo). **El
ingreso funciona** — el guard resuelve `gestor=true · atender=true ·
ceremonia=vendedor-puro`. **Y el primer cuarto que se abre mostraba:**

> **«Tu usuario no tiene un prestador asociado.»** + un botón **Reintentar**
> que no podía arreglar nada, **porque no había nada roto**.

**Es L-178 en el peor lugar posible: la primera pantalla.** *Mis dos cuartos
honestos no alcanzaban — el founder no llegaba a verlos porque se trababa
antes.* `(tabs)/index.tsx` trataba `sin_prestador` como error genérico,
igual que `mascotas` y `atender` antes de esta sesión: **la misma clase, en
el cuarto que no me había tocado revisar.**

**✅ CURADO EN EL MISMO TURNO** (`(tabs)/index.tsx`): el vendedor puro recibe
**SU HOY — pedidos + stock**, que es lo que §2.0 firma, **montando la MISMA
pieza `VentanaPedidos` que su panel**. *Una lista, no dos: si divergieran, el
día que alguien cure una la otra queda mintiendo.* Caminado en web:
cupo · fila con su escalera de cuatro · puerta a Stock, **cero rojo**.
typecheck VERDE · `verify:diseno` **VERDE 37**.

### EL MAPA COMPLETO, punto por punto

| # | qué | veredicto |
|---|---|---|
| ① | **ingreso desde cero** | ✅ entra · el guard identifica `vendedor-puro` |
| ② | **la barra** | ✅ **las cinco tabs**, y cada una abre sin error |
| ③ | **pedidos (ya en `/ventas`)** | ✅ cupo `0 de 15` · nombre · ventana · `1 producto` · `$23,00` · **la escalera de cuatro se ve** |
| ④ | **ATENDER** | ✅ **una sola baldosa, «Venta de productos»** |
| ⑤ | **DATOS** | ✅ estado honesto · **⚠️ «Casos que te confíen» SIGUE VISIBLE** |
| ⑥ | **NEGOCIO → Configuración** | ✅ portada sin nada cortado ni superpuesto · NEGOCIO del vendedor puro correcto (Tu tienda + Cobros, sin «Tus servicios») |
| ⑦ | **tiempos DESPUÉS** | medidos, abajo |

**⑤ tiene explicación y no es una regresión: mi cura de «Casos» es POSTERIOR
al OTA.** El aparato la muestra porque ese bundle no la tiene. *Confirmado en
dispositivo, que es mejor que suponerlo.*

### ⑦ EL DESPUÉS DE TIEMPOS — y lo que NO es comparable, dicho

**Mismo método, mismo aparato, escalas en 1.0, tres corridas, mediana.**
**sha publicado: `7e6dc899` · OTA `01a00717`.**

| anclaje | ANTES (OTA S98) | **DESPUÉS** |
|---|---|---|
| primer frame | 334 ms | **314 ms** |
| JS corriendo | 272 ms | **252 ms** |
| sesión resuelta | 1.228 ms | **1.276 ms** |

**Los tres se mueven dentro del ruido: no hay mejora ni empeora medible en el
arranque.** *Lo cual es exactamente lo que se esperaba — S99 agregó
superficie, no tocó la cadena de arranque; la cura que movería este número es
D-738, que no entró.*

🔴 **Y lo que NO es comparable, declarado en vez de disimulado:** el
**contenido** del ANTES se midió con `demovet` (cuyo HOY tiene día real) y
esta corrida es con `duenodes` —**cuyo HOY, en este bundle, era el error
rojo**—. *Cronometrar cuánto tarda en pintarse un error no es un «después».*
El par de contenido se cierra con `demovet` sobre el bundle nuevo: **una
corrida de 4 minutos que no hice para no cambiarle la sesión al founder en su
ventana.**

### ⚪ DOS COSAS QUE SIRVO Y NO TOCO (son del founder o son dato)

- **`manana`, sin ñ**, como nombre de corte. **Es dato del fixture** —lo
  tipeó quien creó el corte—, no una cadena nuestra. Se dice por si el gate
  lo lee como defecto de la app.
- **«Documentos — Cargando…»** en Datos comerciales quedó así en la captura.
  **No lo declaro defecto:** una sola observación no distingue lento de
  colgado, y no volví a mirar. *Nombrarlo sin medirlo sería fabricar un rojo.*

### 📵 EL UNIVERSO DE LO QUE **NO** CAMINÉ (L-238)

*Una ausencia sin universo declarado no es una medición.* No abrí: **las
Hojas con campos de N11** (agregar corte · registrar repartidor · recurso —
o sea que **los diez campos con teclado quedaron sin ver en aparato**) ·
**Stock** · **Venta de mostrador** · **Mis entregas** · el **detalle de un
pedido** · las pantallas internas de **Cuenta**. Y el **DESPUÉS de contenido
con la cuenta del ANTES**.

## §L1 · CONSTRUIDO — Y LO CAMINADO SE SEPARA DE LO NO CAMINADO

**Commit `6ee84b06`** · typecheck prestador VERDE · `verify:diseno` **VERDE,
36 reglas**.

### ✅ ① LA PIEZA `components/ventana-pedidos` — construida Y CAMINADA

La lista sale de la pantalla; `ventas/index.tsx` pasa a ser su primer
consumidor. **Sin fetch adentro**, como se acordó con D.

**🔴 Y su contrato hace INEXPRESABLE la invisibilidad que D vino a avisar:**
es una **unión discriminada** — *quien monta con `dia` TIENE que pasar
`sinFecha`, o no compila*. **Una convención se olvida; un tipo no.** Los
`sinFecha` **presiden** (firma de mesa, y es la firma de esta ventana
aplicada un escalón más adentro).

**El número que lo saca de lo teórico, medido por mí contra la base: 2 de los
10 pedidos vivos no tienen `entrega_fecha_objetivo`.** Uno de cada cinco.

**CAMINADA en web con `duenodes` (cuenta real, datos reales):** la ventana
renderiza el cupo (`0 de 15 entregas hoy`), el pedido con **su escalera de
cuatro**, el monto y el grupo de celdas al pie. **Cero errores de página.**
Captura: `scripts/capturas/s99-c-l1/00-entrada.png`.

> *Y una lectura mía que la medición corrigió en el acto:* leí la captura y
> creí ver un pedido **terminado** pintado como activo. Fui a la base: es
> `documentado` · `es_terminal=false`, y «Entregado» es **la etiqueta del
> cuarto escalón pendiente**, no un estado. *No era un defecto: era yo
> leyendo una etiqueta en vez de la cosa —la misma forma, van seis.*

### ✅ ②bis LOS DOS CUARTOS — CAMINADOS sobre `main` `7e6dc899` (con el cascarón de D adentro)

**El bloqueo de abajo se levantó y el camino se recorrió.** Metro reiniciado
sobre el árbol mergeado y **verificado que `:8081` lo sirve ESTA worktree**
antes de medir nada.

Con **`duenodes`** (vendedor puro, sin fila de `prestadores`):

| qué | resultado |
|---|---|
| las tabs | **existen** — la barra de cinco, `/atender` y `/mascotas` **ya no terminan en `/ventas`** |
| `ATENDER` | **una sola baldosa: «Venta de productos»** bajo *Tu tienda* — §2.1bis literal |
| `DATOS` | **«Tu negocio es de productos»** + *«Acá viven las mascotas que atiendes, y por eso está vacío. Lo tuyo son tus pedidos y tu stock.»* |
| voces genéricas | **«Revisa los datos» y «Algo salió mal»: AUSENTES las dos** |
| errores de página | **0** |

Capturas: `01-atender.png` · `02-datos.png` (las del redirect —`03`/`04`—
**se conservan**: son el antes de este después).

> ⚠️ **Declarado: la caminata es WEB, no aparato.** El teléfono volvió a
> quedar **bloqueado** y la llave del keychain sigue sin ser el PIN (freno 1
> nunca se curó). *No es un atajo: el ANTES —el redirect— también se midió en
> web con la misma cuenta y el mismo instrumento, así que el par es
> comparable.* La confirmación en aparato cae dentro de la pasada del
> founder, que es donde corresponde.

#### 🟡 UNA OBSERVACIÓN QUE SIRVO Y NO CURO — es del founder, no mía

`DATOS` sigue montando debajo **«Reseñas»** y **«Casos que te confíen · *Se
despierta con el primer caso que un colega te derive*»**. A un vendedor de
productos **nadie le va a derivar un caso clínico nunca**: es una promesa que
no puede cumplirse.

**No lo toco**, y no por pereza: §2.0 dice con todas las letras que *«las
specs finas de cada cuarto del vendedor NO están firmadas»*, y decidir qué ve
él en `DATOS` es producto, no limpieza. *Recortar secciones por mi cuenta
sería exactamente convertir una dirección abierta en spec por inercia.*

### ⏪ ② LOS DOS CUARTOS — el bloqueo, conservado porque es el ANTES

`mascotas.tsx` y `atender.tsx` distinguen `sin_prestador` y dejan de
disfrazar un dato que falta de permiso denegado (**L-178**), con el
precedente que ya existía a dos archivos (`negocio.tsx`, desde S96).
**Y `atender` no solo deja de mentir: FUNCIONA** — su mitad de tienda sale de
`contextoVentas()`, que no necesita prestador, así que el vendedor puro ve
**su única baldosa al mostrador** (§2.1bis literal).

**🔴 Pero NO los declaro caminados, y la razón está medida:** con
`duenodes`, ir a `/atender` y a `/mascotas` **termina las dos veces en
`/ventas`** — el guard raíz todavía redirige, porque **el cascarón de la
barra es de D y no está en `main`**. *Mis dos cuartos existen y son
inalcanzables para el único actor que los estrena.*

⇒ **Es exactamente «las dos mitades» que A describió, ahora con evidencia.**
Lo caminable se caminó: **regresión verificada en el camino normal** —con
`duenovet`, `ATENDER` y `DATOS` siguen pintando como siempre, cero errores
(`01-atender.png` · `02-datos.png`).

### ✅ ③ «CASOS QUE TE CONFÍEN» FUERA PARA EL VENDEDOR PURO — aplicada y caminada

Adjudicación de mesa, con su criterio: **un estado vacío que PUEDE llenarse
es honesto; uno que no puede llenarse NUNCA es una mentira.** A un negocio de
productos **nadie le deriva un caso clínico jamás**.

**🔴 Y la vecina SE QUEDA, con la razón MEDIDA y no supuesta:** «Reseñas»
**sí puede llenarse** — `resenas_productos` existe en el esquema (verificado:
11 columnas). *El mismo criterio que saca una deja la otra: lo que decide no
es «este cuarto es de servicios», es si el vacío tiene futuro.*

Caminado en web con `duenodes`: `DATOS` queda con **«Reseñas» y nada más**
(`02-datos.png`). typecheck VERDE · `verify:diseno` **VERDE 37**.

⚠️ **NO viaja en el OTA publicado** — entra en el próximo. El founder lo va a
ver **sin** este cambio si la pasada corre sobre `f690bdc0`.

### 🔴 ① Y ② BLOQUEADOS — EL APARATO VOLVIÓ A ESTAR CERRADO

**El OTA está publicado y leído del OBJETO** (`update:view`, no del mensaje):

| | |
|---|---|
| group | **`f690bdc0-46a8-4f97-8653-82ddbb1df2b7`** |
| android updateId | `01a00717-74bc-7a5f-8fd7-439f6b65bdd8` |
| **ancla** | **`7e6dc899…`** — *el MISMO sha que caminé en web* |
| dirty | **None** (limpio, sin asterisco) · runtime 1.0.5 · canal `preview` |

> **Que el ancla del bundle publicado sea exactamente el árbol que caminé no
> es un detalle: es lo que hace que mi caminata web y el aparato sean el
> mismo código.** Lo que falta no es saber si funciona — es verlo en el
> teléfono, que es lo que §6bis-B pide y lo que yo no puedo hacer.

**Medido al intentar: `mDreamingLockscreen=true` — el teléfono está bloqueado
otra vez.** La llave del keychain sigue sin ser el PIN (**freno 1, nunca
curado**). ⇒ **la caminata en aparato y el DESPUÉS de tiempos no corrieron, y
no se sustituyen por nada.** *Un «después» de tiempos medido en otro lado no
es un después: es otro número.*

**Lo que los destraba es un acto de una línea del founder** — desbloquear y
dejar el teléfono despierto al cable. Con eso, los dos corren juntos en ~20
minutos con el método ya escrito, y el par ANTES/DESPUÉS queda cerrado
(`ANTES` = OTA de S98 `01a00373` · `DESPUÉS` = `01a00717`, ancla `7e6dc899`).

### 🔍 LO QUE EL FOUNDER MIRA DE LA PIEZA EN EL GATE — la pregunta, no la descripción

*Un ojo con una pregunta ve algo; un ojo con una sensación aprueba cualquier
cosa.* Dos preguntas, las dos con respuesta **sí/no** y las dos sobre lo que
la pantalla YA puede mostrar hoy:

> **① Abrí `Pedidos` y, sin leer nada: ¿el primero de la lista es el que
> agarrarías ahora?**
> Es la firma de la ventana puesta a prueba —*el orden es por lo que falta
> hacer, no por hora*—. **Si el primero no es el que agarraría, la firma está
> mal, no la pantalla.**

> **② Mirá UNA fila sin leer el texto chico: ¿sabés en qué paso está el
> pedido?**
> Es el corte de cuatro escalones. **Si hay que leer para saberlo, la
> escalera no está haciendo su trabajo** — y ahí entra el dictado suyo que
> quedó sin ejecutar: *el cuarto escalón lo marca quien entrega, y hoy se
> pinta con la misma tinta que los tres que él sí puede dar* (§C1).

⚠️ **Y LO QUE **NO** SE PUEDE MIRAR TODAVÍA, para que nadie lo dé por
gateado:** la regla de que **los `sinFecha` presiden** **no es observable
hoy**. `/ventas` usa el lector SIN rango, así que esos pedidos entran
mezclados en la lista general; la sección que los hace presidir **aparece
recién cuando una superficie use el lector por rango (L4)**. *Servir una
pregunta que la pantalla no puede contestar sería gastarle el ojo al pedo.*

### ⚠️ PRECONDICIÓN DE WORKTREE QUE APARECIÓ AL CAMINAR

El repro web falló primero con `getClient: initApi() no fue llamado`: un
worktree nuevo **no tiene `apps/prestador/.env.local`** (gitignored). Se
copió del primario. ⇒ **a la receta de arranque, junto a `CI=true pnpm
install`:** *un worktree nuevo no corre solo — le faltan los paquetes y le
falta el entorno, y los dos fallan diciendo otra cosa.*

### 🔴 LA RAMA NO ESTABA EN ORIGIN — L-217 en su forma más cara, y curada

**Lo cazó D midiendo, no yo.** `pista/s99-c` **no existía en `origin`**: los
15 commits —la pieza, los dos cuartos, el baseline C4 y sus instrumentos—
vivían **solo en mi disco**. Y lo que lo vuelve el caso caro: **el C4 no es
reproducible** — costó la ventana de aparato del founder, y el teléfono ya
había vuelto a reposo.

**Curado y verificado POR CONTENIDO, no por el mensaje del push:**

| verificación | resultado |
|---|---|
| `git push -u origin pista/s99-c` | **EXIT 0** (leído del comando, no de un pipe — L-191) |
| `ls-remote` → sha remoto vs `rev-parse HEAD` | **idénticos** (`25aee2c1`) |
| los **15 commits**, uno por uno, `merge-base --is-ancestor` | **15/15 ✅** |
| los 5 artefactos caros, por `cat-file -e` sobre el ref remoto | **5/5 presentes** |

> ### ⚠️ Y APARECIÓ UNA TRAMPA NUEVA, JUSTO EN EL COMANDO QUE VERIFICA
>
> La orden de mesa decía *«confirmá con `git ls-remote --heads origin …`»* y
> *«leé el EXIT»*. **Medido acá: `ls-remote` devuelve EXIT 0 aunque el ref NO
> EXISTA** — su salida queda vacía y el código dice cero. *Si hubiera leído
> el exit, habría confirmado que la rama estaba justo cuando no estaba.*
>
> ⇒ **El discriminador de `ls-remote` no es el exit: es que la SALIDA no esté
> vacía** — y mejor aún, **que el sha que devuelve sea el mismo que el
> local**. Es L-191 con una vuelta de tuerca: *no alcanza con leer el exit
> del comando y no del pipe; hay comandos cuyo exit no responde la pregunta
> que uno está haciendo.*

### 📨 LOS DOS AVISOS QUE NO PUDIERON SALIR — quedan ESCRITOS, que es lo que vale

**El canal entre pistas dejó de estar disponible al cerrar este bloque**
(`Cross-session messaging is not available`). Se declara y los pedidos se
depositan acá, porque *lo que no queda escrito no existe* y un aviso que
vivía solo en un mensaje se pierde con la sesión.

**PARA D — la pieza está lista y tu `sinFecha` tiene dueño:**
`components/ventana-pedidos.tsx`. Contrato final con **un cambio respecto de
lo acordado**: no es prop opcional, es **unión discriminada** — con `dia`,
`sinFecha` es obligatorio o **no compila**. Presiden, como pediste. **Y tu
preocupación no era teórica: 2 de 10 pedidos vivos no tienen fecha.**
⇒ **Avisá cuando el cascarón entre a `main`**: camino mis dos cuartos como
`duenodes` ese mismo día y recién ahí le avisamos juntos a A.

**PARA A — el disparador de la pasada de gate NO está completo:** C4 ✅ pero
**los dos cuartos no se pueden caminar** (evidencia arriba: el guard
redirige). *No dispares la hoja todavía* — el ojo del founder es el recurso
escaso y una pasada sobre un lote a medias lo gasta al pepe.

## ESTADO AL CERRAR EL PARTE

- **Construido: nada** (L0). **Migraciones: cero. SQL de escritura: cero.**
  Todas las consultas a la base fueron `SELECT` sobre catálogo y esquema.
- **Un archivo nuevo, y es un instrumento:**
  `scripts/s99/censo-viajes-pantalla.mjs` (solo lectura, sin residuo).
- **Dispositivo — VENTANA DEL FOUNDER, ABIERTA Y CERRADA (15-ago):** se
  compró la ventana con `svc power stayon true` (**primer comando**, tras
  confirmar `device` y no `unauthorized`) y **se devolvió el aparato tal como
  se recibió**, verificado y no supuesto: `mStayOn=false` · `mWakefulness=Dozing`
  · app con `force-stop` · **cero archivos `.xml` de residuo en `/sdcard`** ·
  cero intentos fallidos depositados · **la sesión `demovet` NO se tocó** (el
  panel de ventas quedó sin medir antes que cambiarle la cuenta al founder).
  *El founder no tiene que limpiar nada detrás.*
- **Lo que espera al founder, en orden de costo:** ① **el PIN real del
  aparato** (destraba C4 entero, ~20 min) · ② la firma de la propuesta de
  primera tanda de IA (C3) · ③ el borde `propuesto` vs `publicada` del espejo
  de stock (C5).
- **Lo que espera a otras pistas:** **A1** cruza C1 · **D — costura 1
  CERRADA** (la raíz vive, la pieza es mía, la lápida está en los dos partes;
  nada mío espera a D ni D a mí hasta que abra L1).
- **Mi cola de L1, ya con TRES entradas:** ① la pieza `ventana-pedidos` con su
  contrato de cinco campos · ② los dos cuartos honestos (`mascotas.tsx:203` ·
  `atender.tsx:223`), **que son la mitad sin la cual el lote no se publica** ·
  ③ `FotoDelRepartidor` → `HojaCaptura` (cierra el doble disparo, §C-B).

---

# §V · LA VENTANA DE APARATO DEL 15-AGO — el OTA `01a00747` caminado

**Premisa verificada ANTES de medir nada** (L-235): el pie de Cuenta dijo
`update 01a00747 · preview · 15/08 16:15`, leído EN PANTALLA tras doble
reinicio. Todo lo de esta sección responde sobre ESE objeto.

**Aparato:** `R5CY201ZDVL` · WiFi `NETLIFE-uiolgsuarezg3` · batería 94 % ·
ventana comprada con `svc power stayon true` y **restaurada al cerrar**
(stayon apagado · aparato en reposo · residuo en `/sdcard` = **0**).
**Sesión al cerrar: `demovet`** (se cambió para el par de tiempos; se declara
porque el founder abre donde uno deja).

---

## ✅ LAS DOS CURAS, VERIFICADAS EN APARATO SOBRE BUNDLE PUBLICADO

**Nunca las había visto correr fuera de mi worktree.** Ahora sí:

| cura | qué se vio |
|---|---|
| **HOY del vendedor puro** | **la tarjeta roja NO está.** Techo «Pedidos», `0 de 15 entregas hoy`, la ventana con su pedido y la celda de Stock. *Es exactamente el punto ① donde el founder se trababa.* |
| **«Casos que te confíen»** | **no aparece en DATOS.** Queda solo «Reseñas», que es lo que la mesa adjudicó (`resenas_productos` existe y puede llenarse; los casos no, nunca, para un vendedor puro) |

Y el cuarto honesto de `mascotas` se leyó tal como se escribió: *«Tu negocio
es de productos / Acá viven las mascotas que atiendes, y por eso está vacío.
Lo tuyo son tus pedidos y tu stock.»*

---

## 🔴 ① EL NÚMERO: EL CONTENIDO DEL HOY TARDA **1,5 s MÁS** QUE ANTES

**Es lo único de las cuatro preguntas del gate que era mío, y trae número.**

| anclaje | ANTES (OTA S98) | **DESPUÉS (`01a00747`)** |
|---|---|---|
| primer frame (`am start -W`, 3 corridas) | 334 ms | **323 ms** (386/317/323) |
| **contenido del HOY pintado** | **2,0–2,5 s** | 🔴 **3,8–4,0 s** |

**Es comparable y lo digo con su método**, porque el error caro sería
comparar dos cosas distintas (L-235): **misma cuenta** (`demovet`) · **mismo
aparato** · **mismo método** (bracket por offsets fijos con `force-stop` +
arranque en frío por corrida, jamás sondeo — el sondeo mide su propio costo) ·
**y la MISMA ancla visual**: el texto `Hoy libre · 1 esta semana`, que **solo
existe con datos cargados**. A 3,8 s hay esqueleto; a 4,0 s está el día.

**La alternativa que descarté antes de gritar:** la red. Misma WiFi que el
ANTES, y tres medidas contra Supabase dieron **60–98 ms** de total. *No
explica 1,5 segundos.*

**⚠️ Y su límite, declarado en vez de disimulado:** con **una sola corrida en
el tiempo no puedo probar causalidad**. Lo que sé es que entre los dos puntos
entró **S99 entera** (A, B, C y D en el mismo OTA) y que el número empeoró
casi al doble. **Bisecar es del arranque de la próxima pasada**, y el
sospechoso natural es la cadena de arranque que **D-738** ya tiene fichada
(622 ms en cuatro viajes encadenados solo para resolver quién soy).

**Dato de paso, honesto:** a los 4,0 s la plata del día dice **«Calculando»** —
un estado que declara que ese número todavía no llegó. Eso está BIEN y no es
parte del hallazgo.

---

## LO MEDIBLE QUE ENCONTRÓ CAMINAR (lo que se corta, se superpone o no entra)

### ② 🟡 EL NOMBRE DEL PRODUCTO SE CORTA EN **CUATRO** SUPERFICIES — es clase, no caso

| dónde | lo que se lee |
|---|---|
| Hoja «Agregar producto» del mostrador | `Croquetas De…` |
| fila del carrito del mostrador | `Croquetas Demo Se…` |
| **Stock** | `Croquetas Demo Senior …` |
| Configuración › Repartidores | `Repartidor S97 de Pr…` |

**El patrón es el mismo en las cuatro:** el dato de la derecha (precio ·
stock · código) toma su ancho primero y el nombre se queda con el resto.
**Y el nombre es justo el dato con el que se elige** — con dos productos
parecidos, «Croquetas De…» no distingue nada. **Es mío y es de FORMA**, así
que la cura pide decidir la forma una vez (dos líneas · dato abajo · ancho
mínimo del título) y aplicarla a las cuatro.

### ③ 🟡 EL RÓTULO CORTADO POR EL TÍTULO FIJO DE LA HOJA — **destinatario B**

En «Registrar repartidor», al scrollear, el rótulo **«Foto del documento»
queda seccionado a media altura** por el borde del título fijo: el contenido
pasa por debajo sin máscara ni degradado. Se ve la mitad inferior de las
letras. **La Hoja y su scroll son de `packages/ui`**, por eso va servido y no
curado.

### ④ ⚪ EL CTA NO ENTRA AL ABRIR LA HOJA — y se resuelve solo con el teclado

La Hoja «Ajustar stock» abre mostrando dos campos y una frase; **«Guardar el
ajuste» queda fuera de pantalla** y hay que scrollear. **No es defecto de
inset** —lo verifiqué—: es que el contenido supera el alto de apertura. Y
cuando el teclado sube, la Hoja se re-encuadra y **entra todo**. *Se registra
como observación de composición, no como rojo.*

### ✅ EL GATE DE N11 CON TECLADO: **PASA**, en sus dos formas

**Los diez campos con teclado nunca se habían visto en el teléfono.** Se
vieron tres Hojas: la Hoja **sube con el teclado**, el campo enfocado queda
visible con su foco teal, el CTA queda arriba del teclado, y **nada queda
tapado** — con teclado **numérico** (Cantidad) y con **alfabético con ñ**
(Motivo). *Este era el riesgo real de N11 y no se materializó.*

### ⑤ ⚪ EL VENDEDOR PURO VE LA MISMA LISTA DOS VECES

Su **HOY** y la raíz **`/ventas`** muestran la misma ventana de pedidos, el
mismo pedido y el mismo `0 de 15 entregas hoy`; la única diferencia es que la
raíz suma cuatro celdas. **Es exactamente lo que la costura con D previó** y
por eso la raíz sobrevive con lápida y dos condiciones de muerte escritas.
Se registra que **ya es visible**, no que haya que curarlo por mi cuenta.

---

## 🔍 DOS COSAS QUE PARECÍAN DEFECTO Y NO LO ERAN — medidas antes de declararlas

**Las dos las habría reportado en falso si me quedaba con la captura.**

**(a) «Voy hacia acá» sobre un pedido que parecía entregado.** La escalera
mostraba los cuatro puntos y «Entregado» destacado; la pantalla del repartidor
ofrecía sus tres acciones. Parecía una puerta ofreciendo lo que va a rechazar.
**La base dijo otra cosa:** `estado = en_reparto`, narrativa `en_camino`,
**`es_terminal = false`**. El pedido está en reparto y las tres acciones son
correctas. **La lectura era mía, no de la app.**

**(b) La escalera que se lee como «entregado».** Con eso resuelto, revisé la
derivación: `escalera-pedido.ts` mapea `en_reparto → 3 hechos + accion
'enManosRepartidor'`, así que el cuarto paso queda **`actual`**, no `hecho`.
**La lógica está bien.** Lo que pasa es que `EscaleraEstados.tsx:174` dice
literal *«hecho y actual se llenan igual a propósito»* — es una **decisión
firmada de `packages/ui` con su razón escrita** (el actual preside por el peso
del texto). **No es defecto. Queda servido a B como observación de lectura**,
arriba en el HANDOFF.

*Las dos son el mismo método: leer el objeto antes de escribir el rojo.*

---

## ④ LA GALERÍA DE PIEZAS **SÍ ES ALCANZABLE** — con dos toques

**Camino exacto, verificado en aparato:** tab **Cuenta** → celda **«Láminas de
gate · para firmar»** (bajo «Tu histórico»; con un scroll si se entra desde
arriba). Abre `/gallery`, que vive fuera de la barra.

**La lámina del glifo `moto` EXISTE** en la galería (`TokenGallery.tsx:4868`,
sección *«Hoja de contacto · el glifo `moto` (CANDIDATAS — gate POR ÍCONO del
founder)»*, S99-B §6b) con sus variantes y con la ausencia declarada: *el
registry tiene 46 glifos y ninguno es moto*.

**⚠️ Lo único incómodo, medido:** la sección vive cerca del **final** de una
página muy larga (línea ~4868 de ~5000). **Es alcanzable, pero es un scroll
largo** — si el gate por ícono se traba, es por eso y no por falta de camino.

---

## ✅ CURA APLICADA EN LA MISMA VENTANA (lo mío y reversible)

**El techo de Cuenta decía «No pudimos cargar tu negocio» al vendedor puro.**
Es la MISMA clase que ya curé tres veces —`sin_prestador` tratado como fallo
cuando es un ESTADO (L-178)— y **faltaba justo acá porque este techo se lee al
final del recorrido y nadie lo había caminado**. Ahora, ante `sin_prestador`
con cuenta vendedora, el techo pinta el **nombre comercial**; sin logo, sin
ciudad y sin cohorte, porque la cuenta comercial no tiene esas cosas y no se
inventan (L-139). El contexto de ventas está **cacheado** por el HOY ⇒ el
viaje es gratis en la práctica.

`(tabs)/cuenta/index.tsx` · typecheck VERDE · `verify:diseno` VERDE 37 ·
commit `c4e8ad12`, **en origin por sha y por contenido**.

⚠️ **Esta cura NO se vio corriendo**: necesita el OTA siguiente igual que las
otras dos.

---

## LA FRONTERA QUE RESPETÉ, dicha

**No juzgué diseño.** Las tres preguntas del gate que son del ojo del founder
—si los campos respiran o se ven enrejados · si ATENDER destacada se lee como
importante o como vacío · si el primero de la lista es el que él agarraría—
**no tienen opinión mía en este parte**. La cuarta, la velocidad, es la única
que era mía: **está arriba, con su número, su método y su límite.**

**Y lo que NO toqué, a propósito:** no registré la venta de mostrador (habría
descontado stock real) ni ejercí las tres acciones del repartidor (escriben en
la base). *Un gate no se paga con efectos secundarios en datos vivos.*

---

# §VI · LA COLA PROPIA, EJECUTADA (15-ago, mientras B escribe la receta de L2)

**Lo hecho, con su número, y lo frenado con su razón.** Nada de esto tocó
la ficha del repartidor: es toque 1 de B y va antes de que yo escriba.

## ✅ ③ UNA SOLA ENTRADA PARA EL VENDEDOR PURO

La caminata hizo visible la duplicación; la adjudicación la cerró. Tres
movimientos, y el orden importa:

1. **El HOY del vendedor puro gana las CUATRO celdas del módulo.** No es
   adorno: **si la tab es la única puerta, lo que vivía detrás de la otra
   entra por acá o queda INALCANZABLE.** Mostrador tenía además su camino
   por ATENDER; **Mis entregas y Configuración no tenían ninguno.**
2. **La baldosa de NEGOCIO deja de ofrecerse a quien NO tiene prestador.**
   Para el prestador que además vende **SE QUEDA**, y es su única entrada:
   su HOY es la jornada de citas, no el panel. Por eso el gate es
   `!sinPrestador` y no un apagado a secas — *cerrarla para todos habría
   dejado sin panel justo a quien no tiene otro camino.*
3. **La raíz `/ventas` NO muere:** queda para el empleado-vendedor
   no-gestor (§0bis), que es la condición que su lápida ya declaraba.

**Y nace `celdas-modulo-ventas`**, hermana de `ventana-pedidos` y por el
mismo motivo: el grupo vivía duplicado, y esa copia es la que hace que una
pantalla nueva del módulo se agregue en uno y se olvide en el otro.

> **Efecto medido que vale como nota de método:** al sumar las celdas, **N3
> dio rojo** (techo de 3 separadores por pantalla) sobre el HOY, que es la
> pantalla más cargada de la app. La salida no fue quitar líneas ni subir el
> techo: **al extraer la pieza, cada archivo vuelve a responder por lo que
> compone él.** *La regla no se esquivó — se le dio la razón.*

`misEntregasAsignadas` entra en la **misma ola** (L-224 · N16), no en una
segunda; y la celda de reparto se ofrece solo con `ok && length > 0`: **un
fallo de lectura NO la enciende** (Ley 23).

## ✅ ④ LAS 6 VOCES DE R44 — baseline **9 → 3**

**La cura no fue inventarles un QUÉ.** `datos_invalidos` es el fallback de
CUALQUIER CHECK: nombrar un campo habría sido **verosímil y falso** (L-139),
y mandar a mirar el lugar equivocado es peor que no decir nada.

Se escribió **lo que sí se sabe con verdad**: que rechazó el **SERVIDOR**
(no la red ni la sesión) y que **reenviar lo mismo va a fallar otra vez** —
que es justo lo que «intenta de nuevo» prometía en falso.

Las 3 de `packages/api` siguen bloqueadas (dueño A): su cura de raíz es
**D-827**, códigos tipados por constraint.

## ✅ ⑤ R42 — baseline **10 → 6**, y las cuatro bajas NO son lo mismo

| archivo | por qué salió |
|---|---|
| `alta/PasoDocumentos.tsx` · `seccion-documentos.tsx` | **anatomía ya canónica** (`Hoja` + dos `Boton bloque`) ⇒ migrar **no cambió la forma**: les dio el **cerrojo** contra el doble tap. Cura, no swap |
| `veterinaria/verificacion.tsx` | **acá sí cambió la forma, con firma detrás**: tenía dos `Celda interactiva` y la canónica es `Boton` porque abrir la cámara es un COMANDO (**Ley 22c**). No es gate de forma nuevo — es aplicar ley ya firmada |
| `(tabs)/cuenta/perfil.tsx` | 🔍 **FALSO POSITIVO del instrumento**: `capturarConCamara` estaba **importado y jamás llamado**. La pantalla nunca tuvo puerta de dos ramas. Cura = retirar el import muerto |

> **Y el baseline se describía a sí mismo de más:** su nota decía *«migra la
> de avatar»* — **esa captura de avatar no existe en ese archivo**. Un
> baseline que describe algo que no está es un permiso con forma de deuda.

**🛑 `FotoDelRepartidor` NO se migró, y es freno declarado:** es parte de
**la ficha del repartidor**, y la receta de forma de B es el **toque 1**.
Migrarla hoy sería decidir su forma antes de la receta. Queda en el
baseline con su nombre.

---

# 📮 PEDIDOS A B — dos ensanches de piezas suyas, los dos nacidos de la caminata

*L-175: se lee el registry y se ENSANCHA, jamás se copia. Los dos vienen con
su medición, no con una preferencia.*

## ① `Celda` — el título necesita poder NO truncarse

**El caso:** el nombre del producto se corta en **cuatro** superficies del
módulo de ventas (`Croquetas De…` · `Croquetas Demo Se…` · `Croquetas Demo
Senior …` · `Repartidor S97 de Pr…`). **Adjudicación del founder: donde el
nombre ES el criterio de elección, no se trunca.**

**La causa, medida:** `Celda.tsx:120` fija `numberOfLines={1}` +
`ellipsizeMode="tail"` en el título, **sin prop para cambiarlo**. El patrón
es el mismo en las cuatro: el dato de la derecha (precio · stock · código)
toma su ancho primero y el nombre se queda con el resto.

**Por qué no lo curé de mi lado:** `Celda` tiene **162 usos** en las dos
apps. Cambiar el default sería tocarlas todas; **el ensanche correcto es
opt-in** (algo como `lineasTitulo?: 1 | 2`, default 1 ⇒ **nadie cambia**).
Y componerlo con `children` de mi lado rompería el label de accesibilidad,
que la pieza arma de `titulo + subtitulo + metadataMono`.

## ② `Hoja` — no tiene dónde poner un CTA fijo, y **ya trae su propio scroll**

**El caso:** «Ajustar stock» abre mostrando dos campos y una frase; **el
botón «Guardar el ajuste» queda fuera de pantalla** y hay que scrollear.
Con el teclado arriba entra todo — es solo el estado de apertura.

**Lo que encontré midiendo, y cambia la cura:** `Hoja` envuelve sus children
en un `AnimatedGHScrollView` propio (`Hoja.tsx:453`). ⇒ **① un `<Boton>`
puesto después de `</HojaScroll>` caería igualmente dentro del scroll** —no
hay slot de pie fijo—, y **② mis Hojas de formulario tienen scroll anidado
dentro de scroll**, lo que probablemente explica que el gesto se sintiera
raro al volver arriba en el aparato.

**Los dos caminos que NO tomé, con su razón:**
- `altura="contenido"` (60 % con alto natural) en vez de `media` (50 % fijo):
  **ninguna Hoja `contenido` de la casa convive hoy con `HojaScroll`** —lo
  medí— y un ScrollView sin alto dentro de un contenedor con solo
  `maxHeight` puede **colapsar a 0**. *Cambiar una Hoja que funciona por una
  que podría abrir vacía es peor que el síntoma.*
- Sacar el `HojaScroll` interno (redundante con el de la pieza): toca
  composición de gestos en Android, **y L-132 dice literal que web no lo
  delata**. Sin aparato, es adivinar.

**El ensanche que pido:** un slot de **pie fijo** en `Hoja` (fuera de su
scroll), que es además la gramática canónica ya firmada — *«el CTA abajo
FIJO»* (S61-A3/A13). Afecta a **cuatro Hojas mías** con campos + CTA
(`stock` y tres de `configuracion`), y a las del cliente que estén igual.

---

# §VII · LA MEDICIÓN DEL OTA `01a0085d` — #0a NO CIERRA

**Premisa verificada ANTES de medir** (L-235): pie de Cuenta =
`update 01a0085d · preview · 15/08 21:18`, leído EN PANTALLA tras doble
reinicio. **Cuenta: `demovet` / Clínica Aurora — la MISMA del ANTES**, así
que el número es comparable. Ancla publicada `adc25429`, verificada
presente en `origin/main`. Escalas de animación las tres en **1.0**.

## EL NÚMERO

| anclaje | ANTES (OTA S98) | ayer (regresión) | **HOY (`01a0085d`)** |
|---|---|---|---|
| primer frame (3 corridas, mediana) | 334 ms | 323 ms | **331 ms** |
| **contenido del HOY pintado** | **2,0–2,5 s** | 3,8–4,0 s | 🔴 **~3,5–4,5 s** |

**🔴 #0a NO CIERRA.** Su discriminador es *«vuelve a su estado previo o
mejor»* y el estado previo era **2,0–2,5 s**. Hoy el contenido llega
alrededor de **4 s**: prácticamente donde estaba la regresión.

## Y EL MÉTODO CAMBIÓ DE RESULTADO A MITAD DE CAMINO — la dispersión ES el dato

La primera pasada por offsets dio algo incoherente: **3,8 s con contenido y
4,2 s con esqueleto**. En vez de reportar eso, repetí **tres corridas por
offset** (mi propia candidata #22: *contra un tiempo variable, una corrida
es una moneda al aire con forma de medición*):

| offset | esqueleto | contenido |
|---|---|---|
| 3,0 s | 3/3 | 0/3 |
| 3,5 s | 2/3 | **1/3** |
| 4,0 s | 1/3 | **2/3** |
| 4,5 s | 0/2 | **2/2** |

⇒ **el cruce no es un punto: es una franja de casi un segundo.** El MISMO
offset da esqueleto o contenido en corridas consecutivas. *La variable que
domina no es el código: es la red o el arranque del runtime.*

## 🔴 EL CRUCE DE LOS TRES INSTRUMENTOS — declarado, NO promediado

**A dice 17 → 14 olas (13 en el camino real). Yo digo que el tiempo no
mejoró.** Los dos pueden ser ciertos a la vez, y por eso no se promedian:

- **Tres olas menos ≈ 450 ms** si fueran seriales (peaje ~150 ms, L-223).
  Sobre ~3,8 s eso es **~12 %**.
- **Mi dispersión medida es de ~1 s.** ⇒ **una mejora de 450 ms queda
  ENTERRADA bajo el ruido**: mi instrumento no puede verla aunque exista.

*El censo de A y mi cronómetro no se contradicen — miden cosas distintas, y
la mejora que A logró es más chica que mi ruido.* **Lo que sí afirmo con
mi número: el usuario no la percibe.**

## LA ARITMÉTICA QUE EL FOUNDER PIDIÓ, contestada

> Firmó postergar **#0b** sobre el supuesto de que el HOY quedaría cerca de
> **2,3 s**.

**El supuesto NO se cumple: estamos en ~4 s.** ⇒ **la decisión de postergar
#0b vuelve a la mesa.** *El número importaba justamente para esto, y sale
peor que el supuesto — no mejor.*

**Lo que NO afirmo, y lo digo:** no medí POR QUÉ. No sé si lo que domina es
el prólogo serial de arranque (**D-738**, 622 ms en cuatro viajes
encadenados solo para resolver quién soy), la red del momento, o algo del
bundle nuevo. **Bisecar exige otra ventana** y no la gasté acá.

---

# §VIII · EL GATE 2 NO SE PUDO CORRER — y la medición de #0a, contestada

## 🔴 EL FRENO, medido antes de gastar la ventana

**El OTA `01a0089a` (15/08 22:25) NO trae L2.** No lo deduje: lo caminé.
Toqué «Registrar repartidor» en el aparato y **abrió la Hoja VIEJA**, con
el literal que lo prueba:

> `Teléfono — opcional` — **el campo que la firma manda matar.**

Y el cruce contra el objeto dice dónde está el desfase, que **no es de A**:

| | estado |
|---|---|
| `origin/main` | ✅ **ya tiene el cableado correcto**: el CTA apunta a `/ventas/repartidor/nuevo` y `altaRepartidor` está en **0 menciones** |
| el OTA del aparato | 🔴 se publicó **ANTES** de ese merge |

⇒ **El recorrido de 5 pasos no se puede correr**: ① daría de alta por la
Hoja muerta, y ③④⑤ prueban un camino que este bundle no tiene.
**Lo que falta es un OTA nuevo desde el main actual** — y con él deberían
viajar mis tres commits finales, que **todavía no están en main** y uno es
funcional: la cura de que **en la edición no se exija foto** (sin ella no
se puede corregir un documento mal tipeado, justo lo que D-791 cura).

*No inventé un recorrido sobre la pantalla vieja para tener algo que
contar: habría gateado la superficie que L2 vino a reemplazar.*

## ✅ LA MEDICIÓN DE #0a — los tres discriminan, y la respuesta es una

**P3 · RTT desde el APARATO** (`ping`, 6 paquetes):
`min 12 · avg 39 · max 111 · mdev 33 ms`.
**P2 · desde ESCRITORIO, el mismo minuto** (5 corridas): total `52–96 ms`.

**P1 · el cruce DENTRO de una sola corrida** (ráfaga con marca de tiempo,
16 capturas por arranque, 3 arranques en frío):
**4,83 · 4,59 · 4,13 s** → mediana **4,59 s**.

### La respuesta a «¿de quién es la franja de ~1 s?»

| candidato | veredicto |
|---|---|
| **la red** | ☠️ **DESCARTADA.** El aparato varía en **decenas de ms** (mdev 33, max 111). *Una red que varía 33 ms no produce una franja de 1 s.* |
| **el instrumento** | ⚠️ **PARCIAL.** La ráfaga infla ~0,3–0,5 s contra los offsets fijos (16 `screencap` compiten con la app). **Pero la dispersión persiste en los DOS métodos** (~0,7–1 s), así que no la explica entera. |
| **el dispositivo / arranque** | ✅ **ES LO QUE QUEDA.** La variación vive en el arranque del runtime, no en la red ni en mi cronómetro. |

### 🔍 Y el instrumento se delató solo, otra vez

El primer frame de cada ráfaga (t≈0,95 s) pesaba **2 MB** y mi umbral lo
clasificó como CONTENIDO. **No era la app: era la pantalla anterior
todavía visible.** *El peso como proxy tiene un falso positivo justo al
principio* — corregido descartando los dos primeros frames. Es la tercera
vez en esta sesión que el instrumento contesta sobre otro objeto, y la
tercera que lo caza repetir en vez de creerle a una corrida.

**El número que la mesa necesita, sin maquillaje: ~4,1–4,8 s.** El estado
previo era 2,0–2,5. **#0a sigue sin cerrar, y la aritmética de #0b sigue
apoyada en un supuesto (~2,3 s) que no se cumple.**
