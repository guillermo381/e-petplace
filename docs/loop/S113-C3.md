# S113 · PISTA C — lote 0.1 «Foto se enciende» · parte

> **Este archivo es LA fuente del handoff.**
> Rama **`pista/s113-c3`** · SHA **`6e6efafb`** · desde `main` **`9d19de78`**.
> Árbol limpio · pusheado y verificado **por SHA**.

⚠️ **Medido al ramificar:** `main` está **un commit adelante** del SHA pedido
(`8f568432`), y ese commit toca **un solo archivo de docs**
(`docs/loop/S113-A.md`, 101 líneas). Ramificar del SHA pedido no cuesta nada —
se declara en vez de corregirlo por mi cuenta.

---

## ① LA PUERTA — medida antes de usarla

`registrarRecuerdoFamilia({ mascotaId, texto?, fotoPath?, fecha? })` **existe en
mi SHA**: `packages/api/src/wrappers/salud.ts:331`, exportado en `index.ts:335`,
RPC `registrar_recuerdo_familia` de la migración `20260909040000`. Sus seis
rebotes son los seis del encargo.

La foto viaja como **PATH** (bucket `mascotas`, carpeta del dueño), subida
**antes** por `subirFotoMascota` con prefijo `'recuerdo'`. **Procedencia y modo
de captura los estampa el servidor** — leído en la migración, no supuesto — y
por eso no son parámetros.

---

## ② LA PANTALLA · `/recuerdo` con `{ mascotaId, nombre }`

Calcada de `/carnet` y `/antiparasitario`. **La foto grande arriba**: tocarla
abre `HojaFotoMascota`, el selector que la app ya usa (cámara/galería, resize
800). **Sin foto no hay ícono de error: hay un lugar que invita** — la misma
diferencia que hay entre *«falta un dato»* y *«acá va algo tuyo»*. Debajo, unas
líneas opcionales con «Qué pasó…». La fecha: hoy, editable, nunca mañana. Un
solo botón.

### El orden de los dos actos, y no es intercambiable
**La foto primero.** Si la subida falla, **el recuerdo no se intenta**: *guardar
un recuerdo que dice tener foto y no la tiene es peor que no guardarlo.* El
formulario queda intacto y la razón se lee en una línea.

⚠️ **Las cinco razones de la subida no se reescribieron:** se reusan las de
`carnet.subida*`, ya en voz de la casa y en los dos idiomas (`L-175`). *Cinco
frases nuevas para decir lo mismo son cinco frases que algún día divergen.* Su
namespace dice `carnet` porque nacieron ahí y **describen la subida, no el
carnet**: el nombre quedó corto, no equivocado. El gate las vigila: si alguna se
retira de allá, suena antes que una pantalla muda.

---

## ③ LAS TRES DECISIONES SALIERON A UNA FRONTERA PURA

`lib/recuerdo/decidir.ts`, **sin un solo import de runtime** — y es lo que
vuelve **medible** el «cero llamadas» del encargo. *Que el botón no dispare nada
sin foto ni texto se puede leer en el código —el `return` está antes del primer
`await`— pero leer no es medir.*

- **`frenoDelRecuerdo`** — 🔴 **el orden de las dos preguntas importa**: primero
  «¿hay algo?», después «¿la fecha sirve?». Con el formulario recién abierto la
  razón que corresponde es *«una foto o unas palabras»*, no una sobre una fecha
  que nadie tocó.
- **`cuerpoDelRecuerdo`** — **un texto vacío no viaja**. `undefined` es «no
  hay»; `''` sería basura para una puerta que confía.
- **`keyDelRebote`** — `recuerdo_vacio` cae en la **misma key** que el freno de
  pantalla: *son el mismo hecho dicho por dos bocas.*

**Dos cosas las dijo el compilador, no yo:** el parámetro es `string` porque el
`ResultadoWrapper` ensancha el código con `'datos_inconsistentes'`; y el
**retorno se tipa como unión de literales**, porque con `string` el riel de i18n
deja pasar cualquier cosa y **una key mal tipeada llega a pantalla como su
propio nombre**.

---

## ④ EL CUARTO DEDO SE ENCENDIÓ, Y SU RAZÓN MURIÓ CON ÉL

`'sin_puerta'` salió del tipo `RazonApagado`, de `razonDeApagado`, de
`razonDelDedo` y de los dos diccionarios (`nexo.razonSinPuerta`). *Una razón que
sobrevive a su motivo es una frase esperando que alguien la vuelva a montar
creyendo que rige* (Ley 37). Queda **una** razón viva, y es de dominio y no de
construcción: **el acuario**.

**La mitad «papel» (documentos) no entró**, como estaba dicho: el dedo sigue
diciendo «Foto».

---

## ⑤ 🔴 LO QUE ESTE LOTE **NO** ENTREGA — y es de `packages/api`

> *«El recuerdo aparece en la línea de vida con el texto de la familia como su
> voz»* **no se cumple hoy.** No es un olvido: **el texto no llega.**

**Medido:** el motor escribe el texto en **`evento_hito_narrativo.texto`**
(migración `20260909040000`, el `INSERT`), y en `eventos_mascota.datos` pone
**sólo** `{clave_hito: 'recuerdo_familia'}`. `timeline.ts` lee `datos` y de ahí
saca `vacuna` y `clave_hito`; **ningún lector de `packages/api` toca
`evento_hito_narrativo`** (grep: cero). ⇒ `ItemTimeline` **no tiene por dónde
traer el texto**.

**Consecuencia medida, no supuesta:** `vozHecho` resuelve `hito_clave` contra
`VOZ_HITO` —tres claves— y `recuerdo_familia` no está, así que cae al `switch`
por tipo, `hito_narrativo` no es un `case`, y termina en el `default`:
**«Momento de cuidado»** (es) / **«A care moment»** (en).

**No le puse voz, y ésa es la decisión.** La mesa dijo que ese hito no tiene
frase de la casa y que no se la invente; con el texto fuera de alcance **no hay
nada verdadero que decir**, y cualquier frase que escribiera sería exactamente
lo prohibido. Dejarlo caer al genérico **tampoco está bien** —«momento de
cuidado» dice *cuidado* sobre algo que no lo es— y por eso va declarado como lo
único que el lote no entrega, no como detalle.

### 📮 Pedido a A, por nombre
**Que `ItemTimeline` traiga el texto del hito** — join a
`evento_hito_narrativo`, o que el motor también lo escriba en
`eventos_mascota.datos`. Con eso, `vozHecho` es **una línea** de mi lado: *si el
ítem trae texto de recuerdo, ese texto ES la voz.*

---

## ⑥ EL ROJO, ANTES DEL VERDE

Rompiendo el freno en sus dos mitades (el `trim` y la comparación de fechas):

```
ROJO · vacío · el espacio en blanco no cuenta como texto
ROJO · mañana se rechaza
ROJO · mañana NO se puede guardar
ROJO · 296 casos, 3 fallos.
```

Y el control de la ruta nueva, con `tipos:rutas` corrido antes: cambiando
`/recuerdo` por `/recuerdos` el typecheck contesta `TS2820` con las 151 rutas.

---

## ⑦ GATES

| gate | resultado |
|---|---|
| `verify:nexo` | **296 casos · 0 fallos** |
| `verify:puerta-unica` | **VERDE — cero accesos directos a la base** |
| `verify:diseno` | **VERDE, 61 reglas** |
| `verify:razon-muda` | **VERDE — el trinquete no subió** (el botón dice por qué) |
| `verify:coach` | **46/0** · `verify:glifos` **32/0** |
| typecheck | **4 tsc en 0**, y el del cliente **con las rutas medidas** |

---

## ⑧ LO QUE NO TOQUÉ

`packages/` · `supabase/` · el `package.json` raíz · el prestador ·
`BurbujaPendientes` · `voz-hecho.ts` (**a propósito** — ver ⑤) · la pantalla de
antiparasitario y sus keys · los glifos (ya son propios desde el lote 0).
**Cero dependencias nuevas** ⇒ sale por OTA. **No publiqué. Cero aparato.**

---

# ⏪ ENMIENDA — EL Z-ORDER DEL ORBE (`92e73361`)

## ⑨ EL MERGE DE B FUE NO-OP, Y SE DECLARA

Su SHA nuevo es **`4e944cb3`** y **ya estaba en mi rama**: A lo mergeó a `main`
en `fa7cbe5c`, **antes** de `9d19de78`, que es el SHA del que ramifiqué. El
merge contestó *«Already up to date»* y el control de ancestría lo confirma.
*No es que no hiciera falta pedirlo: es que llegó por la vía de `main`.*

Y lo que trae **resuelve la observación que dejé en la tercera enmienda del lote
0**: `montaje="control"` va **dentro de la pieza**, así que los cuatro dedos son
actos y `vacuna` deja de llevar su huella ahí — sin perderla en el carnet ni en
la línea de vida. **Mi montaje no cambia una línea**: el contrato de props es el
mismo (medido con el diff de `PresenciaCoach` entre `5dbcc5e2` y `4e944cb3`:
once líneas, todas comentario más la prop del glifo).

## ⑩ EL Z-ORDER — dos hechos de naturaleza distinta

**① La barra pinta DESPUÉS del orbe.** En el `tabBar` del shell, `NexoDelShell`
es el **primer** hijo del fragmento y `BarraTabs` el **segundo**; en RN los
hermanos posteriores pintan encima. *Donde se toquen, gana la barra* — lo único
que salva al orbe es que **no se toquen**.

**② Una tarjeta de pantalla no puede taparlo, por otra razón:** el orbe vive en
el subárbol del `tabBar`, que el navegador pinta **después** del contenedor de
pantallas. Los dos `zIndex: 2` que hay —Hogar `index.tsx:1749` y la ficha
`[mascotaId].tsx:1092`— ordenan **entre hermanos de su propio padre** y no
cruzan de subárbol. Medido además: **ninguna de las dos pantallas usa
`elevation`**, que es lo único que en Android podría cruzar.

### Los números (barra = `ALTO_FILA` 85 + inset típico 34 = **119**)

| qué | holgura sobre la barra |
|---|---|
| cuerpo del orbe | **+20** (= `AIRE_BORDE`, no un sobrante casual) |
| caja del halo | **+11** (el aro desborda 9 parejo) |
| resplandor | **−4** ⇒ **roza**, y es SOMBRA, no cuerpo |

**El orbe no queda tapado.** Lo que la barra recorta son **4 px de los píxeles
más tenues de su brillo**, y va declarado en vez de escondido: *el día que
alguien mueva `AIRE_BORDE` o `RESPLANDOR`, el cambio aparece en el gate y no en
un teléfono.*

### El alto de la fila se LEE del archivo, y no es un atajo
`ALTO_FILA_TABS` se exporta desde `BarraTabs.tsx` —un componente—, así que
importarlo arrastra `react-native` y **el arnés no arranca** (medido:
*«Unexpected typeof»* en `react-native/index.js`). *Ésa es exactamente la razón
por la que B puso su geometría en un módulo sin runtime.* Se lee del objeto, y
**si no se puede leer el gate NO da verde**: sale NO CONCLUYENTE.

## ⑪ 🔴 EL GATE SE CORRIGIÓ A SÍ MISMO ANTES DE QUE LE CREYERA

La primera versión del assert buscaba `aireInferior={altoBarra}` **en todo el
archivo** y **daba VERDE con la presencia en `0`**: encontraba el de
`BurbujaPendientes`, que está tres líneas más arriba y también lo recibe. *Dos
consumidores del mismo dato en el mismo archivo, y el gate no distinguía cuál
estaba midiendo.* Es **un verde por la razón equivocada**, y lo cazó producir el
rojo antes de confiar en el verde. Ahora mira el bloque de `PresenciaCoach`:

```
ROJO · la PRESENCIA recibe el aire medido
```

Y su **control negativo** nombra el modo de falla real: sin `aireInferior` el
orbe caería **99 px dentro** de la banda de la barra y, como la barra pinta
después, **quedaría tapado sin que nada falle**.

## ⑫ LAS KEYS ALIMENTAN LAS PASTILLAS, SIN CAMBIOS

`voz.chat` / `voz.pedidos` siguen viajando **siempre** —singular y plural desde
mis keys, aunque la cuenta sea 0— y `voz.preguntar` sale de `nexo.almohadilla`
con `{{nombre}}` interpolado desde `coach.nombre`. El gate lo mide en los dos
idiomas y prueba que ninguna deja una llave cruda.

## ⑬ GATES

`verify:nexo` **308 · 0** · `verify:puerta-unica` **VERDE** · `verify:diseno`
**VERDE, 61 reglas** · `verify:razon-muda` **VERDE** · `verify:coach` **46/0** ·
`verify:glifos` **32/0** · **4 tsc en 0** · typecheck propio del cliente en **0
con las rutas medidas** (`tipos:rutas` corrido antes).

**Cero aparato:** esto es geometría y orden de pintado **leídos del objeto**, no
una pantalla. Sigue en pie el hueco de ⑤ — el texto del recuerdo no llega al
timeline, y es pedido a A.

---

# ⏪ CIERRE DEL 0.1 — LA PIEZA NUEVA DE B (`7a45b8ea`)

Mergeado **`pista/s113-b-01`** fijado a **`edf9091a`**; control de ancestría
verde. **Es un merge de verdad:** 13 archivos, +842/−657.

## ⑭ ① EL ORDEN DE LOS DEDOS — confirmado en el gate

B lee `atajos` **de abajo hacia arriba, en el orden en que se pasan**. Mi
`ORDEN_DE_PATA` es `['peso','vacuna','antiparasitario','foto']` ⇒ **Peso abajo**
(el más cerca del pulgar) y **Foto arriba**. Es el orden de la mesa. Asertado, y
también que son **exactamente cuatro**, que es lo que la tupla exige.

## ⑮ ② `onPreguntar` NO SE DISPARA DOS VECES — medido de los dos lados

**En la pieza** hay dos productores y son **dos nodos distintos**: la pastilla de
la voz (`:592`) y el orbe abierto (`:621`, ternario `abierta ? onPreguntar :
onAbrir`). *Un toque enciende uno.*
**En mi montaje** `onPreguntar` se pasa **una** vez, `tocar('coach')` tiene **un**
solo llamador, y `onAbrir` sólo enciende la fila. Los tres asertados.

## ⑯ ③ EL TEXTO DEL RECUERDO — la condición NO se cumple, y es medición

> *«Si A ya declaró `texto` en `ItemTimeline`…»* — **no lo declaró.**

Medido: `origin/main` sigue en `8f568432`, **no hay rama `pista/s113-a` en
origin**, y el wrapper tiene **cero** ocurrencias de un campo de texto de hito.

**No agregué la línea.** Contra un campo que no existe sería código que no
compila, y ponerle una voz de la casa al hito es exactamente lo que la mesa
prohibió. **El hueco del §⑤ sigue donde estaba**, con el mismo pedido: que
`ItemTimeline` traiga el texto — de mi lado es una línea.

## ⑰ 🔴 EL GATE SE ROMPIÓ AL MERGEAR, Y ESO ES LO QUE TENÍA QUE PASAR

B rehízo la presencia: **la huella murió en el teléfono** y la reemplaza una
**fila ascendente** sobre el eje del orbe, que ya no viaja. Con ella murió
`HALO`, que mi arnés del z-order importaba, y **el arnés no arrancó**:

```
SyntaxError: … does not provide an export named 'HALO'
```

*Un gate atado a la geometría REAL se rompe cuando la geometría cambia, en vez
de seguir verde midiendo el mundo de ayer.* Reescrito contra la pieza nueva.

### Los números, re-medidos (barra = `ALTO_FILA` 85 + inset típico 34 = **119**)

| qué | holgura sobre la barra |
|---|---|
| cuerpo del orbe | **+20** (= `AIRE_BORDE`) |
| caja de los arcos | **+14** (desborda `ARCO_GROSOR·2`) |
| **orbe ABIERTO** | **+11,5** (crece 52/48 y **no viaja**) |
| resplandor | **−4** ⇒ roza; es SOMBRA, no cuerpo |
| *sin `aireInferior`* | **−99** ⇒ el control negativo |

**El orbe no queda tapado en reposo NI abierto.** Lo único que la barra recorta
son 4 px de los píxeles más tenues del brillo, declarado.

⭐ **Y el estado ABIERTO es un assert que la pieza vieja no necesitaba:** antes
el orbe viajaba al centro; ahora **crece donde está**, así que *crecer es la
única forma en que podría bajar hacia la barra.*

## ⑱ GATES DEL CIERRE

| gate | resultado |
|---|---|
| `verify:nexo` | **314 casos · 0 fallos** |
| `verify:coach` | **74/0** · `verify:glifos` **31/0** |
| `verify:diseno` | **VERDE, 61 reglas** |
| `verify:razon-muda` | **VERDE** · `verify:puerta-unica` **VERDE** |
| `verify:contrast` | **OK** · `tipos:rutas` **OK** |
| typecheck | **4 tsc en 0**, el del cliente **con las rutas medidas** |

## ⑲ LO QUE NO TOQUÉ EN EL 0.1 ENTERO

`packages/` (sólo lo que trajeron los merges de B) · `supabase/` · el
`package.json` raíz · el prestador · `BurbujaPendientes` · **`voz-hecho.ts` y
`timeline.ts` a propósito** (§⑯) · la pantalla de antiparasitario y sus keys ·
los glifos · el montaje de la presencia, que **no cambió una línea** con la
pieza nueva. **Cero dependencias nuevas** ⇒ sale por OTA. **No publiqué. Cero
aparato.**
