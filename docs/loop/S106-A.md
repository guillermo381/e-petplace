# S106-A · PARTE DE PISTA — telemedicina, el quinto oficio

> **Pista A** — conducción · DB (escritora única) · `packages/api` · `docs/` ·
> merges/push/OTA/builds (regla 88: `main` lo escribe solo A).
> **Abrió:** 25-ago-2026. **Estado:** turno ⓪ cerrado — mediciones depositadas,
> **cero código, cero migraciones**, esperando **Checkpoint 1**.

---

## ESTADO AL CIERRE DEL TURNO ⓪

| | |
|---|---|
| worktree | `../e-petplace-s106-a` · rama `pista/s106-a` |
| `node_modules` | **no existe en el worktree** (sin `pnpm install`) — no corrí Metro ni typechecks de app; se declara para que nadie asuma enganche probado |
| migraciones | **442 local = 442 remoto, cero desemparejadas** (medido, no citado) |
| escrito | `docs/PLAN_S106_TELEMEDICINA.md` (**verbatim**, PROPUESTA) · `docs/relevamientos/2026-08-25-s106-a-mediciones.md` · este parte |
| **construido** | **NADA.** Ninguna migración, ningún wrapper, ninguna enmienda de letra |

---

## LA COLA VIVA, EN ORDEN — próximo paso ejecutable primero

1. **ESPERAR CP1.** Ocho choques 🔴 contra fuente firmada están servidos en §9
   del relevamiento y **ninguno se resolvió**. La tanda 1 no abre sin firma.
2. Apenas CP1 firme: **enmiendas de letra ANTES que motor** — `LETRA_SALDO` §3
   (letra vieja tachada, no borrada) y `LETRA_TELEMEDICINA` §9/§10 + los dos
   punteros de C-7. *Construir sobre letra que la mesa todavía no arregló es
   fabricar la deuda de la próxima sesión.*
3. Recién entonces el motor, en este orden: **modalidad en el hold** (es lo que
   hoy hace nacer la marca mintiendo) → consentimiento por cita → ventana →
   no-realizable → habilitación → receta.
4. Cada migración: **reversa escrita ANTES**, 76(g) declarada aunque no rija,
   cinturón que aborta con el agujero cerrado, verificación **contra el objeto**
   (no contra el «Finished» de `db push` — L-422).

---

## COMPROMISOS ABIERTOS ENTRE PISTAS

**Lo que YO DEBO:**

- A **C**: corrí sus tres censos SQL (§7 del relevamiento) y **le devuelvo dos
  correcciones de nombre medidas** — `country_config.servicios_activos` no
  existe (es **`services_enabled`**) y `prestadores.nombre_negocio` no existe
  (es **`nombre_comercial`**). Los dos rebotaron `42703`. Resultados completos
  en §7.
- A **C**: confirmo sus dos hallazgos y los agravo con el escritor —
  `acepta_telemedicina` **cero lectores** y además **dos funciones lo siembran
  en cada alta**; `telemedicine` en `explorar/index.tsx:107` **solo brazo de
  apagado**, confirmado contra el archivo.
- A **D**: 🔴 **su medición de transporte tiene un límite que no sabía** —
  `cita_telemedicina_detalle.proveedor` tiene **CHECK cerrado a
  `daily|whereby|zoom` con default `daily`**. Si la mesa elige LiveKit/Agora/
  100ms/Stream/Vonage/Jitsi, **la tabla los rechaza**. Es decisión de letra, no
  un valor que se agrega de paso.
- A **D**: para D-M3 — esa misma tabla guarda **`token_prestador` y
  `token_cliente` como columnas**, y su policy de SELECT deja leer la fila a
  *cualquiera con acceso a la mascota*. Es un modelo distinto (y más débil) que
  el mint-por-cita que su medición propone. **No lo resolví: se lo paso.**
- A **B**: la marca de §7 **ya está decidida por letra canónica** —
  `BIO_EXPEDIENTE` **D13.6**: el padre es la propia cita, **no nace evento
  separado**. Su pieza dibuja desde `evento_cita_servicio.modalidad`, que ya
  admite `'telemedicina'`.

**Lo que ME DEBEN:** nada pendiente al cierre del turno ⓪.

---

## FRENOS Y SUPUESTOS VIVOS

- 🔴 **El motor de saldo NO EXISTE** (cero tablas, cero funciones). Las dos ramas
  donde la plata vuelve —§4 y §5 de la letra— **no tienen a dónde volver**. El
  plan §5-C afirma *«el saldo ya existe por `LETRA_SALDO`»*: **es falso, medido**.
- 🔴 **La ventana de cancelación tiene tres valores firmados** (`LETRA_SALDO`
  ≥24 h · **T&C §25.1 24 h, publicado 24-ago** · letra 30 min), y la banda
  30 min–24 h tiene dos respuestas opuestas.
- 🔴 **`LETRA_PAGO_CITAS` §5 firma ②** manda la cancelación clínica **a soporte,
  manual**, mientras P22 no tenga letra — y **P22 sigue sin letra**.
- 🔴 **La receta está EXCLUIDA de v1 por §9 de la letra**; el plan la incluye.
  Necesita enmienda escrita, no interpretación.
- 🔴 **Sustancias fiscalizadas: hoy no se pueden identificar por diseño** —
  cero catálogo, cero CHECK, y `principio_activo` **vacío en 4 de 4 filas**.
  Un guard sobre ese campo daría verde siempre.
- ⚠️ **`cita_telemedicina_detalle` es legado sin migración** — y es **clase, no
  caso**: **57 de 268 tablas** (21 %) no las nombra ninguna migración.
- ⚠️ **Dos archivos «106» que hablan de sesiones distintas**: `PLAN_MESA_106`
  (mesa, dice *«no construye telemedicina»*) y `PLAN_S106_TELEMEDICINA` (pista,
  la construye). No es contradicción — es la numeración doble que el propio
  `PLAN_MESA_106` advierte. **Declarado para que nadie lo lea como choque.**
- ⚠️ **El plan está SIN FIRMAR.** Se depositó verbatim porque así se ordenó; su
  §1 y su §5-C contienen al menos tres afirmaciones que las mediciones
  contradicen (saldo, receta, punteros). **La firma llega en CP1, no antes.**

---

## ERRORES PROPIOS DE ESTE TURNO — declarados

1. **Casi reporto un «cuarto interruptor suelto» que no existe.** Mi primer
   regex (`s\.reservable`) **confundía `ts.reservable` con `ps.reservable`**.
   Re-medido separando prefijos, el motor dice en su propio cuerpo *«reservable
   en DOS niveles»* (S68): plataforma **AND** oferta, deliberado y comentado.
   *Un regex que matchea de más produce un hallazgo que suena grave y es falso.*
2. **Copié el SQL de C sin relevar nombres** (regla 22 / L-057) y dos consultas
   rebotaron `42703`. El error de origen era de C, pero **yo lo ejecuté sin
   verificar** — la DB es mía y la verificación también.
3. **Casi se me escapa `cita_telemedicina_detalle`**: mi censo de columnas
   buscaba `%tele%` y el de tablas buscaba patrones de consentimiento. La cazó
   **un patrón de otra medición** (`%medic%` matchea dentro de «tele*medic*ina»).
   *La encontró la suerte, no el método* — por eso el censo de §6 se hizo después
   con el patrón correcto y se dimensionó la clase entera.

---

## PARA QUIEN ABRA LA TANDA 1

Leer **§9 del relevamiento primero** (los ocho choques) y **§10 después** (lo que
sí está a favor). El orden importa: §10 tiene buenas noticias reales —la
categoría propia no rompe nada, la marca ya está decidida por letra, el desglose
lo hace un trigger— y leerlas antes de §9 invita a subestimar los frenos.
