# S106-A · PARTE DE PISTA — telemedicina, el quinto oficio

> **Pista A** — conducción · DB (escritora única) · `packages/api` · `docs/` ·
> merges/push/OTA/builds (regla 88: `main` lo escribe solo A).
> **Abrió:** 25-ago-2026. **Estado:** turno ⓪ cerrado — mediciones depositadas,
> **cero código, cero migraciones**, esperando **Checkpoint 1**.

---

## ESTADO AL CIERRE DEL TURNO ⓪

| | |
|---|---|
| worktree | `../e-petplace-s106-a` · rama `pista/s106-a` |
| `node_modules` | **no existe en el worktree** (sin `pnpm install`) — no corrí Metro ni typechecks de app; se declara para que nadie asuma enganche probado |
| migraciones | **442 local = 442 remoto, cero desemparejadas** (medido, no citado) |
| escrito | `docs/PLAN_S106_TELEMEDICINA.md` (**verbatim**, PROPUESTA) · `docs/relevamientos/2026-08-25-s106-a-mediciones.md` · este parte |
| **construido** | **NADA.** Ninguna migración, ningún wrapper, ninguna enmienda de letra |

---

## LA COLA VIVA, EN ORDEN — próximo paso ejecutable primero

1. **ESPERAR CP1.** Ocho choques 🔴 contra fuente firmada están servidos en §9
   del relevamiento y **ninguno se resolvió**. La tanda 1 no abre sin firma.
2. Apenas CP1 firme: **enmiendas de letra ANTES que motor** — `LETRA_SALDO` §3
   (letra vieja tachada, no borrada) y `LETRA_TELEMEDICINA` §9/§10 + los dos
   punteros de C-7. *Construir sobre letra que la mesa todavía no arregló es
   fabricar la deuda de la próxima sesión.*
3. Recién entonces el motor, en este orden: **modalidad en el hold** (es lo que
   hoy hace nacer la marca mintiendo) → consentimiento por cita → ventana →
   no-realizable → habilitación → receta.
4. Cada migración: **reversa escrita ANTES**, 76(g) declarada aunque no rija,
   cinturón que aborta con el agujero cerrado, verificación **contra el objeto**
   (no contra el «Finished» de `db push` — L-422).

---

## COMPROMISOS ABIERTOS ENTRE PISTAS

**Lo que YO DEBO:**

- A **C**: corrí sus tres censos SQL (§7 del relevamiento) y **le devuelvo dos
  correcciones de nombre medidas** — `country_config.servicios_activos` no
  existe (es **`services_enabled`**) y `prestadores.nombre_negocio` no existe
  (es **`nombre_comercial`**). Los dos rebotaron `42703`. Resultados completos
  en §7.
- A **C**: confirmo sus dos hallazgos y los agravo con el escritor —
  `acepta_telemedicina` **cero lectores** y además **dos funciones lo siembran
  en cada alta**; `telemedicine` en `explorar/index.tsx:107` **solo brazo de
  apagado**, confirmado contra el archivo.
- A **D**: 🔴 **su medición de transporte tiene un límite que no sabía** —
  `cita_telemedicina_detalle.proveedor` tiene **CHECK cerrado a
  `daily|whereby|zoom` con default `daily`**. Si la mesa elige LiveKit/Agora/
  100ms/Stream/Vonage/Jitsi, **la tabla los rechaza**. Es decisión de letra, no
  un valor que se agrega de paso.
- A **D**: para D-M3 — esa misma tabla guarda **`token_prestador` y
  `token_cliente` como columnas**, y su policy de SELECT deja leer la fila a
  *cualquiera con acceso a la mascota*. Es un modelo distinto (y más débil) que
  el mint-por-cita que su medición propone. **No lo resolví: se lo paso.**
- A **B**: la marca de §7 **ya está decidida por letra canónica** —
  `BIO_EXPEDIENTE` **D13.6**: el padre es la propia cita, **no nace evento
  separado**. Su pieza dibuja desde `evento_cita_servicio.modalidad`, que ya
  admite `'telemedicina'`.

**Lo que ME DEBEN:** nada pendiente al cierre del turno ⓪.

---

## FRENOS Y SUPUESTOS VIVOS

- 🔴 **El motor de saldo NO EXISTE** (cero tablas, cero funciones). Las dos ramas
  donde la plata vuelve —§4 y §5 de la letra— **no tienen a dónde volver**. El
  plan §5-C afirma *«el saldo ya existe por `LETRA_SALDO`»*: **es falso, medido**.
- 🔴 **La ventana de cancelación tiene tres valores firmados** (`LETRA_SALDO`
  ≥24 h · **T&C §25.1 24 h, publicado 24-ago** · letra 30 min), y la banda
  30 min–24 h tiene dos respuestas opuestas.
- 🔴 **`LETRA_PAGO_CITAS` §5 firma ②** manda la cancelación clínica **a soporte,
  manual**, mientras P22 no tenga letra — y **P22 sigue sin letra**.
- 🔴 **La receta está EXCLUIDA de v1 por §9 de la letra**; el plan la incluye.
  Necesita enmienda escrita, no interpretación.
- 🔴 **Sustancias fiscalizadas: hoy no se pueden identificar por diseño** —
  cero catálogo, cero CHECK, y `principio_activo` **vacío en 4 de 4 filas**.
  Un guard sobre ese campo daría verde siempre.
- ⚠️ **`cita_telemedicina_detalle` es legado sin migración** — y es **clase, no
  caso**: **57 de 268 tablas** (21 %) no las nombra ninguna migración.
- ⚠️ **Dos archivos «106» que hablan de sesiones distintas**: `PLAN_MESA_106`
  (mesa, dice *«no construye telemedicina»*) y `PLAN_S106_TELEMEDICINA` (pista,
  la construye). No es contradicción — es la numeración doble que el propio
  `PLAN_MESA_106` advierte. **Declarado para que nadie lo lea como choque.**
- ⚠️ **El plan está SIN FIRMAR.** Se depositó verbatim porque así se ordenó; su
  §1 y su §5-C contienen al menos tres afirmaciones que las mediciones
  contradicen (saldo, receta, punteros). **La firma llega en CP1, no antes.**

---

## ERRORES PROPIOS DE ESTE TURNO — declarados

1. **Casi reporto un «cuarto interruptor suelto» que no existe.** Mi primer
   regex (`s\.reservable`) **confundía `ts.reservable` con `ps.reservable`**.
   Re-medido separando prefijos, el motor dice en su propio cuerpo *«reservable
   en DOS niveles»* (S68): plataforma **AND** oferta, deliberado y comentado.
   *Un regex que matchea de más produce un hallazgo que suena grave y es falso.*
2. **Copié el SQL de C sin relevar nombres** (regla 22 / L-057) y dos consultas
   rebotaron `42703`. El error de origen era de C, pero **yo lo ejecuté sin
   verificar** — la DB es mía y la verificación también.
3. **Casi se me escapa `cita_telemedicina_detalle`**: mi censo de columnas
   buscaba `%tele%` y el de tablas buscaba patrones de consentimiento. La cazó
   **un patrón de otra medición** (`%medic%` matchea dentro de «tele*medic*ina»).
   *La encontró la suerte, no el método* — por eso el censo de §6 se hizo después
   con el patrón correcto y se dimensionó la clase entera.

---

## PARA QUIEN ABRA LA TANDA 1

Leer **§9 del relevamiento primero** (los ocho choques) y **§10 después** (lo que
sí está a favor). El orden importa: §10 tiene buenas noticias reales —la
categoría propia no rompe nada, la marca ya está decidida por letra, el desglose
lo hace un trigger— y leerlas antes de §9 invita a subestimar los frenos.

---
---

# TANDA 1 — estado al cierre de la jornada (26-ago-2026)

**`origin/main` = `41691513`.** Todo lo de abajo está ahí, verificado por
contenido. **`node_modules` del worktree: enganchado a `packages/ui` de MI
worktree** (re-medido con `pwd -P` tras `pnpm install`, como pedía la
precondición — en ⓪ no había paquetes).

## LO CERRADO

**Acto 1 · las letras** — `LETRA_TELEMEDICINA` **v1.1** (§3 tuteo + línea de
tránsito · §4/§5 medio de pago por soporte · §4 el profesional que no asiste ·
§7 la marca por D13.6 + la cita §31 · §9 la receta partida en dos · el FRENO
adjudicado) · **el acta de CP1 como objeto** (10 firmas) · el documento del
abogado VERBATIM · `PLAN_S106_TELEMEDICINA` **v1.1** con §5-C tachado ·
**`LETRA_SALDO` §3 NO se tocó** (el freno se disuelve por la adjudicación, no
por enmienda).

**Acto 2 · el motor — SIETE migraciones**, todas con reversa escrita ANTES,
76(g) declarada y cinturón:

| migración | qué |
|---|---|
| `…200000` | la ventana deja de ser constante (telemedicina 30 · resto 1440 = la conducta de hoy) |
| `…210000` | modalidad DERIVADA del tipo + consentimiento ATÓMICO |
| `…220000` | la habilitación §6/§8, con el gate en la LECTURA |
| `…230000` | no realizable (§5) + cancelación en ventana (§4) + `solicitudes_devolucion` |
| `…240000` | se jubila `acepta_telemedicina` (columna muerta, parámetro con lápida) |
| `…250000` | muere `cita_telemedicina_detalle` (D-930) |
| `…260000` | `puede_entrar_a_videollamada` (pedido 76b de D) |
| `…270000` | sin diagnóstico no se prescribe (respaldo legal) |

**Acto 3 · los wrappers** — `telemedicina.ts` con los cuatro · `agendamiento.ts`
gana `acepta_teleconsulta` y dos códigos tipados · `index.ts` en hunk aditivo
declarado · `gen:types` regenerado.

**Fuera de plan, porque eran bloqueantes:** la cura del agujero de **R66** ·
los **permisos del cable** verificados contra el manifiesto generado ·
`D-930` · `D-931` · `D-932`.

## OPERATIVO, MEDIDO

- **450 migraciones local = 450 remoto, cero desemparejadas.**
- **4 typechecks en 0** · `verify:diseno` **VERDE, 57 reglas**.
- **`tipos_servicio.telemedicina.reservable = false`** — la llave sigue del
  founder y va última.

## LOS ERRORES PROPIOS DE ESTA TANDA — declarados

1. **Reporté en ⓪ que el hold rebotaría una teleconsulta.** Falso: el guard
   de modalidad vive DENTRO del bloque de grooming ⇒ **nacía con
   `modalidad='presencial'`**. Mismo daño, mecanismo distinto.
2. **Un commit entero se perdió** porque el mensaje llevaba backticks y el
   shell los ejecutó; el push dijo `Everything up-to-date` sobre nada. *Los
   wrappers estuvieron sin commitear mientras C los esperaba.* Cura: mensaje
   por archivo (`-F`).
3. **Mergeé desde dentro del worktree** y salió `Already up to date` — el
   merge a `main` va desde el primario, en su propio comando. Estaba escrito
   en el canon y lo hice igual.
4. **Un cinturón se disparó contra su propia lápida** (leyó el comentario
   como código, L-170) y **otro miró `prosrc` buscando un parámetro que vive
   en la FIRMA** (L-235). Los dos quedaron escritos en su migración.
5. **`expo prebuild` muta el repo**: cambió los scripts de `package.json` y
   borró `.expo/types/router.d.ts` de las dos apps. Revertido y regenerado —
   pero es la misma clase que el `deno` de `verify-edge-deno`.

## LO QUE QUEDA

- **La build del cable** — perfil **`development`**, no `preview` (corrección
  de C: en `preview` `__DEV__` es false y la pantalla del cable no se monta).
  Espera el aviso de C de que el cable está aplicado.
- El **version bump** de las dos apps va **en el mismo acto que la build**.
- `NSMicrophoneUsageDescription` (redactada por C) al lote de strings del
  founder; la cadena de cámara es **`D-932`**, ficha y no obra.
- El **arnés del camino feliz** de `puede_entrar_a_videollamada`: hoy hay
  **cero citas de telemedicina**, así que no se puede probar contra dato real.
  Se declara **NO CONCLUYENTE**, como pidió D — el verde flojo no vale.

---

## ADDENDA — la build autónoma del cable (26-ago-2026)

**Lo que el founder me reconoció y queda anotado, porque son mis errores:**

1. **El marcador del pie del prestador estaba curado a medias desde S103-C.**
   Decía `bundle embebido / dev` para los dos casos ⇒ **el paso 2 del gate no
   era verificable**. *Es la clase de cura peor que ninguna, porque parece
   hecha.* Curado en `e6f88661`.
2. **Mi grep dio cero sobre el módulo nativo y casi lo reporto como
   bloqueo.** La librería no se llama «livekit» ni «webrtc»: es
   `libjingle_peerconnection_so.so`. *Un instrumento que busca el nombre que
   yo esperaba no mide la presencia: mide mi vocabulario.*

**Hallazgos nuevos de esta pasada:**

3. 🔴 **`eas-cli build --local` VUELCA EL KEYSTORE Y SUS CONTRASEÑAS a
   stdout cuando el build falla** — base64 de la firma + `keystorePassword`
   + `keyPassword`, en claro. Apareció en el primer intento fallido del
   cliente. Logs borrados; **queda en el transcript de la sesión**. *Es la
   familia de `D-712`: los artefactos de un proceso son un vector nuevo.*
   **Decisión de rotación: del founder.**
4. **El perfil `preview` no compilaba en esta máquina**: `OutOfMemoryError:
   Metaspace` en `lintVitalAnalyzeRelease` — tareas que **solo corren en
   release**, por eso `development` (debug) sí pasaba. El
   `android/gradle.properties` que genera Expo fija
   `-Xmx2048m -XX:MaxMetaspaceSize=512m`. Resuelto **sin tocar el repo**,
   con `GRADLE_OPTS=-Dorg.gradle.jvmargs=…`. *`GRADLE_OPTS` a secas no
   alcanza: no llega al daemon.*
5. **El prestador está más atrasado que el cliente en la calle**: su último
   OTA es de **S104**; el del cliente, de S105.

---

## OTA PUBLICADO — 26-ago-2026, las dos apps (orden del founder)

| app | canal | group | runtime | ancla |
|---|---|---|---|---|
| **cliente** | `preview` | `4d7fdf84` | **1.0.5** | `5aea7f98` |
| **prestador** | `preview` | `e11cc565` | **1.0.6** | `5aea7f98` |

**El freno que el founder pidió se corrió ANTES de publicar y salió limpio:**
el JS entre el ancla de los APK del gate (`fafffefa`) y `main` era
**idéntico — 0 archivos distintos** (los dos commits del medio eran solo
`docs/`). ⇒ **el OTA no podía pisar el bundle del cable: ES el mismo JS.**

**Runtime verificado contra los binarios instalados:** cliente `1.0.5`,
prestador `1.0.6` — leídos del `badging` de los APK, no de `app.json`.

🔴 **Y una consecuencia que hay que decirle al founder, porque INVIERTE la
instrucción de anoche:** al aplicarse el OTA, el pie deja de decir
**`bundle embebido`** y pasa a **`update 01a03ebd… · preview`** (cliente) /
**`update 01a03ebe… · preview`** (prestador). *Anoche eso era la señal de
«PARÁ»; desde este publish es lo esperado.* **La señal de alarma pasa a ser
un `update` con un id que NO sea uno de esos dos.**

**Qué alcanza este publish, medido:** los **17 commits de S105** del cliente
que nunca habían salido (los 160 voseos de `packages/api` curados, la voz de
`ya_invitada`, el email validado de verdad, `retomar-compra`) y **el ciclo
entero** que le faltaba al prestador, cuyo último OTA era de **S104**.

---
---

# 🔒 CIERRE DE LA TANDA 1 — S106-A · 26-ago-2026

> **Esto NO es un resumen para mí: es el objeto que va a leer quien siga.**
> La tanda 2 abre en **sesión y worktree nuevos**, leyendo del repo. Todo lo
> que hace falta para arrancar sin mí está acá o apuntado desde acá.

## ⓪ EL ESTADO, EN UNA TABLA

| | |
|---|---|
| `main` | **al día**, árbol limpio |
| migraciones | **450 local = 450 remoto**, cero desemparejadas (medido) |
| typechecks | los **4 en 0** |
| `verify:diseno` | **VERDE, 58 reglas** (R67 nació en esta tanda, de B) |
| `gen:types` | al día |
| OTA vigentes | cliente `4d7fdf84` (rt **1.0.5**) · prestador `e11cc565` (rt **1.0.6**), canal `preview`, ancla `5aea7f98` |
| 🔑 **el encendido** | **`tipos_servicio.telemedicina.reservable = false`** — sin tocar, y va **última** |

## ① LOS NÚMEROS USADOS — para que nadie los repita

**Deudas nacidas por A en esta tanda: `D-926` → `D-933`.**

| # | qué |
|---|---|
| `D-926` | 🔴 el motor de saldo **no existe** (0 tablas, 0 funciones) |
| `D-927` | catálogo de sustancias fiscalizadas + guard — con las fuentes VERBATIM del abogado y sus cuatro reglas |
| `D-928` | fase REV telemática (emisión + FirmaEC + diagnóstico obligatorio + la decisión de antibióticos) |
| `D-929` | **57 de 268 tablas (21 %)** que ninguna migración nombra |
| `D-930` | ☠️ `cita_telemedicina_detalle` — muerta; la ficha dice **por qué** (los tokens), no que estuviera vacía |
| `D-931` | webhooks de LiveKit: **registrar sin juzgar** — escrita y **sin construir** hasta firma |
| `D-932` | la cadena de cámara del cliente habla de la foto de la mascota |
| `D-933` | la pantalla del cable sin preview propio — **observación**, no defecto |

**Lección candidata: `L-426`** — *la plataforma jamás sugiere medicamentos,
tratamientos ni posologías*, con su **frontera fina** (ejemplos de FORMATO
legales; nombres de fármaco o principio activo **jamás**, ni en placeholder ni
en prompt). **CANDIDATA: no rige hasta firma del founder.**

> **Siguiente libre, medido por grep al cerrar: `D-934` · `L-427` · `R68`.**
> *Se re-verifican contra el objeto antes de usarse (L-141) — esta línea es
> derivada y decae.*

## ② LAS OCHO MIGRACIONES — todas con reversa escrita ANTES y 76(g) declarada

`20260826200000` ventana de cancelación como parámetro (telemedicina 30 · el
resto 1440 = la conducta de hoy) · `210000` **modalidad derivada del tipo +
consentimiento atómico** · `220000` habilitación §6/§8 con el gate **en la
lectura** · `230000` no realizable + cancelación en ventana +
`solicitudes_devolucion` · `240000` jubilación de `acepta_telemedicina` ·
`250000` **DROP** de `cita_telemedicina_detalle` · `260000`
`puede_entrar_a_videollamada` (pedido de D) · `270000` **sin diagnóstico no se
prescribe**.

**Reversas** en `docs/relevamientos/2026-08-2*-s106a-REVERSA-*.sql`. Dos
llevan cuerpos embebidos **porque son su única fuente**.

## ③ DÓNDE QUEDÓ CADA COSA

| qué | dónde |
|---|---|
| la letra | `docs/LETRA_TELEMEDICINA.md` **v1.1** |
| las diez + **una** firmas de CP1 | `docs/actas/2026-08-25-s106-CP1-ACTA.md` |
| el verde del cable | `docs/actas/2026-08-26-s106-GATE-CABLE-LIVEKIT.md` |
| el documento del abogado | `docs/legal/2026-08-25-receta-videoconsulta.md` — 🔴 **pendiente de ratificación escrita, por su propia nota** |
| el plan | `docs/PLAN_S106_TELEMEDICINA.md` **v1.1** (su §5-C **tachado**: era falso) |
| mis mediciones de ⓪ | `docs/relevamientos/2026-08-25-s106-a-mediciones.md` |
| los contratos para C | `docs/relevamientos/2026-08-25-s106-a-CONTRATOS-PARA-C.md` |

## ④ LO QUE LA TANDA 2 TIENE QUE SABER

**Lo que YA está y no hay que rehacer:** el motor entero de la tanda 1 · los
wrappers (`packages/api/src/wrappers/telemedicina.ts`, 5 funciones) ·
`video-token` desplegada (D) · el cable probado en dispositivo · el aviso §3
con su juez `R67` (B).

**Lo que NO está, con su razón:**
- **La in-call real** — el cable probó el transporte, no la pantalla. `D-933`.
- **El motor de saldo** (`D-926`) ⇒ **la devolución va al MEDIO DE PAGO por
  soporte**, jamás como saldo. Está en la letra §4/§5 y en la firma ③ de CP1.
- **La emisión de la REV y el guard de fiscalizadas** (`D-928`, `D-927`).
- **El camino feliz de `puede_entrar_a_videollamada` NO se probó**: hay
  **cero citas de telemedicina** en la base. Declarado **NO CONCLUYENTE** a
  pedido de D — *el verde flojo no vale.* **Su arnés es lo primero que la
  tanda 2 puede ejercer de verdad**, en cuanto exista una cita real.

**Los tres frenos vivos que NO son míos:** el deploy de `apps/pagos-web`
(ventana de Vercel; se libera ~21:30 -05 del 26-ago) · el guard del IVA
(Erick) · la ratificación del abogado.

## ⑤ LO QUE ESTA TANDA ME ENSEÑÓ, sin maquillar

1. **Un commit se perdió por backticks en el mensaje** y el `push` dijo
   `Everything up-to-date` **sobre nada**. Los wrappers estuvieron sin
   commitear mientras C los esperaba. *Miré el comando en vez del objeto.*
   Cura: `git commit -F`.
2. **Mergeé desde dentro del worktree** y salió `Already up to date`. El
   merge a `main` va **desde el primario, en su propio comando** — estaba
   escrito en el canon.
3. **Dos cinturones se dispararon contra sí mismos**: uno leyó **su propia
   lápida** como código (L-170); otro buscó un parámetro en `prosrc`, que es
   **el cuerpo y no la firma** (L-235). Los dos quedaron escritos en su
   migración.
4. **Reporté en ⓪ un mecanismo equivocado**: dije que el hold rebotaría una
   teleconsulta; medido, el guard vive **dentro del bloque de grooming** ⇒
   nacía con `modalidad='presencial'`. Mismo daño, otra causa.
5. **Dos greps míos dieron cero y casi los reporto como hechos**: el del
   agujero de R66 (la inyección nunca ocurrió — ancla equivocada) y el del
   módulo nativo (**la librería se llama `libjingle_peerconnection_so.so`**,
   ni «livekit» ni «webrtc»). *Un instrumento que busca el nombre que yo
   esperaba mide mi vocabulario, no el mundo.*
6. **El pie del prestador estaba curado a medias desde S103-C** — con eso el
   paso 2 del gate no era verificable. *La clase de cura peor que ninguna,
   porque parece hecha.*

## ⑥ DOS COSAS OPERATIVAS QUE VAN A VOLVER A MORDER

- 🔴 **`eas-cli build --local` vuelca el keystore y sus contraseñas a stdout
  cuando el build falla** (base64 de la firma + `keystorePassword` +
  `keyPassword`). Pasó una vez; logs borrados, **pero quedó en el transcript
  de esa sesión**. Familia de `D-712`. **La rotación es decisión del
  founder.**
- **El perfil `preview` no compila en esta máquina sin ayuda**:
  `OutOfMemoryError: Metaspace` en las tareas de lint, que **solo corren en
  release**. El `gradle.properties` que genera Expo fija 512 MB. Se resuelve
  **sin tocar el repo**:
  `GRADLE_OPTS="-Dorg.gradle.jvmargs=-Xmx6g -XX:MaxMetaspaceSize=2g -Dorg.gradle.daemon=false"`.
  *`GRADLE_OPTS` a secas no sirve: no llega al daemon.*
- **`expo prebuild` MUTA el repo**: cambia los scripts de `package.json` y
  borra `.expo/types/router.d.ts` de las dos apps. Revertir y regenerar
  (`expo start`) **antes de commitear**.
