# S101-A · BITÁCORA — EL MOTOR DE PAGOS

> Pista única. Consume `LETRA_MOTOR_PAGOS_S101.md` + el paquete de arranque (19-ago).
> **Objetivo del paquete: que la callback URL exista y sea real antes de que Erick la pida.**

---

## ✅ ENTREGADO HOY

### B0 · El censo — `docs/relevamientos/2026-08-19-s101-censo-pagos.md`

**Titular: el motor ya está construido en su mayor parte.** Lo que falta es quien lo llame.

Los tres reportes inmediatos de §1.3:
- **⑥ VERDE** — `confirmar_pago_pedido` sigue revocada de `authenticated` (solo `service_role`), y además tiene el gate en el cuerpo. D-764 se sostiene.
- **① 🔴 SÍ HAY TABLAS PREEXISTENTES** — `pagos_intentos` (15 col) y `pagos_eventos` (9 col), más `compras` + `compra_desglose`.
- **⑦ VERDE** — `seller_comisiones` **ya no existe** (jubilada). El 20 % de D-748 no tiene dónde vivir. `fee_configs` tiene el **10 % firmado vigente para EC** y el 14 % de EC **cerrado con fecha**.

### Migración 1 · `webhook_events` — APLICADA Y REGISTRADA
- `supabase/migrations/20260821000000_s101_webhook_events.sql`
- Reversa escrita **antes**: `docs/relevamientos/2026-08-19-s101-REVERSA-webhook-events.sql` (declara que revertir **borra la auditoría**).
- Veda 76(g): **NO RIGE** (DDL aditivo, sin backfill).
- Cinturón adentro: aborta si queda sin RLS, con policies, o con grants.
- Verificado **por medición aparte**: `rls=true · policies=0 · anon=false · authenticated=false · service_role_insert=true · registrada=true`.

### La función buzón — DESPLEGADA
- `supabase/functions/pagos-webhook-stg/index.ts`, deploy con `--no-verify-jwt`.
- Secreto puesto: `PAGOS_AMBIENTE=sandbox`. **Faltan `NUVEI_APP_CODE_SERVER` y `NUVEI_APP_KEY_SERVER`** (son del founder/Erick).
- Ensayos §5 contra el endpoint real: **200 · 200 · 405**, los tres correctos.
- Control post-ensayo: **nada se movió** (6 pedidos en `pagando`, suma 479,03 · `pagos_intentos` 14 · `pagos_eventos` 14, idénticos al pre).

### 🔗 LA URL, QUE ES EL ENTREGABLE

```
https://zyltipqscdsdsxnjclhp.supabase.co/functions/v1/pagos-webhook-stg
```

*El `project-ref` se leyó de `supabase/.temp/project-ref`, no se transcribió de ningún documento.*

---

## 🔴 LO QUE EL CENSO CAMBIA DEL PLAN — para quien siga

1. **Migración 2 NO crea una tabla `pagos`: ENMIENDA `pagos_intentos`.** Crearla al lado sería el modo de falla que la letra §1.6 vino a evitar. Le faltan: `compra_id`, `confirmado_por`, `authorization_code`, marca/bin/últimos4, y **UNIQUE sobre `(proveedor, proveedor_referencia)`** (hoy el índice no es único ⇒ la idempotencia por transaction_id no tiene candado).
2. **Ítem nuevo que la letra no tenía: la orquestación compra → N pedidos.** `confirmar_pago_pedido` recibe UN pedido; una compra puede tener N (`pedidos.compra_id` + `crear_compra_desde_pedidos(uuid[])`). **No existe quien itere.** Sin eso, una compra de dos tiendas se confirma a medias.
3. **La validación de monto** (letra §5.4) es cambio de `confirmar_pago_pedido`, que hoy escribe `monto = v_ped.total` sin comparar con lo que la pasarela dice haber cobrado.
4. **Los clavados son SEIS, no cuatro** (suma 479,03 USD), y `pagando` es **narrativa**, no estado — la firma del founder está cumplida en el código (`escalera.ts:247` devuelve `{pasos: []}`).
5. **`crear_intento_pago` no crea ningún intento** — reserva stock, congela el desglose y devuelve el sobre. El nombre induce a error a quien lo lea.
6. **El desglose congelado SÍ está implementado**, no solo escrito.

---

## ⏳ COLA VIVA, EN ORDEN

| # | Qué | Bloqueo |
|---|---|---|
| 1 | **Pasarle la URL a Erick** + la pregunta 5 (¿URLs distintas para staging y producción?) | del founder |
| 2 | Cargar `NUVEI_APP_CODE_SERVER` / `NUVEI_APP_KEY_SERVER` como secrets | del founder |
| 3 | **Decidir migración 2 sobre el censo** — enmendar `pagos_intentos`, no crear `pagos` | firma |
| 4 | La orquestación compra → N pedidos | tras 3 |
| 5 | Validación de monto en `confirmar_pago_pedido` | tras 3 |
| 6 | Máquina de estados de pasarela como dato (letra §4) | tras 3 |
| 7 | Los cuatro casos con arnés sobre fixtures (letra §6) | tras 4-6 |
| 8 | Taxonomía de fallo con voz (letra §7) | tras 6 |
| 9 | Cobro contra sandbox de punta a punta | 1 + 2 |

---

## 🚧 FRENOS Y SUPUESTOS VIVOS

- **NO existe proyecto de staging** (medido: `zyltipqscdsdsxnjclhp` activo/linkeado · `fdgocvplonivuejcmtbq` INACTIVE, otra región, nunca linkeado — un primer intento abandonado). **Rige la mitigación de §0 completa.** Convertir el inactivo en staging es decisión y trabajo aparte.
- **La ubicación del `stoken` en el payload NO está medida** contra sandbox. La función lo busca en tres lugares a propósito y `detalle` va a decir cuál acertó en el primer evento real. **Cuando se sepa, se fija uno y se borran los otros dos.**
- **`planintegracionnuveiepetplace.md` no está en el repo.** La letra lo cita como fuente del censo ③ y no existe en el árbol. Nada de lo que esta sesión afirma sobre Nuvei viene de él.
- **El 14 % fuera del monorepo** (vistas y código del portal admin, D-759) **no se midió** — está fuera del territorio de esta pista.
- Nada tocó ledger, devengo, comisión ni liquidación (letra §9).

---

# TANDA 2 · MIGRACIÓN 2 — ENTREGADA, **NO APLICADA**

> Firma de mesa 19-ago: enmendar `pagos_intentos`, no crear `pagos`.
> **Entregada lista con su comando; la corre el founder.** Ensayo en seco hecho
> dentro de `BEGIN … ROLLBACK`: **corre entera, residuo 0 medido.**

Archivos: `supabase/migrations/20260821010000_s101_motor_pagos.sql` ·
reversa `docs/relevamientos/2026-08-19-s101-REVERSA-motor-pagos.sql` ·
registro `docs/relevamientos/2026-08-19-s101-REGISTRAR-motor-pagos.sql`.

## ① `pagos_eventos` vs `webhook_events` → **HERMANOS LEGÍTIMOS. Migración 1 se queda.**

Lo que lo decide no es el parecido de columnas: **ninguna se deriva de la otra**
(de `pagos_eventos` no se reconstruye lo ilegible/rechazado/duplicado; de
`webhook_events` no se reconstruye el `intento_id` ni el candado de
idempotencia). Y la prueba más limpia es la **cardinalidad**: un golpe HTTP de
una compra de 3 pedidos deja **1 fila allá y 3 acá**. Medido además que
`pagos_eventos` tiene **un solo escritor y un solo lector**, los dos
`confirmar_pago_pedido`. **No se propone reversa de la migración 1.**

## 🔴 ② EL UNIQUE PEDIDO ES INCONSTRUIBLE — con rojo producido

`proveedor_referencia` guarda el `dev_reference`, que **es la compra**;
`pagos_intentos` es **por pedido** ⇒ N pedidos → N filas con la misma
(proveedor, referencia). Probado sobre la compra real `fc8e2a85`, que **ya
tiene 2 pedidos**, en BEGIN/ROLLBACK con residuo 0:

```
1 · UNIQUE(proveedor, proveedor_referencia) → creado sin conflicto
2 · intento del pedido 1                    → OK
3 · intento del pedido 2                    → 🔴 duplicate key value violates unique constraint
```

⇒ Se entrega el candado que cumple **la intención** de la letra §3 sin romper ⑤:
`UNIQUE (proveedor, proveedor_transaction_id, pedido_id)` parcial.
`proveedor_referencia` conserva su índice **no** único, con comentario que dice
que es un puntero y no una llave.

## ③ ④ Columnas y puente
`compra_id` (FK RESTRICT, **sin backfill**: medido 0 de 14) · `confirmado_por`
(CHECK webhook|consulta_activa — sin esto los 4 casos de §6 no son auditables) ·
`proveedor_transaction_id` · `authorization_code` · `marca` · `bin` · `ultimos4`
(TEXTO con CHECK de 4 dígitos: `'0042'` no es `42`).

## 🔴 ⑤ El defecto que la orquestación evita, y que fallaba EN SILENCIO
`confirmar_pago_pedido` corta temprano con `duplicado:true` si ya vio la clave.
Llamarla N veces con la clave del webhook confirmaría **solo el primer pedido**
y devolvería `ok:true` en los demás. **Compra a medias reportando éxito.**
`confirmar_pago_compra` deriva la clave por pedido (`<clave>:<pedido_id>`), y
cualquier rebote tumba la transacción entera. Revocada de anon/authenticated,
solo `service_role`, verificado por el cinturón (L-216).

⚠️ **`p_monto` no estaba entre los seis ítems.** Se incluye porque la letra §5.4
lo exige; es **opcional por construcción** (`NULL` ⇒ no valida). Sacarlo es
borrar un bloque.

## ⑥ `crear_intento_pago` → **LÁPIDA, no renombre**
Costo medido: llamador **vivo en el bundle publicado** del cliente
(`checkout.tsx:457` vía `despensa-pedido.ts:751`). Renombrar rompe el checkout
que corre hoy hasta que aterrice un OTA. Llamadores dentro de la base: **cero**.
⇒ El nombre queda y el `COMMENT` dice que miente y por qué.

## 🔴 HALLAZGO QUE CAMBIA UNA PREMISA DE LA FIRMA: LOS CLAVADOS SE VENCEN SOLOS

**Ya son CINCO, no seis.** `09a2f00b` (70,90) pasó a `cancelado_sistema`
**durante esta sesión**. Causa medida: **cron job 12, `7 * * * *`, ACTIVO,
corriendo `expirar_pedidos_sin_pago()`**.

⇒ «Los seis clavados son semilla confirmada, no tocarlos» tiene un supuesto que
la base no sostiene: **el conjunto decae hora a hora sin que nadie lo toque.**
Si el corte semilla/real tarda, puede no quedar ninguno.

**Y me costó un cinturón:** el mío afirmaba «tienen que ser 6» y **abortó el
ensayo en seco** por algo que no tenía nada que ver con la migración. Corregido
a medir **el delta contra su propia foto de entrada** — que es lo que de verdad
quiere decir «esta migración no movió ninguno». *Un cinturón que afirma un
número vivo se rompe solo.*

---

# TANDA 3 · LOS DOS DEPÓSITOS + EL CIERRE DE LA MIGRACIÓN 2

## ① `LETRA_SALDO.md` DEPOSITADA — con el freno corrido primero

Verificado antes de depositar, como se ordenó:
- **Nombre libre**: `docs/LETRA_SALDO.md` no existía; ningún archivo de `docs/` lo reclama.
- **Sin choque de territorio**: `MODELO_FINANCIERO` §7.16 **no ocupa el terreno — lo
  RESERVA para esta letra** (*«su letra propia nace antes del primer crédito real»*, con
  el contrato contable explícitamente sin diseñar). La letra nueva es el ocupante
  esperado, no un encimado.

⇒ **Depositada VERBATIM.** No le agregué nota de depósito: la orden decía verbatim.

### 🔴 PERO HAY UNA COLISIÓN DE VOCABULARIO, MEDIDA — y no es de territorio

`LETRA_SALDO` §1 dice que el saldo *«se muestra en la app como lo que es: **tu saldo**»*.
**Esa palabra ya está ocupada en la app del cliente, hoy, para el MISMO usuario:**

```
apps/cliente/src/i18n/es.ts:1111
  saldoActivo: 'Tu paquete está activo — elige día y hora, y al elegir paseador
                usas tu saldo.'
  teQuedan:    'Te quedan {{n}} salidas'
```

Ahí «saldo» son **salidas restantes de un paquete**, no plata. Y hay un tercer sentido
vivo: `MODELO_DESPENSA` usa «saldo» para el **balance del vendedor en el ledger**, y
`MODELO_NOTIFICACIONES` tiene el tipo `saldo_pagado` (renovación de plan).

**Cuatro sentidos de la misma palabra, dos de ellos visibles al mismo usuario en la misma
app.** No bloquea el depósito —ninguna letra viva contradice a la nueva— pero es decisión
de producto y va al founder. *La plata y las salidas restantes no pueden llamarse igual
en la misma pantalla.*

## ② `ENMIENDA_S101_MOTOR_v1.1.md` — depositada y APLICADA

E1-E7 incorporadas al cuerpo de `LETRA_MOTOR_PAGOS_S101.md` → **v1.1**, con `Historial`
nuevo (la letra no tenía). **La letra vieja va tachada, no borrada.** El archivo de
enmienda **se conserva** y ganó una *Nota de aplicación* con las dos precisiones que la
enmienda no podía saber: el ítem ① de E1 **se resolvió a favor de conservar
`webhook_events`**, y el «UNIQUE primero» **quedó descartado por rojo producido** y
reemplazado por firma.

Lo que cambia mi cola: **entra el paso 3bis** (compuertas pre-cobro) después de la
migración 2 · **E2 corrige el mapa antes de cablearlo** (`esperando_otp` es del alta) ·
**E4 fija la cadencia mismo-día** con 22:00 Guayaquil como supuesto declarado ·
**E6 prohíbe construir refund por API** hasta la respuesta de Erick.

## ③ MIGRACIÓN 2 CERRADA CON EL CANDADO FIRMADO

Firma ① (19-ago, cierre): `UNIQUE (proveedor, proveedor_transaction_id, pedido_id)`
parcial. **Ya era el que estaba en la migración — no se tocó una línea del candado.**

Firma ② (camino (a)): **me obligó a sacar mi propio cinturón de los clavados.** Medía
el delta contra su foto de entrada; pero si el cron dispara DURANTE la migración —que por
firma ② es comportamiento esperado y correcto— **abortaría una migración buena por algo
que no controla.** Y esta migración es DDL más una definición de función: **cero DML
sobre `pedidos`**, así que no puede moverlos. ⇒ Se saca la medición y se declara por qué.

*Es la segunda vez en la sesión que este cinturón se calibra mal: primero afirmando un
número fijo (abortó el ensayo), después midiendo un delta que un cron ajeno puede mover.
**Un cinturón que puede abortar por algo que la migración no controla no protege:
interrumpe.***

Re-validada en seco tras el cambio: **corre entera, residuo 0**, candado firmado
verificado en el índice creado, `anon`/`authenticated` en `false`.

---

# TANDA 4 · CREDENCIALES EPETPLACESTG — redeploy, ensayo, y una cura propia

## El ensayo sintético NO discrimina el cambio de credenciales, y hay que decirlo

Redesplegada y corrido el ensayo: la fila salió **byte-idéntica** a la del turno anterior
(`stoken_invalido` · `stoken no coincide`). **No es un verde: es un no-resultado.**
Un `stoken` inventado falla contra CUALQUIER clave ⇒ el ensayo distingue
«hay credenciales / no hay», pero **no distingue CUÁLES**.

**El discriminador que sí sirve** (y no toca ningún valor secreto): los `updated_at` de
los secretos, que el CLI expone junto al digest.

| secreto | actualizado |
|---|---|
| `NUVEI_APP_CODE_SERVER` | **2026-08-19 20:06:38** |
| `NUVEI_APP_KEY_SERVER`  | **2026-08-19 20:07:04** |

Posteriores al ensayo previo (16:57:49) y anteriores al redeploy y al ensayo de ahora
(20:08:32) ⇒ **el cambio ocurrió y la función que contestó corre sobre los nuevos.**

## 🔴 CURA DE UN DEFECTO PROPIO — la procedencia del stoken no se registraba

El comentario del código prometía que *«el campo `detalle` va a decir cuál acertó»* y
**la primera versión no lo guardaba.** Con el callback ya registrado, **el evento real de
Nuvei es una observación de UNA SOLA VEZ**: si llegaba y anotábamos solo `stoken no
coincide`, un `false` **no distinguiría «la fórmula está mal» de «lo leí del lugar
equivocado»**, y habría que esperar al siguiente evento para desempatar.

Curado: `extraer()` registra cuál de los tres lugares trajo el `stoken`, y va a `detalle`
como `stoken_de=…`. **Probado con dos ensayos que discriminan de verdad:**

```
TEST-1 (stoken en transaction) → stoken_de=transaction.stoken
TEST-2 (stoken en card)        → stoken_de=card.stoken
```

*Dos payloads distintos, dos procedencias distintas: eso sí es un discriminador.*

## Qué esperar del primer evento real

`detalle` va a traer `stoken_de=<lugar>` y `stoken_valido=true|false`:

- **`true`** ⇒ fórmula y lugar confirmados. Se fija el lugar, se borran los otros dos
  candidatos y el buzón pasa a poder ser actuador.
- **`false` con `stoken_de` poblado** ⇒ el lugar es ése; **la fórmula es lo que falla**
  (orden de los campos, o `user_id` no es el que creemos).
- **`stoken_de=ninguno de los tres`** ⇒ viene en un cuarto lugar; se lee el `payload`
  crudo de la fila, que está guardado entero.

**Nada de esto exige otro evento de Nuvei: la fila sola alcanza para desempatar.**
