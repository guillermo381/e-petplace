# S112-C · PARTE DE LA PISTA C — `apps/*`

> **ESTE ARCHIVO ES LA FUENTE DEL HANDOFF.** Quien retome abre acá y **no
> reconstruye de memoria**. Todo lo que sigue está medido contra el objeto;
> donde algo no existe, se dice con su control.
>
> **Rama:** `pista/s112-c` · **HEAD `e4bcf629`** (en origin, verificado por SHA:
> `git ls-remote` = `git rev-parse HEAD`) · árbol limpio.
> **Gates en HEAD:** `tsc` cliente · prestador · ui = **0 · 0 · 0** ·
> `verify:diseno` **VERDE 62** · `verify:razon-muda` **VERDE**.

---

## §0 · 🔴 LO PRIMERO — LOS DOS ESTADOS QUE NO SE PUEDEN OLVIDAR

**① `ADOPCION_ALCANZABLE = false`** (`apps/cliente/src/lib/gate-adopcion.ts`),
con **tres** lectores: Explorar · el hogar sin mascotas · el onboarding.
**Se enciende —y la constante se borra con sus tres lectores— el día que abra
el lote de adopción.** *Un interruptor sin fecha de retiro es cómo una función
terminada queda apagada seis meses.*

**② EL PORTAL DEL PRESTADOR ESTÁ REVERTIDO, NO PERDIDO.** `63054b7c` revierte
`2226081c`. **Vuelve con `git revert 63054b7c`, una línea.** Se sacó por orden
de mesa: viajaba en el binario del lote de guardería.

⚠️ **Al revertirlo vuelve `verify:razon-muda` a 7, y NO es una razón muda:** es
un **falso positivo**. El gate cuenta `razonDeshabilitado` sin `onRazon` porque
su cabecera dice que así el botón «queda sin hint» — **y esa premisa dejó de ser
cierta con la cura de B: hoy la razón SE DIBUJA sola.** ⇒ **jamás curarlo con un
`onRazon` vacío** (error que S111 se negó a cometer) y **jamás subir el baseline
en el mismo commit que lo reintroduce** (lo prohíbe el propio gate). **A tiene la
elección pendiente** — ficha de B en `S112-B-para-A-gate-razon-muda.md`.

---

## §1 · LA COLA VIVA — en orden, con el próximo paso ejecutable

**ORDEN FIRMADO:** aceptaciones ✅ → **ficha** → formulario → portal → firma.

| # | qué | bloqueante | próximo paso EJECUTABLE |
|---|---|---|---|
| 1 | ~~pantallas de aceptación~~ | — | ✅ **HECHO** (`1dad1bcc`) |
| 2 | **LA FICHA DEL ADOPTABLE** | `obtener_adoptable` de A — **medido 0 en `main 5d8ea11d`** | al llegar: montar `TarjetaAdoptable` en `adoptar.tsx` (`onPress` → ficha) + crear `app/adoptar/[publicacionId].tsx`, y **pedir a B `Convivencia` con dato real** |
| 3 | formulario de postulación | `crear_solicitud_adopcion` con `p_respuestas` + `p_composicion_hogar` — **sigue en 2 parámetros** | rangos `0-5 · 6-12 · 13-17`, **jamás nombres ni edades de menores** (P5) |
| 4 | portal del prestador | **el lote de guardería YA salió** ⇒ desbloqueado; falta `obtener_mi_cuenta_refugio` para su tab | `git revert 63054b7c` + resolver §0② con A |
| 5 | pantalla de firma | `obtener_acta_adopcion` · `firmar_acta_adopcion` — **0 los dos** | ver §4, el hueco del `actaVersion` |

---

## §2 · LO MONTADO, CADA PIEZA CON SU PUERTA (*entregada ≠ montada*)

| pieza | puerta | estado |
|---|---|---|
| **`D-1000`** — muere el espejo del tramo vivo | el durante de guardería | 🟢 alcanzable |
| **`SinQuienReservar`** — el guard de dos hechos, 5 raíces de oficio | las 5 raíces | 🟢 alcanzable |
| **Hogar sin mascotas recompuesto** (dos cartas + «i» N22) | el hogar | 🟢 (la carta de adopción, ⏸️ por gate) |
| **`D-1001`** — rebote con nombre **+ puerta apagada ANTES del botón** | `explorar/guarderia/[prestadorId]` + checkout | 🟢 alcanzable |
| **`app/legales/[codigo].tsx`** — lectura y aceptación | el rebote `condiciones_no_aceptadas` | 🟢 alcanzable |
| **`BifurcacionDeEntrada`** | `ADOPCION_ALCANZABLE` | ⏸️ apagada |
| **Entrada de adopción en Explorar** | `ADOPCION_ALCANZABLE` | ⏸️ apagada |
| **Portal del publicador** (Home + hilo) | revertido — §0② | ⏸️ fuera del binario |

**`app/legales/[codigo].tsx` tiene UN consumidor hoy.** El segundo
(`terminos_refugio`, primer ingreso del refugio) **vive en la app de negocios y
llega con el punto 4**. Cuando llegue: **se comparte el COMPONENTE** (pedido de
promoción a B), **jamás se copia la ruta.**

---

## §3 · LO QUE YA ESTÁ MEDIDO — **no se vuelve a medir, se lee acá**

| pregunta | veredicto | dónde |
|---|---|---|
| ¿el guard tras el alta manda a onboarding? | 🟢 **SÍ, los tres callers** | `registro:111` · `verificar-correo:111` · `index:64` |
| ¿recuperar deja sesión viva y aterriza bien? | 🟢 **SÍ** — `replace('/')` con sesión de `verifyOtp` | `recuperar.tsx` |
| ¿hace falta deep link de recuperación? | 🔴 **NO** — el correo manda **código**, no link | confirmado por A |
| §12 ⑥ ¿tabs por tipo de cuenta sin bifurcar? | 🟢 **SÍ, con precedente vivo** | `QuienEntra` es unión discriminada; el vendedor puro entra **sin `prestador_id`** |
| ¿los otros `.length === 0` tienen el guard de dos hechos? | `hogar/index` **NO** (los `return` de carga/error van antes) · `lamina-fusion` **tiene la FORMA** (error y sin-familia → mismo `[]`) **pero no la consecuencia**: es lámina de gate | — |
| ¿existe el formulario de postulación? | 🔴 **NO** — postular es un toque | E lo midió igual |
| ¿la cédula del adoptante? | 🔴 **NO EXISTE.** Hay `identificacion_fiscal` — **es otra cosa**: del vendedor y la factura. **Reusarla pondría un RUC donde el acta dice cédula.** Firma: se pide **en el acto de firmar** | medido por A |

---

## §4 · 🔴 HALLAZGOS VIVOS QUE NO ESTÁN EN NINGÚN OTRO LADO

1. **EL RELOJ DE LOS 5 DÍAS ES MOTOR SIN PUERTA.**
   `obtener_solicitudes_en_silencio` existe y tiene **CERO consumidores** — 0
   cron, 0 edge, 0 wrapper; sus únicos llamadores son los asserts de su propia
   migración. ⇒ **§5 firma que e-PetPlace avisa si el refugio no responde, y hoy
   nadie avisa.** **Por eso NO se escribió esa promesa en la pantalla**, aunque
   la directiva la dicta literal: sería `L-472` sobre la frase con la que el
   vertical se diferencia de Instagram. **Entra cuando exista el DESPACHADOR.**
2. **`traspasarMascotaAFamilia` EXIGE `actaVersion` Y NADIE LA PROVEE.**
   Es `number`, no opcional — y `actaCodigo?` **sí es opcional en la línea de al
   lado**, o sea que es **decisión, no descuido** (discriminador de D). Sin
   lector, la pantalla tendría que **hardcodear la versión** y el día de la v2
   seguiría firmando la v1 **compilando**. **Pedido: que `obtener_acta_adopcion`
   devuelva la versión con el cuerpo y la pantalla la mande de vuelta tal cual
   — viaje redondo, la app nunca elige.**
3. **`guarderia.sinElegiblesTitulo/Detalle` existen y NADIE las monta** (0
   consumidores). Voz sin pantalla. **No se curó**: guardería llega con
   `mascotaId` por parámetro, su caso cero se resuelve antes.
4. **`mascota_requerida` NO se ramifica en el checkout, a propósito:** el
   checkout siempre manda `mascotaId` ⇒ el código **no es alcanzable** desde
   ahí. *Un brazo por un código que no puede llegar es cobertura aparente.*

---

## §5 · COMPROMISOS — qué debo · qué me deben

**ME DEBE A** (buzón: `PEDIDO-1` · `-2` · `-3` · `-4`): `obtener_adoptable`
(**lo próximo**) · el formulario con `p_composicion_hogar` · el acta con
`faltantes` **y su `quien`** (*«falta tu cédula» y «falta que el refugio cargue
el microchip» no se le dicen a la misma persona*) · `obtener_mi_cuenta_refugio`
· el lector de «mis animales» (tab Mascotas — hoy hay **escritores sin lector**,
`D-980` del otro lado) · el **despachador** del reloj · la decisión de §0②.

**ME DEBE B:** `Convivencia` **espera mi pedido con dato real** (no lo manda sin
él, y tiene razón) · `BloqueConCriterio` **no se monta hasta que exista
`destacado_espera`** — §4 **prohíbe explícito** ordenar por antigüedad ·
`FiltroPills` modo varias, ya entregado.

**LE DEBO A B:** el pedido de `Convivencia` y del bloque **con dato real**,
apenas llegue la ficha. Formato que le sirvió: **campos firmes / campos
pedidos / qué puede faltar / dónde vive / qué pasa al tocar.**

**DE D:** el contrato del hilo (`S112-D-para-C-CONTRATO-DEL-HILO.md`) rige.
**Su cura `b71d7be3`**: el id de `solicitud_ya_viva` llega en **`detalle`** ⇒
cuando A la mergee, el rebote de `adoptar.tsx` pasa de llevar **a la lista** a
llevar **al hilo exacto** — es cambiar el destino, una línea.

**ARBITRADO POR EL FOUNDER:** el gate de `Convivencia` corre **en el lote de
adopción, sobre la ficha con datos reales** — no en el de guardería.

---

## §5bis · PUNTEROS EXTERNOS — **lo que vive en OTRAS ramas y voy a necesitar**

*Medido: estos tres no estaban en ninguna parte de este repo salvo en la
conversación, y una conversación no sobrevive a una compactación.*

| qué | dónde | para qué |
|---|---|---|
| **Contrato de B** — props de `TarjetaAdoptable`, `Convivencia`, `BloqueConCriterio`, `FiltroPills` modo varias | `docs/loop/buzon/S112-B-para-C-CONTRATOS.md`, rama **`pista/s112-b`** | montar la ficha sin adivinar props |
| **Código de B** termina en **`fdedee75`** (lo posterior es documentación) | `pista/s112-b`, en origin | **re-mergear** cuando toque la ficha — su `Boton.tsx` ya está curado (el `TS2322` de `55f51ad6` murió en `ba730968`) |
| **Censo del formulario de E** | `docs/loop/buzon/S112-E-para-C-y-FOUNDER-CENSO-FORMULARIO.md`, rama **`pista/s112-e`** | midió que el formulario **no existe** y que **ningún dato de menores se pide hoy** — la forma correcta ya es la que está, y el formulario no debe romperla |

**Contrato de D:** `S112-D-para-C-CONTRATO-DEL-HILO.md` en `pista/s112-d`
(su §4 quedó **corregido dos veces**: el acta **sí** está cargada; ver §4② de
este parte para el hueco que queda).

⚠️ **Dato de B, medido contra mi rama:** `D-999` toca **12** puertas, no 13 —
la 13ª era la del portal que mi revert se llevó. **Las 6 de guardería no se
movieron.**

## §6 · FRENOS Y SUPUESTOS VIVOS

- **NO montar adjuntos en el hilo.** Medido por D en tres lugares: 0 columna, 0
  parámetro, 0 bucket privado. ⚠️ **`adopcion-fotos` existe y es PÚBLICO** — es
  la vidriera. *Colgar ahí adjuntos de una conversación privada los deja a la
  vista de cualquiera.*
- **NO prometer la respuesta automática al postular:** nadie la inserta. El
  estado `recibida` es **esperando**, y así se diseñó.
- **NO dibujar «Aceptar» del lado de la familia:** sólo el publicador acepta;
  declinar pueden los dos.
- **NO defender la privacidad en el cliente:** está en la RLS y D la probó con
  un tercero dando 0 en las cuatro puertas.
- **`acta_adopcion` NO se acepta** por `aceptarDocumentoAdopcion` (rebota
  `documento_no_aceptable`): aceptar y firmar son actos distintos.

---

## §7 · OPERATIVO — trampas que ya se pagaron

🔴 **`\.expo/types/router.d.ts` está GITIGNORED y sin él `tsc` NO MIDE RUTAS —
sale VERDE sin mirar una sola.** Se **GENERA, no se copia** (copiarlo trae las
rutas de otra rama): `cd apps/<app> && npx expo start`, ~3 s, y matar. **Con él
puesto cazó 4 errores reales** de contrato de piezas. **Se regenera al agregar
una ruta nueva**, antes de diagnosticar cualquier rojo.

⚠️ **Contar con `wc -l` la salida de `tsc` NO es contar errores** — un `TS2322`
imprime encabezado **más** detalle. *Un número que sale de contar líneas no es
una medición.*

⚠️ **Un censo ancho también da falsos positivos:** `grep` de «documento» matcheó
`adopcion.ts` por un **comentario** y parecía que A había entregado. **Se cierra
mirando las FIRMAS, no el conteo.** Y su gemela (`L-437`, de D): un censo por
**dos nombres exactos** acota pero no cierra — buscar **cualquier** función con
la raíz.

**Cuatro instrumentos mintieron en la sesión, todos POR EL ENTORNO desde donde
se midió**, no por el hecho: mi `router.d.ts` ausente · mi conteo de líneas · el
`node_modules` faltante de B · la Management API en vez de PostgREST de A.
⇒ **Ante un «no se puede» sobre el servidor: preguntar desde dónde se midió.**

**Números:** deuda libre **`D-1005`** (máximo real `D-1004`; ⚠️ `sort|tail` con
`\{3\}` subcuenta — usar `D-[0-9]\{4\}`).

---

## §8 · LO QUE NO ESTÁ VERIFICADO, Y SE DICE

**NINGUNA PASADA DE APARATO EN TODA LA SESIÓN.** Lo verificado es typecheck,
`verify:diseno`, `verify:razon-muda` y medición contra el objeto.
*Que el motor conteste no prueba que la pantalla pinte.*

**El lote de guardería SÍ salió** (cliente `6aedf349` · prestador `51a263b2`,
ancla `fde8494d`, `dirty=None`, runtime 1.0.7, leídas del OBJETO) — pero **su
gate en aparato no lo corrí yo**. De lo mío, lo que el founder ve por primera
vez ahí: **la voz del hogar vacío · el guard de los oficios · la razón del plan
de guardería.**
