# S112-C · PARTE DE LA PISTA C (`apps/*`)

> **Se lee ANTES de tocar nada.** Handoff: SHAs, lo curado, lo medido para no
> re-medirlo, y la cola con su bloqueante NOMBRADO. Lo que no está acá no existe.

---

## §0 · LOS SHAs DECLARADOS

| lote | hasta | qué entra |
|---|---|---|
| **guardería 2** | **`98f812e5`** | G1 · G2 · G3 · G7 (la mitad de la cara). **Nada de adopción.** |
| **adopción 2** | **`a2147fae`** | **A2** |
| **lote 3 · las capturas del founder** | **`ba17bf06`** | A3 · A4 · A5 · A7 · A8 · A9 · G4 · G7 (el resto) · G8 · la ventana de la foto. ⚠️ **Sale MEZCLADO a propósito**: las capturas llegaron mezcladas y separarlo habría partido curas que comparten archivo. Si A necesita el corte por frente, se parte a pedido. |

**Lote 1 (ya en `main`):** los nueve puntos del vertical de adopción + `ADOPCION_ALCANZABLE` encendida por A.

---

## §1 · LA COLA VIVA

### Cerrado en el lote 3

| # | qué | nota que vale guardar |
|---|---|---|
| **A3** | la solicitud del refugio desbordaba | **`EstadoSolicitudAdopcion` no es una pill: es una ESCALERA** (su propio encabezado dice «reusa `EscaleraEstados`»). La monté como tercer hijo de un `flex row` y quedó pegada al borde con el texto cortado. **Ningún gate podía verlo**: un desborde es una medida que sólo existe cuando algo se pinta. Ahora es columna. |
| **A4** | con foto la foto, sin foto la cara de la casa | El bucket `especies-razas` tiene **111 objetos, es público, y `caraDeMascota` ya lo resolvía — con CERO llamadores** en estas superficies (`L-318`). Curadas: Peluditos · lista del cliente · el hilo del refugio (ahí **ni la foto real se firmaba**). |
| **A5** | «Mis animales» → **«Peluditos»** | la razón es de forma: la etiqueta larga desbordaba el botón de la tab. |
| **A7** | la Cuenta del refugio decía «No pudimos cargar tu negocio» | **`L-178` un actor más tarde.** El refugio no tiene fila en `prestadores` ⇒ `sin_prestador` ⇒ la rama lo mandaba al fallo. Misma forma exacta que el vendedor puro de S99-C. |
| **A8** | se retira la entrada a la galería | firma del founder (`L-478`). **Con ella se jubila `R18`** — la regla manda su propio retiro «en el mismo commit de la firma». Ver §6. |
| **A9** | «En conversación» listaba todo | ver §5, que es donde está lo que vale. |
| **G4** | el día de guardería en el «en vivo» del hogar | **UN viaje, no N**: `obtenerMisEstadiasGuarderia()` sin argumentos trae todas las mascotas y ya viene con `estadoEstadia`. `obtenerTramoVivoDeMiMascota` es POR MASCOTA y habría costado una petición por animal (`L-223`). |
| **G7** | el plan dice para quién sirve | `=== false` y **no** `!aplica`: `null` es «no hay pregunta que contestar» y la negación lógica lo aplasta contra «no puede». |
| **G8** | cancelar no se llamaba cancelar | vivía como `Interruptor` con **el título del plan como etiqueta** ⇒ la palabra «cancelar» no aparecía en la pantalla. *No falla el descubrimiento: falla el nombre.* |
| **foto** | el recorte a «adentro» se queda | **firmado por el founder.** Ahora el botón **no desaparece: se apaga con su razón.** Ver §1bis. |

### Abierto, con su bloqueante NOMBRADO

| # | qué | bloqueante |
|---|---|---|
| ~~**A6**~~ | la vitrina del refugio | ✅ **CERRADA CONTRA EL LITERAL ENTERO.** «Mi vitrina» (espejo + editor) · la vitrina del lado familia por cuenta comercial · **la puerta** (`publicador.onPress`) · **el buscador**. |
| ~~**A1**~~ | filtros en una sola hoja | ✅ **CERRADA.** Mueren tres `FiltroPills` con doce chips, el tipo `Marca`, el helper `alternar` y —lo que importa— **el bloque que traducía**. |
| ~~**A4** (mitad)~~ | ~~la cara en las superficies del publicador~~ | ✅ **CERRADA.** Llegó `mascota_especie` (A, `20260908520000`). *El defecto entero, para el registro: el lector de la familia traía la especie y el del publicador no, así que sobre el MISMO animal la familia veía la cara de la casa y el refugio la huella — **no era una superficie sin curar: eran dos superficies del mismo hecho contando cosas distintas**.* |
| ~~**A4** (forma)~~ | ~~migrar a `fotoDeEspecie`~~ | ✅ **MIGRADA** (4 sitios). *La distinción que yo no había visto: la foto propia lleva **encuadre de retrato** —zoom + corrimiento, porque en una foto de mascota la cara queda alta— y la ilustración de la casa **va a sangre**, porque ya viene encuadrada.* `resolverUrlGenericaEspecie` y no `resolverUrlRaza`: mis lectores traen el NOMBRE de la raza y **para un adoptable el caso normal es sin raza declarada** — caer al genérico no es un fallback pobre, es el caso. |
| ~~A1 (fila vieja)~~ | — | ✅ ver arriba. Contexto: `HojaFiltros` (medido). Se montará apenas entre. Pedida con su contrato completo (buzón + mensaje). La lista sale de la lista blanca del motor, no de mi cabeza: nueve grupos de los trece que `obtener_adoptables` acepta. **Las dos que quedan afuera, con su razón:** `edad_min/max_meses` **el motor las acepta y yo no las puedo redactar** —traducir «cachorro/adulto/mayor» a meses **depende de la especie**, los umbrales viven en `cat_especies_perfil` y ningún lector de adopción los trae—; y `country_code`, que no es una elección de la familia. **Tampoco pedí «cerca de mí»**: no hay filtro de distancia y fabricarlo con `ciudad_id` sería llamar «cerca» a otra cosa. |
| **G5** | «Quiénes están hoy» (Hoy del prestador) | 🟡 la pieza de fila es de B y **no se la pedí todavía a propósito**: el founder no la volvió a listar, y pedir una pieza con un contrato que yo estoy adivinando es cómo nacen las piezas que nadie monta. **El lector NO falta:** `obtener_estadias_del_dia` ya trae `estado` por animal + `llegada_en` · `retorno_en` · `entregada_en` · `no_recogida_en`. |
| **G7** (resto) | — | ✅ **cerrado**: llegó `especieAplica`. |

## §1bis · 🔴 UNA DECISIÓN QUE NO ES CURA Y NO SE TOCA SIN MESA

**EL BOTÓN DE LA FOTO VIVE EN UNA VENTANA QUE SE CIERRA SOLA.** Dos hechos que
midió A y que cambian de quién es el recorte:

1. **`publicar_media_guarderia` NO exige `en_guarderia`** — publica sobre una
   estadía `reservada`. ⇒ **el recorte a «sólo con animales adentro» es de MI
   PANTALLA, no del motor.**
2. **Las estadías del founder estuvieron `en_guarderia` 68 SEGUNDOS.**

⇒ *Mi puerta se abre y se cierra sola en poco más de un minuto, y **nada lo
dice**.* Desde el asiento del cuidador eso es indistinguible de que la función
no exista — es la misma ambigüedad que ya declaré, ahora con su número.

**NO SE ENSANCHA POR MI CUENTA:** el recorte existe por una razón escrita —*§5:
las fotos de estadía se toman en las instalaciones; ofrecerlo durante un viaje
invitaría a sacarlas en la calle o en la puerta de una casa*—. **Ensancharlo yo
sería reabrir una decisión de producto disfrazada de cura.** Va a la mesa con
sus dos números.

### ✅ FIRMADO (founder, 2-sep-2026)

> *«Se queda el recorte a "adentro", con la razón debajo del botón. No
> ensanchar.»*

**Ejecutado.** El botón **dejó de desaparecer**: se dibuja apagado con su razón
—una para el viaje abierto, otra para «todavía no hay nadie adentro»—.

🔑 **Lo que la firma resuelve, y conviene leerlo bien:** el reporte del founder
(*«sigue sin funcionar bien»*) tenía **dos mitades y sólo una era un defecto de
código**. La otra era ésta: *desaparecer y estar apagado cuestan lo mismo de
construir y no cuestan lo mismo de entender* — uno deja al cuidador buscando
algo que vio hace un minuto, el otro le dice cuándo vuelve. **La ventana no se
movió un segundo; lo que cambió es que ahora se explica.**

---

## §2 · LO CURADO, con su capa

**G1 · la capa era LA PANTALLA, no el estado.** `EvidenciaFoto.Capturar` **ya
captura** (pide permiso, redimensiona a 1600) y entrega el uri en `onFoto(uri)`.
Las dos hojas de guardería **tiraban ese argumento** y volvían a capturar ⇒ **dos
cámaras por toque**, la 1ª foto perdida y la 2ª guardada.
**Rojo por contraste:** de 11 montajes de la pieza, **9 reciben el uri y 2 lo
tiraban — las dos hojas de guardería**. *El founder reportó una y eran dos.*
Con la cura mueren `sacarFoto` y el aviso de permiso propio de cada hoja: **la
pieza dibuja su camino** («probar de nuevo»), y dos superficies para el mismo
permiso es cómo una envejece.

**G2 · la capa era EL PERMISO, y el que cortaba era el MICRÓFONO.** Era
bloqueante; con el permiso negado dos veces Android devuelve `granted:false`
**sin diálogo** (`canAskAgain:false`) ⇒ **el botón no hace nada**.
*La decisión vieja se apoyaba en algo que su autor declaró no haber medido.*
Hoy: **cámara denegada corta** con voz que distingue los dos noes (si se puede
volver a preguntar vs. Ajustes); **micrófono denegado NO frena** — se graba mudo
y se dice. **`mute` es prop de `CameraView`, no de `recordAsync`**: lo escribí en
el grabador y el typecheck me mandó a leer el tipo instalado.

**G3 / A2 · la capa era la RUTA vs la URL.** Un path y una URL son los dos
`string`, **así que ningún typecheck lo ve**: no falla, dibuja un hueco.

---

## §3 · LO MEDIDO — no re-medirlo

- **CENSO DE IMÁGENES (2-sep):** **51** pantallas dibujan imagen · **25 firman**
  con `resolverUrlsFotos` (`D-308`) · **3 pintaban crudo**
  (`cliente/guarderia/[estadiaId]` ✅ curada · `despensa-piezas` **es PIEZA**, la
  firma es del llamador · `prestador/mostrador/autorizar` **sin revisar**) ·
  23 sin campo de foto de wrapper.
- **La LISTA de adopción trae la RUTA y la FICHA la URL** —
  `20260907900000:114` devuelve `m.foto_url` crudo. *Por eso la portada sólo se
  veía en el detalle.*
- **PEPE (G6) — el DATO está bien y MI CONCLUSIÓN estaba mal.**
  Medido y firme: es **`ave`**, tiene **22 estadías** (20 `reservada` ·
  1 `en_guarderia` · 1 `no_recogida`) y **ninguna suscripción con mascota**.
  ⏪ De ahí concluí *«el rojo de `D-1001` vive en la puerta de RESERVA»*.
  🔴 **FALSO, y lo falsó A por camino real:** `reservar_dia_guarderia` con Pepe
  contra Aurora **rebota `mascota_no_elegible` (400)**. **La puerta valida hoy.**
  ⇒ *Sus 22 estadías son HISTÓRICAS: nacieron antes de que existiera el guard.*
  **No hay un agujero abierto: hay datos viejos que la puerta de hoy ya no
  dejaría entrar.** Medí el EFECTO y afirmé sobre la CAUSA — la misma familia de
  `L-487`, cobrada por segunda vez en el día. **No se borró nada.**
  ⚠️ **Lo que SÍ sigue abierto es de A y es otro:** de las cinco puertas,
  `comprar_paquete_guarderia` y `cobrar_periodo_mensualidad_guarderia` **no
  consultan especie**, y la suscripción `20d025ca` de Pepe está **`activa`** ⇒
  el día que el reloj corra le crea estadías a un ave.
- ⚠️ **`en_guarderia` pasó de 0 a 3 mientras medía.** Mi reporte anterior era
  verdadero cuando se hizo. ⇒ **el botón «Sacar foto» ahora SÍ se dibuja.**
- **Los chips del durante NO dibujan avatar** — no es una firma que falte, es
  una pieza que no está.
- **G8:** no hay RPC genérica de cancelar suscripción; existe
  `cancelar_mensualidad_guarderia`.
- **G4:** `obtenerTramoVivoDeMiMascota` tiene **un solo consumidor**.

---

## §4 · TRAMPAS DE INSTRUMENTO, cobradas HOY

1. **El arnés de G1 medía `HEAD`, no el árbol** ⇒ dio el mismo rojo con la cura
   ya escrita en disco. *Midió el commit, no mi trabajo.*
2. **Después contó MI PROPIO COMENTARIO** — `L-170`, **tercera vez en el día**, y
   las tres dentro de la prosa que la cita. Cura: exigir el **montaje**, no la
   mención.
3. **El censo de imágenes dio CERO** por un pathspec con comodín entre comillas.
   *Lo falsó un control positivo — una pantalla que yo sabía que dibuja.*
4. **`pnpm -s <script inexistente>` devuelve 0.**
5. **Adiviné columnas SQL cuatro veces seguidas** (`fecha`, `desde`,
   `mascota_id`, `modalidad`) antes de medir `information_schema`. *Medir el
   esquema primero cuesta una consulta; adivinarlo costó cuatro.*

---

## §5 · LO NO VERIFICADO, SIN MAQUILLAR

**Cero pasadas de aparato en todo el lote 2.** Ninguna cura se ejerció: ni la
captura, ni el clip mudo, ni una foto firmada bajando de verdad. Todo se leyó del
objeto y de la base.
**Y el control positivo de los dos lectores de media exige un JWT real** — sé que
los datos están y que el guard cierra; **no sé** que devuelvan las filas al
usuario correcto. Es de E.

---

## §6 · 🔴 LOS TRES HALLAZGOS DEL LOTE 3 QUE NO SON FICHAS

### ① EL MOTOR TIENE SIETE ESTADOS Y EL TIPO DECLARA SEIS

Medido contra el CHECK vivo de `adopcion_solicitud`:

```
recibida · en_conversacion · aceptada · declinada · desistida ·
no_concretada_fallecimiento · no_concretada_otra_familia   ← el séptimo
```

`EstadoSolicitudAdopcion` en `packages/api` declara **seis**, y el wrapper pasa
el valor con **`as`, no con un parse** ⇒ **la fila llega tipada como algo que no
es y ningún typecheck lo ve.**

**Y no es teórico: casi me cuesta la pantalla.** Mi primer arreglo de A9
enumeraba los terminales **por nombre**, y eso cambiaba un rótulo mentiroso por
algo peor — **esa fila no entraba en ningún grupo y desaparecía**.

> ### Filtrar por complemento miente en voz alta; filtrar por una lista incompleta calla, y callar es el modo de falla que nadie encuentra.

**Cura, y es la que hay que copiar:** el discriminador pasó a ser **`cerradaEn`**,
que es el invariante que el motor ya publica en su segundo CHECK (`vivas ⇒
cerrada_en IS NULL`) y **es cierto para los siete y para el octavo**. Lo que no
encaja cae en «por revisar»: *una solicitud abierta es trabajo aunque no sepamos
cómo se llama.*

**Pedido a A:** la unión a siete, y el `as` a un parse que rebote
`datos_inconsistentes` sobre un valor que no conoce. **Pedido a B:** su
`EstadoSolicitud` también tiene seis, y **si el séptimo lleva escalera o no es
decisión suya** (mi lectura: sí — hubo proceso y se interrumpió, a diferencia
del fallecimiento, que es una noticia).

### ② UNA CADENA DE TERNARIOS NO TIENE «NINGUNO DE LOS ANTERIORES»

La voz de cada final la resolvía una cadena de ternarios cuyo **último** brazo
era el fallecimiento. ⇒ el séptimo estado habría salido diciéndole al refugio
**que el animal murió**.

> ### La forma equivocada no era larga: era que su rama final AFIRMABA en vez de admitir que no sabía.

Pasó a mapa con `??` y salida por defecto que **no inventa un motivo**.

### ③ R18 SE JUBILÓ, Y LA ORDEN ESTABA ESCRITA EN ELLA MISMA

Con la firma de A8 retiré la última entrada a `/gallery` y `R18` salió roja. Su
propio texto dice, palabra por palabra: *«El día que el founder firme el retiro
del prestador, ahí sí la regla se borra entera, **en el mismo commit de la
firma**»*.

**Toqué `scripts/verify-diseno.mjs`, que es de B, y lo declaro:** la regla delega
su retiro en quien traiga la firma y lo pide en el mismo commit; y `verify:diseno`
corre en el **pre-commit de las cuatro pistas**, así que dejarla roja las frenaba
a todas por un rojo que no habla de ningún defecto — *y el peor resultado no es
que frene, es que las cuatro aprendan a pasarle por encima*. Queda lápida
completa y **`R17` declarada viva**: se retiró el camino, no la sala. Pedido a B
en el buzón por si quiere revertir.

`verify:diseno` queda **VERDE con 61 reglas** (bajó exactamente 1).

---

## §7 · LO QUE ESTA TANDA **NO** VERIFICÓ

**Cero aparato, otra vez.** Nada del lote 3 se ejerció en un teléfono: ni el
desborde de A3 corregido, ni el botón de cancelar, ni la celda de guardería en el
hogar, ni el avatar por especie. Todo se leyó del objeto, de la base y de los
gates.

**Y una que importa más que las otras:** las tres superficies de A4 que curé
muestran hoy la ilustración **con el encuadre de retrato aplicado** (ver §1). El
typecheck no lo ve, `verify:diseno` no lo ve, y **sólo el ojo del founder o la
migración a `fotoDeEspecie` lo van a cerrar**.

---

## §8 · EL SÉPTIMO ESTADO, CERRADO — y me equivoqué en la lectura

**Mi lectura era estructural y estaba mal.** Yo dije que `no_concretada_otra_familia`
**sí** llevaba escalera porque *hubo proceso y se interrumpió*, igual que
`declinada`. B miró otra cosa: **qué AFIRMA el dibujo.**

> ### La escalera lleva a `aceptada`. Dibujarla interrumpida dice que alguien miró tu postulación y no llegaste.

En `declinada` eso **pasó de verdad**. Acá **nadie evaluó nada**: el refugio
aceptó a otro y un trigger cerró la solicitud. Pintar los pasos apagados **le
atribuye a quien lee un veredicto que nunca existió**, con la forma que en esa
pieza significa rechazo.

**El par que queda, y es lo que hay que recordar:**

| estado | por qué NO lleva escalera |
|---|---|
| `no_concretada_fallecimiento` | **no hay PROCESO** en el que estar: murió su sujeto |
| `no_concretada_otra_familia` | **no hay JUICIO** que mostrar: nadie evaluó nada |

*Dos caminos al mismo dibujo no es una coincidencia que se acomoda: es que el
dibujo dice «esto es una noticia», y las dos lo son.*

**⚠️ Las voces de este estado en mis tres call sites las escribió A**, no esta
pista, al desbloquearlas en `020d6c20`. Las revisé y **las dejo** (respetan `D-3`
y del lado familia dicen la buena noticia sobre el animal sin pedir perdón por
ella). **Se declara quién las escribió** porque la voz es de C y quien lea el
historial tiene que poder saber que hasta hoy no pasaron por su criterio.

### 🔴 Y una trampa de instrumento, cobrada acá mismo

Medí si `vozOtraFamilia` estaba en `main` con `git grep -c … | wc -l` — eso
**cuenta ARCHIVOS, no ocurrencias**, y casi concluyo al revés. *Lo cazó que el
número no cerraba con la otra medición* (`branch --contains` decía que el commit
no estaba y el grep decía que sí). **Ya estaba en `main`.** Es la quinta de §4 con
otra ropa: el instrumento contestó bien a una pregunta que no era la mía.

---

## §9 · A6 · LAS TRES DECISIONES QUE NO SE VEN EN LA PANTALLA

### ① El atajo que se descartó, y su razón está en el código

Cada adoptable trae `publicadorNombre` y `publicadorFoto`, así que la vitrina
**«se podía» armar con eso** sin esperar ningún lector. No se hizo:

> ### Es deducir la identidad de una casa a partir de uno de sus animales, y se rompe exactamente cuando el refugio no tiene ninguno publicado — que es cuando su vitrina más importa.

*La única excepción está acotada y escrita:* sin perfil, el **nombre** sale de
sus animales, para no dibujar un encabezado en blanco. **No se arma la vitrina
con eso.**

### ② Mi pedido a A estaba mal formulado, y lo declaro

Pedí *«falta una columna»* — **describí el síntoma, no la operación**. A me dio
la columna en el RESULTADO y yo necesitaba poder FILTRAR por ella, así que el
puente quedó en la dirección que nadie necesitaba: *se podía confirmar a qué
cuenta pertenece un prestador que ya encontraste, y no encontrarlo.* Costó una
vuelta entera. **Un pedido que nombra el dato y no el acto se cumple al pie y
no sirve.**

### ③ Los dos vacíos son dos frases, y la promesa es condicional

«No armó su página» ≠ «todavía no publicó ninguno»: la familia los resuelve
distinto —en uno espera al refugio, en el otro vuelve otro día—. Y
*«puedes ver sus animales igual»* **sólo se dibuja si los hay**: prometerlo con
la lista vacía sería mandar a mirar algo que no está.

### ⚠️ Y lo que la pantalla DICE en vez de callar

**Portada y logo no se editan desde «Mi vitrina».** La portada vive en
`prestador_fotos` con su propia puerta, y `logoUrl` es una **URL, no una
subida** — sin el camino de storage, un campo pidiendo una URL le trasladaría
al refugio un problema que es nuestro. *Se declara porque el silencio se leería
como que la vitrina no tiene portada.*

---

## §10 · 🔴 `cat_ciudades` LEE CERO PARA `anon`, SIN ERROR

Medido contra la base al montar A1:

```
has_table_privilege('anon','cat_ciudades','SELECT') → true
RLS                                                 → ON
policies: cat_ciudades_admin               → authenticated · is_admin()
          cat_ciudades_select_authenticated → authenticated · activo = true
```

**El GRANT está y no hay ninguna policy para `anon`** ⇒ lee **cero filas y no
falla**. El wrapper devuelve `ok` con `[]`.

> ### Un grant sin policy no da error: da silencio.

⚠️ **Y no es un borde: es el caso normal.** La vidriera de adopción se mira sin
sesión por firma del founder, y `HojaFiltros` tiene un grupo «Ciudad» que se
alimenta de ahí. Para cualquiera que no haya entrado, ese grupo llega vacío —
con su rótulo y sin opciones, que **no se lee como «no hay»: se lee como «esto
se rompió»**.

**Nada lo detecta:** ni el typecheck, ni `verify:diseno`, ni un `!r.ok`. El
resultado es indistinguible de «no hay ciudades cargadas».

**Repartido, y las DOS llegaron:**
- **A abrió la policy** — `cat_ciudades_select_anon` con `USING (activo = true)`
  y **no `true`**: *ofrecer un filtro por una ciudad dada de baja es prometer una
  búsqueda que no puede devolver nada.* **Verificado por C contra la base**:
  grant `true`, **1 policy para `anon`**, el anónimo ya recibe las ciudades.
- **B puso la regla de existencia** en `GrupoDeCatalogo` (un solo lugar, no los
  cuatro sitios de llamada), con discriminador **cero → NO · una → SÍ · dos →
  SÍ** — *«no se dibuja» tiene que poder volver a dibujarse.*

⇒ **La regla ya no es el camino más transitado y NO se retira:** vuelve a ser lo
que protege del catálogo que no respondió. *El dibujo no puede depender de que un
catálogo siempre tenga filas.*

### ⚖️ La decisión que B me dejó abierta, tomada con su dato

*¿Un grupo con UNA sola opción debería dibujarse, si filtrar por ella no cambia
nada?* **Medido antes de contestar: `EC = 8 · CO = 1`** — el caso **existe hoy**
(Colombia tiene una sola ciudad activa) **y no llega a esta pantalla**, porque la
vidriera es anónima, no sé de qué país es quien mira, y pido el catálogo **sin
`country_code`**: el grupo tiene nueve.

**Corte en CERO y no en uno**, por tres razones:
1. **Un control que aparece cuando el catálogo crece es peor que uno que no
   angosta.** Con corte en uno, el grupo **se materializa** el día que alguien
   agrega la segunda ciudad de Colombia — *una superficie que cambia de forma
   según cuántas filas tiene una tabla le enseña a la familia que la app es
   inestable.*
2. Con una sola opción, *«sólo operamos en Quito»* **es información**: esconderla
   ahorra un toque inútil y cuesta el dato.
3. **El defecto de ese caso no es el grupo: es que la única opción no se
   distingue de «todas»** — y la cura de eso no es ocultar, es que el vacío lo
   explique. *El vacío de esta pantalla ya lo hace:* con filtros puestos dice «no
   hay con ESE criterio» y ofrece limpiarlos.

---

## §11 · LO QUE SIGUE SIN VERIFICARSE, AL CIERRE DE ESTA COLA

**Cero aparato en todo S112-C.** Nada se ejerció en un teléfono: ni el desborde
de A3 corregido, ni el botón de cancelar, ni la celda de guardería en el hogar,
ni el avatar por especie, ni la vitrina, ni la hoja de filtros. Todo se leyó del
objeto, de la base y de los gates.

**Y tres que ningún gate puede ver:**
1. **El encuadre** de las ilustraciones de especie (migrado a `fotoDeEspecie`,
   pero *que la prop sea la correcta no prueba que se vea bien*).
2. **El desborde de A3**: produje el rojo del árbol de layout, no de píxeles.
   *Un desborde es una medida que sólo existe cuando algo se pinta.*
3. **La hoja de filtros con nueve grupos** en una pantalla real — si entra, si
   se scrollea, si el grupo de ciudad vacío se nota.

---

## §12 · EL CIERRE, VERIFICADO — y el control que casi no corro

**Los cuatro typechecks en 0** (`api` · `ui` · `cliente` · `prestador`) ·
`verify:diseno` **VERDE con 61 reglas** · `razon-muda` no subió · `vio-todo`
6/6 · **árbol limpio** · `999309f2` en origin, **verificado por SHA**.

### ⚠️ Y el control que lo hace valer, que salió de un aviso de B

B avisó: *«un `tsc` sin `node_modules` **no falla — devuelve ceguera que se lee
igual que un verde**»*. Al correr los cuatro, medí que **`packages/api` NO tiene
`node_modules`** y su salida era vacía.

**No lo di por verde ni por roto: produje el rojo.** Metí un error de tipo
deliberado y salió:
```
src/index.ts(1766,7): error TS2322: Type 'string' is not assignable to type 'number'.
```
⇒ el instrumento **ve** (pnpm hoistea a la raíz) y su verde es real.

> ### Una salida vacía no es una medición hasta que se sabe que el instrumento podía hablar.

Es la misma clase que me cobró hoy con `grep -c | wc -l` y a A con
`2>/dev/null`: **el verde no era falso — lo que faltaba era saber que podía ser
rojo.**

### Y la regla de B que va con eso

**Correr los cuatro, no uno.** Ella midió que **cuál typecheck caza una clase
dada no es estable entre árboles**: el mismo error de estilos salió por
`apps/cliente` en un árbol y por `apps/prestador` en otro, con los mismos 159
archivos y las mismas opciones. Nunca se aisló la causa; lo que quedó es la
regla.

---

# ═══ S112-C · LOTES 3, 4 Y 5 — LO QUE SIGUE DESPUÉS DE §12 ═══

## §13 · 🔴 LO PRIMERO PARA QUIEN RETOME: EL CHAT REVIENTA Y NO ESTÁ CURADO

**Síntoma (founder, bundle `a3ca0121`/`c60d81f3`, ancla `8bd1ce4e`):** al abrir
el hilo de adopción sale pantalla de error y hay que matar la app. **En las DOS
apps.** Con el lote 3 abría; con el 4 no.

### Los tres sospechosos del mandato, CENSADOS — y ninguno es

| # | sospechoso | cómo se descartó |
|---|---|---|
| ① | el `Record` de etapas rebota un estado del motor | las dos uniones (pieza y dominio) coinciden **exactamente** en los cinco valores · A comparó la escalera contra el CHECK: **7 vs 7, diferencia vacía en las dos direcciones** |
| ② | `suscribirseAlHilo` revienta o se desuscribe dos veces | A la **ejerció desde los dos asientos**: `SUBSCRIBED` en familia y en refugio, 200 y 3 hilos |
| ③ | la fila memoizada lee un campo que el item no trae | **arnés sobre el dominio con datos hostiles** (`creadoEn` inválido · `undefined` · autor `null` · lista vacía · fusión consigo misma · los 7 estados + uno inexistente): **ninguno lanza**. `mismaFila` sólo toca `.item` |

### 🔴 LA HIPÓTESIS VIVA, con su evidencia — **declarada como hipótesis**

**`useAnimatedKeyboard()` en `SuperficieChat`** (`packages/ui`, línea ~166).

- Es **lo único NATIVO que entró en el lote 4**.
- Está en **la única pieza común a las dos pantallas que revientan** — y revientan
  las dos, que es el dato más fuerte.
- **Se usa en UN solo lugar de todo el monorepo** (medido): nada de la casa lo
  había ejercido nunca ⇒ **el lado nativo del APK jamás se probó contra él**.
- **Un OTA no puede traer nativo.** El bundle llegó por OTA sobre un binario que
  nunca lo ejecutó.
- **`softwareKeyboardLayoutMode` no está declarado** en ninguna de las dos apps.

**NO SE CURÓ, y es deliberado:** la pieza es de B y el mandato prohíbe curar por
sospecha. **Pedido a B con la evidencia y una prueba barata**: envolver la
llamada y caer a `useSafeAreaInsets` sin inset animado — se pierde que la barra
viaje *con* el teclado y se recupera la pantalla.

> ### Lo que cierra esto es el logcat, y no lo tengo. **Sin aparato, esto es hasta donde llega la medición honesta.**

---

## §14 · EL CHAT, MONTADO EN LAS DOS APPS (C1–C6)

Sobre `SuperficieChat` · `BurbujaMensaje` · `CabeceraHilo` · `BarraEscribir` ·
`PastillaNuevoMensaje` · `EventoDelHilo` · `SeparadorDia` (todas de B) y el
contrato del hilo de D.

**El rojo de C1, y su mitad cara:** el cliente tenía `ScrollView` y **cero**
manejo de teclado —el teclado tapaba el campo—; el prestador tenía
`EvitaTeclado` pero `ScrollView` plano **sin anclar al final**. *El del prestador
se veía «bien» lo suficiente como para que nadie lo reportara* — por eso el del
cliente era el visible y éste el caro. **Dos comportamientos distintos para la
misma conversación**: por eso es UNA pieza y no dos composiciones.

**Lo que vive en `packages/domain` y por qué:** `armarHilo` (agrupado, separadores
de día, eventos) · `leerEscalera` (estado → `etapa` + `final`) · `fusionarPorId` ·
`fusionarFilas` · `contarPresencia`/`vozDePresencia`. **Todo derivación pura que
las DOS superficies necesitan idéntica** — *si la familia y el refugio derivaran
la etapa por su cuenta mostrarían etapas distintas para la misma solicitud, y no
habría forma de saber cuál miente.*

**Tres trampas escritas ahí porque son fáciles de invertir:** el orden es
**descendente** (lista invertida) · el separador de día va **DESPUÉS** de sus
mensajes en el arreglo para quedar **ARRIBA** en pantalla · el grupo se corta
**también por día** (sin eso, 23:58 y 00:01 quedan pegados con un separador en
el medio).

**Realtime:** el sondeo de 5 s **murió**. Hacía **12 peticiones por minuto**;
el socket hace **cero hasta que alguien escribe**. Y **el lado refugio nunca
había tenido sondeo** — *la misma conversación con una punta que se enteraba y
otra que no.*

**Lo que la letra pide y NO se dibuja, con su razón:** nada de adjuntar (D midió:
sin columna, sin parámetro, sin bucket privado — y `adopcion-fotos` es **público**)
· nada de «te vamos a responder» sobre la automática (sólo existe si el refugio
escribió su texto).

---

## §15 · LO MEDIDO EN EL LOTE 5 — H1 a H4

| # | estado | lo medido |
|---|---|---|
| **H1** | ✅ **curada, y eran OCHO** | censo cruzando las **47 claves con `{{count}}`** contra sus llamadores: 8 pasaban `{ n }`. Ahora **0 de 47**. El censo vive en `scripts/_censo-count.mjs`. ⚠️ **se cobró a sí mismo**: exigía `count:` con dos puntos y marcaba como malas las ya curadas en forma corta `{ count }` — *un censo que da rojo sobre lo ya curado enseña a ignorarlo* |
| **H2** | ✅ curada | **la foto no llegaba porque NO HAY FOTO**: la ruta viaja y se firma, y A midió `mascota_foto_url` **`null` en las cinco**. *No era `D-308`: no había nada que resolver.* Cae la cara de la casa. **Dato de A que ordena la expectativa: 3 de 5 no tienen raza** ⇒ caen al genérico de especie, que es el escalón correcto |
| **H3** | 🔴 **MEDIDA Y NO CURADA** | los oficios del Home salen de **una lista escrita en la pantalla** — `oficios: { paseo, grooming, adiestramiento, vet }` en `(tabs)/index.tsx:191` — y **guardería no está**. A confirmó que **`guarderia_dia` SÍ está** entre los oficios activos de demo-vet ⇒ el dato existe y la pantalla no lo mira. **No se curó porque tocar esa lista mueve el techo, el filtro y la unidad del HOY**: va con su propia pasada |
| **H4** | 🔴 **NO EMPEZADA** | lado familia: no llegan chips de conducta, fotos ni clips del durante. **A midió que los lectores devuelven: 25 eventos con bitácora y foto** ⇒ es MONTAJE. Falta censar cada puerta (el en vivo · la ficha de la estadía · el expediente) contra ese dato y montar lo que falte, firmado (`D-308`) |

---

## §16 · LA REGRESIÓN DEL DÍA PEGADO — dos fuentes de verdad

**El síntoma parecía cinco defectos y era UNO.** «0 a bordo · faltan 0», «5
animales hoy», el botón de foto con la razón del viaje, y **ni lista ni actos**.

🔴 **El viaje abierto vive EN EL DISCO** (`leerViaje`) y **sólo se borra si el
cuidador toca «Llegamos»**. Los datos decían que los tres ya habían llegado ⇒ el
viaje terminó **de hecho** y el disco seguía diciendo «estoy en la calle».

**Cura:** el viaje **se deriva de los datos, no del disco** — es real si y sólo si
hay alguien en su estado de tránsito. *El disco pasa de ser la verdad a ser una
pista: dice qué tramo era, y los datos dicen si sigue vivo.* El disco se limpia en
**su propio efecto**, con guard de `listo`: *mientras carga, las listas están
vacías por ausencia de datos, no por ausencia de viaje.*

**Y el `git log` descartó lo obvio primero:** sólo dos commits tocaron `dia.tsx`
desde el ancla, los dos míos, **y ninguno cambió el criterio de la rama**.

---

## §17 · EL DÍA SALÍA DE LA ZONA DEL TELÉFONO — y la raíz era una COPIA

`dia.tsx` tenía **su propio `hoyLocal()`** con su trampa documentada
—*«`toISOString()` da UTC y a la tarde corre el día»*— y esa nota **curaba media
clase**: dejaba de usar UTC y pasaba a usar la zona del **dispositivo**.

> ### Una copia con su trampa documentada es más peligrosa que una sin comentario: el comentario dice que alguien lo pensó, así que nadie lo vuelve a mirar.

Entró `hoyEnZona()`/`horaEnZona()` en `apps/prestador/src/lib/dia-local.ts`.

✅ **CERRADO (`b1495875`), y el hallazgo es el mejor de la tanda: EL HUECO ERA
EL TIPO, NO EL MOTOR.** `obtener_mi_prestador` devolvía `zona_horaria` **desde
`20260908820000`**; el `Pick` de `MiPrestador` no la incluía, y como el wrapper
hace **spread**, *el dato ya viajaba en la respuesta y no aparecía en el
autocompletado de nadie*.

> ### 🔴 Un tipo que declara de menos NO ROMPE NADA: esconde un dato que ya está viajando, y quien lo consume lo da por no construido. **Me pasó DOS VECES el mismo día** — con `sinLeer` en `MiSolicitud` y con `zona_horaria` —, las dos veces reporté «pendiente a A», las dos veces estaba, y las dos veces el hueco era el tipo.

**Regla acordada con A: si algo que ya entregó no aparece, se mira PRIMERO el
tipo** — y si no, se le pide por nombre, que le cuesta un paso medirlo y a mí
una búsqueda entera.

**Y la cura NO fue cambiar el default: fue MATARLO.** Mientras la zona era
opcional, `hoyEnZona()` sin argumento devolvía la de Ecuador **sin error y sin
aviso** — un valor plausible para un negocio que puede no estar acá. *Un atajo
que puede producir un valor equivocado no se documenta: se vuelve inexpresable.*
Ahora la zona es **obligatoria** y el compilador no deja llamar sin ella;
`ZONA_DE_LA_CASA` sobrevive como último recurso **escrito en el llamador, a la
vista** (`p.data.zona_horaria ?? ZONA_DE_LA_CASA`), donde se lee que hubo una
decisión. **13 sitios del día pasan a la zona real del negocio.**

⚠️ Y se fija **ANTES del primer uso**: fijarla después dejaría la primera
lectura del roster pidiendo el día en la zona equivocada, y devolvería *una
lista plausible del día de al lado* — sin error y sin aviso.

---

## §18 · LOS SHAs Y LOS PEDIDOS VIVOS

| lote | SHA | qué |
|---|---|---|
| 3 | `679aec11` | publicado por A (cliente `fe5b911b` · prestador `267dbff8`) |
| 4 | **`7945660a`** | G10 · vitrina desde el chat · A11–A15 · la regresión · la zona |
| 5 (en curso) | **`98e9a0b5`** | H1 · H2 · el reloj de 12 h · el censo del crash |

**Esperando de A:** la **zona del prestador** en su fila.
**Esperando de B:** el **crash** (§13) · **`fotoDeEspecie` en `FilaCita.mascota`**
— hoy la cara de especie va por `fotoUrl` y recibe el encuadre de retrato.

---

## §19 · LO QUE NINGÚN GATE VE, Y SIGUE SIN VERIFICARSE

**Cero aparato en todo S112-C.** Y en esta tanda apareció el caso extremo: **una
pantalla que revienta con los cuatro typechecks en 0, `verify:diseno` verde con
61 reglas y todos los trinquetes en su número.**

> ### Ningún gate de esta casa mira lo que pasa cuando el código CORRE. Por eso el aparato no es una etapa más: es la única que puede ver esta clase.

Y las tres que ya estaban: el encuadre de las ilustraciones · el desborde de A3
corregido · la hoja de nueve grupos. Más las nuevas: el teclado, la escalera con
sus glifos, las cartas de G10, la hoja de la postulación, el grabador a pantalla
completa.

**Y el número del perfilador de A14**: las tres patas están puestas (item con su
estado de envío · filas con identidad · render estable), pero **que den cero lo
dice el perfilador, no yo**.

---

## §20 · EL MÓDULO NATIVO QUE RESUELVE POR HOISTING — censado, y la clase CIERRA

**Lo levantó E leyendo el límite ① de C-A**, y tenía razón en que era una clase
y no una nota de commit: un paquete NATIVO importado por una app que **no lo
declara** resuelve igual —pnpm lo hoistea a la raíz—, así que **compila, corre
en dev, pasa los cuatro typechecks, y el APK no lo tiene**.

> ### Es la misma forma que la key de mapas de S84: el binario sin `geo.API_KEY`, el guard cayendo, y la sección viéndose igual que ayer. Su modo de falla es que la superficie funcione *casi*.

**Instrumento:** `scripts/_censo-hoisting-nativo.mjs` — los imports REALES del
código de cada app contra sus dependencias DECLARADAS. *No mira `node_modules`
a propósito: `node_modules` es justo lo que miente, porque ahí está todo venga
de donde venga.*

**⭐ Su verde está probado en ROJO antes de creerle** — sacada la declaración
que acababa de agregar, el censo dice `🔴 expo-clipboard — declarado en cliente
(~57.0.1): hoisting`, y nombra el mecanismo.

| | |
|---|---|
| **cliente** | 28 paquetes usados · 51 declarados · **cero** nativos sin declarar |
| **prestador** | 32 paquetes usados · 53 declarados · **cero** nativos sin declarar |
| único hallazgo | `expo-modules-core`, transitiva de `expo` — garantizada en todo runtime Expo |

⇒ **La población de la clase era UNA: `expo-clipboard` en el prestador, creada
y curada en el mismo commit.** De las dos salidas que E anticipó —*«o es cero y
entonces cierra, o es una lista de cosas que van a morir en el próximo APK sin
avisar»*— salió la primera.

**Servido como candidato a gate y NO instalado por mí** (el hook es
infraestructura compartida y sus gates son los que más pesan). **A lo tomó,
lo convirtió en `pnpm verify:hoisting-nativo` y lo cableó al pre-commit — y al
hacerlo encontró DOS cosas mías que hay que dejar escritas:**

🔴 **① SALÍA CON `exit 0` SIEMPRE.** Verificado acá: con 2 hallazgos en
pantalla, `echo $?` da **0**. Imprimía «🔴 TOTAL: 2» y pasaba igual.

> ### Y el error no es que un censo devuelva 0 — uno que se lee con el ojo no necesita código de salida. El error es que **lo entregué como candidato a gate** —«corre en 0,24 s, cabría en el pre-commit»— **sin darle la única propiedad que hace que un gate sea un gate.** Entregar algo listo para cablear cuando habría dicho que sí para siempre es peor que no entregarlo.

*Y es `L-488` una capa más abajo, contra mí: mi propio texto dice que «un censo
que entrega un número como veredicto está afirmando algo que no midió» — y yo
entregué un instrumento cuyo **código de salida** afirmaba «todo bien» sin haber
medido nada.* A le puso además la lista de TOLERADOS **con la razón de
`expo-modules-core` escrita adentro**, que es `L-488` en su forma operativa: *la
clasificación se escribe, no se deja en la cabeza del que corrió el censo.*

🔴 **② SÓLO ESCANEA `apps/<app>/src`** — verificado: el corpus es
`apps/${app}/src` y nada más. ⇒ **un nativo que entra por `packages/ui` es
invisible**, y ésa es la ruta de la mayoría de las piezas. *Su verde dice
«ninguna app importa un nativo sin declararlo DIRECTAMENTE», jamás «el APK los
tiene todos».* **Yo declaré el límite del binario y no declaré éste**, que es el
que más achica el alcance del número.

⚠️ **Y lo que A declaró de su propio cableado, que conviene no perder:** el rojo
del gate está probado; **su enganche al hook NO** — sus dos controles chocaron
con gates anteriores, y la forma es *parecida* a la de un bloque probado, que no
es lo mismo que medida. **El primer commit que agregue un nativo sin declarar es
su prueba real.**

⚠️ **Y lo que el censo NO contesta, declarado:** un módulo **declarado** en la
app pero **no horneado en el APK instalado**. Eso no lo dice el repo — lo dice
el aparato. *Este censo mide DECLARACIÓN, jamás presencia en el binario*, y esa
mitad es exactamente la que le costó a `expo-clipboard` del prestador quedar
con el botón dibujado y sin motor hasta la próxima build.

## §21 · LA LECCIÓN DEL CENSO DE TRES VUELTAS (C-B) — y por qué no es la de siempre

El censo de la cara de especie se equivocó **cuatro veces**: v1 dio 22 rojos con
tres ya curados aguas arriba · v2 dio 18 y al menos tres seguían siendo falsos ·
v3 dio 8, **y cinco de esos ocho no eran defecto: eran una decisión firmada**
(`cara={false}`, r40-2b/r41 — *«el log ya filtra por mascota, la cara repetida
no informa»*).

Las dos conclusiones son distintas y las dos importan:

1. **«¿esta puerta cae al monograma?» NO se contesta mirando la puerta.** La
   respuesta vive donde se armó el dato, y eso un censo estático no lo alcanza
   sin volverse un intérprete. La cura fue **cambiar la pregunta** por una que
   la puerta sí contesta y no admite falso positivo: *¿pasa `fotoUrl`, sí o no?*
   — un subconjunto más chico donde **cada rojo es un hecho y no una sospecha**.

2. 🔴 **Y la que va más lejos, porque no se cura con mejor instrumento:**
   *ningún `grep` puede leer una intención.* Cinco de ocho eran criterio, no
   olvido, y **ninguna versión del censo iba a poder distinguirlos.**

### `L-488` — el texto acordado con E, depositado por A en `main 954f2e3c`

> **Un censo estático entrega candidatos, no veredictos.** Ninguna medición
> sobre el árbol distingue una ausencia que es **defecto** de una que es
> **criterio firmado** — y no porque la firma esté fuera del alcance: puede
> estar **tres líneas arriba de la puerta, en el mismo archivo**, y el
> instrumento la lee como prosa. *Ningún grep puede leer una intención.* La
> diferencia no se cura con un instrumento mejor. Su consecuencia operativa: el
> censo **nombra** los casos y **los clasifica quien conoce la letra**; un censo
> que entrega un número como veredicto está afirmando algo que no midió.
>
> **Corolario, que es lo que la separa de su vecina:** un instrumento que mide
> desde el asiento equivocado se cura **cambiando de asiento**; éste no se cura
> **midiendo mejor**.

**La primera redacción decía *«ninguna medición sobre el árbol distingue…»* a
secas, y se corrigió porque era demasiado indulgente con el instrumento:** se
lee como *«el dato no estaba»*, y acá el dato **sí estaba** — en el mismo
archivo que el censo abrió y leyó.

> ### 🔴 Y el costo de la versión suave no era la imprecisión: **mandaba a buscar la cura donde no está.** «Mejorá el grep» en vez de «el grep no puede leer una intención, punto» *(la lectura es de E, y es la que la vuelve accionable).*

⚠️ **Por eso las dos familias quedan SEPARADAS y se dice acá:** juntarlas hace
que ésta se lea como *«el instrumento estaba mal hecho»* — y entonces el
próximo gasta una vuelta afilando un regex que nunca iba a poder.

*El instrumento que sí puede contestar la pregunta original es el que la casa ya
tiene y necesita aparato: el `console.warn` de `AvatarMascota` —«sin foto y sin
`fotoDeEspecie` ⇒ monograma»— que corre con datos reales y no puede equivocarse
de rama.*

---

## §22 · H4 — LOS CLIPS CERRADOS, LA BITÁCORA FIRMADA Y ESPERANDO MOTOR

### ✅ Lo cerrado: los clips (`8d34fcd5`)

`MediaGuarderia.tipo` es `'foto' | 'clip'` y la pantalla del durante tenía un
filtro `=== 'foto'` **sin una sola nota**. La sección se llama literalmente
*«SU DÍA — las fotos y los clips»*.

> ### 🔴 Y no era «faltaba algo»: **el contador decía `media.lista.length` —que sí cuenta los clips— y la grilla dibujaba sólo fotos.** La familia leía «5 momentos» y veía 3. *Un número que no coincide con lo que hay debajo es peor que un número ausente: el primero se descubre contando, y para entonces ya no se le cree a la pantalla.* **No faltaba: mentía.**

Montados con **`ClipSesion`** (S63) — *un clip del mismo negocio no puede verse
distinto según quién lo mire*.

### 🖊️ Lo firmado por el founder (2-sep): **expediente + ficha del durante**

Los dos caminos se llevaron a la mesa con su costo. **Ganó el que A votó, con
su argumento:** *la bitácora se escribe **mientras el animal está adentro**, y
una anotación que sólo aparece en el expediente **llega cuando ya no sirve para
nada más que el registro**.* Y lo que se ve desde la pantalla: *una sección
llamada «SU DÍA» que no puede decir qué le pasó hoy está incompleta por
definición, no por alcance.*

### 🔴 Lo que espera motor de A — y el hallazgo es de la clase del día, por TERCERA vez

**Iba a escribir la voz de `bitacora_familia` y me frené.** El mismo tipo lo
escriben **dos manos** —la familia desde su app y el cuidador desde la
guardería— y el discriminador **no llega**:

```
timeline.ts:122  .select('… , prestador_id, datos, …')      ← se PIDEN
lo que sale:     evento_id · mascota_id · tipo · eje_jtbd · fecha_evento ·
                 titulo_fuente · duracion_min · atencion_id · fotos_count ·
                 hito_clave · vacuna_nombre                  ← se TIRAN
```

> ### Las dos anteriores fueron *el dato viaja y el tipo lo esconde*. **Ésta es peor: el wrapper ya paga el costo de traerlo y lo descarta en el mapeo.**

**Sin el aviso de A yo habría escrito «Anotaste algo sobre Thor» sobre una
conducta que anotó el cuidador — y habría salido VERDE en todo.** *Preguntar por
nombre, en vez de deducir del tipo que tenía a mano, es lo único que lo evitó.*

**Pedido, en orden de desbloqueo:** ① `prestador_id` (u `origen`) en el ítem —
*cero viaje nuevo: es dejar de tirarlo* · ② los chips en `datos` como
`{codigo, nombreFamilia}`, **con el nombre resuelto por A** porque el idioma
sale de `cat_conductas_bitacora` · ③ el lector por estadía, ya firmado.

⚠️ **Y hasta que llegue: la sección «SU DÍA» muestra fotos, clips y estado, y
nada de qué le pasó. NO se pone placeholder ni «Pronto»** — la sección existe,
se llena, y lo que falta se nota. *Un hueco honesto avisa mejor que una promesa*,
y una promesa en pantalla envejece sin que nadie la mire.

### Dos prosas vencidas curadas de paso, y las dos se contradecían con su propio archivo

· el encabezado del durante decía *«la estadía NO se puede leer»* y *«por eso NO
  se cableó la entrada»* — falsas desde el 29-ago, **con dos lápidas del mismo
  archivo diciéndolo treinta líneas más abajo**. Medido: la pantalla es
  alcanzable desde DOS lugares.
· el hub decía *«el lector NO existe»* y **cinco líneas después** *«el lector
  existe»*.

> ### Un encabezado que se contradice con su cuerpo es peor que uno vencido a secas: **quien lee de arriba hacia abajo se lleva la versión falsa y deja de leer.** Se conserva la REGLA de esos comentarios y muere su DIAGNÓSTICO — lo que venció es el hecho, no la ley.
