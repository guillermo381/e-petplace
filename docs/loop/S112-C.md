# S112-C · PARTE DE LA PISTA C (`apps/*`) — el loop de adopción

> **Se lee ANTES de tocar nada.** Es el handoff: estado, cola viva con su próximo
> paso, y lo que ya se midió para no re-medirlo. Lo que no está acá no existe.

---

## §0 · LOS DOS ESTADOS QUE NO SE PUEDEN OLVIDAR

**① `ADOPCION_ALCANZABLE = false`** (`apps/cliente/src/lib/gate-adopcion.ts`).
**CUATRO lectores**: Explorar · el hogar sin mascotas · la bifurcación del
onboarding · **y el botón del login**. El cuarto se agregó hoy: viajaba **vivo**
con el flag en `false`, y el propio archivo del gate afirmaba que estaba gateado.
*Un flag que apaga tres puertas de cuatro no apaga nada.* **Lo enciende A en el
último commit del lote** (§9.2 del loop), y ahí la constante se borra con sus
cuatro lectores.

**② El portal del prestador VOLVIÓ.** Estaba revertido (`63054b7c`) para que no
viajara en el lote de guardería; el revert del revert es `c43c1d77`.
`verify:razon-muda` VERDE con él adentro (baseline 141).

---

## §1 · LA COLA VIVA, en orden, con el próximo paso ejecutable

| # | qué | estado | bloqueante NOMBRADO |
|---|---|---|---|
| C1 | puerta sin sesión (login, **gateada**) + la línea con su «i» | ✅ | — |
| C2 | la lista: destacados con su porqué · filtros · keyset | ✅ | — |
| C3 | la ficha en un viaje + semáforo (`lector="observador"`) + **Reportar** | ✅ | galería multi-foto (ver §6) |
| C4 | «quiero adoptar» vuelve a donde estaba | ✅ hilado, **no ejercido** | la rama la abre `ADOPCION_ALCANZABLE` |
| C5 | el formulario, con su consentimiento y la promesa del reloj | ✅ | — |
| C6 | mis solicitudes + hilo + **desistir con su verbo propio** | ✅ | — |
| C7 | portal: Home · solicitud · términos · tab Mascotas · ficha de edición · **fotos** | ✅ | — |
| C8 | el acta y la firma en las dos apps + el hilo como su puerta | ✅ | — |
| C9 | avisos: **push ✅ · campana ✅** | ✅ | — |

**LOS NUEVE ESTÁN. Nada de mi lista queda abierto.**

⚠️ **Y eso NO quiere decir que el vertical esté verificado:** todo lo de §2 sigue
siendo **construido y no ejercido**. Lo que falta ya no es mío — es correrlo.
Lo primero que va a doler, si duele, está nombrado en §4: **las caras no cargan
para `anon`** (hallazgo de E), así que la vidriera del founder puede abrir con
caras genéricas de especie en vez de las fotos de los animales.

## §2 · LAS PUERTAS DE LO MONTADO (ENTREGADA ≠ MONTADA)

| pieza / motor | dónde se monta | ejercido |
|---|---|---|
| `obtenerAdoptables` (keyset) | `/adoptar` | ❌ ni aparato ni base |
| `obtenerAdoptable` | `/adoptar/[publicacionId]` | ❌ |
| `TarjetaAdoptable` · `BloqueConCriterio` · `FiltroPills(envuelve)` | `/adoptar` | ❌ |
| `FichaAdoptable` · `Convivencia` · `SenalesAdoptable` · `SemaforoSanitario(observador)` | `/adoptar/[publicacionId]` | ❌ |
| `DocumentoLegalLectura` | `/legales/[codigo]` en **las DOS apps** | ❌ |
| `EstadoSolicitudAdopcion` | hilo (`completa`) y lista (`compacta`) del cliente | ❌ |
| las 7 del hilo (D) | `/adoptar/solicitud/[id]` · `/adopcion/solicitud/[id]` | ❌ |
| `tengoAceptadoDocumento` | compuerta del portal | ❌ |
| el filtro de rutas de push | `_layout.tsx` de las dos apps | ❌ ninguna push real |

**Entregadas por B y NO montadas, con su razón:** `FormularioPostulacion` (C5
bloqueado) · `CodigoFirmaInput` (C8) · `HitoUnaVidaNueva` (C8) ·
`ConvivenciaInput` y `TarjetaMascotaRefugio` (tab Mascotas).
**`MemorialAdoptable` NO EXISTE y no es deuda:** B censó que *el memorial de
esta casa no es una pieza, es un TEMA* — se envuelve la ficha en
`<ThemeProvider memorial>` y no se le pasan `cta`, `apadrinar` ni `bono`.

---

## §3 · LO YA MEDIDO — no re-medirlo, y no creerle al canon

- **`obtener_adoptables` acepta `anon`** desde S111 (`20260907520000:345`
  `GRANT EXECUTE … TO authenticated, anon`; las fotos, `20260907560000`). La
  cabecera de `adoptar.tsx` decía lo contrario y frenaba a quien la leyera.
- **`talla IN ('S','M','L')`** · **`estado_vacunal IN ('al_dia','incompleto','sin_datos')` o NULL** · `sexo` = `macho|hembra`.
- **El bloque de voz `ficha:` YA EXISTÍA** (FichaMascotaHogar). El de la ficha del
  adoptable se llama **`fichaAdoptable`**.
- **`AvatarMascota` llega hasta `lg`** — es un avatar, no una foto a sangre.
- **Los dos `toque-de-push.ts` son byte-idénticos.** Lo que cambia por app es la
  LISTA, y por eso vive en `destino-de-push.ts`, uno por app.
- **`guarderia`, `paseo`, `adiestramiento`, `avisos`, `invitacion` y `login`
  existen en LAS DOS apps** y llevan a pantallas distintas ⇒ el primer segmento
  **no** distingue de qué app es una ruta.
- **`_guarderia_aplicar_acto` emite `/guarderia/<estadiaId>`** en código vivo
  (censo por la CAUSA, de D).

---

## §4 · HALLAZGOS VIVOS QUE NO SON MÍOS

- 🔴 **Las CARAS no cargan para `anon`** (E): el predicado de
  `mascotas_select_vidriera_anon` pregunta por tres tablas con RLS que `anon` no
  ve, y el EXISTS se vuelve falso desde su asiento. Verde desde superusuario,
  falso desde anon. **La vidriera va a mostrar la cara genérica de la especie**
  — honesto, pero no es la foto del animal. Dueño: A.
- 🔴 **Una sola foto por animal** (E): la vista resuelve por `mascotas.foto_url`,
  singular, y `adopcion-fotos` tenía INSERT en `is_admin()`. La galería
  deslizable de §4.1 **no tiene motor**. *(A entregó `20260907920000` después;
  re-medir antes de dar por cerrado.)*
- ⚠️ **`chk_cierre_coherente` no admite `cerrada_en` para `desistida`** (D): el
  estado nuevo va a exigir tocar DOS constraints, no una.

---

## §5 · COMPROMISOS ABIERTOS

**Debo:** nada pendiente de responder.
**Me deben:** el lector de mis adoptables · `p_respuestas` en la solicitud ·
`reportar_publicacion` · el acta y la firma · `obtener_mi_cuenta_refugio` y la
rama refugio del arranque · `datos`/`ruta` en `AvisoDeCampana` (todos A) ·
la enmienda del estado `desistida` cuando exista (D).

**Confirmado a D:** la ruta del portal es **`/adopcion/solicitud/<solicitudId>`**
(medida en el objeto, con control positivo).

---

## §6 · FRENOS — lo que NO se hizo, y por qué

- **No escribí el segundo carrusel de la casa.** Ya existe dentro de
  `FichaPrestador`, y **su propiedad clave la cerró el ojo del founder en
  aparato** («no tironea en Android»), no la lectura del repo. Escribirlo
  convertiría una extracción pendiente en una duplicación consumada. Y hoy hay
  **una sola foto**: un carrusel de un elemento no es un carrusel.
- **No monté `SemaforoSanitario` con un `onResolver` de mentira** ni lo dibujé a
  mano al lado. Se pidió el ensanche y llegó con **dos** brazos.
- **No dibujé «cerca de mí» ni el filtro de edad.** El primero no tiene lector de
  ciudades ni permiso de ubicación; el segundo exige umbrales **por especie**
  que viven en `cat_especies_perfil` sin lector acá. *Inventar un corte único
  para perro y gato sería la pantalla decidiendo una regla de producto.*
- **No dibujé «Reportar» apagado.** Ley 23.
- **No monté `desistida`.** El CHECK vivo no lo acepta; va con `declinada`.

---

## §7 · TRAMPAS OPERATIVAS MEDIDAS HOY

1. **`router.d.ts` se GENERA, no se copia.** Sin él `tsc` sale VERDE sin medir
   una sola ruta. Se regenera con `npx expo start` **desde la app** — y **en un
   puerto libre**: 8081 y 8099 estaban tomados por otro harness, y sin puerto
   expo no arranca, no regenera, y el fallo se lee como «la ruta no existe».
2. **`git commit` a secas está bloqueado por el hook**: siempre con pathspec.
3. **`pnpm -s <script inexistente>` devuelve 0.** Corrí `verify:rutas-de-aviso`
   antes de tener el script y me dio VERDE. *Un exit 0 de un script que no
   existe se lee igual que un gate que pasó.*
4. **Un arnés con el dato COPIADO mide su copia**: el primero del filtro de push
   siguió verde después de que la lista real cambiara. Y al leerla del archivo,
   contó un prefijo dos veces porque el regex leía **mi propio comentario**
   (`L-170`, cobrada dentro del arnés que la citaba).

---

## §8 · LO NO VERIFICADO, SIN MAQUILLAR

**CERO pasadas de aparato en toda la sesión.** Ninguna pantalla del vertical se
ejerció contra la base: no hay refugio con rol, no hay solicitud viva, no hay
push emitida con ruta. Todo lo de §2 es **construido y no ejercido**.
