# S113-C · 1.0.2 · reconciliación contra el contrato de la adenda

Rama `pista/s113-c-1.0.2`, desde main `35f8a71f` (el revert de A).
Mergeado **sólo `pista/s113-b-1.0` @ `0957beb2`**. Verificado por ancestría:
**`b-1.1` (`182e6480`) y `b-1.2` (`6d37ed92`) NO están en esta rama.**
**Cuenta: `guillo381+8@gmail.com`.**

## ⓪ EL MERGE TRAJO CINCO CONFLICTOS, y los cinco los produjo el revert

`main` tiene el revert de la adenda anterior, así que git enfrentó «A la borró»
contra «B la trae de nuevo»: `FilaConfirmacionVacuna` · `vacunas-estado` ·
`S113-B.md` · `_arnes-carnet` · `package.json`. **En territorio de B gana B**
—el revert fue para desbloquear `main`, no una decisión sobre el contenido— y
en `package.json` entran sus dos gates (`verify:carnet`, `verify:barra-web`).

## ① EL ROJO QUE A ANUNCIÓ, REPRODUCIDO

```
apps/cliente/src/app/carnet.tsx(448,17): error TS2322
  Type '{ … origen: string; vozOrigen: string; … }' is not assignable to
  'FilaConfirmacionVacunaProps'
```

## ② LA LLAMADA, REHECHA — y lo que cambia no es la forma, es lo que se dice

- 🔴 **`vozOrigen` NO SE PASA, y ésa es la decisión.** Antes le mandaba «leído
  del carnet» porque `origen` era obligatoria y había que poner algo; ahora la
  pieza dice que **ausente ⇒ no dibuja ninguna línea de procedencia**. *De un
  carnet donde no se distingue si fue sello o lapicera no sale una procedencia
  por defecto: sale ninguna.*
- ☠️ **Murió mi botón «Esta no es»**: la pieza trae el descarte con su voz, y
  el retiro va en el **mismo acto** que el montaje (`L-395`).
- `confianza: 'baja'` en todas sigue siendo un dato honesto: la extracción no la
  devuelve, así que **no la sabemos**.
- ⭐ **Monté `PieConfirmacionVacunas`** en lugar de mi `Boton`, y el número de
  «Guardar N» **lo pone el pie**: con tres descartadas mi cuenta habría
  prometido «Guardar 5» sobre un botón que guarda 2.
- 🔴 **Saqué mi cuenta de toques del guard de `guardar()`.** Para la pieza
  **descartar ES revisar**, así que una tanda toda descartada le daba una cuenta
  a ella y otra a mí. La regla que las une, `resumenDeLaTanda`, **existe en el
  módulo de B y no está exportada** — replicarla sería volver a tener dos
  cuentas con distinto dueño. *La puerta es el pie*, que sólo llama a
  `onGuardar` cuando se pinta encendido.

## ③ LA PANTALLA DE VACUNAS: LOS CÓDIGOS VIAJAN SIN TRADUCIR

Murió mi `switch` de clases. Ahora `estadoDelPlan({ estado, proxima }, hoy)`.
**El mapeo a mano era el defecto**: `aun_no_corresponde` terminaba pintado como
`sinRegistro` —la clase de la FALTA— justo sobre lo que el motor dice que *jamás
se muestra como falta*. La voz lo tapaba; el color no. Lo que queda de la
pantalla es la voz, con su `default` que habla en vez de callar.

## ④ MEDIDO EN WEB, con la cuenta declarada

**El par del pie** — y ojo, **la razón se mudó de lugar**: el botón dice siempre
qué hace y la razón vive en una línea arriba.

```
🔴 ROJO  «Sumar 3 vacunas a su historia» · apagado=true  · razón «Faltan 3 por revisar»
✅ VERDE «Sumar 3 vacunas a su historia» · apagado=null  · razón «(ninguna)»
   singular: «Falta 1 por revisar» / «Sumar 1 vacuna a su historia»
```

**El guardado real, en la base**: `Prueba C113` (`a19d407b`) **3 → 6 filas**.

**🔴 LA TANDA ENTERA DESCARTADA NO DEJA EL PIE SIN DIBUJAR**, y la pieza tiene
razón: se dibuja y dice **«No queda ninguna para guardar»** (medido: sin pie
=no · «no queda ninguna»=sí · «faltan 0»=**no**). El pie sólo desaparece con
**cero filas**, que es otro caso — y su propio comentario lo explica: *«no queda»
implica que hubo*.

**«Su plan», con un cachorro creado para el caso** (`Prueba C1102`,
`50d2a72d`): sale **«Todavía no le toca»** ⇒ `aun_no_corresponde` **con su clase
nueva**, que es justo lo que antes se pintaba como falta.

🔴 **`vence_en` no se pudo ver, y la razón es un hallazgo:** guardé una vacuna
con próxima **20/09/2026** —dentro de la ventana de 30 días— y el plan la ignoró.
Medido en la base: **`vacuna_codigo` es `null`**. *La extracción no ata la
vacuna al plan*, así que cae en `aplicadas_sin_clasificar` y ninguna fila del
plan puede llegar a `vence_en` por el camino del carnet. Es la misma razón por
la que Thor muestra la nota de «no pudimos ubicar en el plan».

## ⑤ DOS COSAS DE B QUE ESTÁN ROTAS, y no las toco

1. 🔴 **`TokenGallery.tsx` no compila contra la pieza nueva de B**: 3 errores
   TS2322 (`origen`, `vozOrigen`, `vozGuardar` como string). Se cuelan en los
   tsconfig de cliente, prestador y ui. **Cero errores míos** (medido: los tres
   son del mismo archivo). *Esto es lo que deja `main` rojo, y sigue.*
2. 🔴 **`verify:carnet`, el gate nuevo de B, rompe en esta rama**: abre
   `packages/ui/src/components/PuntoEstado.tsx` y **ese archivo no existe acá**
   (no lo trae `b-1.0` ni `b-1.1`). *Un gate de un lote que depende de una pieza
   de otro lote no se puede correr en el lote donde vive.*

## ⑥ MIS PROPIOS ERRORES, DECLARADOS

- **el comentario JSX cerrado con `*/` sin `}`** — tercera vez en la sesión;
- **se me cayó el bloque de guardado del arnés en una edición** y el arnés
  **siguió dando verde sin él**: reportaba el par rojo/verde y callaba que nunca
  había guardado. *Un instrumento al que se le cae un paso no falla: mide de
  menos y lo llama verde.* Repuesto, con la nota adentro;
- **mi medición del rojo quedó ciega** al cambiar el pie: leía la etiqueta del
  botón y la razón se había mudado a una línea. *El rojo no desapareció, se
  mudó, y yo seguía mirando el lugar viejo.*

## BASELINE

`verify:razon-muda` **140 → 139**, asentado **en este mismo commit**: murió mi
botón «Esta no es», que llevaba su razón. *Un baseline que baja y no se asienta
convierte la próxima subida en invisible.*

## LO QUE PIDO, POR NOMBRE

**A B:** `TokenGallery` contra su pieza nueva (deja main rojo) · `verify:carnet`
sin depender de `PuntoEstado` · **exportar `resumenDeLaTanda`** (para que el
guard de la pantalla recupere su cinturón sin copiar la regla) · `OrbeCoach`
exportado.

**A A:** que el guardado ate la vacuna al plan (**`vacuna_codigo` sale `null`**,
y sin eso el plan nunca ve lo que la familia carga) · confianza por fila ·
`plan_impreso` · laboratorio/vía/quién · la columna `modo_captura`.

## GATES

`pide-en-memorial` · `nexo` **344 · 0** · `hooks` · `hooks-bajo-return` ·
`ref-antes-de-uso` · `gates-existen` · `razon-muda` **139** · `puerta-unica` ·
`diseno` **61 reglas** · `coach` **134 · 0** · `glifos` **31 · 0** · `contrast`
OK · `tipos:rutas` OK · **tsc api 0** · **cliente/prestador/ui: 3, todos de la
galería de B**.

## RESIDUO

`Prueba C113` (`a19d407b`, 6 vacunas) y `Prueba C1102` (`50d2a72d`, 1) quedan
vivas: sus nombres dicen lo que son y el producto no tiene borrado desde acá.
Traje de mis ramas el parte de `74927e23` y dos instrumentos míos
(`medir-vacunas-fina`, `crear-mascota-de-prueba`) **sin ninguna pieza**.

**Cero aparato.**
