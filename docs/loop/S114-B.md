# S114-B · LAS PIEZAS DE LA POSTVENTA

> | | |
> |---|---|
> | **Rama** | `pista/s114-b-1.0` |
> | **Commit del trabajo** | `2d0a4c6c` — las ocho piezas, los cinco guards y la skill |
> | **Punta** | **se lee del objeto: `git ls-remote origin refs/heads/pista/s114-b-1.0`.** *Un parte no puede escribir su propio SHA sin cambiarlo al escribirlo — y un SHA intermedio nombrado como si fuera la punta es la clase que S114 ya midió: se despliega un estado que existió unos minutos y que nadie volvió a mirar.* |
> | **Base** | `main` en `e516a089` (`origin/main` en el mismo commit, verificado con `git fetch` + `rev-parse`) |
> | **Territorio** | `packages/ui/` · `scripts/verify-{diseno,contrast,fila-memoizada}` · la skill del design system |
> | **Letra** | `docs/DIRECCION_POSTVENTA.md` (leída entera) |
> | **Estado** | **ENTREGADAS Y NO MONTADAS.** Ninguna pantalla las consume: eso es de C. |

---

## ⓪ LA FRASE CONTRA LA QUE MEDÍ CADA PIEZA

*«Una familia que ya pagó tiene a dónde ir cuando algo sale distinto, en el
mismo lugar donde está lo que pagó — y no tiene que pelear por lo que la letra
ya le prometió.»* (§0)

**Y el corolario que ordenó el lote entero:** de las ocho piezas, **cinco no
son piezas nuevas** — son la de despensa o la de adopción con otro
vocabulario. *Eso no es economía mía: es la letra.* §3 lo pide con esas
palabras: *«es la misma escalera del pedido de la despensa y el mismo chat de
la adopción — **no aprendo nada nuevo**»*.

---

## ① LAS OCHO, Y QUÉ GARANTIZA CADA UNA POR CONSTRUCCIÓN

| # | pieza | lo que **no se puede** hacer con ella |
|---|---|---|
| B1 | `EscaleraCaso` | montar una etapa sin glifo (`Record` completo ⇒ no compila) · meterle un color (no recibe ninguno: sólo `acento: control\|oficio`, que resuelve contra el tema) |
| B2 | `CabeceraCaso` | decir el monto — **ni por prop ni por slot** |
| B3 | `SelectorMotivo` | traer un «Otro» (la última fila la pone la pieza) · esconder la lista en un scroll |
| B4 | `TarjetaDestinoPlata` | hacer las dos tarjetas distintas (una receta, `stretch`, cero rama por destino) · preseleccionar |
| B5 | `BannerPlazo` | gritar (cero `danger`/`warning`) · latir (cero reloj adentro) |
| B6 | `FilaBandejaCaso` | redibujarse sin motivo (`memo` + `mismoCaso`) |
| B7 | `BurbujaMensaje` + `cara` | **hablar como la casa desde la derecha** (`cara` vive sólo en la rama ajena) |
| B8 | `LineaAlgoSalioDistinto` | aparecer en memorial (devuelve `null`) |

**`EventoDelHilo` NO SE TOCÓ, y es lo correcto:** su `accion?: ReactNode` ya
era el slot de la carta que §3.3 pide, con su razón escrita desde S112 (*«la
acción cambia por hecho… lo que la pieza garantiza es el LUGAR»*). *El pedido
decía «reusá lo de adopción, no lo dupliques»; reusarlo acá era no escribir
una línea.*

---

## ② ROJO PRIMERO — **11 BRAZOS, LOS 11 SOBRE LOS ARCHIVOS REALES**

*«La primera prueba de un guard nuevo no es que dé verde: es que dé rojo sobre
el primer caso real»* (`L-459`). Los cinco guards nacen con su fixture **y
además** se mutó **el archivo de producción de a uno**, se corrió el lint, y se
revirtió:

```
ROJO ✅ R72 · la etapa que existe en la unión y falta en ORDEN_CASO
ROJO ✅ R72 · la etapa sin glifo
ROJO ✅ R73 · la rama por destino  (=== 'saldo')
ROJO ✅ R73 · el default de elección
ROJO ✅ R73 · la palabra «recomendado»
ROJO ✅ R74 · el monto en la cabecera
ROJO ✅ R74 · el slot ReactNode
ROJO ✅ R75 · el cajón «otro»
ROJO ✅ R75 · el scroll interno
ROJO ✅ R76 · el rojo de alarma
ROJO ✅ R76 · el countdown que late
```

**Son DOS pruebas distintas y hacen falta las dos:** el fixture dice que el
brazo *sabe decir que no*; la mutación del archivo real dice que *sabe
encontrarlo donde de verdad vive*. Los seis brazos que un fixture único no
enciende quedaron cableados en `EXTRAS_BRAZOS`, para que sigan probados en
cada corrida.

**Y el más caro de los cinco es `R72`, porque mide lo que el tipo NO puede.**
`GLIFO` es un `Record<EtapaCaso, IconoNombre>` ⇒ una etapa sin glifo no
compila, y ese rojo ya estaba cerrado. Pero `ORDEN_CASO` es un
`readonly EtapaCaso[]`, y **un array de un tipo compila con menos miembros que
el tipo**: una etapa que exista en la unión y falte en el orden **desaparece de
la escalera y nada falla**. *No rompe, no avisa, y la pantalla se ve
perfectamente normal con un paso menos.*

---

## ③ LOS TRES HALLAZGOS DE MEDIR EN VEZ DE ASUMIR

### 🔴 (a) `text.tertiary` no es texto — y tres de mis piezas lo usaban

El pedido decía *«contraste VERIFICADO, jamás asumido»*, así que censé los
cuatro pares que mis piezas pintan: **dos ya estaban medidos** y **dos no**.
Los agregué al gate y el resultado tumbó una decisión de diseño mía:

```
text.tertiary / accent.controlBg ⊕ card
  light 2,11 · dark 3,22 · light·oficio 2,20 · dark·oficio 3,08   (piso 4,5)
text.secondary, el mismo par ……… 4,84 · 7,18 · 5,04 · 6,18        ✅
```

**Y no era un detalle de accesibilidad: era la simetría de §4 rompiéndose por
el lado que nadie mira.** El tiempo del banco es la frase larga; si el tiempo
se apaga, el que pierde legibilidad es justo el dato que la letra puso ahí
**para que la opción lenta no quede escondida**. *Un dark pattern no necesita
un botón más grande: alcanza con que un dato pese menos de lo que dice el
papel.*

La casa ya lo tenía escrito —`text.tertiary` es *«placeholder/decorativo, no
texto funcional»*— y yo lo había usado tres veces para texto funcional: el
tiempo de la tarjeta, el reloj de la fila de bandeja y el estado del caso en la
puerta. **Las tres a `secondary`.** *Un plazo, un tiempo declarado y el estado
de un caso no son decoración.*

### 🔴 (b) `conIconos` tiraba el `tamano` que la escalera le pasa

`EscaleraEstados` escribió un párrafo entero para prevenir esto: *«`tamano` lo
pasa LA PIEZA, derivado del nodo: el consumidor lo recibe y **no lo inventa**.
Si el nodo crece y el glifo se queda, el defecto sale en la pantalla y no en
ningún gate»*. **Y el único consumidor de ese slot en toda la casa era
exactamente ese defecto:** el nodo pasó de 20 a 32 (`GLIFO_EN_NODO` = 24) y el
glifo se quedó en un `12` hardcodeado.

⚠️ **No se curó cambiando el default, y el porqué es una medición ajena:** los
cuatro `nodo*` de despensa *«nacieron con la anatomía de silueta rellena porque
un check de trazo a 12 px se convierte en una mancha»* — subirlos a 24 de un
saque **cambia lo que el founder ya aprobó en dos pantallas de despensa y una
de adopción**, desde un frente que no es el suyo. ⇒ **el legado queda como
default y lo correcto entra por opción** (`'delNodo'`, que es lo que usa
`EscaleraCaso`). `D-1006` sigue viva y ahora tiene su salida escrita.

### 🔴 (c) El contador de piezas de la skill decía 81 y eran 171

Pre-existente (lo verifiqué con `stash`: rojo antes de mi lote). **Casi cien de
brecha, once sesiones después de su última «re-medición».** Y lo notable es que
**su propia nota lo había predicho**: *«un número viejo acá no desinforma a un
lector — desinforma a cada construcción»*. **Se cumplió, y la nota no lo evitó:
la hizo más creíble** — un contador con «re-medido» al lado se lee como un dato
ya verificado, así que nadie lo va a ir a mirar.

**Tercer cobro de la clase en esta casa** (migraciones cayó cuatro veces,
fichas seis), y las tres veces la cura fue la misma: **sacar el número y
declarar el comando.** Hecho. Y quedan nombradas **las tres escalas** que
confundirlas era la mitad del lío: **piezas** (lo que mide el gate) · **filas
del índice de la skill** (un subconjunto) · **exportaciones** (lo que cuenta
`R17`).

---

## ④ LAS DOS DECISIONES QUE UNA MEDICIÓN DIO VUELTA

### La superficie del tercer asiento: **no hay tercer color, y no es gusto**

La salida obvia para «la casa» era darle su propio fondo de burbuja. Medí los
tres temas antes de escribirlo:

```
memorial → bg.card, bg.elevated, bg.overlay y bg.hundido son EL MISMO color
```

⇒ **cualquier tercera superficie colapsa contra la del prestador en memorial.**
*Una distinción que existe en dos temas de tres no es una distinción: es un
defecto que aparece en el tema donde menos se lo mira.* Lo que separa los tres
asientos es **la cara y el nombre** — que es literal lo que §3.3 pide.

### `CabeceraCaso` no copió el `acciones` de `CabeceraHilo`

Su hermana tiene un slot `ReactNode`. **Copiarlo habría dejado la promesa de
§3.2 —«la cabecera nunca dice el monto»— dependiendo de que nadie lo llenara
mal.** *Un slot es una escotilla: la ley que se puede saltear por un slot no
está puesta.* Por eso `R74` mide las dos cosas, y el segundo brazo es el que de
verdad cierra la puerta.

---

## ⑤ LO QUE EXTRAJE EN VEZ DE COPIAR, Y LO QUE ENMENDÉ

- **`FilaDeCabecera`** sale de `CabeceraHilo` (extracción interna, **cero
  cambio de API**: sus dos consumidores vivos de adopción no tocan una línea).
  *Dos cabeceras de hilo con la misma fila escrita dos veces son la Ley 19.9 un
  día antes de que se note.* Gana `detalle`, la segunda línea en voz de máquina
  —la fecha del objeto—, que adopción no usa.
- **`Texto` gana `tabular`** (aditiva, default `false`). No es «poner el número
  en mono»: la Ley 3 reserva la mono para metadata y esto es **voz humana con
  una cifra que cambia**. Su MATIZ ya resolvió el caso hermano a escala
  display. *Sin ancho fijo, al pasar de 14 a 13 la frase se corre unos píxeles
  y eso se lee como si la pantalla estuviera contando* — el countdown que §5
  prohíbe, por la puerta de atrás.
- **`BurbujaMensaje` gana `cara`** en la rama ajena, con su columna reservada
  para todo el grupo. ⚠️ **Y envolver en una fila tenía un precio escondido que
  se vio midiendo el caso SIN cara** (el de los dos hilos vivos): sin `flex: 1`
  el cuerpo se encoge a su contenido y `flex-end` deja de empujar los míos a la
  derecha. *La cura de una pieza compartida se mide primero contra sus
  consumidores viejos.*
- **`candado`**, el único glifo dibujado. Su censo de metáforas descartó
  `caso` (es la carpeta CLÍNICA — `D-976`) y `nodoEntregado` **por vecindad, no
  por significado**: la etapa de al lado es `checkEnCirculo`, y *dos checks
  seguidos no dejan distinguir «se decidió» de «se terminó»*.
  🔴 **GATE POR ÍCONO PENDIENTE** (§2.9: a su tamaño y a 21 px).

---

## ⑥ NÚMEROS DEL CIERRE

| | |
|---|---|
| `verify:diseno` | **VERDE · 66 reglas** (nacen `R72`–`R76`) |
| `verify:contrast` | **431 pares · 0 fallos** (2 pares nuevos, medidos) |
| `verify:fila-memoizada` | **7 casos** (los 3 de `mismaFila` + 4 de `mismoCaso`), y **se probó en rojo** rompiendo el comparador |
| typechecks | `ui` · `api` · `cliente` · `prestador` **en 0** |
| `verify:contador-piezas` | ✅ ninguna fuente escribe el número · **179 piezas · 122 wrappers** |
| `verify:glifos` | 62 verdes · 0 rojos |
| otros | `hooks` · `ref-antes-de-uso` · `hoisting-nativo` · `vio-todo` · `abanico` **verdes** |

⚠️ **`@epetplace/domain` sigue con su rojo PRE-EXISTENTE de S45** (`@types/emscripten`
del raíz). **Cero archivos míos ahí** — se declara para que nadie lo lea como
de este lote.

---

## ⑦ LO QUE **NO** HICE, DECLARADO

1. 🔴 **NADA EN APARATO.** Ninguna pieza se abrió en un teléfono. La casa exige
   captura en el emulador Android para *dibujo nuevo* —SVG, gradiente, sombra,
   máscara, animación— y el único dibujo nuevo del lote es **`candado`**, cuyo
   gate por ícono está pendiente igual. *Todo lo demás es composición con
   tokens vivos, y el gate de una pieza es la pantalla que la monta, no la
   galería* (`L-478`).
2. **Cero i18n.** Las ocho piezas reciben **todo texto por prop**: el lote de
   strings es de las apps y lo lee el founder aparte (§7 de la dirección).
   *No agregué una sola clave al namespace `ui` y es a propósito* — la familia
   y el prestador no leen la misma frase.
3. **No toqué `EscaleraEstados`, `EventoDelHilo` ni `BarraEscribir`.** Las tres
   ya hacían lo que la letra pide.
4. **No curé los cuatro `nodo*` de despensa** (`D-1006`): son de otro frente,
   tienen gate por ícono propio, y redibujarlos desde acá cambiaría lo que el
   founder ya aprobó.
5. **No medí `R75` contra los catálogos de las apps** — mide LA PIEZA. Si
   alguien agrega un motivo llamado «Otro» desde una pantalla, este guard no lo
   ve. *Se declara para que su verde no se lea de más.*

---

## ⑧ EL CONTRATO PARA C — lo que cada pieza espera y lo que NO hace

| pieza | lo que la pantalla tiene que traer | lo que la pieza **no** hace |
|---|---|---|
| `EscaleraCaso` | `etapa` y `final` **por separado** · las 5 voces · la línea de estado entera · `abierta`/`onAlternar` | no deriva la etapa · **no se colapsa sola al escribir**: eso es un hecho del campo y vive en la pantalla |
| `CabeceraCaso` | nombre + **fecha ya redactada** por el riel · los dos `onPress` (cada uno opcional) | no formatea horas · no muestra plata |
| `SelectorMotivo` | el catálogo **sin** la última fila · `elegido` arrancando en `null` · la voz de «contame» | no pide la foto (§2: se pide después y **se puede saltar**) |
| `TarjetaDestinoPlata` | las dos opciones con **la misma forma** · el monto ya redactado, con su porqué si es parcial | no calcula montos · no elige por vos |
| `BannerPlazo` | la línea entera, redactada | no cuenta el tiempo · **no decide cuándo dejar de mostrarse** (vencido, lo que va es el hecho del hilo, no el plazo) |
| `FilaBandejaCaso` | un `caso` con **referencia estable** entre refrescos y un `onPress` estable | 🔴 la memoización es **necesaria y no suficiente**: si la lista se arma con `.map(c => ({...c}))` se redibuja todo igual |
| `BurbujaMensaje` casa | `cara={<LogoNegocio nombre="e-PetPlace" tamano={CARA_EN_HILO} />}` | el tamaño **se pide**, no se teclea |
| `LineaAlgoSalioDistinto` | el estado ya resuelto (**la ventana de 7 días la dice el motor**) · el destino según ese estado · 🔴 **`enMemorial` OBLIGATORIA** = `estado_vida === 'fallecida'` (⚠️ **`perdida` NO cuenta**: la línea SE MUESTRA) | no deriva la vida de nadie · ⏪ *esta fila decía que su `null` en memorial era «un piso estructural». **Era falso** — ver la adenda ⑬* |

---

# ADENDA · LA SEGUNDA PUERTA, Y EL CONTADOR QUE NO ERA EL MISMO

## ⑨ ① · LA CLASE NO ERA DE MI PIEZA — CENSO DE `packages/ui`

**El censo por NOMBRE (`ORDEN_*`, `CATALOGO_*`, `REGISTRO_*`) da CUATRO. No es
la respuesta: mide la convención, no el hecho** — un array que hace de orden y
se llama `candidatas` o `LOTE` es invisible para él.

**Por FORMA —todo array cuyo tipo declarado es una unión cerrada de
literales— son ONCE.** Y al cerrar los dos ciegos del instrumento (arrays **sin
anotación** y el enumerado que **no es un array**) el total sube a **TRECE**:

| clase | n | cuáles |
|---|---|---|
| **exhaustivos** (deben estar completos) | **8** | `ORDEN` (ConvivenciaInput) · `ORDEN_CASO` · `ORDEN` (EscaleraSolicitud) · `ORDEN_CONVIVENCIA` (HojaFiltros) · `candidatas` (ModalDosAlturas) · `TIPOS_VIDA` (galería) · **`push:ClaseCoach`** y **`push:ClasePastilla`** (coach-geometria) |
| subconjuntos curados **a propósito** | 4 | `LOTE` · `LOTE3` · `NUEVOS_COACH` · `VECINOS_COACH` — los cuatro en la galería, lotes del gate por ícono |
| acumulador (nace vacío) | 1 | `vivas` en `clasesConAlgo` |

> ### 🔴 **LOS OCHO EXHAUSTIVOS ESTÁN COMPLETOS HOY. CERO ROJOS VIVOS.**
> **El hueco no está abierto: está esperando.** Ninguno se rompe hoy y ninguno
> avisa el día que alguien agregue un miembro a su unión. *Ése es exactamente
> el perfil del defecto que llega con el próximo cambio, no con éste.*

### La segunda puerta, y apareció midiendo — no leyendo

El primer instrumento miraba **arrays con anotación de tipo**. Al cerrar sus
ciegos aparecieron **dos formas más del mismo defecto**, y la segunda no es un
array:

**`clasesConAlgo` arma las cuatro `ClaseCoach` con cuatro `if` y cuatro
`push`.** Si mañana nace una quinta, **el orbe del Coach dibuja tres arcos
donde hay cuatro**: compila, no falla, y se ve perfectamente normal. *Es mi
`R72` con otra ropa, en una pieza que no toqué.*

⇒ *Un censo por la forma que uno tiene en la cabeza acota; no cierra.*

## ⑩ EL GUARD — **UNO**, `R77`, Y POR DECLARACIÓN

**Por qué uno y no cuatro:** `R72` mide UNA pieza y por eso pudo ser
específica. Cuatro reglas gemelas serían el clon que la Ley 19 caza un piso más
arriba, y **la quinta escalera que nazca no tendría la suya**.

🔴 **Por qué por DECLARACIÓN y no por derivación, y esto lo decidió la
medición:** de los trece sitios, **cinco están incompletos correctamente**
—cuatro lotes curados de la galería y un acumulador—. **Un guard que midiera
«¿está completo?» sobre la forma sola saldría rojo cinco veces sobre cinco
sitios sanos.** *La forma no distingue el defecto.* ⇒ la tabla es una
**clasificación de lo que el objeto ya contiene** (mismo precedente que
`verify:jornada-completa`).

**Sus tres rojos:**

| | qué caza |
|---|---|
| ① | un sitio `exhaustivo` al que le falta un miembro |
| ② | **un sitio que la tabla no clasifica** — *no dice «está mal»: dice «no puedo decir si está bien», y en un lint eso es rojo.* Un requisito no se pierde en silencio |
| ③ | una entrada de la tabla **cuyo sitio desapareció** — un lector que se fue deja la tabla afirmando algo sobre nada |

**ROJO PRIMERO, 4/4 sobre los archivos REALES** (mutados de a uno y
revertidos): el `ORDEN` de adopción sin `acta_firmada` · la cadena de `push`
del orbe sin `avisos` · un array nuevo sin clasificar · `candidatas` borrado.
Más dos fixtures por brazo, para que ② y ③ sigan probados en cada corrida.

⚠️ **Y un defecto del guard que vi en su propio rojo, no leyéndolo:** la
corrida arma su corpus con `ui` **más** `archivosCodigo('packages/ui/src')`, así
que **cada `.tsx` entraba dos veces y cada fallo se imprimía duplicado**.
Deduplicado por ruta. *Un guard que reporta dos veces el mismo defecto enseña a
leer sus números al descuido.*

**Lo que NO ve, declarado:** `switch` sin `never` exhaustivo · arrays armados
por `map`/`filter` · **el mismo defecto en `apps/`** (mira sólo `packages/ui`,
que es su territorio). *Su verde dice «lo que enumera a mano en `packages/ui`
está declarado y completo», jamás «nadie enumera a mano en ningún lado».*

## ⑪ ② · EL CONTADOR DE 53 — **NO ES EL MISMO, Y LA PREMISA SE CORRIGE**

Lo medí antes de anotarlo, y el objeto corrige el encargo en dos puntos:

**① El `53` del canon YA ESTÁ CURADO.** Sus dos apariciones en `CLAUDE.md`
(líneas 667 y 687) son **CITAS**: una vive dentro de la línea que declara el
comando, la otra dentro de su lección. Son la evidencia de por qué se curó —
*borrarlas dejaría la cura sin su razón*.

**② El que estaba VIVO era otro, en otro archivo y en otra forma.**
`packages/ui/CLAUDE.md` publicaba **DOS**:

```
línea   3  **Estado: S103 — 81 componentes…**      ← el encabezado
línea 210  - **49 componentes** (`src/components/`) ← el inventario de «Qué hay»
```

> ### 🔴 Y EL HALLAZGO NO ES EL NÚMERO: ES QUE EL GATE ESTABA VERDE.
> `verify:contador-piezas` **mira ese archivo** — está en su corpus, con nombre
> y todo — **y no veía ninguno de los dos**, porque vigilaba **una sola forma**
> (`tokens v4 + **N componentes**`) y acá el número se publicaba en otras dos.
>
> *Su verde significaba «ninguna fuente escribe el número EN LA FORMA QUE
> MIRO», jamás «ninguna fuente lo escribe»* — y entre esas dos frases vivían
> **122 y 130 de brecha**. **Un gate que acota su pregunta al formato que su
> autor tenía en la cabeza no cierra la clase: la recorta.**

**⇒ SON DOS CURAS, y la adenda acertó el número aunque no el sujeto:**
la del lote (la skill, `81`) y **ésta** (`packages/ui/CLAUDE.md`, `81` y `49`).
**Más una tercera que no se pidió: el gate.** Sus dos patrones nuevos se anclan
a **la oración que declara el estado**, no al formato — porque el archivo está
lleno de bandas históricas que citan números (`= **85**, menos **3**`, `52 →
53`, `decía 63`) **y ésas son la evidencia de cada cura**. El `--control` pasó
a **cinco casos, cada uno con su NEGATIVO**, y los dos brazos nuevos se
probaron **en rojo sobre el archivo real**.

## ⑫ NÚMEROS DE LA ADENDA

| | |
|---|---|
| `verify:diseno` | **VERDE · 67 reglas** (nace `R77`) |
| `verify:contador-piezas` | ✅ · `--control` **5/5 con su negativo** · **179 piezas · 122 wrappers** |
| `verify:contrast` | 431 pares · 0 fallos |
| typechecks | `ui` · `api` · `cliente` · `prestador` en 0 |
| rojos probados sobre archivos REALES | **4** de `R77` + **2** del contador (más los 11 del lote) |

---

# ADENDA 2 · EL PISO QUE NO SE ENCENDÍA, Y SUS ONCE HERMANOS

## ⑬ MI PROPIO GUARD ESTABA APAGADO — y no lo encontré yo

El parte de arriba dice, con todas las letras: *«🔴 **En memorial devuelve
`null`** — piso estructural, no un recordatorio»*. **Era falso.**

**Medido (`D-1021`, lo halló C montando, no leyendo): nadie monta
`<ThemeProvider memorial>` en ninguna de las dos apps.** El único provider vivo
es el raíz, con `mode={light|dark}`. *El tema memorial existe en `packages/ui`
desde S43 y jamás se usó en producto.*

**El guard estaba bien escrito y colgado de un interruptor que nadie aprieta.**
La clase **está depositada como `L-498` por firma del founder** —*lo que parece
cuidado es lo que impide que alguien mire*— con sus dos casos de esta misma
sesión: este `return null` y el contador con su nota «re-medido» al lado.
*Acá queda el hecho medido; el porqué general vive en el canon, que es donde
alguien lo va a leer sin tener que abrir el parte de una pista.*

### La cura, y una distinción que la casa no tenía resuelta — hoy FIRMADA

`enMemorial: boolean`, **obligatoria sin default** (19.9). La pieza está
entregada y no montada, así que exigirla no rompe a nadie **y obliga a
decidirla**.

🔴 **Y al escribirla apareció que la casa tiene DOS reglas vivas de «es
memorial», y no coinciden.** Medido en `apps/cliente`:

```
hogar/index · [mascotaId] · vacunas · atajos.ts
     → estado_vida !== null && !== 'activa'        (perdida ES memorial)
pasaporte
     → … && !== 'perdida'                          (perdida NO lo es)
```

🔴 **FIRMA DEL FOUNDER (7-sep-2026): manda la segunda — `perdida` NO es
memorial y la línea SE MUESTRA.** *Una familia cuyo animal se perdió sigue
pudiendo decir que el paseo salió distinto: apagarle el reclamo sería
castigarla por su pérdida.* **Con ella queda ratificada también la forma de la
prop: booleana, no `estado_vida`.**

⇒ **por eso la prop es un booleano y no `estado_vida`.** `packages/ui` no
depende de `packages/api` (verificado en su `package.json`: depende de
`domain` e `i18n`), así que el vocabulario no le llega — **y aunque le
llegara, la pieza tendría que elegir una de las dos reglas por su cuenta, y
cuál corresponde depende de para qué es la pantalla.**

🔴 **`theme.mode` se CONSERVA en el `OR`** — la misma cura que C escribió en la
ficha: **la galería sí monta el sub-tema**, y ahí el guard tiene que seguir
valiendo. *Lo que estaba mal no era mirar el tema: era mirar SÓLO el tema.*

**Y la galería ganó su DISCRIMINADOR:** un cuarto montaje con `enMemorial` que
**no dibuja nada**. *Un guard que sólo se ve encendido no se puede juzgar
apagado.*

## ⑭ EL CENSO DE LA CLASE — 60, pero la clase son 11

**`mode === 'memorial'` en `packages/ui`: 60 usos en 51 archivos.** Contarlos
no es la respuesta; hay que clasificar **por lo que cada uno CONTROLA**:

| clase | n | qué cuesta su inercia |
|---|---|---|
| color y superficie | ~8 | **estética**: la pieza no degrada |
| movimiento | ~17 | **estética**: la pieza anima donde la Ley 6 dice que no |
| 🔴 **EXISTENCIA** (`if (…) return null`) | **11** | **respeto**: el pedido no se apaga |
| otros / galería | resto | — |

> ### 🔴 LA CLASE SON LAS ONCE, Y CADA UNA LLEVA SU PORQUÉ ESCRITO
> ```
> PastillaConociendolo  «En memorial no se pide terminar de contar nada»
> ChipsSugerencia       «El Coach no existe en memorial»
> AvisoAnticipacion     «Nada que adelantar cuando ya no hay mañana»
> TarjetaHoy            «No hay un "hoy" que resolver»
> ```
> **La casa escribió la protección once veces, con su razón en negrita, y las
> once están apagadas.** Es el mecanismo exacto de `D-1021`: *a una familia que
> perdió a su animal, la app le pidió el peso de hoy* — y el chip
> «Conociéndolo · 1 por resolver» que la captura muestra sale **de una de estas
> once**, con su `return null` escrito y sin encender.

**Las otras 49 NO entran a la regla, y es una decisión:** degradar un color o
apagar una animación **es para lo que existe un tema**, y su inercia cuesta
estética. *Un guard que midiera los 60 confundiría «este color no degrada» con
«la app le pide algo a quien perdió a su animal», y son dos cosas de distinto
precio.*

## ⑮ `R78` — el guard, y su verde dice MENOS de lo que parece

**Ratchet SOLO-BAJA en 11.** Su rojo: la pieza número doce.

**Probado en las DOS direcciones sobre archivos reales:**
- ① agregar un `return null` por tema a una pieza nueva → **11 → 12, ROJO**;
- ② **desandar mi cura** (sacarle el `enMemorial ||` a `LineaAlgoSalioDistinto`)
  → **11 → 12, ROJO**. *Ése es el discriminador: la regla no cuenta
  `mode === 'memorial'`, cuenta **el tema como ÚNICA señal** — y distingue una
  pieza curada de una que no lo está.*

⚠️ **Su verde dice «no creció», JAMÁS «las once andan».** Las once **siguen
inertes** y esta regla **no las cura: impide la doce.** Curarlas exige que cada
pieza reciba su señal **y que su pantalla se la pase** — dos territorios, tanda
propia. *Se escribe así en su `info` para que el número no se lea como salud:
es exactamente el «guard verde que significa menos de lo que su nombre dice»
que la adenda nombró, y la única forma honesta de tenerlo es que lo diga él
mismo cada vez que corre.*

## ⑯ ③ · EL BARRIDO DE `R77` SOBRE `apps/` — RECIBIDO, NO EJECUTADO

Tanda propia, después de S114. **No lo abrí y no lo miré**: `R77` sigue
declarando en su `info` que **no ve `apps/`**, así que su alcance está escrito
en el propio gate y no depende de que alguien recuerde esta línea.

## ⑰ NÚMEROS DE LA ADENDA 2

| | |
|---|---|
| `verify:diseno` | **VERDE · 68 reglas** (nace `R78`) |
| clase medida | **60 usos · 11 de EXISTENCIA · 2 ya curados** (`Atmosfera` y el mío) |
| rojos probados sobre archivos reales | **2** de `R78`, uno por dirección |
| al canon | **`L-498`** — *lo que parece cuidado es lo que impide que alguien mire* (firma del founder) |
| firmas ratificadas | **`perdida` NO es memorial** en la puerta de postventa, y con ella la forma booleana de la prop |
| `verify:contrast` | 431 pares · 0 fallos |
| typechecks | `ui` · `api` · `cliente` · `prestador` en 0 |

**Total de la sesión: 19 rojos probados mutando archivos reales** (11 del lote
· 4 de `R77` · 2 del contador · 2 de `R78`).

---

# ADENDA 3 · EL CONTRATO PARA C — y una corrección de mi propio censo

**Todo el detalle vive en `docs/loop/S114-B-CONTRATO-MEMORIAL.md`.** Acá el
titular, porque son dos cosas que cambian lo que C tiene que hacer.

## ⑱ B8 le sirve a C — con tres avisos, y el tercero es letra

1. 🔴 **Ninguna de las tres pantallas del §1 tiene `estado_vida` hoy** (0 hits
   en las tres). *Si al montar se pasa `false` por comodidad, se reintroduce
   exactamente el guard apagado que la prop vino a curar* — `L-498`, esta vez
   con la lección ya escrita al lado.
2. ✅ **Las tres SÍ tienen con qué cruzar y no hace falta motor nuevo:**
   `ParteConsulta.mascotaId` ya está en el tipo · las otras dos ya usan su id ·
   y **`obtenerMascotasDeFamilia` trae `estado_vida`** — la pantalla del pedido
   **ya la llama**. **Y la segunda puerta está cerrada, verificada: ese lector
   NO filtra a las memoriales**, así que el cruce por id las encuentra.
3. ✅ **FIRMADO (7-sep): el PEDIDO nunca se apaga — es del HOGAR.** Precedente
   `bonos.familia_id`. **Y la letra pasó al TIPO:** `sujeto: 'mascota'` exige
   `enMemorial`; `sujeto: 'hogar'` la vuelve **inexpresable**. *Las dos
   direcciones son errores de compilación* — el pedido no puede pasarla, la
   cita no puede olvidarla. **Un `false` por decisión y uno por comodidad se
   ven idénticos; la única forma de que el segundo no vuelva es que no se pueda
   escribir.** El `theme.mode` también cuelga del sujeto: *la letra no se
   cumple a medias por un `OR` heredado.*

## ⑲ Las once son **dos archivos y una decisión**

Contar piezas no dice el trabajo; **contar puntos de montaje sí**:

| grupo | n | dónde | costo |
|---|---|---|---|
| el Coach | **5** | `nexo.tsx` | 🟢 **cero props**: se curan en `focoNexo`, que es la mitad ② de `D-1021` y ya tiene dueño |
| la ficha | **4** | `[mascotaId].tsx` | 🟡 una prop c/u, **y el booleano ya está calculado ahí** (`:546`) |
| importada y no montada | 1 | — | ⚪ nada hoy · su import muerto es de C |
| cromo global | 1 | `MarcaDeAgua`, ~90 pantallas | 🔴 **no tiene mascota**: la única que de verdad depende del tema |

## ⑳ Y ME CORREGÍ ANTES DE ENTREGAR

La primera versión de esa tabla decía **«nadie la monta»** sobre
`PastillaConociendolo`, con `grep -rl "<Pieza"` como evidencia. **Al verificar
apareció importada** — usada cero veces, así que el hecho no cambió, **pero la
evidencia era incompleta y quien grepeara el nombre habría concluido lo
contrario.**

**Al canon como `L-499` por firma del founder** — y lo que la vuelve regla no
es mi error: es que **la clase cruzó de subsistema**. En SQL era `L-170`/`L-437`
(el censo por `pg_get_functiondef`), en git `L-494` (`ls-tree | grep` mide el
nombre del archivo), y acá en JSX. *Tres lenguajes, tres instrumentos, el mismo
error: preguntarle a la superficie del código por un hecho que vive un piso más
adentro.* **Mientras vivía en SQL se leía como peculiaridad de `pg_proc`; al
aparecer tres veces es del método.**

---

# ADENDA 6 · EL PISO DE `R78` SE IMPRIME, Y EL CONTADOR QUE SE CEGÓ SOLO

## ㉑ El piso vive en el gate, no en este parte

`R78` ahora imprime en **cada corrida**:

```
🔴 PISO 2, NO 0 — MarcaDeAgua (duro) · PastillaConociendolo (blando):
   NO se curan con una prop y bajar de ahí NO es deuda
```

*Un ratchet que sólo dice «baseline 11 SOLO-BAJA» se lee como «faltan once».* **Y
se VERIFICA, no se recuerda:** bajar el baseline por debajo del piso **sale
rojo** (probado), y si una del piso desaparece de la medición la salida pide
confirmar que se curó de verdad **y no con una señal inventada**.

⚠️ **El brazo del piso NO tiene fixture y no puede tenerlo** — depende de una
constante del módulo, no del corpus. **Se probó en rojo editando la constante** y
**se declara en la regla en vez de fingir cobertura**: *un brazo sin fixture
declarado vale más que uno con un fixture que no lo ejecuta.*

## ㉒ 🔴 MI PROPIA CURA CEGÓ AL CONTADOR DEL GATE, y lo dijo un número

Al pasar B8 a la unión de sujeto, su guard quedó **anidado**:

```js
if (esDeUnaMascota && (props.enMemorial || theme.mode === 'memorial')) return null
```

El matcher de `R78` era `\(([^)]*mode === 'memorial'[^)]*)\)` — **`[^)]*` corta
en el primer paréntesis**, así que **mi pieza desapareció de la medición**.

**El brazo del rojo seguía andando** —revertir el guard a sólo-tema sigue dando
12 y rojo, verificado— **así que la regla no estaba rota: contaba de menos.** Y
lo único que lo dijo fue **`conDato`, que bajó de 2 a 1**.

> ### Ésa es la razón por la que su `info` lleva números y no adjetivos: un contador que baja sin que nadie lo haya tocado es la única señal que un gate correcto puede dar de que se quedó ciego.

Curado con un `.*?` anclado en `) return null`. **Los tres brazos re-probados en
rojo** después del cambio — *la prueba vieja quedó vencida al tocar el regex, y
una prueba vencida no se hereda.*

---

# ADENDA 7 · `L-500`, Y APLICADA A MIS PROPIOS GATES

## ㉓ La lección, con las dos mitades y su atribución

> ### `L-500` — **UN GATE PUBLICA CIFRAS, NO ADJETIVOS: «verde» no puede delatar ceguera, un contador sí**

Nace de **dos pistas el mismo día con el mismo error y distinta ropa**:

| | el error | qué lo cazó |
|---|---|---|
| **F** *(citado de `S114-F-TANDA1.md` — **lo midió F, no yo**)* | `if [ "$H" != "hash" ]`: **una cadena vacía también es distinta** ⇒ *«un comparador de desigualdad trata «cambió» y «no pude leerlo» como la misma cosa»* | el mensaje **imprimía el hash y salió en blanco**: `✅ DEPLOY NUEVO ·  · 22:02:00`. F lo dice mejor que yo: *«si sólo hubiera dicho "DEPLOY NUEVO", me lo creía»* |
| **B** | `[^)]*` corta en el primer paréntesis ⇒ al curar una pieza con condición **anidada**, `R78` dejó de verla | **`conDato` bajó de 2 a 1** sin que nadie tocara ese sujeto |

🔴 **Lo que los hermana es más incómodo que el error: en los dos el instrumento
seguía dando el VEREDICTO correcto.** El watcher habría anunciado un deploy
real; `R78` sigue enrojeciendo ante la regresión. *Lo que se rompió no fue la
respuesta: fue de cuánto la estaba sacando* — **y una pérdida de alcance no
tiene síntoma.**

## ㉔ Y la apliqué a mis propios gates, que la violaban

**Tres de las cinco reglas que escribí esta sesión publicaban sólo ceros de
resultado:** `R74` (*0 menciones de plata · 0 slots*), `R75` y `R76`. *Un `0`
que siempre fue `0` y un `0` que acaba de quedarse ciego se imprimen idénticos.*

Ahora las tres publican **una cifra de ALCANCE que se mueve sola**:

```
R74 · 10 prop(s) miradas — el universo donde el monto podría entrar
R75 · 9 campo(s) del vocabulario · 1 tocable(s) dibujados
R76 · 1 lectura(s) del tema — y con ANCLA: sin ninguna, ROJO
```

*Una lección escrita por su autor y no aplicada por él es exactamente la forma
que `L-498` nombra.* **Los cuatro brazos tocados, re-probados en rojo** — la
prueba vieja quedó vencida al editar la salida.

---

# ADENDA 8 · LA SEGUNDA REGLA VIVA — y son TRES, en tres capas

**Detalle en el contrato (§③bis).** C tenía razón: en Sombra las piezas ya
estaban apagadas por el `{!esMemorial …}` de la pantalla. **Y el censo da algo
más grande que «dos reglas».**

```
servidor  · la edge devuelve `codigo === 'memorial'`   → el nexo hace router.back()
pantalla  · estado_vida !== null && !== 'activa'       → la ficha apaga (~17 usos)
pieza     · theme.mode === 'memorial'                  → INERTE
```
**Y con el pasaporte —que además excluye `perdida`— son CUATRO definiciones del
mismo hecho conviviendo.**

**Medido por balance de llaves, no por ventana:** los **10 montajes** de la
ficha caen dentro de un condicional `esMemorial` · `nexo.tsx` **no tiene
`esMemorial`** (su regla es un rebote desde el servidor) · `MarcaDeAgua`:
**cero de sus 85 montajes gateado.**

> ### 🔴 Las nueve están protegidas por la capa que SÍ se enciende, no por la que dice protegerlas.
> *El apagado que C vio es real y no es de la pieza.* Si una se monta en otra
> pantalla —o el condicional se refactoriza— **la protección desaparece, y el
> guard de la pieza no la salva porque está inerte.**

## ⚠️ Y la conclusión obvia era la equivocada: **ese condicional NO se borra**

Lo medí antes de recomendar. **Dos de los tres condicionales apagan una SECCIÓN
ENTERA** —uno envuelve **cinco** `TarjetaHoy`, otro envuelve
`TarjetaConociendolo` **+** `BotonContanos`—. **Eso la pieza no lo puede hacer:
no sabe que tiene hermanas.**

⇒ **no son duplicados: son capas anidadas y las dos hacen falta.** La pantalla
apaga *lo que la sección significa*; la pieza **se apaga a sí misma y viaja con
ella**. *El defecto no es que haya dos — es que la de adentro está apagada.*

**La única candidata real a morir es la de `HojaContanos`**, que envuelve una
sola pieza y dice literalmente lo mismo que su guard. *Decisión de C sobre su
archivo, no mía.*

---

# ADENDA 9 · EL ÚLTIMO CENSO DE LA CLASE (contrato §⑥ y §⑦)

## ㉕ En `packages/ui`, **cero piezas derivan «es memorial»**: lo reciben

| pieza | forma | ¿exige la señal? |
|---|---|---|
| `LineaAlgoSalioDistinto` | unión de sujeto | 🟢 obligatoria · inexpresable en el pedido |
| `FichaMascotaHogar` | `enMemoria?` **`\|\|` `theme.mode`** — *mi mismo patrón, y ya existía* | 🟡 **opcional** ⇒ sin ella cae al tema, que está inerte |
| `TarjetaPasaporte` | `enMemoria?: boolean = false` | 🔴 **default `false`**: el guard apagado por omisión, escrito |

**Lo que queda mal con la firma no está en las piezas: está en quien calcula el
dato.** `hogar/index.tsx` (2 sitios) y `[mascotaId].tsx:961` usan
`!== 'activa'` ⇒ 🔴 **una mascota PERDIDA se dibuja como memorial**.
`pasaporte.tsx` es **la única que cumple**.

⚠️ **Falso positivo de mi propio censo, declarado:** las 29 `M6` de
`packages/ui` **son paths SVG**. *`L-499` otra vez, el mismo día.*

> ### Y la salvedad que el censo obliga a decir
> **`perdida` no es memorial — pero tampoco es `activa`, y la casa sólo tiene
> DOS tratamientos.** *Aplicar la firma al Hogar no arregla el caso: lo mueve
> del tratamiento equivocado a otro tratamiento equivocado.* **Eso es letra.**

## ㉖ Dónde debería vivir la definición — **el lugar ya existe**

🔴 **`packages/domain` está VIVO, lo consumen LOS CUATRO paquetes, y
`momentoVital.ts` ya recibe `esMemorial` como ARGUMENTO** (`if (entrada.esMemorial) return 'M6'`).
**La casa ya trató este dato como de dominio: `domain` lo consume y nunca lo
definió.** *El lugar no hay que elegirlo — hay que llenarlo.*

Las cuatro opciones con su costo están en el contrato §⑦. **Mi voto: `domain`,
con el motor como su espejo** — y su consecuencia dicha: *si el motor y `domain`
deciden lo mismo por separado van a divergir, y la única forma de que no pase es
que alguien lo MIDA.* **Eso es lo que no existe, y es por lo que hay cuatro
definiciones.**

### 🔴 Y la forma importa más que el lugar: **no un booleano**

**Un booleano es lo que causó la divergencia** — obliga a cada llamador a
decidir por su cuenta dónde cae `perdida`, y **cuatro decidieron distinto**. Una
unión de tres (`'activa' | 'perdida' | 'memorial'`) **no deja colapsarla sin
escribirlo**. *Es la misma cura que `R77` pide para los órdenes y que la unión
de sujeto aplicó al pedido: volver la pregunta inexpresable de contestar mal.*

---

# ADENDA 10 · TRES FIRMAS, UNA CURA Y LA PROPUESTA DEL ESPEJO

**Detalle en el contrato §⑨ · §⑩ · §⑪.**

## ㉗ Firmado: la forma, el lugar y qué es `perdida`

`EstadoVidaMascota = 'activa' | 'perdida' | 'memorial'` en **`packages/domain`**,
con el motor como espejo. **Y `perdida` es su propio estado:** *«la app entera
disponible, sin ninguna pieza de duelo, sin apagar el Hoy ni el Coach — quien
está buscando a su animal necesita el producto funcionando»*. Su única
superficie propia es **el pasaporte en modo «se perdió»**.

⇒ **la tabla de qué NO apagar está en el contrato §⑨**, para que C la monte
contra letra. *`estado_vida === 'perdida'` no alimenta `enMemorial`/`enMemoria`
en NINGUNA pieza* — y los dos sitios que hoy lo hacen (`hogar/index.tsx` ×2,
`[mascotaId].tsx:961`) quedan nombrados.

## ㉘ Curado: `TarjetaPasaporte`

`enMemoria?: boolean = false` → **obligatoria sin default**, con el patrón de B8.
**Rompió exactamente un sitio y la variable ya existía ahí**, con la regla que la
firma ratificó. ⚠️ **Cruce declarado y mínimo:** el cambio de API es mío; pasar
la prop es su única consecuencia — *dejar el árbol sin compilar para que otro
escriba una palabra es peor que escribirla.* **Los cuatro typechecks en 0**, y
la galería ganó el discriminador: la tarjeta **`perdida` SE DIBUJA** al lado de
la que no.

## ㉙ Cómo se mediría el espejo — **dos gates, partidos por la red**

🔴 **Lo primero: los dos vocabularios NO son la misma palabra.** El motor
**almacena** `fallecida`; el dominio **clasifica** en `memorial`. *`fallecida` es
el hecho; `memorial` es lo que la casa hace con él* — **y esa traducción es la
costura donde van a divergir.**

- **① la tabla de verdad** (sin bautizar: no existe, y su sujeto tampoco): para cada valor del
  CHECK, qué dice el motor y qué dice `domain`. Rojo si difieren **o si el CHECK
  tiene un valor que nadie clasificó**. **Publica cuántos valores comparó**
  (`L-500`): *si el CHECK crece y el gate sigue comparando tres, el número lo
  delata; un «coinciden» no puede.* Exige la DB ⇒ **paso ⓪ y cierre**, no el
  hook.
- **② `verify:memorial-derivado`** ✅ **construido** — ratchet **sin red, al hook**: nadie fuera de
  `domain` compara contra los literales para decidir memorial. Baseline = los
  sitios que el censo ya nombró; **cada cura lo baja sola.**

⚠️ **Y lo que ninguno resuelve:** la edge del nexo decide en su propio idioma.
El espejo cubre **motor↔dominio**; que la edge use la clasificación del motor
**es cableado, no medición** — y es de A.

## ㉚ `L-499` gana su tercer caso

Las **29 `M6`** que conté como momentos vitales **eran paths SVG**. *Un token de
dos caracteres no tiene contexto: contarlo mide su ortografía.* **Tres veces la
misma clase en un día, al mismo autor** — y eso es lo que la separa de un
descuido: *el instrumento barato siempre está a mano y siempre contesta algo.*

---

# ADENDA 11 · EL PEDIDO A C, `L-501`, Y LA CUARTA CAPA CON DOS HABITANTES MÁS

## ㉛ ① El rojo del identificador — **pedido al buzón, patrón NO escrito**

`docs/loop/buzon/S114-B-para-C-los-tres-literales-del-identificador.md`.

**Busqué primero en la rama de C para no hacerle trabajo:** su parte (319
líneas) menciona `resuelto_entre_partes` vs `_ustedes` **como divergencia de
vocabulario con A, no como el defecto de render**. Los tres casos como tales no
están escritos. *Por eso los pido en vez de deducirlos: **un gate construido
sobre mi paráfrasis mide mi paráfrasis**.*

**Y le pregunté lo que decide la FORMA del patrón:** `paseo` y `calidad` son
valores del motor sin traducir; **la llave de i18n puede ser otra clase** —ahí
el fallo no es que el dato llegue crudo, sino que `t()` devolvió su propia
llave—. *Si son dos clases, el gate lleva dos brazos con dos rojos, y quiero
saberlo antes de escribir uno que los mezcle.* **Sin su respuesta no escribo el
patrón.**

## ㉜ ② `L-501` al canon

> **El delimitador aparece adentro de lo que querés capturar** — `[^)]*` en
> `R78`, el `{` de una anotación de tipo acá. **Dos de la forma exacta**, y el
> conteo va sin inflar (`L-499` el mismo día): *decir «tres iguales» habría sido
> más contundente y menos cierto.*

🔴 **Y la peor es la CURA sin techo**, que caminó 286 líneas y **acusó a un
archivo de otra pista**. En la palabra del founder:

> ### **Un instrumento que acusa a otro merece un techo antes que un patrón mejor.**

*El primer defecto contaba de menos; el segundo acusaba de más.* **Un falso
negativo cuesta una medición; un falso positivo cuesta el tiempo de otro y su
confianza en el instrumento** — y quien la recibe no tiene cómo distinguirla de
una real: le llega con `archivo:línea`, en su archivo, de un gate que corre.

## ㉝ ③ No eran cuatro capas — al contrato §⑥bis

**Dos de las nueve derivaciones viven en `packages/api`**
(`adiestramiento-antes:195` · `grooming-atencion:397`) ⇒ **la capa que alimenta
al PRESTADOR también decide memorial por su cuenta.**

*Mi censo de §⑥ miró `packages/ui` porque eso fue lo que se pidió, y su
resultado era cierto **para `packages/ui`**. Lo que no vi es que la pregunta
tenía otra mitad al lado.* **Los encontró el gate, no la lectura.**

✅ **Y lo que la firma del lugar gana:** `packages/api` **ya depende de
`domain`**, así que esas dos pueden consumir la definición única **sin ninguna
capa nueva**.

---

# ADENDA 12 · `R79` — EL BRAZO ESTÁTICO, CONSTRUIDO DESDE LOS CASOS DE C

## ㉞ 4/4 contra los objetos reales, no contra un fixture mío

```
✅ ROJO  ① ANTES   6982e17e~1  caso/[casoId].tsx:300 · objeto.titulo
✅ verde ① DESPUÉS 6982e17e    (pasa por `vozServicio`)
✅ ROJO  ③ ANTES   f65ca58c~1  negocio/casos.tsx:81  · c.motivo
✅ verde ③ DESPUÉS f65ca58c    (`vozDeMotivo[c.motivo] ?? c.motivo`)
```

**Las líneas coinciden exactamente con las que C reportó** (300 y 81) — *y eso
también es una medición: si el gate señalara otra línea, estaría cazando otra
cosa.*

## ㉟ Lo que la construcción se cobró — TRES defectos, los tres del rojo

**Ninguno lo encontró leer el código.** Los tres salieron de correr la regla
contra los archivos de C:

| | el defecto | qué lo destapó |
|---|---|---|
| ① | **el nombre del campo no es ancla**: `.titulo` da **50** ocurrencias (casi todas llaves de i18n) y **38** aun despojando strings | correr el primer patrón: *marcaba `carnet.titulo`, `avisos.titulo`, `parte.motivo`* |
| ② | **la línea estaba corrida**: el `sinComentarios` compartido colapsa líneas | señaló **259** un defecto que vive en la **300** |
| ③ | 🔴 **el `enRiel` cruzaba de sentencia**: el `t(` del renglón de ARRIBA quedaba a <80 caracteres del `c.motivo` de abajo ⇒ **el caso real de C salía VERDE** | el rojo de ③ que no llegó |

> ### ③ es `L-501` en su forma exacta, y es la **cuarta** vez en la sesión.
> *El delimitador —acá el paréntesis de otra sentencia— aparece adentro de la
> ventana.* **Curado con un balance acotado AL RENGLÓN, que no puede cruzar.**

## ㊱ El borde es el RENGLÓN — ni menos ni más, y las dos mitades se midieron

- **menos** (por ocurrencia) → **rojo sobre la línea YA CURADA**: `vozDeMotivo[c.motivo] ?? c.motivo`. *C me avisó de esto antes de que lo escribiera, y su rojo lo confirmó.*
- **más** (sentencia de 4 líneas) → **verde sobre el caso real**.

⇒ *si el campo pasa por un riel **en su propio renglón**, lo desnudo de al lado
es la salida honesta; si no, es el defecto.*

## ㊲ Y hoy **NO ESTÁ MIDIENDO**, y lo dice en cada corrida

Las dos rutas declaradas viven en la rama de C: en `main` el alcance es **0**.

```
🔴 NO ESTÁ MIDIENDO (0 ocurrencias de las rutas declaradas…) · alcance: 0
```

**No frena el commit** —bloquear por un corpus que todavía no llegó castiga a
todos por algo que nadie rompió— **y el número pasa a N el día que su lote
merge**: ése es el salto que delata si la regla empezó (o dejó) de mirar
(`L-500`).

**Convive con el `missingKeyHandler` de C y no lo duplica:** *el mío no ve una
llave armada en ejecución; el suyo no corre en CI.* **C lo probó midiendo:
ninguno de los dos caza al otro.**

---

# ADENDA 13 · LA OTRA MITAD DE LA FIRMA, Y EL CONTEO DE `L-501`

## ㊳ ① Lo que hay que leer DESPUÉS del merge, escrito en la regla

La firma tiene dos mitades y la segunda es la que se olvida:

> **No frena el commit** — *bloquear por un corpus que todavía no llegó castiga
> a todos por algo que nadie rompió.*
>
> ### 🔴 **Y si el salto de 0 a N NO ocurre cuando el lote de C entre a `main`, la regla dejó de mirar.**

**No sería «no hay defectos»: sería que las rutas declaradas dejaron de existir
con ese nombre** —un rename, una pantalla que se movió, un campo que cambió— **y
su `0` volvió a significar «no vi».** *El día del merge, ese número es lo único
que hay que mirar en `R79`: si sigue en 0, lo roto es la regla y no el código.*

**Queda escrito en la regla misma**, no acá: *un parte no se relee el día del
merge; la salida del gate sí.*

## ㊴ ② `L-501` gana la cuarta, con el conteo exacto

| | |
|---|---|
| **del mecanismo exacto** | **TRES** · `[^)]*` en `R78` · el `{` de la anotación de tipo · `[^;]{0,80}?` en `R79` |
| **de su familia** (falta el techo, no hay delimitador) | **UNA** · la ventana de 286 líneas |
| **de la clase** | **CUATRO**, en cuatro instrumentos distintos, **todos míos y del mismo día** |

*Decir «cuatro iguales» habría sido más contundente y menos cierto: la del techo
no comparte mecanismo — comparte consecuencia.*

⚠️ **Y una distinción para que no se mezclen los números:** las cuatro de
`L-501` son **instrumentos míos de una sesión**. La que **cruzó de subsistema**
es `L-499` —SQL · git · JSX · SVG—, y ésa es *su* propiedad distintiva.
**Las dos tienen cuatro y no son el mismo cuatro.**

---

*Pista B · S114 · las ocho entregadas y no montadas · `verify:diseno` verde con
66 reglas · contraste 431/0 · los 11 brazos de los cinco guards probados en
rojo sobre los archivos reales.*
