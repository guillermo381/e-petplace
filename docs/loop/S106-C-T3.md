# PARTE DE LA PISTA C · S106 · TANDA 3

**Worktree:** `../e-petplace-s106-c-t3` · **rama:** `pista/s106-c-t3`
**Base:** `main = cccd53cc` · **rama en origin verificada por SHA** al abrir.

**Territorio:** `apps/cliente` · `apps/prestador`.
**Frenos permanentes:** la DB la escribe sólo A (todo SQL sale como pedido
autocontenido, 76b) · piezas de B por pedido, jamás inline · ninguna key de
cuenta en el repo.

---

## ⓪ · OBRA 0 — EL RECORRIDO DEL SERVICIO ENTERO ✅ DEPOSITADA

`docs/relevamientos/2026-08-26-s106-c-t3-RECORRIDO-DEL-SERVICIO.md`

Los dos recorridos completos en voz de usuario, con cada paso medido y con
dueño. **Lo que sale de caminarlo:**

**Desbloqueado y listo para construir**
- **Obra 1 · las dos puertas** — el bloqueo que la asignación presumía **no
  existe** (§D1): la fila no sale de `leerCitaResuelta`, y el hecho ya viaja
  como `tipo_servicio` porque el propio contrato de A declara que *«la
  modalidad se deriva del tipo de servicio, server-side»*.
- **Obra 2** — la reserva viaja sola por el flujo existente (la pantalla lee
  del motor, no de una lista escrita); lo único de superficie es **la voz**:
  `vozServicio` no conoce `'telemedicina'` y la fila saldría muda.
- **Obra 4** — el silencio: sin dependencias.
- **La insignia en la jornada del vet** — no estaba en la asignación y es un
  paso real; `CitaAgendaPaseo` ya trae el dato y `Insignia` arma su etiqueta.

**Frenado con su medición**
- **Obra 3 · los consejos de preparación** — 🔴 **el texto no está en el
  repo.** Búsqueda ancha con control positivo (los seis signos de §3 sí
  aparecen). `§1` de `LETRA_TELEMEDICINA` es otra cosa, leído entero. *No
  invento consejos sobre una consulta médica.*
- **Obra 5 · el estado de certificación** — 🔴 `CodigoErrorTelemedicina` no
  tiene ningún código de verificación; sus nueve hablan de otra cosa.
  Espera A (§E2) y el estado de B.
- **La marca §7 en el expediente** — 🔴 `LineaDeVida` de B **ya acepta
  `modalidad` y pinta la insignia**; el dato no llega (el timeline no lo
  expone). Pieza construida esperando su prop.

**Sin dueño, declarado**
- **«¿Cómo se entera el vet de una cita nueva?»** — no medido a propósito: es
  motor de notificaciones, y el grep que intenté no imprimió nada
  concluyente ⇒ no midió nada (L-321). Va a la mesa.

**Pedidos a A (76b, autocontenidos en §E del relevamiento):** `modalidad` en
tres lectores — el más urgente es el del timeline, porque es el único sin
rodeo disponible — y el código tipado de «no verificado».

---
