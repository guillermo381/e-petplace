# S101-D · BITÁCORA — LA PASADA DE DISEÑO DEL CIRCUITO DE PAGO

> **Frente A** de la **MESA 103** del founder. Pista de diseño.
> Consume `PLAN_MESA_103.md` v1.1 · `DIRECCION_ARTE` §9bis · skill
> `epetplace-design-system` · `DIRECTIVA_CRAFT_CLIENTE` ·
> `LETRA_PUERTA_DE_PAGO_S101B.md` v1.3 · las fichas `D-851`→`D-857`.
> **Regla de precedencia declarada al abrir: si el plan contradice al repo o a la
> letra, gana la fuente** (la misma cláusula con la que S101-B abrió).

---

## ⚠️ EL NÚMERO DE ESTA BITÁCORA — **S101-D por dictamen de mesa (relevo 2)**

**Rige `S101-D`**: la pasada de diseño es del **mismo arco que S101-B/C** —el motor
de pagos y su superficie— y **`S102` queda ÍNTEGRA para la plata**.

**Verificado antes de ejecutar, como ordenó el relevo:** `S101-D` estaba **LIBRE**
(`docs/loop/` tenía solo `S101-A.md` y `S101-B.md`; grep amplio sobre `docs/` en
cero). La renumeración se hizo con `git mv` **en un commit NUEVO — jamás amend ni
rebase** (regla 86), y alcanzó a la bitácora, al relevamiento del censo y a la rama.

### ⏪ Lo que este bloque decía antes, y por qué se conserva

La apertura eligió **`S102-A`** con este razonamiento: *«la convención viva es
`S<N>-<PISTA>` con las pistas de una sesión compartiendo número (S99-A/B/C/D ·
S100c-A/B/C/D); los tres frentes de la mesa 103 son UNA sesión ⇒ S102, y esta pista
es la A»*. **Era una lectura defendible de la convención y era la equivocada** — le
faltaba el eje que la mesa sí tenía: **el arco**, no la mesa, es lo que agrupa un
número de sesión.

*Se conserva porque un bloque que se corrige en silencio deja a la próxima pista
repitiendo la misma deducción.* **Y lo que sí funcionó, funcionó: el número se
DECLARÓ en vez de deducirse callado, y por eso la mesa pudo corregirlo con una
línea en vez de descubrirlo tres commits después.**

---

# TANDA 0 · APERTURA — WORKTREE, DEPÓSITO, FIRMAS Y CENSO

*(Los cinco puntos de la orden de mesa. La cola de A1 NO se tocó: cero commits de
UI en este turno, como se ordenó.)*

## ✅ ① EL WORKTREE — primera decisión (regla 85)

Antes de cualquier acción de escritura, con el árbol de `main` **medido limpio**
(`git status --porcelain` en 0, HEAD `a1f1b231`):

| | |
|---|---|
| **Rama** | `pista/s101-d` *(nació `pista/s102-a`; renombrada con la renumeración — **nada había mergeado**, así que el renombre no arrastra historia ajena)* |
| **Directorio** | `../e-petplace-s101-d` |
| **Base** | `main` @ `a1f1b231` (*«S101-B/C · el canon cerrado entero»*) |

Convención tomada de las pistas de S96-S99 (`pista/s99-a`), no de la de S100
(`pista-a`, sin prefijo) — la prefijada es la que dice a qué sesión pertenece la
rama cuando queden doce worktrees en la lista, que es exactamente lo que hay hoy.

## ✅ ② EL PLAN, DEPOSITADO VERBATIM

`docs/PLAN_MESA_103.md` — **cuerpo sin editar una coma**, encabezado e historial
incluidos.

**Se depositó SIN nota de depósito, y eso es un resultado, no un olvido.** El
precedente de la casa (S101-B sobre `PLAN_S101B_SUPERFICIE_PAGO`) agrega nota
cuando el documento trae una incoherencia interna que no se cura sin firma. **Se
buscó y no la hay:** el encabezado dice `v1.1` y el `Historial` registra `v1.1`
sobre `v1.0` del mismo día — coherentes; las cuatro firmas del §9 concuerdan con
las de §3-B0 y con el §0. *Se declara que la verificación se hizo, para que nadie
la repita ni suponga que se salteó.*

## ✅ ③ LAS FIRMAS DE D-856 — REGISTRADAS (lo que rige y lo que NO)

**Firmas del founder, 21-ago-2026, mesa 103** — registradas acá **como ordena el
punto 3, y solo acá**: la ficha `D-856` y `POLITICAS` las transpone **la mesa** al
escribir la letra (plan §3-B1). *Esta pista no escribe letra de plata.*

| # | Estado | Qué queda firmado |
|---|---|---|
| **①** | ✅ **FIRMADA** | El **plan de paseos** cobra **al contratar y en cada renovación** — confirmación de **Decisión S** (S55), transpuesta al motor real |
| **②** | ✅ **FIRMADA** | El **paquete de salidas** cobra **al comprar** — verificada contra **P16(a) + Decisión T**. Frontera explícita: *encender el cobro real del paquete **NO** enciende el cron del breakage* — son dos actos, y el segundo no es de esta mesa |
| **⓪** | ✅ **RESUELTA por letra propia (γ)** | *Lo jamás agendado: se **acumula** si renueva, se **pierde** si no (breakage declarado). Lo agendado sin asistencia o sin entrega de la mascota: **no-show**, se da por visto y se factura.* Muere el proporcional de P14(a); **P14(b) sobrevive** |
| **③** | ⏳ **PENDIENTE** | El **programa** de adiestramiento. Espera el censo de `MODELO_ADIESTRAMIENTO`; la mesa trae (a)/(b)/(c) con su consecuencia de reembolso |

⚠️ **α y β NO se registran como firmadas** — no existen como desenlace: el founder
**no eligió entre las dos**, escribió una tercera (γ). *Anotar «α firmada» sería
registrar una opción que la firma descartó.*

## ✅ ④ EL CENSO A0 — corrido

Salida: **`docs/relevamientos/2026-08-21-s101d-CENSO-CIRCUITO-DE-PAGO.md`** — los
siete momentos con archivo:línea, el reparto marca→ley→ficha, los candidatos sin
ley y lo que NO se midió. **Baseline fijado ANTES de tocar nada:** `verify:diseno`
VERDE/49 · `R57` en 0 · WCAG 368/0.

### Lo que hay que saber sin abrirlo

🔴 **El censo le corrige TRES ítems a la cola, y los tres ACHICAN el trabajo:**

| Ítem | Decía | La fuente dice |
|---|---|---|
| **1 · botón ocre** | pendiente | ✅ **ya ejecutado en S101-C** (`a3b5d4f1`): `index.html:139` consume `var(--cta)`. **Pasa de cola a GATE** |
| **2 · assets de franquicia** | trabajo | 🔒 **bloqueado por objeto ausente** — cero assets en el repo. **Dueño: FOUNDER** |
| **4 · columnas del comprobante** | pasada de UI | 🔄 **el comprobante es un CORREO server-side** (`despachar-correo` L135-170) — otro territorio |

**Vivos como trabajo de pantalla:** ⑤ la voz vieja del éxito —**localizada en
`es.ts:1858`**, describe el mundo anterior al motor— · ⑥ el fold y el truncado
(**exigen dispositivo**) · ⑦ D-857 · **y cuatro marcas NUEVAS con ley**: los cuatro
checkouts de oficio con `<Text style>` crudo en vez de `<Texto>` (Ley 1) ·
`checkout-reserva` con tipografía cruda y `gap: 2` fuera de `spacing` · dos claves
huérfanas **y el encabezado de `cuenta/pagos.tsx` que sigue diciendo «simulado» con
el motor real vivo** (Ley 37 — *`D-855` en forma de comentario*) · **una línea en
VOSEO en la página del alta** (`index.html:472`, la única disidente de esa
superficie, alcanzada por §8ter).

⏸️ **⑧ la animación NO se toca** — la propia cola la condiciona a que el founder la
pida, y la pieza cumple hoy su regla firmada (el segmento **viaja, no crece**).

**Cuatro candidatos SIN ley quedan declarados y sin ejecutar** (§4 del censo), el
más filoso: **la página del alta tiene 20 literales duros fuera del riel i18n — la
app es es/en desde el día 1 y esa pantalla es solo-es.**

## ✅ ⑤ CERO COMMITS DE UI · CERO ÍTEMS DE COLA SIN GATE

No se tocó una línea de `apps/` ni de `packages/ui`. La cola A1 queda **intacta y
sin ejecutar**, esperando su gate.

---

## 🔎 LOS DOS HALLAZGOS DE LA APERTURA (antes del censo, leyendo la ley)

### ① `LETRA_PUERTA_DE_PAGO_S101B` v1.3 tiene **DOS secciones numeradas §8**

Medido en el objeto: **`§8 · EL VOCABULARIO DE LA PLATA`** (línea 234) y
**`§8 · LA SUPERFICIE DE PAGO ES UNA PIEZA`** (línea 305), separadas por `§9` y
`§10`. La segunda nació en la v1.3 del 21-ago y se agregó **después** de `§10`,
sin ver que el número estaba tomado.

**Por qué importa y no es cosmético:** el plan de la mesa 103 **cita «§8» dos
veces con sentidos distintos** —el `LETRA_SALDO`/vocabulario en su §5, y la pieza
única en la cola de A1— y `R57` vigila la segunda. *Una referencia a «§8» de esta
letra hoy es ambigua, y el modo de falla es silencioso: quien la siga llega a la
sección equivocada y no recibe ningún error.*

**No se curó** — es letra firmada y renumerar secciones citadas desde otros
documentos es acto de mesa. **Va como decisión chica al founder**, con las dos
salidas baratas: renumerar la segunda a **§11** (va al final del cuerpo, nadie la
cita por número todavía salvo el plan) o dejarla y **desambiguar por título** en
las citas. *La casa ya pagó por dos letras firmadas que se contradicen; dos
secciones firmadas con el mismo número es la misma clase de deuda, más chica.*

### ② `D-857` — el número **no diverge**: 17 medidas · 7 curadas · **10 vivas**

El plan y el canon dicen «diez líneas»; el **título** de la ficha dice «17 líneas
medidas». Parecía divergencia y **no lo es**: el cuerpo de la propia ficha
reconcilia —17 fue el total medido, **7 se curaron en S101-C** (las del camino de
pago, justo las que el gate ⑤ iba a comparar) y **las 10 restantes quedaron sin
tocar A PROPÓSITO**. *Se verificó antes de reportarlo como choque.*

🔴 **Pero el número igual se re-midió antes de barrer, y la propia ficha decía por
qué:** su primer censo dio **5** porque el regex pedía espacio después del verbo y
`Elegí` a principio de frase no matcheaba — ***«el número chico no era la deuda:
era el instrumento»***.

### 🔴 RE-MEDIDO: son **34 cadenas**, no 10 — y BARRIDAS a tuteo. Y la advertencia se cobró DOS veces más

El instrumento propio pagó **cuatro trampas**, no una (§6 del censo):
① la de la ficha · ② **comentarios leídos como voz** (L-170: 43 falsos) ·
③ **`vas` y `estás` NO son voseo** —el verbo es idéntico en tuteo, y meterlos infló
el censo de 5 a 20 con 15 cadenas que no hay que tocar; *prueba viva: `es.ts:1326`
dice «Vas a borrar… pero **puedes** volver», tuteo puro con un «vas» adentro*— y
🔴 ④ **`\b` en JavaScript es ASCII**: después de una vocal acentuada no hay
frontera de palabra, así que `\b(probá)\b` **nunca** matchea «Probá de nuevo» ⇒
**todo imperativo voseo era INVISIBLE** y solo pasaban las formas terminadas en `s`.

**Lo que cazó ④ y es lo reutilizable:** un barrido de *recall* independiente
devolvió `Probá`×7 · `Elegí`×6 · `Ingresá`×2 **contra un instrumento que reportaba
0 de esas formas.** *Dos instrumentos que miden lo mismo y no coinciden: uno está
roto, y el que da el número más chico es el sospechoso.*

| Archivo | Voseo |
|---|---|
| `apps/cliente/src/i18n/es.ts` | **34** ← territorio de D-857 · ✅ BARRIDAS |
| `apps/prestador/src/i18n/es.ts` | **37** ← ⚠️ **FUERA del territorio de la ficha** |
| `packages/ui/src/i18n/es.ts` | **0** ✅ |

🔴 **Las 37 del prestador NO se tocaron y van a la mesa con dueño abierto.** La
ficha acota D-857 al cliente; ensanchar el territorio de paso es justo lo que la
ley de la sesión prohíbe. *La regla 27 alcanza a las dos apps: lo que falta no es
la ley, es el dueño.*

---

# TANDA 1 · LAS CORRECCIONES DE MESA (relevo 2 + su adenda de idioma)

## ✅ ① RENUMERACIÓN — `S102-A` → **`S101-D`**

`S101-D` **verificado LIBRE** antes de tocar. `git mv` de bitácora y relevamiento
+ curación de referencias, **en commit NUEVO — jamás amend ni rebase** (regla 86).
Rama y worktree renombrados; **nada había mergeado**, así que el renombre no
arrastra historia ajena. El bloque de numeración **conserva el razonamiento
corregido** en vez de borrarlo (§ arriba).

## ✅ ② `D-858` NACE — el voseo del prestador, con dueño propio

**37 cadenas** en `apps/prestador/src/i18n/es.ts`. Número verificado libre por grep
en todo el repo. **Dueño: la sesión del prestador. Disparo: cuando esa sesión abra**
— *barrer strings de un actor cuyas pantallas nadie va a mirar es cómo se queman los
gates*. `D-857` queda **en 32 y solo del lado cliente**, con su enmienda adentro.

## ✅ ③ `L-327` NACE — la ley del instrumento ciego

*Un instrumento puede ser ciego a la clase entera que vino a medir, y su síntoma es
un número chico que parece una buena noticia.* Sus tres mitades: **`\b` es ASCII en
JS** · **una forma compartida por las dos voces no discrimina** (`vas`/`estás`) ·
**el barrido de recall como contraprueba** — *dos instrumentos que miden lo mismo y
no coinciden: el que da el número más chico es el sospechoso.*

## ✅ ④ MARCA ④ REASIGNADA a Pista B — con puntero, no borrada

El comprobante es **territorio server** (`despachar-correo` L154-158). Viaja con su
clase de defecto ya nombrada: *el mismo «DINER / S» que S101-C curó midiendo contra
la palabra más larga.*

## ✅ ⑤ CURA EDITORIAL de la letra → **v1.4** · ⚠️ con una colisión que la orden no tenía

**La mesa ordenó «el segundo §8 pasa a §8bis» sin el dato de que `§8bis` YA EXISTÍA**
como **hijo de esa misma sección** (`LA PÁGINA DEL ALTA TAMBIÉN ES LA CASA`).
**Medido antes de tocar**, y con sus citadores externos censados: **solo los dos
documentos de esta pista**. Se aplicó la resolución que **conserva la intención**:

| Antes | Ahora |
|---|---|
| `§8 · EL VOCABULARIO DE LA PLATA` | `§8` — sin cambio |
| `§8 · LA SUPERFICIE DE PAGO ES UNA PIEZA` | **`§8bis`** |
| `§8bis · LA PÁGINA DEL ALTA…` | **`§8ter`** |

**Cero ediciones de contenido: tres encabezados y una entrada de historial.**
**Reversible por una línea si la mesa prefiere `§11`** — se declara porque es una
decisión que tomé con un dato que la orden no tenía.

## ✅ ⑥ FIRMA DE IDIOMA REGISTRADA — el candidato pasa a LEY

**El circuito de pago habla los DOS idiomas de la app (es/en).** El candidato ①
de §4 queda **resuelto**; la página del alta pasa de candidato a **trabajo de cola**.

## ✅ ⑦ CENSO DEL PERÍMETRO i18n — corrido ANTES de construir (§4bis del censo)

🔴 **El hallazgo reparte el trabajo y ACHICA la mitad server: el riel de idioma del
correo YA EXISTE Y NO SE USA.** `despachar-correo` L390-397 lee
`user_preferencias.idioma` del destinatario y se lo pasa a la plantilla — **y dentro
de la plantilla se usa en UN solo lugar: el atributo `<html lang>` (L212).**

> *El correo se DECLARA en inglés y escribe en español.* **Peor que no declarar
> nada**: el `lang` es una afirmación, y el lector de pantalla le cree.
> Es **`L-318` en su forma de idioma** — la pieza construida, correcta, y
> desconectada del único lugar donde su resultado importa.

**Cuatro piezas con territorio y cañería:** **P1** la página del alta (**A**) —
🔴 **hoy la URL lleva `?alta=<id>` y NADA más** (`alta-tarjeta.tsx:72`): el idioma
**viaja de la app por la URL**, ⚠️ **jamás `navigator.language`**, que reporta el
locale del sistema y no la preferencia elegida (`D-316` existe porque son cosas
distintas) · **P2/P3** el comprobante y el resto del correo (**B**) — **ya lo
reciben** · **P4** los dos fallbacks de `despachar-push` (**B**), cuya voz principal
**no es suya**: la escribe el encolador.

⚠️ **Lo que ninguna medición nuestra contesta, y va a la mesa como pregunta:
el idioma del formulario del proveedor.** Sus campos los rotula Nuvei. **Aunque P1
se cure entera, el formulario de la tarjeta puede seguir en español** — que nadie
declare la brecha cerrada sin ese dato.

## ✅ ⑧ ASSETS — dueño founder, **kit YA PEDIDO a Nuvei**

El fallback de texto **no se toca**. Al llegar los archivos, *cambia el interior de
la caja y nada más.*

---

## ✅ ⑨ LA TANDA 1 DE LA ORDEN — **PREPARADA EN LA RAMA, sin push, sin gate**

### ⑤ · La voz vieja del éxito — CURADA en los dos idiomas

`es.ts` *«Te avisamos cuando el vendedor lo confirme»* → **«Tu pago quedó
confirmado. Puedes seguir el pedido en Tus pedidos.»** · `en.ts` su espejo.

🔴 **Se midió el MOMENTO antes de reescribir la voz**, porque una voz de éxito
cambiada sin medir su momento fabrica una mentira nueva: la fase se alcanza **solo
con `espera.estado === 'pagada'`** (`checkout.tsx:208`) — el servidor, vía webhook o
barrido. ⇒ **puede decir «confirmado» sin violar §0 de la letra**, que prohíbe
tratar la respuesta síncrona como confirmación. *Y no promete un aviso que esta
pantalla no controla: dice el hecho y ofrece el camino.*

### El encabezado «simulado» de `cuenta/pagos.tsx` — CURADO, con su distinción fina

Decía **dos** cosas y **solo una era falsa**:
- *«"Pago simulado" declarado por fila **mientras el pago sea simulado**»* — **la
  marca por fila NO se toca**: es condicional a `p.pago_simulado` de **esa fila**, y
  las citas históricas siguen siendo simuladas de verdad. *Lo que caducó es la
  cláusula que hablaba del producto en presente.*
- *«Métodos de pago = en preparación honesto»* — **falso**: apunta a `cuenta/medios`.

*Es `D-855` en su forma más barata: una banda que dejó de ser cierta. Ningún
typecheck la ve, y quien abra el archivo hereda la premisa de que acá no se cobra.*

### D-857 · **las 34 del cliente, BARRIDAS** — y el número subió DESPUÉS de reportarlo

🔴 **Corrección al founder: reporté 32 y 36; son 34 y 37.** La **quinta trampa** del
instrumento apareció **al ir a escribir el mapa de reemplazos**: *«el imperativo
voseo siempre lleva tilde» es falso en cuanto hay enclítico* — `contá`+`nos` =
**«contanos»**, sin tilde. El medidor era **ciego a la clase entera**: perdía
`«Contanos quién recibe.»` y `«Escribinos al {{numero}}.»`.

**Barrido:** mapa explícito · **solo dentro de comillas** · **`vos` por frase
completa** (*«casi como vos»* → **tú**, *«te llegó a vos»* → **ti**; un mapa
palabra-a-palabra lo habría errado en dos de tres). **Censo post-barrido: 0 ·
typecheck cliente: verde.** Las **37 del prestador NO se tocaron** (`D-858`).

⚠️ **Falta su gate en dispositivo: el número está cerrado, el ojo no.**

---

## 📌 LO QUE ESTA TANDA DEJA PARA LA MESA

| # | Qué | Tipo |
|---|---|---|
| 1 | **El reparto del perímetro i18n** (§4bis): P1→A · P2/P3/P4→B · **+ la pregunta del formulario del proveedor** | reparto |
| 2 | Los **tres candidatos que quedan** en §4 (hex sin declarar · URL del isotipo · caja del logo) | gate |
| 3 | La **cola re-ordenada** (⑤ ⑥ ⑦ + N1-N4 + P1), tanda por tanda | autorización |
| 4 | ③ **la elegida inconfundible** y ⑧ **la animación** | su ojo, en dispositivo |
| 5 | **`§8bis`/`§8ter`**: confirmar o pedir `§11` | decisión chica |

**Autorización pendiente del founder: push de cualquier tanda · gates.**

*Se publica lo incompleto, jamás lo falso.*
