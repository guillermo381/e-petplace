# S101-D · BITÁCORA — LA PASADA DE DISEÑO DEL CIRCUITO DE PAGO

> **Frente A** de la **MESA 103** del founder. Pista de diseño.
> Consume `PLAN_MESA_103.md` v1.1 · `DIRECCION_ARTE` §9bis · skill
> `epetplace-design-system` · `DIRECTIVA_CRAFT_CLIENTE` ·
> `LETRA_PUERTA_DE_PAGO_S101B.md` v1.3 · las fichas `D-851`→`D-857`.
> **Regla de precedencia declarada al abrir: si el plan contradice al repo o a la
> letra, gana la fuente** (la misma cláusula con la que S101-B abrió).

---

## ⚠️ EL NÚMERO DE ESTA BITÁCORA — **S101-D por dictamen de mesa (relevo 2)**

**Rige `S101-D`**: la pasada de diseño es del **mismo arco que S101-B/C** —el motor
de pagos y su superficie— y **`S102` queda ÍNTEGRA para la plata**.

**Verificado antes de ejecutar, como ordenó el relevo:** `S101-D` estaba **LIBRE**
(`docs/loop/` tenía solo `S101-A.md` y `S101-B.md`; grep amplio sobre `docs/` en
cero). La renumeración se hizo con `git mv` **en un commit NUEVO — jamás amend ni
rebase** (regla 86), y alcanzó a la bitácora, al relevamiento del censo y a la rama.

### 📌 **`pista/s101-d` es la rama de trabajo de S101-D**

*(Dictamen de mesa, relevo 5.)* Se escribe porque al llegar el pedido de la otra
pista apareció la duda: **B trabaja en `pista/s102-b` y esta pasada es `S101-D`**
— parecía una pisada de número y **no lo es**.

> **El canon es `docs/loop/`; los nombres de rama NO son canon.** `S101-D` y
> `S102` son sesiones distintas y sus bitácoras no se tocan. *Una rama es una
> dirección de trabajo, no una declaración de a qué sesión pertenece lo que
> lleva adentro* — y por eso el nombre de una rama no puede contradecir al canon:
> no está en la misma conversación.

### 🚧 TERRITORIO — las edge functions de PAGO son de B mientras dure S102

*(Dictamen de mesa, relevo 5. Se escribe en las DOS bitácoras.)*

> **Las edge functions de PAGO son territorio de B mientras dure S102 — el motor
> es su sujeto. A no las toca sin aviso de mesa.**
> **Alcance:** `pagos-cobro` · `pagos-alta-tarjeta` · `pagos-webhook-stg` ·
> `pagos-conciliar` · `pagos-borrar-tarjeta`. **Las otras 17 no cambian de dueño.**

Nació porque **`supabase/functions` no está en la tabla de territorios del
método** y B tocó `pagos-cobro` por orden del founder — y **lo declaró**. *Un
territorio que no está en la tabla no es tierra de nadie: es tierra que todavía
nadie nombró, y se resuelve con el criterio que la tabla ya usa — el territorio
va con la verdad de la cosa.*

### 📌 UN HALLAZGO SE DEPOSITA DONDE VIVE SU FUENTE

*(Dictamen de mesa, 21-ago — corrección de una orden de la propia mesa.)*

Se me ordenó depositar como ficha **un hallazgo de B del que yo no tengo el
literal** (la falsa alarma `estado='activa'` vs `'guardada'`). **Frené y lo
declaré; la mesa corrigió la orden y el ítem salió de mi cola: lo deposita B, que
tiene su fuente.**

> **El principio, registrado porque es más grande que el caso:** *un hallazgo se
> deposita donde vive su fuente, jamás por relato de un tercero.* **Es de la
> familia de `L-328`** —medir el caso, no heredar el patrón— y su costo estaba a
> la vista el mismo día: **`D-864` empezó con un CERO mío contra la columna
> equivocada**, y solo la medición propia evitó que contradijera un hallazgo
> correcto. *Depositar de oídas produce exactamente ese error, pero firmado.*

### ⏪ Lo que este bloque decía antes, y por qué se conserva

La apertura eligió **`S102-A`** con este razonamiento: *«la convención viva es
`S<N>-<PISTA>` con las pistas de una sesión compartiendo número (S99-A/B/C/D ·
S100c-A/B/C/D); los tres frentes de la mesa 103 son UNA sesión ⇒ S102, y esta pista
es la A»*. **Era una lectura defendible de la convención y era la equivocada** — le
faltaba el eje que la mesa sí tenía: **el arco**, no la mesa, es lo que agrupa un
número de sesión.

*Se conserva porque un bloque que se corrige en silencio deja a la próxima pista
repitiendo la misma deducción.* **Y lo que sí funcionó, funcionó: el número se
DECLARÓ en vez de deducirse callado, y por eso la mesa pudo corregirlo con una
línea en vez de descubrirlo tres commits después.**

---

# TANDA 0 · APERTURA — WORKTREE, DEPÓSITO, FIRMAS Y CENSO

*(Los cinco puntos de la orden de mesa. La cola de A1 NO se tocó: cero commits de
UI en este turno, como se ordenó.)*

## ✅ ① EL WORKTREE — primera decisión (regla 85)

Antes de cualquier acción de escritura, con el árbol de `main` **medido limpio**
(`git status --porcelain` en 0, HEAD `a1f1b231`):

| | |
|---|---|
| **Rama** | `pista/s101-d` *(nació `pista/s102-a`; renombrada con la renumeración — **nada había mergeado**, así que el renombre no arrastra historia ajena)* |
| **Directorio** | `../e-petplace-s101-d` |
| **Base** | `main` @ `a1f1b231` (*«S101-B/C · el canon cerrado entero»*) |

Convención tomada de las pistas de S96-S99 (`pista/s99-a`), no de la de S100
(`pista-a`, sin prefijo) — la prefijada es la que dice a qué sesión pertenece la
rama cuando queden doce worktrees en la lista, que es exactamente lo que hay hoy.

## ✅ ② EL PLAN, DEPOSITADO VERBATIM

`docs/PLAN_MESA_103.md` — **cuerpo sin editar una coma**, encabezado e historial
incluidos.

**Se depositó SIN nota de depósito, y eso es un resultado, no un olvido.** El
precedente de la casa (S101-B sobre `PLAN_S101B_SUPERFICIE_PAGO`) agrega nota
cuando el documento trae una incoherencia interna que no se cura sin firma. **Se
buscó y no la hay:** el encabezado dice `v1.1` y el `Historial` registra `v1.1`
sobre `v1.0` del mismo día — coherentes; las cuatro firmas del §9 concuerdan con
las de §3-B0 y con el §0. *Se declara que la verificación se hizo, para que nadie
la repita ni suponga que se salteó.*

## ✅ ③ LAS FIRMAS DE D-856 — REGISTRADAS (lo que rige y lo que NO)

**Firmas del founder, 21-ago-2026, mesa 103** — registradas acá **como ordena el
punto 3, y solo acá**: la ficha `D-856` y `POLITICAS` las transpone **la mesa** al
escribir la letra (plan §3-B1). *Esta pista no escribe letra de plata.*

| # | Estado | Qué queda firmado |
|---|---|---|
| **①** | ✅ **FIRMADA** | El **plan de paseos** cobra **al contratar y en cada renovación** — confirmación de **Decisión S** (S55), transpuesta al motor real |
| **②** | ✅ **FIRMADA** | El **paquete de salidas** cobra **al comprar** — verificada contra **P16(a) + Decisión T**. Frontera explícita: *encender el cobro real del paquete **NO** enciende el cron del breakage* — son dos actos, y el segundo no es de esta mesa |
| **⓪** | ✅ **RESUELTA por letra propia (γ)** | *Lo jamás agendado: se **acumula** si renueva, se **pierde** si no (breakage declarado). Lo agendado sin asistencia o sin entrega de la mascota: **no-show**, se da por visto y se factura.* Muere el proporcional de P14(a); **P14(b) sobrevive** |
| **③** | ⏳ **PENDIENTE** | El **programa** de adiestramiento. Espera el censo de `MODELO_ADIESTRAMIENTO`; la mesa trae (a)/(b)/(c) con su consecuencia de reembolso |

⚠️ **α y β NO se registran como firmadas** — no existen como desenlace: el founder
**no eligió entre las dos**, escribió una tercera (γ). *Anotar «α firmada» sería
registrar una opción que la firma descartó.*

## ✅ ④ EL CENSO A0 — corrido

Salida: **`docs/relevamientos/2026-08-21-s101d-CENSO-CIRCUITO-DE-PAGO.md`** — los
siete momentos con archivo:línea, el reparto marca→ley→ficha, los candidatos sin
ley y lo que NO se midió. **Baseline fijado ANTES de tocar nada:** `verify:diseno`
VERDE/49 · `R57` en 0 · WCAG 368/0.

### Lo que hay que saber sin abrirlo

🔴 **El censo le corrige TRES ítems a la cola, y los tres ACHICAN el trabajo:**

| Ítem | Decía | La fuente dice |
|---|---|---|
| **1 · botón ocre** | pendiente | ✅ **ya ejecutado en S101-C** (`a3b5d4f1`): `index.html:139` consume `var(--cta)`. **Pasa de cola a GATE** |
| **2 · assets de franquicia** | trabajo | 🔒 **bloqueado por objeto ausente** — cero assets en el repo. **Dueño: FOUNDER** |
| **4 · columnas del comprobante** | pasada de UI | 🔄 **el comprobante es un CORREO server-side** (`despachar-correo` L135-170) — otro territorio |

**Vivos como trabajo de pantalla:** ⑤ la voz vieja del éxito —**localizada en
`es.ts:1858`**, describe el mundo anterior al motor— · ⑥ el fold y el truncado
(**exigen dispositivo**) · ⑦ D-857 · **y cuatro marcas NUEVAS con ley**: los cuatro
checkouts de oficio con `<Text style>` crudo en vez de `<Texto>` (Ley 1) ·
`checkout-reserva` con tipografía cruda y `gap: 2` fuera de `spacing` · dos claves
huérfanas **y el encabezado de `cuenta/pagos.tsx` que sigue diciendo «simulado» con
el motor real vivo** (Ley 37 — *`D-855` en forma de comentario*) · **una línea en
VOSEO en la página del alta** (`index.html:472`, la única disidente de esa
superficie, alcanzada por §8ter).

⏸️ **⑧ la animación NO se toca** — la propia cola la condiciona a que el founder la
pida, y la pieza cumple hoy su regla firmada (el segmento **viaja, no crece**).

**Cuatro candidatos SIN ley quedan declarados y sin ejecutar** (§4 del censo), el
más filoso: **la página del alta tiene 20 literales duros fuera del riel i18n — la
app es es/en desde el día 1 y esa pantalla es solo-es.**

## ✅ ⑤ CERO COMMITS DE UI · CERO ÍTEMS DE COLA SIN GATE

No se tocó una línea de `apps/` ni de `packages/ui`. La cola A1 queda **intacta y
sin ejecutar**, esperando su gate.

---

## 🔎 LOS DOS HALLAZGOS DE LA APERTURA (antes del censo, leyendo la ley)

### ① `LETRA_PUERTA_DE_PAGO_S101B` v1.3 tiene **DOS secciones numeradas §8**

Medido en el objeto: **`§8 · EL VOCABULARIO DE LA PLATA`** (línea 234) y
**`§8 · LA SUPERFICIE DE PAGO ES UNA PIEZA`** (línea 305), separadas por `§9` y
`§10`. La segunda nació en la v1.3 del 21-ago y se agregó **después** de `§10`,
sin ver que el número estaba tomado.

**Por qué importa y no es cosmético:** el plan de la mesa 103 **cita «§8» dos
veces con sentidos distintos** —el `LETRA_SALDO`/vocabulario en su §5, y la pieza
única en la cola de A1— y `R57` vigila la segunda. *Una referencia a «§8» de esta
letra hoy es ambigua, y el modo de falla es silencioso: quien la siga llega a la
sección equivocada y no recibe ningún error.*

**No se curó** — es letra firmada y renumerar secciones citadas desde otros
documentos es acto de mesa. **Va como decisión chica al founder**, con las dos
salidas baratas: renumerar la segunda a **§11** (va al final del cuerpo, nadie la
cita por número todavía salvo el plan) o dejarla y **desambiguar por título** en
las citas. *La casa ya pagó por dos letras firmadas que se contradicen; dos
secciones firmadas con el mismo número es la misma clase de deuda, más chica.*

### ② `D-857` — el número **no diverge**: 17 medidas · 7 curadas · **10 vivas**

El plan y el canon dicen «diez líneas»; el **título** de la ficha dice «17 líneas
medidas». Parecía divergencia y **no lo es**: el cuerpo de la propia ficha
reconcilia —17 fue el total medido, **7 se curaron en S101-C** (las del camino de
pago, justo las que el gate ⑤ iba a comparar) y **las 10 restantes quedaron sin
tocar A PROPÓSITO**. *Se verificó antes de reportarlo como choque.*

🔴 **Pero el número igual se re-midió antes de barrer, y la propia ficha decía por
qué:** su primer censo dio **5** porque el regex pedía espacio después del verbo y
`Elegí` a principio de frase no matcheaba — ***«el número chico no era la deuda:
era el instrumento»***.

### 🔴 RE-MEDIDO: son **34 cadenas**, no 10 — y BARRIDAS a tuteo. Y la advertencia se cobró DOS veces más

El instrumento propio pagó **cuatro trampas**, no una (§6 del censo):
① la de la ficha · ② **comentarios leídos como voz** (L-170: 43 falsos) ·
③ **`vas` y `estás` NO son voseo** —el verbo es idéntico en tuteo, y meterlos infló
el censo de 5 a 20 con 15 cadenas que no hay que tocar; *prueba viva: `es.ts:1326`
dice «Vas a borrar… pero **puedes** volver», tuteo puro con un «vas» adentro*— y
🔴 ④ **`\b` en JavaScript es ASCII**: después de una vocal acentuada no hay
frontera de palabra, así que `\b(probá)\b` **nunca** matchea «Probá de nuevo» ⇒
**todo imperativo voseo era INVISIBLE** y solo pasaban las formas terminadas en `s`.

**Lo que cazó ④ y es lo reutilizable:** un barrido de *recall* independiente
devolvió `Probá`×7 · `Elegí`×6 · `Ingresá`×2 **contra un instrumento que reportaba
0 de esas formas.** *Dos instrumentos que miden lo mismo y no coinciden: uno está
roto, y el que da el número más chico es el sospechoso.*

| Archivo | Voseo |
|---|---|
| `apps/cliente/src/i18n/es.ts` | **34** ← territorio de D-857 · ✅ BARRIDAS |
| `apps/prestador/src/i18n/es.ts` | **37** ← ⚠️ **FUERA del territorio de la ficha** |
| `packages/ui/src/i18n/es.ts` | **0** ✅ |

🔴 **Las 37 del prestador NO se tocaron y van a la mesa con dueño abierto.** La
ficha acota D-857 al cliente; ensanchar el territorio de paso es justo lo que la
ley de la sesión prohíbe. *La regla 27 alcanza a las dos apps: lo que falta no es
la ley, es el dueño.*

---

# TANDA 1 · LAS CORRECCIONES DE MESA (relevo 2 + su adenda de idioma)

## ✅ ① RENUMERACIÓN — `S102-A` → **`S101-D`**

`S101-D` **verificado LIBRE** antes de tocar. `git mv` de bitácora y relevamiento
+ curación de referencias, **en commit NUEVO — jamás amend ni rebase** (regla 86).
Rama y worktree renombrados; **nada había mergeado**, así que el renombre no
arrastra historia ajena. El bloque de numeración **conserva el razonamiento
corregido** en vez de borrarlo (§ arriba).

## ✅ ② `D-858` NACE — el voseo del prestador, con dueño propio

**37 cadenas** en `apps/prestador/src/i18n/es.ts`. Número verificado libre por grep
en todo el repo. **Dueño: la sesión del prestador. Disparo: cuando esa sesión abra**
— *barrer strings de un actor cuyas pantallas nadie va a mirar es cómo se queman los
gates*. `D-857` queda **en 32 y solo del lado cliente**, con su enmienda adentro.

## ✅ ③ `L-327` NACE — la ley del instrumento ciego

*Un instrumento puede ser ciego a la clase entera que vino a medir, y su síntoma es
un número chico que parece una buena noticia.* Sus tres mitades: **`\b` es ASCII en
JS** · **una forma compartida por las dos voces no discrimina** (`vas`/`estás`) ·
**el barrido de recall como contraprueba** — *dos instrumentos que miden lo mismo y
no coinciden: el que da el número más chico es el sospechoso.*

## ✅ ④ MARCA ④ REASIGNADA a Pista B — con puntero, no borrada

El comprobante es **territorio server** (`despachar-correo` L154-158). Viaja con su
clase de defecto ya nombrada: *el mismo «DINER / S» que S101-C curó midiendo contra
la palabra más larga.*

## ✅ ⑤ CURA EDITORIAL de la letra → **v1.4** · ⚠️ con una colisión que la orden no tenía

**La mesa ordenó «el segundo §8 pasa a §8bis» sin el dato de que `§8bis` YA EXISTÍA**
como **hijo de esa misma sección** (`LA PÁGINA DEL ALTA TAMBIÉN ES LA CASA`).
**Medido antes de tocar**, y con sus citadores externos censados: **solo los dos
documentos de esta pista**. Se aplicó la resolución que **conserva la intención**:

| Antes | Ahora |
|---|---|
| `§8 · EL VOCABULARIO DE LA PLATA` | `§8` — sin cambio |
| `§8 · LA SUPERFICIE DE PAGO ES UNA PIEZA` | **`§8bis`** |
| `§8bis · LA PÁGINA DEL ALTA…` | **`§8ter`** |

**Cero ediciones de contenido: tres encabezados y una entrada de historial.**
**Reversible por una línea si la mesa prefiere `§11`** — se declara porque es una
decisión que tomé con un dato que la orden no tenía.

## ✅ ⑥ FIRMA DE IDIOMA REGISTRADA — el candidato pasa a LEY

**El circuito de pago habla los DOS idiomas de la app (es/en).** El candidato ①
de §4 queda **resuelto**; la página del alta pasa de candidato a **trabajo de cola**.

## ✅ ⑦ CENSO DEL PERÍMETRO i18n — corrido ANTES de construir (§4bis del censo)

🔴 **El hallazgo reparte el trabajo y ACHICA la mitad server: el riel de idioma del
correo YA EXISTE Y NO SE USA.** `despachar-correo` L390-397 lee
`user_preferencias.idioma` del destinatario y se lo pasa a la plantilla — **y dentro
de la plantilla se usa en UN solo lugar: el atributo `<html lang>` (L212).**

> *El correo se DECLARA en inglés y escribe en español.* **Peor que no declarar
> nada**: el `lang` es una afirmación, y el lector de pantalla le cree.
> Es **`L-318` en su forma de idioma** — la pieza construida, correcta, y
> desconectada del único lugar donde su resultado importa.

**Cuatro piezas con territorio y cañería:** **P1** la página del alta (**A**) —
🔴 **hoy la URL lleva `?alta=<id>` y NADA más** (`alta-tarjeta.tsx:72`): el idioma
**viaja de la app por la URL**, ⚠️ **jamás `navigator.language`**, que reporta el
locale del sistema y no la preferencia elegida (`D-316` existe porque son cosas
distintas) · **P2/P3** el comprobante y el resto del correo (**B**) — **ya lo
reciben** · **P4** los dos fallbacks de `despachar-push` (**B**), cuya voz principal
**no es suya**: la escribe el encolador.

⚠️ **Lo que ninguna medición nuestra contesta, y va a la mesa como pregunta:
el idioma del formulario del proveedor.** Sus campos los rotula Nuvei. **Aunque P1
se cure entera, el formulario de la tarjeta puede seguir en español** — que nadie
declare la brecha cerrada sin ese dato.

## ✅ ⑧ ASSETS — dueño founder, **kit YA PEDIDO a Nuvei**

El fallback de texto **no se toca**. Al llegar los archivos, *cambia el interior de
la caja y nada más.*

---

## ✅ ⑨ LA TANDA 1 DE LA ORDEN — **PREPARADA EN LA RAMA, sin push, sin gate**

### ⑤ · La voz vieja del éxito — CURADA en los dos idiomas

`es.ts` *«Te avisamos cuando el vendedor lo confirme»* → **«Tu pago quedó
confirmado. Puedes seguir el pedido en Tus pedidos.»** · `en.ts` su espejo.

🔴 **Se midió el MOMENTO antes de reescribir la voz**, porque una voz de éxito
cambiada sin medir su momento fabrica una mentira nueva: la fase se alcanza **solo
con `espera.estado === 'pagada'`** (`checkout.tsx:208`) — el servidor, vía webhook o
barrido. ⇒ **puede decir «confirmado» sin violar §0 de la letra**, que prohíbe
tratar la respuesta síncrona como confirmación. *Y no promete un aviso que esta
pantalla no controla: dice el hecho y ofrece el camino.*

### El encabezado «simulado» de `cuenta/pagos.tsx` — CURADO, con su distinción fina

Decía **dos** cosas y **solo una era falsa**:
- *«"Pago simulado" declarado por fila **mientras el pago sea simulado**»* — **la
  marca por fila NO se toca**: es condicional a `p.pago_simulado` de **esa fila**, y
  las citas históricas siguen siendo simuladas de verdad. *Lo que caducó es la
  cláusula que hablaba del producto en presente.*
- *«Métodos de pago = en preparación honesto»* — **falso**: apunta a `cuenta/medios`.

*Es `D-855` en su forma más barata: una banda que dejó de ser cierta. Ningún
typecheck la ve, y quien abra el archivo hereda la premisa de que acá no se cobra.*

### D-857 · **las 34 del cliente, BARRIDAS** — y el número subió DESPUÉS de reportarlo

🔴 **Corrección al founder: reporté 32 y 36; son 34 y 37.** La **quinta trampa** del
instrumento apareció **al ir a escribir el mapa de reemplazos**: *«el imperativo
voseo siempre lleva tilde» es falso en cuanto hay enclítico* — `contá`+`nos` =
**«contanos»**, sin tilde. El medidor era **ciego a la clase entera**: perdía
`«Contanos quién recibe.»` y `«Escribinos al {{numero}}.»`.

**Barrido:** mapa explícito · **solo dentro de comillas** · **`vos` por frase
completa** (*«casi como vos»* → **tú**, *«te llegó a vos»* → **ti**; un mapa
palabra-a-palabra lo habría errado en dos de tres). **Censo post-barrido: 0 ·
typecheck cliente: verde.** Las **37 del prestador NO se tocaron** (`D-858`).

⚠️ **Falta su gate en dispositivo: el número está cerrado, el ojo no.**

---

## ✅ ⑩ ASSETS DE FRANQUICIA — **el objeto apareció y quedó DEPOSITADO** (adenda 8-bis)

**El kit era la doc de Nuvei.** Se bajaron los cinco SVG y viven **vendored** en
`apps/cliente/assets/marcas/`, con `PROCEDENCIA.md`: fuente, URLs originales, fecha
de descarga y el porqué de no hotlinkear.

### 🔬 El mapeo, MEDIDO por los dos lados

| `card.type` | Marca | Archivo | ¿En nuestra base? |
|---|---|---|---|
| `vi` | Visa | `ic_visa.svg` | ✅ **3 filas** |
| `di` | Diners | `ic_diners.svg` | ✅ **4 filas** |
| `mc` · `ax` · `dc` | Mastercard · Amex · Discover | sus `.svg` | — |

Los códigos salieron de la **tabla de la doc**; el contraste,
de `select marca, count(*) from tarjetas_guardadas` → **`di`(4) · `vi`(3)**.

✅ **Y una sospecha que se cayó al medirla:** el mapa vivo de `logo-franquicia.tsx`
**ya estaba bien** — sus claves son los códigos en minúscula y la pieza normaliza
con `toLowerCase()`. *Se temía que mapeara nombres («VISA») que la base nunca
produce; no es el caso.* **La doc lista 24 marcas** (Colombia y Brasil incluidas):
**se depositaron las CINCO que la casa mapea** — vendorear marcas de países donde no
operamos, para un fallback que ya las dibuja bien, sería trabajo contra nada.

### 🔴 FRENO DECLARADO — **no se cablearon, y el motivo es de dependencia**

**Medido: el repo NO tiene transformer de SVG** (cero `react-native-svg-transformer`,
cero `svgr`). RN **no importa un `.svg` como componente sin uno**. Las dos salidas:
**(a)** transformer —dep de desarrollo + `metro.config.js`, JS puro sin build nativa,
pero **toca el pipeline del bundle de las dos apps**— · **(b)** convertirlos a
componentes con `react-native-svg`, que es **el patrón de la casa** (`Huella`,
`Isotipo`, `Guijarro`) **pero los cinco traen `<defs>` + `<mask>` + `xlink:href` y
tres relaciones de aspecto distintas** (32×21 · 56×18 · 56×37).

> *Convertir a mano una marca registrada con máscaras, contra reloj y para llegar a
> un gate, es exactamente cómo se rompe un logo sin que nadie lo note.* **Elegir el
> pipeline de assets de la casa no es decisión de esta tanda.**

⇒ **El fallback de texto sigue intacto y la firma se cumple**: cuando se cablee,
*cambia el interior de la caja y nada más*. **La marca 2 pasa de «bloqueada por
objeto ausente» a «objeto depositado, pipeline por decidir».**

---

---

# TANDA 2 · RELEVO 3 — el idioma de la puerta, y dos preguntas contestadas midiendo

## ✅ ① §8bis/§8ter VALIDADO · ② `L-327` ya traía la quinta trampa

La enmienda del enclítico entró **en `L-327`, sin número nuevo** — se depositó en la
tanda anterior junto con la corrección 32→34 / 36→37. **Nada que rehacer.**

## ✅ ③ P1 CONSTRUIDA — la página del alta habla los dos idiomas

**Lado app** (`app/pagos/alta-tarjeta.tsx`): la URL pasa de `?alta=<id>` a
`?alta=<id>&lang=<idioma>`, con **`obtenerIdiomaActual()`** del riel i18n.
**Lado página** (`apps/pagos-web/src/index.html`): diccionario `es`/`en` con **sus
24 voces**, `aplicarIdioma()` que fija `document.documentElement.lang`, el `<title>`,
los textos del markup y el `placeholder` del alias.

⚠️ **`navigator.language` NO se usa, y está escrito en las dos puntas.** En un
WebView reporta el **locale del sistema**, no la preferencia elegida — `D-316` existe
porque son cosas distintas. *Si la página lo adivinara, contradiría a la app que la
abrió, en la pantalla donde la familia entrega su tarjeta.* **Verificado: la única
aparición del literal en la página es el comentario que lo prohíbe.**

☠️ **De paso murió `N4`**: la línea en voseo (`probá de nuevo`) era una de las 25 y
salió con el diccionario. **La superficie de pago queda 100 % en tuteo.**

## ✅ ④ EL FORMULARIO DEL PROVEEDOR — **contestado midiendo, no preguntando**

Medido en la fuente real (`payment_stable.min.js`, el que la página carga):

| Lo que la mesa traía | Lo medido |
|---|---|
| *«la doc acepta `locale` es/en/pt en `init_reference`»* | ✅ cierto — **pero `init_reference` es OTRA API** (el checkout hospedado). **`paymentez.js` NO la usa** |
| ¿lo expone para el Add Card? | 🔴 **NO.** `Payment.init(env_mode, app_code, app_key)` y `Payment.addCard(uid, email, card, ok, fail, payment_form)` — **cero parámetro de idioma** |

🔴 **Y el hallazgo que vuelve innecesaria la pregunta:** los textos del widget viven
como **20 propiedades estáticas de `PaymentForm`** (`CREDIT_CARD_NUMBER_PLACEHOLDER`,
`VERIFICATION_PLACEHOLDER`, `INVALID_CARD_TYPE_MESSAGE`…) y **se LEEN al iniciar el
formulario, no al cargar el script** — medido contando ocurrencias: cada clave tiene
**una asignación y una lectura**, y la lectura está dentro del `prototype`.
⇒ **Asignarlas desde nuestra página, antes de montar, SÍ las cambia.** Hecho en
`traducirWidget()`, solo cuando `lang=en`.

✅ **Y un segundo camino, más limpio, que apareció en la misma medición:** la lectura
está guardada por `elementHasAttribute(input,"placeholder") || input.attr(...)` ⇒
**si nuestro markup ya trae `placeholder`, el SDK NO lo pisa.**

⚠️ **Límite declarado:** esto **toca API del proveedor**. Si un día renombra esas
constantes, el formulario **vuelve al español sin romperse** — *degrada, no falla*,
que es la única forma en que se puede aceptar depender de un interno ajeno.
**Para Erick queda UNA pregunta, ya angosta: si hay una vía soportada de idioma para
`paymentez.js`, o si estas constantes son su API pública.**

## ✅ ⑤ PIPELINE SVG — medido: **`react-native-svg` ES dependencia viva**

`15.15.4` **pinneada en las dos apps y `^15.15.4` en `packages/ui`**, instalada en el
árbol. ⇒ Por la regla del relevo: **el transformer es CONFIG, no dependencia de
runtime** (`react-native-svg-transformer` es `devDependency` + `metro.config.js`;
**JS puro, sin build nativa**). **La rasterización por script NO hace falta** — su
condición era que `react-native-svg` no estuviera, y está.

**Queda listo para ejecutar, sin ejecutar** — el relevo pedía *medir*. Los cinco SVG
vendored siguen siendo la fuente de verdad, y **la conversión a mano queda descartada
por orden**.

## ⏸️ ⑥ PUSH — **NO EJECUTADO**, esperando autorización

Cinco commits en `pista/s101-d`. **Rama solamente, sin merge a `main`, sin OTA.**
Al recibirla: push + verificación **por contenido** (regla 84 ② — `cat-file` +
`merge-base`, jamás un `git show` contra ref móvil) **y la rama en origin POR SHA**.

---

---

# TANDA 3 · RELEVO 4 — el canario, los logos cableados y una decisión de rumbo

## 🧭 DECISIÓN DEL FOUNDER — **21-ago-2026: el plan es migrar a STRIPE**

**Al alcanzar ~1000 usuarios se migra a Stripe. Nuvei queda como riel de
Ecuador.** *Se anota con fecha porque una decisión de rumbo que vive en un chat
se pierde, y ésta explica por qué algunas deudas del motor tienen fecha de
vencimiento en vez de cura.*

⚠️ **NO cambia nada del arco vivo:** certificación · octubre · DeUna · S102 siguen
igual. **La migración será sesión propia con letra propia cuando su disparo
llegue — hoy no se construye ni se diseña una línea de Stripe.**

## ✅ ① EL CANARIO — la degradación silenciosa gana síntoma

`traducirWidget()` **censa las 20 claves del SDK en cada carga —también en
español—** y avisa por consola y por el puente **nombrando cuáles faltan**.
*Un aviso que no nombra la clave obliga a re-medir el SDK entero.* El censo corre
siempre y no solo en inglés: *si esperáramos a que alguien abra la página en
inglés para enterarnos, el síntoma llegaría meses tarde.*

### 🔴 Y el canario destapó un defecto propio ANTES de existir

El aviso iba a viajar con `fuente: 'epetplace-alta-tarjeta'` — **y el `onMessage`
de la app no mira el contenido: con ese `fuente` da el trámite por terminado y
CIERRA la pantalla** (`alta-tarjeta.tsx:114-122`). Como el censo corre al
arrancar, **habría cerrado el alta apenas cargara, y solo el día que el
diagnóstico hiciera falta.** Va con `fuente: '…-diagnostico'`, que su propio
guard descarta.

> **Es `L-318` invertida, y por eso vale registrarla: la ley dice que un actuador
> que recibe un sujeto que no conoce lo IGNORA. Acá pasa lo contrario — el
> consumidor no ignora lo que no conoce: ACTÚA.** ⇒ *agregar un sujeto a un
> puente obliga a censar al consumidor **en las dos direcciones**: qué hace con
> lo que espera, y qué hace con lo que no.*

## ✅ ⑥ `D-859` NACE — la deuda del idioma del widget, **aceptada por el founder**

El idioma del formulario cuelga de **20 constantes no documentadas** de
`paymentez.js`. Si el proveedor las renombra, **degrada a español sin romperse**.
Aceptada **con su canario adentro**; ☠️ **su disparo de cierre es la migración a
Stripe**. **La pregunta a Erick sobre el idioma del widget queda RETIRADA.**

## ✅ ⑤ TANDA 2 (a) — TRANSFORMER DE SVG Y LOS CINCO LOGOS, CABLEADOS

⚠️ **Delta con la premisa de la orden, declarado:** *«el transformer es config»*
era cierto a medias — **no existía `metro.config.js` en ninguna app**. Ambas
corrían con el default de Expo, así que **configurarlo fue CREARLO**. Se declara
en el propio archivo: *un default que se hereda solo deja de heredarse cuando
alguien escribe el archivo que lo reemplaza* — cada subida de SDK tiene que
mirarlo ahora.

`react-native-svg-transformer` entra como **devDependency** (JS puro, cero build
nativa) + `svg.d.ts`, porque **el transformer es de Metro y Metro no le habla a
`tsc`**: son dos verdades separadas y hay que declararla de los dos lados.

**La promesa de la pieza se cumplió al pie:** `ANCHO_LOGO`, `ALTO_LOGO`, radio,
fondo y fallback **idénticos** — *cambió el interior de la caja y nada más*.
**Cada logo se escala CONTENIENDO**, con su aspecto medido del `viewBox`
(Visa `56×18` · Mastercard `56×37` · los otros tres `32×21`): *pasarles un
tamaño fijo a los cinco estiraría tres marcas registradas, y la deformación no
daría error — se vería «casi bien»*. **El fallback de texto sigue intacto** para
las 19 marcas del catálogo que no vendorizamos.

## ⏳ TANDA 2 (b) — ③ y ⑥ NO ejecutadas, con su dirección ya escrita

**③ la elegida inconfundible** y **⑥ la sección bajo el fold** quedan pendientes.
*No se ejecutaron a las apuradas junto al push:* las dos terminan en el ojo del
founder, y ⑥ **no es medible leyendo** — depende del alto real y del contenido,
con pedido FRESCO (`D-851`).
**Lo que sí queda escrito para que nadie lo re-derive:** por **L-b**, el relleno
pleno **es de la elección que cierra** ⇒ la fila elegida va **rellena**, no
contorneada. *La ley da la dirección; la dosis la firma el ojo.*

---

---

# TANDA 4 · LA PASADA POR INSTRUMENTO, ANTES DE CONVOCAR

> El protocolo del gate dice: **corro todo yo primero; se convoca al founder solo
> cuando cada paso está verde por instrumento.** Esto es esa pasada.
> 🔴 **Y su límite, dicho antes que los resultados: NO TENGO APARATO.** No hay
> dispositivo que yo pueda manejar, ni sandbox del proveedor que yo pueda operar.
> **Los pasos 1, 3, 6 y 7 son INEJECUTABLES desde acá** — se declara en vez de
> sustituirlos por una repro web que mediría otra cosa (el precedente exacto:
> `D-728`, donde la repro en navegador *«daría verde midiendo otra cosa»*).

## ✅ LO VERIFICADO POR INSTRUMENTO

| # | Qué | Instrumento | Resultado |
|---|---|---|---|
| — | **El bundle se arma** | `expo export` android **y** web | **exit 0** en las dos |
| **2** | 🔴 **El transformer COMPILA los SVG de verdad** | discriminador en el bundle de texto plano | `17.35h-4.525` (path de Visa) **1** · `011340` (su color) **1** · **`fillRule` 4** —svgr convirtió `fill-rule` a JSX— · **cero `.svg` en `assets/`** y cero `ic_visa` ⇒ **componente, no archivo** |
| **2** | **Aspecto de los cinco logos** | aritmética sobre el `viewBox` medido | **desvío 0,000000 en las cinco** |
| **1** | Paridad del diccionario es/en | conteo de claves + cruce de usos | **24/24 · cero huérfanas · cero `T.x` sin clave** |
| **1** | El canario cubre el SDK real | cruce contra `payment_stable.min.js` | **20/20 exacto** — cero claves sin vigilar, cero vigiladas inexistentes |
| **4** | La marca por fila sigue viva | lectura del objeto | `pagos.tsx:101` — `p.pago_simulado ? … : undefined` ✓ |
| **5** | Voseo del cliente | censo con el instrumento de `L-327` | **0** |
| — | Instrumentos de la casa | `verify:diseno` · WCAG · `tsc` | **VERDE/49 · 368/0 · verde** |

## 🔴 DOS DEFECTOS PROPIOS QUE CAZÓ ESTA PASADA, antes del gate

**① El instrumento del paso 2 estaba roto, y casi reporto un defecto que no
existía.** Grepeé el bundle Hermes buscando el path de Visa, dio **0**, y la
lectura obvia era «el transformer no corrió». **Antes de declararlo probé el
instrumento contra un caso de resultado conocido** (`L-321`): busqué strings que
SÍ están —«DINERS», y una voz que acababa de escribir— y **también daban 0**.
⇒ *El bytecode no es greppable y mi control con tildes se escapaba por el
`ó`.* Con bundle de texto plano y controles ASCII, la evidencia positiva
apareció entera. **Un rojo por la razón equivocada está tan roto como un verde
por la razón equivocada.**

**② `Math.round` deformaba las cinco marcas — lo encontró la aritmética, no el
ojo.** Mi v1 redondeaba **las dos dimensiones por separado**: desvío de aspecto
de hasta **0,032** en Visa (3,111 → 3,143). *Un logo de marca registrada torcido
no da error: se ve «casi bien», que es exactamente el criterio que el paso 2
existe para juzgar — o sea que habría llegado al gate a que el founder cazara con
el ojo algo que una cuenta ya decía.* Curado sin redondeo (el SVG escala continuo
dentro de su `viewBox`): **aspecto exacto en las cinco**.

## ⛔ LO QUE NO PUEDO CORRER, PASO POR PASO

| # | Paso | Por qué es inejecutable desde acá |
|---|---|---|
| **1** | La página en es/en + el widget en inglés + el canario en consola | **WebView nativo + SDK del proveedor con credenciales de sandbox.** Una repro en navegador no monta el WebView ni el widget: mediría otra cosa |
| **3** | Cobro fresco punta a punta | Exige **datos frescos y el sandbox del proveedor** (`D-851`: toda medición usa compra fresca) |
| **6** | ③ la elegida, dosis del relleno | **Es firma de ojo.** La ley (`L-b`) da la dirección —**la fila elegida va rellena**—; la dosis no se deduce |
| **7** | ⑥ bajo el fold y truncado del título | **Depende del alto real del aparato y del contenido.** No es medible leyendo — ya declarado en el censo |
| **8** | La animación | **Solo si el founder pide tocarla.** Hoy cumple su regla firmada |

**Los pasos 2, 4 y 5 quedan verdes por instrumento y esperan solo la confirmación
visual.** Los pasos 1 y 3 necesitan que alguien con el aparato los corra.

---

---

# TANDA 5 · EL GATE ARRANCÓ Y SE FRENÓ EN EL GUARD — no en el aparato

## ✅ El aparato SÍ estaba, y el error fue mío

Declaré *«no tengo aparato»* **sin correr `adb devices`**. Estaba conectado —
`R5CY201ZDVL`— en estado **`unauthorized`**: la autorización USB nunca se había
aceptado. El founder la aceptó y pasó a **`device`**.

> *Es la misma ley que vengo cazando en los instrumentos toda la sesión —medir
> antes de afirmar— y me la saltée yo.* **Una ausencia supuesta es una medición
> que no se hizo.**

## ✅ Build de desarrollo: CONSTRUIDA

`eas build --profile development --platform android --local` desde `pista/s101-d`
⇒ **exit 0, APK de 262 MB**.

## 🔴 Y AHÍ FRENÓ EL GUARD — **`D-574` cobrando exactamente lo que existe para cobrar**

```
node scripts/verify-manifest-apk.mjs … --app cliente   →  EXIT 1
ROJO — LA BUILD NO SE DISTRIBUYE (D-574):
  ✗ FALTA meta-data com.google.android.geo.API_KEY
  ✗ FALTA resource google_app_id — google-services.json NO se horneó
```

**La build salió «exitosa» y le faltan dos secrets.** Es la ficha en su letra
exacta: *los secrets del build local **no fallan, SE OMITEN***. Y el primero es el
que en S80 mató la app **en hilo nativo, fuera de toda ErrorBoundary, invisible
durante tres sesiones** — precisamente porque ningún gate entraba al mapa.

⇒ **NO se instaló.** *Instalarla habría dado un gate donde los pasos 2, 4 y 5
salían verdes y la app se moría al abrir cualquier mapa — un verde comprado con
un crash que el propio gate no toca.*

**Causa medida:** los secrets viven como **env de EAS con `environment:
development`**, y un build `--local` no los trae. Es el mismo hueco de `D-574`,
no un defecto nuevo.

## ⚠️ Y UN COBRO DE `L-191` EN MI PROPIA MANO, dos líneas después

Leí el resultado del guard con `… | tail -15; echo $?` y me dio **0**: *el exit
del pipe, no el del comando.* **Casi declaro verde un guard que estaba en rojo.**
Re-medido sin pipe: **exit 1**. *La ley dice que el exit se lee del comando, y la
rompí en el turno en que el guard estaba salvando la sesión.*

## ⛔ EL GATE, HONESTO: qué falta y de quién es

| Paso | Estado | Qué lo destraba |
|---|---|---|
| **2 · logos** · **4 · encabezado** · **5 · voseo** | 🟢 verdes por instrumento, **listos para el ojo** | una build **con sus secrets** |
| **1 · la página en es/en + el widget** | 🔴 doble bloqueo | ① la build · ② **las credenciales del cliente Nuvei NO están en el keychain** (verificado por nombre, sin leer valores) ⇒ la página se puede construir con placeholders y gatear **su propia voz y el canario**, pero **no el widget** |
| **3 · cobro fresco** | 🔴 | build + sandbox |
| **6 · 7 · 8** | ⏸️ | son firma de ojo |

**Preparado y esperando:** la página construida y servida en `127.0.0.1:8899`
(HTTP 200 verificado) para apuntarle el WebView por `EXPO_PUBLIC_PAGOS_ALTA_URL`
sin desplegar nada.

---

---

# TANDA 6 · EL PERFIL DE DESARROLLO ESTABA ROTO — y ningún instrumento podía verlo

## 🔴 EL HALLAZGO

La build de la nube salió **`FINISHED`**, el **guard de manifest dio VERDE**
(`geo.API_KEY` ✓ · `google_app_id` ✓ · receptor de push ✓), la APK se instaló…
**y se quedó en el splash de Expo para siempre.** No lo supuse: **captura de
pantalla primero, medición del objeto después.**

```
unzip -l cliente-cloud-s101d.apk | grep index.android.bundle   →  0
expo-dev-client en apps/cliente/package.json                   →  AUSENTE
eas.json · perfil development · developmentClient              →  true
```

⇒ El perfil **construye una APK sin bundle embebido** —espera bajarlo de Metro—
**y el paquete que se lo bajaría no estaba instalado.** *Una app que arranca y no
puede cargar nada, nunca.* **No lo causó esta sesión: el perfil venía así, y
cualquiera que lo haya usado obtuvo lo mismo.**

### 🔴 Y LO QUE LO VUELVE UNA LEY, NO UNA ANÉCDOTA

> **Acá NO falló ningún instrumento.** `verify-manifest-apk.mjs` dio verde **y
> tenía razón**: el manifest estaba bien.
>
> ### **Verificábamos lo que el artefacto DECLARA, jamás lo que CONTIENE.**
>
> Es *«se verifica el artefacto, no la materia prima»* **un piso más abajo**: el
> manifest es una declaración SOBRE la APK; el bundle **es** la APK. Hermano de
> `D-574` con una diferencia que lo empeora: **D-574 al menos tenía guard.**

## ✅ LA CURA Y SU GUARD

**`expo-dev-client` instalado** — *hacer verdad lo que el perfil ya prometía; es
cura, no decisión* — y build relanzada (`d980dbc1`).

**Nace `scripts/verify-apk-contenido.mjs`**, con la regla en una línea:
***una APK sin bundle solo es legítima si algo puede dárselo.*** Trae bundle →
verde · no lo trae pero puede bajarlo → verde · **no lo trae y nada puede
dárselo → ROJO**. Más su mitad que **no necesita APK** y corre **antes de gastar
un build**: *el perfil promete `developmentClient` y falta el paquete*.

**Autoprueba VERDE contra el caso malo real** (la APK sin bundle):
*un guard que nunca vio el caso malo no prueba que sepa verlo.*

### ⚠️ Y EL DEFECTO QUE LE ENCONTRÉ A MI PROPIO GUARD, del mismo tipo que vino a cazar

Su primera versión **juzgaba la APK leyendo el `package.json` de HOY** — o sea
**el estado del REPO para dictaminar sobre un ARTEFACTO construido antes**.
*Con `expo-dev-client` ya instalado, la APK mala habría pasado en verde.*
**La APK que viene es su discriminador:** si trae el dev-launcher adentro y la
vieja no, el guard mide el objeto; **si no logro distinguirlas, el guard dice «no
puedo determinarlo» — jamás pasa por no saber.**

🔒 **La ficha del perfil roto NO se abre hasta que exista la segunda APK**, con
las dos como evidencia. *Hasta entonces la cura es una hipótesis.*

## 📋 `lib-db.mjs` — alcance declarado, y el encuadre lo corrigió B

B curó el helper que usan **26 scripts de la casa**: el CLI manda un `npm warn`
fijo a `stderr` y el error de Postgres a `stdout`, y el `||` tomaba siempre la
primera fuente ⇒ **todo `verify` que fallara por SQL decía «npm warn Unknown
project config» en el campo donde iba la causa.** Ventana: **12-jul → 21-ago**.

**Mi reporte anterior decía «ninguna medición mía contaminada»; el encuadre de B
es más preciso y lo adopto: el helper NO podía producir un verde falso** —
`dbQuery` lanza siempre que el CLI sale ≠ 0 — **lo que se perdía era la CAUSA, no
el veredicto.**

**Juicio de mis mediciones, medido y sin re-correr nada:** *ninguno de mis dos
instrumentos usa `lib-db.mjs`*, y todas mis consultas fueron por el CLI directo —
**la prueba es que vi errores crudos de Postgres** (`column "creado_en" does not
exist`), que es exactamente lo que el bug tapaba. **Y sobre la pregunta que
importa —«¿algún `sin-medir` archivado sin entender por qué?»— hoy no archivé
ninguno.**

---

---

# TANDA 7 · EL ARTEFACTO SE ATA A UN COMMIT — y el gate cambia de alcance

## ① LA ORDEN DE MESA, Y POR QUÉ NO ERA CELO

> **El gate NO se corre sobre un artefacto de procedencia irreconstruible. Un
> veredicto del founder que no se puede atar a un commit no se puede repetir ni
> citar** — y entonces no es un gate.

**El dato que lo volvió hecho lo produje sin buscarlo:** la build salió de
`76f83f5f`, que **no** tiene `expo-dev-client`, **y la APK resultante lo trae**
(`expo/modules/devlauncher` ×1884).

> ### **No es que EAS IGNORE lo no commiteado: LO USA.**
> *Y eso empeora el problema en vez de mejorarlo — el hash del acta apunta a un
> código que nunca se construyó.* Mi *«probablemente»* de dos horas antes dejó de
> serlo, **en la peor dirección de las dos posibles.**

Es **la gemela nativa de lo que S91 curó en OTA** (`publicar-ota.mjs`, veda +
acto único: *«un publish sucio es inauditable después»*). **Allá el riesgo era
estructural; acá hubo un artefacto concreto instalado en un teléfono.**
`requireCommit: true` **en las dos apps** — de clase, no de instancia: *curar solo
donde se vio el síntoma es exactamente cómo el perfil `development` del cliente
pasó seis semanas roto.*

## ✅ ② LA RAZÓN SE VOLVIÓ MECÁNICA PORQUE `eas.json` NO LA ADMITE

Quise dejar el porqué adentro del archivo y **el esquema lo rechazó**:
`"cli.// is not allowed"`. **Medido antes de gastar un build, no después.**

⇒ **`--coherencia` ahora EXIGE `cli.requireCommit: true`.** *Un comentario se
borra sin que nadie se entere; un guard en rojo obliga a leer por qué.* Control
positivo corrido (`L-330`): sacándolo a mano, **ROJO exit 1**.

## ✅ ③ LA CURA DEL PERFIL, VERIFICADA POR COMPORTAMIENTO

La APK vieja se colgaba en el splash **para siempre**. La nueva arranca en
`DevLauncherActivity`, conecta a Metro y **corre el JS** (`Running "main"`).
*Seis semanas de perfil roto, cerradas por el aparato y no por el diff.*

## 🔴 ④ EL HALLAZGO QUE CAMBIA EL ALCANCE DEL GATE, y se declara ANTES

Medido al armar el arnés: `EXPO_PUBLIC_PAGOS_ALTA_URL` apunta a
**`epetplace-pagos-stg.vercel.app`**. **Mis cambios de i18n viven en
`apps/pagos-web/` y NO están desplegados** — y el canon de S101 registra que **la
sesión CLI de Vercel se cerró** en el barrido de seguridad.

⇒ El gate corre contra **la página servida desde esta máquina** por `adb
reverse`. **Se le dice al founder con todas las letras**, porque *su OK sobre mi
página no cubre la desplegada*, y leerlo al revés sería dar por gateado algo que
no está vivo.

> **Es `L-318` un piso más arriba:** pieza construida ✓ · gate posible ✓ ·
> **desplegada ✗, y el canal está cerrado.** *La lección madre de S101 volviendo
> a cobrar, ahora sobre la tanda entera en vez de sobre una función.*

## 📋 ⑤ UN COSTO DE LA REGLA 85 QUE NADIE HABÍA DECLARADO

La app no alcanzaba Supabase: `apiLista=false`, `initApi()` nunca corría, la
pantalla mostraba su error honesto. **No era defecto de la app: era mi arnés.**

**`.env.local` no está trackeado ⇒ NO VIAJA al worktree nuevo.** La regla 85
manda abrir worktree como primera decisión, y **su costo no declarado es que todo
lo no trackeado se queda atrás** — justo los archivos que hacen que la app
funcione. *Cuesta un `cp`; me costó dos arranques y una captura entenderlo.*

---

---

# 📡 CARRIL EXTERNO · DEUNA — nota de mesa del 21-ago (registro, **cero construcción**)

**No es trabajo de esta pista y no se toca:** *la construcción DeUna es sesión
propia post-credenciales*. Se anota acá porque **dos de sus datos ya condicionan
decisiones de superficie del circuito de pago**, y la sesión que las tome tiene
que encontrarlas escritas.

- **La doc llegó** (comercial + técnica). **QA depende solo del correo con RUC**
  —enviado por el founder—; la afiliación de producción corre en paralelo y
  exige **cuenta propia de Banco Pichincha**, en verificación.
- **Tarifario del riel, MEDIDO: 2 % + IVA.**
- 🔴 **El reverso es del mismo día, por el monto TOTAL, sin parciales** ⇒
  **ratifica la arquitectura saldo-céntrica.** *Un riel que no admite reversos
  parciales convierte cualquier devolución parcial en un problema de saldo, no de
  pago — y eso es decisión de diseño antes que de motor.*
- 🔴 **Su webhook trae DATO PERSONAL NUEVO: cédula y nombre del pagador** ⇒ entra
  al **capítulo de retención v3.0** y a **`D-405`**. *Un campo que llega solo
  porque el proveedor lo manda no es un campo que tengamos derecho a guardar: eso
  lo decide la letra, no el payload.*
- **El corte del 11-sep sigue vigente y hoy corre a favor.**

---

---

# TANDA 8 · EL GATE EN EL APARATO — dos pasos verdes, un botón nuevo, y un rojo que era mío

## ✅ PASO 1 · LA PÁGINA DEL ALTA HABLA LOS DOS IDIOMAS

Canario **20/20 en inglés**, y llegó **por los dos canales** —consola del
WebView y `ReactNativeJS`—, o sea que **la cura de `L-318` funciona**: el
diagnóstico ya no muere en el `onMessage`. Página **y campos del proveedor** en
inglés (`Cardholder name` · `Card number` · `MM / YY` · `CVC`).

### 🔴 EL HALLAZGO QUE CASI ME HACE REPORTAR UN ROJO FALSO

**Ver la pantalla en español no prueba NADA:** medido, *«Ponle un nombre»,
«Agregar tarjeta», «Puedes dejarlo vacío» están en el HTML estático*. El caso
español **se ve idéntico con la maquinaria andando y con la maquinaria muerta**.

> ### **El caso que discrimina es el INGLÉS. El español es el caso nulo.**
> *Un gate que solo mira el idioma por defecto no está midiendo el idioma:
> está mirando el archivo.*

## 🔴 EL ROJO QUE ERA MÍO — la precedencia de `.env.local`

Primer intento: header nativo en inglés, **página entera en español**, botón
encendido, canario ausente. **Tres síntomas independientes ⇒ una sola causa**, y
no eran tres defectos: **la app estaba abriendo la página de Vercel.**

**Supuse que la variable del shell ganaba sobre `.env.local`. Es al revés.**
*El override que creí estar aplicando nunca se aplicó, y estuve mirando código
ajeno convencida de mirar el mío.* Curado apuntando el `.env.local` al servidor
local. **Va al relevamiento de instrumentos: es una precedencia supuesta, no
medida — la misma familia que el enum `'vivo'`.**

## ✅ PASO 2 · LOS CINCO LOGOS, con su límite dicho

**Invariante 5/5:** aspecto **exacto** (desvío `0.0e+0` / `2.2e-16`) y los cinco
contenidos en la caja. **Control positivo:** el mismo cálculo **con** `Math.round`
—el defecto que se cazó— da **0,0317** ⇒ *el invariante discrimina.*

⚠️ **LÍMITE DECLARADO:** visualmente **solo se ejercitan Visa y Diners** — la
cuenta no tiene tarjetas de Mastercard, Amex ni Discover. *El invariante cubre
el aspecto de las cinco; el ojo, dos.* **Y el fallback de texto tampoco se pudo
ver**: exige una marca sin archivo, que hoy no existe en la cuenta.

## 🔴 EL BOTÓN ÚNICO — orden del founder, y destapó que había DOS

**① Nace apagado y enciende con los datos** — verificado en el aparato: pálido
al entrar, ocre pleno al completar los cuatro campos.

**② Con el OTP pasa a «Validar»** — y al medir el SDK apareció lo que nadie
había mirado: **`initVerificationInput()` crea su PROPIO botón** (`value:
'Validar'`, en español **fijo** dentro del SDK) cuyo click llama
`verifyTransaction()`. ⇒ **Al aparecer el desafío había dos botones para un solo
acto.** *Y el suyo habría dicho «Validar» en medio de una pantalla en inglés.*
Se oculta el suyo; el nuestro llama **al mismo** `verifyTransaction()`.

**🔒 ② queda CONSTRUIDO-NO-GATEADO.** Sin credenciales reales el proveedor no
emite OTP. **Disparo: la primera ventana con OTP real** (staging con
credenciales o certificación). *Se declara en vez de darlo por visto: nadie lo
vio cambiar de etiqueta.*

### 🔴 Y EL DEFECTO QUE MI PROPIA CURA TRAJO ⇒ `L-334`

La v1 preguntaba `pf.isValidData()`. **Su nombre dice consulta; su cuerpo llama
a los ocho `refresh*Validation()`, que PINTAN** ⇒ los cuatro campos entraban
**con borde rojo antes de que la persona tocara nada**. *Peor que el botón
encendido que la cura venía a arreglar.* Curado componiendo **la misma
conjunción** con los predicados **puros** del propio SDK.

## ⚖️ LA FRONTERA DEL FAIL-OPEN *(dictamen de mesa — a la bitácora Y al comentario)*

> ### **La SUPERFICIE puede fallar abierta SOLO PORQUE EL MOTOR FALLA CERRADO.**
>
> El botón no autoriza nada: abre un formulario que el proveedor valida y un
> endpoint que rechaza. **Lo que protege el dinero está del otro lado.**
>
> 🔴 **Es DIRECCIONAL y jamás se copia al servidor.** Una policy, una RPC o una
> compuerta que ante un error propio **conceda** no es amable: es una puerta
> abierta. *El precedente no existe para ese lado, y queda escrito en el propio
> `catch` para que quien lo lea buscando permiso encuentre la negativa.*

---

---

# TANDA 9 · LOS PASOS 3, 4 Y 5 · LAS DOS CITAS · Y `main`

> ⚠️ **Este bloque existe porque el acta de estado se escribió ANTES de correr
> estos pasos.** *Un acta que se cierra antes de que termine el trabajo describe
> la intención, no el resultado* — y el modo de falla es que nadie vuelva a
> abrirla. **Se agrega marcado en vez de reescribir el acta, que está firmada.**

## ✅ PASO 3 · EL COBRO FRESCO, DE PUNTA A PUNTA CONTRA SANDBOX

Reservado **por la app en el aparato**, no sembrado: mascota → duración → día →
hora → paseador → checkout con hold de 15 min → medio de pago → **Pagar**.

**La espera pasó SOLA** —barra de marca, voz honesta *«Puedes cerrar esta
pantalla: tu reserva sigue en pie»*— **a «Paseo confirmado»**, con el glifo del
oficio y el CTA ocre.

**El rastro, medido en la base y no inferido de la pantalla:**

```
evento_cita_servicio  5cfec36f-887d-4770-b030-87f4b7cfedfc
                      confirmada · pagada · $6.00 · 21-ago
cita_desglose         1 fila
pagos_intentos        aprobado · $6.00 · authorization_code 7cQx83
```

> *El circuito que S102-B va a tocar en su paso 2 quedó **probado vivo justo
> antes de que lo toque**.*

## ✅ LAS DOS CITAS · la segunda es insumo de otra pista

```
5cfec36f  21-ago  confirmada · pagada          $6.00  desglose:1   ← pagada acá
4f7a18ce  22-ago  pendiente  · pendiente_pago  $6.00  desglose:1   ← insumo de S102-B
```

**Las dos nacieron por la puerta real, con su desglose congelado al insertarse.**
*La mesa descartó sembrarlas a propósito: **un caso sembrado probaría al
sembrador, no la puerta.*** Dueño `guillo381+8` — **la única cuenta con tarjeta
guardada**, lo que hace que el cobro de cierre de B **no dependa del alta**, que
es justo la pieza que no está desplegada.

⚠️ **Y esta tanda le VENCIÓ el snapshot al guard de S102-B**: esperaba
`citas=7 · pedidos=34` y la base pasó a **`8/35`** — *porque las citas del gate
son tráfico real*. **No se editó a 8/35: se re-mide bajo veda en el acto del
apply, declarando la hora.** Es **`L-329`** cobrándose el mismo día que se fundó.

## ✅ PASO 4 · `cuenta/pagos` — el encabezado dejó de mentir

Verificado **en los dos idiomas**: el título va solo, **sin la banda de
«simulado»**, y **cada fila histórica conserva su marca por fila** — que es lo
correcto y no un defecto.

## ✅ PASO 5 · VOSEO — cero, y el instrumento con su control

**0** en las seis superficies de pago. **El instrumento se corrigió dos veces
antes de dar ese cero:** su primera versión filtraba por línea y su único hit era
**la lápida del voseo ya curado** — *un falso positivo que habría mandado a
«curar» un comentario*. Cura: borrar comentarios antes de medir. **Control:
sembrado uno dentro y uno fuera de comentario ⇒ detecta 1.**

## ✅ `main` — todo adentro, y el freno que lo hizo posible

**`main` = `660a6c44`**, con `--no-ff`, sobre el merge de S102-B (`ebfd8fb2`).

🔴 **Se frenó antes de tocarlo.** S102-B tenía **la misma orden del founder** y
estaba mergeando en ese momento. *Dos merges simultáneos a `main` no producen un
conflicto que se resuelve: producen dos árboles que alguien reconcilia a mano sin
saber cuál era el bueno.* **Fue B primero, A después, sobre su resultado.**

**El merge salió sin un solo conflicto, y se midió ANTES:** *archivos tocados por
las dos ramas = **0***. **`DEUDAS_CANONICAS.md` incluido** — y ése es el caso
interesante: **es el archivo que más se disputa entre pistas, y no hubo disputa
porque B redactó y A depositó.** *La regla de territorio no evitó el conflicto
por suerte: lo volvió inexpresable.*

### 🔴 Y EL HUECO QUE B ENCONTRÓ EN `main`, que era MÍO

Tras el merge de B, **`main` se contradecía a sí mismo**: sus fichas citaban
`D-865`, `D-867`, `L-331`… **y el `DEUDAS_CANONICAS.md` de `main` no los
contenía**, porque el depósito es territorio de A y vivía sin mergear.

> ***`L-331` un piso más arriba, y peor: un número perdido en un mensaje no
> engaña a nadie; una cita rota en `main` sí.***

**Cerrado y verificado leyendo el objeto remoto** — los nueve presentes, control
`D-999` → 0.

## 📚 LO DEPOSITADO EN EL CANON — nueve números, y cinco no son míos

| número | qué dice | autoría |
|---|---|---|
| **`L-330`** | el control positivo viaja **con el número**, no con la confianza de quien lo corrió | A |
| **`L-334`** | un método que se llama `isX` **puede ser una acción**: el nombre no es contrato | A |
| **`L-331`** | medir contra `origin` **reduce** la ventana; la cierra **preguntarle al dueño del territorio** | S102-B |
| **`L-332`** | **capa que borra ≠ superficie equivocada** — se curan distinto | S102-B |
| **`L-333`** | la voz de un instrumento se calibra **contra lo que mide** | S102-B |
| **`L-335`** | un control de instrumento exige **un veredicto, no una detección** | S102-B |
| **`D-865`** · **`D-866`** · **`D-867`** · **`D-868`** | el perfil roto · `requireCommit` · el colapso en `desconocido` · los tres estados sin ejercitar | S102-B |

**`L-335` es la que S102-B había dejado sin número esperando confirmación.**
*Después de un día entero aprendiendo que un número en un mensaje no existe, no
se la iba a dejar viviendo en un mensaje.*

---

# 📋 ACTA DE **ESTADO** DE S101-D — no de cierre

> **Orden de mesa, 21-ago:** *«nada se marca cerrado por estar construido: el
> gate cierra pasos, no los cierra el reporte. Se publica lo incompleto, jamás
> lo falso.»* **Cuatro columnas, y la diferencia entre la primera y la segunda
> es toda la honestidad de este documento.**

## ✅ CERRADO CON EVIDENCIA

| pieza | qué lo prueba |
|---|---|
| **La pasada de diseño (censo A0)** | cola firmada por la mesa; **corrigió el plan TRES veces, las tres achicando** |
| **P1 · el alta habla los dos idiomas** | página **y campos del proveedor** en inglés · canario **20/20** por los dos canales |
| **Los 5 logos + transformer de SVG** | invariante de aspecto **5/5 exacto**; control con `Math.round` da 0,0317 ⇒ discrimina |
| **`D-857` · el voseo del cliente** | **34** curadas (no 32: el enclítico) · censo post-barrido **0**, con control que ignora comentarios |
| **El botón que nace apagado** | verificado en aparato: pálido al entrar, ocre pleno al completar |
| **La renumeración a S101-D** | commit nuevo, jamás amend (regla 86) |
| **La cura editorial `§8bis`/`§8ter`** | la colisión que la orden no conocía, declarada y reversible |
| **El perfil de build + `requireCommit`** | la APK vieja se colgaba para siempre; la nueva arranca. `⇒ D-865 · D-866` |
| **El censo de los 5 repos** | 2 consumidores reales, los dos bajo `ProtectedRoute` ⇒ **`D-860` desbloqueada** |
| **Las fichas de B en el canon** | `D-865` · `D-866` · `D-867` · `D-868`, con su mapeo reportado |
| **`L-334`** | fundada con su caso y **con su límite escrito adentro** |

## 🔶 CONSTRUIDO **NO GATEADO** — con su disparo, y sin maquillar

| pieza | por qué no se gateó | disparo |
|---|---|---|
| **El botón «Validar» del OTP** | sin credenciales reales **el proveedor no emite OTP**; nadie lo vio cambiar de etiqueta | **primera ventana con OTP real** (staging con credenciales o certificación) · `D-859` |
| **La página del alta DESPLEGADA** | los cambios viven en `apps/pagos-web/` y **la sesión CLI de Vercel se cerró** en el barrido de S101 | reapertura del canal — **dueño founder** |
| **3 de las 5 marcas** (MC · Amex · Discover) **y el fallback de texto** | la cuenta **no tiene tarjetas** de esas marcas | una tarjeta de cada una, o una marca sin archivo |

> 🔴 **La segunda fila es `L-318` un piso más arriba** — pieza construida ✓,
> gateable ✓, **sin puerta al lugar donde importa**. *Un OK del founder sobre la
> página de esta máquina NO cubre la desplegada, y leerlo al revés sería dar por
> gateado algo que no está vivo.*

## ⏸ ABIERTO ESPERANDO AL FOUNDER

- **El veredicto del gate** — ③ la elegida · ⑥ bajo el fold · el acabado del alta con el botón nuevo. *Son juicios de píxeles: los cinco pasos están verdes por instrumento, pero el ojo no lo puede reemplazar un invariante.*
- **La autorización de push del CÓDIGO** (regla 82 — hubo escrituras después de la última).

## ⏸ ABIERTO EN COLA

- **Tanda 2: ③ y ⑥**, que **no se construyen antes del juicio del ojo** — construir primero sería elegir por él.

## 🔬 NOTA DE INSTRUMENTO DEL PROPIO CIERRE

Verificar que ningún número citado quedara huérfano **falló dos veces antes de
funcionar**: el canon tiene **al menos TRES formas** para la misma familia
(`### D-864 …` · `## ☠️ D-851 …` · `- **L-191 — …**`), y mis dos primeras pasadas
**asumieron una sola** ⇒ reportaron **1 y luego 11 huérfanas falsas**.

> *Un verificador que asume el formato de lo que busca produce huérfanas que no
> existen — y su salida se lee exactamente igual que un hallazgo.*

**Resultado con las tres formas medidas: 19 citados, CERO huérfanas.** Controles
declarados: `D-999`/`L-999` inventados → ausentes ✓ · `D-865`/`L-334` recién
depositadas → presentes ✓.

---

## 📌 LO QUE ESTA TANDA DEJA PARA LA MESA

| # | Qué | Tipo |
|---|---|---|
| 1 | **El reparto del perímetro i18n** (§4bis): P1→A · P2/P3/P4→B · **+ la pregunta del formulario del proveedor** | reparto |
| 2 | Los **tres candidatos que quedan** en §4 (hex sin declarar · URL del isotipo · caja del logo) | gate |
| 3 | La **cola re-ordenada** (⑤ ⑥ ⑦ + N1-N4 + P1), tanda por tanda | autorización |
| 4 | ③ **la elegida inconfundible** y ⑧ **la animación** | su ojo, en dispositivo |
| 5 | **`§8bis`/`§8ter`**: confirmar o pedir `§11` | decisión chica |

**Autorización pendiente del founder: push de cualquier tanda · gates.**

*Se publica lo incompleto, jamás lo falso.*
