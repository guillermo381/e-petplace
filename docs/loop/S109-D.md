# S109 · PISTA D — parte

> 31-ago-2026 · rama `pista/s109-d`. **Sin OTA** (el founder tiene uno en curso).

---

## ① LA GUARDERÍA ENTRA AL HOY DEL PRESTADOR — cerrado y verificado

El founder pagó **5 estadías para hoy** y no las veía. La causa era una línea:

```ts
if (oficio === undefined) continue; // hospedaje/otro: fuera del v1
```

`guarderia_dia` tiene `categoria = 'hospedaje'` ⇒ `obtenerOficiosNegocio` la
descartaba y **su baldosa nunca se pintaba: no vacía, ausente.** *Una exclusión
escrita cuando el oficio no existía, que sobrevivió al mundo que la justificaba.*

**Y una premisa se cayó al medir:** el HOY **no es una lista de citas ordenada
por hora** — es una grilla de baldosas por oficio que sólo usa `r.data.length`.
Por eso la `hora` NULL de la estadía **no era problema y no hubo criterio de
orden que elegir**. *(Si hiciera falta, medí que no hay con qué: la franja es del
PRESTADOR —`guarderia_franjas` no tiene `estadia_id`—, las cinco comparten
ventana 7:00–9:00 / 16:30–18:30 y `duracion_minutos = 690`. La familia eligió un
**día**.)*

**Discriminador, ejercido con la sesión del titular contra la base:** la baldosa
vería **5 estadías hoy — Kira, Kira, Kira, Pepe, Thor**. Las cinco que el founder
pagó y no veía. **Commit `39c3162b`.**

---

## ② LOS MAPAS — medido, sin curar el diagnóstico

**Las tres causas candidatas, descartadas contra el objeto:**

| # | qué | resultado |
|---|---|---|
| ① | ¿existe la zona? | ✅ `lat −0.197058 · lon −78.438188 · radio 500 m` |
| ② | ¿el lector la trae? | ✅ `obtenerPerfilesPublicos` selecciona las tres columnas por nombre |
| ③ | ¿mal encuadre? | ✅ correcto: `initialRegion` y `Circle` comparten centro y con `AIRE = 2.6` el círculo ocupa **~77 % de la vista** |

⇒ **El defecto no está en el software del repo.** Queda la hipótesis del
**binario local** (no existe build 1.0.6 de cliente en EAS ⇒ su APK es local ⇒
**otro keystore, otro SHA-1**; con la key restringida por firma, el `MapView`
monta y **los tiles no cargan** — que es el síntoma exacto). **La prueba
discriminante es del founder:** instalar el **APK de nube 1.0.5** —verificado
VERDE (`geo.API_KEY` · `google_app_id` · package · SHA-1
`3331ac303cdcf82517ae1279e0900ceedce95b61`)— y abrir la misma vitrina.

**Construido (commit `c9872408`):** los guards con voz en las **cuatro**
superficies que montan mapa desde la pantalla — paseo ×3, pedido en camino, el
durante de guardería y el pin de dirección. *(Censo corregido en el camino: en
`checkout` el `PinMovible` **no se monta**, sólo figura en comentarios.)*

🔴 **El flip del flag queda FRENADO, con su medición:** el módulo
`sonda-manifest` entró al cliente el **29-ago** y el APK de nube es del **24** —
**ese binario no lleva el módulo**. Aplicarlo hoy **apagaría los mapas en un
binario que sí tiene la key**, y caer al literal es *fail-open con mejor nombre*.
**Viaja con el próximo binario, después de los guards.**

---

## ③ 🔴 LA CURA DE «CÓMO TE VEN» ESTÁ CORRECTA Y **SIN EJERCER**

**No se declara cerrada, y la razón es la que esta sesión viene nombrando toda
la semana: es una pieza que llegaría a producción sin haber corrido.**

**Qué es** (commit `f66620f9`): de las **tres** superficies que pasan zona a
`FichaPrestador`, era **la única sin guard** — y es la app donde el flag **sí
mide**. En un binario sin `geo.API_KEY` el flag daba `false`, la pantalla no lo
consultaba, montaba el `MapView` igual y **mataba la app en hilo nativo**, fuera
de toda ErrorBoundary.

**Por qué no se puede ejercer hoy:** es del **prestador**, y **no hay build
publicada de esa app**. Verificado por typecheck y por `verify:diseno`; **no
verificado en aparato, porque no hay aparato donde verlo.**

⚠️ *Y el modo de falla que cura es invisible por definición: una app que muere en
hilo nativo no deja pantalla de error. Que su cura llegue sin correr es
exactamente la clase que esta casa aprendió a no dar por buena.*

---

## ④ QUÉ HARÍA FALTA PARA PUBLICAR UNA BUILD DE PRESTADOR — medido, **no ejecutado**

**Lo que YA está** (nada que resolver):

- perfil **`preview`** en `eas.json` (`distribution: internal`, `buildType: apk`,
  `environment: development`);
- **las dos claves en ese environment**: `GOOGLE_MAPS_API_KEY` y
  `GOOGLE_SERVICES_JSON` ⇒ la build traería **mapas y push**;
- **keystore**: existe (hubo builds hasta **1.0.6 · 24-ago**).

**Lo que hay que resolver ANTES, y son tres decisiones, no un comando:**

1. 🔴 **De qué commit sale.** `requireCommit: true` hornea **el HEAD del
   worktree**. Lanzada desde `pista/s109-d`, la build llevaría **mi trabajo sin
   mergear** — y nada de las otras pistas. **Sale de `main`, con todo mergeado,
   o no representa a la sesión.**
2. 🔴 **El salto de runtime.** `app.json` dice **1.0.7** y la última build es
   **1.0.6**. Con `runtimeVersion: appVersion`, **una build 1.0.7 no recibe
   ningún OTA publicado contra 1.0.6** — y el founder **tiene uno en curso**.
   *Publicar la build antes de que ese OTA cierre deja el teléfono sin poder
   recibirlo* (precedente literal en canon: el 1.0.3 de S78).
3. **La cuota de EAS**, que no es medible sin lanzar: el precedente de S78 es que
   rebotó y hubo que hacer `build --local`.

**Y la pregunta que no es técnica:** una build nueva **cambia el runtime del
prestador**. Eso es del founder, no de una pista.

### ✅ FIRMADO (founder, 31-ago): NO SE LANZA TODAVÍA

**Por la razón ② y por el orden:** una build 1.0.7 **no recibe el OTA publicado
contra 1.0.6** y dejaría el teléfono de negocios sin poder recibir lo que ya
está arriba (S78, literal). Y el OTA de familia **todavía no se verificó en
aparato**: *un runtime nuevo en la otra app mete una variable más en un
recorrido que no corrió.*

**CUÁNDO SÍ:** cuando el founder verifique el OTA y **S109 cierre**. Sale
**desde `main` con todo mergeado** —la ① no se negocia— y con eso **«Cómo te
ven» deja de ser una cura sin ejercer**.

🔑 **La condición no vive acá: vive en `como-te-ven.tsx`, al lado del guard.**
*Un parte lo lee quien lo busca; el código lo lee quien lo toca.*

⚠️ **Y sobre `build --local` si la cuota rebota** (aceptable para el prestador,
**avisando antes**): **cambia el keystore y con él el SHA-1** — que es
**exactamente la variable que estamos midiendo hoy en el cliente**. Una build
local del prestador nacería con el mismo problema de firma que estamos
diagnosticando, y **no serviría para descartarlo: lo reproduciría**.

## ⑧ LA QUINTA PATA Y EL CENSO (31-ago, las dos firmas)

**① `1f26ca39` — las estadías entran al HOY.** Causa: `(tabs)/index.tsx` pedía
CUATRO lectores escritos a mano y sus comentarios lo cantaban («tercera pata de
la MISMA jornada», «CUARTA pata»). **No es el mismo defecto que la baldosa de
ATENDER** —ahí era un mapa (`KEY_CITAS_DEL_DIA`), acá cuatro llamadas literales—:
por eso curar uno no curó el otro, y por eso yo había curado la pantalla
equivocada. Forma firmada: **agrupadas ARRIBA como bloque de presencia**, jamás
intercaladas (poner 00:00 para que entren en una lista por hora es fabricar un
dato que nadie firmó, y el prestador lo lee como compromiso). Tap → `/guarderia/dia`,
donde ya llegaba. Y el vacío se corrigió: *«hoy no tienes citas»* es falso con
animales a bordo.

**DEUDA DECLARADA EN EL CÓDIGO:** viene en su propio viaje porque
`obtenerEstadiasDelDia` toma UNA fecha y los otros cuatro traen diez días en un
fetch. **Pedido a A: el lector por rango.** El día que exista, esto se pliega al
`Promise.all` y el efecto muere.

**② `521c54a2` — `verify:jornada-completa`.** El universo sale del objeto (los
`obtener…DelDia` de `packages/api`); cuenta **referencias, no llamadas** (si no,
ciego sobre `atender.tsx`); control positivo **mutante** (rojo con el lector
borrado, verde sobre el real) y las dos ramas probadas; declara adentro las
cuatro cosas que no ve. **Encontró una CUARTA superficie que nadie reportó:
`historico.tsx`** — «Tu histórico» enumera los mismos cuatro. Queda como **deuda
con nombre** (bloqueada por el mismo lector por rango), y el gate se pone rojo
también si una deuda listada aparece curada.

**Sigue abierto y es mío:** la vitrina de guardería que no pinta la imagen hasta
arriba — medir contra una vitrina que sí lo hace antes de tocar.

## ⑨ EL PLIEGUE, LA VITRINA Y LOS DOS VERDES FALSOS (31-ago)

**② `ab833f2b` — la quinta pata se plegó.** Murió el efecto propio, el
`obtenerMiPrestador()` encadenado y la re-consulta por giro de rueda. **Y al
plegarse adoptó la ley de sus hermanas** (Ley 13): su fallo rebota la pantalla —
callar era correcto mientras el bloque era un añadido, pero ahora el vacío mira
las DOS listas. **El pliegue obligó un filtro nuevo** (`estadiasHoy`): el estado
tenía un día y pasó a tener diez. *Cambiar de dónde viene un dato cambia qué hay
que filtrar.*

**🔴 Y EL PLIEGUE DESTAPÓ QUE MI GATE DABA VERDE POR DOS RAZONES EQUIVOCADAS**
(`L-457`): el universo estaba atado al sufijo `…DelDia` ⇒ el lector nuevo era
invisible; y el verde del HOY venía de **un comentario** — la lápida del pliegue
nombra al lector muerto y el regex la contó como referencia viva. El control no
lo cazó porque **mutaba de más**. Curado: universo por OFICIO, texto sin
comentarios, mutante que borra sólo el código.

**`historico.tsx` NO se cura con este lector, y la razón se midió al intentarlo**
—no se asumió—: (a) ofrece «90 días» y un «ver más» de 30 en 30 **sin tope**, y
el lector rebota sobre 62; (b) su lista es de `CitaAgendaPaseo` y una estadía no
trae hora, tipo, duración, precio, empleado ni atención ⇒ montarla obliga a
**fabricar nueve campos**, que es la fila verosímil-falsa de `L-139` y es lo
mismo que rechacé en el HOY con la hora. **Queda en el gate con su razón
ACTUALIZADA** —una razón vencida en un baseline es su propio defecto— y con lo
único que la destraba: **la proyección de guardería con la forma de sus cuatro
hermanas** (la cita existe: la estadía carga su `cita_id`).

**① `b929f2d5` — la vitrina de guardería sangra hasta el techo.** Era **un header
propio**, y la causa es una contradicción que la pantalla declaraba: `aSangre` +
una barra opaca arriba (`L-458`). Descartados uno por uno safe area y padding de
contenedor contra la vitrina que sí lo hace. Se quita sólo el de la vitrina
cargada; carga y error lo conservan.

**Cerrado lo mío.** Queda pedido a A la proyección de guardería con forma de
cita — destraba `historico.tsx` y saca la última deuda del gate.

## ⑩ EL DURANTE MEDIDO, LA CARA Y EL MAPA (31-ago, cierre)

**El aviso del durante era CORRECTO (`449ca9aa`), no una lápida vencida.** 95
estadías **todas en `reservada`**, timestamps en cero, y **seis de los siete
estados del CHECK son inalcanzables**: ninguna función escribe la transición.
Ganó su **condición de muerte** con los números que hoy la prueban. Y `b722d858`:
el apoyo pasó a decir **qué SÍ se puede** — explicaba nuestro plan de obra, no su
trabajo, y era la frase que se pudría primero.

**La cara (`a6a08623`).** La escalera existía (`D-806`) y **tres** superficies no
la usaban. Curadas dos; `mostrador/autorizar` **no puede** —su lector no trae
especie— y va como pedido. 🔴 `dia.tsx` **parecía correcta leyendo el JSX**
porque pasaba `especie`, y el contrato de `AvatarMascota` dice que esa prop *«hoy
no cambia el render»* ⇒ **`L-460`** (nació `L-459` y chocó con la de A del mismo día — número provisional, lo confirma A).

**El mapa (`25d26cf3`).** Nace **`MapaPunto`** — pieza nueva, no `MapaZona` con
radio chico: esa pieza *prohíbe* la coordenada exacta en su cabecera y su
etiqueta de accesibilidad dice «aproximada». **La cura es de la clase**: paseo y
grooming ganan lo mismo. **El guard no es opcional** y su caída es el estado
anterior completo, no un degradado roto. La distinción de la letra de S84
—**familia mirando una vitrina** vs **prestador yendo a una casa**— quedó escrita
**dentro de la pieza**.

**Pedidos a A: `docs/loop/S109-D-PEDIDO-A.md`** (el peldaño de la raza · la
proyección de guardería con forma de cita · los dos gates al hook).
