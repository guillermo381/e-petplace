# S100c-D · PARTE — Pedidos como casa, y el mapa que no se deja tocar

> ## 🔖 HANDOFF DE RELEVO — LEER ESTO PRIMERO
>
> ### CÓMO SE LEE EL ESTADO (comandos, no SHAs tecleados)
> **No hay SHA escrito acá a propósito:** *un commit que registra su propio SHA
> lo cambia al registrarlo.* Se leen del objeto:
> ```
> git ls-remote origin pista-d      # el mío; contra él se compara lo local
> git rev-parse pista-d             # tienen que coincidir (L-239)
> git status --porcelain | wc -l    # 0 al cierre
> git merge-base --is-ancestor pista-d origin/main   # ¿llegó todo al canon?
> git diff --name-only <ancla-del-gate> pista-d | grep -v '^docs/'  # código afuera
> ```
> ⚠️ **Y lo mismo vale para lo ajeno y para lo propio:** *«en cero» describe un
> INSTANTE y una veda necesita un INTERVALO* — avisé «estoy en cero» cuatro
> veces y seguí commiteando después (§9).
>
> ### ✅ ② LO MÍO EN `main`, MEDIDO — Y LO QUE FALTA ES ESTE ARCHIVO
> **Todo mi CÓDIGO está en `main`** (entró con el merge de `b979e6a0`) y **está
> en el ancla del bundle que el founder gateó** — verificado por
> `merge-base --is-ancestor`, y por blob: `nombre="pedido"` = 1 y `miniatura=`
> = 1 en el ancla. **Código de `pista-d` fuera del ancla: CERO.**
>
> 🔴 **LO ÚNICO QUE NO ESTÁ EN `main` ES `docs/loop/S100c-D.md` — este
> archivo** (mis últimos commits de parte). **Lo declaro con su nombre porque
> es lo único que se pierde silencioso**, y porque *un handoff que no está en
> main es un handoff que no existe para quien abre después.* **Pedido a A: el
> merge de docs.** Se verifica con
> `git show origin/main:docs/loop/S100c-D.md | grep -c "HANDOFF DE RELEVO"`.
>
> ### VERIFICACIONES, CORRIDAS AL CERRAR (18-ago-2026)
> `tsc apps/cliente` **exit 0** · `verify:diseno` **VERDE, 46 reglas** · árbol
> en **0** · **cero migración, cero dato vivo movido, cero `packages/ui`
> tocado**.
>
> ---
>
> ### ① 🔴 EL CRUCE QUE LA MESA PIDIÓ Y **NO PUEDO HACER: EL VEREDICTO 1-31 NO ESTÁ EN EL REPO**
> Se me pidió marcar cada hallazgo *«con el número del founder al lado
> (1-31)»*. **Lo busqué antes de escribir y no existe:**
> ```
> git grep -lE "hallazgo 3[01]" origin/main -- docs/            → nada
> docs/loop/S100-GATE-FOUNDER.md  → es de S100, DOS vueltas atrás
> docs/loop/S100c-RESUMEN-DE-PUBLICACION.md §8 → OCHO juicios, y son PRE-gate
> ```
> ⇒ **lo que existe es la lista de ocho de A, escrita ANTES del gate; el
> veredicto pantalla por pantalla no llegó a ningún archivo.** *No lo
> reconstruyo de memoria ni le invento números: poner la vara equivocada
> delante del ojo es el modo de falla que esta sesión pasó ocho gates
> aprendiendo.* **Abajo va MI numeración —los seis encargos que recibí,
> `D-01` … `D-06`— y el cruce lo hace quien tenga la lista.**
>
> ### ① EL ESTADO DE MIS SEIS ENCARGOS
>
> | # | encargo | estado |
> |---|---|---|
> | **D-01** | Pedidos como tab + casa del postventa | ✅ **HECHO** — **quinta tab**, Pedidos **cuarto** para que la Despensa quede centrada (§7 ciclo del trono), casa con **en curso arriba / historial abajo** (corte por `es_terminal`, dato del catálogo), acceso al local en los **dos** estados, encabezado de portada, y **entrada desde Cuenta**. **Facturas: resuelto por medición** (el número en la carta del total, **sin casa** — ver ⑦). **«Reclamaciones»: NO-HECHO y frenado en la mesa** — no tiene objeto (§2①) |
> | **D-02** | pedidos en vuelo en el Hogar | ✅ **HECHO** — UNA fila colapsada (letra §10ter.1: *acción no colapsa, información sí*), `pagando` afuera, `mascotaId: null` |
> | **D-03** | la tarjeta no se distingue | ✅ **HECHO** — título = **qué trae** + **miniatura** con `Image`. ⚠️ **y MEDIDO-NO-RESUELTO: no llega a unicidad en la cuenta del gate**, y la causa es el dato (§0⑥) |
> | **D-04** | el resumen sin superficies | ✅ **HECHO** — **cuatro cartas** (N21), y el corte lo dio **medir la referencia**, no interpretar la ley |
> | **D-05** | el mapa inútil + hoja arrastrable | 🟡 **DOS TERCIOS HECHOS.** ✅ hoja arrastrable con la física del paseo · ✅ **destino dibujado** (mapa aunque no haya track) · ✅ **la moto** (`MarcaDeMapa`, N14 cierra) · 🔴 **NO-HECHO: el mapa sigue sin aceptar el dedo** — `scrollEnabled={!esVivo}` / `zoomEnabled={!esVivo}` (`MapaRecorrido.tsx:145-146`). **Es pieza ⇒ de B**, acordado para la tanda que se gatea junta |
> | **D-06** | ficha del repartidor tipo Uber | ✅ **HECHO** — carta, **placa mandando**, vehículo, nombre, y **el hueco de la foto dibujado y con voz**. La foto sigue siendo deuda firmada (bucket de cédulas) |
>
> ### 🔴 ③ LAS CURAS DE DOS MITADES — quitar una reintroduce el defecto sin verlo
> - **La hoja arrastrable + `PISO_DEL_MAPA`.** El `maxHeight` de la hoja **se
>   DERIVA del lienzo medido** y el `flexShrink: 1` del bloque plegable es lo
>   que hace que ese techo lo ACOTE. *Sacar el `flexShrink` deja un
>   `ScrollView` sin altura acotada: **no scrollea, se recorta**, y en pantalla
>   se ve idéntico a una zona muerta de gesto.* **Van juntos.**
> - **El destino del mapa + `centroInicial`.** Se pasan **los dos**: sin
>   `centroInicial` un pedido sin track abre el mapa en el default de Quito —
>   *un mapa correcto de un lugar que no es el tuyo.*
> - **La miniatura + su ausencia.** `res?.portada == null → undefined`, y la
>   pieza **no reserva lugar** cuando falta. *Rellenar el hueco con un
>   placeholder pone una caja rota en 1 de cada 5 filas* (5 de 23 medidos).
> - **`portadorDeEstado` + su guard** (herencia de mi predecesora, intacta):
>   el defecto era **el ORDEN de las ramas**, que no tiene forma y ningún
>   typecheck ve. *Si alguien vuelve a poner esos `if` en la pantalla, el
>   guard mide su eco.*
>
> ### ④ LO QUE DECIDÍ **NO** HACER, con su razón
> - **NO monté la casa «Facturas»:** las 6 facturas tienen `archivo_url` **y**
>   `pdf_url` en **CERO** ⇒ *un botón que no abre nada es una puerta que
>   rebota, y encima sobre el papel de la contabilidad ajena.*
> - **NO inventé «Reclamaciones»:** cero tablas `%reclamo%`, y el nombre ya
>   está ocupado por *«¿Compraste en el local?»*.
> - **NO puse el `numero_orden` en la tarjeta:** cerraría la ambigüedad medida
>   y **choca con letra Chanel firmada**. Servido a la mesa, no ejecutado.
> - **NO toqué `packages/ui`** en ningún momento: **cuatro pedidos a B con su
>   caso**, y los cuatro volvieron construidos.
> - **NO curé el `insets.bottom` de `cuenta/index.tsx`** teniéndolo a un
>   renglón: *una cura que viaja de polizón en un commit de otra cosa es una
>   cura que nadie revisó.*
> - **NO corrí instrumento de RN-web:** medido por mi predecesora que **toda
>   navegación por recarga corre los efectos antes de que el entry inicialice
>   la API** ⇒ *un rojo que significa «no pude medir» se lee como «está
>   roto».*
> - **NO abrí la ventana para la voz del pin** — razón de A, mejor que la mía:
>   **el costo del cuarto group** (⑨bis).
>
> ### ⑤ 🔴 LO MEDIDO, Y CONTRA QUÉ — porque lo caro es no saber quién puede re-medirlo
> **MÍO, contra la BASE VIVA (18-ago-2026) — se re-mide con `db query`, sin teléfono:**
> · **23 pedidos del founder en 5 días locales; NUEVE el 17-ago y NUEVE el
> 12-ago** · con solo el día, **1 de esas 9 es distinta**; +producto **4**;
> +estado **5-6**; +total **7**; +`HH:MM` **7 y 4** · **20 de 23 con UN ítem**
> · **18 de 23 con foto** (catálogo: **161 de 470**) · **12 en vuelo**
> (preparando 10 + en_camino 2) · **`facturas` = 6, del founder 5,
> `archivo_url` 0, `pdf_url` 0, `clave_acceso` 1**.
>
> **MÍO, contra la FUENTE — se re-mide con `grep`:** los tres cabos del destino
> ya construidos y sin unir · `scrollEnabled={!esVivo}` en `:145-146` · las 16
> rutas de `(tabs)/explorar` · los dos consumidores de `/despensa/pedidos` ·
> `FilaReco_.mascotaId` usado en UN solo lugar · la fila del pedido
> **declarada sin montar** en `hogar/index.tsx` desde S82-C.
>
> 🔴 **AJENO Y EN APARATO — ESTO NO SE RE-MIDE SIN EL TELÉFONO** (B, 18-ago,
> SM-S938B): **caja del ícono 24,2 dp** · **valle 82,7 dp** · **hueco del
> prestador con 5 tabs: 20,7 dp** contra **28,8 dp del cliente con 5** · la
> barra usa **368 dp y no 384** · el visor de una pantalla de tab termina en
> **699,0 dp** · fondo/carta **1,10** contra **1,119** de Laika · el detalle
> del pedido con **0 superficies y 88,5 % de un color**.
> ⚠️ **Y yo NO TUVE APARATO en toda la vuelta**: nada de lo mío es de píxeles.
>
> ### ⑥ DEUDAS, CON DUEÑO Y DISPARO
> | qué | dueño | disparo |
> |---|---|---|
> | **el gesto del mapa en modo `vivo`** (+ recentrar, como Rappi) | **B** | la tanda que se gatea junta |
> | **la INTERPOLACIÓN del marcador** (fixes cada ~60 s ⇒ salta) | B | necesita exponer la proyección; su costo está medido en `MapaRecorrido.tipos.ts` |
> | **los tres glifos a 21 px** (`pedido`, `carrito`, `papelera`) | **founder** | el gate — **no hay rasterizador SVG en el entorno** |
> | **los DOS huecos angostos** de la barra de cinco | **founder** | el gate: *si dos se leen peor que uno, eso es ojo* |
> | **`numero_orden` en la tarjeta** | **mesa** | si el gate ve tarjetas repetidas — **con el aviso de que 9 pedidos en 4 minutos es tráfico de prueba** |
> | **«Reclamaciones»** | **mesa** | no tiene objeto; D-774 (postventa) abierta |
> | **la casa «Facturas»** | — | **el día que `archivo_url` o `pdf_url` se pueblen** |
> | **la voz del pin** (`direccion.puntoAyuda` dice «pin», el dibujo es un punto) | **mío** | la primera ventana después del gate (⑨bis) |
> | **`cuenta/index.tsx` cuenta el inset dos veces** | sin asignar | quien toque esa pantalla por su propia razón |
> | **`H-113`**: narrativa `entregado` con 0 ítems y sin `entregado_en` | motor | heredada de mi predecesora, sigue viva |
>
> ### ⑦ LAS PREGUNTAS DEL GATE QUE NINGÚN INSTRUMENTO CONTESTA
> ① **¿la lista distingue ahora?** — con el aviso de que **algo de repetición
> va a quedar y es el DATO, no la cura** · ② **el `PISO_DEL_MAPA` al 40 %**:
> ¿el mapa preside o la hoja se lo come? · ③ **la hoja arrastrable**: ¿se
> siente como la del paseo? · ④ **el rango flotando arriba** en vez de dentro
> de la hoja (reversible en una línea) · ⑤ **el título de la fila = el
> producto** en vez de la fecha (reversible en una línea; la ley Chanel del
> `numero_orden` sigue intacta) · ⑥ **la fila del pedido en «Ponte al día»**:
> ¿colapsada alcanza, o querés una por pedido?
>
> ---

> **Estado al abrir:** rama `pista-d`, worktree `e-petplace-s100-d`, mergeada
> `origin/main` (`cec1c693`). Territorio: `apps/cliente` — barra de tabs,
> Hogar, Pedidos, detalle, EN CAMINO, entregado.
>
> **Cómo se lee el estado (comandos, no SHAs tecleados — herencia de mi
> predecesora, y su razón: *el commit que registra su propio SHA lo cambia al
> registrarlo*):**
> ```
> git ls-remote origin pista-d      # el mío; contra él se compara lo local
> git rev-parse pista-d             # tienen que coincidir (L-239)
> git status --porcelain | wc -l    # 0 al cierre
> ```
>
> **Este bloque es el DEPÓSITO DE HALLAZGOS previo a construir** (orden del
> arranque). Todo lo de acá está medido contra la base viva, contra la fuente
> o contra la imagen de referencia. Lo que es impresión va rotulado.

---

## §0 · 🔴 LAS CINCO MEDICIONES QUE CAMBIAN UN ENCARGO

**Ninguno de los seis encargos era incorrecto.** Cinco tenían adentro una
premisa que la medición mueve, y en tres casos mueve *quién* hace el trabajo.

### ① D-01 · NADA TIENE QUE CEDER POR CONTRATO — Y LA GEOMETRÍA IGUAL NO DA

El arranque dice *«la barra tiene cuatro slots y hay que decidir qué cede —
probablemente Explorar»*. Medido en la pieza:

> `BarraTabs` declara **«2 a 5 tabs»** (`BarraTabs.tsx:526`). El quinto slot
> **está dentro del contrato firmado**. Nadie tiene que ceder para que
> Pedidos entre.

**Pero la geometría de S99 se calibró con cuatro, y a cinco deja de entrar.**
Aritmética sobre las constantes reales (`DISCO_RADIO 33` · `ANILLO 9` ·
`ENCUENTRO_GRADOS 100` · `INSET_CONTENIDO 8`), donde
`VALLE_SEMI = (33+9)·sin(100°) = 41,36` ⇒ **el valle mide 82,7 dp de ancho**:

| ancho de pantalla | tabs | ancho de tab | **valle / tab** |
|---|---|---|---|
| 360 dp | 4 | 86,0 | **0,96** ✅ |
| 360 dp | **5** | 68,8 | **1,20** 🔴 |
| 412 dp (el aparato de B) | 4 | 99,0 | **0,84** ✅ |
| 412 dp | **5** | 79,2 | **1,04** 🔴 |
| 430 dp | 4 | 103,5 | **0,80** ✅ |
| 430 dp | **5** | 82,8 | **1,00** 🔴 |

⇒ **con cuatro tabs el valle vive dentro de su propia pestaña en todo ancho
medido; con cinco, no entra en ninguno.** Sumando los hombros (22 dp por
lado) la cresta ocupa **126,7 dp = 1,53 a 1,84 pestañas**.

🔴 **Y esto es exactamente L-283 en ropa nueva** (*«un plano y un arco no
producen un cambio de signo: producen un codo»*): **antes de calibrar
números hay que preguntar si la forma admite el efecto.** La línea del
contrato dice cinco; la anatomía que se construyó en ocho gates dice cuatro.
**Las dos son de la misma pieza y nadie las había cruzado, porque nunca
nadie pasó de cuatro.**

⚠️ **Lo que NO afirmo, y es la mitad honesta:** que el valle desborde su
pestaña **no prueba que se vea mal**. El ícono del vecino está a
`anchoTab` del centro activo y el valle llega a 41,4 dp; lo que sí cruza es
**el hombro** (63,4 dp desde el centro). **Si eso muerde el ícono del vecino
depende del ancho de la caja del ícono, que no medí y que se mide en
aparato.** ⇒ **es de B, con su instrumento, y va con el caso completo.**

### ①bis · 🔴 B MIDIÓ, Y ME CORRIGIÓ: MI NÚMERO ERA BUENO Y LA VARIABLE NO ERA LA QUE DECIDE

**B midió en aparato lo que yo declaré que no podía** (caja del ícono
**24,2 dp** · media caja **12,1**), y su resultado da vuelta el foco:

| ancho | pestaña (5 tabs) | borde del ícono vecino | valle (41,4) | **hombro (63,4)** |
|---|---|---|---|---|
| **360** | 72,0 | 59,9 | ✅ libre | 🔴 **MUERDE por 3,5 dp** |
| **384** ← el del founder | 76,8 | 64,7 | ✅ libre | ⚠️ **libra por 1,3 dp** |
| 412 | 82,4 | 70,3 | ✅ | ✅ margen 6,9 |
| 430 | 86,0 | 73,9 | ✅ | ✅ margen 10,5 |

⇒ **EL VALLE NUNCA TOCA EL ÍCONO — sobran ≥23 dp en todo ancho.** Lo que
decide es **el hombro**, y **1,3 dp no es una respuesta que se pueda usar**
(es menos que un redondeo de layout).

🔴 **Y esto es L-285 cobrándome a mí, no a otro:** *declarar contra qué
mediste te salva de la geometría vieja; no te salva de haber medido la
VARIABLE equivocada.* Mi tabla del valle era aritmética correcta **sobre la
magnitud que no decide** — y una medición correcta sobre la variable
equivocada es justo el dato que después alguien cita para cerrar una
discusión. **Se corrige acá, en el mismo lugar donde la escribí, y no en una
nota al pie**; el número viejo queda arriba a propósito, porque borrarlo
escondería de qué me corrigieron.

**La pregunta buena, en la formulación de B:** *no es «¿entra?» — es «¿entra
en el teléfono más chico de la familia?», y ahí la respuesta medida es no.*
**Y B enmienda el contrato «2 a 5» con este número diga lo que diga la
mesa**, por la razón correcta: *un contrato que permite una combinación que
la forma no admite es letra que invita al defecto.*

**Lo demás de D-01, medido y barato:**
- **Mover la ruta cuesta dos líneas:** `/despensa/pedidos` tiene **exactamente
  dos consumidores** — `despensa/index.tsx:776` y `despensa/checkout.tsx:588`.
- **La entrada desde Cuenta es una línea:** `cuenta/index.tsx:56` es un array
  de `lugares` dentro de una `Tarjeta relleno="ninguno"`. *De paso: Cuenta ya
  cumple N21 — el grupo vive en carta. El patrón existe en la casa; el detalle
  del pedido simplemente no lo usa* (ver ④).

### ② 🔴 D-01bis · «FACTURAS Y RECLAMACIONES» — UNA TIENE OBJETO Y LA OTRA NO

**FACTURAS: TIENE OBJETO, Y ES DEL FOUNDER.** Medido:

```
facturas                     = 6 filas
  · con pedido_id            = 6
  · del founder (guillo381+8)= 5
policies                     = facturas_owner (+ insert/update/delete)
lectores en apps/cliente     = CERO
```

⇒ **la tabla existe, la policy deja leer al dueño, hay cinco facturas suyas y
ningún lector las pide.** Es la MISMA forma que H-01 y H-11 de S100 (*la
columna existía, la policy ya abría, el lector no preguntaba*) — la tercera
vez que aparece en este frente. **Construible; es mío.**

**RECLAMACIONES: NO TIENE OBJETO, Y ADEMÁS EL NOMBRE YA ESTÁ OCUPADO.**
- **Cero tablas `%reclamo%` en `public`** (medido en `information_schema`).
- **La pantalla que se llama `reclamo` significa otra cosa:**
  `despensa/reclamo.tsx` es *«¿Compraste en el local?»* — **reclamar una
  compra de mostrador con el código de la factura** (`LETRA_RECORRIDO` §4),
  no una queja.
- Las quejas hoy salen por **«Tengo un problema» → WhatsApp del equipo**
  (`pedido/[pedidoId].tsx:513`), con **D-774 (postventa) abierta**.

⇒ **poner un acceso «Reclamaciones» en la casa de Pedidos sería prometer un
lugar que no existe, o llevar a una pantalla que quiere decir otra cosa.**
*Dos significados bajo una palabra es cómo alguien le dice la placa al
repartidor* — el mismo modo de falla que mi predecesora frenó en la ficha.
**No lo invento: va a la mesa** (§2).

### ③ 🔴 D-02 · LA FILA NO FALTA — ESTÁ DECLARADA SIN MONTAR DESDE S82, Y SU DISPARO YA SONÓ

Literal, en `hogar/index.tsx:1414`, escrito por la pista C de S82:

> *«La fila ALIMENTO del pedido queda **DECLARADA SIN MONTAR**: cero motor de
> despensa (L-139 — no se fabrica el dato); **monta cuando exista**.»*

**El motor existe desde S95** y la casa lo cerró de punta a punta en S96 y
S100. ⇒ **no es un olvido de diseño: es una deuda con disparo escrito cuyo
disparo sonó hace tres sesiones y nadie volvió.** *Un ítem diferido con su
condición adentro es lo correcto; lo que falla es que nada avisa cuando la
condición se cumple.*

**Y la estructura lo admite sin tocar nada** (medido, no supuesto):
`FilaReco_.mascotaId` ya es `string | null` (`:916`) y **se usa en UN solo
lugar**: el cotejo `__DEV__` de la línea `1117`. Un pedido no tiene mascota
—sus ítems pueden ir a varias o a ninguna— y **cae en el mismo precedente ya
firmado que la fila `cita-`: existe en la lista y no cuenta como pendiente**,
porque *«N por resolver» no puede incluir algo que no se resuelve.*

**El sujeto está vivo hoy en la cuenta del gate:**

```
pedidos del founder por narrativa (18-ago-2026):
  preparando 10 · cancelado 5 · pagando 5 · en_camino 2 · entregado 1  = 23
```

⇒ **12 EN VUELO DE VERDAD** (preparando + en_camino). **`pagando` NO entra**,
y no es un detalle: **la promesa nace antes del pago** (censo de mi
predecesora: `pagando` = 4, con promesa = 4, **con pago confirmado = 0**).
*Meter un `pagando` en el Hogar sería prometer una entrega que todavía no
está comprada — la mitad exacta del defecto que S100b curó.*

### ④ D-04 · N21, Y LA CASA YA TIENE EL PATRÓN EN LA PANTALLA DE AL LADO

El censo de B es la vara: **detalle del pedido = 4 grupos rotulados, 0
superficies, 88,5 % un solo color**, con el código de la puerta en un `View`
con padding (`:396`). Medido por mí sobre la fuente: **seis bloques, los seis
`View` pelados** — escalera/estado · código · «Qué pediste» · «A dónde te lo
llevamos» · «El total» · la salida.

**Y la referencia lo confirma midiéndola, no describiéndola.** Uber
(`referencia-uber-tarjeta-del-conductor.jpeg`): su hoja sobre el mapa **no es
un fondo con texto — es una pila de cartas**: el PIN en su carta tintada, «Ride
details» en carta blanca con filete, el conductor en carta blanca con filete.
**La ley N21 ya está aplicada en la referencia de la misma pantalla.**

⚠️ **Y N21 pide UNA superficie por GRUPO, jamás una por elemento.** De los
seis, **la salida (los botones) no lleva carta** —es la acción de la pantalla—
y **la puerta a «en camino» tampoco** —navega—. Quedan **cuatro cartas**,
que es el orden de magnitud de la referencia (Laika resumen: siete cartas).

### ⑤ 🔴 D-05 · DE LAS TRES PARTES, UNA YA ESTÁ CONSTRUIDA Y NADIE LA ENCHUFÓ

**(a) «No puedo navegarlo con los dedos» — la causa es literal y tiene
file:line:**

```
MapaRecorrido.tsx:145   scrollEnabled={!esVivo}
MapaRecorrido.tsx:146   zoomEnabled={!esVivo}
```

En modo `vivo` **el pan y el zoom están apagados los dos**. No es un defecto:
es que **la pieza es dueña de la cámara** y la re-encuadra con cada fix
(`:111-127`). Si el dedo mueve el mapa, el próximo punto lo devuelve de un
tirón — **el mapa pelearía contra la mano.** ⇒ la cura no es borrar dos
líneas: es *el gesto suspende el seguimiento + un control de recentrar*.
**Y la referencia lo trae medido:** Rappi tiene **el botón de recentrar** a la
derecha del mapa (`referencia-rappi-mapa-rango-y-codigo.png`) — *lo tiene
porque su mapa SÍ se navega.* **Es pieza ⇒ es de B, con el caso completo.**

**(b) «Al menos ver dónde está la dirección de entrega» — LOS TRES CABOS
EXISTEN Y NADIE LOS UNIÓ.** Medido en las tres puntas:

| cabo | estado | dónde |
|---|---|---|
| el dato | ✅ existe | `despensa-seguimiento.ts:172-173` (`destino_lat` / `destino_lon`) |
| la pieza | ✅ **implementada, no solo declarada** | `MapaRecorrido.tsx:172-179` (`Marker`) + `:113-127` (el encuadre abarca los dos extremos) |
| la pantalla | ❌ **no las pasa** | `en-camino/[pedidoId].tsx:229-246` |

⇒ **B construyó `destino`, `marcadorDestino` y `marcadorVivo` en S100-B a
pedido de esta misma pista, y la pantalla nunca los consumió.** *Una pieza
entregada y sin consumidor muere sin que nadie se entere* — mi predecesora
escribió esa frase para el `Encabezado.busqueda`, y acá pasó de nuevo con
tres props.

🔴 **Y la cabecera del archivo AFIRMA HOY LO CONTRARIO** (`:48-54`): *«el pin
de DESTINO no se dibuja — `PinEnMapa` recibe píxeles y el mapa no expone
proyección»*. **Era cierto cuando se escribió y B lo resolvió después con
slots.** Es la ley ⑦ de mi predecesora cobrando otra vez: *la prosa del
código envejece igual que la del canon, y con menos testigos.* **Se corrige
al construir, no se deja.**

**(c) La hoja arrastrable — el arranque dice «el paseo ya lo resuelve» y es
LITERAL.** Medido en `paseo/[atencionId].tsx:110-190`: `Gesture.Pan` +
`withSpring {duration normal, dampingRatio .85}`, **dos imanes**
(asomada/extendida), **umbral de velocidad 800**, `PEEK = 26` con su porqué
medido en cinco pasadas de S81-B, arrastre sobre el asa **y** la cabecera.
**Cero mecanismo nuevo: se consume el de la casa.**

⚠️ **Con una corrección que la referencia obliga:** hoy el rango vive
*adentro* de la hoja (`en-camino:324-336`). **Rappi lo pone FLOTANDO ARRIBA
sobre el mapa, en su propia carta** — y eso resuelve la tensión que mi
predecesora dejó abierta (*«¿el mapa preside o la hoja se lo come?»*): con el
rango arriba, **la hoja puede bajar sin esconder el dato por el que la
familia abre la pantalla.** Es reordenamiento, reversible en el gate.

### ⑥ 🔴 D-03 · LA CURA ES LA CORRECTA Y EN ESTA CUENTA NO ALCANZA — MEDIDO

El founder ve *«pedido 17 de agosto, pedido 17 de agosto»*. **Tiene número:**

```
23 pedidos del founder en 5 días LOCALES (America/Guayaquil)
  17-ago: 9 pedidos   ·   12-ago: 9 pedidos   ·   16-ago: 3   ·   13-ago: 1   ·   18-ago: 1
```

**Nueve tarjetas con el mismo título, dos veces.** Y lo que pasa al agregar
cada dato — **cuántas quedan DISTINTAS de las nueve**:

| lo que agrego | 17-ago | 12-ago |
|---|---|---|
| solo el día (hoy) | **1 de 9** | **1 de 9** |
| + nombre del producto | 4 | 4 |
| + estado | 5 | 6 |
| + total | 7 | 7 |
| + hora local `HH:MM` | 7 | **4** |

⇒ **la miniatura y «qué trae» son la cura correcta y NO llegan a unicidad en
esta cuenta.** El caso extremo está medido: **tres pedidos de `ACTIVE MIND
7+`, los tres `cancelado`, los tres $38,05, a las 23:21, 23:22 y 23:23.**

🔴 **Y la razón por la que esto no cambia la cura: ese dato es TRÁFICO DE
PRUEBA, no una familia.** Nueve pedidos en cuatro minutos no es un
comportamiento real. **Lo digo ahora porque si no, el gate va a ver tarjetas
repetidas y va a leer «la cura no funcionó»** — cuando lo que va a estar
viendo es el dato.

**Lo único que alcanza unicidad es el `numero_orden`** (`P-20260818-1c7a09`,
17 caracteres) — **y la ley Chanel de la lista lo excluye a propósito**:
*«la fila no dice el número de orden — dato de máquina»* (`pedidos.tsx:11`),
decisión de S100-D. **No la reabro yo. Va a la mesa como línea reversible**
(§2).

**Los otros dos números que la construcción necesita:**
- **20 de 23 pedidos tienen UN solo ítem** ⇒ *«1 producto»* es el caso
  dominante y *«y 2 más»* es el raro (3 de 23). La voz se escribe para el
  caso real, no para el ejemplo.
- **18 de 23 tienen foto; 5 NO** (y en el catálogo entero: **161 de 470
  productos con imagen**) ⇒ **la miniatura necesita su hueco honesto**, no un
  placeholder que finja ser el producto (misma regla que `foto_url: null` en
  la vitrina).
- 🔴 **`TarjetaPedido` no tiene slot de miniatura** (leído entero:
  `titulo`/`detalle`/`monto`/`pasos`/`desvio`/`estado`). **Es pieza ⇒ es de
  B**, y es el mismo lugar donde mi predecesora ya frenó por orden de mesa
  (*«mientras B decide, no lo inventes»*).

---

## §1 · EL REPARTO QUE SALE DE LA MEDICIÓN

**MÍO, y arranco por acá (`apps/cliente`):**

| # | qué | por qué es mío |
|---|---|---|
| 1 | **D-02 · la fila del pedido en vuelo en «Ponte al día»** | pantalla; la fila está diseñada y declarada, el tipo ya la admite |
| 2 | **D-04 · las cuatro cartas del detalle (N21)** | pantalla; el patrón ya vive en Cuenta |
| 3 | **D-05(b) · enchufar `destino` + `marcadorDestino` + `marcadorVivo`** | la pieza ya los expone; falta que la pantalla los pase — y corregir la cabecera que dice lo contrario |
| 4 | **D-05(c) · la hoja arrastrable con la física del paseo** | consumo de un mecanismo ya gateado |
| 5 | **D-06 · la ficha del repartidor con el hueco de la foto marcado** | composición sobre la referencia medida |
| 6 | **D-03 parcial · «qué trae» en la línea de la tarjeta** | el `detalle` ya existe; la miniatura no |
| 7 | **D-01 parcial · la entrada desde Cuenta + Facturas como acceso** | una línea + un lector nuevo sobre una policy que ya abre |

**DE B, con el caso servido (§3):** el quinto slot de `BarraTabs` (geometría
del valle) · el gesto del mapa + recentrar · el slot de miniatura de
`TarjetaPedido`.

**DE LA MESA (§2):** qué es «Reclamaciones» · si el `numero_orden` entra a la
tarjeta · el orden Pedidos-como-tab contra el ciclo del trono.

---

## §2 · 🔴 LO QUE NO DECIDO YO — servido, no resuelto

1. **«Reclamaciones» no tiene objeto** (§0②). Puedo montar el acceso a
   *«¿Compraste en el local?»* —que existe y funciona— **pero eso NO es
   reclamaciones**, y ponerle ese rótulo sería el verosímil-falso de una
   pantalla. *Prefiero un hueco declarado a una puerta que rebota* (Ley 23).
2. **El `numero_orden` en la tarjeta.** Cierra la ambigüedad medida de §0⑥ y
   **choca con una ley Chanel firmada en S100-D.** Dos letras firmadas que se
   contradicen son peores que una equivocada. **Una línea lo pone y una
   línea lo saca.**
3. **Pedidos como quinta tab contra el CICLO DEL TRONO.** `DISEÑO_EXPERIENCIA`
   §7 dice que el cuarto slot es de la Despensa y que **cede a Comunidad en
   F3**. Un quinto slot permanente es letra nueva sobre esa firma. *Lo
   declaro para que la firma sea explícita y no una consecuencia.*

---

## §3 · 📤 LOS TRES PEDIDOS A B, CON SU CASO

**① `BarraTabs` con cinco tabs.** El contrato dice 2–5 (`:526`); la aritmética
del valle dice que a cinco no entra en su pestaña en ningún ancho medido
(tabla de §0①). **Lo que no puedo medir yo: si el hombro muerde el ícono del
vecino** — necesita el ancho de la caja del ícono y aparato. *Si la respuesta
es que no entra, la decisión vuelve a la mesa como «qué cede», y ahí el número
ya está.*

**② `MapaRecorrido`: el gesto en modo `vivo`.** Hoy `scrollEnabled` y
`zoomEnabled` son `!esVivo` (`:145-146`) **con razón** — la pieza re-encuadra
con cada fix (`:111-127`) y pelearía contra el dedo. La forma que la
referencia trae medida: **el gesto suspende el seguimiento y un control de
recentrar lo devuelve** (Rappi lo tiene, y lo tiene porque su mapa se navega).
⚠️ **Y un dato para tu instrumento: `MapaRecorrido.web.tsx` no implementa
`destino` ni los marcadores** ⇒ *medir esto en RN-web es medir otro objeto*
(L-235, la ley que C y mi predecesora encontraron por separado).

**③ `TarjetaPedido`: el slot de miniatura.** El caso con número: **9 tarjetas
idénticas en un día**; **20 de 23 pedidos con un solo ítem**; **5 de 23 sin
foto ⇒ el slot tiene que admitir el hueco honesto**, no exigir imagen. *Mi
predecesora ya frenó acá una vez y de ese freno nació `GlifoConContador`:
pido la pieza en vez de inventarla.*

---

## §3bis · ✅ LO CONSTRUIDO, en orden de commit

| commit | qué |
|---|---|
| `337356d6` | **D-02 · el pedido en vuelo llega al Hogar** — UNA fila (colapso por §10ter.1), `pagando` afuera, `mascotaId: null` por el precedente de `cita-` |
| `c07c8015` | **D-04 · N21 en el detalle: CUATRO cartas**, y el corte lo dio la referencia, no la interpretación de la ley |
| `dee3c06c` | **D-05 + D-06 en EN CAMINO** — hoja arrastrable con la física del paseo · el destino dibujado · el rango a carta flotante · la ficha del repartidor con el hueco de la foto |
| `0bf548a0` | **la entrada a «Tus pedidos» desde Cuenta** — deuda abierta desde S100b, sin dueño |

**Verificado en cada tanda: `tsc apps/cliente` exit 0 · `verify:diseno`
VERDE, 46 reglas. Cero migración, cero dato vivo movido, cero `packages/ui`
tocado.**

### 🔴 EL LINT ME CAZÓ HEREDANDO UNA DEUDA AJENA, Y VALE MÁS QUE LA CURA

Copié la física de la hoja del paseo **y con ella su
`motion.duration.legacy_normal`**. **R51 la rebotó**: el vocabulario firmado
de N10 es `fast 150 · estandar 300 · grande 520`, y el token asigna
`estandar` **a las Hojas por nombre**.

> *La receta se copia; la deuda del vecino no.* Y **el que lo distinguió no
> fui yo: fue una regla que existía justo para eso.** Sin R51, el token viejo
> entraba a un archivo nuevo con el aval de «así lo hace la pantalla que
> funciona» — que es como una deuda pasa de un archivo a la mitad del repo.

### ⚠️ Y UNA COSA QUE NO SE PUDO CERRAR SOLA: EL MARCADOR VIVO

`marcadorVivo` existe, está implementado, y **lo dejé sin pasar a
propósito**: **no existe el glifo de la MOTO**, y N14 dice *«paseo = la cara
de la mascota · entrega = la moto»* ⇒ **hoy una entrega se sigue dibujando
con la identidad de un paseo.** *No lo tapé con `despensa` ni con
`ubicacion`: elegir la forma desde la palabra es exactamente lo que S99 dejó
como lección.* Declarado en la cabecera de la pantalla y pedido a B.

---

## §3ter · 🔴 LO QUE QUEDÓ BLOQUEADO EN `packages/api` (territorio de A)

**Dos encargos firmados del founder no se pueden construir desde la pantalla
sin romper la puerta única, y los dos van pedidos a A con su caso:**

| qué | por qué está bloqueado |
|---|---|
| **D-03 · «qué trae» + la miniatura** | `listarMisPedidos` lee `v_pedidos_narrativa` y **la vista no trae ítems** ⇒ la lista no puede decir ni el producto ni cuántos son. Hace falta un lector que traiga, por pedido, **primer ítem · cuántos · portada** (con `fotosDeProducto`, que ya existe — una fuente, no un cómputo paralelo) |
| **D-01 · «Facturas» como acceso** | `facturas` tiene **6 filas, 5 del founder**, policy `facturas_owner`, y **cero lectores**. La escribe el panel del vendedor. *El dato entra por una puerta y no sale por ninguna* |

*Los dos tienen la forma de H-01/H-11 —el dato existe, la policy abre, el
lector no pregunta— y es la tercera vez en este frente.*

---

## §8 · 🔴 AFIRMÉ UN NEGATIVO Y ESTABA MAL POR DOS RAZONES, NO UNA

**Le dije a A y a B que `L-296`–`L-301` «no existen en ninguna rama
pusheada».** B lo midió contra el objeto y **existen: están en `origin/main` y
en `origin/pista-b`.** Re-verificado por mí, no tomado de su palabra:

```
git fetch origin
git show origin/main:docs/DEUDAS_CANONICAS.md | grep -oE "^- \*\*L-(29[0-9]|30[0-9])"
→ L-290 … L-295 L-296 L-297 L-298 L-299 L-300 L-301
```

**Y mis dos errores no son «un fetch viejo»** —eso es la mitad amable—:

① **para el canon grepeé `docs/DEUDAS_CANONICAS.md` de MI WORKTREE**, no
`origin/main`. *Mi copia era correcta cuando la mergeé y quedó vencida cuando
A ensambló lo de B* (L-166 en su forma más barata, y me la cobré el mismo día
en que mi predecesora la dejó escrita en el handoff que leí primero).

② 🔴 **y el que de verdad importa: para las ramas filtré por `docs/loop/`** —
donde **una lección NUNCA podía estar**, porque las lecciones viven en
`DEUDAS_CANONICAS.md`. **Mi búsqueda no podía encontrar lo que buscaba, así
que su cero no significaba «no existe»: significaba «no busqué ahí».**

> ***UN NEGATIVO SALIDO DE UNA BÚSQUEDA RESTRINGIDA NO ES UN NEGATIVO.***

✅ **YA ES CANON: `L-305`**, depositada por B con mi nombre (verificado contra
el objeto: `git show origin/main:docs/DEUDAS_CANONICAS.md | grep -cE '^- \*\*L-305'`
→ 1). **La ley NO se repite acá y esto es un puntero a propósito** —*un tercer
lugar que la afirme es un lugar más que puede quedar viejo*—; **lo que queda
acá es el CASO, que es mío.**

**Y la versión depositada le agregó la regla operativa que a mí me faltaba:**
> **todo negativo declara SU ALCANCE, no solo su comando** —qué ref, qué
> rutas, qué patrón— *y quien lo recibe verifica el alcance antes que el
> número.*

*Eso es lo que convierte la lección en algo que se puede cumplir: yo había
escrito el diagnóstico; esa línea es el procedimiento.*

**La ironía completa, y por eso queda escrito:** el mensaje en el que afirmé
eso **era el mensaje donde le enseñaba a A que «un número citado en un chat no
es un número reservado»**. La ley era correcta y **la apliqué contra el objeto
equivocado.** B le puso la cara que faltaba:

> **un número citado en un chat no es un número reservado; y tampoco lo es un
> número NEGADO en un chat.**

⚠️ **Y el costo real no fue mío:** la mesa nos pasó a los dos que *«el canon
llega a L-295»*, y **obedecerlo habría creado SEIS duplicados**. Lo salvó que
B midiera antes de depositar. **Números libres al escribir esto: desde
`L-305`** (B depositó hasta L-304) — *y quien los use los re-mide igual, que
es el punto entero.*

---

## §7bis · ✅ **LA QUINTA CABE — Y EL CENSO DE ABAJO NO SE USA (se guarda)**

**B midió las dos barras en el mismo aparato y su propia restricción resultó
falsa.** Lo que decide:

| | tabs | Ø del disco | **hueco al ícono vecino** |
|---|---|---|---|
| **prestador** (aprobado por el founder) | 5 | 65,8 dp | **20,7 dp**, un solo lado |
| **cliente con cinco** | 5 | ~65,4 dp | **28,8 dp**, dos lados |

⇒ **28,8 > 20,7: el cliente tendría MÁS aire que el caso que el founder ya
aprueba.** ⇒ **Pedidos entra COMO QUINTA y Explorar NO se retira.**

**Y las dos correcciones que B declaró sobre sí misma valen registrarse:**
① dividió **384** entre 5 cuando **la barra usa 368** (arranca en 8,2 y
termina en 376,2) · ② **recalculó mi fórmula del hombro en vez de MEDIR EL
OBJETO** — *«la respuesta estaba en una app nuestra, en el mismo teléfono,
desde antes de que empezáramos»*.

> 🔴 **Y eso me alcanza a mí en la misma medida.** Mi tabla del valle, su
> tabla del ícono: **dos mediciones buenas, ninguna mirada en pantalla.** Lo
> que faltaba no era un tercer número — era **comparar con la otra barra**, y
> ninguno de los dos fue a buscarla. *Cuando dos pistas se pasan números y la
> pregunta no se cierra, lo que falta suele no ser otro número: es el objeto
> que nadie miró.*

### ⚠️ LO QUE LA MEDICIÓN NO CIERRA, Y NO LO DECIDO YO

① **El prestador tiene UN hueco angosto; el cliente con cinco tendría DOS**
(los dos lados del disco). 28,8 > 20,7 en cada uno, **pero si dos huecos
angostos se leen peor que uno, eso es ojo y no número** (B lo declara sin
resolver, y coincido).

② 🔴 **DÓNDE VA PEDIDOS EN EL ORDEN DE CINCO — y hay una letra que lo
restringe.** `DISEÑO_EXPERIENCIA` §7 (**ciclo del trono**) dice que la
Despensa ocupa el trono y que *«cuando llegue Comunidad, Comunidad va al
CENTRO»*. **Con cinco tabs el centro es la posición 3 exacta** —con cuatro no
existía centro verdadero—, así que:

> **`Hogar · Explorar · Despensa · Pedidos · Cuenta`** deja la Despensa
> **centrada** y Cuenta cerrando la barra (la convención medida en las dos
> apps). **`Hogar · Pedidos · Explorar · Despensa · Cuenta`** la empujaría a
> la 4ª y **rompería el trono en la única barra que el dueño ve todos los
> días.**

**Voto: Pedidos 4º.** Es una línea del array y **va al gate como reversible**,
pero *elegir el orden sin nombrar la letra que lo restringe es cómo una firma
se rompe sin que nadie lo note.*

---

## §7 · 🔴 EL CENSO DEL RETIRO DE EXPLORAR — HECHO, Y **NO EJECUTADO** (queda de archivo)

**Cronología, porque es lo que explica el estado del código:**
① el founder firmó *«la barra queda en CUATRO ⇒ Pedidos entra en el slot de
Explorar»*, con la orden correcta: **medir y declarar qué se pierde antes de
construir** · ② lo medí y lo construí entero · ③ **minutos después la firma
quedó EN SUSPENSO por un dato que da vuelta la premisa: `e-PetPlace Negocios`
—nuestra propia app— TIENE CINCO TABS Y SE VEN BIEN, en el mismo teléfono** ·
④ **lo revertí.**

> *Las dos mediciones pueden ser correctas sobre objetos distintos.* B midió
> la caja del ícono **del cliente** (24,2 dp) y calculó contra **esa**
> anatomía. Si el prestador resuelve algo que el cliente no, la conclusión
> deja de ser *«cinco no caben»* y pasa a ser ***«cinco no caben con ESTA
> anatomía»*** — que es una conclusión distinta y la que decide la
> construcción. **Es la misma familia que nos mordió tres veces hoy.**

### EL CENSO, QUE SE GUARDA POR SI LA MEDICIÓN VUELVE A FAVOR

| bloque de `explorar/index.tsx` | ¿queda huérfano al retirar la tab? |
|---|---|
| ① los **cuatro servicios** → `/hogar/{paseos,grooming,veterinaria,adiestramiento}` | **NO.** El rail «TUS SERVICIOS» del Hogar apunta a **las mismas cuatro rutas**, con mínimo 4 garantizado ⇒ *era una segunda puerta al mismo cuarto* |
| ② **refugios / adopción** | **NO, y el Hogar lo SUPERA:** Explorar tiene un `EstadoVacio` **sin camino**; el Hogar tiene `CeldaNavegacion` → **`/adoptar`, que existe** (`app/adoptar.tsx`) |
| ③ **«Próximamente honesto»** (hotel · guardería · seguros · telemedicina · Prime) | **es lo ÚNICO propio** — `explorar/index.tsx` es el único archivo que lo nombra |

### 🔴 Y LO QUE HACE BARATO EL RETIRO: **RETIRAR LA TAB NO RETIRA EL MUNDO**

`(tabs)/explorar` contiene **16 rutas — el flujo de reserva ENTERO de los
cuatro oficios**, y los cuatro `lib/reserva/*` empujan a
`/explorar/<oficio>/checkout`. **El `Tabs.Screen` se queda; lo que sale es el
botón.** *Sacarlo de la lista de pantallas habría roto las cuatro cadenas de
reserva sin que ningún typecheck lo viera.*

Y la raíz de Explorar **conserva una entrada viva**: «Reservar un servicio de
{nombre}» en el perfil de la mascota (`hogar/mascota/[mascotaId].tsx:1959`).

⇒ **NADA QUEDA INALCANZABLE. Lo que se pierde es DESCUBRIMIENTO** — la
superficie de «mirar todo lo que hay» pasaría de tab a estar detrás de un
botón del perfil de una mascota. *Una pérdida de prominencia no es una pérdida
de alcance, y confundirlas en cualquiera de las dos direcciones es caro.*

### 🔴 H-116 · SUBE AL FOUNDER COMO PREGUNTA DE NEGOCIO — **YO NO LA DECIDO**

**El encuadre lo afiló la mesa y se deposita con su literal, porque es mejor
que el mío:** lo que se pierde al retirar Explorar es

> ***el lugar donde alguien que entró por la Despensa se entera de que también
> hacemos paseos, grooming y veterinaria.***

*Yo lo había escrito como «descubrimiento», que es el nombre del mecanismo;
esto es el nombre del NEGOCIO* — y la diferencia importa porque **quien decide
no compra un mecanismo: decide si el ecosistema se sigue presentando solo a
quien ya lo conoce.** Es EL NORTE en una pantalla: *la experiencia impecable
de CADA actor es condición de existencia*, y el cliente que llegó por comida
es exactamente el que todavía no sabe que hay un veterinario del otro lado.

**Dueño: FOUNDER. No tiene respuesta técnica** — las dos formas están
construidas y cuestan lo mismo.

### LO QUE SÍ ENTRÓ, PORQUE NO DEPENDE DE ESA DECISIÓN

**Pedidos nace como CASA aunque no tenga botón:** sus tres rutas viven en
`(tabs)/pedidos` y se alcanzan desde Cuenta, «Ponte al día» y el «listo» del
checkout. **En curso arriba, historial abajo** (corte por `es_terminal`, que
lo dice el catálogo), rótulos solo si existen las dos secciones, el acceso al
reclamo del local en **los dos** estados y no solo en el vacío, y encabezado
de **portada** — *una casa no tiene flecha de atrás.*

**Por qué se mudaron y no quedaron colgando de la Despensa:** dejar el detalle
bajo `/despensa` haría que **tocar un pedido cambie la tab encendida** — *estás
en Pedidos, tocás tu pedido, y la barra te dice que estás en la Despensa.*

⚠️ **Costo cruzado declarado:** la mudanza cambia las **rutas tipadas** de
expo-router y `.expo/types/router.d.ts` **está gitignoreado** ⇒ **después de
mergear hay que regenerarlo** (arrancar el dev server una vez, ~4 s) o el
typecheck falla **sobre rutas que sí existen**. *Su síntoma dice lo contrario
de lo que pasa.* Avisado a A y a B.

---

## §9 · ✅ CIERRE — LO QUE ESTÁ EN EL BUNDLE DEL GATE, VERIFICADO CONTRA EL OBJETO

**Ancla del gate: `e20113b6`** (OTA cliente group `e6ebf9d3` · runtime 1.0.3).
No lo tomo del reporte de A: lo medí.

```
git merge-base --is-ancestor b979e6a0 e20113b6                        → VERDADERO
git show e20113b6:'…/(tabs)/_layout.tsx'   | grep -c 'nombre="pedido"' → 1
git show e20113b6:'…/pedidos/index.tsx'    | grep -c 'miniatura='      → 1
git diff --stat e20113b6 pista-d           → docs/loop/S100c-D.md, y NADA MÁS
```

⇒ **todo mi código está en el gate**, y lo único que `pista-d` tiene por
delante del ancla es este parte. *El corte que A declaró es exacto.*

### 🔴 LA CASI-PÉRDIDA QUE VALE MÁS QUE EL ARREGLO

El primer bundle ancló en `902cb47b`, que **no** tenía el glifo propio ni la
miniatura. Medido antes del gate: la tab de Pedidos y la de Despensa **con la
misma bolsa**, y la lista **sin miniaturas**.

> **Y la lista sin miniaturas es LITERALMENTE el hallazgo original del
> founder** (*«dice pedido 17 de agosto, pedido 17 de agosto»*). Si lo miraba
> así, **la lectura natural era «la cura no funcionó» cuando la verdad era «la
> cura no viajó»** — y ese diagnóstico invertido habría mandado a rediseñar
> una lista que ya estaba curada.

**Lo que lo evitó no fue un proceso: fue medir el ancla contra mi último
commit en vez de suponer que el merge me incluía.** *Un SHA mergeado y un SHA
publicado son dos afirmaciones distintas* — la misma familia que L-217 dejó
escrita para «en origin» contra «en el canon».

⇒ **regla que me llevo: al cerrar, no se verifica que TU rama esté pusheada —
se verifica que TU ÚLTIMO COMMIT esté en EL ANCLA.** Lo primero es sobre mí;
lo segundo es sobre lo que el founder va a ver.

### 🔴 Y LA FORMA QUE MORDIÓ CUATRO VECES HOY, EN CUATRO PARES DISTINTOS

A la nombró al cerrar y es el resumen de la jornada entera: **tratar un
INSTANTE como si fuera un ESTADO.**

| par | el dato que envejeció mientras se usaba |
|---|---|
| A ↔ C | midió `8f8bec14` y ya valía `956928fd` |
| D ↔ B | mi «`L-296`–`L-301` no existen» — cierto al medirlo, falso al usarlo |
| A ↔ B | el group de 12 h |
| **A ↔ D** | **mis cuatro «estoy en cero»**, y después seguí commiteando |

**Mi mitad, y la formulación quedó mía porque le saca la acusación:**

> ***«en cero» describe un INSTANTE, y la veda necesita un INTERVALO.***

*La versión de A nombraba a quien avisa; ésta nombra la FORMA DEL DATO — y la
forma es lo que se puede arreglar.* **Yo produje varias de las veces en que
`main` se movió debajo de su bundle**, así que la ley no es sobre B ni sobre
ella: es sobre que un aviso de estado sin intervalo **no es un aviso de
estado**.

*Y es la misma familia que L-305 —el negativo que no declara su alcance— y que
L-166. Cuatro caras del mismo animal en una vuelta: el dato correcto,
consultado tarde.*

---

## §4 · LA COLA VIVA — con su próximo paso

| # | ítem | estado · próximo paso |
|---|---|---|
| ① | **D-02 la fila del pedido en el Hogar** | ✅ **CONSTRUIDA** (`337356d6`) — falta el ojo del founder: ¿la fila colapsada dice lo suficiente, o querés una por pedido? |
| ② | **D-04 las cuatro cartas** | ✅ **CONSTRUIDAS** (`c07c8015`) — al gate: ¿la escalera sin carta se lee como parte de la pantalla o como huérfana? |
| ③ | **D-05 hoja arrastrable + destino** | ✅ **CONSTRUIDO** (`dee3c06c`). ⏳ **El GESTO DEL MAPA es de B y NO me bloquea**: la mitad «ver la dirección antes de que salga» ya funciona sin cámara navegable |
| ④ | **D-06 ficha del repartidor** | ✅ **CONSTRUIDA** (`dee3c06c`) con el hueco de la foto dibujado y con voz |
| ⑤ | **D-03 «qué trae» + miniatura** | ✅ **CERRADO ENTERO.** El lector lo construyó A con mis números, el slot lo construyó B con los mismos, y **la miniatura va montada con `Image` y NO con `LienzoProducto`** — esa pieza pinta su fondo detrás de la foto (el «marco lila» de H-115) y acá habría muchas más miniaturas juntas que en el carrito. *Evitar la pieza que tiene el defecto es más barato que depender de que su cura viaje* |
| ⑥ | **D-01 la quinta tab** | ✅ **CONSTRUIDA** — entra CUARTA por el ciclo del trono (§7bis). Al gate: **los DOS huecos angostos** |
| ⑦ | **D-01 «Facturas»** | ✅ **resuelto por medición**: el número vive en la carta del total y **no se montó la casa** porque las 6 facturas no tienen documento. **«Reclamaciones» sigue frenado en la mesa** (§2①) |
| ⑧ | **el marcador de la MOTO** | ✅ **CERRADO** — `MarcaDeMapa` (N14 cierra) |
| ⑧bis | **el glifo `pedido` de la tab** | ✅ **CERRADO, y el préstamo duró un commit.** Monté `despensa` **declarando su costo** (dos tabs con el mismo dibujo) en vez de inventar una forma; B construyó el propio con el pedido en la mano — **su discriminador es LA TAPA**, una recta que sobrevive a 21 px. ⚠️ **sin gate de ícono: nadie lo vio a 21 px** (B no tiene rasterizador SVG) |
| ⑨bis | **`direccion.puntoAyuda` dice «pin» y el dibujo es un PUNTO** | 🔴 **MÍO, y HELADO — con la razón de A, que es mejor que la mía y la reemplaza.** Hallazgo de B: `PinMovible` tiene razón de forma y **lo barato es la voz**, que vive en `apps/cliente/src/i18n` — mi territorio. **Yo lo congelé porque un commit mío volvería vieja la declaración «cero código afuera» del resumen de publicación; A lo desestimó bien: *un renglón de un documento se actualiza en diez segundos y eso no vale congelar una cura.*** ⇒ **lo que decide es EL COSTO DEL CUARTO GROUP:** ya hubo tres publishes y **un bundle declarado superseded** para que el historial del canal sea legible; **un cuarto group por una voz cosmética le pone al founder, justo antes de mirar, la pregunta «¿cuál de los cuatro estoy viendo?»** — y esa confusión cuesta más que la voz del pin. *Publicar para gatear significa que el objeto del gate tiene que ser UNO y estar QUIETO.* **La ventana se abre después del gate, con lo que el ojo devuelva.** |
| ⑨ | **`cuenta/index.tsx` suma `insets.bottom + spacing[8]`** | 🔴 **no lo toco.** Es la misma línea vieja que mi predecesora sacó de sus tres pantallas — el inset contado dos veces (B: el visor ya termina en el filo). **Toqué esa pantalla para agregar una fila y NO aproveché para curarlo**: *una cura que viaja de polizón en un commit de otra cosa es una cura que nadie revisó.* Queda declarado con dueño |

---

## §5 · LO QUE **NO** HICE, con su razón

- **No toqué `packages/ui`.** Tres de los seis encargos terminan en pieza y
  los tres van con su caso (§3).
- **No moví la ruta de Pedidos todavía.** Cuesta dos líneas, pero **mover la
  casa antes de que la mesa firme el quinto slot es construir sobre una
  decisión que no está tomada.**
- **No inventé «Reclamaciones».** §2①.
- **No puse el `numero_orden` en la tarjeta.** Choca con letra firmada. §2②.
- **No sembré ni moví dato vivo** para que la lista se vea mejor en el gate.
  *El dato repetido es la verdad de esa cuenta y hay que decirlo, no
  maquillarlo.*
- **No corrí instrumento de RN-web.** Mi predecesora midió que **toda
  navegación por recarga completa hace correr los efectos antes de que el
  entry inicialice la API**, y que su síntoma *«no encontré el scroller»* **se
  lee como defecto de layout**. *Un rojo del instrumento que significa «no
  pude medir» no se lee como «está roto».*

---

## §6 · LO QUE ESTE TRAMO YA DEJA COMO MÉTODO

① 🔴 **UN ÍTEM DIFERIDO CON SU DISPARO ESCRITO NO TIENE QUIÉN ESCUCHE EL
DISPARO.** La fila del pedido en el Hogar se declaró bien en S82 —*«monta
cuando exista el motor»*—, **el motor existe desde S95**, y llegó al gate del
founder como si nunca se hubiera pensado. *Declarar el disparo es lo
correcto; lo que falta es que alguien barra los disparos cumplidos.*

② **UNA PIEZA ENTREGADA Y SIN CONSUMIDOR MUERE SIN QUE NADIE SE ENTERE — y
esta vez fueron tres props construidas a pedido de esta misma pista.**
`destino`, `marcadorDestino` y `marcadorVivo` existen implementadas desde
S100-B y la pantalla nunca las pasó. **Y la cabecera del archivo sigue
afirmando que no existen**, que es lo que hace que nadie vaya a buscarlas.

③ **DOS LÍNEAS DE LA MISMA PIEZA PUEDEN CONTRADECIRSE SIN QUE NADIE LO NOTE,
MIENTRAS NADIE PISE EL BORDE.** `BarraTabs` dice «2 a 5» y su geometría entra
hasta 4. *Nadie mintió: nadie había pasado de cuatro.*

④ **MEDIR LA REFERENCIA EN VEZ DE DESCRIBIRLA cambió TRES decisiones en una
tarde** — la hoja de Uber es **una pila de cartas** (y con eso N21 deja de ser
una ley abstracta y pasa a ser la anatomía de esta pantalla) · el rango de
Rappi **flota arriba sobre el mapa**, que resuelve la tensión
mapa-contra-hoja que quedó abierta del gate anterior · y **la escalera de
Rappi va SOBRE EL FONDO PELADO**, que es lo que decidió que fueran cuatro
cartas y no seis. *Es la ley de la barra de S99, cobrando a favor esta vez.*

⑤ 🔴 **UNA MEDICIÓN CORRECTA SOBRE LA VARIABLE EQUIVOCADA ES PEOR QUE NO
MEDIR, PORQUE SE CITA.** Mi tabla del valle (§0①) era aritmética buena y
**contestaba otra pregunta**; lo que decide la quinta tab es el hombro, y eso
lo midió B. **Es L-285 y me la cobré yo mismo el mismo día en que la cité
para otra cosa.** *Ver la clase de error no vacuna contra ella; medir, sí —
y que te midan, también.*

⑥ **UNA CURA QUE VIAJA DE POLIZÓN EN UN COMMIT DE OTRA COSA ES UNA CURA QUE
NADIE REVISÓ.** Toqué `cuenta/index.tsx` para agregar una fila y tenía a un
renglón el `insets.bottom` de más. **No lo curé**: no es mi territorio y
mezclarlo habría escondido un cambio de layout adentro de un cambio de menú.
*El costo de declararlo es una fila en una tabla; el de colarlo es que el
próximo gate no sepa qué movió la pantalla.*
