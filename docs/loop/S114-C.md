# S114-C · LA PUERTA Y LA ELECCIÓN DEL MOTIVO

> | | |
> |---|---|
> | **Rama** | `pista/s114-c-1.0` |
> | **Base** | `main` @ `c72ea582` + merge de `pista/s114-b-1.0` @ `2253bd62` (las ocho piezas) |
> | **Punta** | **se lee del objeto:** `git ls-remote origin refs/heads/pista/s114-c-1.0` |
> | **Territorio** | `apps/cliente` · `docs/loop/` |
> | **Letra** | `DIRECCION_POSTVENTA` y `LETRA_POSTVENTA`, las dos enteras |
> | **Estado** | **C1–C5 y C7 CONSTRUIDAS SOBRE EL MOTOR REAL.** C6 sin lector (omisión mía, §⑪) · C8 bloqueado por F1 · memorial pausado con su parte hecha (§⑫). |

---

## ⓪ LA FRASE CONTRA LA QUE MEDÍ

*«Una familia que ya pagó tiene a dónde ir cuando algo sale distinto, en el mismo lugar donde
está lo que pagó.»* (§0)

---

## ⓪ ⏪ ESTE PARTE SE ESCRIBIÓ EN DOS MOMENTOS, Y SE LEE ASÍ

Los §① a §⑨ son de **antes de que A entregara el motor** y quedan como estaban: describen lo que
se podía construir sin él y por qué no construí el resto. **Los §⑩ en adelante son de después.**
*Un parte que reescribe su primera mitad para que parezca que siempre supo cómo terminaba pierde
lo único que hacía útil su primera mitad.*

---

## ① 🔴 LO PRIMERO, PORQUE CAMBIA EL ALCANCE — **EL MOTOR DEL CASO NO EXISTE** *(vigente hasta que A3 aterrizó)*

El mandato dice *«montá sobre las piezas de B y las RPCs de A»*. **Las piezas de B están; las
RPCs de A no.** Medido, no supuesto:

```
grep -rln "casos_postventa\|caso_mensajes\|abrir_caso\b\|resolver_caso\|saldo_hogar" \
     supabase/migrations/ packages/api/src/     ⇒ 0 archivos
```

Lo que A **sí** entregó y usé: `cat_motivos_postventa` · `cat_motivos_herencia` ·
`v_motivos_resueltos` · el wrapper `postventa-motivos.ts`. *(Los hits de `abrir_caso` en el repo
son `abrir_caso_clinico`, de S70 — otro sujeto.)*

⇒ **C1 y la primera mitad de C2 se podían construir hoy y están construidas. C3, C4, C5, C6, C7
y C8 no tienen a qué llamar, y NO las escribí contra una firma inventada:** si no coincide, el
trabajo se rehace entero **y además compila**, que es la peor forma de estar mal. El contrato
completo —wrapper por wrapper, con el renglón de la letra que lo pide— está servido en
**`docs/loop/S114-C-PEDIDO-A-A-EL-MOTOR-DEL-CASO.md`**.

**El más bloqueado es C8**, y conviene decir por qué: su línea de las 48 h le promete al
prestador que **no se cobra**. Eso tiene que ser verdad en el ledger antes de decirlo, y depende
de F1 entero (las dos ventanas, el cron y `no_ejecutado`).

---

## ② LO ENTREGADO

| | qué | dónde |
|---|---|---|
| **C1** | «¿Algo salió distinto?» como **última fila** del detalle de la **cita**, la **estadía** y el **pedido** | `paseo/[atencionId]` · `guarderia/[estadiaId]` · `pedidos/pedido/[pedidoId]` |
| **C2** | la elección del motivo sobre el catálogo REAL, el campo N11 y la confirmación | `app/postventa/motivo.tsx` |
| — | el veredicto de la puerta (memorial · ventana de 7 días · caso abierto) y **su destino**, UNA vez | `lib/postventa/puerta.ts` |
| — | la costura catálogo↔pieza (glifos y `otra_cosa`) | `lib/postventa/motivos.ts` |

**Lo caminado en el emulador** (AVD propio `s114_C`, puerto 5560, `adb -s` siempre; binario
verificado `com.epetplace.cliente` 1.0.7; `Android Bundled … 3030 modules` de MI Metro en el
8114): la lista de los 10 motivos de `cita` → la última fila abre el campo → el relato → la
tarjeta «Entendí: …» con sus dos botones. **Capturas en `docs/loop/capturas-s114-c/`.**

---

## ③ LOS TRES HALLAZGOS DE MONTAR — *ninguno se ve leyendo las piezas por separado*

### 🔴 (a) El piso de memorial de la pieza de B **no se enciende nunca**

`LineaAlgoSalioDistinto` (B8) garantiza *«no aparecer en memorial»* con
`if (theme.mode === 'memorial') return null`. **Ese guard no se enciende en esta app**, y no lo
deduje: está medido y escrito en el perfil de la mascota desde `D-1021` —*«NADIE monta
`<ThemeProvider memorial>` en ninguna de las dos apps; el único provider vivo es el raíz, con
`mode={light|dark}`»*—, que fue justamente el hallazgo que apagó cuatro guards escritos y cero
encendidos.

**El guard de B se conserva y es correcto** (la galería sí monta el sub-tema). Lo que no alcanza
es dejarlo solo: la señal verdadera es `estado_vida` de la mascota. ⇒ el veredicto la exige, y
**la puerta no se dibuja mientras no se sepa** — ante la duda, la app se calla.

⚠️ **Y el dato ya estaba a mano**: `paseo/[atencionId]` pedía el perfil completo *sólo para
grooming* y **tiraba `estado_vida`**. Se ensanchó esa condición. *No faltaba el dato: faltaba
que decidiera algo* — el mismo patrón que `D-1021`.

### 🔴 (b) `otra_cosa` se decía **dos veces**

Es fila real del catálogo (`objeto='todos'`) **y** `SelectorMotivo` agrega su propia última fila
con `MOTIVO_CONTAME` — a propósito, es lo que vuelve inexpresable un «Otro». Montadas tal cual,
la lista dice lo mismo dos veces. ⇒ se saca de la lista y **su voz del catálogo pasa a ser la de
la pieza**, así la frase sigue saliendo de donde el founder la va a leer.

### 🔴 (c) El pedido tenía otra puerta — **ratificado en el fondo, corregido en la forma**

Reporté que «Tengo un problema» → WhatsApp era el segundo sistema de postventa que la letra
prohíbe, y lo resolví haciéndolos **excluyentes**: donde había puerta, el otro desaparecía.

**La mesa ratificó el fondo y corrigió la forma (7-sep), y la corrección es la que importa:**
*no son dos sistemas — uno es la puerta del reclamo y el otro es ayuda general*, y la letra lo
dice sola cuando aclara que **«¿dónde está mi pedido?» NO es un caso**.

⇒ Lo que estaba mal **no era que convivieran: era que se llamaban parecido.** «Tengo un
problema» y «¿Algo salió distinto?» le piden a la familia que adivine cuál le toca, **y la que
elija mal la deja sin lo que la letra ya le prometió.**

**Aplicado:**
- ① «¿Algo salió distinto?» es la **única** puerta de reclamo del pedido, y ahora se gobierna
  sola —`puerta.hay` y nada más—, fuera del ternario de narrativas.
- ② WhatsApp **se queda**, con su condición intacta, y cambia de nombre:
  **«Tengo un problema» → «Necesito ayuda con este pedido»**. La llave vieja se retiró (un solo
  consumidor, medido) en vez de dejarla conviviendo.

*Mi lectura original hacía desaparecer la ayuda general cuando aparecía el reclamo — o sea que
para pedir «¿dónde está mi pedido?» había que no tener nada que reclamar.*

### 🔴 (c-bis) Y la corrección destapó un hueco que yo había dado por bueno

Si «todo lo que la letra llama caso entra por ahí, **sin excepción**», entonces había que mirar
las excepciones que no parecen excepciones. Encontré dos, y son la misma:

1. La puerta vivía **adentro** de la rama del medio del ternario ⇒ un pedido `cancelado` no la
   veía. **Y `cancelado_vendedor` es un motivo de clase 1 del catálogo.**
2. Su ancla era `envio.entregado_en`, que es `null` cuando el pedido **nunca se entregó** ⇒
   `no_llego` y `cancelado` tampoco la alcanzaban.

**Las dos fallas de clase 1 del pedido eran justo las dos que no llegaban a la puerta** — los
casos en que el motor ya sabe que a la familia le fallaron y la letra resuelve sola. Curado: el
ancla pasa a ser **«terminó»** y no «se entregó» (`lib/postventa/cierre-del-pedido.ts`), y la
puerta se gobierna sola.

⚠️ **La aproximación va declarada, no disimulada:** para las dos que terminan sin entrega el
ancla es `actualizado_en` —**verificado que `v_pedidos_narrativa` lo expone**, migración
`20260811220000` línea 254, no supuesto—, que es la última escritura de la fila y no el instante
en que terminó. **Se eligió el error que ABRE y no el que cierra:** de más, el motor rebota
`fuera_de_ventana` y la familia lee un no con su razón; de menos, se queda sin puerta **y de eso
no se entera nadie**. La cura de raíz es `cerrado_en` por objeto, y está en el pedido a A (C1bis).

⚠️ **Y lo que NO pude verificar en pantalla, dicho:** la familia demo **no tiene ningún pedido**,
así que la tab no existe y no pude caminar el detalle. **El cambio del pedido está verificado por
typecheck y por lectura, no por píxeles.**

---

## ④ LOS DOS QUE ENCONTRÓ EL APARATO Y NINGÚN GATE

### 🔴 `tsc` a mano **no mide rutas**, y tenía tres `push` a una ruta que no existe

Escribí `router.push('/postventa/caso/${casoId}')` en las tres pantallas. **`npx tsc --noEmit`
salió VERDE cuatro veces seguidas.** Lo cazó `pnpm run typecheck`, que corre primero
`guard-rutas-tipadas.mjs` — y ese guard exige `.expo/types/router.d.ts`, **generado y fuera de
git**, que en un worktree nuevo no existe.

⚠️ **Y la cura obvia era la trampa:** copiar el `router.d.ts` de otro worktree habría medido
**otro árbol de rutas** y dado un rojo falso sobre mi ruta nueva. Se generó en ESTE worktree
(`npx expo start`), y ahí el gate encontró los tres. *Un verde que no puede producir su rojo no
es una medición* (`L-450`). **La navegación se consolidó en una función** (`destinoDeLaPuerta`),
que es el único lugar que se toca cuando C3 exista.

### 🔴 «Sí, es eso» **no hacía nada visible**

En la pantalla, el rebote estaba montado sólo en la rama de la lista: al confirmar, el estado
cambiaba y **nadie lo veía**. *Un toque que no responde no se lee como un error: se lee como una
app rota.* Curado, y las dos capturas están (`c2-04-ANTES` / `c2-05-DESPUES`). **Lo encontró
tocar el botón, no leer el archivo.**

---

## ⑤ LO QUE VI EN EL APARATO Y **NO CURÉ**, con su razón

| qué | por qué no lo toqué |
|---|---|
| 🔴 **La pantalla mezcla dos idiomas.** El marco va en el idioma de la app («What happened?») y **los motivos siempre en español**, porque `cat_motivos_postventa.voz` es es-only | Es **motor, no superficie**: la tabla no tiene `voz_en`. Familia de `D-324` (catálogos es-only). Se ve limpio en la captura 01 |
| 🟡 **El orden de la lista se lee como azaroso**: «Lo canceló el prestador» antes que «No vino», y dentro de la clase 2 el orden es alfabético por CÓDIGO | El orden lo fija el servidor y **A lo declaró explícitamente** (*«la letra no fija un orden de display»*). Cambiarlo es decisión de producto, no mía |
| 🟡 Dos filas comparten glifo (`pagos` en los dos «me cobraron»; `atender` en los dos de clase 1) | Es el criterio que declaré —*el glifo nombra el objeto, no el enojo*— y se ve razonable; queda a la vista del founder |
| 🟡 La tarjeta de confirmación **ocupa la pantalla y deja el resto vacío** | §2 la llama «una tarjeta» y no dice dónde vive. Con C3 construida seguramente sea una Hoja; hoy sería inventar |

---

## ⑥ EL DICTADO — **no está, y es build, no OTA**

§2 pide dictado en el campo. `expo-speech-recognition` está instalado **sólo en
`apps/prestador`** (medido en los dos `package.json`): en la app de la familia es **módulo
nativo nuevo ⇒ build, jamás update** (`L-134`). El precedente de la casa es `D-456`: el mic se
preparó y esperó cinco sesiones a que otra cosa obligara un build — *jamás un build sólo por él*.

**El camino está escrito y es corto:** portar `apps/prestador/src/components/dictado-en-vivo.tsx`,
que ya resuelve la ausencia del módulo sin romper el APK viejo (`require` en try/catch, el
control no se dibuja — Ley 23). **No lo agregué en esta sesión porque C2 no puede completarse
igual** (①) y meterle una dependencia nativa al tren de A por un flujo que no cierra es sumar
riesgo de build sin ganar nada.

---

## ⑦ LOS «ONCE SITIOS» DE MEMORIAL — **hoy son OCHO, y ninguno es de este arco**

`pnpm verify:habla-en-presente` ⇒ **8 sitios fuera del guard** (el mandato dice once: es el
número de `S113-E-CIERRE` y **decayó**; se re-mide, no se cita). **Los ocho viven en UN archivo,
`hogar/mascota/[mascotaId].tsx`** — `edadMeses`, `pesoVigente` ×6, `vozEdad`.

**El arco de postventa no toca ese archivo**, y el mandato los acota a *«los que este arco
toque»*. Curarlos es decidir qué dice el perfil de un memorial sobre su peso y su edad —*«pesaba
12 kg»* en vez de *«pesa»*— y eso es letra de producto, no un rename. **Queda medido y sin
tocar, a propósito.**

---

## ⑧ LO OPERATIVO

- **4 typechecks en 0** (`cliente` · `prestador` · `ui` · `api`). `domain` con su **rojo
  pre-existente de S45** (`@types/emscripten`), declarado por el canon; nada de C lo mueve.
- **`verify:diseno` VERDE con 66 reglas** (las 61 del canon + las cinco de B).
- **`verify:habla-en-presente`: 8**, sin subir (⑦).
- **Cero migraciones, cero `packages/api`, cero `packages/ui`** — todo lo mío vive en
  `apps/cliente` y `docs/loop/`.
- **Emulador propio** `s114_C` en el puerto 5560; el teléfono físico conectado **no se tocó**.

---

## ⑨ LOS NÚMEROS DE RENDIMIENTO (N16) — **NO SE MIDIERON, y no es un olvido**

El pedido era *«abrir un caso de 200 mensajes en un viaje de 50, scroll a 60 fps, escribir no
re-dibuja la lista»*. **Los tres se miden sobre la pantalla del caso (C3), que no existe porque
su motor no existe.** Medir el perfilador contra una pantalla que no está sería publicar un
número sin sujeto.

**Lo que sí queda escrito para que C3 nazca con eso adentro** (y va en el pedido a A): el
paginado del hilo **con cursor compuesto `valor|id`, jamás offset** — con offset la página
siguiente se saltea filas cuando entra un mensaje nuevo, que es **el defecto exacto que S99
midió en la línea de vida** (55 de 62, se perdían 7).

---

## ⑩ LO QUE SIGUE, EN ORDEN

1. **A ratifica o corrige el voto del glifo** (①  del pedido): si el glifo va a la fila, el mapa
   que escribí es deuda desde el día uno.
2. **A construye el motor**; C3–C8 entran en el orden del pedido.
3. **El founder decide** sobre (c) —el WhatsApp del pedido— y sobre el orden de la lista (⑤).


---
---

# SEGUNDA MITAD · después de `S114-A @ 69065369`

## ⑩ LO CONSTRUIDO SOBRE EL MOTOR REAL

| | qué | dónde |
|---|---|---|
| **C1** | la puerta **pregunta** por el caso abierto y **lee la ventana del motor** | los tres detalles + `useCasoDelObjeto` · `useVentanaDeCaso` |
| **C2** | `abrirCaso` de verdad; el seguro se retiró en el mismo acto (`L-395`) | `app/postventa/motivo.tsx` |
| **C3** | la pantalla del caso: escalera + hilo, sobre `SuperficieChat` | `app/postventa/caso/[casoId].tsx` |
| **C4** | las dos tarjetas parejas, con el saldo **deshabilitado con su razón** | `components/postventa/CartaDeDevolucion.tsx` |
| **C5** | «¿Prefieres que te atienda una persona?» en el encabezado **fijo** | ídem C3 |
| **C7** | la bandeja + el caso del prestador, con sus tres acciones | `apps/prestador/negocio/casos` · `negocio/caso/[casoId]` |

**A me corrigió en la ventana y tenía razón:** yo pedí computarla en la app; el motor la exige
igual, y la divergencia se cierra **publicando el número**. Ningún archivo de C escribe un `7`.

## ⑪ 🔴 LOS TRES HALLAZGOS DE MONTAR EL MOTOR

### (a) `EtapaCaso` está DOS VECES, con el mismo nombre y distinto contenido

`@epetplace/api` y `@epetplace/ui` la exportan las dos: `con_casa` vs `con_epetplace`,
`resuelto_entre_partes` vs `_ustedes`, y A mete los finales **dentro** del tipo mientras B los
tiene aparte. **Coinciden en estructura y divergen sólo en los nombres.** *Lo cazó el compilador
porque el choque fue exacto; con una letra de diferencia habrían quedado los dos conviviendo.*
Traducción **total** en `lib/postventa/caso.ts`.

### (b) Con un final alterno, **la etapa previa se pierde**

`_caso_mover` guarda el final en `etapa` **y** en `estado_final`. §3.1 quiere la fila congelada
donde estaba, y ese dato ya no existe ⇒ **la escalera no se dibuja y se dice el final solo.**
*Verdadero, en vez de una escalera con un paso inventado.* Pedido a A.

### (c) C6 no tiene lector, **y es omisión MÍA**

Mi primer contrato cubrió C1, C2, C3, C4, C7 y C8 **y se saltó C6**. A entregó todo lo que le
pedí. `obtenerMisCasos` va en el segundo pedido; con él C6 son treinta líneas.

## ⑫ MEMORIAL — lo hecho antes de la pausa, y lo que queda

**② está CERRADO: `perdida` ya no es memorial en ningún lado.** La definición vive en
`lib/memorial.ts`, es un `switch` exhaustivo (un cuarto estado no compila), y **nueve
ocurrencias en cinco archivos** que decían `!== null && !== 'activa'` ahora delegan. *Dos decían
la regla buena y perdían por mayoría; una de las nueve la había escrito yo.*

**① queda ABIERTO y sin empezar.** Medido: los guards de `packages/ui` que R78 cuenta son **13**,
de los cuales `LineaAlgoSalioDistinto` ya recibe el dato y **`Atmosfera` es un falso positivo**
—R78 la cuenta como curada **porque tiene un `||`**, y sus dos lados son tema; su doc además la
cita como precedente de la forma correcta—. Las once restantes viven en **dos pantallas**:
`hogar/mascota/[mascotaId]` (4) y `nexo` (5), más `MarcaDeAgua` (decoración, en 80+ pantallas) y
`PastillaConociendolo`, **que no tiene ningún consumidor** — inerte por otra razón que las demás,
y R78 mezcla las dos clases en un número.

⚠️ **Y el chip que el mandato cita ya estaba curado:** el «Conociéndolo · 1 por resolver» del
perfil **no sale de `PastillaConociendolo`** (código inline) y su mitad de pendientes se apaga
por `esMemorial` desde S113. La foto de `D-1021` es el rojo **histórico**.

**Dejé una mascota en memorial lista para medir**: «Nube», beagle, en la familia demo.

## ⑬ LO QUE NO SE VERIFICÓ EN PANTALLA, DICHO

**C3, C4 y C7 no se caminaron en el aparato.** El motor aterrizó al final de la tanda y no hay
ningún caso creado contra el que abrirlas. Están verificadas por typecheck **con las rutas
medidas de verdad** (`router.d.ts` regenerado en este worktree para las dos apps) y por lectura.
*El gate de estas tres es correr el arco entero: abrir un caso desde un servicio cerrado.*

## ⑭ OPERATIVO

- **4 typechecks en 0** · **`verify:diseno` VERDE con 68 reglas** · `habla-en-presente` **8**, sin subir.
- **Cero migraciones, cero `packages/api`, cero `packages/ui`.**
- Pedidos a A: `S114-C-PEDIDO-A-A-EL-MOTOR-DEL-CASO.md` (el primero, contestado entero) y
  **`S114-C-PEDIDO-A-A-2-LO-QUE-FALTA.md`** (el segundo).

---

# CIERRE DE LA TANDA — punta `930bc398`, mergeada al tren `a6260a9b`

> Todo lo de acá es **posterior** a §⑭ y lo corrige donde toca. Los números
> viejos quedan porque eran ciertos ese día; lo que cambió se dice acá.

## ⑮ LO QUE SE CAMINÓ EN EL APARATO — y es lo que produjo casi todo

**Cinco de los seis defectos de la jornada los encontró CAMINAR, no leer.** Ninguno rompía
nada: todos funcionaban mal en silencio, que es la clase que ningún gate ve.

| lo que se caminó | veredicto |
|---|---|
| la puerta de Nexo desde el buscador | ✅ y destapó **tres** más (abajo) |
| la puerta a la casa (`pedirACasa`) | ✅ punta a punta: escalera avanza, el hecho entra al hilo, el botón se calla solo |
| el historial de citas con **Thor** | ✅ «Lo que ya pasó» se dibuja |
| el chip «Terminados» | ✅ |
| los alérgenos recortados + su toque | ✅ al literal de la firma |
| el caso resuelto tras la cura de A | ✅ «Estás en: Resuelto» |

## ⑯ LOS HALLAZGOS, POR CLASE

**① Puertas que invitan y no abren.**
· **`buscar.tsx` empujaba `/nexo` sin `mascotaId`** y del otro lado `enviar` salía en silencio
con el botón encendido — *deja escribir y no deja enviar*. Curado con `focoNexo`, lo que la
casa ya resolvió para la burbuja. La `semilla` viajaba desde S113 **y nadie la leía**.
· **`pedirACasa` tenía CERO consumidores en el cliente.** El motor la acepta para `familia`
desde A3, el prestador la usa, y la familia —a quien P14 protege— **no tenía por dónde**. Donde
debía estar la puerta había una frase en `Texto`. `L-318` en su cara más cara.
· **«Retiro en tienda» era un callejón**: el CTA exigía receptor y teléfono y el bloque que los
pide vivía dentro de la rama de despacho.
· **El historial de citas vivía dentro de la rama del hero** ⇒ sin citas por venir, las pasadas
no se dibujaban nunca. *La sección construida para las 265 pasadas quedaba escondida justo en
el caso donde es lo único que hay.*

**② Controles que devuelven silencio — tres en un día, las tres mitades visibles mías.**
El botón de Nexo · «devolver una parte» (llamaba sin `monto` **y tiraba el resultado**) · el
chip que necesitaba dos toques con el teclado arriba. ⇒ el `disabled` ahora **dice por qué**
(`razonDeshabilitado`).

**③ La regla duplicada por copia, cobrándose.** Curé el hilo del cliente y **no su gemela del
prestador**. Tenía los mismos tres defectos: el guard imposible (`local-<ts>` vs UUID, *no falla:
acumula*), las optimistas al principio de un array ascendente, y **sin sondeo**. Y midiendo eso
apareció el que nadie había visto: **el hilo del prestador estaba entero al revés**
(`datosDelMasNuevoAlMasViejo` es el contrato; recibía el orden del motor). *Lo tapaba el defecto
de al lado.*

**④ Una carrera.** `cargar()` cada 5 s **sin cancelar la anterior**: la respuesta atrasada
escribía un hilo sin el mensaje que la nueva ya había traído ⇒ *aparece, desaparece, vuelve*.
Y pisaba `optimistasRef`, o sea lo que la familia escribió y no salió. Curado con secuencia +
en-vuelo, **en las dos apps**.

**⑤ Un rótulo que nombraba a un miembro del conjunto.** El chip «Cerrados» agrupaba CINCO
etapas ⇒ un `resuelto` bajo «Cerrados» se leía como caso cerrado mal dibujado. **La pantalla
estaba bien; el que mentía era el chip.** → «Terminados».

## ⑰ LO QUE **NO** SE CAMINÓ, SIN MAQUILLAR

- 🔴 **La Hoja de resolver del prestador entera.** Mi emulador tiene el APK **1.0.3** y no toma
  el dev-client (el del cliente es 1.0.7 y sí). El founder caminó **el parcial**; `sin_devolucion`
  **nació con la firma del 9-sep y nadie lo tocó**.
- 🔴 **El hilo del prestador al revés entra al tren SIN caminar.** Si alguien abre un caso desde
  el prestador, **el orden de los mensajes es el primer lugar donde mirar**.
- 🟡 **El check del saldo**: verde por gate, **no por aparato**.

## ⑱ LO QUE ME CAZARON A MÍ, Y QUIÉN

- **El aparato**, tres veces: la clave repetida del hilo (`s${turno}` no es único — la pregunta
  y la respuesta comparten turno), la hora reimplementada mal (`es-EC` clavado + 12 h con
  sufijo, cuando `horaCortaDeMensaje` ya existía), y **una llave i18n cruda en pantalla con el
  typecheck en verde**. Esta última es clase propia: *las llaves tipadas cubren «la llave no
  existe»; no cubren «la llave existe y el bundle no la tiene»* — hermana de `L-138`/`L-160`.
- **`verify:diseno`**: un `gap: 2` crudo (R36).
- **Yo mismo, dos veces, y las dos frenando a tiempo**: casi reporto «la Despensa está rota»
  sobre un estado transitorio que no se reprodujo, y casi levanto dos defectos del prestador
  **ya curados en S96** por estar mirando un bundle anterior a esa sesión.

## ⑲ OPERATIVO AL CIERRE

- **4 typechecks en 0** · **`verify:diseno` VERDE con 73 reglas** (subió de 68).
- **Cero migraciones.** Un solo toque a `packages/api`: ninguno — todo lo del motor lo hizo A
  (`b94457a9`, `b1164a8e`, `b7fc0beb`) y lo traje por merge.
- **Punta `930bc398`**, mergeada por A al tren **`a6260a9b`**, esperando el autorizo del founder.
