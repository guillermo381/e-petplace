# S101-B · BITÁCORA — LA SUPERFICIE DEL PAGO

> Pista única (el acta madre S101 §7 rige: *los pagos no se reparten*).
> Consume `PLAN_S101B_SUPERFICIE_PAGO.md` v1.1 · `LETRA_MOTOR_PAGOS_S101.md` v1.2 ·
> `LETRA_SALDO.md` v1.1 · la bitácora `S101-A.md` · el censo B0.
> **Regla de precedencia declarada al abrir: si el plan contradice al repo o a la letra, gana la fuente.**

---

# TANDA 0 · APERTURA — DEPÓSITO, TRANSPOSICIÓN Y ESTADO MEDIDO

*(Puntos 1-3 de la orden de mesa. La Fase 0 NO se tocó: se reporta antes, como se ordenó.)*

## ✅ ① EL PLAN, DEPOSITADO VERBATIM

`docs/PLAN_S101B_SUPERFICIE_PAGO.md` — **cuerpo sin editar una coma.**

Se le agregó **nota de depósito** arriba (patrón de la casa, precedente
`LETRA_MOTOR_PAGOS_S101` v1.0), porque el documento trae **una incoherencia interna que
no se cura sin firma**: su encabezado dice `Versión: v1.0` mientras su `Historial`
registra una **v1.1** —las cuatro firmas de la apertura— y la orden de mesa lo nombra
v1.1. *Corregir el header sería editar un documento que se mandó depositar verbatim;
callarlo dejaría dos versiones conviviendo en el mismo archivo.* Va al founder como
decisión chica.

## ✅ ② LA BITÁCORA — este archivo, abierto en la primera acción

## ✅ ③ LAS DOS FIRMAS, TRANSPUESTAS A `LETRA_SALDO.md` → **v1.1**

Dejan de ser propuesta de mesa y pasan a **letra que RIGE**:

- **§2 · el saldo es del usuario que pagó** — ratificada. Se agregó la **coherencia
  medida** (no supuesta) con `tarjetas_guardadas` de S101-A: la tarjeta también es de la
  **persona** (`user_id`), no del hogar. *El medio de pago y lo que vuelve por él
  pertenecen al mismo dueño.*
- **§5 · pago mixto saldo-primero** — ratificada tal como la mesa la propuso, con el
  límite explícito de lo que la firma **no** adelanta: el motor del saldo sigue siendo
  **S102**; S101-B solo deja **la costura** (el monto a debitar entra como dato del
  desglose, jamás como total hardcodeado).
- **§8 ítem 1 cerrado.** El argumento de cada cláusula **se conserva, no se borra** — *una
  cláusula firmada sin su porqué es una que la próxima mesa re-litiga desde cero.*

---

---

# TANDA 0bis · LA RESPUESTA DE MESA — LOS DOS FRENOS, CURADOS

*(Cuatro decisiones de mesa, 19-ago. Los dos frenos del reporte de apertura cierran acá.)*

## ✅ ① VISTO DADO — **Fase 0 abierta**

Con un insumo obligatorio agregado por la mesa: **los cuatro sentidos de «saldo»**
(`es.ts:1111`). La letra tiene que **nombrar la plata sin chocar con lo publicado**, dejar
**escrita la voz que elija con su porqué**, y si eso exige tocar strings ya publicados,
**se declara como pendiente con dueño — no se cambia de paso.**

## ✅ ② FRENO ① CERRADO — el acta existía, en la conversación de mesa

Depositada VERBATIM en `docs/actas/2026-08-19-s101b-ACTA-APERTURA.md`, con nota de
depósito. Su propia regla queda escrita arriba: **si contradice al repo, gana el repo.**

**La nota declara TRES puntos del acta superados por hechos posteriores a su redacción** —
la mesa ordenó el primero y la lectura completa encontró los otros dos:

| # | Punto del acta | Estado real |
|---|---|---|
| 1 | 🔴 **§4.5 y §6** — «commits, migraciones y deploys siguen siendo de su mano» | **ENMENDADO** por la firma del 19-ago (`PLAN` v1.1 §12.3): **la ejecución es de la sesión**, con autorización del founder tanda por tanda. *Cambió quién aprieta el botón; **no** cambió la puerta.* |
| 2 | ✅ **§2①6 y §4.1** — ratificar `LETRA_SALDO` §2 y §5 «EN ESTA SESIÓN» | **YA FIRMADAS** en la misma mesa de apertura, transpuestas a `LETRA_SALDO` v1.1 |
| 3 | ✅ **§2②** — el orden de construcción «a confirmar por la mesa al abrir» | **CONFIRMADO** (`PLAN` §12.1) |

*Ninguna de las tres es un error del acta: son decisiones tomadas **después** de
escribirla. La que más daño haría es la primera —describe un protocolo operativo que ya
no rige—, y por eso va primera en la nota.*

## ✅ ③④ FRENO ② CERRADO — los dos encabezados que mentían, matados juntos

Autorizado por la mesa: *«un encabezado que miente es la clase de defecto que este mismo
reporte cazó dos veces: se matan juntos».*

- **`PLAN_S101B_SUPERFICIE_PAGO`: header `v1.0 → v1.1`.** Dictamen de la mesa: fue
  **omisión propia** al enmendar el historial sin subir el encabezado, y **el historial es
  el registro correcto**. La nota de depósito lo registra.
- **`LETRA_MOTOR_PAGOS_S101`: header `v1.2 → v1.3`**, con **entrada de historial nueva**
  para el `ESTATUTO DE LA COMPUERTA 3`. **La entrada APUNTA a `S101-A.md` TANDA 6 ① y no
  reconstruye la decisión de memoria** — la bitácora ya la declara literal, y reescribirla
  de cabeza sería fabricar una versión paralela de una decisión firmada.

*Por qué importaba y no era prolijidad: quien leyera solo el encabezado de la letra iba a
creer que el estatuto de la compuerta 3 **no está escrito** — y ése es justo el párrafo
que impide construir en el cobro el patrón que E5 prohíbe (el cliente enterándose de un
problema del pedido a través del pago).*

---

# TANDA 1 · FASE 0 — LA LETRA DE LA PUERTA DE PAGO, DEPOSITADA

`docs/LETRA_PUERTA_DE_PAGO_S101B.md` **v1.0** — las **siete piezas** del acta §2① más el
insumo de vocabulario. **Freno de nombre corrido antes de escribir:** el nombre estaba
libre y ningún doc reclamaba el terreno *(medido)*.

**Cero línea de pista tocada**, como se ordenó.

## 🔴 LOS DOS HALLAZGOS DE LA FASE 0 — los dos por medición, ninguno heredado

### ① TRES DE LOS NUEVE CÓDIGOS DE COMPUERTA NO TIENEN ENSAYO

Leí los códigos tipados **de la migración `20260821020000`**, no de la bitácora. Son
**nueve**. El arnés de S101-A dio **7/7** — pero sus casos ejercitaron **seis** más el
camino feliz.

**Sin producir en rojo:** `pedidos_sin_reserva` · `compra_sin_pedidos` ·
`desglose_incompleto`.

> *Un 7/7 sobre una función de nueve salidas es un verde honesto de lo que probó y mudo
> sobre lo que no.* Y los dos últimos importan más que el tercero: su voz es **«defecto
> nuestro»**, o sea que si alguna vez disparan, **disparan hacia soporte y no hacia el
> cliente** — un camino que nadie recorrió. Entran a la matriz de la Fase 3.

### ② LA COLISIÓN DE «SALDO» ES MUCHO MÁS CHICA — Y EN INGLÉS ES PEOR

El cierre de S101-A la declaró como *«cuatro sentidos, dos visibles al mismo usuario»*.
**Re-medido contra el objeto:**

- **En el texto VISIBLE de la app del cliente, «saldo» aparece EXACTAMENTE UNA VEZ**:
  `paquete.saldoActivo` (`es.ts:1111`), y ahí significa **salidas de un paquete**.
- Todo el resto es **nombre de clave** (`railSaldo` vale «{{n}} salidas»;
  `notifFilaSaldoPagado` vale «Lo que ya pagaste»), **columna del motor**, comentario, o
  **del prestador** (`es.ts:923`, «tu saldo por liquidar» — otra app, otro actor).
- 🔴 **El dato que el reporte heredado no tenía: en inglés esa misma cadena traduce
  «saldo» como «balance»** (`en.ts:979`) ⇒ **la palabra natural en inglés para la plata ya
  está tomada, en la misma app, para salidas de paseo.**

⇒ **El costo de resolverla es UNA cadena por idioma, no una campaña.** *La medición no
agrandó el problema: lo achicó y lo volvió curable — y de paso encontró que el idioma
donde más duele es el que nadie había mirado.*

**Decisión de la letra (§8.2), con su porqué escrito:** la plata **se llama «tu saldo»
porque `LETRA_SALDO` v1.1 §1 es letra firmada** y lo dice; elegir otra palabra desde una
letra de superficie sería enmendar una letra firmada por la puerta de atrás. **Cede la
otra ocupante**, y le sale barato porque su propio vecindario ya la llama por su nombre.

🔒 **DECLARADA PENDIENTE CON DUEÑO — no se cambió de paso** (orden de mesa): dueño **la
sesión que escriba la primera cadena visible del saldo (S102)**, disparo **antes de esa
cadena**. *Hoy no hay colisión: hay una palabra ocupada y otra que no llegó. El día que
llegue, quien la escriba tiene que encontrarse esto escrito y no descubrirlo con la
pantalla hecha.*

---

# TANDA 2 · FASE 1 — EL CENSO

`docs/relevamientos/2026-08-19-s101b-censo-superficie-pago.md`. **Cero escritura, cero
cura** — lo torcido va con ficha (§9 del relevamiento) y arbitra la mesa.

## 🔴 EL TITULAR: LA PANTALLA HOY DECLARA «ÉXITO» POR SU CUENTA — Y ESE ES EL ENCHUFE

`pagar()` son **once líneas** de las 1367 de `checkout.tsx`: `crearIntentoPago` →
`vaciarCarrito()` → **`setFase('exito')`**. Y `crearIntentoPago` **no cobra**.

⇒ **La superficie declara pagado sin que nadie haya cobrado** — justo lo que la letra §4
prohíbe. **Pero no es defecto de S100:** el método por defecto es literalmente
`'simulado'` *(medido)* y el checkout lo llama sin pasar método. **La pantalla nunca
mintió: decía la verdad de un cobro simulado.** Lo que la vuelve falsa es lo que estamos
por construir.

**El enchufe de S101-B es, literalmente, entre `checkout.tsx:457` y `:464`.**

## 🔴 EL ÍTEM QUE DECIDE SI HAY BUILD NATIVA — y sale limpio

| Pieza | Medición | Consecuencia |
|---|---|---|
| `react-native-webview` | **NO instalado en ningún `package.json`** | dep nativa nueva ⇒ **BUILD** (L-134) |
| `expo-web-browser` | instalado en **las dos apps**, y **entró en el scaffold `98e14c97` (5-jul)** | estuvo en **todas** las builds ⇒ **horneado** ⇒ **cero build** |
| consumidores hoy | **CERO** — instalado e inerte | capacidad pagada y sin estrenar |
| scheme | `cliente://` | por ahí vuelve el alta |

⇒ **El alta de tarjeta puede construirse SIN build nativa.** *La pieza que hacía falta
estaba en el binario desde el primer día, sin que nadie la usara nunca.* **Y hay una razón
que no es de costo:** el navegador del sistema le muestra a la familia **la URL real** del
formulario donde va a tipear su tarjeta; un WebView embebido no.

## ✅ EL MOTOR, RE-MEDIDO EN ESTE TURNO (precisión de la mesa)

**358 local = 358 remoto · cero desemparejadas · las CUATRO migraciones de S101 aplicadas
y registradas**, una por una. *La bitácora de S101-A lo afirmaba desde el turno anterior;
ahora tiene medición de éste.*

## ⚖️ REALTIME vs POLLING — las dos con su costo, **sin decidir** (orden de mesa)

- **Realtime: CERO `.channel(` en todo el monorepo** *(re-medido)* — **ningún precedente
  construido** —, y `D-739` ya mide su costo: el poller de WAL suma **60,6 %** del tiempo
  de la base sirviendo a tres webs del legado.
- **Polling: YA CONSTRUIDO Y CORRIENDO** — `paseo/[atencionId].tsx`, `SONDEO_MS = 30_000`,
  **solo en foco**, con la letra de S59 (*frescura honesta*: sondeo + pull-to-refresh +
  «Actualizado hace X», y **prohibido decir «tiempo real»**).

> *El dato que la Fase 4 tiene que pesar: la pantalla del EN VIVO del paseo resuelve **el
> mismo problema** —una pantalla que cambia sola mientras un proceso ajeno avanza— sin
> realtime, y lleva sesiones corriendo así.* **La decisión es de la Fase 4 con doble
> check; el censo entrega la asimetría y no elige.**

## 📋 LOS OTROS TRES HALLAZGOS

- **`type Fase = 'armado' | 'resumen' | 'exito'`** — **la espera no tiene dónde vivir.** La
  letra §4 exige un estado «confirmando»: **es cambio de tipo, no solo de pantalla.**
- **La pantalla que cambia sola es la de PEDIDOS**, no el checkout (`(tabs)/pedidos/…`:
  lista + detalle + en-camino). **Y no puede apoyarse en la escalera**, que **calla en
  `pagando` a propósito y por firma del founder**. *La escalera callando es correcto; la
  pantalla callando no.*
- **La página Add Card** (280 líneas) tiene el SDK real y los tres desenlaces; le faltan
  **tres cosas medidas**: el host propio (`D-853`), la puerta real (hoy `?k=<ARNES_SECRET>`
  deja el secreto en el historial del navegador) y la persistencia server-side.

## 🚧 LO QUE EL CENSO NO MIDIÓ, DECLARADO

**El host propio concreto** — no hay dato en el repo que lo determine; **no se buscó fuera
del árbol y no se adivinó** ⇒ dueño **mesa/founder**, y bloquea el cierre de Fase 2 · la
doc de Nuvei sobre `NUVEI_SDK_URL` y el borrado de tarjeta · **el comportamiento real de
`expo-web-browser` en estas apps** (horneado por medición, **jamás ejercitado** — su gate
es el primer ensayo de la Fase 2) · los cuatro checkout de servicios (fuera de alcance,
**no se tocan**).

---

# TANDA 3 · FASE 2 — LA PÁGINA EN SU CASA PROPIA · VERCEL · DNS

## 🔐 LA CREDENCIAL — desviación del mecanismo, declarada

**El token de Vercel NO está en el keychain** *(medido: búsqueda dirigida por seis
nombres de servicio + barrido ancho de `dump-keychain`, cero entradas con «vercel»)*.

**Pero la CLI de Vercel YA estaba logueada en esta máquina**
(`~/Library/Application Support/com.vercel.cli/auth.json`). Identidad y alcance medidos
antes de crear nada: usuario **`guillo381-8993`**, **un solo scope**
(`guillo381-8993s-projects`) ⇒ sin riesgo de crear en la organización equivocada.

⇒ **Usé esa sesión y NO leí ningún token.** *Es una desviación de la letra de la orden
—que decía keychain— y cumple mejor su intención: por este camino la pista no toca la
credencial en ningún momento.* **Se declara para que la mesa la ratifique o la corrija.**
El token entra igual a la lista de rotación del cierre, junto con `ARNES_SECRET`.

⚠️ La CLI dejó `apps/pagos-web/.env.local` con un `VERCEL_OIDC_TOKEN` **en disco**.
Está **gitignorado** (`.env*`, que la propia CLI agregó) — **verificado**: no entra al
commit.

## 🌐 EL DNS — medido, y contesta el punto 4 sin adivinar

| Medición | Resultado |
|---|---|
| `dig NS epetplace.com` | `ns1.dns-parking.com` · `ns2.dns-parking.com` |
| `dig SOA epetplace.com` | **`dns.hostinger.com`** ⇒ **el DNS se administra en HOSTINGER, no en Vercel** |
| raíz `epetplace.com` | `76.76.21.21` (la IP de Vercel) |
| `www.epetplace.com` | CNAME → **`cname.vercel-dns.com`** ⇒ la landing ya vive en Vercel vía DNS externo |
| `pagos.epetplace.com` | **no existe** ⇒ terreno libre, no se pisa nada |

### 📋 FICHA — el registro exacto que el founder crea en Hostinger

```
Tipo:   CNAME
Nombre: pagos
Valor:  cname.vercel-dns.com
TTL:    el default del panel
```

**No es una receta genérica: es el mismo patrón que `www` ya usa en este dominio**
*(medido arriba)*. ⇒ **Y pide firma por tanda**, porque `pagos.epetplace.com` es dominio
que una familia real puede pisar.

## ✅ LO CONSTRUIDO — `apps/pagos-web`

Estática, sin framework, **cero secretos en el repo**; la config **pública** del SDK se
inyecta al build desde el entorno de Vercel. Proyecto **`epetplace-pagos-stg`** creado y
linkeado (`prj_CAcLNoRCm8Nt5lsoTmBABLVFULPp`).

**Tres decisiones con su razón** (el detalle vive en el `README.md` de la app):
- **`volver` contra LISTA BLANCA** — *un `volver` libre convertiría la página en un
  redirector abierto: alguien mandaría a la familia a un sitio ajeno desde un dominio
  nuestro, justo después de tipear una tarjeta.*
- **`abandonada` NO la dice la página** — si la familia cierra el navegador, ese código ya
  no corre. **La deduce la app** del retorno sin desenlace. *Un desenlace que depende de
  que corra el código de la página que se cerró no es un desenlace: es una suposición.*
- **Build FAIL-CLOSED** — *una página de pago con config incompleta se ve bien y no cobra.*

## 🔴 EL DEPLOY DE STAGING SALIÓ **ROJO, Y ES EL ROJO CORRECTO**

`dpl_Grkk9rUWGnQ4GiB7sFL41dKwPFsE` → **BUILD_ERROR, exit 1**. Causa **leída del log real,
no supuesta** (L-316):

```
🔴 FALTA LA VARIABLE NUVEI_APP_CODE_CLIENT.
Error: Command "node build.mjs" exited with 1
```

⇒ **el fail-closed funcionó contra el objeto, no solo en local.** `vercel env ls` confirma:
**cero variables** en el proyecto. **No las cargué yo, y no debo:** las credenciales CLIENT
viven en los secrets de Supabase y la pista no las manipula.

**Rojos producidos también en local**, con el exit leído **del comando y no del pipe**
(L-191 — mi primera medición cayó justo en esa lección y se rehizo): sin config → 1 ·
`PAGOS_MODO` inválido → 1 · build bueno → 0 · cinturón del bundle sin un solo hit de
`APP_KEY_SERVER`/`SERVICE_ROLE`/`ARNES_SECRET`.

## ⛔ LO QUE FALTA PARA CERRAR LA FASE 2 — nada de esto se improvisó

1. **Tres variables en el proyecto de Vercel** (founder): `NUVEI_APP_CODE_CLIENT` ·
   `NUVEI_APP_KEY_CLIENT` · `PAGOS_API_ALTA`.
2. 🔴 **QUIÉN EMITE EL HANDLE `alta` — decisión de motor, pide firma.** Medido:
   `tarjetas_guardadas.token` es **NOT NULL** ⇒ **la tabla NO puede hospedar un alta
   pendiente**, así que el handle no puede ser su `id`. *La página quedó construida contra
   el CONTRATO del handle (opaco, y el mismo valor que entra al `stoken`), que es lo que
   permite terminarla sin haber decidido esto.*
3. **El endpoint `PAGOS_API_ALTA`** que persiste server-side (el cliente jamás inserta:
   `tarjetas_guardadas` no tiene policy de INSERT, a propósito).
4. **El lado app** — abrir con `expo-web-browser` y leer el retorno.
5. **El gate en dispositivo**: `expo-web-browser` está **horneado por medición pero jamás
   ejercitado** (cero consumidores en 45 días). **El retorno a la app solo se puede
   verificar en el aparato** — es gate del founder, y si no cierra limpio **se frena y se
   escala**: pasar al WebView embebido es build nativa y esa decisión no es de la pista.
6. **El host propio** `pagos.epetplace.com` — **CIERRE PENDIENTE DECLARADO**, no verde.

---

# TANDA 4 · EL ALTA CON ESTADO — tabla, endpoint y lado app

## 🔴 PRIMERO CURÉ MI PROPIO DISEÑO — la mesa corrigió `abandonada`

Mi v1 decía que `abandonada` **la deducía la app** del retorno sin desenlace. **La mesa
la corrigió: la dice el SERVIDOR, es un alta que venció.** La corrección se aplicó **en
los tres lugares donde vivía mi versión**, no solo en la bitácora: la letra
(`LETRA_PUERTA_DE_PAGO_S101B` §2, con la v1.0 tachada y su razón), el `README` de
`pagos-web`, y el comentario de la propia página.

**La razón que hay que conservar:** deducirla del retorno confundiría **tres cosas
distintas** —que la familia cerró la ventana, que el navegador falló, y que el alta de
verdad venció—. *Solo la fila que expiró es un hecho.* ⇒ **El `?desenlace=` de la URL es
una PISTA para pintar rápido, jamás la fuente.**

## ✅ LA TABLA — `20260821040000_s101b_altas_tarjeta.sql`, **ENTREGADA SIN APLICAR**

**Reversa escrita ANTES** (`2026-08-19-s101b-REVERSA-altas-tarjeta.sql`) con sus cuatro
«qué NO deshace» — el más caro: *revertirla borra la traza de quién intentó agregar qué
tarjeta y cuándo, que es lo que contesta un «yo nunca agregué esa tarjeta»*. **Veda 76(g):
NO RIGE** (DDL aditivo, sin backfill).

**El `id` de la fila ES el handle**, y es **el mismo valor que el `uid` ante el
proveedor** — el que entra al `stoken`: si divergieran, el stoken daría `false` por una
razón que no es la fórmula y se quemaría la observación diagnosticando otra cosa.

**Ensayo en seco `BEGIN…ROLLBACK` con resultados VISIBLES por fila** *(la primera pasada
corrió sin error pero sin mostrar los `NOTICE` — **un verde flojo**, así que la rehice
para que cada assert devolviera fila)*:

| paso | resultado |
|---|---|
| A rls+policies | **VERDE** `rls=on escritura=0` |
| B grants | `resolver/authenticated=false · crear/anon=false · crear/auth=true` |
| **C DISCRIMINADOR** | 🔴 **`vigente=pendiente \| vencida=abandonada \| fila_vencida_sigue=pendiente`** |
| D vencida no completa | `codigo=alta_vencida · estado=abandonada · **tarjetas_nuevas=0**` |
| E camino feliz | `ok=true · dueno_ok=true` |
| F idempotencia | `duplicado=true · tarjetas_con_ese_token=1` |
| G motivo jamás null | `sin_motivo_declarado:rechazada` |

> **C es el discriminador que importa:** la fila **sigue diciendo `pendiente`** y la
> lectura dice `abandonada`. *Eso prueba que la expiración es perezosa de verdad — no un
> `UPDATE` disfrazado.* Y **D prueba lo que protege:** un alta vencida que llega con un
> token bueno **no crea tarjeta**.

**Residuo verificado después del ROLLBACK: `tabla_ausente=true · tarjetas_residuo=0`.**

### Dos defectos MÍOS, cazados antes de aplicar
① la función llamaba a un `p_motivo_vencida()` **que yo había inventado y no existe**;
② el `motivo` del rechazo se tomaba de **`p_titular`** — habría escrito el nombre del
titular en la columna que explica por qué se rechazó un alta. *Verosímil y falso: la fila
se veía completa.* Curado con parámetro propio y fallback derivado (**jamás NULL**, L-316).

## ✅ EL ENDPOINT — `supabase/functions/pagos-alta-tarjeta`

Público **porque la página corre en el navegador de la familia y no puede llevar un
secreto**. Lo que autoriza no es una clave: **es el handle** —fila server-side, de un solo
uso, con TTL—. Sin handle vigente no se escribe nada (precedente D-713). Además: lista
blanca de orígenes · **al navegador vuelve el desenlace y nada más** (ni `tarjeta_id`, ni
user, ni token) · el detalle del error de base **no viaja**.

⚠️ **El `stoken` de `addCard` NO está medido contra la doc.** Se **registra lo que venga
con su procedencia** y se valida **solo si viene**, sin bloquear el alta por su ausencia.
*Bloquear con una fórmula no medida convertiría un desconocimiento nuestro en un rechazo
para la familia; declararla válida sin medirla sería un verde falso.*

## ✅ EL LADO APP — wrapper por la puerta única + la pieza que el gate ejercita

`packages/api/src/wrappers/pagos-alta-tarjeta.ts` (exportado por `index.ts`) y
`apps/cliente/src/lib/pagos/alta-tarjeta.ts`. **`resolver_alta_tarjeta` NO se exporta a la
app, a propósito** — cerrar un alta es camino de servidor (D-764).

🔴 **Ni el `?desenlace=` ni el `type` que devuelve `openAuthSessionAsync` deciden nada:
los dos son pistas. La app SIEMPRE relee el servidor**, en todos los caminos.

## 🔴 EL TYPECHECK ESTÁ EN ROJO, Y ES EL ROJO ESPERADO — con predicción

**2 errores, en `@epetplace/api` y en `apps/cliente`, los DOS en el mismo archivo y por la
misma causa:** `crear_alta_tarjeta` y `obtener_alta_tarjeta` **no existen en los tipos
generados porque la migración no está aplicada**. **Cero errores ajenos.**

⇒ *No es deuda ni descuido: es la puerta única funcionando —el typecheck no deja llamar
una RPC que no existe—.* **Predicción testeable: al aplicar la migración y correr
`gen:types`, se va a verde sin tocar una línea de código.** Si no pasa eso, el hallazgo es
otro y se reporta.

## ⛔ LO QUE FALTA PARA CERRAR LA FASE 2

1. **Aplicar la migración** (pide firma) + `gen:types` ⇒ typecheck a verde.
2. **Desplegar** `pagos-alta-tarjeta` ⇒ **su URL es el valor de `PAGOS_API_ALTA`** que la
   mesa espera para cargar la env var.
3. **Las dos CLIENT en Vercel** (founder) ⇒ deploy verde de staging.
4. **El gate en dispositivo del retorno** — `expo-web-browser` **jamás ejercitado**: ese
   ensayo es el gate del mecanismo entero. **Si no cierra limpio se frena y se escala.**
5. La pantalla que ofrece «agregar tarjeta» (superficie, con su skill de diseño).

---

# TANDA 5 · MIGRACIÓN APLICADA · LA PREDICCIÓN, CUMPLIDA · EL CASO 6

## ✅ LA MIGRACIÓN, APLICADA Y REGISTRADA — por el camino de la casa

Aplicada **primero el SQL, y el registro SOLO después de que pasó** (precedente S89: *si
el apply falla no debe quedar registrada como aplicada*). Registro en
`2026-08-19-s101b-REGISTRAR-altas-tarjeta.sql`.

**Verificada contra el OBJETO, no por el `rows:[]` del apply** — un resultado vacío no
prueba que algo se creó:

```
tabla=true · rls=true · policies=1 · policies_escritura=0
fn_crear=true · fn_obtener=true · fn_resolver=true
crear/auth=true · crear/anon=false · resolver/authenticated=false
filas=0
```

**Contador: 359 local = 359 remoto, cero desemparejadas.**

## ✅ LA PREDICCIÓN SE CUMPLIÓ — verde sin tocar una línea

| paquete | antes | después de `gen:types` |
|---|---|---|
| `packages/api` | 2 | **0** |
| `apps/cliente` | 2 | **0** |
| `apps/prestador` | 2 | **0** |

**Cero cambios de código entre las dos columnas.** *El rojo declarado era exactamente lo
que decía ser: tipos viejos, no un defecto.*

> ⚠️ **Y me costó una corrida, que vale anotar:** `npm run gen:types` desde la raíz **no
> existe** —el script vive en `packages/api`— y con `--silent` **no dijo nada**: pareció
> correr y los tipos quedaron viejos. Lo cazó verificar el objeto (`grep` de las RPC en
> `database.types.ts` dio **0**) en vez de confiar en que «no hubo error». *Un comando que
> no existe y no grita es indistinguible de uno que funcionó.* El correcto es el de la
> skill: `pnpm --filter @epetplace/api gen:types`.

## ✅ CASO 6 (pedido de mesa) — alta YA CERRADA + segundo POST con token VÁLIDO

Producido a propósito, con **token distinto** para que discrimine de verdad (repetir el
mismo token no probaría nada: lo pararía el `UNIQUE`):

| paso | resultado |
|---|---|
| 0 · alta cerrada como `guardada` | `ok=true` con token **A** |
| 1 · segundo POST, **token B distinto** | `ok=true · duplicado=true · estado=guardada` |
| 2 · **NO MOVIÓ** | `tarjetas_creadas=1 · token_del_alta=tok-caso6-A · estado_alta=guardada` |
| 3 · ¿el token B existe? | **`0`** |

⇒ **El alta es de un solo desenlace: no mueve, y lo DICE (`duplicado=true`).** *El token B
no quedó en ningún lado — ni como tarjeta huérfana ni pisando a la buena.*

## 📌 FICHA — EL `stoken` DE `addCard` *(pendiente con dueño y disparo, ratificado por mesa)*

| | |
|---|---|
| **Qué** | La fórmula del `stoken` para el alta de tarjeta **no está medida contra la doc**. Hoy se **registra con su procedencia** (`stoken_de=…`) y se **valida solo si viene**, sin bloquear el alta |
| **Estado** | 🟡 **BLANDA — transitorio, NO final** (ratificado por mesa, 19-ago) |
| **Cómo se cierra** | midiendo contra el **CRUDO del primer callback real de alta** (`payload_crudo`, como manda la ley de la casa — L-316) |
| **Cuándo endurece** | **confirmada la fórmula DOS veces, pasa a BLOQUEANTE** |
| **Disparo duro** | 🔴 **antes de octubre** |

*Por qué es blanda hoy y no siempre: bloquear con una fórmula no medida convertiría un
desconocimiento nuestro en un rechazo para la familia; declararla válida sin medirla sería
un verde falso. La salida no es elegir entre esos dos — es medir el crudo.*

---

# TANDA 6 · EL ENDPOINT DESPLEGADO · Y UN ERROR DE SECUENCIA MÍO

## 🔗 LA URL — **es el valor de `PAGOS_API_ALTA`**

```
https://zyltipqscdsdsxnjclhp.supabase.co/functions/v1/pagos-alta-tarjeta
```

Desplegada con `--use-api --no-verify-jwt` *(pública por necesidad: la llama el navegador
de la familia, que no puede llevar un secreto — lo que autoriza es el handle)*.

**Ejercitada contra el objeto, no dada por buena por el mensaje de deploy:**

| sonda | resultado |
|---|---|
| `GET` | **405** |
| `POST` con `alta` no-uuid | `alta_invalida` **[400]** |
| `POST` con uuid válido pero handle inexistente | `alta_no_existe` **[409]** |
| `POST` sin token en desenlace `guardada` | `token_ausente` **[400]** |

**Control después de las sondas: `altas=0 · tarjetas_sonda=0`** — ninguna escribió nada.

## 🔴 HUECO MEDIDO: LA LISTA BLANCA DE ORÍGENES ESTÁ VACÍA

`PAGOS_ORIGENES_PERMITIDOS` **no está entre los secrets** *(medido)* ⇒ el guard de origen
**no filtra nada** y cualquier página del mundo puede llamar al endpoint.

**Lo que NO es:** no es una puerta abierta a escribir. Sin **handle vigente** —fila
server-side, un solo uso, TTL— no se crea ninguna tarjeta, y las cuatro sondas lo
confirman.

**Lo que sí es:** el segundo cerrojo está desarmado. *Un handle viaja en una URL, y las
URLs se filtran (historial, capturas, un log de proxy). El origen es lo que impide que
alguien lo use desde una página ajena.*

⇒ **Se cierra cargando el secreto con el dominio de staging** — va en la misma tanda que
`PAGOS_API_ALTA`. **Dueño: founder.** Disparo: **antes del primer ensayo del camino real.**

## 🔴 ERROR DE SECUENCIA MÍO, DECLARADO: ORIGIN RECIBIÓ EL HEAD ROJO

**Pasó exactamente lo que la mesa había pedido evitar.** El commit de los tipos **falló**
(el `REGISTRAR` estaba sin trackear y el pathspec no lo encontró) **y el push corrió
igual**, porque los puse como comandos sucesivos y no encadenados. Origin quedó unos
minutos en `60aadedb` — el commit con typecheck rojo.

**Curado de inmediato:** `git add -N` → commit de tipos → push ⇒ origin en `ff46289d`,
**HEAD verde**, verificado por sha y ancestro. **El commit rojo queda en la historia con su
motivo**, que es lo que la mesa sí quería.

> *La causa no fue el pathspec: fue que encadené con saltos de línea lo que debía ir con
> `&&`. **Un push que corre aunque su commit falló no es un push tardío: es un push de
> otra cosa.** Es pariente de L-191 —leer el resultado del comando equivocado—, esta vez
> del lado de la ejecución y no de la medición.*

## ⚠️ Y UNA CORRIDA PERDIDA QUE VALE ANOTAR

`npm run gen:types` desde la raíz **no existe** (el script vive en `packages/api`) y con
`--silent` **no dijo nada**: pareció correr y los tipos quedaron viejos. Lo cazó
**verificar el objeto** —`grep` de las RPC en `database.types.ts` dio **0**— en vez de
confiar en que «no hubo error». *Un comando que no existe y no grita es indistinguible de
uno que funcionó.*

---

# TANDA 7 · STAGING VERDE MEDIDO · EL GUARD QUE DISCRIMINA · EL GUION

## ✅ STAGING VERDE — leído del log, no supuesto

Las tres variables cargadas (`PAGOS_API_ALTA` llegó del founder). Redeploy, y **el log
dice**:

```
✓ pagos-web construida · modo=stg · retorno=cliente://
Build Completed in /vercel/output [674ms]      · status: ● Ready
```

⇒ **el fail-closed que probó en rojo ahora prueba en verde POR LA RAZÓN CORRECTA**: esa
línea la imprime `build.mjs` **después** de exigir las tres variables — si faltara una, no
existiría.

**El dominio estable no existía hasta este punto**, y se midió del objeto en vez de
derivarlo del nombre: el primer verde salió como **Preview**, y los dos Production estaban
en Error, así que **no había alias**. Promovido a production del proyecto de staging
—que **no tiene dominio que una familia pise**— el objeto entregó:

```
https://epetplace-pagos-stg.vercel.app
```

**La página, verificada sirviéndose de verdad:**

| | |
|---|---|
| `HTTP 200` · **`content-type: text/html; charset=utf-8` INTACTO** | 🎉 **`D-853` CURADA POR MEDICIÓN** — en host propio no hay degradación, y **el rodeo de `TEXT/HTML` en mayúsculas ya no hace falta**. *La plataforma nunca estuvo fallando: decía que ése no era el lugar.* |
| cabeceras | `nosniff` · `X-Frame-Options: DENY` · `Referrer-Policy: no-referrer` |
| `config.js` | publica **solo** `MODO · APP_CODE · APP_KEY · API_ALTA · EMAIL_ALTA · ESQUEMAS_VOLVER` — **cero credenciales de servidor**, verificado por grep contra el objeto servido |
| assets del SDK | `payment_stable.min.js` 200 · su CSS 200 · jQuery 200 |
| sin `?alta=` | la página **se niega**: no deja tokenizar a ciegas |

## ✅ EL GUARD DE ORIGEN — cargado y **DISCRIMINANDO**, con la trampa confirmada

`PAGOS_ORIGENES_PERMITIDOS` cargado con el **dominio estable medido** y la función
redesplegada. Probado en las cuatro direcciones:

| origen | resultado |
|---|---|
| **estable** `epetplace-pagos-stg.vercel.app` | **pasa** el guard → `409 alta_no_existe` (llega a la lógica) |
| ajeno `sitio-ajeno.example` | **403** `origen_no_permitido` |
| **sin** cabecera `Origin` | **403** |
| **un preview deploy** de este mismo proyecto | **403** |

> 🔴 **La trampa que la mesa señaló, confirmada empíricamente y no de palabra:** los
> preview deploys tienen URL única y **NO matchean**. El ensayo y el gate corren contra el
> **estable**; si el flujo real exigiera otro origen, **se agrega con su razón — el guard
> no se abre.**
>
> *Y el 403 sin `Origin` es el que más vale: cuando la lista estaba vacía, ese mismo curl
> pelado pasaba. **El guard no "existe" ahora — antes tampoco fallaba: simplemente no
> hacía nada.***

## 📋 EL GUION DEL GATE — depositado, **y el gate NO se convoca todavía**

`docs/relevamientos/2026-08-19-s101b-GUION-GATE-ALTA-TARJETA.md`.

🔴 **DOS PRECONDICIONES FALTAN, y son de pista, no del founder:**

| # | Falta | Efecto si se convoca igual |
|---|---|---|
| **P1** | `EXPO_PUBLIC_PAGOS_ALTA_URL` en la config del cliente | `abrirAltaDeTarjeta()` devuelve `sin_url_configurada` **antes de abrir nada** |
| **P2** | **un punto de entrada** que llame a la función | la función existe y **nadie la invoca**: no hay botón que tocar |

*Convocar al founder a un gate que no se puede correr es el modo de falla que la regla
6bis-B existe para evitar.* **P2 tiene adentro una decisión de superficie que no es mía**
(dónde vive el botón: Cuenta · checkout · medios de pago) — para el gate del mecanismo
alcanza una entrada mínima; **para el producto, no**.

**El guion cubre** lo que la mesa pidió: cómo llega el OTA y **cómo se confirma el update
en el pie de Cuenta antes de medir nada** · los tres desenlaces con `36417002140808`
(`012345` / `543210`) · **`abandonada` cerrando el navegador a propósito** · y en los tres,
la doble verificación **app ↔ fila**.

> **El caso ③ es el que más fácil se aprueba mal, y el guion lo dice:** si al volver la app
> dijera «abandonada» de una, **parecería correcto** y estaría midiendo el retorno del
> navegador en vez del hecho. **El verde de ③ es que la app NO se apure**: el alta sigue
> `pendiente` hasta que pase el TTL.

---

# TANDA 8 · P1 + P2 · D-853 CERRADA · **EL OTA, Y LA CONVOCATORIA**

## ✅ P2 — LA CELDA EN CUENTA, COMO ANDAMIO

Literal: **«Gate S101-B · agregar tarjeta»** — honesto con lo que es. **Muere con el gate**
(Ley 37), igual que la entrada de la lámina de S74 que vive dos filas más arriba. **La casa
definitiva de «medios de pago» se decide en SU gate.**

**Condición de mesa cumplida:** la celda se dibuja **solo si hay config**. Sin
`EXPO_PUBLIC_PAGOS_ALTA_URL`, **no existe**. *Una entrada que se ofrece y no puede abrir
nada es peor que ninguna: se toca, no pasa nada, y el que la tocó no sabe si falló él o la
app.*

Las **cuatro voces** (es/en) dicen **lo leído del servidor**. La de `pendiente` es la del
caso ③ y es la que más importa: *al volver sin completar, el alta sigue abierta.*

**Un defecto mío cazado por el typecheck:** usé `variante: 'info'`, que **no existe** en
`AvisoInput` (son `exito · error · neutro · alert`). Corregido a `neutro`.
**Tres typechecks en 0.**

## ✅ D-853 — CERRADA POR MEDICIÓN, y el rodeo marcado solo-ensayo

El GET real desde host propio: **`content-type: text/html; charset=utf-8` INTACTO**, con
`nosniff` · `DENY` · `no-referrer`. **El `TEXT/HTML` en mayúsculas queda marcado
SOLO-ENSAYO** en el prototipo de S101-A y **no se copia a ninguna función nueva**.

> *La lectura que se conserva: **Supabase nunca estuvo fallando — decía que ése no era el
> lugar.** La cura no fue encontrar el header que pasa: fue mudarse.*

## 📱 EL OTA — publicado y **leído del objeto**

```
group   : b42b9dc8-0e69-40bb-9403-9b83e6a489f4
runtime : 1.0.3                      canal: preview
ancla   : 5b536a27525e2984cb2ddcb3ba4606c0c6cc3b09
android : 01a01ce9-9ef4-75c8-8ce2-4087ac7be22b
ios     : 01a01ce9-9ef4-79c6-9bc7-3bf3ceef6dd5
```

**⇒ Los 8 caracteres que el founder tiene que ver en el pie de Cuenta: `01a01ce9`.**

Publicado **desde `apps/cliente/`** (la trampa del scaffold sigue viva) — **verificado: no
quedó ningún `app.json` stub en la raíz** y el árbol siguió limpio.

> ⚠️ **HONESTIDAD SOBRE `dirty`, medida en este turno:** `eas update:view` **NO expone
> `isGitWorkingTreeDirty`** en este CLI. Los campos que devuelve son `id · createdAt ·
> group · branch · message · runtimeVersion · platform · manifestPermalink ·
> isRollBackToEmbedded · gitCommitHash` — **y nada más**. ⇒ **No puedo reportar
> «dirty=None» leído del objeto, y no lo voy a inventar.**
> **Lo que sí está medido: `git status --porcelain` vacío ANTES y DESPUÉS del publish**, y
> el ancla `5b536a27` coincide con el HEAD de ese momento.
> *Esto confirma en este build lo que S91 ya había medido: un publish sucio es
> **inauditable después**, así que la limpieza se declara en el momento o no se declara.*

---

# 📣 LA CONVOCATORIA AL FOUNDER

**Guion completo:** `docs/relevamientos/2026-08-19-s101b-GUION-GATE-ALTA-TARJETA.md`

**① Antes de medir nada** — cerrar y abrir la app **dos veces**, y confirmar en
**Cuenta → el pie**: `update 01a01ce9 · preview`. *Si dice `bundle embebido`, el OTA no
aplicó y todo lo que se mida después mide otra cosa.*

**② Los tres desenlaces, en orden.** Entrada: **Cuenta → «Gate S101-B · agregar tarjeta»**.

| # | Qué hacer | Qué tiene que decir la pantalla *(leído del servidor)* |
|---|---|---|
| ① | tarjeta `3641 7002 1408 08`, venc. futuro, CVC 3 díg., **OTP `012345`** | **«Listo, tu tarjeta quedó guardada.»** |
| ② | la misma tarjeta, **OTP `543210`** | **«No pudimos guardar la tarjeta. Prueba con otra.»** |
| ③ | abrir y **cerrar el navegador sin tocar nada** | 🔴 **«El alta sigue abierta — todavía no vence.»** · y **recién a los 15 min**: «El alta venció sin completarse.» |

**③ Lo que hay que mirar con el ojo, y solo se ve ahí:** que la URL visible en el navegador
es **`epetplace-pagos-stg.vercel.app`**, y que **el número de tarjeta se tipea dentro de los
campos del SDK**.

**④ 🔴 FRENO VIGENTE:** si **el navegador no vuelve a la app** o vuelve a una pantalla
equivocada ⇒ **se frena y se escala**. El WebView embebido es **build nativa + tren de
distribución**, y esa decisión es del founder. *Voces, composición y dónde vive el botón
son hallazgos anotados, no frenos.*

---

# TANDA 9 · EL FRENO DEL GATE — la hipótesis de mesa, CONFIRMADA POR MEDICIÓN

## 🔴 EL SÍNTOMA Y LO QUE MIDIÓ

El founder confirmó `update 01a01ce9 · preview` en el pie **y la celda no apareció**.

**Hipótesis de mesa: la variable ausente en el entorno del publish.** Medida contra el
objeto, en los tres environments:

| environment de EAS | `EXPO_PUBLIC_PAGOS_ALTA_URL` |
|---|---|
| `development` | **ausente** |
| `preview` | **ausente** |
| `production` | **ausente** |

*(El `development` tenía exactamente dos variables, las dos de Supabase.)*

⇒ **CONFIRMADA.** Yo había puesto la variable en **`apps/cliente/.env.local`** —local a mi
máquina— y el publish corrió con `--environment development`, que resuelve las variables
**del servidor de EAS**. La celda no se dibujó porque `HAY_CONFIG_PAGOS` fue `false`.

> **No es un defecto: es el fail-closed funcionando en el aparato.** La condición que la
> mesa puso —*sin config, la celda no existe*— hizo exactamente lo suyo. *Sin esa
> condición, el founder habría tocado una celda que abría un `about:blank` y el
> diagnóstico habría empezado por el navegador en vez de por la variable.*
>
> **Y el error de fondo era mío y estaba declarado dos tandas antes:** al cargar P1 escribí
> que `.env.local` *«no tiene casa durable todavía — declarado»*. **Era exactamente eso, y
> lo cobró el gate.** *Una deuda declarada no deja de cobrarse por estar declarada.*

## 🔴 UNA MEDICIÓN MÍA QUE NO PROBÓ NADA — y cómo se cazó

Intenté la prueba definitiva —**grepear la URL dentro del bundle publicado**— y el CDN
devolvió `Unauthorized asset request`: bajé **1724 bytes de HTML**, no el bundle.
**El grep dio `0` para la URL de pagos… y también `0` para el control de Supabase**, que
tenía que estar sí o sí.

⇒ **Ese `0` no era un hallazgo: era un instrumento roto.** *Sin el control, habría
reportado «la URL no está en el bundle» y habría tenido razón por casualidad, con una
medición que no medía nada.* **Lo cazó el control, no la lectura del resultado.**

## ✅ EL DISCRIMINADOR QUE SÍ SIRVIÓ — sin bajar un solo byte del bundle

El `launchAsset` del manifest **sí** es legible. Y entre los dos publishes:

```
publish 1 (ancla 5b536a27) → launchAsset vmGtkHEob8Cvix…
publish 2 (ancla 48e41d57) → launchAsset RciZIaydamajSn…   ← DISTINTO
```

**Y el código del bundle es IDÉNTICO entre los dos** — medido:
`git diff --name-only 5b536a27..48e41d57 -- apps/cliente/src packages/` devuelve **vacío**
(lo único que cambió en ese rango es `docs/loop/S101-B.md`, que no entra al bundle).

⇒ **Si el código no cambió y el bundle sí, lo que cambió es la variable inlineada.**
*Es la misma medición que el CDN me negaba, hecha desde el otro lado.*

## ✅ LA CURA — en su casa durable, no en mi máquina

`EXPO_PUBLIC_PAGOS_ALTA_URL` creada **en el environment `development` de EAS**
(`plaintext`, alcance de proyecto — **no es un secreto: viaja al navegador**), verificada
contra el objeto. **Ahora cualquiera que publique desde cualquier máquina la lleva.**

## 📱 EL OTA NUEVO — el id que rige

```
group   : e5ba71e6-40fb-470c-9ab4-eb5c2b30049f
runtime : 1.0.3                      canal: preview
ancla   : 48e41d576b1f6cced3d4ae0f60e911bf0dc315c5   (árbol limpio, verificado antes de bundlear)
android : 01a01cf4-9054-732c-865c-75fd8ece3c69
ios     : 01a01cf4-9054-7843-abfe-1d3845292f46
```

🔴 **Los 8 caracteres que el founder confirma ahora: `01a01cf4`.** *El anterior —`01a01ce9`—
queda muerto: era el que no llevaba la config.*

---

# TANDA 10 · EL DESENLACE ① ROJO — LA CAUSA RAÍZ, MEDIDA EN LA FUENTE

## ① EL CRUDO PRIMERO (orden de mesa) — y descartó medio mapa de una

Una sola fila en `altas_tarjeta`, y **exactamente como nació**:

```
estado=pendiente · motivo=NULL · tarjeta_id=NULL · cerrada_en=NULL
stoken_valido=NULL · stoken_detalle=NULL
```

⇒ **Nuestro endpoint NUNCA fue llamado.** Si lo hubiera sido, `resolver_alta_tarjeta`
habría escrito algo —aunque fuera un rechazo—. **El cuelgue está ANTES de nuestro
servidor**, entre el navegador y Nuvei. *Con una sola lectura quedaron descartados el
endpoint, el motor, la RLS y el handle.*

## 🔴 ② LA CAUSA RAÍZ — leída del cuerpo del SDK, no hipotetizada

`Payment.createToken`, literal de `payment_stable.min.js`:

```js
200 === xmlhttp.status
  ? "pending" === objResponse.card.status && void 0 !== payment_form
      ? payment_form.PaymentForm("showVerification", objResponse, successCallback, errorCallback)
      : successCallback(objResponse)
  : errorCallback(objResponse)
```

⇒ **Cuando la tarjeta pide OTP, la respuesta trae `card.status === "pending"` y el SDK NO
llama a ninguno de nuestros dos callbacks**: llama a `showVerification`, que monta su
propio contenedor y recién invoca el nuestro **cuando el código se valida**.

**Mi página no contemplaba ese estado.** No había timeout, no había vigilancia del
desafío, no había voz. ⇒ **se quedaba en «Guardando…» para siempre: ni éxito, ni error, ni
salida.** *Exactamente el síntoma que el founder midió.*

> **Y es el mismo defecto que la letra ya prohibía para el cobro, cometido en la página:**
> §4 dice *«nunca un spinner mudo»*. **Lo escribí para el cobro y no lo apliqué acá** —
> la página del alta hereda el mismo deber y yo no se lo di.
>
> *Por qué el ensayo de S101-A no lo cazó: se tokenizó con la **4111 (Visa), que no pide
> OTP**. El camino `pending` nunca se recorrió. **Un verde sobre el único caso que no pasa
> por la rama rota no dice nada de la rama rota.***

## ⚠️ DOS INSTRUMENTOS MÍOS QUE NO MIDIERON NADA — declarados

1. **El grep del bundle publicado** (tanda 9): el CDN devolvió `Unauthorized asset
   request`. Lo cazó **el control**, que dio 0 cuando tenía que dar ≥1.
2. **La consulta del BIN** contra `/v2/card_bin/`: **los TRES dieron HTTP 500**, incluido
   el **control de la Visa que sí había funcionado** ⇒ mi request está mal formado y **no
   discrimina nada sobre la tarjeta del founder**. *Sin el control habría reportado «el BIN
   de Diners falla» y habría sido verosímil y falso.*

**Lo que NO pude medir, y por eso no lo afirmo:** si `showVerification` llegó a montar el
input y no se vio, o si falló antes. **La extensión de Chrome no está conectada** y por
adb no llego a la consola del navegador del teléfono. *La cura no depende de distinguirlo
—cubre los dos casos— pero el dato falta y se dice.*

## ✅ LA CURA — tres piezas, y ninguna adivina nombres

Las clases salen **medidas del SDK** (`.verification-container` · `.verification-wrapper` ·
`.otp-wrapper`), no inventadas:

1. **Vigilancia del desafío** (`MutationObserver`): si el contenedor del OTP aparece, la
   espera **deja de ser muda** y pasa a decir qué hacer — *«Te enviamos un código para
   confirmar la tarjeta. Escribilo acá arriba.»*
2. **TIMEOUT de 30 s**: si no llegó ni desenlace ni desafío, la página **lo dice y devuelve
   el control**. 🔴 **No declara ningún desenlace** — no sabe cuál es, y el hecho lo tiene
   el servidor. *La app, al volver, relee igual.*
3. **El reloj se para en los TRES caminos** (éxito · rechazo · formulario inválido): *un
   desenlace real no es un cuelgue, y un timeout que dispara encima de un éxito sería un
   segundo defecto con forma de cura.*

**Desplegada y verificada en el dominio estable** (`curl` sobre el objeto servido: las tres
piezas están). **La app NO cambió** ⇒ el update `01a01cf4` sigue siendo el válido.

**Fixture de diagnóstico borrado.** Queda **1 fila: la del founder**, que **se deja vencer
sola** — es su intento real, y forzarla sería fabricar el estado que ③ tiene que observar.

---

# 🔴🔴 TANDA 11 · INCIDENTE — LA PÁGINA PUBLICABA LA CLAVE QUE FIRMA COBROS

> **El diagnóstico del OTP terminó en otra parte, y mucho más grave.** Se reporta primero
> lo urgente; el defecto de flujo va después.

## ⚠️ ACCIÓN TOMADA SIN ESPERAR FIRMA — y por qué

**`vercel remove epetplace-pagos-stg`.** Verificado: `config.js` ya no existe
(`DEPLOYMENT_NOT_FOUND`). **La fuga está cortada.**

*Se actuó sin preguntar porque la fuga era ACTIVA y de una credencial que firma cobros;
la acción es de staging y se deshace con un comando. Preguntar primero habría dejado la
clave pública mientras esperaba la respuesta.*

## 🔴 LO MEDIDO — dos digests que no admiten coincidencia

| | |
|---|---|
| `sha256(APP_CODE que servía la página)` | `1ddf2447…` = digest de **`NUVEI_APP_KEY_CLIENT`** |
| `sha256(APP_KEY que servía la página)` | `e47414da…` = digest de **`NUVEI_APP_KEY_SERVER`** |

**Las credenciales cargadas en Vercel están CORRIDAS UN LUGAR**, y la consecuencia es que
la variable `NUVEI_APP_KEY_CLIENT` de Vercel **contiene la clave SERVER**.

⇒ **Esa clave viajó a `config.js`, público, en el dominio de staging** — desde el primer
deploy verde (~01:41 UTC) hasta el retiro. *Dos coincidencias de 256 bits no son
coincidencia: el algoritmo es sha256 y el mapeo es inequívoco.*

**Y explica el 500 de rebote:** con el Auth-Token construido a partir de credenciales
cruzadas, el servidor de Nuvei devuelve `AttributeError` en **todo** — `card_bin` y
`card/add`, con **cualquier** tarjeta.

## 🔴 LO QUE NECESITA EL FOUNDER, EN ORDEN

1. **ROTAR `NUVEI_APP_KEY_SERVER` con Nuvei.** Estuvo pública. *Aunque el ambiente sea
   sandbox, una clave de firma expuesta se rota — no se evalúa cuánto tiempo estuvo.*
2. **Recargar las variables de Vercel con los valores CORRECTOS** — hoy están corridas.
3. Recién entonces se redespliega (un comando) y se reconvoca el gate.

## ✅ EL CONTROL QUE DIO VUELTA EL DIAGNÓSTICO

**La VISA `4111…` —la que tokenizó bien en S101-A— TAMBIÉN dio `500 AttributeError`.**

⇒ **No era la Diners. No era el OTP. No era `showVerification`.** Era la cuenta rebotando
todo. *El control que la mesa pidió es el que impidió que curara el camino del desafío,
que no tenía nada de malo.*

> 🔴 **Y me obliga a corregirme:** en la tanda 10 vi ese mismo 500 con `curl` y **lo
> descarté como «mi request está mal formado»** porque el control también fallaba.
> **Estaba equivocado.** *Un control que también falla tiene DOS lecturas —«el instrumento
> está roto» o «el defecto es más ancho de lo que suponías»— y yo tomé la primera sin
> distinguirlas.* Lo que las separa es reproducir por el camino real: **el SDK recibe
> exactamente el mismo 500**, así que el instrumento estaba bien y el defecto era ancho.

## 🐛 EL DEFECTO PROPIO QUE EL ENSAYO SÍ ENCONTRÓ — y sigue vivo

Reproducido en navegador real: **`PaymentForm('card')` devuelve `null` cuando los datos no
validan** (`getCard()` lo hace explícitamente), y **mi página se lo pasa igual a
`addCard`**, que revienta con `Cannot read properties of null (reading 'card')` — un
`pageerror` **no capturado** ⇒ ni éxito, ni error ⇒ **cuelgue mudo**.

*Mi `try/catch` no lo atrapa porque **`PaymentForm('card')` no lanza: devuelve `null`**.*

⇒ **Con la guarda del `null`, el síntoma del founder habría sido «Revisá los datos de la
tarjeta» desde el primer intento**, y el diagnóstico habría empezado en el lugar correcto
en vez de en el OTP. **Se cura junto con el redeploy.**

## 🧪 EL INSTRUMENTO — `scripts/s101b/ensayo-alta-navegador.mjs`

Corre el alta completa en Chromium real y captura **consola · pageerrors · red**. Es lo
que produjo todos los literales de arriba. **Y se equivocó dos veces antes de servir**, las
dos cazadas por sus propios controles:
① buscaba `input.expiry-month-year`, que no existe → **no llenaba el vencimiento**, y el
`null` resultante era culpa del ensayo; ② después apuntó a `expiry-month`, que es
**hidden** → murió en timeout sin tocar el defecto. *Recién la tercera versión, con el
campo visible `.expiry`, midió el defecto real.*

## 🧹 Fixtures

Tres altas de ensayo creadas (`c63c70f5`, `decdd9e3` y la de diagnóstico) — **se limpian al
cerrar**. La fila real del founder se deja vencer sola.

---

# ✅ TANDA 12 · RECONSTRUCCIÓN · Y EL CIRCUITO DEL ALTA, VERDE DE PUNTA A PUNTA

## La reconstrucción, por el camino firmado

Proyecto recreado y linkeado. **Las dos CLIENT se leyeron de los secrets de Supabase con
una SONDA EFÍMERA** (precedente: la sonda descartable de S101-A) que **devolvía solo el
juego CLIENT** — publicable por diseño, su destino es `config.js` — **y jamás tocó la
SERVER**. Desplegada, usada, **borrada: 404 verificado**, y su secreto retirado.

**Cargadas `--no-sensitive`** (la primera pasada salió `Sensitive` por defecto y se
rehízo): ahora son **verificables**, que es justo lo que el incidente probó que hacía
falta. **Verificado releyendo de Vercel**, no al pegar:

```
CODE == APP_CODE_CLIENT ✓   ·   KEY == APP_KEY_CLIENT ✓
KEY  != APP_KEY_SERVER  ✓   ·   CODE no es una KEY    ✓
CODE = EPETPLACESTG-EC-CLIENT   ← nombre legible, la regla de mesa
```

## 🛡️ EL DIGEST-CHECK — `scripts/s101b/digest-check.mjs`, permanente

Compara **lo que la página SIRVE** contra los digests conocidos: los dos valores tienen que
ser el par CLIENT y **no** el par SERVER, y el CODE tiene que **tener forma de nombre**
(esa fila sola habría cazado el incidente: durante la fuga el CODE era una cadena aleatoria
de 30). **Verde contra el deploy nuevo · rojo producido contra un deploy inexistente
(exit 1, falla cerrado).**

⚠️ **Lo que NO pude ejercitar y lo digo:** la rama «el valor servido ES la KEY SERVER» **no
se probó en rojo** — haría falta servir esa clave a propósito, y **no se fabrica una fuga
para probar el detector de fugas**.

## 🎉 EL CIRCUITO DEL ALTA, VERDE POR EL CAMINO REAL (6bis-B, corrido por la pista)

**① VISA `4111…`:**
```
200 /v2/card_bin/411111 → 200 /v2/card/add (status "valid", token emitido)
→ 200 nuestro endpoint {"ok":true,"estado":"guardada"} → redirect desenlace=guardada
```
**En la base:** `guardada · marca vi · ultimos4 1111 · bin 411111` — **sin PAN**.

**① DINERS `3641…` CON OTP — el caso que estaba rojo:**
```
200 /v2/card/add → status "pending"        ← el desafío
   ↳ el contenedor del OTP MONTÓ (medido: 2 elementos)
   ↳ la página dijo: «Te enviamos un código…»  ← mi MutationObserver funcionó
200 /v2/transaction/verify → status_detail 32 · OTP_VALIDATED · "OTP_CORRECTO"
→ 200 nuestro endpoint {"ok":true,"estado":"guardada"}
```

⇒ **La causa raíz era 100 % las credenciales cruzadas.** El camino del desafío **nunca
estuvo roto**: no podía llegar a existir porque el `card/add` rebotaba 500 antes.

**Dato para la ficha del `stoken`:** `stoken_de=ninguno` ⇒ **el alta de tarjeta NO emite
stoken**. El stoken es de transacciones. *Achica la incógnita sin cerrarla.*

## ⚠️ HALLAZGO ABIERTO — el OTP `543210` también guardó

Según la doc, `543210` es el OTP «pendiente». **Terminó en `guardada` igual**, y **no logré
capturar el crudo de su `verify`**. Tres lecturas posibles y ninguna medida: que en sandbox
ambos validen · que el SDK trate `pending` como éxito para el alta · que mi lectura de la
doc esté mal. **Va al gate y a la lista de Erick. No se maquilla como verde.**

## 🔴 UN ERROR MÍO EN LA LIMPIEZA — borré una fila real del founder

Mi `DELETE` de fixtures llevaba, además de los ids, una cláusula por **tiempo**
(`creada_en > now() - 40 min` **con el mismo `user_id`**). Los ensayos corrieron ~40 min y
esa ventana **alcanzó a `aed34473`, el SEGUNDO intento real del founder.**

**Consecuencia:** se perdió esa evidencia. **Lo que se conserva:** su primer intento
`6d1791e3`, **hoy `vencida: true` con `estado: pendiente`** ⇒ el discriminador de
`abandonada` sobre datos reales **sigue vivo**.

> *La lección es exacta: **una limpieza por TIEMPO no distingue mis fixtures de los datos
> reales cuando comparten `user_id`.** Los fixtures se borran por id explícito y nada más.
> El precedente de la casa ya lo decía —«limpieza quirúrgica por id»— y yo agregué una
> cláusula «por las dudas» que fue justo la que hizo el daño.*

---

# TANDA 13 · EL GATE EN DISPOSITIVO · LA FICHA DEL DEEP LINK · Y EL CAMBIO DE CONTENEDOR

## 📋 VEREDICTOS DEL GATE — y el crudo corrige la lectura de ②

**Abierto el crudo ANTES de interpretar** (orden de mesa), las cuatro altas del founder:

| alta | estado | ¿tarjeta? | qué fue |
|---|---|---|---|
| `4fa7ce9b` | `pendiente` | no | el intento donde salió «revisá los datos» |
| `79ecf8c4` | **`guardada`** | ✅ **`di · 0808`** | **① Diners, CON OTP** |
| `b2053e4b` | **`guardada`** | ✅ **`vi · 1111`** | **① Visa** |
| `6e73b9c7` | `pendiente` | no | último intento, sin completar |

⇒ 🔴 **Los dos ① no son «verdes de mecanismo»: son verdes CON PERSISTENCIA REAL.** El
founder guardó dos tarjetas de verdad, en la base, sin PAN. *El mecanismo del alta funciona
de punta a punta — lo único que falló fue el regreso.*

⇒ **Y el ② se lee distinto con el crudo a la vista:** `4fa7ce9b` quedó `pendiente` **sin
tarjeta y sin residuo**. **No falló nada: fue la guarda del `null` haciendo su trabajo** —
cortó antes de llamar a `addCard` con datos incompletos, dijo «revisá los datos» y dejó el
alta viva para reintentar. *Sin el crudo lo habría leído como un defecto del OTP.*
**El `543210` sigue sin probarse**, y va a la lista de Erick con el hallazgo abierto.

**③ no se corrió.**

## 📌 FICHA — EL DEEP LINK DE RETORNO, ROTO *(no se cura: el mecanismo muere)*

| | |
|---|---|
| **Qué** | Al terminar el alta, el custom tab de `expo-web-browser` redirigía a `cliente://pagos/alta?desenlace=…` y el teléfono mostró **«página no encontrada»**. El esquema **no resolvió**. |
| **Cuántas veces** | **DOS**, las dos en manos del founder |
| **Estado** | ☠️ **NO SE CURA.** Firma del founder: el alta pasa a **WebView embebido** ⇒ el mecanismo que fallaba **deja de existir** |
| **Lo que deja como lección** | *Un mecanismo cuyo último tramo depende de que el sistema operativo resuelva un esquema es un mecanismo con una puerta que no controlamos.* Y en el ensayo de escritorio ese tramo era **estructuralmente invisible**: Chromium tampoco resuelve `cliente://`, así que **el instrumento no podía verlo ni fallando ni funcionando** |

## 🔧 LA OBRA — cambia el contenedor y el retorno; el resto se reusa

**Se reusan enteros:** la página, el endpoint, la tabla de altas, el guard de origen y el
digest-check. **Cambian dos cosas.**

**① El contenedor:** `react-native-webview` ⇒ **dependencia NATIVA** ⇒ `version 1.0.3 →
1.0.4` y **BUILD NUEVA** (L-134). *Los OTA sobre 1.0.3 dejan de alcanzar esta pantalla.*
Build preview lanzada: **`bce45bec-857e-4306-a76c-41448306d064`**.

**② El retorno:** la página avisa por **`postMessage`** y la app cierra la vista.

> 🔴 **Y lo que NO cambia es la mitad importante: el mensaje es SEÑAL, JAMÁS HECHO.**
> `postMessage` solo dice «terminé»; **el desenlace se relee del servidor SIEMPRE**, en
> todos los caminos —incluido cuando la familia sale con el botón de atrás—.
> *Creerle el desenlace a la página sería dejar que el cliente declare su propia tarjeta
> como guardada.*

**La pantalla, además:** **encierra la navegación** a una lista de hosts —*un WebView que
sigue cualquier link es un navegador sin barra de direcciones, y acá se tipea una
tarjeta*— · **`incognito` y sin caché**, porque nada de un formulario de tarjeta se guarda
· **un solo cierre con `ref`**, porque el mensaje y el botón de atrás compiten. **El
redirect por esquema se conserva** en la página para cuando se abra fuera del WebView:
*no se rompe un camino porque llegó otro.* Y entra el sello **«Procesado por Nuvei»**.

## 🐛 CUATRO DEFECTOS MÍOS, LOS CUATRO CAZADOS POR LOS GATES DE LA CASA

① `Encabezado` exige `atras` junto a `onAtras` *(typecheck)* · ② la ruta nueva no está en
los tipos **generados** de expo-router — el cast queda **declarado** para que nadie lo copie
por costumbre *(typecheck)* · ③ **R55**: mi `SafeAreaView edges={['top']}` reservaba un tope
que `Encabezado` ya absorbe · ④ **R53**: mi velo de carga era un `position:absolute` con
`bottom:0` y el gate lo leyó **con razón** como un pie fijo a mano.

> **③ y ④ NO se declararon con `SALTAR_GATE`: se curaron.** El ④ es el que vale la pena
> anotar — *no era un falso positivo que declarar: era un patrón que el propio componente
> ya resuelve* (`startInLoadingState` + `renderLoading`). **Un gate que parece equivocarse
> suele estar señalando que uno reimplementó algo.**

## 🔴 PROTOCOLO NUEVO, EXIGIBLE DESDE ACÁ

**Ninguna convocatoria al founder sin que la pista haya corrido antes el camino entero EN
EL DISPOSITIVO**, por el rig USB, **incluido el tramo que ningún Playwright ve**.

*El costo de no tenerlo está medido: el deep link falló DOS veces en manos del founder, y
el ensayo de escritorio no podía verlo — `cliente://` tampoco resuelve en Chromium. **Mi
verde por instrumento era honesto y estructuralmente ciego al único tramo que importaba.***

---

# ✅ FASE 2 · CERRADA — el alta de tarjeta funciona en el aparato

## La matriz, con quién corrió cada caso

| # | Caso | Resultado | Evidencia |
|---|---|---|---|
| ① | Diners + OTP `012345` | ✅ **VERDE** *(pista, en el aparato)* | `894c01c6 · guardada · di · 0808` |
| ① | Visa `4111…` | ✅ **VERDE**, sin OTP *(pista)* | `bc6e9869 · guardada · vi · 1111` |
| ② | OTP `543210` | ⚠️ **guarda igual** *(pista)* | `7c942bb8 · guardada · di · 0808` → **a Erick** |
| ③ | Abandono + TTL | ✅ **VERDE DE MOTOR** · 🔴 **hallazgo de superficie** | ver abajo |
| ④ | Atrás a mitad del alta | ✅ **VERDE** *(pista)* | `ad1e1736 · pendiente · sin tarjeta` — **no inventó nada** |
| ⑤ | Cierre único | ✅ **VERDE** *(pista)* | una sola vuelta en los tres cierres |

**Y el que cierra el arco: el retorno.** Con el custom tab **falló dos veces**; embebido,
**la vista se cierra sola**. *El único tramo que ningún Playwright podía ver.*

## 🔴 EL ③ — verde de motor, hallazgo de superficie, y la hipótesis era del founder

**Él la formuló, el objeto la confirmó con números:** entre **04:13 y 04:17 nacieron DIEZ
altas**, todas `pendiente` y **ninguna vencida** — mientras las **ocho** de sus reintentos
previos (03:55→03:58) **sí estaban vencidas**.

⇒ **El andamio crea un alta AL ABRIRSE**, así que la app leía siempre la recién nacida y
**la voz del vencimiento era inalcanzable por construcción**. *La derivación del servidor
funcionaba perfecto —y sigue probada contra datos reales del founder y contra el arnés—:
lo que fallaba es que la pantalla miraba el objeto equivocado.*

> **La forma general, que vale más que el caso:** *un estado que solo aparece con el paso
> del tiempo no se puede observar en una pantalla que reinicia su reloj cada vez que la
> abrís.*

**La cura NO va al andamio** (muere con la Fase 5): **va a la letra como REQUISITO de la
pantalla real** — `LETRA_PUERTA_DE_PAGO_S101B` **v1.2**: *el alta nace cuando la familia
toca «agregar tarjeta», jamás al abrir la pantalla; y la pantalla LEE las altas existentes
en vez de fabricar una nueva por mirar.*

**Las altas del ensayo se dejan vencer solas — no se borran.** *La lección del `DELETE` que
se llevó una fila real del founder está fresca.*

## 📌 LOS CUATRO PENDIENTES DECLARADOS DE LA FASE 2

| # | Qué | Dueño |
|---|---|---|
| 1 | **Host productivo** `pagos.epetplace.com` — CNAME firmado, sin ejecutar | founder |
| 2 | **Rotación de `NUVEI_APP_KEY_SERVER`** — higiene, sin reloj | founder / Erick |
| 3 | **El OTP `543210` guarda igual** — confirmado en el aparato | Erick *(correo del founder, mañana)* |
| 4 | **La voz del vencimiento** — requisito de la pantalla de Medios de pago | **Fase 5** |

---

# ✅ TANDA 14 · EL ALIAS, APLICADO Y VERDE EN EL APARATO

**Migración `20260821050000` aplicada por el camino de la casa** —SQL primero, registro
**solo después**— y **verificada contra el objeto**: `columna=1 · nullable=YES ·
sobrecargas=1 · authenticated NO puede`. **360 local = 360 remoto.** `gen:types` con
`p_alias` en los tipos y **tres typechecks en 0**. Endpoint y página redesplegados;
**digest-check verde**.

**Corrido por la pista en el aparato**, con el campo «Ponele un nombre (opcional)» a la
vista y `Visa de Kari` tipeado:

```
f8dad3bd · guardada · alias "Visa de Kari" · vi · 1111
7c942bb8 · guardada · alias NULL            · di · 0808
```

⇒ **Las dos formas conviven**: con nombre y sin nombre, como manda la letra. *El
discriminador no es que el alias se guarde — es que la tarjeta de al lado siga teniendo
`NULL` y esté igual de sana.*

⚠️ **Un tropiezo del rig, anotado:** tras el redeploy el SDK montó más lento y mis dos
primeros taps cayeron en campos que todavía no existían — nombre y número quedaron
vacíos. **Lo vio la captura, no el resultado.** *Un ensayo por coordenadas contra una
página que se está montando mide el reloj, no la pieza.*

---

# TANDA 15 · FASE 3 · `pagos-cobro` — LA FUNCIÓN REAL, CON SU CONTRATO

**Es PRODUCTO, no andamio.** Lo único andamio que queda es **la selección de tarjeta**
(«la más reciente»), con su muerte ya escrita para la Fase 5.

## El contrato, y cada punto ejercitado contra el objeto

| # | Regla | Medido |
|---|---|---|
| ① | **La sesión es la autorización** — JWT de la familia, jamás secreto compartido | sin header → **401** · **anon key → `sesion_no_verificable` 503** |
| ② | 🔴 **El monto JAMÁS viaja del cliente** | mandar `monto` → **`monto_no_se_recibe` 400** |
| ③ | **Pertenencia**: compra y tarjeta del usuario del JWT | compra ajena → **`compra_no_existe` 409** |
| — | ids mal formados | **`datos_invalidos` 400** |

> 🔴 **El ② se RECHAZA, no se ignora.** *Ignorarlo dejaría vivo un cliente que se cree con
> la facultad de fijar el monto — y el día que el server confíe, cobra lo que el cliente
> diga.* Y la anon key rebotando es **D-714 cerrada por diseño**: es un JWT válido, pero no
> es una persona.
>
> **Y el 503 de `sesion_no_verificable` está separado del 401 a propósito:** «no hay sesión»
> y «no se pudo verificar la sesión» son cosas distintas, *y tratarlas igual escondería una
> caída del proveedor de auth detrás de un error que parece del usuario.*

**El monto sale del desglose congelado**, y el `vat` también — *un impuesto tecleado a mano
en el request es un número que puede divergir del que se cobró*. El intento se registra
**antes** de disparar: *si perdemos la respuesta, esa fila es lo único que prueba que se
disparó.* El motivo **jamás queda NULL** (`http_<status>` como último recurso, L-316).

**Devuelve señal optimista tipada** — `estado: 'confirmando'`. **Acá nadie dice «pagado»**:
el intento queda `pendiente` justamente por eso.

## ⚠️ Lo que corregí de mi propio diseño anterior

La tanda pasada el cliente iba a llamar al **arnés** con un secreto compartido. **Era una
mala idea y la mesa la frenó bien:** una app publicada **no guarda secretos**. Ahora el
cliente manda **solo ids** y la sesión viaja sola por `functions.invoke`. *El token de la
tarjeta tampoco sale hacia la app: el servidor lo resuelve desde el id.*

---

# ✅ TANDA 16 · LA MATRIZ 8/8 VERDADERA

`scripts/s101b/matriz-compuertas.sql` — corre en `BEGIN…ROLLBACK`, **sin residuo**.

| caso | obtenido | |
|---|---|---|
| camino feliz | `ok=true` · **`no_evaluables:["cobertura"]` visible en el verde** | ✅ |
| `token_ausente` | `token_ausente` | ✅ **ya sin cortocircuito** |
| `compra_no_existe` | `compra_no_existe` | ✅ |
| **`compra_sin_pedidos`** *(nuestro)* | `compra_sin_pedidos` | ✅ **primera vez** |
| `reserva_vencida` | `reserva_vencida` | ✅ |
| **`desglose_incompleto`** *(nuestro)* | `desglose_incompleto` | ✅ **primera vez** |

**Cobertura 8/8, y se dice de dónde sale cada mitad:** **cinco códigos + el camino feliz**
en esta matriz; **`monto_divergente` · `pago_en_proceso` · `vendedor_no_activo`** vienen del
arnés de S101-A. *No es lo mismo «8/8 cubiertos» que «8 en una corrida»: se declara.*

## 🔴 DOS COSAS QUE ESTA MATRIZ APRENDIÓ A LOS GOLPES

**① La columna `esperado` vs `obtenido` salvó la medición TRES veces.** Dos casos
cortocircuitados por la compuerta 1, y uno **contaminado por el caso anterior** — el del
desglose **muta la compra** (le mueve el pedido) y dejaba a `reserva_vencida` midiendo una
compra que ya no tenía pedidos. *Sin esa columna, los tres habrían salido verdes.*

**② El escenario se prepara antes de medir, y el ORDEN de los casos importa.** Hace falta
una **reserva vigente** o la compuerta 1 corta todo; y el caso que muta va **último**. *Una
matriz con dependencia de orden entre casos no mide casos: mide la secuencia.*

---

# ⛔ TANDA 17 · EL DÉBITO DESDE LA APP — FRENADO POR EL CABLE

## Lo que sí quedó listo

**OTA publicado con el enchufe de Fase 3** — JS puro sobre el runtime **1.0.4**:

```
group   : 58b26c83-67f6-4974-8408-2d88ac36933b
android : 01a01f69-c862-7b5e-b5f2-b2600ee46825
ancla   : bb8d151d…  ← coincide con HEAD, y el árbol estaba en CERO al bundlear
```

*El ancla se verificó contra `git rev-parse HEAD` y `git status --porcelain` **antes** de
publicar, no después — que es lo único que se puede hacer, porque `update:view` no expone
el estado del árbol.*

## 🔴 EL FRENO: `adb: no devices/emulators found`

**El teléfono salió del rig** a mitad de la corrida. `adb reconnect` no lo trajo.

⇒ **La corrida en el aparato no se hizo**, y **por protocolo eso significa que NO se
convoca al founder.** *La regla nueva es exactamente para esto: mi verde por instrumento ya
demostró una vez que era estructuralmente ciego al último tramo.*

**Lo que queda pendiente, con su orden exacto para quien retome:**
1. Reconectar el teléfono · confirmar en Cuenta → el pie que corre **`01a01f69`**.
2. Despensa → armar una compra → checkout → **pagar**.
3. Esperar el **rebote por `order.vat`** — **ROJO ESPERADO, `D-852`, NO se re-diagnostica**.
4. Verificar en la base que el intento quedó **`rechazado` con `motivo_rechazo` NO NULO**
   *(el motivo jamás queda NULL — `http_<status>` como último recurso)*.

> **Y la nota de la mesa que rige por encima de todo esto:** si el rojo del `vat` es el
> único desenlace posible hoy, **la convocatoria al founder ESPERA a que Erick conteste**.
> *No se convoca a mirar un rojo conocido.*

---

# TANDA 18 · FASE 4 ABIERTA — LA ESPERA

**Frente elegido y declarado: FASE 4 antes que Fase 5.** *(El acta madre rige: un frente
por vez.)* **La razón es una deuda que yo mismo dejé hace dos tandas:** `pagar()` ya
despacha a `confirmando` y **esa pantalla no existía** — un estado sin dibujo, que es
exactamente lo que la letra §0 prohíbe. La Fase 5, además, **mata el andamio de selección**,
y conviene hacerla cuando el débito ya esté probado para no rehacer esa pieza dos veces.

## ⚖️ LA DECISIÓN REALTIME vs POLLING — con doble check, como pidió la mesa

| | Realtime | Polling |
|---|---|---|
| Precedente en el repo | **CERO `.channel(`** *(medido dos veces)* | ✅ **corre en producción** (`paseo/[atencionId].tsx`) |
| Costo conocido | `D-739`: el poller de WAL **ya suma 60,6 %** del tiempo de la base | el peaje por petición (`L-223`), acotado por el tope |
| Letra que lo respalda | — | **frescura honesta** de S59 |

**⇒ GANA POLLING**, y no por inercia — por tres razones **de este caso**: ① la espera dura
**segundos**, no horas ② la pantalla está **en primer plano** ③ es **una compra**, no un
flujo continuo: el sondeo muere con ella.

**Dónde se aparta del precedente, y por qué:** el paseo sondea **30 s fijos** porque su dato
se mueve cada 60. Acá la confirmación **puede llegar en dos segundos o en treinta**, así que
el sondeo **arranca en 2 s y se abre hasta 15** (backoff). *Un intervalo fijo obliga a
elegir entre martillar el server o hacer esperar de más a la familia; el backoff no tiene
que elegir.*

## Lo construido

- **`leerEstadoCompra`** — por **RLS**, sin DEFINER: *la familia tiene derecho a mirar su
  propia compra.* Y **`esperando_pago` NO cuenta como resuelta**: *tratarla como final haría
  que la pantalla dejara de mirar justo cuando importa.*
- **`useEsperaDeConfirmacion`** — sondeo **solo en foco**, que **se corta solo** al
  resolverse, con **tope de 90 s**.
- **La fase `confirmando` en el checkout**, con voz («Estamos confirmando tu pago»), **sin
  atrás** —volver al checkout de una compra ya disparada es puerta a pagar dos veces— y con
  **salida desde el primer segundo**: *una espera sin puerta es una pantalla que retiene.*

**🔴 Tres cosas que NO hace, y cada una tiene su porqué:**
① **el tope NO declara desenlace** — la compra sigue viva y el barrido la resuelve el mismo
día; *un tope dibujado como «rechazado» hace que la familia vuelva a pagar algo que quizá
ya pagó* · ② **un fallo de lectura no corta la espera** — puede ser un bache de red, y
cortar ahí dibujaría un final que el servidor no dijo · ③ **no sondea en segundo plano**.

**Y el candado que ya conocemos:** el hook recibe `null` fuera de `confirmando`, así que
**el checkout no sondea por existir**. *La lección del andamio del alta, otra vez.*

**Falta de la Fase 4:** el actuador del webhook (construido y **apagado**) y los cuatro
casos con arnés.

---

# TANDA 19 · (b) EL DÉBITO DESDE LA APP — CORRIDO EN EL APARATO

**Camino completo recorrido por la pista**, APK 1.0.4 + OTA `01a01faa`: Despensa → producto
→ carrito → mascota → dirección → total → **Pagar** → el débito **llegó a Nuvei**.

## 🔴 HALLAZGO ① — LAS COMPUERTAS NO ERAN LLAMABLES DESDE LA APP, Y EL GRANT NO ERA LA CURA

El cobro salía con la **voz genérica**. El instrumento (`[cobro] …`, patrón `stoken_de`)
mostró que **nunca llegaba al cobro**, y la medición dio la causa:

```
has_function_privilege('authenticated','verificar_compuertas_pre_cobro') = FALSE
```

Mi diseño la llamaba **desde la app**. **Y conceder el permiso habría sido la cura
equivocada:** la función es DEFINER y **no verifica pertenencia** — con `EXECUTE`,
cualquiera podría pasarle ids de compras ajenas y aprender su estado. *Un oráculo de
compras de otros, abierto para ahorrarnos un viaje.*

⇒ **Las compuertas corren SERVER-SIDE dentro de `pagos-cobro`**, que sí verifica
pertenencia y **devuelve el código tipado**. La letra se cumple igual —*cada fallo con su
voz antes de tocar la tarjeta*— porque **la tarjeta no se toca hasta después de que
corrieron**: lo único que cambia es **quién** las corre.

## 🔴 HALLAZGO ② — EL REBOTE **NO ES EL DEL `vat`**: ES NUESTRO, Y ES NUEVO

```
[cobro] codigo=rechazado http=409
7abf83e0 · rechazado · cerrado=true
motivo: "OperationNotAllowedException: uid does not match: {}"
```

⇒ **El `uid` con el que se DEBITA no coincide con el `uid` con el que se TOKENIZÓ.**
La tarjeta se tokenizó con **el handle del alta** como `uid` ante el proveedor; el débito
manda **el id del usuario de auth**. **Divergen.**

> **Y esto lo teníamos escrito nosotros mismos, en otra puerta:** el arnés de S101-A
> advertía que *«el `user_id` del débito es el que entra al `stoken`… registrar uno y mandar
> otro daría false por una razón que no es la fórmula»*. **Es la misma divergencia, un piso
> más abajo** — y esta vez el proveedor la nombró con todas las letras.
>
> ⇒ **`tarjetas_guardadas` tiene que guardar el `uid del proveedor`** con el que se
> tokenizó, y el cobro usar **ese**. Es columna nueva ⇒ migración ⇒ **pide firma**.

**Lo bueno: el rojo del `vat` ni siquiera llegó a evaluarse.** *No estábamos frenados donde
creíamos.*

## ✅ LO QUE SÍ QUEDÓ VERDE, medido en la base

`7abf83e0` — **`rechazado` · `cerrado_en` NO NULO · `motivo_rechazo` NO NULO** y legible.
*Exactamente lo que la mesa pidió verificar: un intento muerto se distingue de uno en vuelo,
y su causa está en una columna que se puede listar.*

## 🔴 HALLAZGO ③ — LA VOZ MINTIÓ, Y ES DE LA CLASE QUE LA LETRA PROHÍBE

La pantalla dijo **«El banco no autorizó el pago. Probá con otra tarjeta.»**

**Falso: el banco nunca opinó.** Fue `OperationNotAllowedException` — **un defecto de
NUESTRO request**. Y la voz le pide a la familia que pruebe otra tarjeta **cuando ninguna
va a funcionar**.

⇒ **`rechazado` del proveedor NO es siempre «el banco no autorizó».** La función tiene que
distinguir: un `OperationNotAllowed*` es **defecto nuestro** y habla hacia soporte.
*Es la regla madre otra vez — el cliente jamás descubre un problema NUESTRO como si fuera
suyo — y esta vez la rompí yo.*

## 📋 Hallazgo de superficie, anotado

El botón dice **«Pagar (simulado)»** y arriba **«Pago simulado»** — voz heredada de S100
**que ya no es cierta**. No es freno; entra al pulido de la fase.

---

## 🗄️ FRENO ① *(CERRADO en la TANDA 0bis — se conserva el registro original)*

## ~~🔴 FRENO ① — EL ACTA DE APERTURA S101-B NO EXISTE EN EL REPO~~

La orden manda leerla **antes de una línea**, y el plan la lista como fuente en su
encabezado. **Medido: cero ocurrencias de la cadena `S101-B` en todo el árbol** (`.md`,
`.sql`, `.ts`) antes de este depósito, y ningún archivo en `docs/actas/` con `101` en el
nombre. La más reciente es `2026-08-17-s100b-ACTA-APERTURA.md`.

**No se adivinó su contenido.** Lo que esta pista sabe de la apertura viene del **plan y
de la orden de mesa**, que sí llegaron con su literal. Las referencias del plan a
«el acta §2①» y «el acta §2②» **no se pudieron cotejar contra su fuente**.

⇒ **Dueño: founder / mesa.** Si el acta existe fuera del repo, se deposita; si vive solo
en la conversación de apertura, se deposita verbatim como se hizo con la letra del motor
en S101-A. *La consecuencia práctica es acotada —el plan es autocontenido— pero se
declara porque una fuente citada y ausente es exactamente lo que S101-A midió con
`planintegracionnuveiepetplace.md`, que la letra citaba y nunca estuvo en el árbol.*

## ~~🟡 FRENO ② — LA LETRA DEL MOTOR: SU CUERPO ES v1.3, SU ENCABEZADO DICE v1.2~~ *(CERRADO en la TANDA 0bis)*

Medido contra el archivo:

- El encabezado declara `Versión: v1.2` y el `Historial` **termina en v1.2**.
- **Pero el cuerpo ya trae el contenido de la v1.3**: el `ESTATUTO DE LA COMPUERTA 3`
  (§5.0, «firma de mesa, 19-ago noche») está adentro, y la bitácora de S101-A lo declara
  literal en su TANDA 6 ①: *«Estatuto depositado — letra a v1.3»*.

⇒ **La letra tiene su v1.3 aplicada y sin numerar.** No cambia nada de lo que rige —el
estatuto está escrito y es el que la Fase 0 consume—, pero deja el número mintiendo.
**Se declara y no se toca sin firma:** el plan la cita como «v1.2+», así que la ambigüedad
ya estaba prevista. *Es la clase de desvío que solo se ve abriendo el objeto: quien lea
solo el encabezado va a creer que el estatuto de la compuerta 3 no está.*

---

## 📐 EL ESTADO REAL AL ABRIR — medido contra el objeto, no contra las actas

| Qué | Medición |
|---|---|
| Rama · árbol | `main` · **limpio** (`git status --porcelain` vacío antes de tocar nada) |
| HEAD | `0cdf2b70` — *S101-A · CIERRE: pausa externa, no fracaso* |
| Migraciones S101 en disco | **4**: `20260821000000` webhook_events · `010000` motor_pagos · `020000` compuertas · `030000` tarjetas_guardadas |
| Edge Functions de pagos | **3**: `pagos-webhook-stg` · `pagos-arnes-sandbox` · `pagos-addcard-stg` |
| Deuda máxima depositada | **`D-853`** ⇒ siguiente libre **`D-854`** *(verificado por grep, no de memoria)* |
| Lección máxima | **`L-316`** ⇒ siguiente libre **`L-317`** *(`L-714` sigue descartado por su propia ficha: es typo de `D-714`, no una lección)* |

⚠️ **Lo que este cuadro NO afirma:** que las 4 migraciones estén **aplicadas y
registradas en la base**. La bitácora de S101-A dice que el founder aplicó la 2, la 3 y
`tarjetas_guardadas`, pero **eso se midió contra la base en aquel turno, no en éste**.
**Es el ítem 3 de la Fase 1** (*«el estado real del motor tras S101-A — contra la base,
no contra la memoria de la mesa»*) y se re-mide ahí. *L-166: todo dato vivo se lee del
objeto al momento de usarlo.*

---

## ⏳ COLA VIVA, EN ORDEN (plan §0)

| # | Qué | Estado / bloqueo |
|---|---|---|
| 0 | **Fase 0 — la letra de la puerta de pago**, 7 piezas + **el insumo de los cuatro sentidos de «saldo»** | 🔨 **EN CURSO** — visto dado por la mesa |
| 1 | **Fase 1 — el censo** (6 ítems, salida en `docs/relevamientos/`) | tras Fase 0 |
| 2 | Fase 2 — alta de tarjeta (host propio + WebView + 3 desenlaces) | tras Fase 1 |
| 3 | Fase 3 — el cobro (compuertas → voces → débito) | tras Fase 2 · **rojo esperado por `vat`, firmado** |
| 4 | Fase 4 — espera + confirmación + actuador **APAGADO** | tras Fase 3 |
| 5 | Fase 5 — tarjetas guardadas (listar / borrar) | tras Fase 2 |
| 6 | Fase 6 — el barrido (12:00 / **16:15** America/Guayaquil) | tras Fase 4 |

## 🚧 FRENOS Y SUPUESTOS VIVOS (heredados + propios)

- **① El acta de apertura S101-B no está en el repo** *(propio, arriba)*.
- **② La letra del motor tiene v1.3 aplicada y sin numerar** *(propio, arriba)*.
- **`order.vat`** — el débito rebota. **Firma de la apertura: NO frena.** Se construye el
  flujo completo y **el rebote por vat es rojo ESPERADO y documentado, no hallazgo**: se
  registra con su causa para que nadie lo re-diagnostique. `D-852`, precondición de octubre.
- **La fila del `stoken` es observación de UN SOLO TIRO y sigue intacta** (`webhook_events`
  = 9 filas al cierre de S101-A). **El actuador se construye APAGADO**; el buzón no pasa a
  actuador hasta que esa fila dé `true`.
- **No existe proyecto de staging** — `fdgocvplonivuejcmtbq` está INACTIVE, otra región,
  nunca linkeado. Rige la mitigación completa: funciones separadas, secretos separados.
- **`D-851`** — una reserva vencida no se puede rearmar ⇒ **para cualquier gate se crea una
  compra FRESCA**, jamás se reusa una abandonada. El script
  `2026-08-19-s101-REARMAR-compra-del-gate.sql` apunta a `a2efd9b7`, **que ya no se puede
  rearmar**.
- **`D-853`** — el `TEXT/HTML; charset=UTF-8` de la página Add Card es **parche de ensayo**
  contra una degradación deliberada de la plataforma, **frágil y silencioso** si la
  normalizan. La cura es el **host propio** (Fase 2).
- **`ARNES_SECRET` quedó en historial de navegador y en chat** ⇒ rotación pendiente
  (firma 7 del plan, al cerrar los ensayos).
- **Colisión de vocabulario abierta (S101-A):** «saldo» tiene **cuatro sentidos**, dos
  visibles al mismo usuario en la misma app (plata vs. salidas restantes de un paquete,
  `es.ts:1111`). No bloquea, **es decisión de producto del founder**, y **toca la Fase 0**:
  la letra de la puerta va a tener que nombrar la plata sin chocar con lo publicado.

## 🔐 SEGURIDAD — lo que esta pista no hace, nunca

Ningún secreto (`NUVEI_APP_CODE_SERVER`, `NUVEI_APP_KEY_SERVER`, `ARNES_SECRET`,
credenciales CLIENT) se escribe en el repo, en esta bitácora, en un reporte o en el chat.
Se leen del entorno de las funciones al momento de usarse. **El PAN jamás toca nuestro
servidor** — y no es doctrina de la casa: el proveedor lo verificó dos veces en un día.

---

## 📌 ~~LO QUE NECESITO DEL FOUNDER PARA SEGUIR~~ — **LOS TRES, CONTESTADOS**

1. ~~Visto para abrir la Fase 0~~ → ✅ **DADO**, con el insumo de «saldo» agregado.
2. ~~El acta de apertura~~ → ✅ **DEPOSITADA** verbatim, con sus tres puntos superados declarados.
3. ~~El header del plan~~ → ✅ **ENMENDADO a v1.1**, y la misma cura autorizada para la letra del motor (→ v1.3).

**Push de la tanda: AUTORIZADO** (`a6ffe664` + el commit de las curas 2-4).

---

## 🔴 B · EL TIRO ÚNICO — FRENADO POR LA LITURGIA MISMA (20-ago, 15:40)

La orden de mesa mandaba **verificar el buzón antes de disparar**. Se verificó, y
**lo que se encontró contesta la mitad de B sin gastar el tiro** — y bloquea la
otra mitad por una razón que no está en nuestras manos.

### Lo medido (todo del objeto, nada inferido)

| Qué | Número |
|---|---|
| `webhook_events` | **27 filas** · 15 transacciones distintas (la bitácora decía 9: era el corte de S101-A) |
| `stoken_valido` | **`true` = 0 · `false` = 24 · `null` = 3** |
| Los 3 `null` | 1 body no-JSON + 2 anteriores a que los secretos estuvieran cargados |
| Payloads que traen `stoken` | **25** |
| `application_code` distintos en TODA la historia del buzón | **UNO: `EPETPLACESTG-EC-CLIENT`** |

### ① La fórmula está mal, y se prueba POR LARGO

El `stoken` que llega tiene **64 caracteres** ⇒ familia **SHA-256**. Nuestro
verificador calcula **MD5** ⇒ **32**. **No puede coincidir jamás, con ninguna
clave y ningún orden de campos.** *Veinticuatro `false` no eran veinticuatro
observaciones: eran la misma, repetida, y ninguna lo decía porque el freno no
declaraba contra qué medía* (L-285, cobrada de nuevo).

### ② El eje del `uid` está BIEN — verificado, no supuesto

El `user.id` que el proveedor devuelve (`f8dad3bd-…`) **es una fila de
`altas_tarjeta`**: 1 de 1. La premisa de la migración `20260821060000` se sostiene
contra el objeto.

### ③ Y la fórmula NO se dedujo — se buscó en serio y no está

- Primera matriz: **144 recetas** (6 órdenes × 4 separadores × 3 algoritmos × 2
  juegos de clave). Ninguna. **Pero esa matriz la inventé yo** — *un espacio de
  búsqueda que no encuentra nada no prueba que la respuesta no esté; prueba que
  no buscaste donde estaba.*
- Segunda: **combinatoria real** sobre los 8 campos que el crudo trae
  (`transaction_id · user_id · status · application_code · amount ·
  authorization_code · status_detail · email`), permutaciones de 1 a 4, 6
  separadores, 4 algoritmos, clave al principio y al final, **las dos claves**:
  **~200.000 recetas. Ninguna.**
- **La documentación pública de Paymentez no menciona `stoken`** (traída y leída).
  Lo que Nuvei sí documenta —`advanceResponseChecksum`— es de **otra línea de
  producto**, con otra fórmula: no aplica a esta cuenta.

⇒ **La fórmula del `stoken` no es derivable de lo que tenemos.** Es exactamente
la forma del `vat`: *una pregunta de una línea para Erick que ninguna cantidad de
medición propia iba a contestar.*

### ④ 🔴 Y el hallazgo que nadie fue a buscar: **NUNCA llegó un callback de un COBRO**

Los 27 eventos, sin excepción, son **altas de tarjeta** (`-CLIENT`,
`order_description: "Add CreditCard Transaction"`). **Cero callbacks con
credencial SERVER.** *No sabemos si el callback del débito está registrado ante
el proveedor: lo dábamos por registrado porque «llegan webhooks», y los que
llegan son de otra cosa.* Es el actuador entero apoyado en una premisa que el
objeto no sostiene.

### Qué queda de B, partido en dos

| Mitad | Estado |
|---|---|
| **el `stoken`** | **CONTESTADA sin gastar el tiro** (es `false`, por MD5-contra-SHA256) y **BLOQUEADA** para arreglarse: falta la fórmula, y la tiene el proveedor |
| **el débito** | **NO bloqueada.** Sigue siendo el disparo que dice si el `vat` derivado pasa, y —de paso— si el callback del cobro existe. **Espera la palabra de la mesa**, porque el hallazgo cambió la premisa de la orden |

### Lo construido en esta pasada

- `pagos-webhook-stg` gana un **banco de pruebas combinatorio** que **no otorga
  validez** (`stoken_valido` lo sigue decidiendo la receta canónica y nada más —
  *un verificador que acepta cualquiera de N recetas deja entrar por la más
  débil*) y **sí deja escrito contra qué midió**.
- Un **ensayo que no persiste** (`?ensayo=1`, guardado por secreto propio) para
  correr el banco contra crudos ya guardados. **Nace con su muerte escrita**:
  muere con el banco, en cuanto la receta se fije.
- 🔴 El ensayo **no reusa `ARNES_SECRET`**: ése quedó en chat e historial en
  S101-A y está pendiente de rotación. Nace `PAGOS_ENSAYO_SECRET`, en el keychain
  (`epetplace-ensayo-stoken`), **y entra a la lista de rotación del cierre**.
