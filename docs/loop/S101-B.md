# S101-B · BITÁCORA — LA SUPERFICIE DEL PAGO

> Pista única (el acta madre S101 §7 rige: *los pagos no se reparten*).
> Consume `PLAN_S101B_SUPERFICIE_PAGO.md` v1.1 · `LETRA_MOTOR_PAGOS_S101.md` v1.2 ·
> `LETRA_SALDO.md` v1.1 · la bitácora `S101-A.md` · el censo B0.
> **Regla de precedencia declarada al abrir: si el plan contradice al repo o a la letra, gana la fuente.**

---

# TANDA 0 · APERTURA — DEPÓSITO, TRANSPOSICIÓN Y ESTADO MEDIDO

*(Puntos 1-3 de la orden de mesa. La Fase 0 NO se tocó: se reporta antes, como se ordenó.)*

## ✅ ① EL PLAN, DEPOSITADO VERBATIM

`docs/PLAN_S101B_SUPERFICIE_PAGO.md` — **cuerpo sin editar una coma.**

Se le agregó **nota de depósito** arriba (patrón de la casa, precedente
`LETRA_MOTOR_PAGOS_S101` v1.0), porque el documento trae **una incoherencia interna que
no se cura sin firma**: su encabezado dice `Versión: v1.0` mientras su `Historial`
registra una **v1.1** —las cuatro firmas de la apertura— y la orden de mesa lo nombra
v1.1. *Corregir el header sería editar un documento que se mandó depositar verbatim;
callarlo dejaría dos versiones conviviendo en el mismo archivo.* Va al founder como
decisión chica.

## ✅ ② LA BITÁCORA — este archivo, abierto en la primera acción

## ✅ ③ LAS DOS FIRMAS, TRANSPUESTAS A `LETRA_SALDO.md` → **v1.1**

Dejan de ser propuesta de mesa y pasan a **letra que RIGE**:

- **§2 · el saldo es del usuario que pagó** — ratificada. Se agregó la **coherencia
  medida** (no supuesta) con `tarjetas_guardadas` de S101-A: la tarjeta también es de la
  **persona** (`user_id`), no del hogar. *El medio de pago y lo que vuelve por él
  pertenecen al mismo dueño.*
- **§5 · pago mixto saldo-primero** — ratificada tal como la mesa la propuso, con el
  límite explícito de lo que la firma **no** adelanta: el motor del saldo sigue siendo
  **S102**; S101-B solo deja **la costura** (el monto a debitar entra como dato del
  desglose, jamás como total hardcodeado).
- **§8 ítem 1 cerrado.** El argumento de cada cláusula **se conserva, no se borra** — *una
  cláusula firmada sin su porqué es una que la próxima mesa re-litiga desde cero.*

---

## 🔴 FRENO ① — EL ACTA DE APERTURA S101-B NO EXISTE EN EL REPO

La orden manda leerla **antes de una línea**, y el plan la lista como fuente en su
encabezado. **Medido: cero ocurrencias de la cadena `S101-B` en todo el árbol** (`.md`,
`.sql`, `.ts`) antes de este depósito, y ningún archivo en `docs/actas/` con `101` en el
nombre. La más reciente es `2026-08-17-s100b-ACTA-APERTURA.md`.

**No se adivinó su contenido.** Lo que esta pista sabe de la apertura viene del **plan y
de la orden de mesa**, que sí llegaron con su literal. Las referencias del plan a
«el acta §2①» y «el acta §2②» **no se pudieron cotejar contra su fuente**.

⇒ **Dueño: founder / mesa.** Si el acta existe fuera del repo, se deposita; si vive solo
en la conversación de apertura, se deposita verbatim como se hizo con la letra del motor
en S101-A. *La consecuencia práctica es acotada —el plan es autocontenido— pero se
declara porque una fuente citada y ausente es exactamente lo que S101-A midió con
`planintegracionnuveiepetplace.md`, que la letra citaba y nunca estuvo en el árbol.*

## 🟡 FRENO ② — LA LETRA DEL MOTOR: SU CUERPO ES v1.3, SU ENCABEZADO DICE v1.2

Medido contra el archivo:

- El encabezado declara `Versión: v1.2` y el `Historial` **termina en v1.2**.
- **Pero el cuerpo ya trae el contenido de la v1.3**: el `ESTATUTO DE LA COMPUERTA 3`
  (§5.0, «firma de mesa, 19-ago noche») está adentro, y la bitácora de S101-A lo declara
  literal en su TANDA 6 ①: *«Estatuto depositado — letra a v1.3»*.

⇒ **La letra tiene su v1.3 aplicada y sin numerar.** No cambia nada de lo que rige —el
estatuto está escrito y es el que la Fase 0 consume—, pero deja el número mintiendo.
**Se declara y no se toca sin firma:** el plan la cita como «v1.2+», así que la ambigüedad
ya estaba prevista. *Es la clase de desvío que solo se ve abriendo el objeto: quien lea
solo el encabezado va a creer que el estatuto de la compuerta 3 no está.*

---

## 📐 EL ESTADO REAL AL ABRIR — medido contra el objeto, no contra las actas

| Qué | Medición |
|---|---|
| Rama · árbol | `main` · **limpio** (`git status --porcelain` vacío antes de tocar nada) |
| HEAD | `0cdf2b70` — *S101-A · CIERRE: pausa externa, no fracaso* |
| Migraciones S101 en disco | **4**: `20260821000000` webhook_events · `010000` motor_pagos · `020000` compuertas · `030000` tarjetas_guardadas |
| Edge Functions de pagos | **3**: `pagos-webhook-stg` · `pagos-arnes-sandbox` · `pagos-addcard-stg` |
| Deuda máxima depositada | **`D-853`** ⇒ siguiente libre **`D-854`** *(verificado por grep, no de memoria)* |
| Lección máxima | **`L-316`** ⇒ siguiente libre **`L-317`** *(`L-714` sigue descartado por su propia ficha: es typo de `D-714`, no una lección)* |

⚠️ **Lo que este cuadro NO afirma:** que las 4 migraciones estén **aplicadas y
registradas en la base**. La bitácora de S101-A dice que el founder aplicó la 2, la 3 y
`tarjetas_guardadas`, pero **eso se midió contra la base en aquel turno, no en éste**.
**Es el ítem 3 de la Fase 1** (*«el estado real del motor tras S101-A — contra la base,
no contra la memoria de la mesa»*) y se re-mide ahí. *L-166: todo dato vivo se lee del
objeto al momento de usarlo.*

---

## ⏳ COLA VIVA, EN ORDEN (plan §0)

| # | Qué | Estado / bloqueo |
|---|---|---|
| 0 | **Fase 0 — la letra de la puerta de pago**, 7 piezas | **próximo paso**, esperando el visto de la mesa a este reporte |
| 1 | **Fase 1 — el censo** (6 ítems, salida en `docs/relevamientos/`) | tras Fase 0 |
| 2 | Fase 2 — alta de tarjeta (host propio + WebView + 3 desenlaces) | tras Fase 1 |
| 3 | Fase 3 — el cobro (compuertas → voces → débito) | tras Fase 2 · **rojo esperado por `vat`, firmado** |
| 4 | Fase 4 — espera + confirmación + actuador **APAGADO** | tras Fase 3 |
| 5 | Fase 5 — tarjetas guardadas (listar / borrar) | tras Fase 2 |
| 6 | Fase 6 — el barrido (12:00 / **16:15** America/Guayaquil) | tras Fase 4 |

## 🚧 FRENOS Y SUPUESTOS VIVOS (heredados + propios)

- **① El acta de apertura S101-B no está en el repo** *(propio, arriba)*.
- **② La letra del motor tiene v1.3 aplicada y sin numerar** *(propio, arriba)*.
- **`order.vat`** — el débito rebota. **Firma de la apertura: NO frena.** Se construye el
  flujo completo y **el rebote por vat es rojo ESPERADO y documentado, no hallazgo**: se
  registra con su causa para que nadie lo re-diagnostique. `D-852`, precondición de octubre.
- **La fila del `stoken` es observación de UN SOLO TIRO y sigue intacta** (`webhook_events`
  = 9 filas al cierre de S101-A). **El actuador se construye APAGADO**; el buzón no pasa a
  actuador hasta que esa fila dé `true`.
- **No existe proyecto de staging** — `fdgocvplonivuejcmtbq` está INACTIVE, otra región,
  nunca linkeado. Rige la mitigación completa: funciones separadas, secretos separados.
- **`D-851`** — una reserva vencida no se puede rearmar ⇒ **para cualquier gate se crea una
  compra FRESCA**, jamás se reusa una abandonada. El script
  `2026-08-19-s101-REARMAR-compra-del-gate.sql` apunta a `a2efd9b7`, **que ya no se puede
  rearmar**.
- **`D-853`** — el `TEXT/HTML; charset=UTF-8` de la página Add Card es **parche de ensayo**
  contra una degradación deliberada de la plataforma, **frágil y silencioso** si la
  normalizan. La cura es el **host propio** (Fase 2).
- **`ARNES_SECRET` quedó en historial de navegador y en chat** ⇒ rotación pendiente
  (firma 7 del plan, al cerrar los ensayos).
- **Colisión de vocabulario abierta (S101-A):** «saldo» tiene **cuatro sentidos**, dos
  visibles al mismo usuario en la misma app (plata vs. salidas restantes de un paquete,
  `es.ts:1111`). No bloquea, **es decisión de producto del founder**, y **toca la Fase 0**:
  la letra de la puerta va a tener que nombrar la plata sin chocar con lo publicado.

## 🔐 SEGURIDAD — lo que esta pista no hace, nunca

Ningún secreto (`NUVEI_APP_CODE_SERVER`, `NUVEI_APP_KEY_SERVER`, `ARNES_SECRET`,
credenciales CLIENT) se escribe en el repo, en esta bitácora, en un reporte o en el chat.
Se leen del entorno de las funciones al momento de usarse. **El PAN jamás toca nuestro
servidor** — y no es doctrina de la casa: el proveedor lo verificó dos veces en un día.

---

## 📌 LO QUE NECESITO DEL FOUNDER PARA SEGUIR

1. **Visto a este reporte** para abrir la **Fase 0** (la letra de la puerta de pago).
2. **El acta de apertura S101-B** — depositarla, o confirmar que vive solo en la
   conversación y que el plan es la fuente que rige (freno ①).
3. **Decisión chica:** el header del plan dice v1.0 y su historial v1.1 — ¿se enmienda el
   encabezado a v1.1 o se deja como está? *(No lo toqué: se mandó depositar verbatim.)*

*Nada de esto bloquea escribir la Fase 0 salvo el punto 1, que es el orden que la propia
orden fijó: «reportá el estado antes de tocar la Fase 0».*
