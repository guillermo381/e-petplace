# S101-B · BITÁCORA — LA SUPERFICIE DEL PAGO

> Pista única (el acta madre S101 §7 rige: *los pagos no se reparten*).
> Consume `PLAN_S101B_SUPERFICIE_PAGO.md` v1.1 · `LETRA_MOTOR_PAGOS_S101.md` v1.2 ·
> `LETRA_SALDO.md` v1.1 · la bitácora `S101-A.md` · el censo B0.
> **Regla de precedencia declarada al abrir: si el plan contradice al repo o a la letra, gana la fuente.**

---

# TANDA 0 · APERTURA — DEPÓSITO, TRANSPOSICIÓN Y ESTADO MEDIDO

*(Puntos 1-3 de la orden de mesa. La Fase 0 NO se tocó: se reporta antes, como se ordenó.)*

## ✅ ① EL PLAN, DEPOSITADO VERBATIM

`docs/PLAN_S101B_SUPERFICIE_PAGO.md` — **cuerpo sin editar una coma.**

Se le agregó **nota de depósito** arriba (patrón de la casa, precedente
`LETRA_MOTOR_PAGOS_S101` v1.0), porque el documento trae **una incoherencia interna que
no se cura sin firma**: su encabezado dice `Versión: v1.0` mientras su `Historial`
registra una **v1.1** —las cuatro firmas de la apertura— y la orden de mesa lo nombra
v1.1. *Corregir el header sería editar un documento que se mandó depositar verbatim;
callarlo dejaría dos versiones conviviendo en el mismo archivo.* Va al founder como
decisión chica.

## ✅ ② LA BITÁCORA — este archivo, abierto en la primera acción

## ✅ ③ LAS DOS FIRMAS, TRANSPUESTAS A `LETRA_SALDO.md` → **v1.1**

Dejan de ser propuesta de mesa y pasan a **letra que RIGE**:

- **§2 · el saldo es del usuario que pagó** — ratificada. Se agregó la **coherencia
  medida** (no supuesta) con `tarjetas_guardadas` de S101-A: la tarjeta también es de la
  **persona** (`user_id`), no del hogar. *El medio de pago y lo que vuelve por él
  pertenecen al mismo dueño.*
- **§5 · pago mixto saldo-primero** — ratificada tal como la mesa la propuso, con el
  límite explícito de lo que la firma **no** adelanta: el motor del saldo sigue siendo
  **S102**; S101-B solo deja **la costura** (el monto a debitar entra como dato del
  desglose, jamás como total hardcodeado).
- **§8 ítem 1 cerrado.** El argumento de cada cláusula **se conserva, no se borra** — *una
  cláusula firmada sin su porqué es una que la próxima mesa re-litiga desde cero.*

---

---

# TANDA 0bis · LA RESPUESTA DE MESA — LOS DOS FRENOS, CURADOS

*(Cuatro decisiones de mesa, 19-ago. Los dos frenos del reporte de apertura cierran acá.)*

## ✅ ① VISTO DADO — **Fase 0 abierta**

Con un insumo obligatorio agregado por la mesa: **los cuatro sentidos de «saldo»**
(`es.ts:1111`). La letra tiene que **nombrar la plata sin chocar con lo publicado**, dejar
**escrita la voz que elija con su porqué**, y si eso exige tocar strings ya publicados,
**se declara como pendiente con dueño — no se cambia de paso.**

## ✅ ② FRENO ① CERRADO — el acta existía, en la conversación de mesa

Depositada VERBATIM en `docs/actas/2026-08-19-s101b-ACTA-APERTURA.md`, con nota de
depósito. Su propia regla queda escrita arriba: **si contradice al repo, gana el repo.**

**La nota declara TRES puntos del acta superados por hechos posteriores a su redacción** —
la mesa ordenó el primero y la lectura completa encontró los otros dos:

| # | Punto del acta | Estado real |
|---|---|---|
| 1 | 🔴 **§4.5 y §6** — «commits, migraciones y deploys siguen siendo de su mano» | **ENMENDADO** por la firma del 19-ago (`PLAN` v1.1 §12.3): **la ejecución es de la sesión**, con autorización del founder tanda por tanda. *Cambió quién aprieta el botón; **no** cambió la puerta.* |
| 2 | ✅ **§2①6 y §4.1** — ratificar `LETRA_SALDO` §2 y §5 «EN ESTA SESIÓN» | **YA FIRMADAS** en la misma mesa de apertura, transpuestas a `LETRA_SALDO` v1.1 |
| 3 | ✅ **§2②** — el orden de construcción «a confirmar por la mesa al abrir» | **CONFIRMADO** (`PLAN` §12.1) |

*Ninguna de las tres es un error del acta: son decisiones tomadas **después** de
escribirla. La que más daño haría es la primera —describe un protocolo operativo que ya
no rige—, y por eso va primera en la nota.*

## ✅ ③④ FRENO ② CERRADO — los dos encabezados que mentían, matados juntos

Autorizado por la mesa: *«un encabezado que miente es la clase de defecto que este mismo
reporte cazó dos veces: se matan juntos».*

- **`PLAN_S101B_SUPERFICIE_PAGO`: header `v1.0 → v1.1`.** Dictamen de la mesa: fue
  **omisión propia** al enmendar el historial sin subir el encabezado, y **el historial es
  el registro correcto**. La nota de depósito lo registra.
- **`LETRA_MOTOR_PAGOS_S101`: header `v1.2 → v1.3`**, con **entrada de historial nueva**
  para el `ESTATUTO DE LA COMPUERTA 3`. **La entrada APUNTA a `S101-A.md` TANDA 6 ① y no
  reconstruye la decisión de memoria** — la bitácora ya la declara literal, y reescribirla
  de cabeza sería fabricar una versión paralela de una decisión firmada.

*Por qué importaba y no era prolijidad: quien leyera solo el encabezado de la letra iba a
creer que el estatuto de la compuerta 3 **no está escrito** — y ése es justo el párrafo
que impide construir en el cobro el patrón que E5 prohíbe (el cliente enterándose de un
problema del pedido a través del pago).*

---

# TANDA 1 · FASE 0 — LA LETRA DE LA PUERTA DE PAGO, DEPOSITADA

`docs/LETRA_PUERTA_DE_PAGO_S101B.md` **v1.0** — las **siete piezas** del acta §2① más el
insumo de vocabulario. **Freno de nombre corrido antes de escribir:** el nombre estaba
libre y ningún doc reclamaba el terreno *(medido)*.

**Cero línea de pista tocada**, como se ordenó.

## 🔴 LOS DOS HALLAZGOS DE LA FASE 0 — los dos por medición, ninguno heredado

### ① TRES DE LOS NUEVE CÓDIGOS DE COMPUERTA NO TIENEN ENSAYO

Leí los códigos tipados **de la migración `20260821020000`**, no de la bitácora. Son
**nueve**. El arnés de S101-A dio **7/7** — pero sus casos ejercitaron **seis** más el
camino feliz.

**Sin producir en rojo:** `pedidos_sin_reserva` · `compra_sin_pedidos` ·
`desglose_incompleto`.

> *Un 7/7 sobre una función de nueve salidas es un verde honesto de lo que probó y mudo
> sobre lo que no.* Y los dos últimos importan más que el tercero: su voz es **«defecto
> nuestro»**, o sea que si alguna vez disparan, **disparan hacia soporte y no hacia el
> cliente** — un camino que nadie recorrió. Entran a la matriz de la Fase 3.

### ② LA COLISIÓN DE «SALDO» ES MUCHO MÁS CHICA — Y EN INGLÉS ES PEOR

El cierre de S101-A la declaró como *«cuatro sentidos, dos visibles al mismo usuario»*.
**Re-medido contra el objeto:**

- **En el texto VISIBLE de la app del cliente, «saldo» aparece EXACTAMENTE UNA VEZ**:
  `paquete.saldoActivo` (`es.ts:1111`), y ahí significa **salidas de un paquete**.
- Todo el resto es **nombre de clave** (`railSaldo` vale «{{n}} salidas»;
  `notifFilaSaldoPagado` vale «Lo que ya pagaste»), **columna del motor**, comentario, o
  **del prestador** (`es.ts:923`, «tu saldo por liquidar» — otra app, otro actor).
- 🔴 **El dato que el reporte heredado no tenía: en inglés esa misma cadena traduce
  «saldo» como «balance»** (`en.ts:979`) ⇒ **la palabra natural en inglés para la plata ya
  está tomada, en la misma app, para salidas de paseo.**

⇒ **El costo de resolverla es UNA cadena por idioma, no una campaña.** *La medición no
agrandó el problema: lo achicó y lo volvió curable — y de paso encontró que el idioma
donde más duele es el que nadie había mirado.*

**Decisión de la letra (§8.2), con su porqué escrito:** la plata **se llama «tu saldo»
porque `LETRA_SALDO` v1.1 §1 es letra firmada** y lo dice; elegir otra palabra desde una
letra de superficie sería enmendar una letra firmada por la puerta de atrás. **Cede la
otra ocupante**, y le sale barato porque su propio vecindario ya la llama por su nombre.

🔒 **DECLARADA PENDIENTE CON DUEÑO — no se cambió de paso** (orden de mesa): dueño **la
sesión que escriba la primera cadena visible del saldo (S102)**, disparo **antes de esa
cadena**. *Hoy no hay colisión: hay una palabra ocupada y otra que no llegó. El día que
llegue, quien la escriba tiene que encontrarse esto escrito y no descubrirlo con la
pantalla hecha.*

---

# TANDA 2 · FASE 1 — EL CENSO

`docs/relevamientos/2026-08-19-s101b-censo-superficie-pago.md`. **Cero escritura, cero
cura** — lo torcido va con ficha (§9 del relevamiento) y arbitra la mesa.

## 🔴 EL TITULAR: LA PANTALLA HOY DECLARA «ÉXITO» POR SU CUENTA — Y ESE ES EL ENCHUFE

`pagar()` son **once líneas** de las 1367 de `checkout.tsx`: `crearIntentoPago` →
`vaciarCarrito()` → **`setFase('exito')`**. Y `crearIntentoPago` **no cobra**.

⇒ **La superficie declara pagado sin que nadie haya cobrado** — justo lo que la letra §4
prohíbe. **Pero no es defecto de S100:** el método por defecto es literalmente
`'simulado'` *(medido)* y el checkout lo llama sin pasar método. **La pantalla nunca
mintió: decía la verdad de un cobro simulado.** Lo que la vuelve falsa es lo que estamos
por construir.

**El enchufe de S101-B es, literalmente, entre `checkout.tsx:457` y `:464`.**

## 🔴 EL ÍTEM QUE DECIDE SI HAY BUILD NATIVA — y sale limpio

| Pieza | Medición | Consecuencia |
|---|---|---|
| `react-native-webview` | **NO instalado en ningún `package.json`** | dep nativa nueva ⇒ **BUILD** (L-134) |
| `expo-web-browser` | instalado en **las dos apps**, y **entró en el scaffold `98e14c97` (5-jul)** | estuvo en **todas** las builds ⇒ **horneado** ⇒ **cero build** |
| consumidores hoy | **CERO** — instalado e inerte | capacidad pagada y sin estrenar |
| scheme | `cliente://` | por ahí vuelve el alta |

⇒ **El alta de tarjeta puede construirse SIN build nativa.** *La pieza que hacía falta
estaba en el binario desde el primer día, sin que nadie la usara nunca.* **Y hay una razón
que no es de costo:** el navegador del sistema le muestra a la familia **la URL real** del
formulario donde va a tipear su tarjeta; un WebView embebido no.

## ✅ EL MOTOR, RE-MEDIDO EN ESTE TURNO (precisión de la mesa)

**358 local = 358 remoto · cero desemparejadas · las CUATRO migraciones de S101 aplicadas
y registradas**, una por una. *La bitácora de S101-A lo afirmaba desde el turno anterior;
ahora tiene medición de éste.*

## ⚖️ REALTIME vs POLLING — las dos con su costo, **sin decidir** (orden de mesa)

- **Realtime: CERO `.channel(` en todo el monorepo** *(re-medido)* — **ningún precedente
  construido** —, y `D-739` ya mide su costo: el poller de WAL suma **60,6 %** del tiempo
  de la base sirviendo a tres webs del legado.
- **Polling: YA CONSTRUIDO Y CORRIENDO** — `paseo/[atencionId].tsx`, `SONDEO_MS = 30_000`,
  **solo en foco**, con la letra de S59 (*frescura honesta*: sondeo + pull-to-refresh +
  «Actualizado hace X», y **prohibido decir «tiempo real»**).

> *El dato que la Fase 4 tiene que pesar: la pantalla del EN VIVO del paseo resuelve **el
> mismo problema** —una pantalla que cambia sola mientras un proceso ajeno avanza— sin
> realtime, y lleva sesiones corriendo así.* **La decisión es de la Fase 4 con doble
> check; el censo entrega la asimetría y no elige.**

## 📋 LOS OTROS TRES HALLAZGOS

- **`type Fase = 'armado' | 'resumen' | 'exito'`** — **la espera no tiene dónde vivir.** La
  letra §4 exige un estado «confirmando»: **es cambio de tipo, no solo de pantalla.**
- **La pantalla que cambia sola es la de PEDIDOS**, no el checkout (`(tabs)/pedidos/…`:
  lista + detalle + en-camino). **Y no puede apoyarse en la escalera**, que **calla en
  `pagando` a propósito y por firma del founder**. *La escalera callando es correcto; la
  pantalla callando no.*
- **La página Add Card** (280 líneas) tiene el SDK real y los tres desenlaces; le faltan
  **tres cosas medidas**: el host propio (`D-853`), la puerta real (hoy `?k=<ARNES_SECRET>`
  deja el secreto en el historial del navegador) y la persistencia server-side.

## 🚧 LO QUE EL CENSO NO MIDIÓ, DECLARADO

**El host propio concreto** — no hay dato en el repo que lo determine; **no se buscó fuera
del árbol y no se adivinó** ⇒ dueño **mesa/founder**, y bloquea el cierre de Fase 2 · la
doc de Nuvei sobre `NUVEI_SDK_URL` y el borrado de tarjeta · **el comportamiento real de
`expo-web-browser` en estas apps** (horneado por medición, **jamás ejercitado** — su gate
es el primer ensayo de la Fase 2) · los cuatro checkout de servicios (fuera de alcance,
**no se tocan**).

---

## 🗄️ FRENO ① *(CERRADO en la TANDA 0bis — se conserva el registro original)*

## ~~🔴 FRENO ① — EL ACTA DE APERTURA S101-B NO EXISTE EN EL REPO~~

La orden manda leerla **antes de una línea**, y el plan la lista como fuente en su
encabezado. **Medido: cero ocurrencias de la cadena `S101-B` en todo el árbol** (`.md`,
`.sql`, `.ts`) antes de este depósito, y ningún archivo en `docs/actas/` con `101` en el
nombre. La más reciente es `2026-08-17-s100b-ACTA-APERTURA.md`.

**No se adivinó su contenido.** Lo que esta pista sabe de la apertura viene del **plan y
de la orden de mesa**, que sí llegaron con su literal. Las referencias del plan a
«el acta §2①» y «el acta §2②» **no se pudieron cotejar contra su fuente**.

⇒ **Dueño: founder / mesa.** Si el acta existe fuera del repo, se deposita; si vive solo
en la conversación de apertura, se deposita verbatim como se hizo con la letra del motor
en S101-A. *La consecuencia práctica es acotada —el plan es autocontenido— pero se
declara porque una fuente citada y ausente es exactamente lo que S101-A midió con
`planintegracionnuveiepetplace.md`, que la letra citaba y nunca estuvo en el árbol.*

## ~~🟡 FRENO ② — LA LETRA DEL MOTOR: SU CUERPO ES v1.3, SU ENCABEZADO DICE v1.2~~ *(CERRADO en la TANDA 0bis)*

Medido contra el archivo:

- El encabezado declara `Versión: v1.2` y el `Historial` **termina en v1.2**.
- **Pero el cuerpo ya trae el contenido de la v1.3**: el `ESTATUTO DE LA COMPUERTA 3`
  (§5.0, «firma de mesa, 19-ago noche») está adentro, y la bitácora de S101-A lo declara
  literal en su TANDA 6 ①: *«Estatuto depositado — letra a v1.3»*.

⇒ **La letra tiene su v1.3 aplicada y sin numerar.** No cambia nada de lo que rige —el
estatuto está escrito y es el que la Fase 0 consume—, pero deja el número mintiendo.
**Se declara y no se toca sin firma:** el plan la cita como «v1.2+», así que la ambigüedad
ya estaba prevista. *Es la clase de desvío que solo se ve abriendo el objeto: quien lea
solo el encabezado va a creer que el estatuto de la compuerta 3 no está.*

---

## 📐 EL ESTADO REAL AL ABRIR — medido contra el objeto, no contra las actas

| Qué | Medición |
|---|---|
| Rama · árbol | `main` · **limpio** (`git status --porcelain` vacío antes de tocar nada) |
| HEAD | `0cdf2b70` — *S101-A · CIERRE: pausa externa, no fracaso* |
| Migraciones S101 en disco | **4**: `20260821000000` webhook_events · `010000` motor_pagos · `020000` compuertas · `030000` tarjetas_guardadas |
| Edge Functions de pagos | **3**: `pagos-webhook-stg` · `pagos-arnes-sandbox` · `pagos-addcard-stg` |
| Deuda máxima depositada | **`D-853`** ⇒ siguiente libre **`D-854`** *(verificado por grep, no de memoria)* |
| Lección máxima | **`L-316`** ⇒ siguiente libre **`L-317`** *(`L-714` sigue descartado por su propia ficha: es typo de `D-714`, no una lección)* |

⚠️ **Lo que este cuadro NO afirma:** que las 4 migraciones estén **aplicadas y
registradas en la base**. La bitácora de S101-A dice que el founder aplicó la 2, la 3 y
`tarjetas_guardadas`, pero **eso se midió contra la base en aquel turno, no en éste**.
**Es el ítem 3 de la Fase 1** (*«el estado real del motor tras S101-A — contra la base,
no contra la memoria de la mesa»*) y se re-mide ahí. *L-166: todo dato vivo se lee del
objeto al momento de usarlo.*

---

## ⏳ COLA VIVA, EN ORDEN (plan §0)

| # | Qué | Estado / bloqueo |
|---|---|---|
| 0 | **Fase 0 — la letra de la puerta de pago**, 7 piezas + **el insumo de los cuatro sentidos de «saldo»** | 🔨 **EN CURSO** — visto dado por la mesa |
| 1 | **Fase 1 — el censo** (6 ítems, salida en `docs/relevamientos/`) | tras Fase 0 |
| 2 | Fase 2 — alta de tarjeta (host propio + WebView + 3 desenlaces) | tras Fase 1 |
| 3 | Fase 3 — el cobro (compuertas → voces → débito) | tras Fase 2 · **rojo esperado por `vat`, firmado** |
| 4 | Fase 4 — espera + confirmación + actuador **APAGADO** | tras Fase 3 |
| 5 | Fase 5 — tarjetas guardadas (listar / borrar) | tras Fase 2 |
| 6 | Fase 6 — el barrido (12:00 / **16:15** America/Guayaquil) | tras Fase 4 |

## 🚧 FRENOS Y SUPUESTOS VIVOS (heredados + propios)

- **① El acta de apertura S101-B no está en el repo** *(propio, arriba)*.
- **② La letra del motor tiene v1.3 aplicada y sin numerar** *(propio, arriba)*.
- **`order.vat`** — el débito rebota. **Firma de la apertura: NO frena.** Se construye el
  flujo completo y **el rebote por vat es rojo ESPERADO y documentado, no hallazgo**: se
  registra con su causa para que nadie lo re-diagnostique. `D-852`, precondición de octubre.
- **La fila del `stoken` es observación de UN SOLO TIRO y sigue intacta** (`webhook_events`
  = 9 filas al cierre de S101-A). **El actuador se construye APAGADO**; el buzón no pasa a
  actuador hasta que esa fila dé `true`.
- **No existe proyecto de staging** — `fdgocvplonivuejcmtbq` está INACTIVE, otra región,
  nunca linkeado. Rige la mitigación completa: funciones separadas, secretos separados.
- **`D-851`** — una reserva vencida no se puede rearmar ⇒ **para cualquier gate se crea una
  compra FRESCA**, jamás se reusa una abandonada. El script
  `2026-08-19-s101-REARMAR-compra-del-gate.sql` apunta a `a2efd9b7`, **que ya no se puede
  rearmar**.
- **`D-853`** — el `TEXT/HTML; charset=UTF-8` de la página Add Card es **parche de ensayo**
  contra una degradación deliberada de la plataforma, **frágil y silencioso** si la
  normalizan. La cura es el **host propio** (Fase 2).
- **`ARNES_SECRET` quedó en historial de navegador y en chat** ⇒ rotación pendiente
  (firma 7 del plan, al cerrar los ensayos).
- **Colisión de vocabulario abierta (S101-A):** «saldo» tiene **cuatro sentidos**, dos
  visibles al mismo usuario en la misma app (plata vs. salidas restantes de un paquete,
  `es.ts:1111`). No bloquea, **es decisión de producto del founder**, y **toca la Fase 0**:
  la letra de la puerta va a tener que nombrar la plata sin chocar con lo publicado.

## 🔐 SEGURIDAD — lo que esta pista no hace, nunca

Ningún secreto (`NUVEI_APP_CODE_SERVER`, `NUVEI_APP_KEY_SERVER`, `ARNES_SECRET`,
credenciales CLIENT) se escribe en el repo, en esta bitácora, en un reporte o en el chat.
Se leen del entorno de las funciones al momento de usarse. **El PAN jamás toca nuestro
servidor** — y no es doctrina de la casa: el proveedor lo verificó dos veces en un día.

---

## 📌 ~~LO QUE NECESITO DEL FOUNDER PARA SEGUIR~~ — **LOS TRES, CONTESTADOS**

1. ~~Visto para abrir la Fase 0~~ → ✅ **DADO**, con el insumo de «saldo» agregado.
2. ~~El acta de apertura~~ → ✅ **DEPOSITADA** verbatim, con sus tres puntos superados declarados.
3. ~~El header del plan~~ → ✅ **ENMENDADO a v1.1**, y la misma cura autorizada para la letra del motor (→ v1.3).

**Push de la tanda: AUTORIZADO** (`a6ffe664` + el commit de las curas 2-4).
