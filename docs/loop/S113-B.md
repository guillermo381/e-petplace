# S113-B · el parte de la pista B — `packages/ui`

**Lote 0 en `pista/s113-b`** (`4e944cb3`, ya en `main`).
**Lote 0.1 en `pista/s113-b-01` · SHA `0dfe1bda`**, desde `main 8f568432` — que
contiene el `9d19de78` que la mesa nombró, más un commit de docs de A. En
`origin`, verificado por `ls-remote`.

---

## 📮 LOTE 0.3 — DOS CAMBIOS DE PROPS

**① `voz.orbe` MURIÓ. Nacen `voz.abrir` y `voz.cerrar`** (D-1019): el orbe se
llama distinto según lo que va a hacer. **En web `accessibilityState` no llega,
así que la etiqueta ES el mecanismo.** El compilador te lo va a exigir.

**② `PresenciaCoach` gana `coach?: boolean`.** Con `coach: false` es **la
puerta a los pendientes y nada más** —sin «Preguntale», sin dedos— y se viste
del acento de su casa, no del violeta. **Es la que reemplaza a
`BurbujaPendientes`**, y la que el cliente monta en memorial.
⚠️ `BurbujaPendientes` **sigue exportada a propósito**: el retiro y el montaje
de la nueva van en el MISMO commit (`L-395`), y ese commit es de quien toca
`apps/`.

**El parte del 0.3:** `docs/loop/S113-B-03.md` (rama `pista/s113-b-03`).

---

## 📮 LOTE 0.2 — EL CAMBIO DE PROPS QUE TE TOCA

**`onPreguntar` YA NO ES DEL ORBE.** Bajó a la fila: es el **primer nodo**,
pegado al orbe, con su propio orbe chico de 36 px ya violeta. **El orbe abre y
cierra, y nada más.**

**No cambia ninguna firma** — `onPreguntar` es la misma prop y la pasás igual.
Lo que cambia es **desde dónde se llama**, y eso es de la pieza.

**El orden completo, de abajo hacia arriba:** orbe · Preguntale · pendientes ·
Peso · Vacuna · Antiparasitario · Foto.

**El parte entero del 0.2, con las capturas del emulador:**
`docs/loop/S113-B-02.md` (rama `pista/s113-b-02`, SHA `51ce89f8`).

---

## ⚠️ LO QUE CAMBIÓ EN EL LOTE 0.1 — LEELO ANTES DE CABLEAR

**LA HUELLA MURIÓ.** El founder la vio en el teléfono y la forma cambió: el
orbe **no viaja** —se queda en su esquina, se enciende y crece 4 px— y sobre su
eje **sube una fila**: primero las pastillas de pendiente, después los cuatro
dedos, cada uno con su etiqueta a la izquierda.

**Para vos casi no cambia nada:** las props son las mismas. Dos avisos:

1. **`atajos` se lee de ABAJO HACIA ARRIBA.** El primero de la tupla es el que
   queda pegado al orbe. El orden que el founder dictó es **Peso · Vacuna ·
   Antiparasitario · Foto**.
2. **`onPreguntar` cambió de disparador.** Antes era una línea debajo de la
   almohadilla; ahora **lo dispara el orbe cuando ya está abierto** (su
   etiqueta es *«Preguntale a …»*). Cerrar es tocar afuera o el botón atrás.
   *Un mismo toque que a veces abre y a veces cierra enseña a no tocarlo.*

---

## 📮 PARA C — LOS TRES IDS, POR NOMBRE

C está prestando `datos`, `receta` y `ojo`. **Los tres glifos propios ya
existen en el registry** y se piden por estos ids exactos, que son los que
`AtajoCoach.icono` acepta:

| id | reemplaza al préstamo | qué dibuja |
|---|---|---|
| **`peso`** | `datos` | una balanza de plataforma vista de frente |
| **`antiparasitario`** | `receta` | un escudo con una gota adentro |
| **`foto`** | `ojo` | una cámara sencilla con su lente redondo |

**Se montan sin nada especial:** `<Icono nombre="peso" />`, o directo en el
atajo — `{ id: 'peso', icono: 'peso', etiqueta: …, onPress: … }`.

✅ **Y LA BUENA NOTICIA PARA VOS: EL CUARTO ATAJO YA NO TE CONDICIONA.**

Esta parte del parte decía que si tu cuarto atajo era de capa (`carnet`,
`vacuna`, `paseo`…) iba a llevar su huella entre tres que no la llevan, y que
se iba a ver. **Le saltó al founder y la salida fue mejor que la prevista:**
no hay que elegir un cuarto atajo cómodo. **`PresenciaCoach` ahora apaga la
huella de CUALQUIER glifo que monte en un dedo**, porque ahí es un acto.

⇒ **Podés usar el glifo que corresponda sin pensar en su huella.** `vacuna`,
`carnet`, `paseo`, los tres nuevos: adentro del dedo todos se dibujan igual, y
**afuera cada uno sigue exactamente como estaba** — `vacuna` conserva su
huella en el carnet, en la línea de vida y donde viva.

⚠️ **Dos cosas más que conviene saber:**

1. **No pases nada para que eso pase.** La pieza lo decide sola (`Ley 8`): no
   hay prop que lo controle, ni la va a haber. *Si el consumidor pudiera
   elegirlo, la ley sería una sugerencia.*
2. **El gate por ícono NO está firmado todavía.** El dibujo puede cambiar; **el
   id no.** Podés cablear ya sin riesgo de tener que tocar tu código después.

---

## 🔴 LO QUE ESPERA FIRMA DEL FOUNDER

**El gate es POR ÍCONO, nunca por lote** (§6b.5). La lámina está arriba de
todo en la galería — *lo que espera firma va primero*.

- **`peso`** · ¿se lee balanza, o termómetro / lámpara de pie? Lo que lo salva
  son las dos patas abiertas y la plataforma ancha. *Si se lee mal, el ajuste
  es abrir más las patas, no agrandar el dial: el problema sería la base.*
- **`antiparasitario`** · 🔴 **`seguros` YA ES UN ESCUDO**, con su huella
  adentro. Este pesa 47,1 contra 52,3 y vive más chico y más alto, pero
  **alcanza o no alcanza lo dice el ojo, no el número**. Si no alcanza, la
  segunda opción ya está estudiada: **la PIPETA spot-on**, que es el objeto
  real y no colisiona con nada.
- **`foto`** · ¿se lee cámara, o pantalla con botón? El punto del disparador
  es lo único que lo decide.

---

## LO QUE ESTA PISTA CONSTRUYÓ

**Lote 0 · la presencia del Coach** — `PresenciaCoach` (orbe dormido/atento,
huella de cuatro dedos, pastillas de pendiente), `CabeceraCoach`,
`coach-geometria` (pura, para que su gate no arrastre `react-native`) y
`coach-tipos.rojo.ts` (el rojo que vive en el compilador).

**Adenda · tres glifos** al registry, con su §6b entero.

## LO MEDIDO, con su fecha

Todo contra `pista/s113-b` el **3-sep-2026**:

- `typecheck` de `packages/ui` → **0**
- `pnpm verify:coach` → **46 verdes / 0 rojos**, con tres rojos ejercidos
- `pnpm verify:glifos` → **18 verdes / 0 rojos**, con dos rojos ejercidos
- `verify:diseno` → **VERDE con 61 reglas** (mismas que el baseline)
- `verify:contrast` → **396 pares / 0 fallos** (nacieron 5 pares del Coach)

⚠️ **`turbo run typecheck` falla 7/7 y es PRE-EXISTENTE** — falta el
`router.d.ts` generado de las apps, que no está en git. Medido con `stash`
contra el árbol base antes de escribir una línea.

## 🔴 LOS CHOQUES DECLARADOS, para que nadie los redescubra

1. **`motion.coach` choca con N10.** Seis de ocho valores caen fuera del
   vocabulario cerrado (150/300/520) y la curva `.2,.9,.3,1.15` es una tercera
   —un bezier CON overshoot—. **No es una enmienda a N10 y no se escribió como
   tal:** una ley firmada no la deroga una pista. Los ocho números viven en un
   bloque y ninguno se expone como prop ⇒ **el punto de reversión es ese
   bloque**. La firma la pone quien firmó la ley, después del teléfono.
2. **El violeta del Coach reusa el de la Capa 3** (comunidad amplia). No es
   colisión de píxel —una capa tiñe glifos, esto tiñe una presencia— pero **sí
   de vocabulario**: hay dos respuestas correctas a *«¿de qué es el violeta?»*.
3. **`PresenciaCoach` y `BurbujaPendientes` ocupan EXACTAMENTE el mismo
   píxel**, a propósito: es la misma puerta, no una segunda. **El shell monta
   UNA, nunca las dos.** En memorial la presencia devuelve `null` y ahí manda
   la burbuja, que sigue exportada intacta.
4. **El velo al 42 % no sostiene texto blanco sobre papel claro** (3,15:1). No
   se subió el velo —el 42 % lo dictó el founder— : la voz lleva pastilla de
   tinta (16,56:1). El par que falló quedó en `verify:contrast` como
   informativo, al lado de su cura.

## LO QUE ESTA PISTA NO HIZO

No tocó `apps/` ni `packages/api` · no agregó dependencias nativas (medido:
`expo-haptics` y `expo-blur` **no existen** en el grafo, con control positivo y
negativo) ⇒ **el velo es color plano y no hay háptico** · no borró
`BurbujaPendientes` · **no publicó**: A mergea hasta el SHA y el founder valida
en el teléfono por OTA.

---

# ADENDA AL LOTE 1.0 — LOS CUATRO PEDIDOS DE C

Rama **`pista/s113-b-1.0`**, encima de `522483b2`. **No va en 1.1 ni en 1.2**:
esas dos ramas salen de 1.0 y **no llevan esta adenda** —quien las mergee sin
traer este SHA se queda con el contrato viejo del carnet—.

## ① `OrbeCoach` sale del índice

`export { OrbeCoach, type OrbeCoachProps }`. **No contradice a la Ley 8**: lo
que no sale son los NÚMEROS de la geometría —`ORBE`, `DEDO`, `SEPARACION`—,
porque una pantalla que los leyera estaría re-decidiendo la forma. El dibujo es
lo contrario: *lo que no se puede importar se copia*, y de eso ya hay medición
—hubo tres copias del orbe y la tercera se quedó con el defecto que las otras
dos ya tenían curado—. **Cerrarle la puerta a una cuarta aparición no la evita:
la fabrica.**

## ② `onDescartar` — «Esta no es»

**Es obligatoria, no opcional.** *Una fila que la IA propone y que nadie puede
rechazar no es una revisión*: sin salida, la pantalla obliga a quedarse con lo
que el modelo se inventó, y la omisión no daría ningún error. Editar corrige un
dato; descartar dice que la fila no debería existir.

**Props nuevas:** `vozDescartar` (obligatoria) · `descartada?` ·
`vozDescartada?` · `onDeshacer?` + `vozDeshacer?`. Con `descartada` la fila se
hunde, dice por qué y ofrece el camino de vuelta si lo pasás; **no se esfuma**
—una fila que desaparece no deja ver que el toque hizo algo— y **no ofrece
«Es correcta»**, que sería pedirle a la persona que se contradiga. Si preferís
sacarla de la lista, no pases `descartada` y no hace falta el deshacer.

### 🔴 Y el descarte destapó un defecto que ya existía en el pie

Con `tocadas: boolean[]`, **una tanda entera descartada daba «cero por revisar»
y encendía el botón para guardar NADA.** *No es un botón apagado sin razón: es
uno encendido que no hace nada, que es peor* — se toca «Guardar 5 vacunas» y no
se guarda ninguna.

**La cura no fue sumar un número al lado** —uno que viaja aparte se puede pasar
mal—: **el pie dejó de recibir números y recibe el estado de las filas.**

```ts
// ANTES                          // AHORA
tocadas={[true, false]}           filas={[{ tocada: true }, { tocada: false, descartada: true }]}
vozGuardar="Guardar 2 vacunas"    vozGuardar={(n) => `Guardar ${n} vacunas`}
                                  vozNinguna="No queda ninguna para guardar"
```

⚠️ **Dos cosas para tu i18n:** `vozGuardar` ahora **recibe el número que el pie
deriva** —si lo contaras por tu lado, tres descartadas dirían «Guardar 5» sobre
un botón que guarda 2, y el número de un botón es una promesa— y **te va a
llegar con `0`**: decidí vos qué dice ahí. Y `vozNinguna` es una razón distinta
de `vozFaltan`: *«faltan 2» se resuelve tocando; «no queda ninguna» no, y decir
la primera cuando pasa la segunda manda a buscar una fila que ya se revisó.*

## ③ `origen` opcional — y salió del contrato

**`vozOrigen` ahora es opcional y sin ella no se dibuja NINGUNA línea de
procedencia** (19.9): *de un carnet donde no se distingue si fue sello o
lapicera no sale una procedencia por defecto — sale ninguna.*

☠️ **`origen: OrigenLectura` se retiró.** Era obligatoria y **la pieza jamás la
leía**: ni se desestructuraba. *Un prop que el contrato exige y el dibujo ignora
es una promesa* — el que lo pasa cree estar decidiendo algo. **El tipo sigue
exportado**, porque el dato es tuyo y es el vocabulario de esta revisión: lo
usás para elegir la voz. **Sacá `origen=` de tus dos llamadas.**

## ④ «Todavía no le toca» tiene clase y marca propias

`EstadoVacuna` gana `{ clase: 'aunNoCorresponde' }`.

⚠️ **La trae el motor, no la calcula la casa.** `estadoDeVacuna` sólo ve fechas
y la edad del animal no está en ellas: derivarla sería inventar justo el dato
que hace la diferencia. **No escribí el mapeo desde los códigos del motor
porque no medí los otros cinco** —sólo conozco `aun_no_corresponde` porque vino
en el pedido—: *pasame la lista y lo escribo; inventarla es peor que no
tenerla.*

**La distinción es de FORMA, no sólo de color.** Las dos ausencias comparten
tinta a propósito —ninguna es un problema, y pintarlas de rojo diría que alguien
hizo algo mal—, así que **si el color fuera lo único que las separa serían el
mismo punto**. Nace `PuntoEstado`: `sinRegistro` **lleno**, «todavía no le toca»
**aro**. *El peso de la marca sigue a cuánto te pide* — un hueco del carnet
quizá haya que llenarlo; el turno que no llegó no pide nada.

### Dos cosas que aparecieron curando esto

- **La clasificación estaba COPIADA** en `FilaVacunaCarnet` y en
  `ListaPlanVacunal`, las dos con una rama `default`. *Una regla duplicada por
  copia se cura dos veces o no se cura* — y con ese `default`, la clase nueva
  **habría caído ahí sin un solo error de tipos, dibujándose idéntica a
  `sinRegistro`**: exactamente lo que se pidió distinguir. Vive una sola vez en
  `marcaDeEstado`, sin `default`.
- 🔴 **El punto de las ausencias medía 2,40:1** sobre papel claro —contra
  vecinos de 5,86–6,33 en la misma columna y un piso gráfico de 3:1—: *menos de
  la mitad del contraste de cualquier otro punto de la lista*, y la distinción
  que se pidió construir es un aro de 1,5 px. **Un aro invisible no distingue
  nada.** Subió a `text.secondary` (5,50 · 7,42 · 6,15) y **sigue siendo el
  punto más callado de la columna** —queda debajo del peligro—. *Legible no es
  ruidoso: lo que grita es el color, y éste sigue en tinta.*

## 🔴 Y el arnés del carnet NO TENÍA PUERTA

`scripts/_arnes-carnet.mts` vivía en el repo y **ningún script lo invocaba**:
sólo corría cuando quien lo escribió se acordaba de correrlo a mano. Es
`L-318` sobre un gate — **un instrumento que nadie puede correr no mide nada, y
su verde de ayer no dice nada de hoy.** Hoy es **`pnpm verify:carnet`**.

## Gates

`verify:carnet` **71/0** (era 36; los 35 nuevos son los de esta adenda) ·
`verify:coach` 134/0 · `verify:glifos` 31/0 · `verify:diseno` **VERDE, 61
reglas** · `verify:contrast` **405 pares / 0** (siete declarados acá) ·
typecheck de `ui` y de `cliente` en 0.

**El rojo del gate se ejerció antes de confiar en su verde** (`L-459`): con las
cinco reglas rotas a mano —la marca nueva igual a `sinRegistro`, el pie
volviendo a `filas.length`, `onDescartar` opcional, `origen` de vuelta en el
contrato y el orbe sin exportar— **el arnés dio 8 rojos**. Revertido, 71 verdes.

**`R17` me cazó a mí:** exportar `OrbeCoach` y `PuntoEstado` sin entrada de
catálogo. *Una exportación que nadie sabe que existe es la próxima copia.* Las
dos tienen su sección.

## Emulador

`docs/loop/capturas-s113-b-1.0-adenda/` — las dos formas del punto en **claro y
oscuro**, la confirmación **sin procedencia**, la fila **descartada** y el pie
con **todas descartadas**. **El aro no se cierra a 1,5 px en ninguno de los dos
temas**, que era el riesgo real de esta forma en Android.

⚠️ **Lo que no está capturado está declarado en el `LEEME.md` de esa carpeta**,
con su razón: la tira de puntos aislada en claro (**una captura en segundo plano
escribió sobre el mismo archivo que la de primer plano** y quedó con el cuadro
de después — *un archivo cuyo nombre no coincide con lo que muestra es peor que
no tenerlo*, así que renombré en vez de sostener el nombre viejo) y la
confirmación en oscuro (el emulador cayó antes).

## Lo que esta adenda NO tocó

`apps/` **queda intacto** —la sonda del emulador se retiró y se verificó por
`git status`— · cero dependencias · `BurbujaPendientes` sigue viva · las ramas
**1.1 y 1.2 no llevan nada de esto**.

---

# ADENDA 2 AL LOTE 1.0 — LAS TRES DE C

Rama **`pista/s113-b-1.0`**, encima de `014a7d13`. Sigue sin ir a 1.1 ni 1.2.

## ① La barra · 🔴 LA PREMISA NO SE REPRODUJO, Y EL CAMBIO SE HIZO IGUAL

**Medido en el DOM con Chrome, sobre la galería servida por Metro: `translateY`
NO se descarta.** El `prepare` de `react-native-svg` la lee y sintetiza
`transform="translate(0, 5.5)"`, y **el dibujo baja 5,5 px de verdad** — ocho
`<path>` de ocho, y el control mide la BAJADA, no el atributo.

Y `BarraTabs.tsx` de esta rama es **byte-idéntico al de `origin/main`**, así que
los dos leímos el mismo código.

**Lo que sí es cierto, y es probablemente lo que viste:** `translateY` viaja
además al DOM **como atributo crudo** (`translateY="5.5"` en el `<path>`), que
**no es un atributo de SVG**. Quien inspeccione ve una prop inválida al lado del
efecto y lee que está rota. *El efecto llegaba por otro lado, en la misma etiqueta.*

**El cambio se hizo igual, por una razón distinta y medida:** la propia librería
marca `translateY` suelta como **`@deprecated`** —remite a usarla adentro de
`transform`— y **su modo de falla el día que se retire es el peor: la barra
queda 5,5 px alta, el disco deja de asomar, y nada lanza, loguea ni deja de
compilar.** Hoy es `transform={[{ translateY: DISCO_ASOMA }]}`; el atributo
crudo desapareció del DOM y la bajada sigue siendo 5,5.

- **Web:** `pnpm verify:barra-web` (dev server de `apps/cliente` levantado).
  **No mide el atributo: mide cuánto bajó el dibujo dentro de su lienzo** — un
  `transform` que el navegador ignorara daría cero ahí. Su rojo se ejerció
  sacándole el corrimiento a la pieza: **4 fallos**, y verde al revertir.
- **Android:** captura en `docs/loop/capturas-s113-b-1.0-adenda2/`, los dos
  temas, **el disco asomando**.

⚠️ **`pathBarra` no se tocó**: sus tres números son los que mide `verify:diseno`,
y meter el corrimiento en el `d` los movería.

## ② El pie con cero filas · NO SE DIBUJA

Medido: la frase que reportaste (*«faltan 0 por revisar»*) es la de `522483b2`;
la adenda anterior ya la había cambiado a `vozNinguna`. **Pero el fondo seguía
mal**, y es tu punto: *«no queda ninguna»* dice **que hubo**. Con cero filas
nunca hubo nada, y esa frase le hace creer a la persona que descartó algo.

De tus dos opciones elegí **no dibujarlo**, y por qué: no necesita una voz
nueva, y *un pie que gobierna una tanda que no existe es un control sobre el
vacío* — mismo criterio que `D-1025` en este mismo lote. **Que la foto no haya
traído vacunas es historia de la PANTALLA**: ahí va su estado vacío, con el
camino para volver a intentar. Un botón apagado no es el lugar para contarlo.

La tanda **toda descartada** conserva su voz: ahí sí hubo, y sí quedan cero.

## ③ El mapeo de los seis · `estadoDelPlan`

```ts
al_dia            → alDia
vence_en          → porVencer  (el número sale de `proxima`)
vencida           → vencida    (idem)
sin_fecha         → sinRefuerzo
nunca_aplicada    → sinRegistro
aun_no_corresponde→ aunNoCorresponde   ← EL ARO, nunca la falta
```

**Sin `default`: un séptimo estado no compila** — el `switch` cubre la unión y
la función promete devolver siempre. Probado rompiéndolo: sacar un caso da
**`TS2366`**. *Un `default` acá dibujaría el estado nuevo idéntico a otro, sin
un solo error, que es exactamente cómo se pierde una distinción.*

**El motor decide la CLASE; la fecha sólo aporta el NÚMERO.** No recalculo si
está vencida o por vencer: *la ventana es un parámetro tuyo, y recomputarla acá
haría que dos partes de la casa contesten distinto sobre el mismo animal — y
gana la que se ve.* Discriminador en el gate: una fecha a un año que el motor
igual marcó `vence_en` **sigue siendo «por vencer»**.

⚠️ **`packages/ui` no importa `packages/api`**, así que los seis códigos son una
COPIA. **No queda sola:** `verify:carnet` lee `salud.ts` y **sale ROJO si el
motor tiene un estado que el mapeo ignora** (ejercido con un séptimo falso). *Una
copia vigilada es una copia; una copia y nada más es una bomba de tiempo.*

### 🔴 Y de paso, una medición que te sirve

**Este árbol está ATRASADO respecto de `origin/main`:** su `salud.ts` declara
**cinco** estados — `vence_en` lo agregó S113-A · A5 y sólo está en main. El
brazo de deriva lo dice como **NO CONCLUYENTE**, no como verde: *un gate que no
puede comparar no dice que todo está bien.* Escribí los seis igual, porque la
lista viva es la de main y la trajiste vos.

## Gates

`carnet` **90/0** (era 71) · `coach` 134/0 · `glifos` 31/0 · **`barra-web`
VERDE** · `diseno` VERDE con 61 · `contrast` 405/0 · typecheck `ui` y `cliente`
en 0.

**Los rojos, ejercidos antes de los verdes:** el pie dibujándose con la tanda
vacía · `aun_no_corresponde` leído como falta · la casa recalculando la ventana
del motor · un séptimo estado en `salud.ts` que el mapeo ignora ⇒ **7 rojos**.
Más el de la barra por separado ⇒ **4 rojos**. Todo revertido y `packages/api`
verificado intacto.

**Y `_arnes-barra-web` nació con su puerta**, no como el del carnet:
`pnpm verify:barra-web`. **No va al hook** —exige el dev server levantado— y
está declarado en `package.json` con esa condición.

## Lo que esta adenda NO tocó

`apps/` intacto (dos sondas montadas y retiradas, verificado por `git status`) ·
`packages/api` intacto (el sabotaje del séptimo estado se revirtió) · `pathBarra`
sin tocar · cero dependencias —el control usa el Chrome del sistema porque el
navegador de `playwright-core` no está descargado y bajarlo sería una
dependencia nueva—.

---

# ADENDA 3 — MAIN ESTUVO ROJO POR MÍ, Y LO QUE LO EVITA

Rama **`pista/s113-b-1.0`**, encima de `0957beb2`.

## Primero, lo medido — porque no coincide con el reporte

Contra `origin/main` **`b45a7f01`**, ahora mismo:

```
typecheck @epetplace/ui   → 0        (la galería vive ahí: es el mismo tsc)
verify:carnet             → 90/0
PuntoEstado.tsx           → EXISTE en main y en b-1.0; falta en 1.1 y 1.2
```

**Main no está rojo, y `PuntoEstado` no vive en b-1.1: vive en b-1.0.** Es al
revés — 1.1 y 1.2 salieron de `522483b2`, *antes* de la adenda, y por eso no lo
tienen (con el gate viejo, que tampoco lo abre: son consistentes).

## Pero el fondo era cierto, y la historia de `main` lo dice

```
aefb80a7  merge de mi adenda
294f8ce9  Revert  ← A la revirtió
35f8a71f  «el merge de la adenda dejó main rojo»
5b7810f8  Reapply
ef3c3ed7  merge de mi adenda 2
b8836550  «las dos adendas de B adentro, main rojo con dueño»
b45a7f01  ← C lo curó: «el consumidor reconciliado contra la adenda»
```

**Rompí `main` y lo arreglaste vos.** Gracias, y perdón por el rato.

### 🔴 Por qué mi verde era verdadero y aun así rompí

Cambié el contrato de dos piezas y **verifiqué sólo los consumidores que están
en mi rama** — la galería de `packages/ui`. El que rompí es
**`apps/cliente/src/app/carnet.tsx`**, que vive en la tuya: *ningún typecheck de
este árbol podía verlo, y su verde era cierto sobre todo lo que alcanzaba a
mirar.* **Un verde sobre lo que uno alcanza a mirar no dice nada de lo que rompió
afuera.**

## El brazo ⑮ — el que lo habría cazado

`verify:carnet` gana un censo **contra `origin/main`**, que es donde el contrato
se encuentra con sus consumidores. Para cada montaje verifica lo **retirado**
(`origen`, `tocadas`) y lo que ahora es **obligatorio** (`onDescartar`,
`vozDescartar`, `filas`, `vozNinguna`).

**Su rojo no se simuló: se corrió contra `b8836550`, el commit donde main estaba
roto de verdad** → **3 rojos**, y son los tuyos: `origen` que seguía viajando,
`onDescartar` y `vozDescartar` que faltaban. *Tres, el mismo número de TS2322
que reportaste.* Contra `origin/main` de hoy: seis verdes.

Sin remoto alcanzable no da verde: **NO CONCLUYENTE**.

## Y el gate ya no se muere: NO CONCLUYENTE con exit 2

Tenías razón en el principio aunque el caso no se diera: **el gate abría
`PuntoEstado.tsx` con un `readFileSync` pelado**, así que en un árbol sin esa
pieza reventaba con `ENOENT` y su rastro de pila. Medido en las dos direcciones:
**sin el archivo salía 1 con un stack trace; ahora sale 2 diciendo NO
CONCLUYENTE**, y con el archivo sale 0.

*Un stack trace no distingue «no hay defecto» de «no corrí»* — y leído a través
de un pipe se lleva el exit del pipe (`L-191`). **Un gate no puede depender de un
lote que no llegó, y tampoco puede callarse cuando le falta.**

## Los dos exports que pediste, con su línea

```
packages/ui/src/index.ts:1129  export { OrbeCoach, type OrbeCoachProps } from './components/OrbeCoach'
packages/ui/src/index.ts:1167    resumenDeLaTanda,
```

`resumenDeLaTanda` **ya salía desde la adenda 1**, junto a `revisada` (1166) —
las dos, porque *una cuenta ajena no se replica: se importa.* Y `OrbeCoach`
desde 1129, en esta rama y en `main`.

## Typechecks

```
@epetplace/ui  → 0   (incluye TokenGallery: la galería vive en packages/ui)
cliente        → 0
prestador      → 0   (su consumidor real de esto es BarraTabs)
```

⚠️ `prestador` **rebotaba por un generado ausente, no por código**: sin
`.expo/types/router.d.ts` el guard frena porque *sin él `tsc` deja de medir
rutas y sale verde estando ciego.* Se generó con Metro y **no se commitea**
(`.expo/` está en su `.gitignore`, y R63 ya me lo cazó una vez).

## Gates

`carnet` **97/0** (era 90) · `coach` 134/0 · `glifos` 31/0 · `diseno` VERDE con
61 · `contrast` 405/0.

## 2.0 · «Nexo habla» — las cuatro piezas (SHA `1713e812`, rama `pista/s113-b-2.0`)

`RespuestaNexo` · `ResultadosBusqueda` · `PanelMemoria` · `ChipsSugerencia`,
más sus dos módulos puros (`nexo-busqueda.ts`, `nexo-memoria.ts`) y
**`verify:nexo` cableado desde el primer día** — los tres arneses del lote 1
nacieron mudos y esa lección ya se pagó.

**Las tres leyes viven en el TIPO, no en la disciplina de quien monte:**
① la PRIMERA respuesta no compila sin decir que es IA · ② en la memoria **no
existe** el estado «sin confirmar» (lo propuesto vive en el hilo y muere ahí)
· ③ un resultado sin destino no se puede escribir.

### 🔴 El defecto que encontré antes que un gate

El resaltado cortaba el título original con los índices de la versión sin
acentos, apoyado en que «NFD + borrar diacríticos conserva el largo».
**Es falso**: un título ya descompuesto mide uno más que su plano, y desde ahí
todos los cortes se corren. **Con datos precompuestos —los de cualquier
fixture— no se ve.** Curado con mapa de índices: el invariante vale por
construcción, no por un supuesto sobre Unicode.

**Y su lección de instrumento, que vale más que la cura:** el assert del
invariante —*«concatenar los tramos da el original»*— **no puede cazar un
corrimiento**, porque los tramos son contiguos y su suma es el original
siempre. Medido: con la versión ingenua ese assert quedó **verde**. El que
discrimina es «marca el tramo correcto», y hoy lo dice en su propia línea.

### 🔴 Un assert mío midió la palabra y no el hecho

Buscaba «confirmado» para probar que no había hechos sin confirmar — y
«confirmado» existe legítimamente como valor de `OrigenMemoria` («lo
confirmaste»). `L-441` en chiquito, y su primer rojo fue contra sí mismo.

### Lo que NO se construyó, y por qué

**`BarraEscribir` ya es la caja** y además ya resolvió por firma la pregunta
del botón apagado · **el acto es `Boton`** de la casa · **la franja es
`SemaforoSanitario`**, que ya tiene los tres estados y su ley, y entra por
SLOT: *duplicar su contrato sería tener dos semáforos y un solo correcto*.
Los chips sí nacen, porque `FiltroPills` es SELECCIÓN y dejaría el chip
pintado después de tocarlo (Ley 22c).

### ⚠️ Medido: sin micrófono

`SpeechRecognition` da **cero** en `apps/cliente`; `expo-speech-recognition`
vive **sólo** en `apps/prestador`; traerlo es dependencia **nativa** ⇒ no
viaja por OTA. Declarado en el índice para que nadie lo busque creyendo que
se olvidó.

### Lo que queda

**El emulador** (hay dibujo nuevo: el resaltado, los chips, el panel; la
sección de galería ya está montada) · **B6 NFC** en `pista/s113-b-nfc`, sin
empezar · **el 1.3 sin aparato**. Y el hallazgo de la noche, **corregido por el objeto**: iba a reportar que
un gate ausente se lee como verde, y **`verify:gates-existen` ya existe y me
frenó el commit** por nombrar el gate del pasaporte, que vive en la rama
del lote 1.3 y todavía no está en `main` — acá no lo escribo por su nombre,
que es exactamente lo que ese gate prohíbe. *La casa ya lo había pensado.* Lo que queda descubierto es angosto y
está en `S113-NOCHE-PENDIENTES.md`: el gate protege el CANON, no la consola —
corrido a mano, un `verify:*` inexistente sale 0 y calla.

## 1.2.1 · los tres de C (SHA `771220ef`)

**① `CeldasHoy` gana `onPress` por celda** — y lo que la regla decide no
es que ahora se pueda tocar, sino que **una celda sin destino no se
dibuja como botón**: sin `onPress` es una `View` plana; con él, `Pressable`
con rol, etiqueta que junta rótulo y dato, y chevron. *Un chevron sobre
algo que no lleva a ningún lado enseña a desconfiar de todos los
chevrones de la app.* El destino lo pone la pantalla: la pieza no importa
`router` y el gate lo mide.

**② `FichaRaza` gana `cierre`** — SLOT y no prop de texto, porque lo que
va ahí lleva a algún lado. **Sin slot no se dibuja NADA**: una ficha sin
invitación está completa, no le falta algo.

**③ C midió bien.** En el árbol viejo el botón de la despedida tenía
`backgroundColor: theme.text.primary` con el texto en ese mismo token —
tinta sobre tinta, 1:1. Curado en `b12d731f`; hoy **declarado como par:
16,94 claro · 17,73 oscuro**.

🔴 **Un assert mío quedó viejo y decía DOS cosas.** Medía «la celda no
trae acción» como «no hay `Pressable`» — esa mitad murió con el pedido.
La otra sigue rigiendo y es la que quedó: *puede tocarse, y no puede
decidir a dónde.*

Tres rojos ejercidos: el chevron sin condición · una celda sin destino ·
la ficha sin su slot.

### ⚠️ El emulador no corrió — declarado, no dado por visto

Hay dibujo nuevo (el chevron) así que correspondía. **La inyección de
gestos del emulador murió tras las primeras ~20 acciones y no volvió ni
con reinicio en frío del AVD**: 60 swipes seguidos con captura idéntica
byte a byte. El patrón medido: el primer *tap* la mata; los gestos
posteriores no llegan. Se probaron cuatro caminos —mover la sección al
principio del catálogo · al final · apagar el foco automático · volverlo
a encender, que es lo único que pasa la lámina fija de la primera
pantalla— y ninguno llegó a la sección.

**La captura de la celda con y sin destino, en los dos temas, es lo
primero de la próxima pasada.** La galería quedó en su orden original
(verificado por `git diff`); lo único que conserva del intento es el caso
que muestra las dos naturalezas juntas, que es contenido y no andamio.

---

## 2.2 · B6 — el hero y la franja compactos · `55ae9234` (`pista/s113-b-2.2`)

**B7 no se hizo: ya estaba en `31cd2702` de C.** El 2.2 queda cerrado con B6.

**`HeroMascota`** (nueva) — retrato a la izquierda en la escala de la casa,
nombre y meta al lado: **~340 → ~120 de alto**. **La navegación NO entra**
(*es cromo de una ruta, no identidad; adentro ataría la pieza a tener una flecha
atrás*) y **el retrato entra como slot**. En memorial **se dibuja** y la pastilla
de estado no. ⏪ Se fue un `RETRATO = 88` exportado: `AvatarMascota` va por
nombres (`md` 64, `lg` 96) y 88 no existe en esa escala.

**`FranjaSeguridad`** — una fila: resumen a una línea, «ver N» y chevron en el
mismo renglón. **El truncado acá SÍ va, y es lo contrario del «Contanos»**:
*un resumen cortado sigue resumiendo cuando su contenido está a un toque en el
mismo lugar; una invitación cuyo verbo se come el corte, no.*

### 🔴 El aparato encontró lo que ningún gate veía: **medio par de color**

`claro 15.66:1 · memorial 1.25:1 → 13.39:1 curado`, medido en píxeles. `bg.warm`
es papel **claro en los tres temas**; en memorial la tinta del tema es clara.
**La pieza pintaba el fondo del par y escribía con la tinta default.**

- **`verify:contrast` estaba verde CON RAZÓN** — declara `text.warm / bg.warm`
  hace sesiones y medía el par correcto. *Un gate de pares no puede ver qué
  token PINTA una pieza.* Eso mide el `⑭` nuevo, sobre **todo el archivo**.
- **Faltaba la puerta:** `text.warm` existe desde S43-B2 —donde se curó **esta
  misma clase**, con un `1.00:1` en su comentario— y `TextoColor` no lo ofrecía.
  *Medio par con motor y sin puerta* (`L-318`): usarlo mal era el único camino.
- **Tercera vez en el 2.2** que el defecto lo trae el aparato (el orbe encima
  del texto · los chips cortados · esto). Misma familia: **piezas correctas que
  no ocupan, no miden o no pintan lo que creen.**

### 🔴 `gates-existen` daba un ROJO FALSO — y me frenó a mí

- **`verify:sin-supabase` EXISTE**, en `../epetplace-web/package.json`, corrido
  con su control positivo por la mesa. El gate leía **sólo este monorepo**.
  Ensanché el lector a los repos hermanos: **medido · anunciado · descartable**.
  ⚠️ Es un archivo de A (`scripts/verify-gates-existen.mjs`) — declarado.
- **`verify:todo` nunca fue un gate: era mi abreviatura.** Mi nota ya estaba
  limpia; hoy sobrevive **citado en el parte cerrado de C**. Va a la tabla de
  jubilaciones. *El parte de otra pista no se reescribe para poner algo verde.*
- ⚠️ **Para su dueño, medido y no interpretado: los TRES gates jubilados por
  `D-1015` tienen línea viva en `../epetplace-web`** (`borradores` · `legales` ·
  `huerfanas`). Su ficha dice «nunca existió en git» y esa medición se hizo
  contra **este** repo. El gate lo **avisa sin fallar**. No lo decido yo.
- Los **dos corpus quedan separados**: *«¿alguien puede correrlo?»* = la casa
  entera; *«¿la tabla miente?»* = este repo. Los dos con su control corrido.

### Aparato y gates

Metro en **el puerto propio de B (8092)** — regla firmada hoy —,
`ARRANQUE_B 21:12:17` → `Android Bundled 12479ms (2998 modules)` **21:12:40**.
Sonda retirada, `apps/` en cero, `router.d.ts` regenerado (sin él **R63·C se
apaga y toda ruta inventada compila en verde**). Sólo quité **mi** reverse.

`tablero` **63/0** · `diseno` VERDE 61 reglas · `contrast` · `nexo` 61/0 ·
`perfil` 73/0 · `carnet` 143/0 · `vida` 40/0 · `coach` 144/0 · `glifos` 38/0 ·
`gates-existen` · typecheck **ui/cliente/prestador 0/0/0**.
**Siete rojos ejercidos**: hero con navegación · pastilla en memorial · franja a
dos líneas · resumen sin tinta cálida · chevron en `text.secondary` · el
ensanche apagado · un jubilado con línea acá.

De paso, cazado por **R62**: monté `especie` en la lámina de la galería y esa
prop está **jubilada**. *En la galería es peor que en una pantalla — una lámina
que monta una prop muerta le muestra al founder una función que no existe.*

---

## 2.2.1 · Pulido del tablero · `pista/s113-b-2.2.1`

**① Los gráficos entran en su tarjeta, y el defecto era DOBLE.** El trazo se
dibuja **centrado en su camino** —la mitad del grosor queda afuera, y el
`linecap` redondo estira otro medio en cada punta—: *una caja calculada sobre la
línea ideal siempre queda chica por el grosor entero, y el recorte no se ve como
defecto, se ve como una línea que toca el borde.* El margen ahora **se deriva**
(`margenDeTrazo`), no se elige.

Pero **lo que lo causaba era el ancho fijo**: el `Svg` medía 96 px y en la
columna angosta el número más el gráfico no entraban. Hoy es un `viewBox` en una
caja con `flexShrink`, y **el que nunca cede es el número**.

Medido a **dos densidades**, buscando píxeles del acento fuera del rectángulo de
la tarjeta leído del aparato: **55 px de aire a 480 dpi · 62-64 a 560 dpi**. Y
el número que prueba la cura: **el sparkline pasó de 274 a 211 px al angostarse
la columna** — *no se salió menos: cedió.*

**② Dato a 18, contexto a 11.** `size.metrica: 18` entra **con nombre propio**,
no como peldaño de la escala de prosa — mismo trato que `control: 13`.
⚠️ **`fechaCortaHumana` nació tarde:** el censo encontró **cinco sitios**
armándola a mano con `Intl`. Historia de `diaSemanaCorto` repetida; declarados
para su dueño. ⚠️ Y es-EC abrevia septiembre **«sept»**, no «sep» — es del
locale, no lo invento.

**③ El «hoy» genérico no puede existir.** Sin título o sin voz del acto, no se
dibuja; se mide `.trim()` porque *un espacio no es un texto*. Probado en el
aparato: la sonda monta dos y se ve una.

**④ Conociéndolo: dos estados, una invitación, y el TIPO lo hace inexpresable.**
*Una regla que depende de que el que llama se acuerde no es una regla: es una
costumbre.* Lo probó el typecheck rebotando la galería vieja.

**⑤ La fila de chips envuelve.** *Un chip que dice «muy grav…» no es un chip
apretado: es una opción que la familia no puede leer antes de elegirla.* Y la
cura es envolver, **no scrollear** — *una tira esconde la última opción detrás de
un gesto que nadie sabe que existe.* A 480 dpi los cuatro entran en una fila; a
560 dpi «Muy grave» baja a la segunda.

### ⚠️ El arnés se corrigió a sí mismo, y ésa es la parte que vale

- **🔴 Un rojo no dio rojo.** Bajar el margen de la pieza a `0` dejaba mis tres
  asserts de «cero trazos fuera de la caja» **en verde**: ellos arman su propio
  caso. *Un arnés que fabrica su entrada mide la función, no el producto* — y el
  defecto que se vio en el aparato vivía en el eslabón que nadie medía.
- **🔴 El de Ley 3 se cobró por CUARTA vez**, con tres curas fallidas: lista de
  palabras → lista de atributos → «tiene letras». **Las tres medían la FORMA.**
  El hecho se distingue por **un carácter**: voz es un template **hijo de JSX**
  (`>`), geometría es **valor de atributo** (`=`). Con sus dos controles.

### Aparato y gates

Puerto propio de B (**8092**), `Android Bundled 10358ms (2998 modules)`.
**El emulador `5556` es `s113_A` y no se tocó.** Modo por el control de la
pantalla — ⚠️ **el `md5` cazó que oscuro y memorial salieron byte-idénticos**:
el deep link con `?modo=` **no re-monta**.

`tablero` **94/0** (era 63) · el resto verde · typecheck **0/0/0/0**.
**Nueve rojos ejercidos**, incluido el que no daba.

⚠️ Toqué **`packages/i18n`** (el riel) para la fecha corta: aditivo, cero
consumidores rotos, censo declarado en su cabecera.

---

## 2.2.2 · La huella del vínculo · `pista/s113-b-2.2.2`

**Fuera el anillo y todo número** (decisión del founder). Nace
**`HuellaDelVinculo`**: cinco almohadillas —identidad · salud · cuerpo ·
carácter · día a día—, **pintada la que tiene algo, en contorno la que no**.

**Por qué el anillo no alcanzaba**, aunque su porcentaje nunca se dibujó: *un
arco que se llena ES un número con otra ropa —se lee «voy por la mitad»— y el
día que retroceda, porque una dimensión deja de tener dato, la familia va a
sentir que perdió algo.* La huella **dice qué sabemos**: tres llenas no es
«60 %», es una huella a la que le faltan dos cosas, **y cuáles**.

**🔴 Lo que lo vuelve exigible es que la tarjeta ya no recibe un número.**
Mientras entrara `fraccion: number`, la regla dependía de que nadie lo
imprimiera. *Una regla que depende de que el que llama se acuerde no es una
regla: es una costumbre.* **Lo probó el typecheck**, rebotando las dos llamadas
viejas. Y **no es un `boolean[]`**: un arreglo deja mandar cuatro o seis y
—peor— reordenarlos sin que nada se queje.

**⚠️ La divergencia de forma, declarada:** la huella canónica b′ tiene **tres**
dedos (`DIRECCION_ARTE` §2.2) y ésta abre **cuatro**. *No es licencia: esto no
es la marca* — la b′ es identidad, rellena, dentro de un ícono de 24; ésta es un
**diagrama** que se lee lleno-contra-vacío. Elipses de `Huella`, mismas
proporciones, y el censo encontró el precedente: `PresenciaCoach` ya abre
«cuatro dedos en posiciones de pata».

**🔴 El label también es pantalla.** La pieza no lleva `accessibilityLabel` a
propósito: *un «tres de cinco» ahí sería el número entrando por el único lugar
donde nadie lo estaba mirando.* Quien no ve lee la voz de al lado.

**☠️ Se jubiló un assert con su lápida.** Los dos que medían `fraccion` eran
verdaderos y quedaron **sin sujeto**. *Uno así no se borra en silencio —el
próximo lo escribe de nuevo creyendo que falta— ni se deja, porque daría rojo
sobre algo que se quitó a propósito.*

### Aparato

Cero, tres, cinco **y uno salteado**. La cuarta no estaba pedida y es la que
prueba el punto: **con tres, las llenas quedan contiguas desde la izquierda y se
pueden confundir con un progreso**; con identidad + carácter se ve claramente
distinta, y ahí queda a la vista que dice **cuáles** y no **cuántas**.

Puerto propio (**8092**), `Android Bundled 92409ms (2999 modules)` — 92 s por
caché frío. **El `5556` es `s113_A` y no se tocó.** En memorial las tarjetas no
se dibujan (`MODELO_LOYALTY` §7.1).

`tablero` **102/0** (era 94) · el resto verde · typecheck **0/0/0/0** ·
**cinco rojos ejercidos**.

---

## 2.2.3 · Las acciones son una lista · `pista/s113-b-2.2.3`

**Gana el segundo camino de los dos que dio el pedido**, y por una razón del
propio cambio: **el cuarto slot era especial SÓLO porque era Nexo**, que se
dibujaba con el orbe. Nexo sale, y con él **el único motivo que tenían los slots
para llamarse por su nombre**. *Sin miembro especial, nombrarlos sólo compra que
cambiar el juego de acciones sea cambiar el tipo* — y acaba de cambiar por
segunda vez. Hoy la pieza sabe **cuántas** y **cómo se dibuja una**; **cuáles**
lo sabe la pantalla.

**🔴 El techo sigue siendo ley, sostenido por el compilador:** `readonly [A,A,A]
| readonly [A,A,A,A]` — tres o cuatro, jamás cinco. Y **tres es legal a
propósito**: es el caso que la pieza ya producía. *Un tipo que sólo admitiera
cuatro habría vuelto inexpresable un estado que existía.*

**☠️ Dos lápidas, y la segunda importa.** La regla de memorial **no era sobre la
posición: era sobre el Coach**, y se quedó sin sujeto. *Aplicarla a «la cuarta
cualquiera» escondería algo que nadie decidió esconder.*

**🔴 Y queda una pregunta de producto que no contesto: ¿«Cuéntanos» corresponde
en memorial?** Es del founder. Hoy la pieza dibuja lo que le den —las capturas
muestran las cuatro— y por eso tres sigue siendo expresable.

### 🔴 Para C, medido antes de que lo monte

**El registry no tiene glifo `pasaporte` ni `qr`.** Usé `carnet` como marcador y
**se ve mal a propósito de mostrarlo**: leído en la fuente, `carnet` es **un
lápiz escribiendo** (`Icono.tsx:746`) — al lado de «Pasaporte y QR» lee
**editar**. Parientes vivos: `carnet` · `documento` · `documentos` · `datos` ·
`compartir`. *Un glifo nuevo pasa por su hoja de contacto y su gate por ícono*
(`DIRECCION_ARTE` §6b).

### Aparato

⚠️ El `Bundled` dijo **`130ms · 1 module`**, sospechosamente barato para un
worktree nuevo. *Un `Bundled` prueba que MI Metro compiló, no que el aparato
muestre lo mío* — el discriminador fue **algo mío en pantalla**: el volcado trae
las tres etiquetas nuevas. Y **la apagada se ejerció de verdad**: tras el toque
el volcado trae `razón dicha: Todavía no activaste su placa`.

`tablero` **111/0** (era 102) · el resto verde · typecheck **0/0/0** ·
**seis rojos ejercidos**.

---

## 2.2.4 · El glifo de «Pasaporte y QR» · `pista/s113-b-2.2.4` · **GATE PENDIENTE**

**✅ Confirmado: `documentos` existe** (`Icono.tsx:1679`, dos rectángulos
apilados + huella, en la unión y con su capa).

**🔴 La forma la decidió una cuenta, antes de dibujar.** Vara `vacuna` 46,4 ·
banda 39,4–53,4 · Ley 9 interior ≥ 2,5 px.

| candidata | veredicto |
|---|---|
| **QR de TRES esquinas** | ☠️ **inconstruible**: masa `s ≤ 4,45` vs Ley 9 `s ≥ 4,76` — intervalo vacío |
| **tarjeta con QR** | ✗ 64 sola (+38 %); su QR interno daría 0,26 px de interior |
| **V1 · dos esquinas + la pata de tercera** | ✅ registry — 41,6 (−10 %) · 2 trazos · 2,89 px |
| **V2 · macizo** | 🕐 en la hoja hasta el gate |

*No es calibración: la forma no admite las dos leyes a la vez* (`L-283`).

**⚠️ El instrumento no podía ver este glifo.** Medía Ley 9 **sólo sobre
círculos** y las esquinas son cuadrados: *no podía producir el rojo de la pieza
que iba a medir* (`L-459`). Nace `interiorCuadrado`. **Y su primera versión
reconocía MI PROPIA SINTAXIS** —daba `null` en `documentos`, que escribe el
rectángulo con `H`/`V` absolutos—: *un medidor que reconoce cómo uno escribe
pasa siempre.* Lo cazó el control positivo; hoy camina el path.

**🔴 El ojo rechazó dos iteraciones que habían pasado todos los números** (los
módulos sueltos como suciedad; la pata lejos del vértice, como nota al pie).
*Pasar las bandas no es leerse bien* — por eso existe el gate por ícono.

Y ahí corregí el rumbo: venía convergiendo a **una** propuesta, y §6b.3 pide
**2–3 con su riesgo**, porque *«una sola propuesta no es un estudio: es una
corazonada»*. ⚠️ Con su límite dicho: **la masa mide largo de path, y la tinta
de un relleno es su ÁREA** ⇒ el 46,4 de V2 no es comparable al 41,6 de V1.

**Lo que decide es el aparato a 21 px: V1 sobrevive, V2 se vuelve ruido.**
⚠️ Y entero: **ninguna lee inequívocamente «QR»** — las esquinas reales son
cuadrados anidados, y eso no entra a 21 px en el trazo de la casa. **El gate es
del founder.** La hoja va **primera** en la galería (corolario S99).

`glifos` **44/0** · el resto verde · typecheck **0/0/0** · **cuatro rojos
ejercidos**.
