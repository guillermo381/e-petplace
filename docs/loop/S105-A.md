# S105-A · BITÁCORA DE LA PISTA A

> **Mesa 106 en la numeración del founder · S105 en la bitácora.**
> Pista A (conducción · DB · `packages/api` · `docs/` · merges/push/OTA).
> Abre 24-ago-2026. Worktree `e-petplace-s105-a`, rama `pista/s105-a`.

---

## TURNO 0 — SIN CÓDIGO

### 0.1 · EL NÚMERO, MEDIDO Y NO ELEGIDO

El founder numera esta mesa **106**. La bitácora asigna **S105**, y no es un
ajuste: es lo que el objeto dice.

| pregunta | medido contra | respuesta |
|---|---|---|
| ¿Hay bitácora S105? | `docs/loop/` | **no** — la última es `S104-A.md` |
| ¿Hay acta de S104? | `docs/actas/` | **sí** — `2026-08-24-s104-ACTA-CIERRE.md` |
| ¿Quién depositó el `PLAN_MESA_105`? | `git log -- docs/PLAN_MESA_105.md` | **`0d0c2e32`, firmado `S104-A`** |

⇒ **La mesa que el founder llamó 105 corrió en bitácora como S104.** El desfase
es de una y viene de ahí. **No se renumera nada**: S104 se queda como está, esta
abre como **S105**, y las dos numeraciones se declaran juntas cada vez que se
nombre la sesión. *El desfase deja de ser un error el día que está escrito.*

### 0.2 · PASO ⓪ — leído del objeto, en el turno en que se usa

`main` == `origin/main` en **`5959650e`** (los dos `rev-parse` en el mismo
comando) · `git status --porcelain` en **0** antes de crear el worktree y **0**
después · rama `pista/s105-a` **libre** antes de crearla · **411 migraciones
locales**.

**No se copia acá ningún contador que envejezca.** Migraciones remotas, OTA,
numeración D/L/R: **su comando, no su cifra** — están en `S104-A-TRASPASO.md §4`.

⚠️ **Corolario de la regla 85 que rige y no se re-descubre** (enmienda S99): el
worktree aísla el árbol, **no necesariamente los paquetes**. A toca DB, `docs/` y
`packages/api`; si en algún momento toca `packages/ui`, se verifica a dónde
resuelve antes de concluir que una pieza «no anda».

---

## 🔴 0.3 · FRENO DECLARADO — `PLAN_MESA_106` NO LLEGÓ SU LITERAL

**El plan rige esta mesa y no existe en ninguna parte.** Medido: no está en
`docs/`, no está en ningún commit, y no vino pegado en el mensaje de apertura.
Lo que sí llegó es **una referencia a sus secciones** (§0, §1.1, §1.2, §1.3,
§1.4).

**No se compone de memoria y no se infiere de sus secciones.** El precedente de
la casa es explícito y se cobró varias veces (**freno 76b · L-142**): *una letra
que se anuncia y cuyo literal no viajó, se frena; nace cuando su letra llegue.*
Depositar un plan reconstruido —aunque acertara— lo volvería **un documento de
mesa que la mesa no escribió**, y esta casa ya midió lo que cuesta eso.

**Lo que sí se pudo reconstruir del objeto, y por eso se declara:** el §0 que el
prompt cita —***«no alcanza con mirar el color: hay que preguntar quién lo
produjo»***— **es literal de `S104-A.md` y de su traspaso §5**, donde nació con
sus once casos. Esa parte del plan **no hace falta pedirla: ya es canon**.

⇒ **PEDIDO AL FOUNDER: el texto completo del `PLAN_MESA_106`.** Mientras no
llegue, esta pista trabaja sobre lo que el mensaje de apertura sí ordenó
explícitamente, que es todo lo de abajo.

## 🔴 0.4 · SEGUNDO FRENO — LAS MEDICIONES DE D (§1.1 y §1.2) NO EXISTEN TODAVÍA

**No es que no me llegaran: no hay dónde leerlas.** Medido: cero worktrees de
esta mesa además del mío, cero ramas `s105-*` fuera de `pista/s105-a`, cero
archivos nuevos en `docs/loop/`. **D no ha depositado porque D no ha abierto.**

⇒ **El punto 4 del encargo —contrastar §1.1 y §1.2— queda ABIERTO por ausencia
de su insumo, no por falta de trabajo.** El Checkpoint 1 se arma con los dos
huecos **nombrados**, y se completa en cuanto D deposite. *Un checkpoint que
rellena el hueco de otra pista con una suposición es peor que uno incompleto.*

---

## §1.3 · MEDICIÓN — NUVEI

> **Contra qué objeto se midió** (la declaración es parte de la medición):
> **la base de producción linkeada** (`zyltipqscdsdsxnjclhp`) vía
> `supabase db query --linked`, 24-ago-2026 · `cron.job` y `pg_proc` de esa misma
> base · el árbol en `5959650e` para `supabase/functions/` ·
> `docs/LETRA_MOTOR_PAGOS_S101.md` para lo que la certificación exige.
> **Todo lo de abajo es del objeto vivo. Nada viene de un reporte previo.**

### A · Qué exige la certificación

`LETRA_MOTOR_PAGOS_S101` §11 tiene la tabla de ejecución. Su **paso 9** es el que
manda acá: **«el correo de certificación con DF + código de autorización»**, y
figura **`pendiente`**. §5 lo respalda: `authorization_code` es *«requisito del
correo de certificación»*.

### B · Qué corre hoy

**`pagos_intentos` — 44 filas.** Y el primer corte importante es **de quién son**:

| proveedor | filas | estados | `authorization_code` | `transaction_id` |
|---|---|---|---|---|
| **`nuvei`** | **30** | 25 aprobado · 3 pendiente · 2 rechazado | **28 de 30** | 28 de 30 |
| `seed_gate` | 9 | aprobado | 0 | 0 |
| `simulado` | 4 | aprobado | 0 | 0 |
| `siembra` | 1 | aprobado | 0 | 0 |

⇒ **14 de las 44 son sintéticas** y **ninguna** trae los dos códigos. *Un
recuento de «44 pagos» sobre esta tabla sería un número correcto contestando otra
pregunta.*

**✅ El dato que la certificación pide, EXISTE:** los **25 aprobados de Nuvei
traen `authorization_code` y `proveedor_transaction_id`, los 25.** Los rechazados
no traen ninguno de los dos, **que es lo correcto** — un rechazo no tiene código
de autorización, y que la tabla lo refleje es señal de salud, no de hueco.

**🔴 Y el corte que ordena todo lo demás: `forma` = `tokenizacion` en las 44
filas. Cero de cualquier otra.** Los estados existentes en la tabla son
**exactamente tres**: `aprobado`, `pendiente`, `rechazado`.
**`reverso_fallido` tiene CERO filas** pese a estar en el vocabulario
(`chk_hallazgo_vocabulario` admite `reversado_mismo_dia`).

> ### **Hoy Nuvei corre UN caso: tokenización. El reverso no es que falle — es que nunca corrió.**
> *Y la respuesta de DeUna de hoy dice que los reversos se pueden simular* ⇒ el
> caso ya no espera al proveedor. **`D-888` deja de estar bloqueada y pasa a estar
> sin dueño**, que es un problema distinto y se resuelve en la mesa, no midiendo.

**`webhook_events` — 65 filas, TODAS `proveedor=nuvei` y TODAS `ambiente=sandbox`.**

| resultado | n | `stoken_valido` | más reciente |
|---|---|---|---|
| `aplicado` | 23 | true | 21-ago 22:09 |
| `stoken_invalido` | 25 | false | 22-ago 14:40 |
| `desconocido` | 8 | null | 21-ago 06:46 |
| `recibido` | 7 | 6 true · 1 null | **23-ago 02:33** |
| `ilegible` | 2 | null | 19-ago 22:04 |

**La columna `verificado` es NULL en las 65, y eso NO es un defecto:** es la
columna que S103 agregó **para DeUna**, y DeUna **no está desplegada**. *Su
productor no existe todavía porque su proveedor tampoco.* Se declara para que
nadie la lea como hueco.

### 🔴 C · HALLAZGO H1 — TRES COBROS DE NUVEI QUE NO CERRARON, Y UNO ES POSTERIOR A LA CURA DE S103

| intento | monto | `transaction_id` | `authorization_code` | creado |
|---|---|---|---|---|
| `04512c6a` | **10,75** | `DF-2100043` | `UacVQL` | **23-ago 02:33** |
| `bdaf4f57` | 6,00 | `DF-2099049` | `4MBzFW` | 21-ago 03:37 |
| `dcf18373` | 70,90 | `DF-2099041` | `w9MeO3` | 21-ago 02:56 |

**Los tres están en `pendiente` con `motivo_rechazo` y `hallazgo` en NULL** — o
sea: no fallaron, **no se resolvieron**.

**Y sus webhooks SÍ llegaron, autenticados.** El de `DF-2100043` dice, literal:
`credencial=SERVER · autenticado=true · stoken_de=transaction.stoken`. Quedó en
**`recibido`** con **`pago_id = null`**.

**Lo que NO se afirma acá, a propósito:** *que el actuador esté roto.* El canon
dice que el buzón **persiste antes de analizar**, así que `recibido` puede ser el
estado legítimo de «guardado, todavía sin resolver». **El color está medido; quién
lo produjo, no.** Las tres preguntas que lo cierran —y ninguna es cara— son:
① ¿quién promueve `recibido` → `aplicado` y **corrió alguna vez** para estas
filas? · ② ¿los dos `pagos-conciliar` (jobs 14 y 15, activos) los alcanzan? ·
③ ¿por qué `pago_id` quedó nulo si el `dev_reference` del payload trae el uuid?

**Por qué importa igual antes de saber la causa:** `pagos_actuador_vivo = **true**`
(medido), o sea que la explicación fácil —«está apagado»— **está descartada**. Y
el más reciente es del **23-ago, posterior a la cura del actuador multiproveedor
de S103** ⇒ *no queda cubierto por «eso ya se arregló».*

**Y muerde la certificación por una razón concreta:** el paso 9 pide **demostrar
un caso con DF + código de autorización**. Los datos están; **el circuito de esos
tres no cerró**. *Un caso que no cierra no se puede presentar como caso.*

### D · El comprobante por correo — ✅ CORRE, con un límite declarado

`cat_notificacion_tipos` → **`pago_confirmado`**: `activo=true` ·
`canal_forzado='email'` · `ignora_techo=true` · `en_sombra=false`.
**25 intenciones: 20 `entregada` · 3 `diferida` · 2 `encolada`.**

**Sus `datos` traen las nueve claves, y entre ellas las dos que la certificación
pide:** `authorization_code` · `transaction_id` · `concepto` · `negocio` ·
`monto` · `moneda` · `titulo` · `mensaje` · `sujeto_id`.

⚠️ **Dos límites, declarados en vez de omitidos:**
1. **Que el dato esté en la intención no prueba que el correo lo imprima.** Eso
   vive en la plantilla de `despachar-correo` y **no lo medí**. *Es exactamente
   la clase de salto que §0 prohíbe.*
2. **La última entrega es del 21-ago 22:09.** Los tres cobros de H1 **no
   generaron comprobante**, lo cual es coherente con que no se aplicaron — pero
   lo hace síntoma del mismo hallazgo, no un segundo problema.

### E · El recurrente, de paso — sigue inerte POR DISEÑO

**`recurrente_vivo` NO EXISTE como clave en `app_config`** (leídas las 28 que
hay). El job **17 `cobrar-recurrencias` está activo** con `0 14 * * *` = **09:00
Guayaquil** ✓. ⇒ **cada tick devuelve `recurrente_apagado`.** *El cable está
tendido y la llave sigue siendo del founder,* tal como S103 lo dejó.

---

## §1.4 · MEDICIÓN — WHATSAPP

> **Contra qué objeto se midió:** la misma base viva (`cat_notificacion_canales`,
> `cat_notificacion_tipos`, `notificacion_intencion`, `cron.job`, `pg_proc`) ·
> `supabase secrets list` (**da nombres y digests, jamás valores**) ·
> `supabase/functions/despachar-whatsapp/index.ts` en `5959650e`.

### 🔴 EL PRODUCTOR DEL MAPEO TIPO-DE-AVISO → PLANTILLA **SIGUE SIN EXISTIR**, y se confirmó desde las dos puntas

**Por el lado del dato:** `notificacion_intencion` tiene **219 filas** y su
`resuelto_como` viene en **cuatro formas**, ninguna con las claves que la edge
necesita:

| claves de `resuelto_como` | filas |
|---|---|
| `canal_elegido · canales_habilitados · despacho · despacho_en · evaluado_en · gate_que_corto` | 110 |
| `… + proveedor_id` | 52 |
| `canal_elegido · canales_habilitados · evaluado_en · gate_que_corto` | 55 |
| `… + forzado_por` | 2 |

**Cero filas con `plantilla`. Cero con `plantilla_idioma`.** Y `canal_elegido` es
siempre `email`, `push` o `in_app`: **whatsapp nunca fue elegido, ni una vez.**

**Por el lado del código, que es la otra pregunta:** en `pg_proc` de `public`,
**cero funciones** cuyo nombre contenga `whatsapp` **o cuyo cuerpo mencione
`plantilla`**. *Las dos preguntas —«¿alguien lo escribió alguna vez?» y «¿existe
quien pueda escribirlo?»— dan la misma respuesta, que es lo que §0 pide antes de
declarar algo ausente.*

⚠️ **Matiz que vale registrar:** el grep sobre `supabase/migrations/*.sql` **sí**
encuentra la palabra en un `RAISE` de `20260805310000`. **`pg_proc` no.** ⇒ esa
función se redefinió después. **Gana el objeto vivo; la migración es historia.**

**Y la consecuencia es puerta sin motor, exactamente:** `despachar-whatsapp`
(312 líneas) lee `resuelto_como.plantilla` y `resuelto_como.plantilla_idioma`, y
si no están **marca la fila `fallida` con motivo `sin_plantilla_resuelta`**.
⇒ *el día que se encendiera, **todas** las filas caerían ahí.*

### El estado de vivo / cron / token

| pieza | estado medido | dónde |
|---|---|---|
| **`transporte_vivo`** | 🔴 **`false`** | `cat_notificacion_canales`, canal `whatsapp` (`es_piso=false`, `exige_evidencia=true`) |
| **tipos que lo usen** | 🔴 **CERO** tipos con `canal_forzado='whatsapp'` | `cat_notificacion_tipos` |
| **cron** | 🔴 **NO EXISTE** | los **18** jobs, leídos **con su `command`**: hay `despachar-push-tick`, `despachar-notificaciones-tick`, `despachar-invitacion-correo-tick` — **ninguno de whatsapp** |
| **token** | 🟡 **el secreto EXISTE** | `META_WHATSAPP_TOKEN` + `META_WABA_ID` + `META_PHONE_NUMBER_ID`, los tres del **08-ago** |

⚠️ **Sobre el token, con honestidad de instrumento:** `secrets list` devuelve
**digest, no valor** ⇒ **desde acá no se puede saber si es un token de Meta
válido.** El canon de S91 dejó escrito que la credencial de entonces *no parecía
un token de Meta* (largo 23, sin `EAA`). **Eso no se puede ni confirmar ni
desmentir con este instrumento, y por eso no se afirma.**
**Lo que sí lo contestaría sin encender nada:** la propia edge tiene un **modo
diagnóstico de solo-GET** (pide `message_templates` y el estado del número).
*Es la medición correcta y es barata; no se corrió en este turno porque toca
territorio de D.*

> ### **WhatsApp no está apagado por una llave: está apagado en CUATRO capas independientes.**
> Y **tres son configuración; la tercera es CONSTRUCCIÓN.** *Encender las otras
> tres sin construir el productor de plantilla no deja WhatsApp funcionando: deja
> cada aviso cayendo en `sin_plantilla_resuelta`* — el modo de falla que parece
> «lo prendimos y no anda».

---

## 🔴 §1.5 · HALLAZGO FUERA DE GUION — H2: EL CIERRE DE CUENTA ESTÁ ENCENDIDO Y LE FALTA SU RELOJ

**Nadie lo pidió; apareció midiendo `app_config` para otra cosa.**

| clave | valor medido |
|---|---|
| **`cierre_cuenta_vivo`** | 🔴 **`true`** |
| **`exportacion_datos_viva`** | 🔴 **`false`** |

**El traspaso de S104 dice, literal: *«el reloj nace INERTE — `app_config.cierre_cuenta_vivo`. La llave es del founder»*. Hoy está en `true`.**
No se juzga: **la llave es del founder y encenderla es su derecho.** Lo que se
declara es **lo que quedó del otro lado de esa llave**.

**① La función terminal existe y NINGÚN cron la llama.** `ejecutar_cierres_vencidos`
está en `pg_proc`. Los **18 jobs fueron leídos con su `command` completo** —no
por su nombre, que es la pregunta fácil que contesta otra cosa— y **ninguno la
invoca**. ⇒ **el acto ① (quitar acceso, reversible) corre por RPC del usuario; el
acto ② (día 30, terminal) no tiene quién lo dispare.**

**② `exportacion_datos_viva = false` mientras el cierre está vivo.** P15 promete
—y la pantalla lo dice— que **se le ofrece su copia antes de irse**. Con esta
combinación, **alguien puede cerrar su cuenta y no poder llevarse nada.**

> **Es la disciplina de la casa exactamente al revés: *el cable primero, la llave después*. Acá la llave está puesta y falta un cable.**

**Lo que esto NO es:** no es un defecto de código —las tres piezas están bien
construidas— ni una acusación. **Es un estado de configuración que la mesa tiene
que mirar hoy**, porque su modo de falla **no tiene síntoma hasta el día 30 de
alguien real**, y para entonces la ventana de arrepentimiento que §19.2 promete
**existiría en la tabla y no en la vida** — que es la misma frase con la que
S104 justificó separar los dos actos.

⇒ **Tres salidas, y las tres son del founder** (no se toma ninguna por
iniciativa): **(a)** apagar `cierre_cuenta_vivo` hasta que el cron exista ·
**(b)** crear el cron y encender la exportación · **(c)** dejarlo así
declarándolo, si el alcance de hoy es sólo el acto ① reversible.

---

---

# TANDA 1 — autorizada por el founder con las cuatro firmas

**Firmas recibidas:** ① rige **S105** · ② cierre de cuenta → **salida (a)** ·
③ ventana de reverso **POR RIEL** · ④ `holdVenceEn` nulo es **estado firmado**.

## ① ✅ EL RESCATE DE `pista/s103-d` A MAIN — cerrado y verificado

**Medido antes de tocar nada:** **no era ancestro** de `origin/main`, **16
commits** fuera, y **main no había tocado ninguno de sus paths** desde el
merge-base `29c7310f` ⇒ **rescate aditivo puro**. `merge-tree` limpio.

**Verificado por los TRES eslabones, no por el código de salida:**

| eslabón | cómo | resultado |
|---|---|---|
| es ancestro | `git merge-base --is-ancestor` | ✅ |
| **por CONTENIDO** | `git diff main..rama` en sus paths | ✅ **0 líneas** |
| **por SHA** | `rev-parse` vs `ls-remote` | ✅ `e2fe8801` = `e2fe8801` |

**Es `L-217` otra vez, y ahora con costo medido:** el riel entero de DeUna vivía
solo en una rama, y **D no podía desplegar desde el main de hoy**. *Un commit
pusheado a una rama de pista está en origin sin estar en el canon.*

## ② ✅ LOS TRES COBROS ATASCADOS — investigados, NO curados

**Resultado: la causa no era una, eran DOS, y el corte es la fecha.** Ficha
**`D-912`** con el detalle. El titular:

- **Los del 20-21 de agosto autentican HOY** (`credencial='SERVER'`,
  `stoken_valido=true`): quedaron sin aplicar porque **el actuador estaba muerto
  esos días** (hallazgo de S103) y **nadie los reprocesó después de curarlo**.
- 🔴 **El del 23-ago es OTRA causa, y es la viva.** `credencial = NULL` ⇒ la
  puerta lo rechaza. **Porque el sellador deriva la credencial del texto de
  `detalle` en el `BEFORE INSERT`, y el buzón persiste ANTES de analizar** — o
  sea que en el INSERT ese texto todavía no existe, y el trigger sella *«una vez
  y nunca más»*.

> **La cura de S103 movió CUÁNDO se congela el veredicto, no DE DÓNDE sale.**
> Cerró el modo de falla que buscaba y abrió el simétrico. **Y es 1 de 1: el del
> 23-ago es el ÚNICO tráfico posterior a la cura.**

**Dos preguntas mías que la medición DISOLVIÓ en vez de contestar** —y las anoto
porque es la mitad honesta del método—: **`pago_id` es NULL también en los 23
`aplicado`** ⇒ no discriminaba nada, la pregunta estaba mal formulada; y
**`dev_reference` sí apunta a `compra_id`** (4 de 4) ⇒ la llave nunca faltó.

## ③ ✅ LAS ENMIENDAS DE LA FIRMA 3

**`LETRA_DEUNA` enmendada en cinco lugares**, tachando y no borrando: §8 (título
y cuerpo) · §2 (la ruta de `refund`) · la vía manual · el dictamen de quién lo
construye · y **la firma ③ de §11, marcada `ENMENDADA` en vez de reescrita**.

🔴 **Y el hallazgo que la enmienda destapó, que no es de forma:** §8 decía
*«el proveedor lo confirmó por mensaje: **MISMO DÍA**»* — **el 22-ago**. El
**24-ago el mismo proveedor respondió 24 horas.**

> ### **No fue su documentación la que era ambigua: fue el proveedor el que dio dos respuestas distintas a la misma pregunta, con dos días de diferencia.**
> ⇒ **toda respuesta suya se cita con su fecha y su canal.** *«DeUna dijo» sin
> fecha ya significó dos cosas opuestas en esta misma sección.*

✅ **Efecto sobre `LETRA_MOTOR_PAGOS_S101` §5.0, medido y no supuesto:** funda las
compuertas pre-cobro en *«el reverso es mismo-día»*; para DeUna la ventana real
resultó **más ancha, no más angosta** ⇒ el motor venía siendo **más conservador
de lo necesario**. **No hay defecto que curar: hay margen que nadie usaba.**
**§5.0 no se tocó.**

**Nota a §6bis depositada:** su recuadro *«la letra y la superficie están
invertidas»* quedó **marcado como VENCIDO** — la fila de DeUna ya es la primera.
*Se conserva tachado: un «estado medido» sin fecha de vencimiento se sigue
leyendo como presente, y mandaría al próximo a arreglar lo que ya está* (`L-395`).

## ④ ✅ FIRMA 2 EJECUTADA — `cierre_cuenta_vivo = false`

**Verificado leyendo la clave después del `UPDATE`.** Ficha **`D-910`**.

🔴 **Lo que se midió ANTES de apagar, y es lo que hizo segura la salida (a):**
**`revertir_cierre_cuenta` NO lee la llave** ⇒ **apagar no encierra a nadie**.
*Si la reversión hubiera dependido de la misma llave, apagarla habría sido lo
contrario de lo que la firma quería.* `solicitar_` y `ejecutar_` sí la leen ⇒ no
entran solicitudes nuevas y el acto terminal queda con doble seguro.

⚠️ **Un cierre programado quedó vivo y NO se tocó** (no estaba firmado tocarlo):
`+test3`, programado para el **23-sep**, `banned_until = infinity`. **Es cuenta
de prueba del gate de S104-C.** *Hoy es inofensivo porque no hay cron; el día que
el reloj se encienda ejecutaría un cierre de prueba como si fuera real.* ⇒ **se
limpia o se revierte antes de encender el cron, y es del founder.**

## ⑤ EL PLAN, DEPOSITADO Y COTEJADO

`docs/PLAN_MESA_106.md`, **cuerpo verbatim**. **Cotejo pedido por el founder:
NO es el mismo documento que `PLAN_MESA_105`** — subtítulo, fecha, §0, §1 y los
frentes son distintos, **cero solapamiento**, y el 106 nombra al 105 solo como
herencia. *Están en secuencia.*

⚠️ **Una diferencia que no es de forma:** el 105 **declaraba pistas y
territorios**; el 106 **no**. Con cuatro pistas vivas y A como única que mergea,
**el reparto va a hacer falta igual** — o lo declara la mesa, o cada pista lo
infiere, que es como nacen los cruces que el método existe para evitar.

---

---

# CIERRE DE LA TANDA 1 — las cuatro firmas de vuelta, ejecutadas

## ⑥ ✅ EL CIERRE DE `+test3`, REVERTIDO

**Se intentó primero POR LA PUERTA REAL**, y rebotó: `revertir_cierre_cuenta`
exige `is_admin()` y devolvió **`no_autorizado`** sin JWT. *El gate funciona —
se registra como salud, no como obstáculo.*

**Se hizo por vía administrativa, en UNA SOLA SENTENCIA** (`WITH … RETURNING`
encadenando los dos `UPDATE`) **para que fuera atómica**: *dos sentencias sueltas
podían dejar la fila revertida y el ban puesto, que es el peor de los tres
estados.*

**Verificado contra el objeto, no contra el retorno:** `estado = revertido` ·
`banned_until = NULL` · **cierres programados 0** · **usuarios baneados 0** ·
**la llave sigue en `false`**.

⚠️ **`revertido_por` quedó NULL A PROPÓSITO**, y el motivo lo dice adentro de la
fila: *no hubo un admin humano, y escribir uno sería inventarlo.* **La constancia
prefiere un nulo honesto a un dato plausible** — que es la misma regla que rige
para el carnet y para el fallback de la receta.

## ⑦ ✅ `D-888` ENMENDADA — dueño **D**, disparo **después de que el riel corra en QA**

**Y lo que la destrabó no fue una decisión: fue un dato.** Los reversos **se
pueden simular** ⇒ dejó de ser una pieza que no se podía ensayar.

> **Estuvo dos días esperando a un proveedor y en realidad esperaba a un dueño; y
> el mensaje que le quitó el último argumento también le quitó la excusa.**

**Con su ventana nueva adentro:** se construye contra **24 h en DeUna · mismo día
en Nuvei**. *Construirlo contra «mismo día» para los dos rieles sería construirlo
mal para uno.*

## ⑧ ✅ EL REPARTO DE TERRITORIOS, DEPOSITADO

En `PLAN_MESA_106.md` §TERRITORIOS, **marcado como firma posterior** para no
tocar el verbatim del plan: **A** conducción · DB · `packages/api` · `docs/` ·
merges/push/OTA — **B** `packages/ui` · tokens · jueces · **el asset de marca de
DeUna** — **C** las dos apps — **D** `supabase/functions/pagos-*`.

*El asset de DeUna es de B aunque el riel sea de D: el manual de marca del
proveedor manda sobre cómo se ve la fila de pago, y eso es superficie.*

## ⑨ ✅ `L-418` — la regla de la casa

**Toda respuesta de un proveedor se cita con SU FECHA y SU CANAL.** La más nueva
rige; **la vieja no se borra** — *un proveedor que se contradice es un dato sobre
el proveedor.*

**Lo que la volvió regla no fue el typo, fue qué se hizo con la primera
respuesta:** se usó para **cerrar** una pregunta abierta (§8 la sacó de §12
diciendo *«ya no es pregunta abierta»*) y para fundar una promesa que iba a los
T&C. ⇒ **una respuesta sin fecha no envejece: se lee como permanente.**

**Emparienta con `L-166` por el flanco que L-166 no cubre:** ahí el dato envejece
**en nuestra base** y se re-mide cuando se quiera; acá envejece **en la boca de un
tercero** y no hay forma de re-medirlo sin volver a preguntar. *Por eso la fecha
no es prolijidad: es lo único que dice cuán vieja es la única fuente que hay.*

---

---

# HIGIENE DE MESA *(orden del founder, 24-ago)*

## ① Mi rama EN ORIGIN — verificada por SHA, no por recuerdo

`pista/s105-a` local **`f7ad5e27`** = `ls-remote` **`f7ad5e27`**. **Ya estaba
pusheada** (se empujó en el turno 0 y de nuevo antes de cada merge), pero **se
re-midió en el turno en que se afirma**, que es la regla — *«ya lo hice» es un
recuerdo, y un recuerdo no es un `ls-remote`.*

🔴 **Y la orden tiene su razón medida hoy, dos veces:** el trabajo de **B** no
existía fuera de su disco (`ls-remote` vacío — su propio pedido lo decía y era
literal), y **`pista/s105-d` tampoco está en origin**.

> ### **El object store compartido lo hace parecer seguro y no lo es: un worktree vecino es el mismo disco, no una copia.**
> *El merge de B salió sin fricción justamente por eso — y esa facilidad es lo
> que disfraza el riesgo. Estaba a un `rm -rf` de distancia.*

**Lo que sí se hizo al mergear:** se empujó **también la rama de B**, no solo el
merge. *Mergear a main salva el contenido; no salva la rama de quien lo escribió.*

## ② LA NUMERACIÓN — rige **S105**, y acá está quién miente

**Firmado: de acá en adelante todo firma S105.** El log **no se reescribe**: el
commit viejo se queda con su asunto y la corrección se ve.

**Medido, comparando nombre de directorio contra rama en los doce worktrees:**

| worktree | rama | veredicto |
|---|---|---|
| `e-petplace-s105-a` | `pista/s105-a` | ✅ **coinciden — el mío no miente** |
| **`e-petplace-s106-d`** | **`pista/s105-d`** | 🔴 **el DIRECTORIO miente, la rama no** |
| `e-petplace-s100-c` | `pista-c` | 🟡 desajuste viejo, de otra mesa |

**Y en `main` hay UN solo commit firmado S106:** `0209d1a9` — *«S106-B · turno 1
sin codigo…»*. **Se queda como está.**

> **La regla que esto deja, y vale más que el caso:** cuando el directorio y la
> rama no coinciden, **gana la rama** — *el directorio es un nombre que alguien
> eligió una vez; la rama es lo que git usa para decidir dónde cae un commit.*
> **Nadie renombra un worktree ajeno para «emprolijar»**: el nombre es cosmético,
> y renombrarlo mientras su pista trabaja sí rompe algo.

---

---

# LAS TRES PISTAS ENTRARON — main `2a8c0456`

**Mergeadas B, C y D**, cada una verificada antes (merge-tree limpio · sin paths
fuera de su territorio firmado) y después (ancestro de main).

🔴 **Y los gates se corrieron sobre el ESTADO COMBINADO, que es el que nadie
había probado** — *cada pista probó su rama; el merge de las tres es un estado
nuevo que no existió en ningún worktree*: **`verify:diseno` VERDE con 56 reglas**
(la `R65` de B subió el contador desde 55) **y los 4 typechecks VERDES**.
**412 migraciones locales.**

## 🔴 EL ERROR PROPIO, CORREGIDO POR D — y la corrección vale más que el error

**Yo encadené el reproceso a la cura del sellador. Era la razón equivocada:** los
cuatro eventos del grupo ① **ya tenían `credencial='SERVER'` y autenticaban** —
el sellador roto solo mordía al tráfico **nuevo**.

**Lo que sí lo frena es otra cosa, y la encontró el freno y no el diagnóstico:**
aplicar los cuatro **dispara el actuador ⇒ confirma sujetos ⇒ sale un comprobante
por correo.** *Efecto hacia afuera sobre plata, aunque sea sandbox* ⇒ **pide firma
explícita.**

> ### **Las dos razones terminan en el mismo lugar —no se corre— y esa coincidencia es justamente lo peligroso.**
> **La mía se habría disuelto al curar el sellador, y el reproceso habría salido
> sin firma.** *Un freno que se sostiene en la razón equivocada no avisa cuando
> deja de sostenerse: simplemente se abre.* **Lo levantó D, sobre trabajo mío.**

## 🔴 EL REPARTO NO ALCANZA, Y NO ES «ESTA VEZ TOCÓ DB» — dato de D, para la mesa

La migración de D-912 cayó en `supabase/migrations/`, que el territorio firmado
le dio a **A**. **No se objeta:** el founder le asignó `D-912` a D y la cura vivía
en un trigger — no tenía otro lugar. *Y está mejor escrita que muchas mías: veda
declarada, reversa antes con su nota de que revertir REABRE la deuda, y la causa
medida contra el objeto en vez de leída de la ficha.*

**Pero D midió que el frente ENTERO es así, y eso lo vuelve accionable:**

| pieza que falta del riel | dónde vive | de quién es por el reparto |
|---|---|---|
| **`D-887`**, el aplicador del barrido | **DB pura** | **A** — *pero cierra el circuito de D, y su contrato lo escribió D* |
| `_evento_autenticado` y su rama `origen` | **DB** | A — *y es el corazón del buzón* |
| el cron del barrido, cuando se encienda | **DB** | A |

> ### **La línea útil probablemente no sea «functions vs migrations», sino «el riel del proveedor vs el motor de pagos» — con la DB del riel del lado de quien lo opera.**
> **No lo decide una pista.** Se deja dicho **porque la próxima vez no va a haber
> un founder asignando el ítem a mano**, y ahí cada una va a inferir.

## Estado del riel, medido por D y no re-medido acá *(se cita con su dueño)*

**Cuatro functions ACTIVE** (`deuna-solicitud` v1 `verify_jwt=true` ·
`deuna-webhook` v1 `false` · `deuna-barrido` v1 `false` · `pagos-webhook-stg`
v37 `false`) · **cero cron de barrido**, que es **lo correcto por §13bis ③** —
no se enciende hasta que exista `D-887` y haya corrido a mano una vez.

⚠️ **EL FRENO QUE HOY MANDA SOBRE TODOS, y depende de un tercero:** el buzón está
desplegado y **no puede recibir nada hasta que DeUna registre la URL y el
header**. *Si alguien pregunta por qué el riel «está vivo» y no llega ningún
webhook, es eso — y no un defecto.*

---

---

# EL REPROCESO, FIRMADO Y FRENADO DOS VECES — y el censo que salió de ahí

**El founder firmó** con el dato que disolvía mi freno: *los correos de esas
cuentas le llegan a él* ⇒ no hay tercero recibiendo un comprobante sobre plata
suya. **Con la condición: la firma cubre los cuatro medidos, no la categoría.**

**Por eso se aplicaron POR ID LITERAL y no por predicado.** *Un `WHERE` que
describe la categoría habría aplicado lo que apareciera; cuatro UUID aplican
cuatro cosas.*

**Freno ①** — abortó con `transicion_no_permitida: cancelado_cliente →
pago_capturado`. **Rollback completo verificado** (los cuatro en `recibido`,
`pago_id` NULL, `aplicado` sigue en 23). ⚠️ *Que fuera una sola sentencia fue
suerte de forma, no diseño: sueltos, tres se habrían aplicado antes de que el
cuarto avisara.*

**Freno ②** — con firma para correr **solo** el de la cita, se midió **el estado
del SUJETO antes de aplicar**: `estado_reserva = expirada`, fecha ya pasada.
**No se aplicó.**

## 🔴 EL ERROR PROPIO, y es el de toda la sesión cometido por quien lo nombraba

Reporté *«cuatro cobros que no cerraron»*. **Dos ya estaban resueltos** (compra
`pagada`, pedido `pago_capturado`); uno **no era una compra sino una CITA**; y
uno tenía el **pedido cancelado por el cliente**.

> ### **Leí el estado del EVENTO y lo reporté como estado del SUJETO.**
> *El evento en `recibido` era cosmético en dos de los cuatro: el negocio se
> había completado por otra vía.* **Es exactamente «no alcanza mirar el color»,
> pagado por mí y encontrado por el motor al negarse.**

## EL CENSO — orden del founder en el acto (*«si hay dos, puede haber veinte»*)

**28 intentos con plata real** (excluidos `seed_gate`/`simulado`/`siembra`):

| | n | suma |
|---|---|---|
| ✅ sanos | **25** | $358,30 |
| 🔴 plata contra un sujeto que ya no puede recibirla | **3** | **$87,65** |

**Y los tres son EXACTAMENTE los tres intentos `pendiente` con
`authorization_code`. No es coincidencia: es la cadena.** *El pago no se aplicó a
tiempo → el sujeto se venció o lo cancelaron mientras esperaba → ahora ya no se
puede aplicar.* **El defecto técnico de `D-912` produjo el problema de plata**, y
lo produjo por el paso del tiempo, que es lo único que nadie deshace.

⚠️ **El tercero es `DF-2100043`, el que la cura del sellador destraba: su pedido
también está `cancelado_cliente`.** *La cura arregla el futuro; no resucita un
sujeto muerto.*

✅ **Y el censo dice también lo tranquilizador: 25 de 28 están perfectos.** *No
hay fuga sistemática — hay tres casos con una causa común y un techo conocido.*

## `D-913` DEPOSITADA — el hallazgo de D, verificado antes de firmarlo

**«Generar un código nuevo» no puede funcionar**: la compuerta 0 rebota con
cualquier intento `pendiente` y **nada cierra el viejo**. Verificado por A contra
`pg_proc`: **cero funciones miran `codigo_expira_en`.**

✅ **Y el dato que abarata la cura, que quien la halló no tenía: el CHECK de
`pagos_intentos.estado` YA ADMITE `'expirado'`.** *El vocabulario no hay que
escribirlo — falta su productor.* **Tercera vez en la mesa que aparece un cajón
con etiqueta y sin fondo** (`reverso_fallido` · `reversado_mismo_dia` ·
`expirado`) ⇒ **vale censar el CHECK entero, valor por valor.**

---

## TANDA 2a · EL ACTUADOR RECONOCE EL REVERSO (`D-923`) — cerrada en `main`

**`20260825210000`** · cinturón **VERDE contra el evento real** · reversa escrita
ANTES · **76(g) declarada NO RIGE** · merge verificado por contenido, `main` en
`40ad5485`, ancestro de `origin/main` VERDE, ambos árboles en 0.

### El defecto era más fino de lo que decía su ficha

`D-923` decía *«el actuador no maneja `REVERSED`»*. **Medido, no era eso:**

> ### Un reverso de Nuvei llega con `status = 2` — **exactamente igual que un rechazo**.
> Para `_pago_aprobado` los dos son «no confirma» y **morían juntos en `desconocido`**.
> ***No fallaba: los CONFUNDÍA.*** Y un evento que se confunde con otro no deja
> síntoma — deja un contador de rechazos que se lee perfectamente normal.

**El discriminador lo trajo el founder ejerciendo el reverso, no la doc:**
`status_detail`, no `status`. Medido sobre `DF-2102135`: `status=2 ·
status_detail=7 · current_status=CANCELLED · carrier_code=ReversedByMerchant ·
message="Reverse by mock"`. **Y `transaction.id` NO CAMBIA** ⇒ el intento se
encuentra por él. *Nuvei no emite id de reverso propio; DeUna sí. Es el mismo
hecho con dos formas, y por eso la letra se enmienda POR RIEL.*

### Lo que se decidió y por qué

| Decisión | Razón |
|---|---|
| El reconocedor va **antes** de `_pago_aprobado` | ahí es donde el reverso moría |
| **Dos** vocabularios, no uno: «¿es un reverso?» (A) vs «¿qué clase?» (D) | son dos preguntas con dueños distintos; juntarlas obligaría al actuador a saber de reversos parciales |
| `refund_amount` se documenta como **el monto original** | **MEDIDO: ese campo NO existe en el payload.** Lo que viaja es `amount`. *Un campo que se llama como otro miente sin equivocarse.* |
| El reverso **sin intento** se nombra, no se ignora | es plata devuelta sobre un cobro que no registramos — el caso más grave, y esa línea de detalle es el único aviso que va a haber |

### 🔴 EL CINTURÓN ENCONTRÓ UN DEFECTO EN LA CURA, EN SU PRIMER PASO

`SELECT p_detail IN ('7','34')` **no es un booleano**: con `NULL` devuelve `NULL`.
El guard **acertaba igual** —un `IF` con `NULL` no entra— **pero acertaba por cómo
SQL trata los nulos, no por diseño.** Se curó **la función, no el assert**: curar
el assert habría dejado el tri-estado vivo y sin testigo. ⇒ **`L-424`**.

### La tanda 3 ya estaba construida — por otra pista

El founder pidió *«la ventana 17:00 como restricción del motor»*. **Medido: D ya
la puso adentro de `registrar_reverso_nuvei`.** Sonda con rojo producido: un
intento cobrado ayer rebota **`fuera_de_ventana_otro_dia`**. **El brazo de las
17:00 NO se pudo ejercer** (la sonda corrió 15:59 local) y **no se forzó moviendo
el reloj de la base** — queda por medir después del corte. *Verificar salió más
barato que construir, y el hallazgo es de coordinación: dos pistas pueden cubrir
la misma pieza sin saberlo.*

### 🔴 LO QUE **NO** SE HIZO, Y NO ES POR TIEMPO

**El sujeto no se mueve.** El censo dice por qué: son **CUATRO** sujetos, y
**nadie lee `'cancelada'` de `compras`** — cero funciones, cero vistas, cero
policies. Mover el sujeto es **estrenar un estado que ningún lector conoce, en
cuatro objetos distintos**, y el destino de tres de ellos es decisión de
producto: *si una cita pagada se reversa, ¿la familia conserva el horario o lo
pierde? ¿el prestador la ve desaparecer? ¿alguien recibe un aviso?*

**Y dos hallazgos que la ficha daba por otra cosa:**
- la compra reversada **no tiene evento económico** (el devengo nace al entregar,
  no al cobrar) ⇒ **no hay nada que revertir para este caso**;
- 🔴 **`aplicar_reembolso` no tiene UN SOLO llamador en toda la base.** `L-318`
  en su forma pura: construida, probada, sin puerta.

⇒ el puente al ledger es **CONDICIONAL**, y la condición de muerte de `D-923`
quedó enmendada para decirlo.

---

## COLA VIVA — próximo paso ejecutable

0. 🔴 **`D-913` antes del flip de `DEUNA_ELEGIBLE`** — la mesa elige camino.
   *Encender la puerta del cliente con esto abierto es estrenar el riel con un
   callejón de tres minutos.*
0bis. 🔴 **Los tres pagos de `D-888` ($87,65)** — su salida es el reverso.
1. ~~🔴 **EL REPROCESO DE LOS CUATRO — ESPERA FIRMA DEL FOUNDER.**~~ ☠️ **CERRADO:
   ninguno era aplicable.**
   `DF-2099049` ($6,00) · `DF-2099041` ($70,90) · `DF-2098569` · `DF-2098177`.
   ~~*no se toca hasta que el sellador esté curado*~~ — **esa razón era
   equivocada** (ver arriba). **La razón buena: aplicarlos manda correos.**
2. **Leer `S105-D-MEDICIONES-CHECKPOINT-1.md` y CERRAR el Checkpoint 1.**
   *Ya está en main; el hueco de §1.1/§1.2 dejó de estar vacío.* **Declarado: no
   se leyeron todavía, y no se dan por buenas sin leerlas.**
3. **Las tres preguntas de D sobre `REVERSED`** — del founder, y son la última
   pregunta abierta del riel. **La más filosa: qué limpia exactamente el
   `refund`**, porque su peor caso es dejar el punto de venta inutilizable.
4. **El worker de exportación y el cron del cierre** (`D-910`) — su disparo es
   *antes del primer usuario real*, y hoy es cuando más barato sale.
5. **La línea de reparto riel-vs-motor** — de mesa, no de pista.
6. **Los tres oficios nuevos** (frente 3 del plan): **se escriben en la mesa.**
