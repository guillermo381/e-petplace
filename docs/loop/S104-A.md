# S104-A · bitácora de la pista A (conductora)

> **Sesión:** S104 — *«la cuenta, la identidad y la voz que sale por correo»* (Mesa 105
> del founder; **la bitácora asigna S104**, S103 no se renumera).
> **Abre:** 23-ago-2026 · **Pista:** A, conductora · **Rama:** `main`, directorio primario.
> **Rige:** `PLAN_MESA_105.md` + `ORDEN_ARRANQUE_MESA_105.md` (depositados hoy) ·
> `CLAUDE.md` · `CONTRATO_TRABAJO` · `METODO_TRES_PISTAS`.

---

## ESTADO AL ABRIR (medido, no heredado)

| qué | valor | cómo se midió |
|---|---|---|
| rama · HEAD | `main` · `eebe4975` | `git rev-parse` |
| árbol | **limpio** (`--porcelain` en cero) | `git status --porcelain` |
| sincronía | **`HEAD == origin/main`**, ancestro verificado | `git merge-base --is-ancestor` |
| migraciones locales | **392** | `ls supabase/migrations/*.sql \| wc -l` |
| migraciones remoto | **sin medir aún** (turno 1) | `migration list --linked` |
| numeración libre | **D-890 · L-410 · R63** | grep contra `DEUDAS_CANONICAS.md` |
| worktrees | **19**, la mayoría de sesiones cerradas (S96→S103) | `git worktree list` |

⚠️ **Residuo anotado, no curado:** 19 worktrees vivos y solo cuatro pistas por abrir.
S92-BIS ya podó doce una vez (D-718/D-711). **No se toca sin firma** — podar
worktrees con ramas sin mergear es justo el acto que L-217 vigila. Se declara para
que quien abra worktree no crea que el listado describe trabajo en vuelo.

---

## TURNO 0 — HECHO

1. ✅ **Depositados** `docs/PLAN_MESA_105.md` y `docs/ORDEN_ARRANQUE_MESA_105.md`
   (verbatim; la sesión no editó una coma de la letra de la mesa).
2. ✅ **Relevamiento de familia** — el primer acto que §0.3 ordena:
   `docs/relevamientos/2026-08-23-s104a-RELEVAMIENTO-FAMILIA.md`.
   **La letra existe: `MODELO_PRODUCTO.md` §4** (líneas 2114-2353), y su §4.6 ya
   venía declarando su propio drift desde S19.

### Los cuatro hallazgos que van al Checkpoint 1

- 🔴 **`permisos` existe en `mascota_familiar_autorizado` (la mitad que S19 dejó sin
  auditar) y CERO funciones y CERO policies lo leen** ⇒ la tabla es **binaria**, y la
  configurabilidad de §4.3 es **puerta sin motor**. **En v1 el permiso ES el escalón.**
- 🔴 **`familia_invitaciones` no existe**, y el molde que se hereda tiene **CINCO**
  RPCs — no seis (plan) ni tres (`PORTAL_PRESTADOR`) — **y ninguna de las cinco crea
  la invitación**: el acto de invitar es el que no tiene RPC.
- 🟡 **`familia_miembro` no tiene `fecha_nacimiento`** ⇒ el escalón `menor` se puede
  escribir y **nada puede verificar que la persona lo sea**. P5 sobre un escalón sin
  respaldo.
- 🟡 **La exclusión co-dueño ⇄ familiar es MUTUA, con trigger de los dos lados** ⇒ la
  firma 5.1 es la única compatible con el motor, y **ascender a co-dueño es una
  transición (cerrar una fila, abrir otra), no un alta.**

### El choque que decide el founder

`familia.tsx` promete **«invitar co-dueño»** con "Pronto"; la **firma 5.1** dice que el
invitado entra como **familiar autorizado**. **Voto de A: gana la firma y cambia la
pantalla** — pero es decisión de producto. **C no toca ese texto antes del Checkpoint 1.**

---

## COLA VIVA — turno 1 de A (sin código, contra objeto declarado)

1. `profiles.email` vs `auth.users.email`: **divergencia HOY** (se espera 0) ·
   **quién la escribe** (wrappers, triggers sobre `profiles`, la función que la creó) ·
   **todos los lectores**, no solo `miPerfil.ts:37`.
2. **Dashboard de Supabase, con el founder** (§7 ítems 3-4): «Secure email change» ·
   Site URL / Redirect URLs · SMTP Settings. **El `config.toml` no prueba nada** — es
   del entorno local, y el canon ya se cobró esa confusión tres veces.
3. Proveedores habilitados (Google · Apple — §7ter dice que Apple no existe y por qué).
4. **Lo que el relevamiento declaró sin medir** (§⑤ del relevamiento): consumidores de
   `_user_es_familiar_autorizado_mascota` · cuerpo de `user_tiene_acceso_a_mascota` ·
   los 18 tipos de evento contra `cat_tipos_evento`.
5. Migraciones remoto vs local.

---

---

## TURNO 1 — HECHO (medición, cero código)

**Push del turno 0:** `0d0c2e32` en `origin/main`, **verificado por SHA** con
`ls-remote` (no por código de salida — L-239).

### La copia que miente — los cuatro puntos

**(a) Divergencia: 17, y las 17 son SOLO mayúsculas.** 165 `profiles` = 165
`auth.users`, cero huérfanos de ninguno de los dos lados, cero nulos.
**Discriminador corrido:** `divergen=17 · solo_case=17 · divergencia_real=0`
(`lower()` las iguala; `btrim` no cambia nada). `profiles` guarda lo tipeado
(`Luis@…`), `auth` normaliza a minúsculas. **El plan esperaba 0 y hay 17 — pero
ninguna apunta a otra cuenta.**

**(b) Quién escribe.** *No* es "nadie":
- `handle_new_user`, por trigger **`on_auth_user_created`** sobre `auth.users`.
  🔴 **El trigger es `AFTER INSERT` y SOLO INSERT** ⇒ **nada propaga un UPDATE de
  `auth.users.email` a la copia.**
- `invitar_prestador` (escribe `profiles` y toca `email`).
- `_trg_completar_pendiente_registro`, trigger `INSERT/UPDATE` sobre `profiles`.
- 🔴 **`authenticated` tiene grant `UPDATE` sobre la columna `email`** y la policy
  `profiles_update` es `auth.uid() = id` en `qual` **y** en `with_check`, **sin
  restricción de columnas** ⇒ un usuario con sesión puede reescribir su propio
  `profiles.email` por PostgREST directo. **El wrapper no es la puerta: la esquiva
  cualquier cliente.**
- **`actualizarMiPerfil` NO escribe email**: `InputActualizarMiPerfil` y el objeto
  `cambios` (`miPerfil.ts:76`) están tipados sin `email`. *El tipo lo impide; el
  grant no.*

**(c) Lectores medidos (todos, no solo el conocido):**
- `packages/api/src/wrappers/miPerfil.ts:29` (`select`) y **`:37`** —
  `data?.email ?? sesion.session?.user.email` ⇒ **la tabla GANA sobre `auth`.**
  Confirmado el diagnóstico del plan, con línea.
- `packages/api/src/wrappers/familia.ts:70` — lee `profiles` pero **solo `nombre`**:
  no es lector de `email`.
- **Cero lectores de `profiles.email` en `apps/cliente` y `apps/prestador`**: las
  apps lo reciben por el wrapper.

**(d) Inventario de circuito.** ⚠️ **`pg_stat_user_functions` está VACÍO** (sin
`track_functions`) ⇒ *«corrió alguna vez» no se puede responder por esa vía y no se
finge*: se responde **por huella**.
- `handle_new_user`: **165 profiles y `auth_sin_profile=0`** ⇒ corrió para todas.
- `_trg_completar_pendiente_registro`: 79 familias `estandar` · 3
  `pendiente_completar` ⇒ corrió.
- `invitar_prestador`: 11 prestadores · 17 invitaciones ⇒ corrió.
- **El camino de UPDATE de email: NUNCA existió** — no hay trigger que lo cubra.
  *Por eso la divergencia real es 0: no es que esté sano, es que todavía nadie pudo
  cambiar su correo.*

⇒ **La tanda 1 se confirma tal como la orden la escribió**, con un matiz que la
orden no preveía: además del trigger de propagación hace falta **cerrar la escritura
por columna**, o el espejo se puede ensuciar desde el cliente. Y el backfill no es
"copiar auth a profiles": es **`lower()`**, sobre 17 filas.

### Censo de worktrees — **son 21, no 19**

`s104-c` y `s104-d` aparecieron durante el turno ⇒ **C y D ya abrieron worktree.**

| veredicto | cuántos | cuáles |
|---|---|---|
| **LIMPIO (mergeado, 0 commits fuera de `origin/main`)** | 17 + el primario | `pista-a` · `pista-b` · `pista-d` · `s101-d` · `s102-b` · `s103-a` · `s103-b-jueces` · `s103-c` · `s104-c` · `s104-d` · `s96-a` · `s96-d` · `s98-c` · `s98-d-enmienda` · `s99-a` · `s99-b` · `s99-c` |
| **CON TRABAJO SIN MERGEAR** | **3** | `pista-c` **(3)** · `pista/s103-d` **(16)** · `pista/s99-d` **(2)** |

**Las tres están respaldadas en `origin`** ⇒ *podar el worktree no pierde nada; la
rama sobrevive.* Podar worktree ≠ borrar rama.

🔴 **`pista/s103-d` no es residuo: es material de ESTA mesa.** 16 commits, 15
archivos, y entre ellos **`0b09c939` «insumo D-405: eliminacion de cuenta y
portabilidad, medido contra la base»** y **`e4d98ff5` «inventario del ciclo de cuenta
(C)»** — insumo directo del **frente 3**. También trae dos defectos de DeUna que D
encontró en su propia especificación. **L-217 en su forma exacta: está en `origin` y
no está en el canon.** *Se declara; la poda y el merge los firma el founder.*

**NADA PODADO** *(en el turno 1; la poda se firmó después — ver abajo).*

---

## PODA DE WORKTREES — FIRMADA POR EL FOUNDER (23-ago-2026)

**MOTIVO, y no es higiene:** *un Metro huérfano en el worktree de S103-C quedó
sirviendo el puerto 8081 y **secuestró el aparato de gates del founder**.* La poda
existe para prevenir eso. *Un worktree muerto no es espacio ocupado: es un proceso
que algún día se queda con el puerto del único teléfono que gatea.*

**Alcance firmado:** solo los medidos como LIMPIOS. **NO se tocan** `pista-c` ·
`pista/s103-d` · `pista/s99-d` (trabajo sin mergear) ni `s104-b/c/d` (sesión viva) ni
el primario. **Se poda el worktree, JAMÁS la rama.**

### Re-verificación antes de tocar (la lista era de hacía un rato)

Los 15 candidatos, re-medidos **con dos preguntas, no una**: `rev-list --count
origin/main..HEAD` **y** `status --porcelain`. *«0 commits fuera de main» no dice nada
sobre cambios sin commitear, y podar un árbol sucio destruye trabajo que no está en
ningún lado.* **Los 15: 0 y 0.**

### 🔴 EL HALLAZGO: DOS WORKTREES TENÍAN PROCESOS HUÉRFANOS VIVOS

Censados por `lsof -a -d cwd` antes de borrar nada — **los dos con `PPID 1`**, o sea
huérfanos de verdad, no sesiones de nadie:

| worktree | proceso | edad |
|---|---|---|
| `e-petplace-s100-d` | **React Native DevTools**, ejecutando desde el `node_modules` **de ese worktree** (4 procesos) | **4 días 15 h** |
| `e-petplace-s101-d` | Python de Xcode | 1 día 20 h |

**Es el mismo patrón que motivó la poda, encontrado por buscarlo.** Nadie escuchaba
en 8081 al momento del censo (el Metro de S103-C ya no estaba), **pero el de S100-D
lleva cuatro días vivo apuntando a un directorio que el founder acababa de firmar
para borrar.** *Podar ese worktree habría dejado un proceso ejecutando código de un
directorio inexistente — peor que el estado de partida.*

⇒ **Los dos quedaron FUERA de la poda, declarado y no silenciado.** El de Xcode no se
mata por cuenta propia: no es del proyecto. **Decisión del founder pendiente.**

### Ejecutado: 13 de 15

**Podados:** `s100-a` · `s100-b` · `s102-b` · `s103-a` · `s103-b-jueces` · `s103-c` ·
`s96-a` · `s96-d` · `s97-c` · `s97-d` · `s99-a` · `s99-b` · `s99-c`.
**Worktrees: 22 → 9.**

**La red de seguridad, y es lo que vuelve reversible el acto:** se registró el SHA de
cada rama **antes** de podar y se re-leyó **después**. **Las 13 coinciden exactamente**
(`pista-a 232346d6` · `pista-b ebd6a747` · `pista/s102-b 0adbcfc3` · `pista/s103-a
4256acf3` · `pista/s103-b-jueces 74857869` · `pista/s103-c 6ff0498f` · `pista/s96-a
f2fcf518` · `pista/s96-d ff332982` · `pista/s98-c 08ecaf60` · `pista/s98-d-enmienda
566c2f59` · `pista/s99-a 57aa526d` · `pista/s99-b 916af192` · `pista/s99-c 605c3a9c`).
**Cero ramas borradas.** Y como las 13 tienen 0 commits fuera de `origin/main`, su
contenido es alcanzable desde el canon aunque la rama local desapareciera.

*Precedente que esto deja: **el censo de un worktree antes de podarlo lleva TRES
preguntas, no una** — commits fuera de main · árbol sucio · **procesos vivos
adentro**. La tercera es la que este caso agregó, y es justo la que motivaba la poda.*

---

---

## TANDA 1 — EJECUTADA (autorizada con gate posterior en dispositivo)

**Tres migraciones, tres reversas escritas ANTES, 395 local = 395 remoto.**

### ① La copia que miente — cerrada en sus cuatro puntos

- **(a) backfill** `lower(btrim())` sobre las 17 divergentes. Se normaliza contra
  sí mismo y **no se copia de `auth.users` a propósito**: si alguna divergiera de
  verdad, copiar de auth lo taparía. Assert: divergencia = 0.
- **(b) espejo** `on_auth_user_email_changed` (`AFTER UPDATE OF email` en
  `auth.users`), DEFINER, normalizando en la puerta.
- **(c) el guard**, y acá la decisión técnica que se informa y no se vota:
  **`REVOKE UPDATE(email)` NO TIENE EFECTO** con el `GRANT UPDATE` de tabla que
  hay hoy; hacerlo por columna exigiría revocar la tabla y re-conceder **34
  columnas**, y **toda columna nueva futura nacería sin grant y rompería en
  silencio**. ⇒ molde de la casa (D-389): trigger de protección.
- **(d)** `miPerfil.ts`: **auth primero, la copia como cache.**

🔴 **Y el guard nació roto: DEFINER en vez de INVOKER ⇒ NO FRENABA.** El apply
salió verde porque el cinturón preguntaba *«¿existe el trigger?»*. **Lo cazó
producir el rojo.** Curado en `20260823130000`, con cinturón que ahora mide
**que frena** + contra-caso (el nombre sigue guardándose). ⇒ **L-410.**

### ② El consentimiento — primer escritor del monorepo

`registrarConsentimiento()` en `packages/api` (compartido por la **ley de
paridad**), `normalizarEmail()` en registro **e** inicio de sesión,
`VERSION_TERMINOS_VIGENTE`.

🔴 **Dos premisas del mandato corregidas por la medición:**
- **`consentimientos` NO tiene cero filas: tiene 59** (`tipo='registro'`,
  `version='v1.0'`, 25-abr → 10-may-2026, del legado). **Cierto que el monorepo
  nunca escribió ahí; falso que la tabla esté vacía** ⇒ se hereda el vocabulario
  (`tipo`), **no la versión**: escribir `v1.0` afirmaría que esta persona aceptó
  el mismo documento que aquellas 59, y **las páginas legales se corrigieron en
  S103**. Va `legales-2026-08`, **declarado provisional** hasta que D-405
  entregue documentos versionados, con la URL guardada en `metadata` — *lo que
  P23 promete demostrar no es un número: es QUÉ se le mostró.*
- **🔴 La policy admitía escritura ANÓNIMA a nombre de terceros**
  (`with_check=true`, roles `anon,authenticated`) ⇒ **la tabla que existe para
  ser evidencia aceptaba evidencia falsa.** Curada (`20260823140000`) con rojo y
  contra-caso. **D-891**, nacida y muerta el mismo día.

### ③ Depositado

`MODELO_LOGIN.md` · `RITUAL_DE_ENTRADA.md` (los dos verbatim) · la **ley de
paridad de cuenta** al canon con sus tres excepciones.

### Hallazgos que salen de esta tanda

- **D-890 🔴** `_prestador_empleados_protege_gobierno` **no frena** (mismo defecto
  DEFINER, medido con `row_count=1`, no con «no lanzó»). **Frenaba cuando nació**
  — una migración posterior lo recreó y le cambió el modo. **No se curó: es de
  otra pieza y exige censo previo.**
- **D-892 🔴** `epetplace.com` **sin MX** (medido por D): `privacidad@` es un canal
  **prometido en las legales publicadas y en P15** que no existe. Frena el DMARC
  y **toca la tanda 3** (los 30 días se ejercen escribiendo ahí).
- **L-410** DEFINER desactiva todo guard por `current_user`, y el apply sale verde.
- **L-411** un cinturón con `SET LOCAL ROLE` deja la migración **aplicada y sin
  registrar** (pasó **dos veces**; las tres se registraron a mano y se verificó
  395 = 395).

### ⚠️ LO QUE NO SE HIZO, declarado

- **El acta del Checkpoint 1 NO se escribió**: exige las cuatro tablas y **A solo
  tiene la suya**. *Un «checkpoint» con una entrega de cuatro no es un
  contraste: es un parte con otro nombre.* Se escribe cuando B, C y D entreguen.
- **La OTA de la tanda NO se publicó**: B y C todavía no entregaron. Cuando lo
  hagan, A mergea, publica **una** OTA y avisa para el doble reinicio.
- El typecheck corrido es el de `packages/api` (0). Los de las apps corren al
  mergear lo de C.

---

## COMPROMISOS ABIERTOS

**Debo a las otras pistas:** el relevamiento de familia (entregado hoy, es su insumo).
**Me deben:** D → veredicto push *corto o frente* + DNS/DMARC + inventario de
plantillas · C → censo de superficies de cuenta en las dos apps · B → qué piezas
faltan + diseño de R63.
**Al founder:** el Checkpoint 1 con las cuatro tablas contrastadas + el choque de §④.

## FRENOS Y SUPUESTOS VIVOS

- **Un solo teléfono** ⇒ los gates se **piden a A** y se serializan. Ninguna pista da
  por pasado un gate que no corrió el founder.
- **A es la única mano que mergea y publica** (regla 88).
- **Nada del frente 4 se construye** hasta que el founder vea §④ y confirme (o
  corrija) qué promete la pantalla.
- **El D-U-N-S puede bloquear las DOS tiendas**, no solo Apple (§7ter). D lo verifica
  contra la consola de Play en el turno 1. *Si se confirma, el soft launch de octubre
  es por APK directa — y eso se dice ahora, no el 1-oct.*

---

## D-890 SUBIÓ A LA TANDA 1 Y SE CURÓ (firma founder, 23-ago)

**Razón del founder:** *con 27 empleados con acceso, un guard que no frena es
agujero vivo, no deuda.*

**El censo que pidió, hecho por CUERPO y por NOMBRE — y la respuesta es CASO, no
clase:** de las **4** funciones de trigger que gatean por `current_user`, **solo
`_prestador_empleados_protege_gobierno` era DEFINER**; las otras tres son INVOKER
y frenan. De los **16** triggers DEFINER que levantan excepción, los otros 15 **no
gatean por identidad de rol** — validan dominio (acuario, tallas de grooming,
orden del programa, frontera de producto), y ahí DEFINER es lo correcto.
⇒ **El defecto no es DEFINER: es DEFINER *combinado con* `current_user`.**

**⚠️ Y la primera medición casi induce un error — se registra porque es el
método.** El primer rojo salió de `limit 1` sobre la tabla, sin mirar si esa fila
era de un **gestor**. El guard tiene DOS condiciones (`current_user` **y** `NOT
user_gestiona_prestador`): sobre un titular habría dado «pasa» **legítimamente**.
*Un rojo por la razón equivocada está tan roto como un verde por la razón
equivocada.* El discriminador bueno: **no-gestores + `row_count`**.

**EL PAR:** antes `FRENADOS=0 AGUJEROS=8` · después `FRENADOS=8 AGUJEROS=0`, con
**contra-caso verde** (`GESTOR PASA, filas=1`) — el titular sigue gobernando.

**Censo de escritores antes de cerrar (L-215) — cierra D-528, abierto desde S76:**
cuatro escritores, **los cuatro DEFINER** (`aceptar_invitacion_pendiente_login` ·
`crear_empleado_directo` · `dar_de_baja_empleado` · `invitar_prestador`), y el
único escritor directo del repo (`empleado-matricula.ts`) toca
`matricula_profesional`/`matricula_pais_emisor`, **que el guard no vigila**.

**L-411 aplicada y confirmada:** esta migración **no usa `set local role` en su
cinturón** (verifica `prosecdef` y el enganche) y **su `db push` terminó
`Finished` y se registró solo** — al revés que las tres anteriores. La prueba de
que FRENA se corrió aparte. **396 migraciones locales.**

## AVISO A B Y C — `RITUAL_DE_ENTRADA.md` YA ESTÁ EN `docs/`

Depositado verbatim (118 líneas) junto con `MODELO_LOGIN.md` en el commit
`de73d0d8`, ya en `origin/main`. **C estaba bloqueada esperándolo y no lo está.**

---

## CIERRE DE LA TANDA 1 — LAS CUATRO PISTAS MERGEADAS Y LA OTA PUBLICADA

**Acta del Checkpoint 1:** `docs/actas/2026-08-23-s104-ACTA-CHECKPOINT-1.md`.

**Ancla `bff46617`. OTAs leídas DEL OBJETO** (`update:view --json`, jamás del texto
del publish): **cliente `a5704c1d` rt 1.0.4 · prestador `265edbd1` rt 1.0.5**, las
dos **`dirty: None`**, mismo ancla a propósito.

⚠️ **Los dos runtimes estaban CRUZADOS en la lectura rápida y se verificaron uno
por uno antes de publicar.** Un `grep -h` sobre los dos `app.json` los devolvió en
un orden, y la medición de B sobre el aparato decía el inverso. **Publicar contra
el runtime equivocado no entrega nada y no falla**: el update sale verde y el
teléfono nunca lo ve. Medido por archivo: **cliente 1.0.4 · prestador 1.0.5**,
coincidente con el aparato.

**Ratificado (76(c)/(d)):** el hunk aditivo de C a `packages/api/src/index.ts`.
`registrarConsentimiento`, `normalizarEmail` y `TipoConsentimiento` vivían en
`auth.ts` y **no estaban en el index** ⇒ eran inalcanzables desde `@epetplace/api`.
**El hueco era mío y lo encontró C al necesitarlas.** *Una función exportada de su
módulo pero ausente del index es puerta sin motor vista desde el otro lado: existe,
typechequea donde nació, y no se puede consumir.*

**Firmas ejecutadas:** shared-element §3 queda **per-pantalla** como C lo montó (se
gatea en dispositivo) · la prop `estatica` de `PaseoDeHuellas` viaja en la tanda 2
si no llega a tiempo.

**Estado final:** 396 migraciones local = remoto · 4 typechecks VERDE ·
`verify:diseno` VERDE con 54 reglas · las cuatro ramas en **0 commits fuera de
`main`** · fichas hasta **D-894** · lecciones hasta **L-412**.

**EL GATE EN DISPOSITIVO ES LO ÚNICO QUE FALTA, y es del founder.**

---

## TANDA 2 · EL CORREO DE INVITACIÓN, CERRADO ENTRE A Y D

**A puso el motor** (cola con envío único por PK, supresión, baja en un clic) y
**D puso el transporte**: `despachar-invitacion-correo`, **desplegada e INERTE**.

**Los dos agregados de D al contrato de A — ADOPTADOS, porque lo mejoran:**
- **Dos llaves y fail-closed por `URL_APP_BASE`**: *un enlace roto en un correo
  frío es peor que no mandarlo.* Y el principio general: **correo a gente sin
  cuenta no puede encenderse solo porque alguien desplegó** (patrón del reloj de
  S103).
- **Un null honesto que CORTA el envío**: sin el nombre de quien invita, no se
  manda. *«Alguien te invitó» sin decir quién es exactamente el correo que la
  condición ② existe para no mandar.*
  ⇒ **A lo midió antes de confirmarlo**, porque un corte silencioso es caro: de
  **16 titulares activos, `nombre_null=0`, `nombre_vacío=0`, `nombre_es_email=0`**
  — el corte no dispara contra nadie real hoy. **Borde anotado y no curado:**
  `handle_new_user` cae al local-part del correo cuando no hay metadata (S81), así
  que un titular futuro podría tener un «nombre» que parece un usuario. *El guard
  no lo va a cortar —no es null— y el correo va a decir «juan.perez99 te invitó».
  El día que pase va a parecer un defecto del transporte y es de la siembra.*

### 🔴 EL HALLAZGO DE MÉTODO DEL DÍA, Y ES EL TERCERO DE LA MISMA CLASE

**Los tres primeros `curl` de D contra su propia función no probaban nada.**
Rebotaban `401 UNAUTHORIZED_NO_AUTH_HEADER` — **el guard de PLATAFORMA
(`verify_jwt`), no el suyo**: su cuerpo nunca llegó a ejecutarse, y **estaba a
punto de anotarlo como probado**. Repetidos con la anon key para atravesar la
plataforma, recién ahí ejerció **su** guard (sin secreto → 401 propio · secreto
inventado → 401 · secreto real → 200 `invitacion_correo_apagado`).

**Es la TERCERA forma del mismo defecto en esta sesión**, y las tres en piezas
distintas: ① el guard de `profiles.email` nació DEFINER y **el apply salió
verde** · ② la RPC de invitación **no podía correr** y el cinturón dijo VERDE ·
③ un 401 que parece la defensa propia y es la de otro.
⇒ ***El instrumento contestó por una razón que no era la que se estaba
midiendo.*** *No alcanza con mirar el color: hay que preguntar quién lo produjo.*

### Para encender, tres llaves y ninguna es de A ni de D

① la URL final de `/invitacion` y `/baja` → `URL_APP_BASE` (**C**) · ② **la
pantalla `/baja` viva** (**C**) — *sin ella el enlace de baja lleva a la nada, y
ese enlace es una de las cuatro condiciones* · ③ `INVITACION_CORREO_VIVO=true`
(**founder**). **El cron no se creó a propósito: el cable primero, la llave
después** — misma disciplina que dejó el recurrente listo e inerte en S103.

---

## TANDA 2 — CERRADA CON BUILD (no con OTA)

**Ancla `357ce8e3`.** Builds lanzados: **cliente 1.0.5** (`ecdaa093`) · **prestador
1.0.6** (`3c90f764`). `verify:diseno` VERDE 54 reglas · 4 typechecks VERDE ·
árbol limpio verificado antes de bundlear · **402 migraciones**.

**Las cuatro pistas mergeadas.** El tren nativo llevó tres pasajeros:
`expo-local-authentication` · `expo-clipboard` · el splash del cliente + el
`name` del launcher.

### 🔴 EL FRENO DE MESA CON EL QUE CIERRA LA TANDA

**Las dos vías de invitación llevan a un 404.** Medido con control positivo
(`/` · `/legales` · `/terminos` → **200**) y negativo (`/ruta-control` → 404):
**`/invitacion` y `/baja` no existen en el sitio.**

**Y no es una vía, son las dos:** `familia.tsx:66` compone el **enlace copiable**
con el mismo `APP_BASE` ⇒ *el enlace que se manda por WhatsApp lleva al mismo 404
que el del correo*, **y ése ni siquiera depende de `INVITACION_CORREO_VIVO`: sale
en cuanto alguien toque «invitar».** ⇒ **apagar la llave del correo NO alcanza
como freno.**

**Cargado:** `URL_APP_BASE=https://epetplace.com`. **NO cargado y no es de A:**
`INVITACION_CORREO_VIVO`. **Medido por D: el 308 del apex preserva el query
string** ⇒ el token no se pierde; www ahorra un salto y es afinado, no urgencia.

### LAS SEIS FORMAS DEL MISMO DEFECTO — el hilo de la jornada

Un instrumento que **contesta por una razón que no es la que se está midiendo**:
① el guard de `profiles.email` nació DEFINER y **el apply salió VERDE** · ② la
RPC de invitación **no podía correr** y el cinturón dijo VERDE · ③ los `curl` de D
rebotaban con el guard **de la plataforma**, no el suyo · ④ **L-414**: un número
correcto contra el **predicado equivocado**, que casi le hace shipear a D un corte
`fail-closed` sobre dos titulares reales · ⑤ **L-414 al revés, 24 h después**: un
literal que era **mi `COALESCE`**, no el dato — *el alias engaña de un lado y el
coalesce del otro; los dos convierten «cómo lo mostré» en «qué había»* · ⑥ **un
`308` uniforme para TODO** —incluido `/`— que sin `-L` se lee como «nada roto».

⇒ **Las seis dieron el color esperado. Ninguna contestó la pregunta que se creía
hecha.** *No alcanza con mirar el color: hay que preguntar quién lo produjo.*

### Poda firmada, ejecutada

El **React Native DevTools huérfano de casi 5 días** en `s100-d`: muerto (4
procesos) y worktree podado, **con la rama intacta en el mismo SHA `30f8d736`**.
*Y al regenerar `router.d.ts` se levantó Metro — se mató en el acto, porque
dejarlo vivo era repetir exactamente lo que se estaba podando.*
