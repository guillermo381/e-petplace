# S112-D · PARTE DE LA PISTA — el estado, para retomar sin reconstruir

> **Escrito ANTES de compactar.** Lo que vive sólo en la conversación muere con
> ella. Acá está el estado; todo lo demás se re-mide.
> **Territorio:** `packages/mensajeria` (+ dos cruces autorizados, §3).
> **Worktree:** `/Users/guillo381gmail.com/proyectos/ePetPlace/e-petplace-s112-d`
> **Rama:** `pista/s112-d` · **tip `b0bd94f6`** · árbol limpio · local = origin por SHA.

---

## §0 · 🔴 LO PRIMERO AL RETOMAR

**A ya mergeó 5 de mis 6 commits a `origin/main`. FALTA `b0bd94f6`** — medido con
`git merge-base --is-ancestor <sha> origin/main`, uno por uno.

> ### Y es justo el que más importa: es la corrección que le dice a C **que NO hardcodee la versión del acta.** Si se pierde, C monta la pantalla de firma contra una versión escrita en el código.

**Primer paso ejecutable: NINGUNO MÍO — todo cerrado.** ✅ *Cierre verificado el
1-sep 18:xx: **los siete commits están dentro de `origin/main`**, medidos uno por
uno; A los verificó por su lado antes de mergear y encontró que **dos faltaban**
—`b0bd94f6` y `2ade4079`— aunque ya había mergeado mi tip declarado.*

> ### 🔴 LA LECCIÓN QUE ESO DEJA — **vive en `L-481` junto a la del árbol: son las dos puntas de lo mismo.** Es de la mesa y no de una pista: **mergear «el tip» de una rama NO garantiza que entre todo lo que el otro cree haber entregado.** La ancestría se corre **por COMMIT, jamás por rama**. Si A no medía uno por uno, la corrección del acta se perdía **y C montaba la pantalla de firma hardcodeando la versión** — que es exactamente el daño que esa corrección existe para evitar. *La entrega se declara por rama; la recepción se verifica por commit.*

**Pin de estado de la base al medir todo esto:**
**594 migraciones aplicadas · última `20260907760000_s112a_compuerta_acta_exige_vigencia`** ·
local 595 (la de más es `…780000_lector_elegibilidad_guarderia`, de A, en vuelo).
⚠️ **La base se movió CUATRO migraciones durante la sesión y eso me invalidó una
entrega.** Si el ledger no arranca en 594, **se re-corre el arnés**.

> ### ✅ Y EL PIN SIRVIÓ PARA EXACTAMENTE ESO. Cuando A fue a aplicar el barrido **el ledger estaba en 595, no en 594** — así que **re-corrió el arnés** en vez de confiar en mi evidencia. **Verde, residuo 0.** *Un pin de estado no es prolijidad: es lo que convierte «mi arnés dio verde» en una afirmación con fecha de vencimiento legible.* **Al cerrar, `main` lleva 599 migraciones** — se movió otras cuatro después.

---

## §0bis · RONDA DEL LOOP DE ADOPCIÓN (2-sep) — lo entregado y lo NO probado

**SHA `7b311d9a`** · nueve archivos en `docs/loop/buzon/` · **nada aplicado, nada
numerado** · residuo 0 verificado tras cada corrida.

| # | qué | estado |
|---|---|---|
| D1 | **Los cinco avisos del vertical (N3)** — migración + reversa ANTES + arnés **10/10** | entregado a A |
| D2 | **Anti-abuso N1** | ⛔ **NO lo escribo: es de A**, adentro de `crear_solicitud_adopcion` (A7). Acordado por buzón — el guard lejos de su única puerta no sirve |
| D2b | **La purga que clasifica** — cura de MI pieza aplicada — arnés **6/6, dos rojos** | entregado a A, aprobado por ella con tres condiciones, las tres cumplidas |
| D3 | **Prueba del cron** — arnés **4/4** de la mitad (b) | 🔴 la mitad (a) **NO se puede probar hoy** |
| D4 | **Addendum de rutas para C** | entregado |

### 🔴 LO NO PROBADO, con su razón y su fecha

**Que el scheduler INVOQUE al job 48.** No es negligencia: **no se puede antes de
las 14:00 UTC del 2-sep**. Medido alrededor: **289.002 corridas en la casa, la
última hacía 11 segundos**, y **de los 28 jobs el único que nunca disparó es el
mío** — o sea que el mecanismo funciona para 27 de 28 y al 48 sólo le falta la
hora. **Cero filas en `cron.job_run_details` para el jobid 48 después de las
14:00 UTC es un rojo real, no una espera.**

### Lo que le debo a nadie y lo que esperan de mí

**Debo: nada.** **Espero de C:** confirmar la ruta del portal (`/adopcion/solicitud/<id>`
es propuesta mía — su portal no tiene rutas de adopción todavía). **Espero de A:**
número + aplicación de las dos migraciones, y **sus dos hallazgos de A10**.

### 🔴 DOS TRAMPAS QUE ENCONTRÓ EL ARNÉS CHOCANDO — para A10

1. **Son DOS constraints, no una.** `chk_cierre_coherente` sólo admite
   `cerrada_en` para `aceptada` y `declinada` ⇒ **`desistida` no se puede escribir
   cerrada**, y sin `cerrada_en` la purga **jamás la ve**. Tocar sólo el CHECK del
   estado deja el borrado a 90 días incumplido **y en silencio**.
2. **`uq_solicitud_viva` ya existe** — la mitad «una activa por animal» de N1 ya
   tiene índice; falta el guard tipado que EXPLICA (`L-424`).

### El CHECK de A sobre MI tabla: correcto, y ciego al pasado

A puso `chk_no_apagar_lo_inapagable` en `user_notificacion_prefs` y declaró que
*«el día que otra categoría pase a no-apagable, se entera solo»*. **Medido: falso.**
Un `CHECK` que llama a una función se evalúa **cuando se escribe la fila**, jamás
cuando cambia la tabla que la función consulta.

```
VERDE A · el CHECK frena apagar seguridad_cuenta hoy
🔴 ROJO C · la fila vieja SOBREVIVE apagada sobre una categoría ya inapagable
VERDE D · una fila NUEVA sí la frena
```

**Y no es teórico: hay una fila real con `operacion` apagada.** Curado con un
trigger en el CATÁLOGO que **rebota nombrando categoría y conteo**, con el
`DELETE` exacto en el `HINT`. **Frena y no corrige solo a propósito:** encender
una preferencia que alguien apagó es un acto de consentimiento, no una limpieza.
Arnés **6/6, 1 rojo producido** (SHA `eebe0f47`). **El cambio de A no se
revierte: se completa.**

### 🔴 DOS INSTRUMENTOS MÍOS MINTIERON EN LA MISMA TANDA

**① Un assert hardcodeaba «1 persona»** y la base tiene filas reales. *Un assert
que hardcodea un número mide mi supuesto sobre la base, no la pieza.* Ahora se
deriva.

**② Mi lector imprimía valores SIN nombre de columna**, y el CLI devuelve las
claves en **orden alfabético, no el del `SELECT`**. Leí posicionalmente y **creí
por unos segundos que una migración mía había commiteado y que había borrado la
preferencia de una persona real.** Re-medido con nombres: **residuo 0, nada
commiteó, la fila intacta.**
⚠️ **Y la trampa ya estaba escrita en mi propio operativo, de ayer.** La pisé
porque escribí el lector de cero sin consultarlo. *Tener la lección no alcanza si
no se lee antes de construir el instrumento.*

### `verify:rutas-de-aviso` — el gate, y cazó el defecto real en su primera corrida

`scripts/verify-rutas-de-aviso.mjs`, cableado en `package.json` (SHA `d29fee31`).
**Del lado servidor**, porque el que sabe qué es emitible es el motor.

**Su primera corrida real dio ROJO sobre `/guarderia/`** —emitible por
`_guarderia_aplicar_acto`, ausente de las dos listas— **sin que nadie le dijera
qué buscar.** Con la ruta agregada, verde.

**Tres salidas: 0 verde · 1 rojo · 2 NO CONCLUYENTE.** Nunca verde por vacío:
lista ausente o vacía ⇒ 2 · censo en cero ⇒ 2 · **y el punto ciego RUIDOSO** —un
productor que nombra `'ruta'` y no rinde ninguna **sale nombrado con 2**, probado
rompiendo el regex a propósito.

**El control positivo no comparte el supuesto del extractor:** `/guarderia/` salió
de leer **a mano** el cuerpo de la función. Si el regex se rompe, el control cae.

**Y cubre una forma que todavía no existe:** mis emisores **delegan** en
`_adopcion_ruta`; sin esa rama el gate habría dicho NO CONCLUYENTE **el día que A
los aplique**. Las cuatro formas probadas una por una.

⚠️ **Hoy sale 2**: las listas blancas viven en `pista/s112-c`, no en `main`. **No
va al hook hasta que estén.**

### 🔴 LOS TRES ARNESES ROMPIERON AL RE-CORRERLOS, Y LAS DOS CAUSAS ERAN MÍAS

**Elegía el fixture por conveniencia** —`LIMIT 1` sobre «mascota con familia», y
el miembro válido lo buscaba **aparte**—. **Pasó dos veces por suerte**; a la
tercera cayó en una familia sin miembro vigente. *Un fixture elegido por una
condición más débil que la que la prueba exige falla un día cualquiera, y su rojo
no se distingue de un defecto real.* Y **A6 sembró**: `uq_publicacion_viva_por_mascota`
rebota la segunda publicación. Ahora se elige por las tres propiedades juntas, y
**el aborto dice cuál falta y cuántos candidatos había.**

**10/10 · 6/6 · 4/4 contra el ledger 602.**

### 🔴 UNA AFIRMACIÓN MÍA SALIÓ FALSA Y OTRA PISTA CONSTRUYÓ SOBRE ELLA

Escribí *«estos cinco avisos son los primeros productores de `ruta`»*. **Falso.**
`_guarderia_aplicar_acto` ya la emite en código vivo. Lo cierto era sólo la mitad
que medí: **0 intenciones con la clave = «ningún productor CORRIÓ y emitió una»,
jamás «ningún productor emite una».** *Medí el efecto y lo declaré como un hecho
sobre la causa.*

**Y no se quedó en mi documento:** C razonó bien desde mi número y dejó
`/guarderia/` **fuera** de su lista blanca nueva ⇒ el primer acto de guardería
habría llegado y **no habría navegado**. Se lo avisé con el censo hecho **por la
causa** —quién PUEDE emitir— con sus dos controles: `_voz_notificacion` excluido
a mano, y **cero escritores de `notificacion_intencion` fuera del motor**, que es
lo que hace que el censo CIERRE en vez de acotar.

**Corregido en los dos documentos entregados, no sólo en el chat** — C puede
releer el addendum mañana y el chat no está ahí.

### Lo que aprendí en esta ronda

- **Mi propia pieza aplicada omitía en silencio.** La purga decía `estado =
  'declinada'` y el founder firmó *«declinada o desistida»*. **La cura no fue
  agregar el estado: fue que la puerta no pueda ignorar lo que no conoce.**
- **El rojo que me pidió A salía VERDE, y eso era lo correcto.** Se lo dije en vez
  de fabricar el rojo que ella esperaba: *si su A10 tuviera que acordarse de tocar
  mi función, la cura no habría curado nada.*
- **Un arnés que corre en silencio no probó nada.** El mío salió «sin error» y sin
  una palabra, con `RAISE NOTICE`. Pasó a imprimir en tabla temporal (`L-321`).
- **El pin de estado cobró por segunda vez:** A1 aterrizó mientras trabajaba y
  trajo `ingresado_en` NOT NULL. Lo agarré porque **volví a medir antes de sembrar**.

---

## §1 · LO ENTREGADO — qué, dónde, y en qué estado

| # | qué | dónde | estado |
|---|---|---|---|
| 1 | Contrato del hilo para C | `docs/loop/buzon/S112-D-para-C-CONTRATO-DEL-HILO.md` | ✅ mergeado **con la corrección `b0bd94f6` adentro** |
| 2 | Veredicto del reloj para A | `…/S112-D-para-A-VEREDICTO-DEL-RELOJ.md` | mergeado |
| 3 | Los tres rojos (arnés RLS) | `docs/loop/S112-D-ARNES-ROJOS.sql` | mergeado |
| 4 | Cura del wrapper + gate | `packages/api/src/wrappers/adopcion.ts` · `scripts/verify-rebote-lleva-id.mjs` | mergeado |
| 5 | Migración del barrido **sin número** | `…/S112-D-para-A-MIGRACION-barrido-adopcion.sql` | ✅ **APLICADA por A como `20260907800000_s112d_barrido_diario_adopcion.sql`** — arnés **re-corrido** contra el ledger movido, verde, residuo 0 |
| 6 | Reversa (**escrita ANTES**) | `…/S112-D-para-A-REVERSA-barrido-adopcion.sql` | mergeado |
| 7 | Arnés del barrido | `…/S112-D-para-A-ARNES-barrido-adopcion.sql` | mergeado |
| 8 | Evidencia + pin de estado | `…/S112-D-para-A-EVIDENCIA-barrido-adopcion.md` | mergeado |
| 9 | Ficha de adjuntos, **sin número** | `…/S112-D-para-A-FICHA-ADJUNTOS-BORRADOR.md` | mergeado |

**Gates al cerrar** (⚠️ **con `node_modules` INSTALADO** — sin deps el `tsc` da 0
mintiendo): `verify:mensajeria` **50/50** · `verify:rebote-lleva-id` **10/10** ·
typecheck 0 en los cuatro paquetes (por el hook).

---

## §2 · COMPROMISOS ABIERTOS — con nombre

**LO QUE DEBO:** nada. Las tres entregas de la segunda vuelta están cerradas y
verificadas por SHA.

**LO QUE ESPERO DE A** (`e-petplace-78`) — *estado al cierre, confirmado por A
y verificado por mí contra el objeto*:
1. ~~**Mergear `b0bd94f6`**~~ ✅ **HECHO.** Los siete commits dentro de `origin/main`.
2. ~~**Número + `verify:censo`** para la migración del barrido~~ ✅ **HECHO** —
   `20260907800000_s112d_barrido_diario_adopcion.sql`, **arnés re-corrido** porque
   el ledger se había movido. Verde, residuo 0.
3. 🟡 **Plegar la voz a `_voz_notificacion`** — **sigue abierto, tomado por A** y
   copiado literal en `docs/loop/S112-A.md` §5. El bloque exacto está en el §5 de
   la evidencia. ⚠️ **Si la pliega, el barrido tiene que seguir llamando a la voz
   CON la mascota y a la intención SIN ella.** Unificarlas trae de vuelta el
   `descartada_sin_acceso`.
4. 🔴 **El lector de la versión del acta** (§4, hallazgo de C reforzado por mí) —
   **sigue abierto, tomado por A** en su punto 6, con el viaje redondo de la
   versión. *Es el único 🔴 que sobrevive a esta pista.*

**LO QUE ESPERO DE C** (`e-petplace-83`): nada bloqueante. Tiene el contrato y
sabe que **el hilo no es descubrible** (su `ADOPCION_ALCANZABLE = false`).

**B** (`e-petplace-ab`): sin compromiso cruzado.

---

## §3 · CRUCES DE TERRITORIO, DECLARADOS

Dos, los dos **por pedido explícito del founder**, no por iniciativa:
- **`packages/api`** (territorio de A) — la cura de `crearSolicitudAdopcion`.
- **`package.json` raíz** — una línea: `verify:rebote-lleva-id`. *Punto de
  conflicto conocido con 5 pistas; si choca, es trivial.*

---

## §4 · LAS MEDICIONES QUE CUESTA REHACER

Todas contra la base viva, **cada cero con su control positivo**.

| qué | valor | control |
|---|---|---|
| `pg_cron` · jobs vivos · **de adopción** | sí · **28** · **0** | el 28 es el control |
| adjunto en `adopcion_mensaje` | **0** | **2** en `evento_archivo_adjunto` |
| bucket **privado** de adopción | **0** | **8** privados en la casa |
| `adopcion-fotos` | **existe y es `public: true`** | — |
| tabla `%formulario%` | **0** | — |
| `cerrada_por` en `adopcion_solicitud` | **0** ⇒ *declinada* y *desistida* son la MISMA fila | — |
| `_voz_notificacion` | **30.107 chars**, `ELSE → '{}'` (no inventa) | — |
| rama `adopcion_sin_respuesta` en la voz | **0** | **1** para guardería |
| escritores de `aviso_silencio_emitido_en` | **0** (1 lo nombra: el lector) | — |
| emisores de `adopcion_sin_respuesta` | **0** | **2** para `cita_recordatorio` |
| `notificacion_intencion` | **`UNIQUE (clave_dedup)` + `ON CONFLICT DO NOTHING`** | — |
| «90 días» para postulaciones en `docs/` | **0** (3 hits, ninguno es éste) | «5 días» **sí** está, línea 170 |
| `estado_vida` | `{activa, perdida, fallecida}` | — |

### 🔴 LOS TRES HALLAZGOS QUE NO SE PUEDEN PERDER

**① EL GATE 3 — la línea más importante de la migración.**
`registrar_intencion_notificacion` exige que el destinatario sea familia /
familiar autorizado / prestador con acceso **si le pasás una mascota**. **El
postulante de una adopción no es ninguna** (el animal es del refugio) ⇒ con la
mascota da **`descartada_sin_acceso`** y *la familia nunca recibe el aviso*.
**No falla: descarta en silencio.** Probado en los dos sentidos.
**Consecuencia obligada:** el GATE 1 (memorial) **sólo corre si hay mascota** ⇒
con `NULL`, **la exclusión del memorial es de la consulta**, y va en su `WHERE`.

**② EL SELLO NO ES LO QUE EVITA EL DUPLICADO.** *(Corrige lo que yo mismo
reporté en la primera vuelta.)* Sin `aviso_silencio_emitido_en` la consulta
**devuelve la fila todos los días**, pero la intención **no se duplica**: la
frena el `UNIQUE (clave_dedup)`. **El sello evita el trabajo diario y deja la
marca legible; el índice es el piso.** Las dos capas de `L-424`.

**③ EL ACTA — TERCERA POSICIÓN, y las dos primeras fueron mías y erradas.**
texto ✅ (`acta_adopcion` v1 vigente, 5350 chars) · motor ✅ (`p_acta_version`) ·
wrapper ✅ (`actaVersion: number`, **requerido** — `actaCodigo?` opcional al lado
prueba que es deliberado) · **lector de la versión 🔴 0** · **`firmar_acta_adopcion` 🔴 0**.
**Censo ancho para que el 0 CIERRE** (`L-437`): 4 funciones con `acta`, **las
cuatro de guardería** ⇒ no existe con otro nombre. `apps/`: **0** `actaVersion`
(control: `publicacionId` sí aparece).
⇒ **Hoy la pantalla tendría que hardcodear la versión**, y el día de la v2
firmaría contra la v1 **compilando**. *Motor sin puerta en su forma sutil: la
función pide un dato que sólo el servidor sabe y nadie devuelve.* **Es de A.**

---

## §5 · LO NO PROBADO Y LO NO CONSTRUIDO — declarado, no omitido

**No probado:**
- **que el cron DISPARE.** El arnés verifica que `cron.schedule` **deja el job
  agendado**; que corra se ve en `cron.job_run_details` **después de aplicar**.
- **que una push llegue a un teléfono.** Esto deja la **intención**.
- **la voz en pantalla.** Nadie la vio en aparato.
- **el traspaso punta a punta.**

**No construido, con su razón:**
- **El adjunto de imagen** (ítem 14). **No se pudo producir su rojo: no hay
  puerta.** Se reporta la ausencia, jamás un verde.
- **El borrado a 90 días NO tiene letra escrita** — se construyó porque **el
  founder lo firmó en la adenda**, con su forma: borra la **identidad**, no el
  hilo.
- **`_voz_notificacion` NO se tocó** — 30k de `CASE` compartido; reescribirlo
  desde un worktree que no puede aplicar ni medir **es cómo se pisa a otra pista**.

---

## §6 · LO QUE ME PASÓ A MÍ, porque es lo que más cuesta reaprender

**Siete fallas. Cinco donde el instrumento no podía ver, una donde NO HUBO
instrumento, y una de árbol:**

1. `0` buckets — **worktree sin linkear a Supabase**. Los `db query` van desde
   el worktree **principal**; el link vive en `supabase/.temp/`, sin trackear.
2. `0` matches de grep — **zsh se come `--include` sin comillas**.
3. `0` errores de `tsc` — **worktree sin `node_modules`**. *Un `0` ciego y un `0`
   legítimo son indistinguibles al leerlos.* Me frenó **el hook**, no mi cuidado.
4. `cita_archivos` como control positivo — **no es una tabla, es un bucket**.
5. `vigente_desde` — **adiviné el nombre de columna**.
6. 🔴 **«el acta no está cargada» — NO LO MEDÍ, LO DEDUJE DEL CANON DE S111.**
   Era cierto al cerrar S111 y dejó de serlo hoy. **`L-166`: un dato heredado
   del canon se lee igual que uno medido, y por eso es peor.** **Ningún gate lo
   caza**: sólo lo caza volver a preguntarle al objeto algo que ya creías saber.

7. 🔴 **EDITÉ MI PARTE PARADO EN `main`, EN EL WORKTREE DE A.** Un `cd` con
   fallback me dejó en el árbol primario y no lo miré: escribí un archivo mío,
   sí, **pero en el árbol donde A trabaja**. *No costó nada porque el árbol de A
   estaba limpio en ese instante — o sea que **no me salvó el método, me salvó la
   suerte**.* Es la clase exacta del índice compartido (precedentes S63 y S82):
   con WIP de A ahí, mi archivo se iba dentro de un commit suyo, o el suyo dentro
   del mío. **Curado sacando el cambio por parche, restaurando el árbol de A a
   limpio y verificando con `git apply --check` que mi rama tuviera la misma
   base** — jamás copiando el archivo a ciegas.

> ### 🔴 La regla que deja — ☠️ **YA NO VIVE ACÁ: es `L-481` en `DEUDAS_CANONICAS.md`, depositada por A con mi literal adentro.** Se la deja escrita porque es el relato de cómo se descubrió, **no porque sea su casa**: la ley se lee allá, junto a su gemela. *Lo que la volvió ley y no aviso es que le faltaba esa mitad a la regla 85 de la casa — tener worktree no alcanza si nada verifica que estás parado en él.* Y es de worktree y no de git: **la ruta se verifica, no se asume.** `git rev-parse --show-toplevel` **contesta el árbol donde estás parado, no el tuyo** — así que confirma que existís, jamás que estás en tu lugar. Lo único que lo dice es **`git branch --show-current` contra tu rama**, y va ANTES de escribir, no antes de commitear. *Yo lo corrí antes de commitear: por eso lo encontré, y por eso lo encontré tarde.*

> ### Y el corolario que ordena a las seis primeras: sobre el MISMO hecho afirmé de más DOS veces —«no hay acta» (falso) y «se puede montar» (demasiado)—. **Las dos veces la medición la trajo otro.** Lo que funcionó no fue mi cuidado: fue que C no me creyó.

---

## §7 · OPERATIVO — para no redescubrirlo

- **`db query` SIEMPRE desde el worktree principal** (`…/e-petplace`), no desde
  el mío: el mío **no está linkeado** y devuelve `LegacyProjectNotLinkedError`,
  que sin mirar el crudo **se lee como un `0`**.
- **`pnpm install --frozen-lockfile` en el worktree** antes de creerle a un
  typecheck. **Todo número de typecheck se reporta declarando si había deps.**
- **Correr el arnés del barrido** (prueba el texto REAL de la migración):
  ```bash
  cd docs/loop/buzon && { echo "BEGIN;"; \
    sed '/^BEGIN;$/d; /^COMMIT;$/d' S112-D-para-A-MIGRACION-barrido-adopcion.sql; \
    cat S112-D-para-A-ARNES-barrido-adopcion.sql; echo "ROLLBACK;"; } > /tmp/run.sql
  # y desde el worktree PRINCIPAL:
  npx supabase --experimental db query --linked --file /tmp/run.sql
  ```
  **El verde es MUDO** (el CLI se come los `NOTICE`) ⇒ **se falsifica antes de
  creerle.** Las dos falsificaciones que ya dieron rojo: sacar el filtro de
  memorial (`→ avisó 2 en vez de 1`) y meter `aceptada` en la purga
  (`→ anonimizó 2 en vez de 1`).
- **Direcciones:** A `e-petplace-78` · B `e-petplace-ab` · C `e-petplace-83`.
