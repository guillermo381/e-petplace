# S112-A · PARTE DE LA PISTA A (conducción + motor + docs)

> **Se lee ANTES de retomar.** Estado al 2-sep-2026. `main` se mide, no se cita:
> `git rev-parse --short HEAD` en el árbol primario.

## §1 · IDENTIDAD Y TERRITORIO

Worktree `/tmp/claude-501/wt-s112-a`, rama `pista/s112-a`.
**Única que escribe `main` y `docs/`, aplica migraciones y asigna números.**
Contexto entero: `docs/loop/S112-LOOP-ADOPCION.md`.

⚠️ **Antes de la primera ESCRITURA**, no antes de commitear: `git branch --show-current`
contra la rama propia (`L-481`). `--show-toplevel` confirma que existís, jamás que
estás en tu lugar.

## §2 · LO APLICADO (todas con reversa escrita ANTES y cinturón con rojo producido)

| # | qué | cinturón |
|---|---|---|
| `20260907880000` | **A1 · el adoptable completo** — 9 → 26 columnas · convivencia en 3 estados · 5 estados de publicación · trigger de sincronía | 8 brazos · 4 rojos |
| `20260907900000` | **A2 · los lectores** — vista pública por lista blanca · keyset con clave de orden completa · ficha en un viaje · tabla de fotos | 6 brazos · 3 rojos |
| `20260907920000` | **A2b · storage** — la policy de anon estaba MUERTA · el refugio no podía subir · anon se bajaba el catálogo | 5 brazos · 7 rojos |
| `20260907940000` | **A3 · los seis meses** (OM 019 art. 6.7) | 7 brazos · 5 rojos |
| `20260907960000` | **A4 · el refugio** + la cura del traspaso | 5 brazos · 4 rojos |
| `20260907980000` | **A2c · el eje de salud** + `esterilizado` a 3 estados en la FUENTE | 6 brazos |
| `20260908000000` | **A2d · `obtener_mis_adoptables`** — el portal ve sus borradores | 2 brazos · 2 rojos |
| `20260908020000` | **A2e · `ruta` en la campana** | 2 brazos |

**A6 · la siembra corrió por las puertas reales** (jamás por INSERT): Luna, Nube,
Tito publicadas · Bruno publicado **con compromiso** (cachorro) · **Kira rebotó**
con `adoptable_no_esterilizado` y quedó en borrador.
Cuenta `guillo381+refugio@gmail.com` / **`Ni7131711`** · familia `2121bc20` ·
cuenta comercial `80c41ac7`.

## §2-bis · LO APLICADO DESPUÉS (segunda tanda)

| # | qué | cinturón |
|---|---|---|
| `20260907940000` | **A3 · los seis meses** · `20260907960000` **A4 · el refugio** + la cura del traspaso | 7+5 brazos · 9 rojos |
| `20260907980000` | **A2c** · `esterilizado` a 3 estados **en la fuente** | 6 brazos |
| `20260908000000` `20260908100000` | **A2d/A2g** · el portal ve sus borradores y puede LEER uno | 5 brazos |
| `20260908020000` | **A2e** · `ruta` en la campana | 2 brazos |
| `20260908040000` | **A7 · el formulario** con esquema cerrado + N1 + **la purga que clasifica columnas** | 9 brazos · 6 rojos |
| `20260908060000` | **A2f** · la policy de SELECT del bucket (el borrado **mentía**) | 2 brazos |
| `20260908080000` | **A8 · `D-485`** — los helpers aprenden la familia | 4 brazos |
| `20260908120000` `20260908140000` `20260908200000` `20260908220000` | **A9 · el acta y la firma** + tres curas mías del mismo día | 16 brazos |
| `20260908160000` `20260908180000` | **A10** reportar/desistir/memorial · **A2h** la ventana cerrada | 9 brazos |

**Ejercido por camino real (E):** el traspaso completo sobre Luna con sus 2
eventos · §5.9 con los tres rebotes nombrados · N1 con la 4ª rebotada · el acta
rindiendo 5.121 caracteres con sus 4 faltantes nombrados · **el payload del OTP
sin el código, medido desde afuera**.

## §2-ter · EL ESTADO AL CIERRE DE LA RONDA 1

**RONDA 0 y RONDA 1 CERRADAS.** A1→A11 aplicadas, 22 migraciones mías, todas
con reversa escrita antes. Los nueve puntos de C montados. Las ocho piezas de B.

🟢 **E3 CERRADO — el primer traspaso real de la historia del producto**
(`F-2026-000054`): las dos firmas · el traspaso · el hito · y **el expediente
que el refugio cargó ANTES de la entrega viajó con el animal**, verificado por
la hora del evento. §0 paso 5 → paso 15, medido y no supuesto.

**La vidriera del founder:** Luna · Tito · Bruno · **Mica** publicados ·
**Kira** en borrador con su razón · **Nube adoptada de verdad** — que no estaba
en §0 y le da al founder una adopción completada para mirar.

🔴 **LO QUE C DECLARÓ Y VA AL ACTA: todo lo suyo es CONSTRUIDO Y NO EJERCIDO.**
Ninguna pantalla del vertical corrió contra la base ni en aparato: ninguna
solicitud enviada desde una pantalla, ninguna firma, ninguna foto subida,
ninguna push recibida. **Lo que falta ya no es construcción: es correrlo.**

🔴 **LAS FOTOS — DOS CAUSAS, UN SÍNTOMA, Y LA DISTINCIÓN IMPORTA.** La policy de
`anon` está curada y medida verde por E (bajó 62.582 bytes de una portada real).
**Y la vidriera va a abrir sin fotos igual, porque los cinco animales no tienen
ninguna.** *Si sólo se recuerda «la policy se curó», el recorrido falla y va a
parecer que la cura no sirvió.* **No se siembran: poner una ilustración de stock
como «ésta es Luna» es inventar evidencia.** Es el paso 4 del founder, y su
pantalla ya existe.

## §2-quater · EL CIERRE DEL LOOP — LOS CUATRO PUNTOS DEL FOUNDER

| # | qué | verificado contra |
|---|---|---|
| ① | **La purga** con `desistida` Y el formulario | `pg_proc` — y ahí saltó el rojo: la migración de D había re-creado la función y **se llevó la cura de A7** |
| ② | **`en_proceso` con productor** + 🔴 **su SALIDA** (una `aceptada` no tenía vuelta atrás, y aceptar sacaba al animal de la vidriera) | camino real · Bruno rescatado **por la puerta** |
| ③ | 🟢 **El acta no se firma con el animal en memorial** (firma del founder) · `no_concretada_fallecimiento` · voz de duelo bilingüe | cinturón con 4 rojos · el aviso falla si menciona otro animal |
| ④ | **Discriminador: los tres en NO ⇒ OTA** · `ADOPCION_ALCANZABLE` en el último commit | `pnpm-lock` sin mover · runtime 1.0.7 |

🔴 **DOS DEFECTOS MÍOS EN ESTA TANDA, los dos de la misma familia del día:**
la purga perdida (dos pistas curando la misma función por `CREATE OR REPLACE`
— *la última se lleva todo lo de la primera, y no falla al aplicar*) y **los dos
brazos de la voz CRUZADOS** (conté las anclas y no medí cuál era cuál: el aviso
más doloroso del vertical salía en inglés para una familia hispanohablante).

## §2-quinquies · 🟢 EL LOTE DE ADOPCIÓN, PUBLICADO (2-sep)

**Autorizado por el founder sobre `main 23867033`.** Anclas leídas del OBJETO
con `update:view --json`, jamás del texto del publish:

| | group | ancla | dirty |
|---|---|---|---|
| **cliente** | `46e2afd0-9ea6-40bb-b44b-ca48657899e4` | `23867033` | `None` |
| **prestador** | `a6fbd3d8-4694-4fff-a69e-643e543622eb` | `23867033` | `None` |

runtime **1.0.7** · canal **preview** · discriminador con los tres en NO ⇒ OTA.
**El 203 ms de la ficha queda DECLARADO, no relajado: el techo sigue en 200 y la
próxima sesión lo baja por plan o payload.** El servidor trabaja 2,3 ms y el
viaje cuesta 148, y C midió que esa ficha hace **un solo viaje** ⇒ los 3 ms no
salen quitando peticiones.

## §2-sexies · GUARDERÍA — la medición de la foto (pedido del founder, 2-sep)

**Documento entero: `docs/relevamientos/2026-09-02-s112a-MEDICION-GUARDERIA-FOTO-Y-ESTADOS.md`.**
Los dos titulares, porque son contraintuitivos:

🔴 **La familia LISTABA la foto de su animal y NO la podía bajar** — curado en
`20260908420000`. **Es la mitad de MOTOR de DOS defectos apilados con el mismo
síntoma**: la otra es que `obtener_media_de_mi_mascota` devuelve el **path
crudo** y la pantalla lo pinta como URI. *Con la policy curada sola, la foto
sigue sin bajar* — la firma es de `apps/` y es de C (`D-308`).

🟢 **El acta SÍ transiciona; la hipótesis se cae.** Hay 0 `en_guarderia` porque
**es un estado de PASO**: las dos estadías del founder estuvieron ahí **68
segundos**. ⇒ **el botón «Sacar foto» sólo existe entre «llegó» y «retornó», y
nada en la pantalla lo dice.**

## §2-septies · GUARDERÍA · lo medido con el JWT de demo-vet (Clínica Aurora)

**Cuenta del prestador de prueba: `guillo381+demovet@gmail.com` · prestador
`de680000-0000-4000-8000-0000000000e5`.** Su clave sale del keychain al momento
(`epetplace-siembra-s97`), jamás del repo.

**① LA FOTO — mitad de motor CERRADA. No hay edge**: la app sube a Storage y
`publicar_media_guarderia` registra. Ejercida: **3 rojos tipados**
(`no_gestionas_este_prestador` 42501 · `media_sin_etiquetas` ·
`mascota_sin_estadia_ese_dia`), verde, **idempotente**, la familia la lista,
**residuo 0** verificado en `guarderia_media` y `eventos_mascota`.

🔴 **La RPC NO exige `en_guarderia`** — publicó sobre una estadía `reservada`.
**El recorte del botón es de PANTALLA, no de motor**, y con los 68 segundos al
lado, esa puerta se abre y se cierra sola. **Es decisión de producto y va a la
mesa; ninguna pista la ensancha por su cuenta.**

**② EL LECTOR DEL DÍA YA ALCANZA.** `obtener_estadias_del_dia` devuelve `estado`
por animal **y sus cinco marcas de hora**. No hubo que agregar nada. Hoy sobre
Aurora: **3 `en_guarderia` · 1 `no_recogida` · 1 `reservada`** ⇒ *si la lista los
cuenta a todos como «están hoy», el rótulo es de la pantalla.*

**③ PEPE — LA PUERTA DE RESERVA ESTÁ CERRADA HOY.** Ejercida:
`reservar_dia_guarderia` con Pepe (ave) → **`mascota_no_elegible`**. Sus 22
estadías son **históricas**: nacieron antes del guard. *No es un agujero
abierto; son datos que la puerta de hoy ya no dejaría entrar.*

🔴 **Lo que SÍ queda abierto, y es de motor:** censo por causa **con los
comentarios stripeados** (leerlos como código da un falso verde, `L-170`) — de
las cinco puertas, **`comprar_paquete_guarderia` y
`cobrar_periodo_mensualidad_guarderia` no consultan especie**. La viva es la
segunda: **la suscripción `20d025ca` de Pepe está `activa`**, así que el día que
el reloj corra le crea estadías a un ave. **`D-1001` describía UNA puerta y son
DOS — y ninguna es la que la ficha nombraba.**

**④ G7/G8.** `cancelar_mensualidad_guarderia` **existe con wrapper** — mi
mención de sólo `reactivar` estaba incompleta. Medida: **cumple §12.2**
(`corre_hasta`, **`reintegro:false`**, conserva los días creados, idempotente);
rojo **403 `no_sos_de_esta_familia`** con el plan intacto como control.
**DeUna: no hay nada que cancelar** — no existe tabla de intención ni de link
mensual, y `cancelar` no la nombra.

El lector ganó `mascotaNombre`/`mascotaEspecie`/`mascotaFotoUrl` (`…440000`) y
`especiesElegibles`/`especieAplica` (`…460000`). **La foto va como RUTA**:
Postgres no puede firmar una URL de Storage — se firma en pantalla
(`resolverUrlFoto`, `D-308`). **`especieAplica` contesta SÓLO la especie**: el
guard real tiene dos cláusulas y juntarlas haría que la pantalla dijera *«es
para perros y gatos»* **sobre un perro en memorial**. Discriminador vivo:
**Pepe `false`, las Kiras `true`**.

## §2-octies · 🔴 EL ESTADO AL CIERRE — lo que hay que saber sin leer nada más

**`main 265879de075198e1964cff95ca55e3d0baada88f`** · 649 migraciones local = remoto
· árbol limpio · **5 typechecks en 0** · **los DIEZ gates verdes**.

### LO PUBLICADO (autorizado por el founder, anclas leídas del OBJETO)

| lote | cliente | prestador | ancla |
|---|---|---|---|
| **3** | `fe5b911b-02ae-48c8-b387-e5c7f889e631` | `267dbff8-0e4f-4c7d-92bd-f6061753ba08` | `84e1add5` |
| **4** | `a3ca0121-88b7-401d-8524-57a42a546e59` | `c60d81f3-c3c7-48cd-a49c-5ca70d3f0534` | `8bd1ce4e` |

runtime **1.0.7** · canal **preview** · los cuatro `dirty: None`.
Pie de Cuenta: lote 4 = `01a06586` (cliente) · `01a06587` (prestador).

### 🔴 EL LOTE 5 ESTÁ ARMADO Y **NO PUBLICADO** — espera la palabra del founder
SHAs adentro: **C `4ae179bd`** (incluye `4a0313a4`, la cura del crash) · **B
`b9655563`** · **E `b9ad78d2`** · las de A.

**Discriminador: OTA, con UN asterisco medido.** `expo-clipboard` es
**dependencia NUEVA del prestador y su binario 1.0.7 NO la tiene**; la usa el
botón «Copiar» de `pantalla-caida.tsx:164`. **No rompe** —`require` perezoso
dentro de un `try`, con su razón escrita— **pero en el prestador ese botón dice
«no se pudo copiar» hasta la próxima build nativa.** En el cliente funciona.
⚠️ **`verify:hoisting-nativo` da VERDE y no lo ve**: es su límite ① declarado
—mide declaración, no presencia en el binario—. **Lo cazó el discriminador.**

### 🟢 EL CRASH DEL HILO: DIAGNOSTICADO Y CURADO
**Era TDZ, no dato.** Los dos `useRef` estaban declarados DESPUÉS del `useMemo`
que los lee; el cuerpo de un `useMemo` corre durante el render y con Hermes el
`const` en TDZ compila a `var` ⇒ `undefined.current`. **Verificado curado por
CONTENIDO en `main`:** cliente declara 346 / lee 354 · prestador 290 / 298, y
**la prosa de A14 viajó con los refs** (condición de B).

Lo diagnosticaron B y C **en paralelo, con los mismos números**. Nace
**`verify:ref-antes-de-uso`** (C), verde con 62 archivos.

⚠️ **Ratifica la decisión de no publicar antes de capturar:** si el lote 5 salía
primero, el retiro de `useAnimatedKeyboard` **no habría curado nada** y
habríamos visto el mismo crash sin stack, buscando en el lugar equivocado.

### LOS GATES QUE A CABLEÓ AL HOOK EN S112
`verify:hoisting-nativo` (era de C, **salía `exit 0` SIEMPRE** ⇒ como gate decía
que sí pase lo que pase) · `verify:rutas-de-aviso` (**nació saliendo 2 por
fuente ausente y por eso NO se cableó entonces**; hoy verde con 4 emitibles) ·
`verify:ref-antes-de-uso` **le faltaba su entrada en `package.json`** — corría
y `pnpm` no lo encontraba: *motor sin puerta, en un gate*.

🔴 **Y un defecto que A metió y curó:** `rutas-de-aviso` salía **1** sin link de
Supabase y el hook lo leía como *«una push lleva a ninguna parte»* — **B iba a
chocar y a buscar una ruta huérfana inexistente**. Ahora sale **2**, probado sin
link y con link.

### FICHAS Y LECCIONES DEPOSITADAS
`D-1005` (el mapa de glifos vivía dentro de una app) · `D-1006` (los `nodo*` con
anatomía vencida) · `D-1007` (🔴 **`hoy_local()` no mira la zona del prestador y
58 funciones repiten la constante** — la APP ya pide el día correcto, **el MOTOR
no**) · `D-1008` (🔴 **un error que tira una pantalla no llega a ningún lado**:
cero Sentry, cero tabla, cero `captureException`) · **`L-488`** (un censo
estático entrega candidatos, no veredictos) · **`L-489`** (un rojo verdadero con
fecha, leído después como hecho presente).

### 🔴 LO QUE ESPERA AL FOUNDER Y NADIE MÁS PUEDE HACER
1. **Autorizar el lote 5.** Al publicar, **E corre su escalera sobre el bundle
   nuevo** (las dos listas y los cinco hilos) antes de su pasada.
2. **El gate por ícono de los CINCO glifos** de la escalera de adopción.
3. **El ochre del `falta`** en la ficha del adoptable.
4. **Cancelar la suscripción de Pepe** (el ave con plan de guardería) desde la
   app. El guard nuevo ya impide que el reloj le cree estadías.
5. 🔴 **ÍTEM DE CALENDARIO:** la primera emisión del reloj de silencio **exige
   una solicitud NUEVA que nadie conteste durante CINCO DÍAS** — las tres vivas
   no pueden dispararlo nunca. *Un no-evento no se prueba ejecutando: se prueba
   esperando.*
6. **Los cinco animales no tienen fotos** (su paso 4 del §0).
7. **Nada de los lotes 3, 4 y 5 se verificó en aparato.**

### DATOS OPERATIVOS QUE CUESTA VOLVER A MEDIR
- **Clínica Aurora** = `guillo381+demovet@gmail.com` · prestador `de680000-0000-4000-8000-0000000000e5`
- **Refugio de prueba** = `guillo381+refugio@gmail.com` (clave propia, la dio el
  founder) · cuenta `80c41ac7-c39e-44b2-9d70-fd4f9816b521` · prestador `ea9a88bf`
- **La clave compartida** sale del keychain **al momento**:
  `security find-generic-password -a siembra -s epetplace-siembra-s97 -w`
- 🔴 **`hoy_local()` usa `America/Guayaquil` y a las 23:00 difiere del UTC del
  teléfono** — pedir el día equivocado **devuelve filas, no error**.
- **La bitácora se prueba con Jack (5 chips) o Pepe (2)** — con Kira se ve un
  cero que parece defecto.
- **`en_guarderia` es un estado de PASO**: las estadías del founder estuvieron
  ahí **68 segundos** ⇒ el botón de la foto vive en esa ventana y nada lo dice.

## §3 · LA COLA VIVA, EN ORDEN

0. 🔴 **LO PRIMERO AL RETOMAR:** las **dos puertas sin especie**
   (`comprar_paquete_guarderia` · `cobrar_periodo_mensualidad_guarderia`) con
   la suscripción de Pepe `activa` — es el rojo vivo de `D-1001`, ensanchado.
0-ter. ~~mitad de motor de la foto~~ — **CERRADA** (§2-septies). Sin medir aún:
   que la foto **baje por HTTP** (verifiqué el predicado) · la **policy de
   UPDATE ausente** en el bucket.
0-cuater. ~~GUARDERÍA, mitad de motor pendiente:~~
   la **RPC que registra la foto y su edge** desde el asiento del PRESTADOR.
   **Bloqueado por un dato del founder: cuál es la cuenta del prestador de
   prueba de guardería** (el refugio no sirve — no atiende estadías).
   Y sin medir: que la foto **BAJE de verdad** por HTTP con la policy nueva
   (se verificó el PREDICADO, no la descarga) · la **policy de UPDATE ausente**
   en el bucket.
0-bis. ✅ ~~EL LOTE ARMADO~~ — **PUBLICADO** (ver §2-quinquies).
   `ADOPCION_ALCANZABLE` encendida · discriminador con los tres en NO ⇒ **OTA**
   · las cinco pistas dentro · árbol en 0 · gates: 4 typechecks en 0 ·
   `diseno` 62 · `razon-muda` 141 · `vio-todo` · `sin-byte-nul` 3709.
   ⚠️ **La ficha da 203 ms contra su techo de 200, y NO se relaja: se declara.**
   El servidor trabaja 2,3 ms y el viaje cuesta 148 ⇒ ningún índice lo baja.
1. ~~**A7 · EL FORMULARIO** — `crear_solicitud_adopcion(publicacion_id, respuestas, aceptacion_id)`.
   Esquema cerrado por CHECK; cualquier clave fuera rebota. **N1: `uq_solicitud_viva`
   YA existe** (medición de D) ⇒ falta el techo de 3 **y el guard tipado que explica**.
   *Destraba C5.* **D confirmó que no lo escribe él.**
2. **A8 · `D-485`** — E lo probó por camino real: la familia destino ve **0 filas**,
   y `user_id` queda apuntando al REFUGIO. La asimetría: el UPDATE de `mascotas`
   SÍ lee familia, el SELECT no. **La cura son 4 helpers + 3 policies, no 81 puertas.**
3. **A9 · EL ACTA Y LA FIRMA** — monta LA MISMA `evaluar_esterilizacion_adoptable`
   (la segunda puerta de A3, ya escrita).
4. **A10 · `reportar_publicacion`** (N2) · memorial · desistir.
   🔴 **Dos hallazgos de D que cambian la cura:** son **DOS constraints, no una** —
   `chk_cierre_coherente` sólo admite `cerrada_en` para `aceptada`/`declinada`, así
   que una `desistida` no se puede escribir como cerrada **y la purga jamás la ve**.
5. **A11** — aplicar las migraciones de D con su arnés re-corrido.
6. **Filtro por ETAPA** que resuelva el servidor (pedido de C; los umbrales viven
   en `cat_especies_perfil` y ningún lector los trae).
7. **RONDA 3** — encender `ADOPCION_ALCANZABLE` en el ÚLTIMO commit · discriminador · lote.

## §4 · LO QUE DEBO, CON NOMBRE

- **Al hook:** `verify:vio-todo` (de B) — ⚠️ **`verify:rutas-de-aviso` de D NO va al
  pre-commit todavía**: sale 2 (no concluyente) hasta que las listas de C estén en `main`.
- **La re-redacción de R17**: su justificación cita una premisa que el founder derogó
  el 2-sep (las piezas se firman montadas). *Una regla cuya razón está vencida le
  enseña a la próxima pista a hacer lo contrario de lo que se firmó.*
- **`razon-muda` a 141** · **§5-bis** en `LETRA_ADOPCION` · **§18** de la Política.
- **Las lecciones con el comando** — candidatas depositadas abajo.
- 🟠 **`authenticated` conserva SELECT sobre `v_adoptables_publicos`** (E lo nombró
  sin empujar). Se cierra **antes del lote**; queda declarado por si me olvido.

## §5 · AUTORIZACIÓN CONDICIONADA DEL FOUNDER (2-sep)

Publicar sin esperar otra palabra **si y sólo si**: E1 sin rojos abiertos · E2 dentro
de §6 · discriminador con los tres en NO. **Si cualquiera falla, se arma el lote y se espera.**

⚠️ **E2 NO cumple hoy**: ficha p95 **203 ms** contra techo de 200. Y su lectura importa
más que el pass/fail: **el piso de viaje es 146-155 ms y el servidor trabaja 2,3 ms** ⇒
ningún índice baja eso (`L-223`). **Es de la mesa**, no se «optimiza».

## §6 · CANDIDATAS DE LECCIÓN (sin número — se depositan con `pnpm proximo:ficha`)

1. **(E)** *Un predicado de RLS medido desde un asiento que RLS no alcanza no mide la
   policy: mide otra pregunta parecida — y falla de más Y de menos.* Le cobró en las
   dos direcciones el mismo día. Correctivo: `SET LOCAL ROLE` **y al lado imprimir qué
   ve ese rol de cada tabla que el predicado menciona**.
2. **(D)** *«Nunca corrió» no prueba «no existe».* Midió el efecto y lo declaró como
   hecho sobre la causa — `L-402` invertida. Y no se quedó en su documento: C razonó
   bien desde el número y dejó una ruta fuera de una lista blanca.
3. **(B)** *Un binario donde el mundo tiene tres estados.* **Tercera vez en el mismo
   vertical**: convivencia (S111), el código de firma (B4), la salud (hoy). Y su
   corolario: **el eje derivado cura al lector y deja al escritor binario.**
4. **(A)** *Curar no es censar, y el rename es su caso puro:* A1 jubiló un estado
   mirando la tabla y no a quien la escribe. El traspaso **no podía terminar nunca**,
   con cero consumidores ⇒ ningún gate podía verlo.
5. **(A)** *Una cura que vive en un archivo no protege a la migración que se escribe
   después.* El mismo atajo de restaurar con `NULL` se repitió en la migración
   siguiente a la que ya lo había curado.

## §7 · TRAMPAS DE MEDICIÓN DE ESTA SESIÓN

- `npx supabase` **sólo funciona desde el árbol primario** (el worktree no está linkeado).
  `pnpm proximo:migracion` también.
- El `db push` a veces falla con `SASL auth` — es transitorio, se reintenta.
- Un cinturón que **muta datos reales** hace lo que vino a vigilar (`L-406`): el de A3
  eligió a **Thor, la mascota del founder**. **Siembra la tuya y deshacela.**
- `mascotas.estado_adopcion` es **NOT NULL**: restaurar con `NULL` aborta.
- Los arneses de D **imprimen en tabla temporal, no en `RAISE NOTICE`**: si no ves las
  filas, no midieron.
- **El commit va con pathspec y `git add -N` primero** para lo no trackeado.
