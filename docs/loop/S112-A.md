# S112-A · PARTE DE LA PISTA A (conducción) — escrito ANTES de compactar

> **Se lee ANTES que cualquier otra cosa al retomar.** Lo que no está acá se
> perdió con el contexto. Cada número dice contra qué se midió.

## ⓪ IDENTIDAD Y TERRITORIO

- **Soy A: conduzco.** Única que escribe `main` y `docs/`, aplica migraciones y
  asigna números de ficha/lección.
- **Worktree:** `/tmp/claude-501/wt-s112-a`, rama `pista/s112-a`.
  ⚠️ **No está linkeada a Supabase**: las migraciones se aplican corriendo
  `npx supabase ... --file /tmp/claude-501/wt-s112-a/supabase/migrations/<x>.sql`
  **desde el repo primario**, o mergeando a `main` y haciendo `db push` ahí.
- Las otras pistas: **B** `packages/ui` · **C** apps · **D** `packages/mensajeria`
  · **E** censos. Hablan por `SendMessage`; sus sockets viven en la conversación
  y **se pierden al compactar** — si hace falta, `ListAgents`.

## ① ESTADO OPERATIVO AL ESCRIBIR ESTO

| qué | valor |
|---|---|
| `main` | **`9f9c231b`**, en origin (verificado por SHA) |
| migraciones | **599**, local = ledger |
| typechecks | 4 en 0 · `verify:diseno` 62 · `verify:mensajeria` · `verify:rebote-lleva-id` 10/10 |
| **`verify:razon-muda`** | **RE-APUNTADO a (b), baseline 141 por ocurrencia** |
| **OTA cliente** | group `6aedf349-9580-4ca6-8965-650a482627fd` |
| **OTA prestador** | group `51a263b2-0c3b-4ecb-b37b-fccbd3bdc703` |
| ancla de los dos | **`fde8494d`** · `dirty=None` · runtime **1.0.7** · canal `preview` |

**EL LOTE DE GUARDERÍA YA SALIÓ, autorizado por el founder.** Lleva `D-1001`
con su pantalla, `D-1000`, y el `Boton` que dibuja su razón. **NO lleva**
Convivencia ni nada de adopción.

⚠️ **El pie de Cuenta dice `update`, no `updateId`** — esa cura se construyó
después del publish anterior y sigue sin viajar.

## ② NÚMEROS TOMADOS (no re-tomar)

`D-1003` cura del comando · `D-1004` adjuntos · `L-478` galería no es gate ·
`L-479` generado viejo acusa en falso · `L-480` heredado sin medir.

🔴 **El número se PIDE, no se elige: `pnpm proximo:ficha` · `pnpm proximo:migracion`.**
El comando viejo del canon estaba roto para los dos lados y se curó hoy
(`D-1003`). **Y el canon ya NO escribe números que no estén depositados** —
escribirlos envenena al instrumento; se cobró tres veces durante su propia cura.

## ③ LO QUE HICE, con su evidencia

1. **`D-1002` curada** — nace `scripts/proximo-numero-ficha.mjs`. Rojo: el
   comando daba `D-999` (⇒ chocaba) y su corrección obvia daba `D-2026`
   (⇒ saltaba 1024). Verde: dos tomas dan N y N+1.
2. **`D-1001` curada** — especie **y propiedad** en `contratar_mensualidad_guarderia`
   y `reactivar_*`, montando `_mascota_elegible_servicio` (la pieza que ya usaban
   las dos puertas del día suelto). **Censo de las SIETE puertas hecho.**
   Verde por camino real: ave→`mascota_no_elegible` · perro→`tarjeta_no_existe`
   · ajena→`no_access_to_mascota` · NULL→`mascota_requerida`.
3. **Lector para la pantalla:** `puede_contratar_guarderia(uuid)` +
   `puedeContratarGuarderia` — apaga el plan ANTES del botón sin copiar el criterio.
4. **Los tres textos legales cargados** (`terminos_refugio` v2 ·
   `condiciones_adopcion` v2 · `acta_adopcion` v1 PLANTILLA). Hash por trigger,
   texto **inmutable** por trigger, versiones viejas jubiladas y no borradas.
5. **La compuerta del acta exige `vigente`**, no sólo existencia.
6. **Punto 3 de adopción entregado a C:** `obtener_documento_vigente` ·
   `tengo_aceptado_documento` · `aceptar_documento_adopcion` + wrappers, y
   `crear_solicitud_adopcion` ahora rebota `condiciones_no_aceptadas`.
7. **La IP del consentimiento la pone el servidor** (la destapó C negándose a
   mandarla).
8. **Barrido diario de adopción de D aplicado** (`20260907800000`), arnés
   re-corrido porque el ledger se había movido.
9. **Rol de refugio + cuenta de prueba del founder** (ver §⑥).

## ④ 🔴 LA COLA VIVA, EN ORDEN (reordenada por el founder)

**Próximo paso ejecutable: el PUNTO 5.**

1. **PUNTO 5 · EL ADOPTABLE COMPLETO Y SU FICHA** ← *acá se retoma*
   - Campos nuevos en el adoptable: **`ingresado_en`** (lo pide `destacado_espera`)
     · REMETFU · señas particulares · origen (`rescate` / `cesion` + fecha) ·
     estado vacunal · bono (**monto y destino**, §8.1 de los términos).
   - **`obtener_adoptable(publicacionId)` NO EXISTE** — hoy sólo está el plural
     `obtener_adoptables` de 10 campos (medido por C con delimitador y control).
     Tiene que traer: salud en **semáforo**, **convivencia en TRES estados como
     TEXTO** (jamás `boolean|null`: con nullable «no se sabe» y «nadie lo cargó»
     son el mismo valor), historia del rescate, ubicación aproximada.
   - **`destacado_espera` FIRMADO, opción ①**: tres destacados por **tiempo en
     rescate** con su razón redactada («lleva N meses esperando»); el resto por
     publicación reciente. Excluye memorial. Con filtro de convivencia activo,
     dentro de cada lista **primero los confirmados y después los «todavía no se
     sabe»**. **La edad va redactada desde el riel, nunca `fechaNacimiento`.**
   - **La regla de los SEIS MESES, firmada, en LAS DOS puertas** (transición a
     `publicado` **y** firma del acta): mayor de seis meses ⇒ esterilizado.
     Rojo primero: adulto sin esterilizar rebota en las dos; cachorro pasa con
     compromiso. **Es LA MISMA pieza en dos puertas.**
2. **PUNTO 2 · EL REFUGIO** — (b) `obtener_mi_cuenta_refugio` que **nombre el
   rol** y no haga CROSS JOIN de dos · (c) la rama refugio en
   `obtener_contexto_arranque` · (d) registro de negocios con contexto
   `registro_refugio` → resuelve a `terminos_refugio` **v2**, no a
   `terminos_professional` · (e) `publicar_adoptable` mira **estado de cuenta
   activa**, no sólo el rol. *(La (a) ya está hecha: ver §⑥.)*
3. **PUNTO 4 · EL FORMULARIO** — `crear_solicitud_adopcion` recibe `p_respuestas`
   (jsonb, **esquema CERRADO**: composición del hogar por rangos 0-5 · 6-12 ·
   13-17 · adultos, vivienda, otros animales, horas solo, experiencia, motivo)
   + referencia a la aceptación. **Rechaza todo campo fuera del esquema.**
   🔴 **Nada de nombres de menores: si llega, rebota.**
4. **PUNTO 1 · `D-485`** — las tres policies de SELECT de `mascotas` no miran
   `familia_id`. Curar el lector único (`user_tiene_acceso_a_mascota_como`) y que
   las policies pasen por él. Rojo con arnés y ROLLBACK. **Control: las 32
   mascotas con `user_id` se siguen viendo igual (divergencias 0 antes y
   después).** CURAR NO ES CENSAR: listar qué otras tablas colgadas de la
   mascota miran `user_id` y no familia.
5. **PUNTO 6 · EL ACTA** — `obtener_acta_adopcion` devuelve **versión + cuerpo**
   (viaje redondo; la app **nunca elige** versión) · `firmar_acta_adopcion` nace
   · cédula y domicilio **se piden EN EL ACTO de firmar** · la compuerta rebota
   **NOMBRANDO las variables que faltan** (lista, no código mudo) · tabla de
   firmas con expediente probatorio (usuario · rol · **código verificado en el
   acto** por correo · sello del servidor · **hash del texto RENDERIZADO** además
   del de la plantilla · IP · dispositivo · versión) · **el traspaso ocurre sólo
   con LAS DOS firmas**; con una, «esperando firma de …».
   **Fallback: SÓLO `microchip` y `REMETFU` tienen «si vacío»**; toda otra
   variable sin valor rebota nombrándola.
6. **PUNTO 7** — §5-bis en `LETRA_ADOPCION` (los 90 días, firmados) + la fila en
   §18 de la Política.
7. **LA SIEMBRA · PUNTO 2** (ver §⑦, guion completo).

## ⑤ COMPROMISOS ABIERTOS, por pista

**LE DEBO A C** (está quieta esperándome, no monta nada sin contrato aplicado):
- El **punto 5** (la ficha) — es su ②, y además **destraba a B**: el founder fijó
  que el gate de `Convivencia` es esa ficha.
- Después: formulario, acta.
- **Entrego pieza por pieza y por nombre, apenas esté aplicada. Nunca en paquete.**

**C ME DEBE / ya entregó:**
- **Entrega 1 MONTADA** (`1dad1bcc`): `app/legales/[codigo].tsx`, una pantalla
  para los tres documentos. Verificó mi contrato contra el objeto antes de montar.
- Cuando llegue el punto del portal, **revierte el revert** de
  `apps/prestador/src/app/adopcion/**` (`git revert 63054b7c`, una línea).
  🔴 **Eso vuelve a subir `razon-muda`** — pero ahora el gate está re-apuntado,
  así que hay que **re-medir** si sigue siendo el caso.
- Su SHA de lote fue `63054b7c`; su tip después, `1dad1bcc`.
- 🔴 **C tiene 7 commits fuera de `main` al cerrar — SIN mergear.** Al retomar:
  ancestría primero, pedirle su SHA, y mergear HASTA ése.

**B: 7 commits fuera de `main` al cerrar — SIN mergear.** SHA de código `fdedee75` (el delta desde `ecff9799` es **sólo comentario**).
Corrigió un número suyo **en la pieza, no sólo en su parte**: son **87** archivos
con `deshabilitado={`, no 96. Su ficha del gate vive en
`docs/loop/buzon/S112-B-para-A-gate-razon-muda.md`.

**D:** cerrada, **`2ade4079` entero dentro de `main`** — 0 commits afuera. Su
cura, su barrido y su parte (`docs/loop/S112-D.md`) están adentro.
⚠️ **D midió uno por uno cuáles de sus siete commits habían entrado y encontró
DOS afuera** (`b0bd94f6`, la corrección del acta, y su parte). *Yo había
mergeado su tip declarado y aun así dos quedaron fuera: mergear «el tip» no
garantiza que entre todo lo que el otro cree haber entregado.* Verificado con
`merge-base` antes de mergear: era cierto. **Pendiente mío con D:** plegar `_voz_adopcion_sin_respuesta` a
`_voz_notificacion` — son 30.107 caracteres de `CASE` compartido; **el bloque
exacto está en §5 de su evidencia**. ⚠️ **Si lo plegás, el barrido tiene que
seguir llamando a la voz CON mascota y a la intención SIN ella** — unificarlas
trae de vuelta `descartada_sin_acceso`.

## ⑥ LA SIEMBRA · PUNTO 1 — HECHO

| dato | valor |
|---|---|
| correo | `guillo381+refugio@gmail.com` · clave `Ni7131711` |
| confirmado | sí (por SQL) y **entra de verdad** (login 200) |
| cuenta comercial | **`80c41ac7-c39e-44b2-9d70-fd4f9816b521`** · «Refugio de prueba Satori» |
| fiscal | `persona_natural` · EC · cédula **`1711298437`** |
| estado | cuenta `activa` · rol `refugio` `activo` |
| **consentimientos** | **0 — el founder los acepta desde la app. NO aceptar por él.** |

`otorgar_rol_refugio` / `revocar_rol_refugio` nacieron hoy, gemelas de las del
vendedor. **Gate de admin probado en rojo** (`solo_admin_otorga_rol_refugio`).
Para otorgar hay que ser admin: `guillo381@gmail.com` está en `admin_users`.

## ⑦ 🔴 LA SIEMBRA · PUNTO 2 — GUION COMPLETO, NO EJECUTADO

**Bloqueada por el punto 5** (los campos no existen). Cuando exista, publicar
**por `publicar_adoptable`, jamás por INSERT**, con fotos del bucket
`adopcion-fotos`, **fechas de ingreso reales y DISTINTAS** para que «Llevan más
tiempo esperando» ordene con verdad:

| animal | qué tiene que cubrir |
|---|---|
| **Luna** | perra mestiza adulta, esterilizada, **7 meses** en rescate · gatos: sí · niños: sí · **perros: todavía no se sabe** · vacuna con lote y una conducta observada en el expediente |
| **Nube** | gata joven, esterilizada, **2 semanas** · **todo «todavía no se sabe»** — es el caso de `Convivencia` |
| **Tito** | cachorro de **3 meses, SIN esterilizar** (entra con compromiso) |
| **Bruno** | perro adulto grande, **urgente**, **bono declarado USD 20 / esterilización**, **14 meses** |
| **Kira** | perra adulta **SIN esterilizar** ⇒ **TIENE QUE REBOTAR** por la regla de los seis meses. **Queda en borrador y se declara: es el rojo** |

**Reportar qué RPC se usó por cada uno y qué rebotó.**

## ⑧ FRENOS Y SUPUESTOS VIVOS

- 🔴 **La cédula del adoptante NO EXISTE.** Lo que hay en `profiles` es
  `tipo_identificacion` + `identificacion_fiscal` — **fiscal**, del vendedor y
  la factura. **Reusarla pondría un RUC donde el acta dice cédula.** Nace como
  campo propio, y se pide **en el acto de firmar**.
- 🔴 **`traspasarMascotaAFamilia` exige `actaVersion` y ningún lector la da.**
  Motor sin puerta en forma sutil. Se resuelve en el punto 6 con viaje redondo.
- 🔴 **El censo de `ip_hash`: seis escritores, uno captura.** Completo en
  `docs/relevamientos/2026-09-02-s112a-CENSO-IP-DEL-CONSENTIMIENTO.md`.
  `crear_bloqueo_agenda` (teleconsulta, 30 filas, **curable, es motor**) y
  `registrarConsentimientos` (`auth.ts:381`, **NO curable sin mover el insert a
  una RPC** — el cliente no conoce su IP) siguen escribiendo NULL. **Es el
  consentimiento del registro, el más usado de la casa.** No los curé: no son
  adopción y tocan el alta de las dos apps.
- **Ítem 17 CERRADO sin cambio** (firma del founder): `guarderia_retorno` queda
  `relacional`.
- **Ítem 16** (bitácora después de entregar): la lista es
  `ARRAY['cancelada','no_recogida','entregada']` en `registrar_bitacora_guarderia`.
  **Sin hacer.**
- **90 días: FIRMADOS** — borran formulario e identidad del postulante; **el hilo
  se conserva anónimo**. Construidos por D. Falta **§5-bis en la letra**.
- **Adjuntos del hilo: FUERA de S112** (`D-1004`). ⚠️ **`adopcion-fotos` es
  PÚBLICO y es el único bucket de adopción: nada de una conversación va ahí.**
- **19 ramas viejas fuera de `main`** (s86→s109). Clase `L-217`. No las toqué.
- **Cuentas de prueba creadas hoy** (destino: decisión del founder):
  `223ab623` (`guillo381+alta0901@`) y la del control SMTP (`+alta0901b@`).
- **Pepe (`20d025ca`, ave con mensualidad): NO SE TOCA.** El founder la cancela
  desde la app.

## ⑧bis LO MEDIDO QUE VIVE EN SU PROPIO ARCHIVO

- **`docs/relevamientos/2026-09-02-s112a-MEDICION-ALTA-Y-RECUPERACION.md`** — el
  alta y la recuperación capa por capa, con sus controles y contra qué se midió.
  **Veredicto: nada que curar en motor; las dos capas rojas son de C y son la
  misma** (el onboarding sin salida que no sea crear mascota).
- **`docs/relevamientos/2026-09-02-s112a-CENSO-IP-DEL-CONSENTIMIENTO.md`** — los
  seis escritores de `consentimientos` con su veredicto uno por uno.

## ⑨ CÓMO MEDIR ACÁ — trampas que ya se cobraron

- **SQL:** `npx supabase --experimental db query --linked --file <x>.sql`
  desde el repo primario. La salida trae un `{...}` con `rows`.
- 🔴 **`request.headers` NO llega por esa vía** (Management API). Para medir algo
  que depende de headers hay que llamar **por PostgREST con un JWT real**
  (`curl` a `/rest/v1/rpc/<fn>`). *Mi primera medición de la IP dijo «no se
  puede» y era falsa.*
- 🔴 **`router.d.ts` está gitignored y viejo acusa EN FALSO** (`L-479`).
  Si un `tsc` de una app Expo cambia de color tras un merge: **regenerar antes de
  diagnosticar** (`expo start` en la app, **puerto libre** — con el puerto
  ocupado skippea y devuelve éxito igual).
- 🔴 **`tsc` sin `node_modules` da 0 sin ver nada** (`L-480`).
- 🔴 **`DROP CONSTRAINT IF EXISTS` con nombre adivinado no falla: CALLA.**
  Los nombres se miden en `pg_constraint`.
- 🔴 **`L-438` se cobró DOS veces hoy:** un arnés que no alcanza la compuerta
  porque corta un guard anterior. Si el rebote no es el que esperabas, **el
  arnés no llegó**. Se siembra el sujeto dentro de una transacción con ROLLBACK.
- **El exit code se lee del comando, JAMÁS del pipe** (`L-191`) — me pasó hoy.
- 🔴 **`git rev-parse --show-toplevel` NO dice que estés en TU worktree** (`L-481`):
  contesta el árbol donde estás parado. **Antes de ESCRIBIR** —no antes de
  commitear— se corre `git branch --show-current` contra la rama propia.
  *D escribió en el árbol primario sin querer y lo encontró tarde; no lo salvó
  el método, lo salvó que el árbol estaba limpio en ese instante.*

## ⑩ LO QUE NO SE PUBLICA SIN «AUTORIZO»

Ningún OTA sale sin la palabra del founder. **Ninguna llave de `app_config` se
enciende.** **Ningún texto legal se inventa.**
