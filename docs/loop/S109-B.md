# S109-B · EL BARRIDO APLICA LOS SIETE · `D-984` SE DISPARÓ

> **Rama:** `pista/s109-b` · base `origin/main`. Medido contra el objeto: la
> base, las edge desplegadas y el archivo. **`pista/s108-b5` la mergea A** — no
> la toqué.

---

## ① EL CENSO — cómo estaba el terreno al abrir

**Siete sujetos en el XOR, siete catalogados, guard verde.** A agregó `programa`
y **honró el mecanismo**: `verificar_cobertura_sujetos_de_pago()` habría
reventado si no lo catalogaba. *La maquinaria que S108-B2 dejó puesta funcionó
sin que nadie tuviera que acordarse de ella.*

| pieza | qué cubría al abrir |
|---|---|
| `pagos_huerfanos_por_sujeto` (lector) | **los siete**, por catálogo |
| `aplicar_consulta_activa_deuna` | compra y cita · rechaza recurrencia/suscripción · **ignora bono, mensualidad y programa** |
| `resolver_consulta_activa` (Nuvei) | **sólo compra** — tecleada por `p_compra_id` |

⇒ **El lector veía siete y los aplicadores movían dos.** Cita cobra desde S101;
bono y mensualidad desde S108. *Los tres podían quedar con el cobro hecho del
lado del proveedor y ningún rescate: una fila pagada que nada mueve, y del otro
lado una familia que pagó y no recibió.*

**Los otros tres sujetos, medidos:** el **paquete de paseo** vive en el MISMO
`bonos`, el **plan de paseo** en `suscripciones_servicio` y el **programa** en su
columna nueva. **Los tres estaban del lado bueno del lector y del lado malo de
los aplicadores** — nacían con el mismo hueco.

---

## ② 🔴 EL CENSO DE LLAMADORES — 26 candidatos, UN caso real, y era mío

El barrido por patrón dio **26 llamadores que hacen `PERFORM` sobre algo que
devuelve `ok`**. *Un censo por patrón acota; no cierra.* Lo que discrimina es
otra pregunta: **¿el llamado avisa el fallo DEVOLVIENDO o LANZANDO?**

Filtrando por eso —y sacando el andamiaje de prueba— quedó **UNO**:

> **`cobrar_periodo_mensualidad_guarderia` hacía `PERFORM` sobre mi propio
> congelador**, que puede devolver `ok:false` **sin lanzar** (`sin_moneda`,
> `mandato_sin_precio`, `mensualidad_no_existe`).

**La consecuencia:** el mes se comprometía igual, sin desglose congelado, y el
comprobante salía sin subtotal ni impuesto — *la regla «sin desglose congelado no
hay cobro», violada después del hecho.*

⚠️ **Hoy NO es alcanzable** —medido: **cero** cuentas comerciales sin moneda— **y
por eso valía curarlo ahora**: *el modo de falla es silencioso, así que el día
que sea alcanzable nadie lo va a estar mirando.*
**Curado** (`20260905260000`): el veredicto se lee y el acto frena con
`desglose_no_congelado`.

---

## ③ EL BARRIDO APLICA LOS SIETE — `20260905220000`

**Lo que NO se hizo, y es lo que mantiene una sola verdad:** *no se reimplementó
cómo se mueve cada sujeto.* `aplicar_consulta_activa_deuna` ya tenía la forma
correcta —inserta un `webhook_events` **verificado** y llama al actuador, que
conoce los siete— y `_evento_autenticado` **ya contemplaba ese camino**
(`origen IN ('barrido','consulta_activa') ⇒ verificado`).
⇒ **Lo único que se ensanchó es a qué sujetos se les permite pasar.**

- **La resolución del sujeto pasa al CATÁLOGO.** La versión vieja enumeraba a
  mano y decía, con razón, que un `else` con cuatro sujetos *«es una adivinanza
  que compila»*. **Con siete el problema es el mismo con más ramas.** Ahora
  agregar un sujeto es **agregar una fila**.
- **La idempotencia pasa a ser uniforme** —por el intento, no por cada sujeto—
  con el actuador de respaldo. *Dos redes, no dos verdades.*
- **El monto se verifica para los siete** contra su desglose congelado:
  `_total_congelado_del_intento` + `verificar_cobertura_desgloses()`, que
  **revienta al aplicar** si un sujeto catalogado se queda sin rama. Y `NULL` es
  «no sé», y **«no sé» no confirma**.
- **Nace `aplicar_consulta_activa_nuvei`**, que recibe el INTENTO y **para la
  compra DELEGA** en `resolver_consulta_activa`. *Dos funciones que hacen lo
  mismo son dos verdades; una que llama a la otra es una sola con dos puertas* —
  y el camino de compras, que es el único ejercido, no se tocó.
- ☠️ **Muere el escalado `aplicador_no_cubre_*`** de S108-B2: existía porque el
  lector veía lo que el aplicador no podía aplicar. **Ya tiene camino.**

**El discriminador del cinturón:** se FABRICA un bono con su desglose congelado
y se exige que el aplicador **llegue a comparar el monto**. *Que no diga
`sujeto_indeterminado` es lo único que prueba que el sujeto entró; un `ok:false`
cualquiera no distingue una cosa de la otra.* Tres sabotajes, los tres gritan.

### 🔴 El `else`, censado como CLASE

**Cero `else` de sujeto en las cinco edges de pagos** (medido, no supuesto), y
en SQL la cadena del aplicador de DeUna **dejó de existir**: la reemplazó el
catálogo. *La clase se cierra cambiando la forma, no tachando ocurrencias.*

---

## ④ 🔴 `D-984` SE DISPARÓ — Y SE DISPARÓ CONMIGO

**La ficha decía «disparo: antes de que nazca un sexto recurrente». Se disparó
con el TERCERO, el mismo día que la escribí.**

Medido: `pagos-cobro-recurrente` **v15** nombra DOS selectores
(`recurrencias_vencidas_pendientes`, `planes_vencidos_pendientes`) y
**`mensualidades_vencidas_pendientes` —que nació en S108-B2— no está.** Y la
edge **no lee body**, así que el `{sujeto:'mensualidad_guarderia'}` que mi timbre
le manda **no lo mira nadie**.

> ### El día que el founder encienda la llave: el cron suena, la edge corre sus dos de siempre, la mensualidad no se cobra — y el timbre devuelve `ok:true, ejecutado:true`. **Un cron que informa haber ejecutado es exactamente el que nadie va a ir a revisar.**

**Lo que se curó y lo que no, sin maquillar:**

✅ **El timbre deja de mentir.** Si su selector no tiene consumidor, **no postea**
y lo dice con nombre (`selector_sin_consumidor`). *Preferible un cron que declara
que no puede a uno que dice que sí.* Y el cuerpo bueno **queda escrito y a la
vista**, para que cablear sea **borrar el `RETURN`** — *un camino que hay que
reescribir al reactivarlo es un camino que se reactiva distinto.*

✅ **Nace el gate que ve los DOS lados**
(`scripts/verify-selectores-recurrentes.mjs`). **El defecto cruza SQL y TS** —la
base tiene el selector y el archivo decide si lo llama—, así que **ningún guard
de un solo lado puede verlo**. Le pregunta a `pg_proc` qué selectores existen y
al archivo cuáles consume. **Hoy sale ROJO nombrando mi propio selector**, que
es el estado honesto:

```
  🔴 mensualidades_vencidas_pendientes    NADIE LO LLAMA
  ✓ planes_vencidos_pendientes           consumido por la edge
  ✓ recurrencias_vencidas_pendientes     consumido por la edge
```

❌ **NO se cableó el tercer selector en la edge**, y la razón es de forma medida:
los dos que consume devuelven un **objeto** `{para_cobrar, frenadas}` y el mío
devuelve **filas**; el lazo de cobro espera campos de la serie de despensa.
**Unificarlos es reescribir el lazo de una edge que hoy cobra de verdad**, y eso
no se hace de paso ni al final de una sesión.
**Disparo: antes de encender `guarderia_recurrente_vivo`.**

---

## ⑤ LO QUE QUEDA SIN HACER, CON SU RAZÓN

- **Ninguna edge desplegada en esta sesión.** El cambio de `pagos-conciliar`
  —que enruta al aplicador nuevo— **está escrito y no publicado**: publicar pide
  firma del founder (`LETRA_MOTOR_PAGOS_S101` §10). **La cura del motor SÍ está
  viva en la base**; lo que falta es que el barrido de Nuvei la use.
- **La rama DeUna sigue sin ejercerse con plata real** — exige consulta
  verificada.
- **`pista/s108-b5` la mergea A**, como corresponde.

**Verificado al cierre:** 7 sujetos en el XOR = 7 catalogados = 7 con desglose ·
las tres migraciones registradas · congelador real (el saboteado no quedó) · el
`PERFORM` no volvió · llave en `'false'`, motor apagado · **residuo 0** en bonos,
catálogo y `app_config`.
*La diferencia 551 archivos / 556 ledger son cinco migraciones de A en vuelo —
trabajo de otra pista, no desemparejadas.*

---

# PARTE 2 · EL CENSO DEL LADO CREADOR, `D-984` CABLEADO, Y LA MEDICIÓN DE SEGURIDAD

## ⑥ 🔴 EL CENSO DEL LADO CREADOR — el que faltaba

Mi censo miró **consumidores** y **llamadores**. Ninguno de los dos alcanza a
quien **nunca llega a llamar**. Medido contra el objeto —quién puede INSERTAR un
`pagos_intentos`, por sujeto:

| sujeto | quién puede CREAR su intento | intentos vivos |
|---|---|---|
| pedido | 2 edges + `confirmar_pago_pedido` | 53 |
| cita | 2 edges | 39 |
| bono | 2 edges | 1 |
| mensualidad_guarderia | 2 edges | 3 |
| recurrencia | `recurrencias_vencidas_pendientes` (reloj) | 0 |
| suscripcion_servicio | `planes_vencidos_pendientes` (reloj) | 0 |
| **programa** | 🔴 **NADIE** | **0** |

> ### `programa` tenía el arco ENTERO —congelador, compuerta, `confirmar_pago_programa`, actuador, reverso— **y ninguna puerta de entrada.** Se aplica bien y no se puede crear.
> *Un sujeto que se aplica bien y no se puede crear nunca va a cobrar, y hoy se
> lee como completo.*

**Y `suscripcion_servicio` es la otra mitad de la misma forma:** SÍ se puede
crear y **el lazo los frenaba a todos**. Se crea y nunca cobra.

**Los dos hallazgos —el mío de los siete y el de C sobre `SujetoDeCobro`— eran
ciertos a la vez**, porque miraban lados distintos del mismo riel.

✅ **`programa` gana su puerta:** `{tipo:'programa'}` en `SujetoDeCobro`,
`cobrarProgramaAdiestramiento` exportado (R71) y su pata en las **dos** edges —
pertenencia por `user_id`, hold propio, desglose congelado, fail-closed.

## ⑦ `D-984` CABLEADO — el lazo deja de asumir despensa

**La causa medida:** el lazo usaba `it.recurrencia_id` y `it.pedido_id` —campos
de despensa— para rotular, para la compuerta y para el `dev_reference`. Mi
selector devolvía una TABLA. **Por eso no se podía enchufar.**

- **Los tres selectores devuelven la misma forma**, y la normalización es
  **ADITIVA**: los dos vivos conservan todos sus campos y ganan `sujeto` /
  `sujeto_id`. *Quitarles un campo para uniformar sería romper el cobro que hoy
  funciona para arreglar el que todavía no cobra.*
- **Mi selector ahora CREA el intento y CONGELA**, como sus hermanos — «la base
  elige y congela». Sin eso el intento dejaría de ser lo que prueba que se
  disparó **antes** de disparar.
- **El lazo ITERA el catálogo** (`selectores_recurrentes_vivos`): un sujeto
  recurrente nuevo entra **sin tocar el archivo**. Bajo esa forma, *«selector
  huérfano» es inexpresable*.
- **La compuerta y el desglose pasan a despachadores por INTENTO**
  (`verificar_compuertas_del_intento`, `_desglose_congelado_del_intento`), los
  dos **fail-closed con nombre**. Una tabla nombrada en el lazo era justo lo que
  lo ataba a despensa.
- ☠️ **Muere la lista fija de «planes frenados»** que vivía en el consumidor.
  *Un freno que vive en el consumidor es un freno que el dueño del sujeto no
  puede levantar cuando lo cure.*
- ☠️ Y muere el `selector_sin_consumidor` del timbre: **el cableado le sacó la
  razón de existir.**

**Cinturón con los TRES sabotajes por separado, cada uno con su causa:**
despensa sin `sujeto` → *«no emite sujeto/sujeto_id»* · planes ídem · la
mensualidad sin crear el intento → *«el selector NO creó el intento»*.
Y el caso del ítem se **FABRICA**: hoy las tres listas están vacías, y *un arnés
que se apoya en que no haya filas no mide, mira*.

### 🔴 EL GATE: LEÍA EL ÁRBOL EQUIVOCADO

**Hallazgo sobre mi propio instrumento.** El gate de dos lados leía el archivo
del **worktree principal** con una ruta fija: corriendo desde la rama, evaluaba
el árbol de OTRO. **Sus dos ✓ eran el estado de `main`.**
*Medir un árbol y llamarlo el estado del que estás cambiando es la misma familia
que medir la propia rama y llamarla `main`.*

**Y su pregunta cambió porque la arquitectura cambió** — se declara para que
nadie lo lea como un gate ablandado. Antes la edge nombraba cada selector, así
que «¿lo nombra?» era la pregunta correcta. Ahora los itera, y bajo esa forma la
vieja pregunta **no puede fallar**. La nueva sí: *¿de verdad itera, y no quedó
ninguno cableado a mano?*

**La prueba de que el verde vino de la cura y no del ajuste:**

| corrida | exit |
|---|---|
| gate sobre **mi rama** | **0** |
| gate sobre la edge de **`main`** (antes de la cura) | **1** |
| sabotaje: se le saca la iteración | **rojo, nombrando `D-984`** |
| sabotaje: uno cableado a mano | **rojo, «se van a llamar DOS veces»** |

## ⑧ ✅ LA MEDICIÓN DE SEGURIDAD PARA LA FIRMA

**Con el SQL como está desplegado y las edges viejas corriendo, los dos sujetos
que cobran de verdad SIGUEN COBRANDO**, ejercido contra la edge desplegada:

- **bono `DF-2107889`** → `estado_pago=pagado`, hold apagado, `acto2=true`,
  comprobante **«Paquete de 5 estadías de guardería»**
- **mensualidad `DF-2107890`** → plan arrancado (31-ago→29-sep), **22 días**,
  desglose congelado, `acto2=true`, comprobante **«Plan mensual de guardería»**

**Y el guard nuevo NO es alcanzable desde el camino de cobro:** su única mención
en el aplicador es **una línea de comentario** —cero llamadas reales—, así que no
puede reventar sobre el único camino ejercido. *Lo verifiqué porque un censo por
texto cuenta comentarios como llamadas, que es `L-437`.*

⚠️ **Una corrección al encargo, medida:** **bono y mensualidad cobran por NUVEI,
no por DeUna** (`por_deuna = 0` intentos). **DeUna sigue sin ejercerse.**

## ⑨ LO QUE SIGUE SIN HACER

- **Ninguna edge desplegada.** Las tres tocadas —`pagos-cobro`,
  `pagos-deuna-solicitud`, `pagos-cobro-recurrente`— **esperan la firma**.
  ⚠️ **`pagos-cobro-recurrente` no puede desplegarse sola**: usa `sujeto`,
  `sujeto_id` y los dos despachadores nuevos. **La base ya los tiene**, así que
  el orden seguro es **desplegar**, no revertir.
- **El cobro recurrente nunca se ejerció de punta a punta** —ni antes ni ahora—
  porque la llave `guarderia_recurrente_vivo` es del founder y **nadie tiene
  mandatos vencidos hoy**. El lazo nuevo está probado por cinturón, no por plata.

---

## ⑤ EL DESPLIEGUE DESDE `main`, Y LO QUE UN COBRO REAL ENCONTRÓ

**Desplegadas las tres desde `main` (`45695e7f`), leídas del OBJETO, no del
comando:** `pagos-cobro` **v28** · `pagos-cobro-recurrente` **v16** ·
`pagos-deuna-solicitud` **v15**, las tres ACTIVE.

### 🔴 EL HALLAZGO: EL FRENO EXISTÍA Y MI EDGE NO LO LLAMABA

Ejercer el **programa** con un cobro real destapó lo que ningún arnés vio: el
débito salió (`DF-2108181`, **$90, aprobado**), el webhook llegó, y **el acto 2
se cayó** con `programa_excede_vigencia`.

Medido contra el objeto, no razonado:

| medición | resultado |
|---|---|
| `verificar_compuerta_programa` sobre ese mismo programa | **`{ok:false, codigo:'sesiones_no_agendables'}`** |
| `grep verificar_compuerta_programa` en `pagos-cobro` | **0** |

⇒ **La compuerta de la pista A funcionaba perfecto y esta edge nunca le
preguntó.**

> 🔴 **Un freno que sólo puede actuar cuando la plata ya se movió no evita
> vender lo que no se tiene: obliga a devolverlo.**

**Y lo que lo vuelve lección y no anécdota: es la MISMA clase que curé en
S108-B2 para la mensualidad** —construí ahí `verificar_compuertas_mensualidad_guarderia`
justamente porque el freno vivía del lado equivocado del cobro— **y al abrir el
sujeto siguiente cablé su desglose y su pertenencia y me olvidé de su
compuerta.** *Abrir una puerta nueva obliga a cablear su compuerta, no sólo su
desglose* — y quien acaba de pagar esa lección es el más expuesto a repetirla,
porque cree que ya la sabe.

**Curado** (`pagos-cobro`: la compuerta antes de mover plata; `sesiones_no_agendables`
y `programa_no_cobrable` tipados en el wrapper — *«no se pudo» sobre un programa
manda a reintentar algo que va a fallar igual*). ⚠️ **NO desplegado: la firma
fue para desplegar DESDE `main`, y un hallazgo nuevo no cambia esa disciplina.**
Espera merge de A.

### LO EJERCIDO, CON SU ID

| sujeto | transacción | acto 2 | comprobante |
|---|---|---|---|
| **paquete de paseo** | `DF-2108182` | ✅ | «Paquete de 5 salidas de paseo» |
| **bono guardería** (re-medido post-deploy) | `DF-2108188` | ✅ | «Paquete de 5 estadías de guardería» |
| **mensualidad** (re-medida post-deploy) | `DF-2108189` | ✅ | «Plan mensual de guardería» — plan activo desde 31-ago, día de cobro 31 |
| **programa** | `DF-2108181` | 🔴 `programa_excede_vigencia` | — |

**Los tres verdes se re-midieron con las edges NUEVAS**, que es lo que el
encargo pedía: *la cura es re-medir, no concluir.*

### 🔴 EL ARNÉS LEYÓ EN VUELO DOS VECES MÁS

Con espera fija de 6 s y de 9 s los arneses leyeron intento `pendiente`, sujeto
sin mover y comprobante ausente — **y estuvieron a un paso de reportar como
defecto su propia impaciencia.** Re-medir mostró las cadenas cerradas.

> **Una espera fija no puede acertar: o lee antes de tiempo, o gasta tiempo de
> más.** Curado con **sondeo del estado con techo** — y de paso el arnés ahora
> **EXIGE `acto2` aplicado**, así que el defecto del programa habría salido con
> su causa en vez de como «sin comprobante».

### LO QUE FALTA DE MI LADO PARA DEUNA

El riel se ejerce con el **paquete de paseo** (compra suelta). De mi lado está
**todo**: `pedirCodigoDeunaPaquete` exportado y `pagos-deuna-solicitud` **v15**
desplegada con los cinco sujetos. **Falta lo que no es mío:** que el checkout
del paquete ofrezca DeUna (pista C) y **la consulta verificada** del proveedor,
sin la cual el aplicador no se puede ejercer. *Fabricar esa consulta en un arnés
sería simular el veredicto que la consulta existe para dar.*

---

## ⑥ 🔴 `marcar_link_mensual_pagado` — LA CUARTA PIEZA SIN LLAMADOR DEL MISMO DÍA

**La nombró A censando lo suyo, y es mía.** Medido contra el objeto:

| medición | resultado |
|---|---|
| la función existe | ✅ `marcar_link_mensual_pagado(p_link_id uuid)` |
| llamadores **en la base** | **0** |
| llamadores **en el repo** | **0** |
| `pagos-deuna-webhook` la nombra | **no** — rutea todo a `aplicar_evento_de_pago` |

**Y el actuador resuelve un INTENTO. Un link mensual no es un intento** — vive
en `cobro_link_mensual`, con su propio `referencia_proveedor`. ⇒ una familia
paga el link del mes, el webhook persiste el evento, el actuador no encuentra
sujeto, **y el link se queda `emitido` hasta vencer con el plan pausado
habiendo cobrado.**

🟢 **Lo que NO es: silencioso.** El actuador de A ya devuelve `ok:false` y marca
el evento `desconocido` —la cura de S107—, así que **suena**. *El daño está
acotado a que nadie lo escuche, no a que no haya ruido.* Esa cura, hecha para
otra cosa, es la que vuelve este hueco visible en vez de invisible.

**Y la función ya está bien construida para recibirlo:** `solo_servidor`,
idempotente (`duplicado`), y con el pago tardío sobre link vencido **nombrado y
NO aplicado**, esperando una firma que no existe. *No falta motor: falta puerta.*

### LAS DOS CASAS, Y POR QUÉ NO ELEGÍ SOLO

- **(a) el webhook (mía)** — resuelve la referencia contra `cobro_link_mensual`
  y llama a la función cuando no hay intento.
- **(b) el actuador (de A)** — aprende un sujeto que no es un intento.

**`D-923` dice que el sujeto se mueve por la transición del intento, no cableado
por riel** — y un link no tiene intento, así que no puede viajar por ahí. Eso
empuja hacia (a)… pero (a) es exactamente *cablear dentro de un riel*, que es
**cómo el segundo riel se olvida** (lección de S105, de A).

> **Las dos lecturas son defendibles y por eso no la resolví solo: elegir mal
> acá no produce un error, produce un segundo riel que dentro de dos sesiones
> nadie recuerda que faltaba.** Servida a A con su medición.

### LA CLASE, QUE ES LA NOTA DEL DÍA — CUATRO VECES EN UNA JORNADA

1. **La compuerta del programa**: bien construida, nadie la llamaba (mía).
2. **El `never` de `cobro.ts`**: el archivo **declaraba** que frenaba códigos
   nuevos y no frenaba ninguno — **quince** cayeron en el genérico compilando en
   verde (halladas por C, del otro lado del mostrador).
3. **`marcar_link_mensual_pagado`**: motor sin puerta (ésta).
4. **`D-984`**, curada hoy: el lazo recurrente enumeraba sujetos por nombre.

> **Las cuatro compilan en verde y las cuatro se ven al EJERCER, no al leer.**
> Y la más cara de las cuatro no fue la que faltaba: fue la que **declaraba por
> escrito que estaba** — *un cinturón que dice que aprieta es peor que ninguno,
> porque nadie vuelve a mirarlo.*
