# S105-D — el riel DeUna · bitácora de pista

> **Pista D** · territorio: `supabase/functions/pagos-*` · edge functions,
> secretos, ambiente QA del proveedor.
> **➕ `supabase/functions/despachar-whatsapp` — FIRMADO POR EL FOUNDER
> (25-ago-2026): queda en esta pista MIENTRAS DURE EL FRENTE DE ENVÍO.**
> *Razón firmada: la pista ya carga el contexto del instrumento y de sus dos
> huecos.* ⚠️ **Pendiente de A: transponerlo a `PLAN_MESA_106 §TERRITORIOS`** —
> `docs/` es su territorio, y **una asignación que vive solo en un parte de
> pista es exactamente lo que se perdió con el contrato de `D-887`.**
> **Abre:** 24-ago-2026 · **Rama:** `pista/s105-d` · **Base:** `main` `5959650e`.
> **Rige:** `CLAUDE.md` · `METODO_TRES_PISTAS` · `CONTRATO_TRABAJO` ·
> `LETRA_DEUNA` v1.7 · `CONTRATO_WRAPPER_DEUNA`.

---

## TURNO 1 — medición, sin código

**Contra qué objeto se midió, declarado una vez:** repo = `main` local
`5959650e` (árbol limpio verificado antes y después) · base y edge functions =
proyecto `zyltipqscdsdsxnjclhp` vía CLI · ramas = `git branch -a` local +
`ls-remote`. **Ningún dato sale de un reporte previo.**

### ⓪ Un freno de arranque, y la corrección que lo resolvió

**El freno, tal como se levantó en el turno 1:** *«`PLAN_MESA_106` NO EXISTE en
`main`»*. Medido: `docs/` tiene `PLAN_MESA_103`, `104`, `105` y
`ORDEN_ARRANQUE_MESA_105`; **cero coincidencias de «MESA_106» en todo `docs/`.**
El prompt de esta pista lo citaba como fuente que rige. Esta pista trabajó
igual, contra `PLAN_MESA_105` —que en su §5 nombra el `pointOfSale` como la
llave que destraba «el riel entero»— y `LETRA_DEUNA` v1.7.

**✅ RESUELTO, y la medición era correcta pero la conclusión no.** El founder
firmó que **rige la numeración de la bitácora de A**, que midió que la mesa
anterior corrió etiquetada **S104** ⇒ **esta mesa es S105**, y el plan que rige
es `PLAN_MESA_105`, **que sí está en el repo.** *No faltaba un documento:
sobraba un número.*

### 🔴 La lección del renombre — medir un objeto no salva si el objeto lo fabricó el mismo error

El turno 1 escribió: *«El número de mesa NO se adivinó: se midió. Existen
`pista/s106-b` y `pista/s106-c`.»* **La medición era verdadera** —esas ramas
existían— **y la inferencia era falsa.**

> **Por qué falló:** esas ramas salieron del **mismo prompt** que traía el «106»,
> no de la bitácora de A. Medí un objeto **que era eco del error que estaba
> tratando de esquivar** — tres pistas nombrando el mismo número equivocado se
> lee exactamente igual que tres pistas confirmando el correcto.

⇒ **La regla que se deriva, y no es la de siempre:** *«se mide del objeto» no
alcanza cuando el objeto es derivado.* Hay que preguntar **de dónde salió ese
objeto**: una rama de pista hermana **no es fuente de numeración** — la fuente es
la bitácora de A, y sólo ella. **Un consenso entre pares que comparten origen no
es evidencia independiente: es la misma afirmación contada tres veces.**

### Qué se renombró y qué no *(orden del founder, 24-ago)*

| | |
|---|---|
| **Rama** | `pista/s106-d` → **`pista/s105-d`** ✅ |
| **Remoto** | **nada que actualizar**: medido con `ls-remote`, la rama **nunca se pusheó** |
| **Bitácora** | `docs/loop/S106-D.md` → **`docs/loop/S105-D.md`** ✅ (con sus menciones internas) |
| **Mensaje del commit del turno 1** | ⚠️ **NO se reescribió: conserva su asunto `S106-D`.** Se intentó `--amend` y **el permiso lo frenó** — *y el freno tiene razón: reescribir historia para que el log «siempre haya dicho» lo correcto borra que hubo una corrección.* **El renombre entra como commit propio**, así que el log muestra las dos cosas: que se llamó S106 y que se corrigió |
| **Worktree en disco** | ⚠️ **sigue llamándose `e-petplace-s106-d`** — autorizado por el founder: renombrarlo cuesta un rearme y **el disco no queda en el log**. *Quien lo vea: el nombre del directorio miente, la rama no.* |

---

## §1.1 · LO QUE LLEGÓ vs LA LISTA DEL PLAN

La lista es `LETRA_DEUNA` §12 — **cuatro preguntas abiertas**. Contraste literal:

| # | pregunta de §12 | ¿llegó? | qué dice el dato |
|---|---|---|---|
| 1 | 🔴 `pointOfSale` para canal digital, y quién lo emite | ✅ **SÍ** | **QA = `4262774`** |
| 2 | cómo se **registra y rota** URL + headers en QA y PDN | ⚠️ **PARCIAL** | **registro**: nosotros enviamos URL+headers, **ellos configuran de su lado** |
| 3 | ¿firma más fuerte que headers estáticos? | ✅ **SÍ (y la respuesta es NO)** | **solo headers estáticos** — no hay nada más fuerte |
| 4 | ¿QA permite simular `REVERSED` / `REVERSED_FAILED`? | ✅ **SÍ** | se puede simular contra QA |

### Lo que NO llegó — y no se supone

- 🔴 **El mecanismo de ROTACIÓN.** La pregunta 2 tenía dos mitades y volvió
  media: sabemos **cómo se registra**, no **cómo se cambia después** ni **cuánto
  tarda**. *Y esa mitad es la que dimensiona la ventana ciega* — el buzón está
  escrito con **dos secretos** (`ACTUAL` + `SIGUIENTE`) precisamente porque el
  header lo cambia un tercero. **El diseño de dos secretos queda ratificado; su
  ventana sigue sin número.**
- **El `pointOfSale` de PDN** — declarado explícitamente como «otro», y por
  orden de la mesa **no se pide ahora**.
- **El rate limit**: respuesta **parcial, pendiente**. `LETRA_DEUNA` §2 declara
  «~1 req/s» **tomado de la doc en papel** — sigue **sin confirmar contra el
  proveedor**, y el barrido espacia sus consultas apoyado en ese número.
- **Si el webhook se registra por ambiente** (una URL para QA y otra para PDN).
  Se deduce que sí; **no está confirmado**, y no se escribe como si lo estuviera.

### 🔴 Y un dato que NO se preguntó y CONTRADICE letra firmada

**Ventana de reverso: `24 HORAS`.** `LETRA_DEUNA` §8 dice hoy, textual:

> *«✅ **RESUELTO — ya no es supuesto.** ~~un recuadro dice «dentro de las 24
> horas», otro «solo mismo día»~~. **El proveedor lo confirmó por mensaje:
> MISMO DÍA.***

**Las dos afirmaciones vienen del MISMO proveedor con dos días de diferencia**
(22-ago «mismo día» · 24-ago «24 horas»), **y no son lo mismo**: un pago de las
23:50 tiene diez minutos de ventana con una y veinticuatro horas con la otra.

- **No bloquea hoy**, y se dice por qué: §8 declara que **el reverso por API no
  entra en v1** (camino manual de soporte, se construye después del circuito de
  cobro).
- **Sí deja letra desactualizada**: §8, la firma ③ de §11 y el historial v1.2
  celebran «pasa de supuesto a hecho». **Enmendarla es de A / la mesa**, no de
  esta pista; se levanta acá para que nadie la cite creyéndola vigente.
- **Lo que deja como lección, y es del método:** *un «confirmado por mensaje» de
  un proveedor no es más estable que su propia doc — es la misma fuente con otra
  fecha.* La letra trató el mensaje del 22-ago como cierre definitivo (tachó la
  ambigüedad, cerró la pregunta y movió la firma de supuesto a hecho) y **dos
  días después el mismo emisor dijo lo otro.**

---

## §1.2 · ESTADO REAL — el actuador, el wrapper, el riel

**Se corrieron las DOS preguntas de `L-402`**, porque una sola no alcanza:
*¿está alcanzable?* y *¿corrió alguna vez?*

### ¿Corrió alguna vez? — **NO. Cero veces.**

| tabla | medido |
|---|---|
| `pagos_intentos` por proveedor | `nuvei` 30 · `seed_gate` 9 · `simulado` 4 · `siembra` 1 · **`deuna` 0** |
| `webhook_events` por proveedor | `nuvei` 65 · **`deuna` 0** |
| `webhook_events` por origen | `webhook` 65 · **`barrido` 0 · `consulta_activa` 0** |

⇒ **Todo lo que se afirme «vivo» del lado DeUna es lectura de código, no
ejecución.** Es exactamente la distinción que `L-402` ordena declarar.

### Lo que SÍ existe y está aplicado (medido, no recordado)

| pieza | estado | evidencia |
|---|---|---|
| Migraciones | **411 local = 411 remoto, 0 desemparejadas** | `migration list` parseado |
| `20260822220000` forma DeUna | ✅ **aplicada en remoto** | presente en local **y** remote |
| Columnas del riel en `pagos_intentos` | ✅ **completas** | `codigo_numerico` · `codigo_expira_en` · `referencia_corta` · `proveedor_transaction_id` · **`proveedor_reverso_id`** · `hallazgo` · `hallazgo_en` |
| CHECK de la referencia (§4) | ✅ **`<= 20`, al literal de la letra** | `chk_referencia_corta_cabe` |
| Candado de idempotencia (§4) | ✅ **existe, en dos índices parciales** | `uq_pagos_intentos_tx_por_cita` y `…_por_pedido` sobre `(proveedor, proveedor_transaction_id, sujeto)` + `uq_…_referencia_corta` |
| Generador de referencia | ✅ vive | `deuna_nueva_referencia` + `_deuna_base36` |
| Gate `_evento_autenticado` | ✅ **conoce `deuna`, en COLUMNAS** | `coalesce(stoken_valido,false) AND verificado IS TRUE`; `ELSE false` fail-closed |
| `aplicar_evento_de_pago` | ✅ **conoce `deuna`** (y `proveedor`) | pero ver el hueco abajo |
| Cron del barrido DeUna | ✅ **NO existe** — correcto por `§13bis ③` | 19 jobs, ninguno DeUna |
| `app_config.pagos_actuador_vivo` | `true` | |
| Secretos `DEUNA_API_KEY` / `DEUNA_API_SECRET` | ✅ **ya cargados** | `secrets list` |

### Lo que está escrito y NO desplegado

**Cero funciones `pagos-deuna-*` desplegadas.** Medido contra la API del
proyecto: 24 functions ACTIVE, **ninguna DeUna**. No es solo
`pagos-deuna-solicitud` como decía el `CONTRATO_WRAPPER_DEUNA` §6 — **son las
tres**: `solicitud`, `webhook` y `barrido`.

### Lo que FALTA — con dueño

| # | hueco | dueño |
|---|---|---|
| 1 | **`D-887` · el aplicador del barrido.** Confirmado en la base, no en prosa: `aplicar_evento_de_pago` **no menciona `origen` ni `barrido`** ⇒ el barrido clasifica `confirmado`, loguea *«lo aplica el actuador»*, **y nadie aplica nada** | **A (motor)** |
| 2 | Rama del gate para `origen` (§13ter): **no escrita, a propósito** — sería puerta sin motor, y ⛔ jamás `stoken_valido=true` desde el barrido | A |
| 3 | **Los DOS flips de `§13bis ②`.** Medido: `DEUNA_ELEGIBLE = false` (`fila-medio-de-pago.tsx:272`) **y** `deuna-estado.ts` sigue con el bloque `ENSAYO` vivo, que su propio archivo declara que *«muere entero»* al enchufarse | **C** |

---

## 🔴 §1.2bis · EL HALLAZGO DEL TURNO — `main` TIENE LA VERSIÓN VIEJA DEL BUZÓN

**Salió de medir el repo por ancestría, no por lectura.** `pista/s103-d` **nunca
se mergeó a `main`** (`git merge-base --is-ancestor` → NO) y tiene commits
posteriores a los que sí llegaron.

**El número que lo prueba, con su control:**

```
verificado:  main → 0 ocurrencias   ·   pista/s103-d → 2
origen:      main → 0 ocurrencias   ·   pista/s103-d → 2
```

**Por qué es grave y no cosmético:** el gate vivo **en la base** exige, para
DeUna, `verificado IS TRUE`. **La versión de `main` nunca escribe esa columna.**

> ### Desplegar el webhook desde `main` habría producido una función que responde `200`, guarda el crudo, consulta al proveedor — **y cuyo evento el gate rechaza SIEMPRE**, para todo pago, sin error y sin log.
>
> *El pago entra, la plata se mueve del lado del cliente, y el sujeto no se
> mueve nunca.* Es el modo de falla que `§13bis ①` describe palabra por palabra
> — **y lo habríamos construido nosotros, no el proveedor.**

El commit que falta se llama, literal: *«S103-D · el buzón escribe verificado y
origen: **el requisito bloqueante de A, cerrado**»*. **Se cerró en la rama y no
llegó al canon** — `L-217` en su forma más cara: no es un documento perdido, es
el eslabón que hace funcionar el circuito.

**Alcance completo del rescate** (`main` vs `pista/s103-d`), separando **ausente**
de **divergente**, porque no son el mismo problema:

| | archivo |
|---|---|
| **AUSENTE** | `docs/CONTRATO_APLICADOR_BARRIDO_DEUNA.md` ← **el que la ficha `D-887` cita como «contrato ya escrito»** |
| **AUSENTE** | `docs/INSUMO_D405_ELIMINACION_Y_PORTABILIDAD.md` |
| **AUSENTE** | `docs/relevamientos/S103-D-PAQUETE-ALTA-WEBHOOK.md` |
| **AUSENTE** | `supabase/functions/pagos-deuna-solicitud/_codigos.test.ts` |
| **DIFIERE** | `pagos-deuna-webhook/index.ts` (265 → 306) · `_verdad.ts` · `index.test.ts` |
| **DIFIERE** | `pagos-deuna-barrido/index.ts` (190 → 215) |
| **DIFIERE** | 4 relevamientos S103-D · `simulador-local.mjs` · `sondeo-qa.mjs` · las migraciones sin número |

⇒ **`D-887` tiene dueño A y su contrato NO está donde su ficha dice.** A abriría
ese trabajo buscando un archivo que en el canon no existe.

**Nada de esto se mergeó desde esta pista:** `main` es territorio de A. **Se
reporta con el comando de rescate listo** (`git show
pista/s103-d:<ruta>`), y **el despliegue de esta pista se hará desde la versión
correcta, no desde `main`** — declarado acá para que quede escrito antes de
tocar nada.

---

## §3 · LA LIMITACIÓN DEL PROVEEDOR — registrada, no suavizada

> ### **DeUna NO ofrece firma criptográfica de webhook. Su único mecanismo de autenticación son headers estáticos. Preguntado y contestado por el proveedor, 24-ago-2026.**

**Qué significa exactamente, sin adorno:** cualquiera que conozca la URL **y el
valor del header** puede emitir un webhook indistinguible de uno legítimo. **No
hay HMAC, no hay timestamp firmado, no hay reintento verificable.** Contra Nuvei
—que sí firma— **este riel es estructuralmente más débil, y lo va a seguir
siendo**: no es una etapa, es el techo del proveedor.

**Consecuencia operativa, y por eso la capa ② no es opcional:**

- **El buzón de dos capas es la ÚNICA defensa.** Capa ① secreto propio en header;
  capa ② **consulta activa obligatoria** — *un webhook con el secreto correcto y
  datos falsos muere en `payment/info`.* **Quitar la capa ② deja el riel apoyado
  entero en un string que viaja en texto plano en cada request y que administra
  un tercero.**
- **El secreto lo generamos y lo custodiamos nosotros**, y **lo rota un tercero
  a pedido** — de ahí los dos secretos (`ACTUAL` + `SIGUIENTE`). *Medido en el
  código: `coincide()` es fail-closed con vacío (`if (!esperado || !recibido)
  return false`), así que un `SIGUIENTE` sin cargar no abre nada.* **Verificado,
  no supuesto.**
- 🔴 **Y lo que esto le prohíbe a cualquier sesión futura:** **el webhook de
  DeUna jamás transiciona un sujeto por sí solo.** Con Nuvei se podría discutir;
  acá **no hay discusión posible**, porque no hay nada que verifique el origen
  del mensaje salvo un secreto compartido.

---

## §4 · PREPARADO, NO EJECUTADO

### (a) Los secretos a fijar — medidos del código, no de la letra

Fuente: `Deno.env.get(...)` de las tres functions, contrastado contra
`secrets list`.

| secret | estado | valor | quién |
|---|---|---|---|
| `DEUNA_API_KEY` | ✅ ya cargado | — | — |
| `DEUNA_API_SECRET` | ✅ ya cargado | — | — |
| `PAGOS_AMBIENTE` | ✅ **FIJADO EXPLÍCITO a `sandbox`** *(24-ago)* | — | ⚠️ su valor **no era legible** (`secrets list` da digest, no valor) ⇒ **se fijó en vez de leerse: escribirlo es la única confirmación real.** El supuesto del turno 1 queda **cerrado** — la base resuelve a QA **por lectura de secret, no por deducción** |
| **`DEUNA_POINT_OF_SALE`** | ✅ **CARGADO** *(24-ago)* | `4262774` (QA) | el bloqueante del riel, destrabado |
| **`DEUNA_WEBHOOK_SECRET`** | ✅ **CARGADO** *(24-ago)* | `openssl rand -hex 32` · **64 chars** (tope 100) | 🔑 **generado y cargado por firma del founder (`L-409`)**, con la pista como mano ejecutora. **El valor no está en ningún archivo del repo ni en ningún commit** — verificado por barrido en los dos árboles, el scratchpad y `git grep` sobre `rev-list --all`: **0 coincidencias**. Lo envía el founder a DeUna **por canal aparte de la URL** |
| `DEUNA_WEBHOOK_SECRET_SIGUIENTE` | ⚪ **no existe, y es correcto** | vacío | fail-closed **medido** (`coincide()` rebota con vacío); se carga al primer ciclo de rotación |
| `DEUNA_BASE_URL` | ⚪ **NO se fija** | — | es **override del simulador local** y **tiene candado**: en `produccion` se ignora. El default ya es QA |

**Verificado por digest en `secrets list`** *(no por el «ok» del comando: el `set`
del webhook no llegó a imprimir su confirmación y no se dio por bueno)*:
`DEUNA_WEBHOOK_SECRET` `a83d56d4…` · `DEUNA_POINT_OF_SALE` `132d70da…` ·
`PAGOS_AMBIENTE` `b7ad5674…`.

**Restricción medida para el secreto del webhook:** el proveedor acepta header
**value ≤ 100 caracteres** (§2). Lo que se genere entra en ese techo.

### (b) El mensaje a Gabriela — para que lo mande el founder

Los dos datos salen del **código**, no de la letra:
`HEADER_SECRETO = 'x-epetplace-secret'`
(`pagos-deuna-webhook/index.ts:60`) y el project ref del proyecto linkeado.

**Verificado contra los límites de §2 antes de escribirlo:** URL `https`, **sin
IP**, **73 caracteres** (tope 200) · header key **18** (tope 50) · value ≤ 100.

```
Hola Gabriela, buen día.

Van los datos para que registren nuestro webhook de pagos exitosos en QA:

URL (POST, https):
https://zyltipqscdsdsxnjclhp.supabase.co/functions/v1/pagos-deuna-webhook

Header de autenticación:
  nombre: x-epetplace-secret
  valor:  <PENDIENTE — se envía por canal aparte>

Confirmanos por favor:
1) que quedó registrado y desde cuándo está activo;
2) el procedimiento para CAMBIAR ese valor más adelante y cuánto demora
   (tenemos previsto un cambio de secreto sin corte y necesitamos saber
   la ventana);
3) si para producción se registra una URL distinta o se reemplaza esta.

Gracias.
```

🔴 **El valor del header NO va en este mensaje ni en ningún chat**: se genera
con firma del founder y viaja por canal aparte. *Un secreto pegado en el mismo
mensaje que la URL entrega las dos mitades juntas.*

⚠️ **El punto 2 no es cortesía: es la mitad que faltó de la pregunta §12.2**, y
sin su respuesta el ciclo de rotación no tiene número.

---

---

## TURNO 2 — la tanda: D-912, el despliegue, y el riel vivo en QA

**Contra qué objeto:** `main` en `4d82019f` (con el rescate de A adentro,
verificado: buzón de 306 líneas, `verificado`×2, `origen`×2, contrato de
`D-887` presente) · base y functions vía CLI · **ambiente del proveedor = QA
real**, no simulador.

### ① `D-912` — curada en sus dos mitades

La ficha describía bien la causa. **Medir antes de aplicar trajo dos cosas que
la ficha no tenía, y las dos cambiaron la cura:**

1. 🔴 **El comentario del trigger viejo afirmaba una propiedad que no tenía.**
   Decía *«se sella en el INSERT y NUNCA MÁS … un UPDATE ya no puede tocar este
   veredicto»*. **Medido: había UN solo trigger, y es BEFORE INSERT.** Nada
   impedía que un UPDATE reescribiera la columna. Lo que S103 quitó fue la
   *derivación desde el texto*; **la escritura explícita quedó libre.**
   *La propiedad real era más débil que la escrita, y no se notaba porque
   ningún camino la ejercía.*
2. 🔴 **La cura ingenua rompía el buzón entero.** `verificarStoken` puede
   devolver `'desconocida'` y el CHECK vivo solo acepta `NULL/SERVER/CLIENT`
   ⇒ escribirla tal cual **hacía fallar el UPDATE** y el análisis caía al catch:
   *habríamos cambiado un evento sin veredicto por un evento sin análisis.*

**La cura, en dos piezas:** la DB deja de derivar del texto y gana un guard
`BEFORE UPDATE` **de una sola vía** —`NULL → valor` permitido (el sellado tardío
que `D-912` necesita), `valor → otro` y `valor → NULL` **rebotan**—, que **hace
verdadero lo que el comentario afirmaba**; y la edge escribe la credencial en su
columna con el mapeo de ②.

`20260824235000` · **reversa escrita ANTES** con su nota de que **revertir
reabre `D-912`** · **76(g) declarada NO RIGE** (DDL sin backfill) · **cinturón
6/6 con dos rojos PRODUCIDOS** (`42501` al cambiar y al borrar un veredicto
sellado) **y residuo 0** · **412 local = remoto**.

### ② Desplegadas — y el `verify_jwt` se decidió midiendo, no por defecto

Gate `verify-edge-deno` **VERDE, 28 functions**, corrido **sobre copia fuera del
repo** (el canon avisa que `deno` escribe en `package.json`; el árbol quedó
intacto, verificado). Las cuatro **ACTIVE**, comprobado contra el objeto y no
contra el «ok» del comando:

`pagos-deuna-solicitud` v1 `verify_jwt=true` (valida `getUser()` adentro) ·
`pagos-deuna-webhook` v1 **`false`** (DeUna no manda JWT; su guard es el header)
· `pagos-deuna-barrido` v1 **`false`** (`x-despacho-secret` en el cuerpo) ·
`pagos-webhook-stg` v37 `false` (redeploy con la cura).

### ③ 🟢 EL RIEL CORRIÓ CONTRA QA REAL — el bloqueante murió

`payment/request` → **200**, con `transactionId`, **`numericCode` de 6 dígitos**
(la firma ① del founder), QR y deeplink. **7 ✅ · 1 ❌ · 1 ⚪** en 24,7 s.
**Los dos hallazgos valen más que los verdes** — detalle en
`docs/relevamientos/S105-D-MEDICIONES-CHECKPOINT-1.md` §2:

- 🔴 **el discriminador del fantasma no sirve**: una transacción **real** recién
  creada devuelve `amount=0` y `date=""` igual que una inexistente;
- 🔴 **`refund` sobre algo no pagado NO rebota**: `200` con `status:true` y
  *«The QR with id 4262774 has been successfully cleaned»* — **y 4262774 es
  nuestro `pointOfSale`, no la transacción.** No se volvió a ejercer.

**`REVERSED` / `REVERSED_FAILED` NO se ejercieron, y se declara en vez de darse
por probado:** `refund` exige una transacción **aprobada**, y aprobar exige que
una persona pague desde la app. **DeUna dijo que se simulan (WhatsApp,
24-ago-2026) pero no dijo cómo.** Tres preguntas concretas al proveedor en el
depósito §3.

### ④ Depositadas §1.1 y §1.2

`docs/relevamientos/S105-D-MEDICIONES-CHECKPOINT-1.md` — el hueco que A nombró
del Checkpoint 1. **Avisado a A por nombre** para que corra el reproceso de los
cuatro eventos del grupo ①.

### Operativo del turno

**🔴 La rama NO estaba en origin** —lo midió A— y **todo el turno 1 vivía solo
en disco**. Pusheada y **verificada por SHA** (`3eac3ba7` local = remoto),
jamás por código de salida (`L-239`). *El mismo agujero que le pasó a B; acá lo
cerró un aviso ajeno, no un gate propio.*

**El worktree tenía el link de Supabase incompleto** (solo
`linked-project.json`, sin `pooler-url` ni `project-ref`) ⇒ toda query fallaba
con un error que decía «falta `SUPABASE_DB_PASSWORD`», que **no era la causa**.
Se completó el cache local; está ignorado por `supabase/.gitignore`.

**`DEUNA_POINT_OF_SALE` se depositó también en el keychain** (el sondeo lo lee
de ahí, no de los secrets de Supabase). **Es dato del proveedor, no secreto
inventado** — `L-409` se respeta: lo único generado fue el secreto del webhook,
con firma.

---

---

## 🔴 TURNO 3 · EL HALLAZGO — «GENERAR UN CÓDIGO NUEVO» NO PUEDE FUNCIONAR

**Salió de una corrección ajena.** A midió que `pagos-deuna-solicitud` llama a
`verificar_compuertas_pre_cobro` —desmintiendo su propia hipótesis de que la
compuerta 0 no estaría en el camino del modelo push— y me lo pasó para achicar
otro contrato. **Achicó ése y abrió éste.**

### La cadena, medida eslabón por eslabón contra el objeto

| # | eslabón | medido |
|---|---|---|
| ① | `LETRA_DEUNA` §5 + **firma ② del founder** | *«mientras viva el hold, el cliente puede regenerar códigos»* · código vencido + hold vivo → **botón «Generar un código nuevo»** |
| ② | ese botón llama a la misma puerta | `pagos-deuna-solicitud` |
| ③ | la puerta consulta la compuerta **antes** de crear el intento | `index.ts:151` |
| ④ | la compuerta rebota `pago_en_proceso` si hay intento en `('iniciado','pendiente')` | y **NO mira `codigo_expira_en`** |
| ⑤ | el intento viejo quedó `pendiente` y **nada lo cierra** | **cero funciones de la base mencionan `codigo_expira_en`**; el barrido escribe `hallazgo` y `hallazgo_en`, **jamás `estado`** |

> ### ⇒ El primer código de una compra es el ÚNICO que se puede pedir. Vencido a los 3 minutos, la persona queda con el hold vivo y sin forma de obtener otro.
>
> **Y lo peor no es que no pueda: es lo que se le dice.** Recibe
> `pago_en_proceso` — *«ya tenés un pago en curso»*— **cuando lo que tiene es un
> código muerto y ninguna plata en vuelo.** *Una voz correcta para un estado que
> no es el suyo es peor que un error: el error manda a soporte, esto manda a
> esperar algo que no va a pasar.*

**No falla, no lanza, no deja traza:** devuelve un código tipado de la familia ②
del contrato del wrapper, que es una respuesta *legítima*. **Es la clase de
defecto que esta casa ya pagó dos veces esta semana** — el actuador muerto de
S103 y el buzón viejo de `main`.

### Lo que NO es, para no inflarlo

- **No bloquea el estreno**: el primer código de cada compra funciona.
- **Es teórico hoy**: cero sujetos con más de un intento pendiente (medido por A).
  *Pero deja de ser teórico con la primera persona que se distraiga tres
  minutos*, y el TTL de 3 min lo hace probable, no excepcional.
- **No es defecto de quien lo construyó**: la compuerta protege un invariante
  real y **la letra del riel nació después**. Lo que falta es el eslabón entre
  las dos.

### 🔴 Y el borde que ninguna cura puede ignorar

**Si la compuerta simplemente dejara de contar los intentos vencidos, quedarían
DOS códigos vivos del lado del proveedor** —medido en el sondeo: regenerar con
la misma referencia devuelve un `transactionId` nuevo y el viejo sigue
`PENDING`— **y los dos se pueden pagar.** *La compuerta impide crear el segundo;
no impide pagar el primero* (el borde que A dejó declarado). **Ablandar la
compuerta sin matar el código viejo cambia un cliente trabado por un doble
cobro, que es peor.**

⇒ **La cura tiene que MATAR el código viejo, no ignorarlo.** Tres caminos, y
**ninguno lo decide esta pista**:

| | camino | de quién |
|---|---|---|
| (a) | la compuerta ignora los vencidos | **A** — ⛔ **sola no sirve**: abre el doble cobro de arriba |
| (b) | la puerta **cierra** el intento viejo antes de abrir el nuevo | **D** + vocabulario de estado (¿`expirado`?) — **letra** |
| (c) | la regeneración **reusa** el mismo intento | **D** — pide medir el candado UNIQUE |

### 💡 La hipótesis que conecta los dos hallazgos del día — **y es hipótesis, no dato**

El sondeo midió que `payment/refund` sobre una transacción **no pagada** no
rebota: devuelve `200` con *«The QR with id 4262774 has been successfully
cleaned»*. **Eso podría ser exactamente el mecanismo de ANULACIÓN de código del
proveedor** — lo que la cura (b) necesita para matar el viejo del lado de DeUna
y no solo del nuestro.

⚠️ **No se ejerce hasta tener respuesta**: `4262774` es el `pointOfSale`, no la
transacción, y si limpia el QR del punto de venta entero, usarlo para anular un
código **tumbaría los códigos vivos de todos los demás clientes.** *Es la
pregunta 3 que ya está con el founder, y ahora vale el doble: no es curiosidad
sobre un rebote raro — es el insumo de una cura.*

---

---

## FIRMAS DEL FOUNDER SOBRE EL HALLAZGO *(24-ago-2026)*

| | qué se firmó | estado |
|---|---|---|
| ① | **El actuador acepta `'expirado'` HOY**, sin esperar a DeUna | ✅ **FIRMADA con A** — es suya, esta pista no la toca. *Motor listo, puerta cerrada: no habilita nada por sí sola porque hoy nadie produce ese estado* |
| ② | **La ley de la carrera**: aun anulado, el actuador sigue aceptando `'expirado'`, porque entre el pedido de anulación y su ejecución alguien puede pagar | ✅ **ANOTADA** |
| ③ | **El rebote de anulación fallida necesita CÓDIGO PROPIO** — `pago_en_proceso` es falso ahí | ✅ **FIRMADO que hace falta.** ⏳ **El nombre y la voz se proponen AL ARMAR LA PUERTA**, y los mira la mesa |

> **El literal que ordena ③, del founder:** *«que la cura arregle el camino feliz
> y deje la mentira en el camino triste es peor que no curar: la persona ya está
> frustrada cuando llega ahí.»*

### ⏳ Insumos ya medidos para cuando toque proponer ③ — para no improvisarlo

**Son TRES situaciones distintas y hoy las tres dicen `pago_en_proceso`:**

1. **hay un pago de verdad en vuelo** → `pago_en_proceso` **es correcto acá, y
   solo acá**;
2. **el código venció y se pudo anular** → *no debería haber rebote*: la puerta
   sigue de largo y entrega uno nuevo;
3. 🔴 **el código venció y NO se pudo anular** → **es el que no tiene nombre.**

*La voz de (3) tiene que decir dos cosas que hoy no dice: que no hay ninguna
plata en juego, y que la espera no es infinita —muere con el hold—.* **Familia
del contrato del wrapper: es ① DEFECTO NUESTRO, no ② compuerta** —la persona no
hizo nada mal y no puede arreglarlo reintentando— *y eso ya cambia la voz antes
de elegir el nombre.*

---

---

## MEDICIÓN FUERA DEL RIEL · EL TOKEN DE META *(pedido del founder, 24-ago)*

**Solo lectura. No se mandó un mensaje, no se encendió el transporte, no se tocó
el cron.** Instrumento: `despachar-whatsapp?verificar=1`, que **es todo GET
contra Meta** y existe separado del despacho a propósito.

**Control negativo primero** (la function está viva y su guard corta antes de
tocar nada): `POST` sin secreto → **`401 despacho_no_autorizado`**. *Es el guard
interno el que rebota, no el gate de Supabase.*

**El secreto se leyó del `vault` (`despacho_secret`) y se usó sin pasar por la
salida** — nunca impreso, ni en el comando ni en el reporte. *Pedírselo al
founder por chat habría sido exponerlo para poder medirlo.*

### 🔴 EL RESULTADO: EL TOKEN NO SIRVE

```
token_forma: { largo: 23, empieza_con_EAA: false, tiene_espacios: false,
               tiene_comillas: false, tiene_salto: false,
               parece_un_id_numerico: false }
http_plantillas: 401 · http_numero: 401
error: "Invalid OAuth access token - Cannot parse access token"
```

**Un token de System User de Meta tiene ~200 caracteres y empieza con `EAA`.**
El cargado tiene **23** y no empieza con `EAA`. **Y la forma descarta las causas
baratas**: no está truncado por comillas, ni por salto de línea, ni es un id
numérico pegado en el campo equivocado. **Es otro dato.**

⚠️ **Y esto NO es un hallazgo nuevo: es un estado que el canon ya tenía escrito
desde S91** — *«el bloqueo tiene nombre: la credencial NO es un token de Meta
(largo 23, sin `EAA`, sin comillas ni salto ⇒ no está truncada, es otra cosa)»*.
**Catorce sesiones después, el mismo largo 23.** *La pregunta «¿el token sirve?»
tenía respuesta depositada; lo que faltaba no era medirla sino leerla.*

### 🔴 LO QUE LA MEDICIÓN **NO** PUEDE CONTESTAR — y es la mitad del pedido

**Se pidió saber si el token trae `whatsapp_business_messaging` y
`whatsapp_business_management`. Esa pregunta queda SIN RESPUESTA, por dos
razones independientes:**

1. **Con un token que no parsea, Meta no informa permisos de nada.** No hay
   sobre qué preguntar.
2. 🔴 **Y aunque el token fuera válido, ESTE instrumento no mide permisos.**
   Medido en su cuerpo: consulta `…/{waba}/message_templates` y `…/{phoneId}`.
   **No llama a `/debug_token`, que es el único endpoint que devuelve los
   `scopes`** — y también es GET, así que cabría sin romper la regla de «no
   manda nada».

   *Lo que este modo puede probar por lectura exitosa es `…_management` (leer
   plantillas lo exige). **`…_messaging` es el permiso de ENVIAR y este
   instrumento no lo ejerce a propósito** — probarlo sin `/debug_token`
   obligaría a mandar un mensaje, que es justo lo que no se hace.*

⇒ **Deuda de instrumento, no de credencial:** el modo `verificar` necesita una
llamada a `/debug_token` para contestar lo que se le preguntó. **Es
`despachar-whatsapp`, que NO es territorio de esta pista** (`pagos-*`) — se
declara, no se toca.

### ⚠️ EL NÚMERO QUE NO SE DEBE LEER MAL

La respuesta trae **`plantillas_total: 0`**. **Eso NO desmiente las 7 plantillas
aprobadas el 23-ago**: es la consecuencia del `401`. **No se pudo preguntar.**
*Un cero que viene de una puerta cerrada no es un cero medido, y leerlo como
inventario haría concluir que no hay plantillas cuando lo que no hay es
permiso.*

---

---

## SEGUNDA PASADA DEL TOKEN — el token sirve, y el problema es OTRO

**Territorio:** `despachar-whatsapp` **asignado explícitamente por el founder**
en este turno *(«es tu territorio»)* — **resuelve la decisión de reparto que
estaba con él**. Antes de esto, esta pista lo había declarado ajeno y no lo
tocaba. **Veda de A levantada** (`5ac86c72`, ancla `e4ada3b2`) antes de escribir.
**Solo medición: no se encendió el transporte, no se tocó el cron, no se mandó
un mensaje.**

### ✅ EL TOKEN SIRVE, Y TRAE LOS DOS PERMISOS — medido, no supuesto

```
token_valido: true · http_debug_token: 200
whatsapp_business_messaging  : true
whatsapp_business_management : true
```

**Contra la pasada de ayer:** largo **23 → 196**, `empieza_con_EAA` **false →
true**, y el `401 Cannot parse access token` **desapareció**. *El largo 23 que el
canon arrastraba desde S91 murió hoy.*

⚠️ **Y esto solo se pudo afirmar porque el instrumento ganó `/debug_token` en
esta misma pasada.** Con el diagnóstico de ayer, la respuesta habría sido *«las
plantillas no cargan»* y **los permisos habrían seguido sin medirse** — que era
la mitad del pedido original.

### 🔴 EL PROBLEMA REAL: LOS OTROS DOS SECRETOS NUNCA SE CARGARON

```
error_plantillas: Object with ID 'tu_waba_id' does not exist…
error_numero    : Object with ID 'tu_phone_number_id' does not exist…
```

> ### `META_WABA_ID` vale literalmente **`tu_waba_id`** y `META_PHONE_NUMBER_ID` vale **`tu_phone_number_id`**. Son los PLACEHOLDERS de la plantilla de configuración, no identificadores.

**Coincide con lo que A midió por otro lado:** esos dos secretos son **del
08-ago y nunca se tocaron**. *Se cargaron una vez con el texto de ejemplo y
nadie volvió.*

**⇒ La pregunta «¿apunta al WABA ecuatoriano o al de Estados Unidos?» no tiene
respuesta porque no apunta a NINGUNO.** *No es el WABA equivocado: es que ahí no
hay un WABA.*

### 🟢 Y LA BUENA NOTICIA, que corrige la premisa con la que se pidió la medición

El founder advirtió: *«un token válido contra el WABA equivocado devuelve verde
y cero plantillas — el peor resultado, porque no da síntoma»*. **Ese escenario
no ocurrió, y no podía ocurrir con este valor:** un id inexistente **da `400` y
el mensaje NOMBRA el valor**. *El modo de falla que se temía —silencioso— no es
éste; éste grita.* **El peligro sigue vivo para el día que se cargue un WABA
real pero equivocado**, y para ese día el instrumento ahora tiene
`waba_configurado_alcanzable`.

### ⚠️ UN CERO QUE NO ES UN CERO — segunda vez en dos días

`waba_alcanzables` volvió **vacío**. **Eso NO significa que el token no alcance
ningún WABA:** significa que `granular_scopes` no trajo `target_ids`. **Es un
no-dato, no un cero medido** — exactamente lo mismo que ayer con
`plantillas_total: 0`, que venía de una puerta cerrada. *Dos veces en dos días
el mismo riesgo de lectura: **un cero producido por no haber podido preguntar se
ve idéntico a un cero que se midió.***

⇒ **La lista de WABA hay que sacarla del panel de Meta**, no de acá.

### Lo que el instrumento ganó en esta pasada

| | |
|---|---|
| `components` en el `fields` | ✅ pedido del founder — **y con las `{{n}}` ya extraídas**, `componentes[]`, `variables[]` y el `body` recortado. *El mapeo se escribe contra esa lista y no re-parseando en cada lector* |
| `/debug_token` | ✅ **cierra el hueco que permitía un verde entero con un token sin `messaging`** |
| `waba_alcanzables` + `waba_configurado_alcanzable` | ✅ para el día del WABA real pero equivocado |
| Gate `verify-edge-deno` | ✅ **VERDE, 28 functions**, sobre copia aislada · **el conteo fuera de clase quedó en 89, igual que antes: no se agregaron errores** |

**⚠️ `components` todavía NO se pudo ejercer:** con el WABA en placeholder, las
plantillas no cargan. **La línea está puesta y sin probar, y se declara así** —
*se prueba sola en cuanto el `META_WABA_ID` real entre.*

---

---

## 🟢 EL CANAL DE WHATSAPP, MEDIDO ENTERO — y las 7 plantillas con su mapeo

**`200 / 200 / 200`** (debug_token · plantillas · número). **Token válido con
los dos permisos. Número `+593 98 733 0099` · `E-PetPlace`.** *El bloqueo que el
canon arrastraba desde S91 está resuelto.*

### El mapeo — lo que la sesión de implementación necesita

**Todas las variables viven en el `BODY`. Ningún `HEADER` ni `BUTTON` tiene
variables** ⇒ **un solo array de `parameters` por plantilla.**

⚠️ **Eso NO se sabía antes de medirlo, y por eso el instrumento ganó
`variables_por_componente` en esta misma pasada.** En la API de Meta la
numeración `{{1}}` **se reinicia en cada componente**: el `{{1}}` del HEADER y
el del BODY son datos distintos que viajan en listas distintas. *El set
unificado los habría fundido en una sola entrada y producido un mapeo que
typechequea, se ve completo, y manda el nombre de la familia en el lugar de la
fecha.* **Salió limpio, pero la limpieza es un hallazgo, no un supuesto.**

| # | plantilla | estado | `{{1}}` | `{{2}}` | `{{3}}` | `{{4}}` |
|---|---|---|---|---|---|---|
| 1 | `entrega_proxima` | 🟡 PENDING | nombre | pedido | fecha estimada | — |
| 2 | `pedido_no_se_pudo_entregar` | 🟡 PENDING | nombre | fecha del intento | teléfono de contacto | — |
| 3 | `plan_renovacion_fallida_u` | ✅ APPROVED | nombre | monto | **⚠️ ver abajo** | — |
| 4 | `plan_renovado_v` | ✅ APPROVED | nombre | fecha | cuenta / medio | importe |
| 5 | `cita_recordatorio_hoy_v` | ✅ APPROVED | nombre | prestador | fecha | hora |
| 6 | `cita_recordatorio_manana_v` | ✅ APPROVED | nombre | prestador | fecha | hora |
| 7 | `cita_confirmada_v` | ✅ APPROVED | nombre | fecha | servicio | nº de confirmación |

### 🔴 CUATRO HALLAZGOS QUE EL CONTEO NO MUESTRA

**① NO son «7 aprobadas»: son 5 aprobadas y 2 PENDING.** Y las dos pendientes
son **las dos de pedidos** (`entrega_proxima`,
`pedido_no_se_pudo_entregar`) ⇒ **el canal de la despensa no puede despachar
todavía**, y el de citas y planes sí. *Un «7 aprobadas» habría hecho planificar
la despensa sobre un canal que Meta no habilitó.*

**② 🔴 `code_verification_status: EXPIRED`.** El número está `Conectado` en el
panel y su **verificación figura vencida**. *No se midió si eso frena el envío —
este modo no manda nada a propósito— pero es lo primero que hay que mirar antes
de encender, porque es exactamente la clase de dato que aparece recién en el
primer envío real.* `quality_rating: UNKNOWN` (esperable sin tráfico).

**③ 🔴 `plan_renovado_v` NO DICE LO QUE SU NOMBRE DICE.** Su nombre sugiere un
plan **ya renovado**; su texto es un **aviso PREVIO**: *«recordarte sobre tu
próximo pago automático»*. **Elegirla por el nombre manda el mensaje equivocado
en el momento equivocado** — le avisaría a alguien que va a pagar cuando ya
pagó. *El nombre de una plantilla es del panel de Meta; su significado es el
texto. Gana el texto.*

**④ ⚠️ `plan_renovacion_fallida_u` tiene un ARTÍCULO FIJO antes de `{{3}}`:**
*«tu pago de {{2}} para **la** {{3}}»*. **El valor tiene que ser femenino** —
«la **suscripción**», «la **renovación**»—. *Con «el plan mensual» el texto sale
«para la el plan mensual».* **Es una restricción del mapeo, no una preferencia
de redacción.**

**⑤ Dato, no defecto:** `cita_recordatorio_hoy_v` y `cita_recordatorio_manana_v`
son **byte-idénticas** (mismo HEADER, mismo BODY, mismas 4 variables). **La
diferencia hoy/mañana no está en el texto: está en el dato que se le pasa.**
*Que existan dos no molesta; conviene saber que elegir una u otra no cambia lo
que la familia lee.*

### 🟢 Y una preocupación del canon que se puede cerrar

**`MARKETING: 0 · UTILITY: 7`.** El canon marcaba la recategorización
MARKETING→UTILITY como **crítica** —*«un número pausado se recupera por
APELACIÓN, no por corrección»*—. **Medido: no hay ninguna en MARKETING.** *Ese
riesgo no está pendiente: está resuelto.*

---

---

## 🔴 ESTADO DIVERGENTE — lo que CORRE no está en el canon

**Medido contra el objeto**, no contra el recuerdo del despliegue:

```
debug_token en origin/main …… 0 ocurrencias
debug_token en pista/s105-d … 3
commits e86ddd20 y 4ad67564 … NO son ancestros de origin/main
```

**Lo desplegado en `despachar-whatsapp` salió de este worktree, no de `main`.**
Producción corre `/debug_token`, `components` y `variables_por_componente`; el
canon no los tiene.

> ### Es el mismo patrón que el buzón viejo de ayer, **al revés**: entonces `main` era más nuevo que lo desplegado y desplegar desde `main` lo habría roto; ahora `main` es más viejo, y **el próximo despliegue desde `main` borra el instrumento sin que nadie lo note.**
>
> *Se perderían las tres piezas a la vez, y las tres fallan por omisión: el
> diagnóstico volvería a dar verde sin poder ver los permisos ni las variables.*

**Cura: A mergea `pista/s105-d`.** *Se declara acá porque un despliegue que no
tiene commit detrás es inauditable después — y el que lo redespliegue no va a
tener forma de saber qué perdió.*

## RECONCILIACIÓN · «¿el diagnóstico prueba `…_messaging`?»

**La preocupación era de esta misma pista y se parte en tres, no en dos:**

| | |
|---|---|
| ✅ **Se disuelve** | *«no sabemos si el token trae `…_messaging`»* — **`/debug_token` lo contesta: `true`.** Con nombre, no con conteo |
| 🔴 **SIGUE VIVA** | *«el permiso de ENVIAR no lo ejerce ningún instrumento»* — **cierto, y a propósito.** `/debug_token` dice que el permiso está **otorgado**; **no dice que un envío funcione.** *Son dos afirmaciones distintas y solo la primera está medida* |
| ⚠️ **Y una tercera** | **para quien lea `main`, la preocupación es literalmente cierta**: ahí `/debug_token` no existe |

**Y hay razón concreta para no dar el envío por bueno:**
`code_verification_status: EXPIRED`. *Un permiso otorgado sobre un número con
la verificación vencida es exactamente el caso donde «tiene el scope» y «puede
mandar» se separan.* **Lo único que prueba un envío es un envío, y este modo no
manda a propósito.**

---

---

## CIERRE DEL FRENTE DE WHATSAPP — lo firmado y lo que queda abierto

**Firmas del founder, 25-ago-2026:**

| | |
|---|---|
| **Territorio** | `despachar-whatsapp` **queda en esta pista** mientras dure el frente de envío. ⚠️ **Falta que A lo deposite en los territorios** |
| **Merge** | pedido a A: **las tres piezas del diagnóstico no están en `main`** y un despliegue desde ahí las borraría |
| **El panel** | ✅ **falsa alarma resuelta por medición**: el founder estaba parado en el **otro WABA**. **Nada se borró** — la API devolvió las 7 idénticas a las `04:00:22 UTC` |

### 🔴 LO ANOTADO Y SIN CERRAR — la ley que queda de este frente

> ### Que el permiso de enviar esté **otorgado** no prueba que un envío **funcione**. Eso solo lo contesta un envío real.
>
> **Y ese no se hace hasta dos condiciones, las dos del founder:** que el mapeo
> exista, y que `code_verification_status` **deje de estar `EXPIRED`**.

*Es la misma forma que mordió tres veces esta semana —el actuador que nunca
corrió, el buzón que respondía 200 sin escribir `verificado`, el barrido que
clasificaba y no aplicaba—: **«está alcanzable» y «corrió» son preguntas
distintas**, y acá se agrega la tercera cara, que es la más fina: **«tiene el
permiso» tampoco es «puede».***

### Lo que este frente deja medido y no hay que volver a medir

**Token válido con los dos permisos, por nombre** · **7 plantillas, 5 aprobadas
y 2 PENDING** (las dos de pedidos ⇒ la despensa no despacha por este canal) ·
**el mapeo completo**, con todas las variables en `BODY` y ninguna en `HEADER` ·
**`MARKETING: 0`** ⇒ el riesgo de pausa que el canon marcaba como crítico **está
resuelto** · y **tres trampas del mapeo escritas** (`plan_renovado_v` es aviso
PREVIO pese a su nombre · `plan_renovacion_fallida_u` exige valor femenino en
`{{3}}` · las dos de recordatorio son byte-idénticas).

---

---

## 🟢 EL REVERSO DE NUVEI, EJERCIDO DE VERDAD — el caso de certificación

**Primera vez que este endpoint se ejerce en la historia del producto.**
`DF-2102135` · `oDWlzY` · $24.90 · staging · **11:46 Guayaquil, dentro de
ventana**. Autorizado por Erick al teléfono.

### La respuesta CRUDA del proveedor, sin resumir

```json
{"status":"success","detail":"Completed","transaction":{
  "id":"DF-2102135","status":"success","current_status":"CANCELLED",
  "status_detail":7,"amount":24.9,"carrier_code":"ReversedByMerchant",
  "message":"Reverse by mock","authorization_code":"oDWlzY",
  "carrier":"DataFast","trace_number":"374099","lot_number":"001202"}}
```

### ✅ LA PREDICCIÓN SE CUMPLIÓ PUNTO POR PUNTO

*Escrita en la ficha `D-923` **antes** de ejercer, para que el resultado pudiera
falsarla en vez de acomodarse a ella.*

| se predijo | se midió |
|---|---|
| llega un evento nuevo | ✅ **2 eventos** para esa tx (era 1) |
| se autentica | ✅ `stoken=true` · `credencial=SERVER` |
| su `resultado` **no** será `reversado` | ✅ quedó **`desconocido`** |
| el intento seguirá `aprobado` | ⚠️ **quedó `reversado`** — *por nuestra RPC nueva, no por el actuador* |
| el pedido seguirá `pago_capturado` | ✅ **intacto**, `updated_at` sin moverse |

### 🔴 EL REGISTRO LITERAL DE `D-923`, que es lo que se le muestra a Erick

```
EVENTO 15:06:03 · el cobro
  resultado=aplicado · status=1/3
  actuador: {"ok": true, "pedidos_confirmados": 1}

EVENTO 16:47:39 · el reverso
  resultado=desconocido · stoken=true · cred=SERVER · status=2/7
  actuador: status=2 no confirma        ← 🔴 D-923, en su propia voz
```

> ### **Nuvei SÍ manda webhook del reverso. Llega, se autentica, y el actuador lo descarta con «status=2 no confirma».** El pedido queda pagado.
>
> *No es una hipótesis ni una lectura de código: es el sistema diciéndolo en su
> propio registro, con la plata ya devuelta del otro lado.*

### 🔑 EL DATO NUEVO QUE CAMBIA LA CURA DE `D-923` — y no lo teníamos

**Llegó `status=2` con `status_detail=7`.** La letra del motor mapea los
reversos **por `status_detail`** (`7` total · `34` parcial · `27`/`28`
pendiente), **pero el actuador decide por `status`** — y ahí ve un `2` que no
distingue un reverso de cualquier otro no-aprobado.

⇒ **El discriminador del reverso es `status_detail`, no `status`.** *Una cura
que enseñe al actuador a mirar `status` va a saber que «no fue aprobado» y
seguir sin saber que **fue devuelto**.* **Dato para A.**

### Lo que quedó construido, y su mitad declarada

**`registrar_reverso_nuvei`** (`20260825201500`) · **`pagos-reverso`** (edge,
ACTIVE). **Reversa escrita ANTES** con su orden obligatorio —primero la edge,
después la función— porque al revés queda **plata devuelta sin rastro nuestro**.
**76(g) declarada NO RIGE. Cinturón 6/6 con 4 rojos producidos, residuo 0.**

**Las tres reglas quedaron por construcción, no por comentario:** no hay dónde
poner un monto —la edge lo **rechaza** (`monto_no_se_recibe`, verificado) y el
request no lleva `order.amount`— · `more_info: true` siempre · `failure` del
carrier se reintenta hasta 5 (esta corrida: **1 intento**).

⚠️ **Y su mitad declarada: el sujeto NO se movió, a propósito.** La respuesta lo
dice (`sujeto_movido: false`) en vez de dejarlo notar. **Mover el pedido es
`D-923` y es de A** — hacerlo desde acá duplicaría al actuador.

### Dos hallazgos de paso

**① Nuvei NO devuelve un id de reverso distinto:** `transaction.id` vuelve
siendo `DF-2102135`. ⇒ **`proveedor_reverso_id` quedó igual a
`proveedor_transaction_id` y no aporta información.** *El `transactionReverseId`
que nombra la letra de DeUna **no tiene equivalente en Nuvei**.* Lo que sí
distingue el reverso es **`current_status: CANCELLED`** y
**`carrier_code: ReversedByMerchant`**.

**② El cinturón hizo su trabajo dos veces antes de dejar pasar la migración**, y
las dos me enseñó un invariante que yo no conocía: `chk_intento_un_solo_sujeto`
(no hay intento sin sujeto, **ni siquiera de prueba**) y `clave_idempotencia`
NOT NULL. *Los dos fallos fueron míos y los dos abortaron la migración entera —
que es exactamente para lo que está.*

---

---

## LOS DOS BRAZOS SIN EJERCER DEL REVERSO — uno hecho, uno con hora

### ② EL REINTENTO: **ejercido, funciona — y el proveedor trajo una sorpresa**

**Caso: `transaction_id` inexistente**, sobre un fixture propio creado y
**borrado por id (residuo 0 verificado)**.

```
intentos: 5 · http: 500 · 7 segundos
crudo: {"error": {"type": "AttributeError", "help": "", "description": ""}}
resultado: reverso_no_confirmado — NADA se persistió
```

✅ **El bucle corre las 5 veces con espaciado creciente y no persiste un reverso
que no ocurrió.** La política de la doc está construida, no comentada.

🔴 **Y el hallazgo, que es del proveedor:** ante un `transaction_id` que no
existe, **Nuvei devuelve `HTTP 500` con un `AttributeError` de Python** — su
servidor se cae, en vez de contestar un `404` limpio.

> ### Un error NUESTRO (un id mal) llega con la misma forma que una caída DE ELLOS. Desde afuera son indistinguibles.

**Nuestra clasificación lo trata como transitorio y reintenta**, que es lo
correcto **por la dirección del riesgo**: *reintentar de más cuesta cuatro
segundos; no reintentar puede dejar plata sin devolver.* **Pero conviene saber
que esos 5 intentos, en este caso, no tenían ninguna chance** — el 500 era
determinístico. **Va como pregunta a Erick.**

⚠️ **LA OTRA MITAD SIGUE SIN EJERCER, y se declara:** *«un error FINAL corta al
primer intento»*. El caso elegido resultó ser 500, no 4xx, así que **la rama
`!transitorio → break` no se ejerció.** *No se fuerza pidiendo un segundo refund
sobre `DF-2102135` —ya reversado— porque eso es tocar plata sin autorización.*
**El caso que la probaría es un `4xx` del proveedor, y hoy no tenemos uno a
mano.**

### ① EL CORTE DE LAS 17:00: **con hora, y la fórmula ya verificada**

**No es ejercible antes de las `22:00 UTC`** — el guard mira `now()`, no una
hora del intento. *Medido: 16:20 Guayaquil al escribir esto.*

**Control parcial corrido, con su propia corrección adentro:** el primer test
dio **al revés** (16:59 UTC "cortaba", 22:00 "pasaba") y **el defecto era del
test, no de la función**: había una conversión de zona de más
(`AT TIME ZONE 'UTC' AT TIME ZONE 'America/Guayaquil'` sobre un `timestamptz`
ya es doble). Con la expresión que la función usa de verdad:

```
16:59 UTC = 11:59 Guayaquil → pasa
21:59 UTC = 16:59           → pasa
22:00 UTC = 17:00           → CORTA   ← el borde, exacto
23:30 UTC = 18:30           → CORTA
```

*Casi reporto un defecto que no existía.* **Un instrumento mal escrito produce
un rojo tan falso como un verde flojo** — y acá el instrumento era una línea de
SQL escrita al vuelo para verificar otra.

⚠️ **Y esto NO reemplaza el ejercicio:** probar la fórmula **no** es probar el
guard (`L-321`). **Queda con hora: después de las 22:00 UTC de hoy.**

---

---

# ═══ PARTE FINAL DE LA PISTA D — S105 ═══

> **Verificado contra el objeto, no contra el recuerdo.** Rama `pista/s105-d`
> @ **`2f62f8f5`** · local = origin = ls-remote (tres SHA idénticos) · árbol en
> **cero**.

## ⚠️ ENMIENDA AL CIERRE — `insertada` NO llegaba al navegador

Este parte decía *«`insertada` sin lector»*. **Era peor que eso: no había nada
que leer.** La RPC lo devolvía y **la edge lo dejaba caer en su `return`** ⇒
motor sin puerta **un piso más arriba** del que la migración de al lado acababa
de curar. Lo encontró **C**, yendo a escribir la voz que yo le había dicho que
ya podía escribir — y lo midió **contra `main`**, que era lo correcto.

**Curado (`pagos-alta-tarjeta v23`) y EJERCIDO POR HTTP, las dos ramas:**

```
① alta nueva, token nuevo    → insertada: true
② otro handle, MISMO token   → insertada: false
```

Fixtures limpiados: **residuo 0** y `tarjetas_guardadas` de vuelta en **9**, el
mismo número que antes de empezar.

🔴 **Fue el CUARTO error del día con la misma forma:** verifiqué la RPC con
`pg_get_functiondef`, vi el campo y **reporté el eslabón como si fuera la
cadena**.

> ### `insertada` sale de la columna de «sin ejercer»: ahora está ejercido. Lo que falta es su consumidor.

## 🔴 LO PRIMERO, PORQUE ES LO QUE PUEDE HACER DAÑO

**`main` NO tiene mis últimos tres commits**, y **dos son migraciones YA
APLICADAS al remoto**:

| migración | estado |
|---|---|
| `20260826160000` anotar sin cerrar | 🔴 aplicada · **fuera del canon** |
| `20260826170000` insertada | 🔴 aplicada · **fuera del canon** |

*Quien clone `main` hoy levanta una base cuyo esquema no puede reproducir.*
**Pedido a A: merge de `fcef1690`.** Es la cuarta vez en el día.

## CONSTRUIDO Y SIN EJERCER — con el número que lo prueba

| pieza | por qué no se ejerció | número |
|---|---|---|
| **`D-925` anotar sin cerrar** | la puerta existe y **la página no la llama** | **`incidentes` anotados: 0** |
| **`insertada`** | ninguna app lo lee — C esperaba que existiera para escribir la rama | — |
| **`sujeto_movido` medido** | los dos reversos reales fueron **ANTES** del trigger de A | `reversados: 2`, los dos previos |
| **El guard del IVA** | nunca corrió contra el proveedor. **Y tiene combustible real** | **12 ítems con `impuesto_pct ≠ 0`** |
| **Reintento ×5 (Nuvei)** | el brazo *«error final corta al primer intento»* — el caso disponible dio 500, no 4xx | — |
| **Ventana 17:00 contra un refund real** | **no se ejerció A PROPÓSITO**: mover plata para probar un freno es hacer lo que el freno evita | cinturón sobre `DF-2103629` |
| **`pagos-deuna-barrido` → aplicador** | el cron nació **INERTE por diseño** (`active=false`) | — |
| **`components` en el diagnóstico de WhatsApp** | bloqueado por **`code_verification_status: EXPIRED`** | — |

> ### El patrón de esta columna es uno solo: **casi todo lo que quedó sin ejercer espera un llamador o una firma, no más código.**

## ESPERA DE OTROS — con dueño

**A** · el merge (arriba) · reprocesar los **4 eventos del grupo ① de `D-912`**
(espera **firma del founder**: emite comprobante).

**C** · llamar `desenlace:'incidente'` desde la página — **sin ese llamador
`D-925` no junta un solo caso** · consumir `insertada` para la voz *«Esa
tarjeta ya está agregada»*, exigiéndolo explícito (ausente es *falsy* ⇒ diría
«nueva» sobre una re-carga).

**Founder** · las **dos preguntas a Erick** el jueves (el `success` sin `token`
· que **el SDK no manda `stoken` NUNCA**, ni en las que entran — evidencia
nueva para la pieza ④ de `D-921`) · **la ambigüedad 24 h vs «purchase day»** a
Carlos, que su propio `404` contradice · **la cadencia del cron** (mi voto:
**03:00 Guayaquil**, la letra — el webhook cerró en segundos y el barrido dio
`candidatos: 0`) · **la firma para mandar IVA real**, primera corrida contra la
cuenta de pruebas.

**Jueves** · el gate de la **cita reversada** — espera una **cita PAGADA**; la
voz de C ya está escrita (`eb5efd20`).

## LAS TRES QUE ME COBRARON HOY, para que no se repitan

1. **`db push` dijo `Finished` y registró una migración con la función sin
   cambiar.** Reparado a `reverted` y re-aplicado. **La causa no quedó
   explicada y no se inventó una.**
2. **Leí un `push` exitoso como prueba de que mi commit entró** — el gate de
   territorio lo había abortado y lo que subió era el merge de A.
3. **Reporté que faltaba la puerta del uid estable** y A la había curado
   veinte minutos antes. Re-medí antes de mandarlo.

> ### Las tres son la misma: **un mensaje de éxito probó algo distinto de lo que yo leí en él.** El objeto siempre estuvo disponible para preguntarle.

---

# ═══ TANDA `D-925` · ANOTAR SIN CERRAR — 25-ago, 21:30 ═══

**Pedido del founder:** medir si el `success` sin `token` del SDK deja rastro,
reproducirlo, y armar la pregunta para Erick. Después: **construir la cura.**

## LA MEDICIÓN — los tres lugares, uno por uno

| dónde | qué hay |
|---|---|
| **en las altas** | **sí, y es AMBIGUO.** `pendiente` + `stoken_detalle` NULL prueba que la RPC nunca se llamó — pero es **idéntico a «cerró la pestaña»** |
| **en los eventos** | **cero.** La edge corta en `token_ausente` (línea 116) **antes** de la RPC |
| **en los logs** | **no.** Un solo `console.error`, en la 169, del camino de error de la RPC. El dashboard registra el `400` sin decir de qué alta ni qué mandó el SDK |

**La secuencia de anoche, que es lo que informa de verdad:**

```
19:46:11  cd4c8f6a  →  pendiente · RPC NUNCA llamada
20:10:43  d762220b  →  pendiente · RPC NUNCA llamada
20:46:32  c1114731  →  pendiente · RPC NUNCA llamada
20:54:30  6a31e42b  →  GUARDADA en 23 s · vi ****1111
```

> ### Tres seguidas fallan y la cuarta entra con la misma tarjeta. **Es intermitente** ⇒ hasta que haya rastro **no se sabe si es del SDK, de la red o nuestro.**

**Reproducción determinista y sin efecto:** `POST` sin token → `400
token_ausente`. Rebota antes de tocar la base — **y ese mismo hecho es la razón
de que no haya rastro.**

## LA CURA — y el hallazgo de C es el que la ordenó entera

**El camino que ya existía era la trampa.** `desenlace:'rechazada'` CIERRA el
alta y el handle es de un solo uso ⇒ reportar por ahí le saca a la persona la
única acción que la voz le acaba de ofrecer.

> ### *Un rastro que rompe la salida que la voz promete es peor que la falta de rastro.*

`20260826160000` · cinturón **6/6** · residuo 0 · edge desplegada:
`altas_tarjeta.incidentes` (append-only, tope 10 **declarado**) +
`anotar_incidente_alta()`, que **no toca `estado` ni `cerrada_en` nunca**.
**El cinturón ④ es la razón de ser de la migración: anota y DESPUÉS resuelve
con el mismo handle.**

**El enmascarado se repite EN LA BASE**, no por desconfiar de C: *un
enmascarado que depende del llamador es una promesa, y del otro lado hay un PAN.*

**Y un agujero que la habría matado al nacer:** el parseo de `desenlace`
convertía **cualquier valor desconocido en `'guardada'` en silencio** ⇒ un typo
y la puerta nueva nace invisible. Ahora rebota `desenlace_invalido`.

## `insertada` — `20260826170000`, cinturón 3/3

Pedido de C, que hizo lo correcto y **no escribió la rama muerta**. `xmax = 0`
sobre el `ON CONFLICT`; se descartó un `EXISTS` previo **porque tiene carrera**.
`xmax` **no está en el contrato de Postgres** ⇒ el cinturón **ejerce las dos
ramas con el mismo token**: no se confía en el truco, se mide.

## ✅ LA VOZ DEL SUJETO, CORREGIDA EN LOS DOS RIELES

A cerró `D-923` con un trigger sobre la transición. **Mis dos edges devolvían
`sujeto_movido: false` cableado y pasaron a mentir el día que el trigger nació.**

> ### *Una constante que afirma sobre el mundo se vuelve falsa el día que el mundo cambia, y no lo avisa: no hay typecheck para una verdad vencida.*

Ahora **se mide** leyendo `payload_crudo.sujeto_no_movido`.

## 🔴 DOS TRAMPAS OPERATIVAS MEDIDAS HOY

**① `db push` registró `…170000` en `schema_migrations` con la función SIN
cambiar**, y la corrida siguiente contestó *«up to date»*. Reparado a `reverted`
y re-aplicado. **La causa no quedó explicada y no se inventó una.**
⇒ ***`Finished` y `up to date` no son prueba de nada; la prueba es preguntarle
al objeto.***

**② Reporté un commit que nunca ocurrió.** El gate de territorio abortó
(separador **espacio**, no coma) y leí el `push` como confirmación — pero lo
que subió fue el merge de A. Nada se perdió; la afirmación era falsa.
⇒ ***un push exitoso prueba que algo subió, no que fuera lo tuyo.***

## PARA ERICK, con la evidencia adentro

**①** `card` viene poblada —bin, últimos 4, marca, vencimiento— **y aun así no
vino `token`, con el callback de éxito corriendo.** ¿Qué significa un `success`
sin `token`, bajo qué condición pasa, y hay un campo de estado que estemos
ignorando?

**②** 🔴 **Todas** las altas que **sí** funcionaron traen `stoken_de=ninguno`
⇒ **el SDK no manda `stoken` nunca.** No leemos mal el campo: no viene.
**Evidencia nueva para la pieza ④ de `D-921`.**

---

# ═══ ESTADO AL COMPACTAR — 25-ago-2026, 20:30 Guayaquil ═══

> **Esto es el handoff.** Lo que no esté acá se perdió. **Todo verificado contra
> el objeto**, no contra el recuerdo. Rama `pista/s105-d` · `b010bb97` ·
> local = origin · árbol limpio.

## LO QUE CORRIÓ DE VERDAD HOY — los dos rieles, completos

**DEUNA, de punta a punta y por primera vez en la historia del producto:**
puerta → código `891762` → Carlos paga → `payment/info APPROVED $75.86` →
barrido → aplicador → **compra `pagada`**. Y en la segunda vuelta ($94,50) **el
webhook llegó SOLO** (`18:07:24`, `confirmado_por = webhook`), porque Carlos
registró la URL y el header ese día.

**Y el reverso de DeUna, por la edge nueva:** `transactionReverseId
3d909788-…` · `payment/info` en `REVERSED` · `horas_desde_el_cobro 0.07`.
**Los dos casos de certificación de DeUna cerrados en la misma sesión.**

**NUVEI:** reverso ejercido sobre `DF-2102135` con la predicción escrita ANTES,
cumplida punto por punto. `status=2/7` · el actuador dijo *«status=2 no
confirma»* — **`D-923` en su propia voz**, que es lo que se le muestra a Erick.

## LAS PIEZAS QUE QUEDARON — todas desplegadas y ejercidas

| pieza | estado |
|---|---|
| `pagos-deuna-solicitud` v7 · `webhook` v5 · `barrido` v8 | ACTIVE · el riel entero |
| `pagos-reverso` v2 (Nuvei) · `pagos-reverso-deuna` v2 | ACTIVE · con la ventana ANTES del fetch |
| `pagos-tarjetas` v2 | ACTIVE · `card/list`, ejercida sobre el parque real |
| `pagos-cobro` v20 · `pagos-cobro-recurrente` v12 | ACTIVE · con el guard del IVA nuevo |
| `_shared/iva.ts` | discriminador **8/8** |
| Migraciones `…824235000` · `…826023000` · `…826110000` | aplicadas, con reversa y cinturón |

## 🔴 LO QUE ESPERA A OTROS — con dueño y nombre

| # | qué | de quién |
|---|---|---|
| ① | **El merge de `pista/s105-d` a `main`** — hay migraciones APLICADAS al remoto cuyo archivo no está en el canon. **Se lo pedí 3 veces**; su auditoría siempre llegó antes que mi push | **A** |
| ② | **`D-923`**: el actuador no maneja `REVERSED`. **Con el dato que cambia la cura: el discriminador es `status_detail`, NO `status`** — llegó `status=2` con `status_detail=7`, y un `2` no distingue un reverso de cualquier otro no-aprobado | **A** |
| ③ | **`D-913`**: la compuerta 0 rebota `pago_en_proceso` con un código vencido ⇒ *«generar uno nuevo»* no funciona. **Diseño completo en `S105-D-DISENO-matar-intento-vencido.md`**, con su LEY ① y el fundamento corregido | A + mesa |
| ④ | **El guard del IVA NO se ejerció contra el proveedor.** Mandar `vat`/`tax_percentage`/`taxable_amount` reales **no está firmado** y espera a **Erick el jueves**. Con IVA 0 se comporta igual que antes | founder |
| ⑤ | **La cita reversada** — el brazo `pago_reversado` sigue sin ejercerse. Espera una **cita PAGADA**; la voz de C ya está escrita (`eb5efd20`) | jueves, con A |
| ⑥ | **`despachar-whatsapp`**: territorio firmado mío. Faltan **dos líneas**: `components` en el `fields` (puesta, sin ejercer) y `/debug_token` (hecha). **Bloqueado por `code_verification_status: EXPIRED`** | founder |

## PREGUNTAS AL PROVEEDOR — abiertas

**A Carlos (DeUna):** ¿la ventana es 24 h o *«purchase day»*? Su propio `404`
dice *«only valid for the purchase day»* y la letra dice 24 h. **Mi
implementación quedó en 24 h con la ambigüedad declarada.**

**A Erick (Nuvei), jueves:** los tres campos de impuesto · por qué un
`transaction_id` inexistente devuelve **`HTTP 500` con un `AttributeError` de
Python** en vez de `404` — *un error nuestro llega con la misma forma que una
caída de ellos.*

## MI VOTO SOBRE EL CRON, ya dado

**`03:00` Guayaquil (la letra), no `*/5`.** Razón medida: el webhook cerró el
circuito en segundos y el barrido posterior devolvió **`candidatos: 0`**. Con el
webhook vivo, `*/5` serían 288 corridas diarias para casos que hoy son cero —
*un barrido que escala lo mismo en cada pasada entrena a ignorarlo.* **El cron
ya manda el `x-despacho-secret` y la gracia: verde para encender.**

## LOS SIETE BLOQUEANTES QUE CAYERON — todos NUESTROS

compuerta 5 pidiéndole tarjeta a un medio sin tarjetas · **mi `slice(0,4000)`
antes del `JSON.parse`** (la respuesta mide 7844 bytes por el QR: el `catch`
mudo convertía el truncado en *«el proveedor no mandó los campos»*) · el lector
sin `p_proveedor` · el `v_i` sin `ambiente` · el lector sin `intento_id` · **mi
`v.crudo` que no existía** · **la ventana verificada DESPUÉS del refund.**

> ### Ninguno se veía sin correr el camino real. Y tres eran la misma forma: **una validación que corre después del acto que debía validar.**

## LO QUE NO SE HIZO, sin maquillar

**El sujeto no vuelve tras un reverso** (`D-923`): la compra `9cfced78` figura
`pagada` con la plata devuelta. **Está declarado en cada respuesta**
(`sujeto_movido: false`), no escondido · **`DF-2103629` NO se reversó**: nació
fuera de ventana (cobro 19:43, corte 17:00) y la autorización venía de segunda
mano · **el brazo «error final corta al primer intento»** del reintento sigue
sin ejercerse: el caso disponible dio 500, no 4xx.

---

## COLA VIVA — próximo paso ejecutable

Nada de esto se ejecuta antes del **Checkpoint 1**.

1. ~~🔑 **Founder:** generar `DEUNA_WEBHOOK_SECRET` y confirmar `PAGOS_AMBIENTE`~~
   ✅ **HECHO 24-ago** — los tres secretos cargados y verificados por digest.
2. ~~🔴 **A:** el rescate de `pista/s103-d` → `main`~~ ✅ **HECHO** — verificado
   contra el objeto antes de desplegar (306 líneas, `verificado`×2, `origen`×2).
3. ~~Fijar `DEUNA_POINT_OF_SALE` + el secreto del webhook~~ ✅ **HECHO 24-ago.**
4. ~~Desplegar las tres~~ ✅ **HECHO** — las cuatro ACTIVE, `verify_jwt` decidido
   por medición.
5. 🔑 **FOUNDER · el alta del webhook con DeUna.** URL y header están en §4(b);
   el valor del secreto lo manda **por canal aparte**. **Hasta que DeUna lo
   registre, el buzón está desplegado y no puede recibir nada** — el circuito
   depende de un tercero, no de código.
6. 🔑 **FOUNDER · las preguntas al proveedor, mañana temprano.** La que más urge
   es **¿`refund` limpia el QR del punto de venta entero o solo esa
   transacción?** — *dejó de ser curiosidad sobre un rebote raro: es el insumo
   de una cura, y de su respuesta depende si el diseño de §matar-intento-vencido
   sirve o se rehace entero.*
7. ⛔ **EL `refund` NO SE EJERCE CONTRA QA HASTA QUE CONTESTEN** — orden del
   founder, 24-ago. *La hipótesis de que sea el mecanismo de anulación es buena
   y por eso mismo no se prueba a ciegas: si limpia el QR del punto de venta,
   tumba los códigos vivos de todos.*
8. **`REVERSED` / `REVERSED_FAILED`: bloqueados por falta de un pago aprobado en
   QA**, no por falta de código. Se ejercen cuando llegue la respuesta.
9. ⏳ **Al llegar la respuesta, el diseño se bifurca** (documento
   `S105-D-DISENO-matar-intento-vencido.md`): **anulación por transacción** ⇒ el
   diseño rige y se construye la puerta (con ③ adentro) · **anulación por punto
   de venta** ⇒ **la ley ① es inaplicable y se rehace**. *Escrito acá para que
   la bifurcación no se decida de memoria el día que llegue el mensaje.*
8. **A:** `D-887`, el aplicador del barrido — **sigue siendo la pieza que falta
   para que el circuito cierre sin webhook.**
9. **C:** los **dos** flips de `§13bis ②`. **Esperan el ojo del founder**, no mi
   despliegue: el riel ya está vivo.
10. **El cron del barrido NO se enciende** (`§13bis ③`): sin aplicador (`D-887`)
    y sin corrida a mano, un barrido que escala lo mismo cada pasada entrena a
    ignorarlo.

## COMPROMISOS CRUZADOS

| con | qué |
|---|---|
| **A** | debo: el reporte del rescate y la contradicción de §8 · **me deben**: el merge a `main` y el aplicador `D-887` |
| **C** | **me deben**: los dos flips de `§13bis ②` — **y son dos**; el primero solo no conecta la pantalla |
| **mesa** | enmienda de `LETRA_DEUNA` §8 / firma ③ por la ventana de 24 h |

## SUPUESTOS VIVOS — declarados, no dados por ciertos

- `PAGOS_AMBIENTE` = sandbox. **No medido** (es secret).
- Rate limit ~1 req/s. **De la doc en papel**, sin confirmar por el proveedor.
- El webhook se registra por ambiente. **Deducido**, no confirmado.
