# S114-E · POSTVENTA: LOS INSTRUMENTOS

> ### 📌 ADENDA de mesa (7-sep) — tres correcciones, aplicadas
> ① **La «permisividad» que declaré en E2 no existía: era una mala lectura de
> F1**, y sale de la lista. ② **El ancla del evento de guardería es la ESTADÍA**
> ⇒ la tolerancia de E4 **se cerró**. ③ **E3 gana su segundo asiento** — un
> prestador que NO es dueño del objeto. ④ El cruce «emitió vs tiene productor»
> va a `docs/loop/buzon/S114-E-para-A-emitio-vs-productor.md` **para que no se
> escriba dos veces**. *Cada una está aplicada en su lugar y en el código, no
> sólo acá.*

> **Rama `pista/s114-e-1.0`. Nace de `main` en `e2c18c39`** (la punta con
> `LETRA_POSTVENTA` y su enmienda ①).
>
> ## ⚠️ EL CONTEXTO QUE INVALIDA TODO NÚMERO DE ESTE PARTE COMO LÍNEA BASE
> **Ningún dato de servicio de la base es real.** Todo es prueba de
> construcción y **producción es octubre**. Los números de acá abajo sirven
> para **saber si el instrumento mide** — jamás para calibrar un umbral, fijar
> una meta ni estimar un costo. Donde alguno se pueda leer como línea base, lo
> digo en el lugar.
>
> **Cinco piezas nuevas, ninguna de producto:**
> `verify:postventa-plata` · `verify:cierre-ausente` · `verify:asientos-caso` ·
> `verify:devengo-por-sujeto` · `metricas:postventa` · `censo:productores-de-aviso`.
>
> 🔴 **NINGUNA VA AL HOOK DE PRE-COMMIT.** Las cuatro pegan a la base y
> `verify:asientos-caso` **abre una sesión real** — mismo precedente que
> `verify:mis-hilos-realtime` y `verify:d485-familia-lee`. Corren en el paso ⓪
> y en el cierre, al lado de `verify:censo`.

---

## ⓪ LA LEY QUE ORDENA ESTE PARTE, Y CÓMO LA CUMPLÍ

*Un instrumento que no puede producir su rojo no está midiendo.* Los cuatro
gates lo cumplen, pero **de dos formas distintas**, y la diferencia importa:

| | cómo produce su rojo | por qué así |
|---|---|---|
| **E4** devengo | **el dato lo produce solo** — hay 19 objetos sin evento | su sujeto ya existe |
| **E2** cierre ausente | **rojo simulado**: se corrió con el estado dado por existente ⇒ 109 | su sujeto existe, su ESTADO no |
| **E1** plata | **rojo real sobre eventos reales**, con casos sintéticos | su tabla no existe todavía |
| **E3** asientos | **rojo real**: apuntado a una tabla con grants de escritura ⇒ 🔴 | su tabla no existe todavía |

Y **los tres primeros llevan sus controles ADENTRO y corren en cada corrida**,
no una vez. *El problema no es hoy —hoy el rojo sobra—: es el día después de
la cura.* Con todo en verde, un `join` roto o una tabla renombrada darían
**cero violaciones**, y cero se lee como salud. Por eso el mismo predicado se
aplica en cada corrida a filas sintéticas y, si deja de marcarlas, **el gate
sale 2 en vez de verde**.

**La segunda pregunta —¿mi instrumento perdona algo que el producto no
perdona?— tiene respuesta escrita en cada uno**, y en tres casos la respuesta
fue *sí*: están declaradas abajo, no en un comentario.

---

## E1 · `verify:postventa-plata` — 🟠 NO CONCLUYENTE, con **el rojo ya probado**

**Estado: exit 2.** `casos_postventa` no existe. **Pero el predicado ya está
probado, hoy, contra eventos económicos REALES.**

Ésa es la parte que importa: un arnés que se limitara a decir *«no hay tabla»*
no probaría nada el día que la haya. Éste **elige de la base** una cita que SÍ
tiene evento y otra que NO, arma casos sintéticos sobre ellas y corre el
predicado de §6:

| control | resultado |
|---|---|
| **negativo — `declarado` sobre objeto CON evento** | 🔴 **marcado** ✅ |
| positivo ① — `declarado` sobre objeto SIN evento | no marcado ✅ |
| positivo ② — `aplicar_reembolso` sobre objeto CON evento | no marcado ✅ |

**El contrato que el arnés necesita, y se dice ahora para que no se descubra
después de la primera devolución:** `objeto_tipo` · `objeto_id` ·
`camino_plata ∈ (declarado, aplicar_reembolso)` · `monto_devuelto`. §6 lo pide
literal (*«el caso registra por cuál de los dos caminos salió, y con qué
monto»*); **sin esas cuatro, este rojo es inmedible.**

**Perdona dos cosas, declaradas:** ① **no mira el MONTO** — un
`aplicar_reembolso` por la cifra equivocada sale verde acá; ② un evento ya
`reversado` **sigue contando** como «tiene evento», a propósito: una devolución
declarada encima de un reverso es doble devolución y la quiero marcar.

---

## E2 · `verify:cierre-ausente` — 🟠 NO CONCLUYENTE, con **bloqueante nombrado**

**El control positivo del sujeto va primero, porque §2 lo ordena literal**
(*«el arnés confirma que su sujeto existe antes de medir»*), y **pasó**:

| ventana desde el fin declarado | citas | pedidos |
|---|---|---|
| todavía no termina | 51 | — |
| terminó, menos de 24 h | 3 | — |
| **pasó 48 h** | **107** | **2** |

**109 objetos ya deberían estar en `no_ejecutado`.** El fin declarado sale del
objeto: `fecha + hora + duracion_minutos` para la cita,
`envios.promesa_entrega_hasta` para el pedido.

### 🔴 Por qué sale 2 y no 1 — y es la decisión de diseño del arnés

**Medido: `no_ejecutado` NO EXISTE en ningún CHECK de la base.**
`select count(*) from pg_constraint where pg_get_constraintdef(oid) like '%no_ejecutado%'` ⇒ **0**.

Sin el estado, el arnés **no puede separar dos rojos distintos**: *«el cron no
corrió»* manda a mirar un reloj; *«el estado al que había que mover no
existe»* manda a escribir una migración. **Un instrumento que no puede
distinguir sus dos rojos no reporta ninguno: dice qué le falta.**

**BLOQUEANTE NOMBRADO:** agregar `no_ejecutado` al CHECK de
`evento_cita_servicio.estado` (y su gemelo del pedido). *Una deuda con su
bloqueante nombrado alguien la destraba; una «pendiente» espera para siempre.*

**Rojo producido:** corrido con el estado dado por existente ⇒ **🔴 109**.

### ✅ CORRECCIÓN DE MESA — lo que declaré como permisividad no lo era

La primera versión de este parte declaraba como *tolerancia* que el arnés «no
distingue un incumplimiento de un servicio prestado que nadie cerró». **Era una
mala lectura de F1 y se retira de la lista.**

**F1 hace la causa deliberadamente irrelevante.** Su tabla no tiene una fila
para «el prestador incumplió»: tiene tres actos —`cierre_con_calidad`,
`cierre_no_show` y **nada marcado**— y **es el tercero el que dispara**. *El
prestador cobra sólo si marca uno.* Un servicio prestado y no cerrado **no
devenga igual**, y eso no es un hueco de medición: **es la firma.**

⇒ **el sujeto de este arnés y el de F1 son el mismo objeto, y no hay nada que
perdonar.** Lo único que hay que cuidar es el NOMBRE del número: es *ausencia
de cierre* —lo que F1 castiga—, **no** un índice de prestadores que fallaron.
*Esa distinción es de lectura, no de alcance*, y ahora la dice también la voz
del rojo del gate.

---

## E3 · `verify:asientos-caso` — 🟠 NO CONCLUYENTE · el candado que existe, **puesto**

### ① El candado (§9.1), medido de dos formas

| tabla | rol | RLS | SELECT | INSERT | UPDATE | DELETE |
|---|---|---|---|---|---|---|
| `cat_motivos_postventa` | `anon` | ✅ | false | false | false | false |
| `cat_motivos_postventa` | `authenticated` | ✅ | **true** | false | false | false |

**La tabla que A ya creó cumple §9.1 entera.** No hay nada que curar ahí.

**Y la sonda por PostgREST real** (§9.3 pide «real y no simulado»):

```
✅ control positivo: la sesión LEE `cat_motivos_postventa` por PostgREST.
✅ el INSERT rebotó: 42501 · permission denied for table cat_motivos_postventa
```

🔴 **El control positivo no es adorno: sin él la sonda no probaría nada.**
*Una sonda que siempre rebota también rebota* — un token vencido o una URL mal
armada dan el mismo «no pude escribir» que un candado bien puesto. El control
es la **asimetría sobre la misma tabla y con la misma sesión**: el `SELECT`
pasa y el `INSERT` no. Y el `42501 permission denied for table` es el rechazo
por **GRANT**, no por RLS — que es exactamente lo que §9.1 exige.

**La sonda no escribe.** Si por un defecto el INSERT pasara, reporta ROJO
MÁXIMO y **deja la fila**: borrarla escondería la única evidencia.

### ② Los asientos (§9.3) — **ahora con el segundo, que es el que importa**

La tabla del caso no existe, así que **el asiento se ejerce sobre el objeto que
sí existe** (`evento_cita_servicio`, con RLS por prestador). Eso prueba que la
maquinaria del asiento **discrimina**; el día que exista `casos_postventa`, las
mismas sesiones corren contra ella.

```
objeto de prueba: citas de `Paseos Andres` (224 en la base)
✅ asiento DUEÑO      (demo-prestador@epetplace.dev)  ve 224
✅ asiento NO DUEÑO   (guillo381+paseo1@gmail.com)    ve 0
✅ asiento ANON                                        ve 0
```

🔴 **El asiento que importa es el segundo.** El dueño viendo lo suyo no puede
fallar de la forma peligrosa; **el rojo que hace daño es ver el caso de otro.**
Y sin el primero el cero del segundo no significa nada: *una consulta rota
devuelve cero igual que una RLS bien puesta.* **Van juntos o no va ninguno** —
si el dueño ve 0, el gate lo declara y sale 2.

**Rojo producido, y es el del segundo asiento:** apuntado el «no dueño» a la
cuenta del dueño ⇒ `🔴 asiento NO DUEÑO … ve 224` y **exit 1**.
*(Y el anterior sigue valiendo: apuntado el candado a una tabla con grants de
escritura ⇒ 🔴 tres fallos nombrados.)*

**Faltan FAMILIA y CASA**, que son asientos del caso y no tienen tabla.

**Lo que todavía perdona, acotado:** la **sonda del candado** —no los
asientos— corre con una sola cuenta; la medición exacta por
`has_table_privilege` cubre `authenticated` y `anon`, y **para cualquier otro
rol este arnés es ciego**.

---

## E4 · `verify:devengo-por-sujeto` — 🔴 **ROJO HOY, y con control positivo adentro**

**El único de los cuatro cuyo sujeto ya existe entero. 19 objetos ejecutados y
pagados sin evento económico:**

| obj | vía | n | sin evento |
|---|---|---|---|
| cita | **telemedicina** | 10 | **10** |
| cita | **consulta_general** | 3 | **3** |
| **pedido** | **despensa** | 3 | **3** |
| cita | consulta_especializada | 1 | **1** |
| cita | **(cita sin oficio)** | 1 | **1** |
| **estadía** | **guardería** | 1 | **1** |
| cita | adiestramiento | 2 | 0 |
| cita | grooming | 6 | 0 |
| cita | grooming_completo | 1 | 0 |
| cita | paseo | 27 | 0 |

**universo 55 · con evento 36 · SIN evento 19.**

### 🟢 Lo que hace fuerte a este número: el control positivo salió del dato

**Los tres oficios que SÍ tienen productor —paseo, grooming, adiestramiento—
salen en CERO sin evento, 36 de 36.** No es un instrumento que marca todo: es
uno que encuentra el evento cuando existe y lo extraña cuando falta. *Eso vale
más que cualquier fixture sintético: la mitad limpia del resultado es la prueba
de que la mitad roja es verdadera.*

Y coincide **exactamente** con lo que §8 firma: los cuatro productores que
faltan (F10) son veterinaria, telemedicina, guardería y despensa.

### Los tres rojos del instrumento, producidos

| lo que rompí | qué hizo el gate |
|---|---|
| predicado que nunca marca | 🟠 exit 2 «EL CONTROL NEGATIVO NO PRODUJO SU ROJO» |
| predicado que marca de más | 🟠 exit 2 «EL CONTROL POSITIVO SALIÓ MARCADO» |
| tabla del evento renombrada | 🟠 exit 2 «la consulta no corrió — el gate NO dice verde» |

⚠️ **Sin el primer control, el gate roto imprimía «🟢 VERDE · todo objeto
ejecutado produjo su evento económico».** Ése es exactamente el falso verde
que este diseño existe para que no pueda ocurrir.

### ✅ LA TOLERANCIA SE CERRÓ — el ancla de guardería es la ESTADÍA

La primera versión aceptaba, para la estadía, el evento anclado **a la estadía
O a su cita**, porque la letra no había fijado el ancla. **La mesa la fijó: es
la ESTADÍA.** ⇒ **un evento colgado de la cita de una guardería ya NO cuenta**,
y el arnés quedó **más estricto que ayer, no menos**. El número no se movió
(**sigue en 19**): la estadía entregada no tenía evento por ninguna de las dos
vías, así que el cambio endurece el gate sin maquillar el resultado.

⚠️ **Se cita la MESA, no el documento:** al escribir esto, §8 en la punta de
`main` (`c72ea582`) todavía dice *«el acta de `entregar`»* sin nombrar el ancla.
**A deposita la enmienda; yo actúo sobre la decisión y lo declaro así.**

**Queda una tolerancia chica, y es de VOCABULARIO, no de ancla:** el literal de
`origen_tipo` no está fijado —medido: **no existe CHECK sobre esa columna** y el
único valor vivo es `cita`—, así que se aceptan `estadia` y `guarderia_estadia`.
*Las dos apuntan al mismo objeto; lo decidido es cuál es el objeto, no cómo se
escribe su nombre.* El día que exista el CHECK, la lista sale del gate y se lee
de él. · **Y sigue sin mirar el MONTO**, sólo la existencia.

---

## E5 · `metricas:postventa` — los números de §12, cada uno con su comando

Es un **script y no una tabla**, porque §12 exige que cada número nombre el
comando que lo produce. *Una tabla envejece en silencio —el canon tiene el
contador de migraciones caído cuatro veces y el de fichas seis para
probarlo—; un script vuelve a medir cada vez.*

🔴 **Un cero y una ausencia no son lo mismo, y acá no se confunden.** «0 casos
resueltos por el motor» y «la tabla de casos no existe» se ven igual en un
tablero y significan cosas opuestas: cada fila sale `MEDIDO` o `SIN SUJETO`
**con lo que le falta, nombrado**.

**5 de 15 tienen sujeto hoy:**

| # | número | estado |
|---|---|---|
| 0 | denominador · objetos ejecutados | **55** |
| 7 | citas que llegan a `no_ejecutado` | **0** — el estado no existe · **107** ya lo cumplirían |
| 9 | plata devuelta **por camino** | `aplicar_reembolso` **0** · reversados **0** *(control positivo: la tabla tiene 36 eventos, no está vacía)* |
| 10 | plata devuelta **por riel** | nuvei **5** ($169,60) · deuna **1** ($94,50) |
| 13 | costo de WhatsApp por caso | **0 mensajes · costo NULL** |

**Los diez restantes esperan la tabla del caso (§5).** Y **el saldo de §7
tampoco existe**: no hay tabla de saldo de la familia.

⚠️ **El costo de WhatsApp queda en NULL a propósito**, y no por falta de
esfuerzo: `MODELO_FINANCIERO` §11bis **no publica tarifa absoluta** —sólo la
relación ~17× Colombia— porque falta el volumen real. *Completarlo con una
tarifa de internet sería fabricar exactamente el número que esa sección existe
para no tener.*

---

## E6 · WHATSAPP — 🔴 **la precondición NO se cumplió**, y hay un hueco más grave

### La precondición: el número sigue sin re-verificar

```
code_verification_status: EXPIRED     ← igual que lo midió A
verified_name: E-PetPlace · +593 98 733 0099 · quality_rating: UNKNOWN
```

**El founder todavía no re-verificó el número** (o no propagó). Corrí el modo
`?verificar=1` igual, porque el resto del pedido no depende de eso.

### Las 8 plantillas — nombre, idioma, estado y **categoría medida**

| # | nombre | idioma | **categoría** | estado | vars |
|---|---|---|---|---|---|
| 1 | `pedido_confirmado` | es | **UTILITY** | APPROVED | 5 |
| 2 | `entrega_proxima` | es | **UTILITY** | APPROVED | 3 |
| 3 | `pedido_no_se_pudo_entregar` | es | **UTILITY** | APPROVED | 3 |
| 4 | `plan_renovacion_fallida_u` | es | **UTILITY** | APPROVED | 3 |
| 5 | `plan_renovado_v` | es | **UTILITY** | APPROVED | 4 |
| 6 | `cita_recordatorio_hoy_v` | es | **UTILITY** | APPROVED | 4 |
| 7 | `cita_recordatorio_manana_v` | es | **UTILITY** | APPROVED | 4 |
| 8 | `cita_confirmada_v` | es | **UTILITY** | APPROVED | 4 |

**`plantillas_en_marketing: 0` ⇒ categoría distinta de utility: NINGUNA. Sin
rojo por categoría.** *(Ratifica lo de A: la re-categorización que el canon
marca como crítica ya está hecha.)*

### El cruce contra los nueve tipos de §10

**Ninguna de las 8 sirve a ninguno de los nueve.** Las ocho hablan de pedidos,
planes y citas; los nueve hablan del caso. Y **los diez códigos de §10 no
existen en `cat_notificacion_tipos`** (medido: 0 de 10).

**Hay que escribir DOS plantillas, no nueve** — §10 lo acota: WhatsApp sólo en
dos momentos, cuando la familia tiene que **actuar** y cuando la plata **se
movió**:

| tipo de §10 | canal | plantilla |
|---|---|---|
| `caso_elegir_devolucion` | **WhatsApp** + push/in-app | **hay que escribirla** |
| `caso_resuelto` / `caso_saldo_acreditado` | **WhatsApp** + email forzado | **hay que escribirla** |
| los otros siete | in-app / push / email | no necesitan plantilla |

### 🔴 Y EL HUECO QUE NO ESTABA EN EL PEDIDO: **nadie puede nombrar una plantilla desde la base**

La edge lo dice en su propio comentario: *«La plantilla la dice la DB
(`resuelto_como`), jamás esta función»*, y si no la encuentra marca la
intención `fallida` con motivo `sin_plantilla_resuelta`. **Medido:**

| | |
|---|---|
| intenciones totales | **410** |
| intenciones con `resuelto_como ? 'plantilla'` | **0** |
| funciones de la base que escriben `plantilla` en `resuelto_como` | **0** |
| columna de plantilla en `cat_notificacion_tipos` | **no existe** |

⇒ **Las ocho plantillas aprobadas en Meta no tienen quién las nombre.** Aunque
el founder re-verifique el número y se encienda `transporte_vivo`, **todo envío
caería en `sin_plantilla_resuelta`.** *Es un motor sin puerta (`L-318`) en el
canal entero, y el orden de §9 de `NOTIFICACIONES` —lector → pieza → gate →
flip— lo pone antes del flip, no después.*

**No encendí nada.** La llave es del founder y va última.

---

## E7 · EL CIEGO DEL CENSO DE PRODUCTORES — rehecho, y el número cambió

**`censo:productores-de-aviso` corre LOS DOS MÉTODOS en la misma pasada**, que
es la única forma de decir cuánto no veía el primero. No reemplaza el
instrumento de A: lo mide.

**Cómo se le pregunta al objeto:** se recorren **las 1.407 columnas de texto de
las 303 tablas de `public`** y se pregunta cuáles CONTIENEN un código de
`cat_notificacion_tipos`. **No se filtra por nombre de columna** —filtrar por
`~* 'notificacion|aviso'` habría encontrado `tipo_notificacion` y habría dado
el mismo resultado **por casualidad**: un censo atado a un nombre mide la
convención, no el hecho.

### El número

| | A (por literal) | este censo |
|---|---|---|
| tipos en catálogo | 70 | 70 |
| con productor por literal | 48 | 48 |
| **con productor sólo por DATO** | **no los veía** | **10** |
| **sin productor por ningún método** | **22** | **12** |

**El número cambió de 22 a 12: diez tipos tenían productor y el censo por
literal no lo veía.** Cinco son los de guardería que A ya sospechaba
(`a_bordo`, `llegada`, `retorno`, `entregada`, `no_recogida` — productor
`_guarderia_aplicar_acto`, que lee `cat_guarderia_transiciones.tipo_notificacion`).

### 🔴 EL HALLAZGO QUE NO BUSCABA: un productor **BORRADO**, sin síntoma

**`pedido_nuevo_vendedor` emitió 4 veces (16 y 18-ago) y ningún método le
encuentra productor.** No es «sin productor»: es que **lo tuvo y lo perdió**.

Medido, y es una cadena cerrada:

| paso | evidencia |
|---|---|
| S97-A (**15-ago**) le puso el productor | `20260815130000_s97a_productores_ola_negocio.sql:96` — `registrar_intencion_notificacion(p_tipo => 'pedido_nuevo_vendedor', …)` dentro de `confirmar_pago_pedido`, con su comentario *«D-822 · LA VITRINA VENDIÓ (productor ①)»* |
| emitió | 4 intenciones: 3 el 16-ago, 1 el 18-ago — **las 4 `fallida`** |
| S101-B (**21-ago**) redefinió la función | `20260821120000_s101b_reuso_del_intento.sql` · `CREATE OR REPLACE FUNCTION public.confirmar_pago_pedido` — **cero menciones** del tipo y de la puerta de aviso |
| hoy | el cuerpo vivo (4.129 chars) **no nombra el tipo ni llama a la puerta**; la única función viva que nombra el código es `_voz_notificacion`, que es la VOZ |
| ¿alguien lo repuso? | **no**: ninguna migración posterior redefine `confirmar_pago_pedido` |

⇒ **`CREATE OR REPLACE` se llevó puesto un bloque que otra sesión había puesto
seis días antes, y no hubo error, ni test, ni síntoma.** El aviso *«Tu vitrina
vendió»* está muerto desde el 21-ago. **Ningún gate de la casa lo veía**, y
éste sólo lo vio porque cruza *«emitió alguna vez»* contra *«tiene
productor»* — un tipo con intenciones y sin productor **no puede ser las dos
cosas**.

**Lo reporto medido, no supuesto, y no lo curo: es territorio de quien lleve
despensa.**

### Lo que este censo TAMPOCO ve, declarado

· **`jsonb`.** Sólo recorre `text`, `varchar` y `citext`. Un código guardado
dentro de un `jsonb` de configuración no aparece. *Acota, no cierra* — igual
que el de A.
· 🔴 **El discriminador prueba CO-OCURRENCIA, no flujo de dato.** Marca la
tabla cuya lectura y cuya emisión viven en el mismo cuerpo; `cat_tipos_evento →
reservar_salida_paquete` sale marcado **y es casualidad**. Por eso el resultado
se publica como **candidato**, y lo único que afirmo duro es el conteo de tipos
que **emitieron** sin productor literal.

### Dos defectos de mi propio instrumento, encontrados corriéndolo

**Ninguno apareció leyendo el código; los dos aparecieron al correrlo contra el
objeto**, y fallaron en direcciones opuestas:

| # | el defecto | cómo se veía |
|---|---|---|
| ① | `~ '\m'||tabla||'\M'` escrito desde un runner de shell: el patrón llegaba **mutilado** | **NULL hasta para el caso conocido** — falso rojo silencioso |
| ② | corregido a `like '%tabla%'`: `notificaciones` es **subcadena** de `despachar_notificaciones` | la tabla de **SALIDA** salió marcada como productora — **falso verde** |

**El control que los cazó a los dos es el mismo y corre antes de publicar
cualquier número:** el caso que A ya probó (`cat_guarderia_transiciones` →
`_guarderia_aplicar_acto`) **tiene que dar emisor**; si no lo da, el censo sale
2. *Sin ese control, la versión ① habría publicado «12 sin productor» con el
instrumento ciego, y el número habría sido creíble.*

**Y un tercero, operativo:** con dos consultas del CLI en vuelo a la vez, un
lote de 120 columnas rebotó por conexión (`temp role`) y **el censo imprimía
«⚠️ 120 sin leer» y seguía** — *publicando un número más chico y llamándolo
resultado*. Curado con reintento, y si igual no entra **ya no publica: sale 2**.

---

## LO QUE NO MEDÍ, Y POR QUÉ

| | |
|---|---|
| **Los tres asientos de §9.3** | sin tabla del caso. El bloque está escrito y espera |
| **El aviso de 24 h de §2** | su tipo no existe en `cat_notificacion_tipos`; contarlo hoy es contar sobre nada |
| **Dato de prueba vs. dato real** | `evento_cita_servicio` **no tiene `creado_por_sistema`** (la marca que S113 puso en `mascotas`). **Ninguno de mis números separa seed de real**, y por eso ninguno es línea base |
| **El monto** | ni E1 ni E4 miran cifras: miden existencia y camino |
| **Roles distintos de `authenticated` y `anon`** | la SONDA DEL CANDADO de E3 es ciega a ellos (los asientos ya corren con dos prestadores y anon) |
| **Los asientos FAMILIA y CASA** | son del caso; sin tabla no hay sujeto |
| **La mensualidad de guardería por día** | §8 firma que devenga por día ejecutado; si alguien produjera UN evento por el mes entero, E4 **no lo marcaría**. Ese rojo es de otro instrumento y todavía no tiene sujeto |

---

## ④ COORDINACIÓN CON A — el cruce ya está medido, que no se escriba dos veces

**`docs/loop/buzon/S114-E-para-A-emitio-vs-productor.md`**, un solo asunto,
con la forma del buzón: los doce con su categoría e intenciones, la cadena
fechada del productor borrado, **lo que el censo NO ve** (jsonb · co-ocurrencia
en vez de flujo de dato) y cómo se verifica del otro lado
(`pnpm censo:productores-de-aviso`).

⚠️ **Su `exit 1` es correcto y no hay que apagarlo:** se apaga cuando alguien
reponga el productor de `pedido_nuevo_vendedor`, **no cuando alguien edite el
censo.**

---

## LO QUE LE DEJO A OTRAS PISTAS (medido, no supuesto — lo reparte el founder)

1. 🔴 **`no_ejecutado` no existe en ningún CHECK.** Es el bloqueante nombrado
   de E2 y de la métrica #7 de §12. Sin él, 109 objetos que ya cumplieron las
   48 h no tienen a dónde ir.
2. 🔴 **Nadie puede nombrar una plantilla de WhatsApp desde la base** (410
   intenciones, 0 con `plantilla`, 0 funciones que la escriban). Va **antes**
   del flip, no después.
3. 🔴 **El productor de `pedido_nuevo_vendedor` fue borrado por S101-B el
   21-ago** y nadie lo repuso.
4. 🟠 **`origen_tipo` de `eventos_economicos` no tiene CHECK** y su único valor
   vivo es `cita`. **El ancla de guardería ya está decidida (la estadía) y E4 ya
   la exige**; lo que queda abierto es el LITERAL — `estadia` vs
   `guarderia_estadia` — y el del pedido. *Vocabulario, no ancla.*
5. 🟢 **`cat_motivos_postventa` cumple §9.1 entera** (A no tiene nada que curar
   ahí), verificado por PostgREST real con `42501 permission denied for table`.

---

## OPERATIVO

| | |
|---|---|
| rama · nace de | `pista/s114-e-1.0` · `main` en `e2c18c39` |
| piezas nuevas | 6 scripts, **cero producto**, cero migraciones, cero SQL aplicado |
| cableado | `package.json` — 6 entradas nuevas; **ninguna en el hook** |
| gates de gobierno | `verify:gates-existen` ✅ · `verify:arneses-declaran` **63 verdes / 0 rojos** ✅ |
| estado de los cuatro | E4 🔴 exit 1 · E2 🟠 exit 2 · E1 🟠 exit 2 · E3 🟠 exit 2 |
| escrituras a la base | **ninguna.** E3 intenta un INSERT hecho para ser rechazado y **no lo borra si pasara** |
