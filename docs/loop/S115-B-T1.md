# S115-B · TANDA 1 — LAS PIEZAS DE LA FACTURA

> **Rama:** `pista/s115-b-1.0` · **punta:** se lee de `git ls-remote` al pie de esta acta.
> **Contador de piezas:** se PIDE al script, no se teclea — `node scripts/verify-contador-piezas.mjs`.
> **Buzón de A (`S115-A-para-B-y-C-contrato-fiscal.md`): NO EXISTÍA al abrir ni al cerrar.**
> Se arrancó con los nombres del prompt, como el founder autorizó — y se los
> contrastó contra la tabla `facturas` que **ya vive en la base** (§6).

---

## ⓪ LO PRIMERO, PORQUE CAMBIA UNA ORDEN

🔴 **La orden pedía «coma decimal como ya hace la puerta de pago». Medido: la
puerta de pago NO usa coma.**

| dónde | qué escribe |
|---|---|
| `apps/cliente/.../despensa/checkout.tsx:832` | `` `$ ${v.toFixed(2)}` `` → **`$ 45.00`** |
| `PrecioText.formatearPrecio` (la pieza canónica de plata) | `` `$${valor.toFixed(2)}` `` → **`$45.00`** |
| `monto()` de `packages/i18n` — **el único que produce coma** | `$45,00` en es · `$45.00` en en |

**El riel de la coma no lo llama nadie.** Su propio hook lo dice en la
cabecera: *«este hook está escrito, documentado y no lo usa nadie»* — cero
consumidores medidos.

⇒ **`DesgloseCompra` pinta con `PrecioText`, la pieza de la casa.** *Pintarla
con coma habría fabricado una isla: el desglose diría `$45,00` tres
centímetros arriba de un total que dice `$45.00`.* Migrar la casa entera es
**una línea** (`formatearPrecio`) y lo hereda todo el producto — pero es
decisión del founder, no de una pieza. **Queda servido, no ejecutado.**

---

## ① LAS CINCO PIEZAS (+ una que salió de un encierro)

| pieza | la decisión que la gobierna |
|---|---|
| `CampoIdentificacion` | **el tipo se ELIGE, jamás se adivina por el largo** — mientras alguien tipea, toda cédula pasa por ser un RUC incompleto, y un pasaporte no tiene largo |
| `SelectorFacturacion` | **la opción apagada por el tope se ve CON SU RAZÓN** — sacarla haría que la misma persona vea dos pantallas distintas sin entender por qué |
| `DesgloseCompra` | **el IVA en CERO se dibuja; el IVA en `null` no** |
| `TarjetaFactura` | **el error del SRI es INEXPRESABLE**: ni prop ni slot `ReactNode` |
| `CampoClaveAcceso` | **49 dígitos agrupados de a cuatro, no 49 cajitas** |
| `tilde.tsx` | el trazo **ya existía encerrado en `Casilla`**: se ensancha y ella lo consume |

### El IVA en cero — por qué NO deroga la ley del nulo

`PrecioText` dice que **`$0.00` es una mentira con formato de dato**. Esa ley
habla de la **ausencia** — un precio que no llegó. **Acá el cero es un HECHO
MEDIDO:** el alimento tributa 0 %, así que «IVA: $0,00» no dice «no sabemos»,
dice *«corresponde cero»*. ⇒ **`null` no se dibuja · `0` sí**, y son dos
estados distintos que la pieza trata distinto a propósito.

### El foco no se roba — desvío declarado

La orden decía *«y el campo queda enfocado»*. Se implementó como **conserva**
el foco, no como «se lo devolvemos a la fuerza»: *un campo que se re-enfoca al
fallar atrapa a la persona adentro* —no puede corregir el campo de arriba ni
cerrar el teclado para leer el error—. Choca con la Ley 17.4 (los errores
DIRIGEN, no secuestran). **Si el founder quería el re-foco literal, es una línea.**

---

## ② LO QUE ENCONTRARON LOS CONTROLES

*Ninguno de estos cinco lo habría visto una lectura del código.*

**① `status.success` da 1.63 sobre blanco — y el gate estaba VERDE.**
`verify:contrast` daba 431 pares y 0 fallos… **porque mi par no estaba
declarado**. Al declararlo salió el defecto: el verde puro es registro GRÁFICO
(Ley 2) y un tilde que es la única señal de que la cédula cerró **es
funcional**. Curado a `successText` (6.33). *Un gate de pares declarados no
crece con el código.*

**② El control dio VERDE con el módulo 11 roto.**
Rompí `11 - resto === 10 ? 1 : 11 - resto` a propósito y **no pasó nada**:
medido, mis dos RUCs daban **los dos resto 7**, así que la rama que lee 10
como 1 nunca se ejercía. Curado con dos casos de resto 0 y resto 1 verificados
a mano. **El hueco apareció al intentar romperlo, no leyéndolo** (`L-459`).

**③ La colisión 1↔10 del estándar del SRI — no es nuestra.**
`179000001` (resto 1) y `179600001` (resto 10) **producen el mismo dígito
verificador**: hay errores de un dígito que el algoritmo no puede detectar. Se
**declara y se cuenta** en el control (6 casos), en vez de esconderse. Es la
razón de fondo por la que la voz dice **«no parece válida»** y nunca «es válida».

**④ `const TRAZO` encendió rojos en dos piezas ajenas.**
`R70` **deriva su corpus por nombre**: toda constante que arranca con `'M…'` es
un path para ella. En esta casa `TRAZO` ya significa **el grosor**
(`strokeWidth: TRAZO`, en 8 archivos) — así que mi constante entró a su set y
la regla empezó a acusar a `Icono` y `HuellaDelVinculo`, **que esta tanda no
tocó**. Renombrada a `PATH_TILDE`. *Dos vocabularios que se llaman igual no
son el mismo vocabulario, y acá el costo lo pagaban dos piezas ajenas.*

**⑤ El fixture que rotulé «VÁLIDA» no cerraba su dígito.**
Escribí la clave de 49 a mano y la etiqueté válida; su dv daba 3 y yo puse 7.
Sin verificarla, **el founder habría visto un error rojo bajo un rótulo que
dice «válida» y juzgado rota la pieza.**

🔴 **Y un rojo de mi propia regla, por la razón equivocada:** `R84` se acusó a
sí misma marcando `t('desglose.iva')` — que es una **key**, no una voz. *Un
rojo por la razón equivocada está tan roto como un verde por la razón
equivocada*: si lo dejaba, la única cura habría sido renombrar la key, o sea
deformar el código para contentar al instrumento. **Se curó el instrumento.**

---

## ③ LOS GATES, con su rojo probado ANTES de cablearse

| gate | estado | su rojo |
|---|---|---|
| `verify:diseno` | **VERDE · 76 reglas** (baseline 73) | R84/R85/R86 probadas **contra el código real**, no sólo contra su fixture |
| `verify:identificacion-ec` (nuevo) | **VERDE · 652 comprobaciones** | rompiendo el módulo 11 → rojo |
| `verify:contrast` | **VERDE · 436 pares** (5 nuevos) | con el verde puro → 1.63, dos fallos |
| Espejo es↔en | verde | key faltante → `TS2741` · key inventada → `TS2345` |
| typecheck | `ui` · `cliente` · `prestador` en 0 | — |

**El discriminador del control de validadores es la MUTACIÓN DE UN DÍGITO**
(441 sobre la clave de acceso), **no casos que yo mismo generé con mi propia
fórmula** — eso sería el discriminador tautológico que esta casa ya se cobró.

### Lo que los verdes NO dicen (declarado en cada `info`)

- `R84`: «la tarifa llega por props» — **jamás** «el cálculo del IVA está bien», ni «ninguna pantalla la teclea» (no mira `apps/`).
- `R85`: «la voz pasa por el riel» — **jamás** «la traducción es buena».
- `R86`: «no hay por dónde pasar el motivo» — **jamás** «el mensaje que se muestra es el correcto».
- `verify:identificacion-ec`: «los tres discriminan un dígito cambiado» — **jamás** «el número existe». **El pasaporte NO se valida**: no hay dígito verificador universal, y fabricar una regla de largo rechazaría documentos legítimos.

---

## ④ DOS REGLAS QUE ME CORRIGIERON A MÍ

- **`R77`** me obligó a clasificar `TIPOS` en su tabla. Es **exhaustivo**, y la consecuencia de que le falte uno es concreta: quien tenga pasaporte se queda sin forma de facturar.
- **`R17`** se quejó de `LARGO`. La agarró **por el nombre** (mayúsculas sin guión bajo, o sea con forma de pieza) — y al ir a contestarle «es una tabla interna» quedó claro que **tampoco tenía por qué estar exportada**. Se retiró. *Un gate que pregunta por qué algo está exportado suele estar señalando que no debería estarlo.*

---

## ⑤ LO QUE NO SE HIZO, Y POR QUÉ

- **Sin cámara / escáner de código de barras** — no entra nada nativo en esta tanda. **Queda como deseo con su condición:** el día que la cámara ya esté en el binario por otra razón, es una prop más y un botón al costado. *Construirlo hoy sería motor sin puerta.*
- **Ninguna pieza está montada en una pantalla** — eso es de C en la tanda 2. Lo único que las muestra hoy es la **galería**.
- **`Campo` no se ensanchó.** Necesitaba `tono` y `ref`; medido, **no hacía falta ninguno de los dos**: el aviso viaja por `ayuda` (que ya sale sereno) y el foco no se roba. *Se evitó tocar una pieza que consumen decenas de pantallas.*

---

## ⑥ EL VOCABULARIO SALIÓ DE LA BASE, NO DE MI CABEZA

La tabla **`facturas` ya existe** con `tipo_identificacion` · `identificacion`
· `razon_social` · `direccion` · `email` · `clave_acceso` · `numero_factura` ·
`subtotal_0` · **`subtotal_12`** · `subtotal_15` · `iva_valor` · `estado` ·
`sri_error` · `pdf_url` · `xml_url`.

**Dos cosas que eso enseña:**
1. `subtotal_12` **al lado** de `subtotal_15` es la cicatriz de que **la tarifa ya cambió una vez** — es la prueba de que ningún `15` puede vivir dentro de una pieza (lo vigila `R84`).
2. `facturas.estado` nace en **`pendiente`** por decisión escrita (`20260814150000`), porque *«en Ecuador la factura electrónica falla seguido: la factura se REGISTRA, no se emite»* (`MODELO_DESPENSA` §8.6bis). ⇒ **`corrigiendo` no es un caso raro: es un estado normal del sistema**, y por eso no puede verse como una falla del usuario.

**Diferencias de nombre a conciliar con A** (uso los del prompt en las props):
`iva` ↔ `iva_valor` · `descuento` ↔ `descuento_total`.

---

## ⑦ PARA C (tanda 2)

- Las cinco piezas **reciben props y no hablan con nadie**: ninguna llama a un wrapper.
- **`SelectorFacturacion` no construye el formulario adentro**: recibe el `CampoIdentificacion` como hijo, así el prestador puede montar el suyo.
- **`topeFormateado` llega ya formateado**: la pieza no formatea plata (ver ⓪).
- **`mostrarDeducible` nace en `false`.** Afirmar que un gasto es deducible es una afirmación fiscal — la enciende A cuando el contador confirme.
- **`EstadoFactura` NO es `facturas.estado`**: traducir del vocabulario del SRI a lo que se le dice a una persona es decisión de producto, y se hace afuera.

## ⑧ PARA A

- `esIdentificacionValida` / `esClaveAccesoValida` están **exportados desde `@epetplace/ui`** y son puros: sirven igual del lado del motor.
- 🔴 **El pasaporte no se valida** — decisión declarada, no hueco.
- 🔴 **La colisión 1↔10** significa que el dígito verificador **no detecta todos los errores de un dígito**. Si algo del lado del motor depende de eso, conviene saberlo.

---

## ⑨ OPERATIVO

| qué | valor |
|---|---|
| rama | `pista/s115-b-1.0` |
| **punta (leída de `git ls-remote`, por SHA)** | **`9a87d708`** |
| commits | `c9e35634` (las piezas + los gates) · `9a87d708` (esta acta) |
| **OTA cliente** | group **`8d926c2c-cf6b-40ac-8713-2133939db310`** |
| **OTA prestador** | group **`aa7df254-6be6-4a0c-ab1d-a9a296e6659b`** |
| **ancla de los dos, leída del OBJETO** (`update:view --json` → `gitCommitHash`) | **`9a87d708`** |
| runtime | **1.0.7**, canal `preview`, las dos apps |
| contador de piezas (pedido al script) | **180 → 186** · 125 wrappers |
| `verify:diseno` | **VERDE · 76 reglas** (baseline 73) |
| `verify:contrast` | **VERDE · 436 pares** (431 + 5) |
| `verify:identificacion-ec` | **VERDE · 652 comprobaciones** |
| typecheck | `api` · `ui` · `cliente` · `prestador` en 0 (hook de pre-commit) |
| deps nativas nuevas | **CERO** — todo JS puro, por eso el OTA alcanza (L-134) |

**Se publicó en LAS DOS apps y no en una**, aunque la orden decía «un OTA»:
la galería vive en ambas y **`CampoClaveAcceso` es del prestador**, donde el
acento es teal. *Un gate de la pieza del negocio hecho en la casa del cliente
juzgaría el color equivocado.*

⚠️ **`isGitWorkingTreeDirty` viene `undefined` en el JSON de `update:view`** —
el canon ya lo tenía medido: *ese comando no expone el estado del árbol*. Lo
que sí puedo declarar es que **verifiqué `git status --porcelain` en CERO
inmediatamente antes de bundlear**, las dos veces. Es mi palabra sobre una
medición propia, no un dato del objeto, y se dice así.

`eas-cli` se corrió **desde `apps/<app>/` las dos veces** (nunca desde la
raíz) y se verificó después que **no quedó ningún `app.json` stub**.

---

## ⑩ LO QUE ESPERA AL FOUNDER

1. **El gate en dispositivo** — `/gallery`, sección «⭐ GATE S115». Las cinco
   preguntas que decide están escritas en el título de la sección.
2. 🔴 **El formato de la plata (§⓪)** — la orden se apoyaba en una premisa que
   no se cumple. Migrar la casa a coma es **una línea**; dejarla como está
   también es una decisión válida. **No la tomé yo.**
3. **El re-foco literal del campo con error** (§① «el foco no se roba») — si
   lo quería tal cual, es una línea.
4. **El escáner de la clave de acceso** — deseo anotado, con su condición.

---

## ⑪ 🔴 UN HALLAZGO OPERATIVO QUE LE SIRVE A TODAS LAS PISTAS

**Publicar un OTA deja el typecheck CIEGO A RUTAS, y no avisa.**

El `eas update` crea `apps/<app>/.expo/` para computar fingerprints — pero
**con `dev/` solamente, sin `types/router.d.ts`**. Y con `typedRoutes: true`,
sin ese archivo `Href` degrada a `string`: **toda ruta inventada compila en
verde.** Antes del publish `.expo/` no existía y el baseline estaba limpio;
después del publish el instrumento quedó apagado sin que nada fallara.

**Lo cazó `R63·C` en el hook de pre-commit, no yo.** Y su rojo era exacto:
*«`.expo/` existe pero NO su `types/router.d.ts`»*.

**Se curó generando el archivo, no borrando el directorio.** Borrar `.expo/`
—que está en `.gitignore` y no es contenido del repo— habría devuelto el
baseline a verde **sin curar la ceguera**: es apagar la pregunta, no
contestarla. Tras regenerarlo en las dos apps, `R63` pasa de *«no está
midiendo»* a **«780 literales de navegación · 2 apps · brazo C medido en las
dos»**, y los typechecks **siguen verdes con el instrumento ya viendo** — que
es lo que convierte ese verde en una medición.

⚠️ **Y en el camino, la trampa hermana:** el primer intento de generarlo falló
porque **el puerto estaba ocupado por el harness de otra pista**, y `expo
start` en modo no interactivo **se saltea el dev server sin fallar** (*«Input
is required… › Skipping dev server»*). No tira error: arranca, ve el puerto
tomado y sigue. ⇒ **cada pista arranca Metro en su propio puerto, y la prueba
de que arrancó es el archivo generado, jamás el exit code.**

---

# S115-B · TANDA 2 — LA LÍNEA DE LA TARIFA DE SERVICIO

Una fila más en `DesgloseCompra`, entre los conceptos y el total.

## La forma: una UNIÓN DISCRIMINADA, no tres props sueltas

```ts
type TarifaServicio =
  | { promocionada: false; monto: number }
  | { promocionada: true; monto: number; montoLista: number; hasta: string }
```

🔴 **Con `promocionada: true`, el tachado y la fecha son OBLIGATORIOS: una
promoción sin su precio de lista es INEXPRESABLE.** Con tres props opcionales
sueltas, el día que alguien pase `promocionada` sin `montoLista` la pieza
diría «gratis» sin decir **gratis desde cuánto** — que es justo lo que una
promoción tiene que comunicar. *Probado, no supuesto:*

```
TS2322: Type '{ promocionada: true; monto: number; }' is missing the following
properties: montoLista, hasta
```

## Las tres decisiones que no estaban en la orden

**① `hasta` viene por props.** «Gratis hasta **diciembre**» lleva adentro un
dato que **caduca, igual que la tarifa del IVA**. Escrito en el riel, el día
que la promo se extienda a marzo la pantalla afirmaría una fecha vencida hasta
que alguien tocara los dos diccionarios. ⇒ **la FRASE vive en el riel** (es lo
que cambia entre idiomas: «Gratis hasta X» / «Free until X») **y el DATO
llega por props** — el mismo reparto que `iva: 'IVA {{tarifa}} %'`.
*Llega ya en el idioma del usuario: quien la monta tiene el riel de fechas.*

**② La nota va bajo el rótulo, no pegada al monto.** «Tarifa de servicio» + el
tachado + el vigente + «Gratis hasta diciembre» **no entran en una línea de
teléfono**, y lo que se parte es siempre lo último. Bajo el rótulo tiene el
ancho de la columna izquierda entera.

**③ La tarifa va ANTES del IVA.** En una factura el impuesto es el penúltimo
renglón, después de todos los conceptos. Ponerla después sugeriría que quedó
fuera de ese IVA — y **la pieza no puede saber si la tarifa tributa**, porque
recibe los montos ya calculados. *Entre un orden que afirma algo que no
sabemos y uno que no afirma nada, gana el segundo.*

## La promesa de «no se mueve el layout», con precisión

**El monto vigente NO se mueve:** los dos casos alinean a la derecha, así que
el número queda pegado al mismo borde y en la misma columna que los otros
montos; **el tachado crece hacia la izquierda**. Cuando la promo termine, el
precio real aparece exactamente donde estaba el `$0.00`.

⚠️ **Lo que sí cambia es el ALTO de la fila**, por la nota. **No se reservó el
hueco a propósito:** `PieDeCampo` reserva altura porque el error aparece
*mientras alguien tipea* —ahí el salto es intrusivo—, pero este cambio ocurre
entre una compra y otra, meses después, **nunca frente a los ojos**. Dejar un
hueco permanente para una nota que va a desaparecer es peor que el salto que
nadie va a ver.

## El cero de la tarifa vs. el cero del IVA — son distintos

- **IVA `0`** → se dibuja: es un **hecho medido** (el alimento tributa 0 %).
- **`tarifaServicio` ausente** → la línea **no se dibuja**: un «Tarifa de
  servicio · $0.00» donde no hay tarifa **afirma que existe y que es gratis**,
  cuando lo que pasa es que no hay ninguna. Ley del nulo (19.9) en su caso limpio.

## Fixtures y gates

Los cuatro que se pidieron, **con la aritmética verificada** (el precedente de
la clave que rotulé «VÁLIDA» y no cerraba):

| caso | cuenta |
|---|---|
| mixto 0/15 **+ tarifa promocionada** — *el que más se va a ver* | 24,50 + 12 + 1,80 + 0 = **38,30** ✓ |
| la misma fila **ya cobrándose** | 24,50 + 12 + 1,80 + 2,50 = **40,80** ✓ |
| **sin tarifa** (prop ausente) | 24,50 + 12 + 1,80 = **38,30** ✓ |

*(El segundo es el fixture que deja comparar las dos filas y ver que el monto
no se movió.)*

`verify:diseno` **VERDE · 76 reglas** · `verify:contrast` **436 pares, 0
fallos** · `verify:identificacion-ec` **VERDE** · typecheck `ui`/`cliente`/
`prestador` en 0 · Espejo es↔en con sus dos keys nuevas.

---

## ✅ LAS TRES DECISIONES — FIRMADAS POR EL FOUNDER (10-sep-2026)

Se asientan acá **con su literal** porque una decisión que sólo vive en el
chat no rige para nadie más (`L-142`: los literales no viajan; `L-163`: una
sesión no está cerrada hasta que el canon lo dice). **Dejan de ser mi criterio
y pasan a ser letra.**

> *«las tres decisiones firmadas tal como las tomaste. La ① sobre todo: el mes
> por props y la frase en el riel es el mismo reparto que la tarifa del IVA, y
> **una fecha escrita en el diccionario es una fecha que caduca en silencio**.»*
>
> *«la unión discriminada es la forma correcta: una promoción sin su precio
> tachado **no debe poder escribirse**. Probado con el error del compilador y
> **no con una regla que alguien tiene que acordarse de seguir** — eso es lo
> que las hace durar.»*

**① El dato que caduca va por props; la frase, al riel.** La firma le da
NOMBRE a la clase que `R84` ya vigilaba sin nombrarla: **no es «nada de
porcentajes» — es que todo dato con fecha de vencimiento sale de la pieza.**
Una tarifa, un tope, un mes de promoción. *El modo de falla es el mismo y es
el peor: no rompe nada, sigue compilando, y la pantalla afirma algo que dejó
de ser cierto.*

**② La nota bajo el rótulo** (por ancho de teléfono) y **③ la tarifa antes del
IVA** (el impuesto es el penúltimo renglón; la pieza no sabe si la tarifa
tributa, y entre un orden que afirma algo que no sabemos y uno que no afirma
nada, gana el segundo).

**Y el criterio de fondo, que es el que trasciende a esta pieza:**
*una garantía probada con el error del compilador dura; una regla que alguien
tiene que acordarse de seguir, no.* Es la misma forma con la que esta casa
hizo inexpresables el monto de `CabeceraCaso`, la asimetría de
`TarjetaDestinoPlata` y el motivo de rechazo de `TarjetaFactura` — **cuatro
piezas, un solo movimiento.**

⚠️ **Alcance de lo firmado, para que nadie lo lea de más:** las tres rigen
sobre `DesgloseCompra` y sobre la clase que nombran. **No se tocó ninguna otra
pieza ni ningún gate en este asiento** — el árbol quedó como estaba, y los OTA
de la tanda 2 (`bd92f887` · `fd1607f9`, ancla `692b6012`) siguen siendo los
vigentes: esta firma no movió un byte del bundle.

---

# S115-B · TANDA 3 — LAS DOS FIRMAS EJECUTADAS

## ① R84 SUMA EL BRAZO DE FECHAS — hecho

**El caso real que ya existía, encontrado y nombrado:**
`apps/prestador/src/i18n/es.ts` — *«…Desde el **15 de agosto**, quien atiende
sin matrícula cargada no se ofrece a las familias»*, con su par en `en.ts`
(«From **August 15**…»). **Es la frontera de matrícula del canon escrita en un
diccionario**: si la fecha se mueve, el texto miente y nada falla.

**Baseline 2, SOLO-BAJA.** Los dos son el par es/en de esa frase, **viven en
territorio de las apps y no se tocaron**: congelarlos deja el caso visible sin
bloquear a nadie, y la regla sí frena al siguiente. Muere en 0.

### Los tres controles, corridos en las DOS direcciones

| control | resultado |
|---|---|
| **rojo sobre el caso real** — agregar una 3ª fecha | ✅ marca las tres, con archivo y línea |
| **una fecha por props** (`{{cuando}}`) | ✅ **0** — no la marca |
| **un mes en una KEY DE FORMATO** (`mesLargo: 'enero'`) | ✅ **0** — exenta |
| **su discriminador**: mismo valor, otra key (`promoVence: 'enero'`) | ✅ **marca** — la exención no es un agujero |

🔴 **El límite de palabra es UNICODE, jamás `\b`** — y el costo del límite malo
está **medido**: sin él la regla marca **cinco falsos positivos que ya viven en
la casa**, todos por substring:
`M**ayo**r` · `en m**arch**a` · `**Abril**a y ajustalo` · `m**ayo**r a cero` ·
`momento_senior: m**ayo**r`.
*Ya me había cobrado uno de esos en la tanda 1, con R84 acusándose a sí misma.*

**Hueco declarado:** `may` **no entra a la lista** — en inglés es también el
verbo, y marcaría para siempre nuestra propia key `facturacion.deducible`
(*«pet expenses **may** be tax-deductible»*). *Entre un falso positivo
permanente y un mes que no se caza, se declara el segundo.*

## ② EL FORMATO DE LA PLATA: PUNTO — con una corrección a mi propio reporte

🔴 **Mi reporte de la tanda 1 decía que el riel de la coma «no lo llama
nadie». Era falso**: eso valía para el **hook** `useMoneda`, no para el riel.

**Y el gate volvió a corregirme: son 8, no 5.** Mi censo con `grep` medía el
import **en una línea**, y `grep` trabaja línea por línea — perdía los imports
**multilínea** (`historico.tsx`, `ventana-pedidos.tsx`). El regex de la regla
es el mismo patrón, pero en JS `[^}]*` cruza saltos de línea.

**Los 8: siete VIVOS del prestador** (`historico` · `ventas/facturacion` ·
`ventas/mostrador` · `ventas/pedido` · `ventas/producto` · `ventana-pedidos` ·
`vitrina-piezas`) **+ el hook muerto del cliente.**

### Por qué NO se retiró, y por qué tampoco se alineó

**Retirarlo rompía siete pantallas vivas** ⇒ descartado por medición.

**Y alinearlo al punto NO es una línea inocua** — el dato que lo decide:

```
                    $45        $1234.50
  monto()  es  →  $45,00      $1.234,50
  monto()  en  →  $45.00      $1,234.50
  formatearPrecio  $45.00     $1234.50   ← sin separador de MILES
```

⚠️ **Alinearlo le quitaría el separador de miles al prestador**, justo en
liquidaciones y facturación, que es donde viven los montos grandes. *Eso es
una decisión de producto y no la toma un riel.* **Queda servida, con estos
números.**

### Lo que se hizo, que sostiene la firma sin decidir por el founder

1. **`monto()` MARCADO** en `packages/i18n/src/moneda.ts` con la firma
   verbatim, la medición real, la tabla de arriba y el puntero a
   `formatearPrecio` como fuente única.
2. **`R87` nueva, solo-baja desde 8** — un consumidor NUEVO sale en rojo
   (probado: lo caza y lo nombra). *La marca sola sería «una regla que alguien
   tiene que acordarse de seguir», que es literalmente lo que la firma dice
   que no dura. El gate es lo que la hace durar.*

**Lo que `R87` NO mide, declarado:** los `toFixed(2)` escritos a mano —que son
el otro formato, y son decenas— ni juzga cuál de los dos está bien. *Su verde
dice «no entró un consumidor nuevo del riel de la coma», jamás «el producto
muestra un solo formato».*

## Un choque de nombres, por segunda vez en la sesión

`DICCIONARIOS` **ya existía** en `verify-diseno.mjs` apuntando a **sólo los dos
del cliente**. Lo cazó el parser («Identifier already declared») — pero **el
daño real no era el choque: si hubiera reusado esa constante, el brazo no
habría mirado nunca el prestador, que es donde vive el caso real que vino a
cazar.** Habría dado verde sin mirar donde estaba el defecto. Renombrado a
`DICCIONARIOS_R84`. *(La primera vez fue `TRAZO` en la tanda 1.)*

## Operativo

**SIN PUBLICAR, por orden:** esto entra en el OTA que arme A cuando el checkout
esté cableado. Los OTA vigentes siguen siendo los de la tanda 2 (`bd92f887` ·
`fd1607f9`, ancla `692b6012`).

`verify:diseno` **VERDE · 77 reglas** (76 + `R87`) · `verify:contrast` **436
pares, 0 fallos** · `verify:identificacion-ec` **VERDE** · typecheck `ui` ·
`cliente` · `prestador` en 0.

---

# S115-B · TANDA 4 — LA FUENTE ÚNICA DE PLATA

**Firma final: COMA DECIMAL Y PUNTO DE MILES, sin excepciones.**
`$45,00` · `$1.234,50` · `$0,99`. *«es el formato ecuatoriano y el de los RIDE
que el cliente va a recibir del SRI — la app no puede decir un número distinto
del que dice su factura»*.

## 🔴 Lo que hizo frenar, y por qué el founder dio vuelta su propia firma

Al ir a ejecutar «punto en toda la casa» apareció que **`SliderPrecio` parsea
etiquetas de precio formateadas** — su propio comentario lo dice: *«los valores
numéricos derivados de las etiquetas ("$25.00" → 25)»*. Con separador de miles:

```
$1.234,50  →  1.234,50  →  1.234.50  →  parseFloat  →  1.234
```

**Y lo caro no es el número: `1.234` es FINITO**, así que el guard
`numeros.every(Number.isFinite)` **no disparaba** y la edición quedaba activa
sobre un riel mal numerado. *El guard existía y el defecto pasaba por debajo.*

Sobre eso el founder cambió la firma —*«un payout sin separador de miles se lee
mal justo donde el prestador mira cuánto cobra»*— y después la cerró con la
razón del RIDE.

## Dónde vive la fuente única, y por qué NO donde se pidió

La orden decía que `formatearPrecio` (de `PrecioText`, en `packages/ui`) fuera
la fuente y que `monto()` delegara en ella. **Medido: `packages/ui` YA depende
de `packages/i18n`** —su `package.json` lo declara— ⇒ el import al revés sería
un **CICLO entre paquetes**.

⇒ **el formato baja a `packages/i18n`**, donde ya vive el de las fechas, y
**`PrecioText.formatearPrecio` lo re-exporta**: *los consumidores no cambian
una línea y la fuente sigue siendo una sola.*

## El par inverso, y lo que se hizo inexpresable

`parsearPrecio` **valida la FORMA antes de parsear**. En el formato de la casa
el punto SIEMPRE separa grupos de tres y la coma SIEMPRE es decimal, así que un
texto que no cierre esa forma **devuelve `NaN`**:

```
'1.234,50' → 1234.5    ✓        '1234.50'  → NaN  ✓ ← formato AJENO, se rechaza
'1234,50'  → 1234.5    ✓        '1.234.50' → NaN  ✓
'25,00'    → 25        ✓        '25.00'    → NaN  ✓
```

🔴 **El último es el que importa.** Sin la validación de forma daría `123450`
—cien veces el valor— y `Number.isFinite` lo dejaría pasar otra vez. *Entre
rebotar una entrada de formato ajeno y aceptar un número cien veces más
grande, se rebota.*

**Y por eso NO hay dos helpers, uno estricto y otro tolerante:** el tolerante
sería exactamente el que devuelve el número plausible.

## El censo que el gate me volvió a corregir

Reporté **8 sitios de parseo**. El censo real dio **18** — mi lista anterior
estaba cortada por `head`. Y **no todos son plata**:

| | |
|---|---|
| **PLATA → migrados (13 sitios / 9 archivos)** | `SliderPrecio` ×2 · `paseo/taller` ×4 · `grooming/taller` · `veterinaria/taller` · `presupuesto/nuevo` · `mostrador/atencion` ×2 · `negocio/caso` · `adoptables` · `hoja-precio` |
| **NO PLATA → NO se tocan (4)** | dos pesos en kg · **vitales** (temperatura, frecuencia) · `duracion_dias` |

⚠️ **Aplicarles un parseo de PRECIO habría sido trasplantar un criterio
correcto a otra pregunta** — un kilo no lleva separador de miles ni símbolo.
Los tres archivos quedan exentos **por RUTA y no por patrón**, porque la regla
no puede distinguir plata de peso mirando el texto.

**Hallazgo de paso:** `leerPrecio` estaba **copiada byte por byte en tres
talleres** (paseo, grooming, veterinaria). El helper las unifica.

## Los instrumentos

| gate | qué vigila | estado |
|---|---|---|
| `verify:plata` (nuevo) | el formato, su inverso exacto, y que **un parseo fallido NO sea finito** | **VERDE · 29 comprobaciones** sobre el archivo real |
| `R87` | que nadie formatee con otro riel | 8, solo-baja |
| `R88` (nuevo) | que nadie parsee plata a mano | **DURA EN 0** · 693 archivos |
| `R89` (nuevo) | **preventivo**: ningún monto formateado en un payload | **DURA EN 0** · terreno limpio |

**`R89` nació preventiva y ese es su valor**, con la firma escrita: *«la única
forma de que nunca aparezca es que algo lo vigile desde el día cero»*. Los
montos que van al riel o al XML fiscal **siguen siendo numéricos con punto
decimal**; el formateador es sólo de presentación.

**Los cuatro rojos, probados contra código real:** quitar la validación de
forma → `'1234.50'` da `123450` finito ✓ · un parseo a mano nuevo → R88 lo
nombra ✓ · `p_monto: formatearPrecio(total)` → R89 lo nombra ✓ · un consumidor
nuevo del riel → R87 ✓.

**Las dos pruebas web actualizadas** (`$10.00` → `$10,00`, `$2.00` → `$2,00`):
*que nadie las corra no las vuelve inocuas.*

## Una regla que sobrevivió a la enmienda, y por qué

`R87` se escribió con la firma «punto» y **siguió sirviendo con la firma
«coma»**, sin tocarla. Lo que mide no es el separador: es **que haya UNA sola
fuente**. *Una regla atada al valor habría muerto con la enmienda; atada al
invariante, no.*

## Y la marca vieja se ENMENDÓ en su lugar, no se borró

En `moneda.ts` había quedado mi marca con la firma anterior («EL FORMATO ES
PUNTO») conviviendo con la nueva. **Dos letras firmadas que se contradicen son
peores que una equivocada** — el arco entero (primera firma → el dato que la
dio vuelta → la enmienda) queda escrito ahí, que es lo que evita que la próxima
mesa lo vuelva a decidir sin los números.

## Operativo

**SIN PUBLICAR, por orden.** Entra en el OTA que arme A con el checkout
cableado. Los OTA vigentes siguen siendo los de la tanda 2 (`bd92f887` ·
`fd1607f9`, ancla `692b6012`) — **y ya no reflejan el formato**: el próximo
que se publique lo lleva.

`verify:diseno` **VERDE · 79 reglas** · `verify:plata` **VERDE** ·
`verify:identificacion-ec` **VERDE** · `verify:contrast` **436 pares** ·
typecheck `ui` · `cliente` · `prestador` en 0.

---

# S115-B · TANDA 5 — LOS DOS PEDIDOS DE C, Y UN HALLAZGO DE MEDIO DE PAGO

**La rama se puso al día con `main` primero: estaba 53 commits atrás** y yo
estaba midiendo un árbol viejo. Merge limpio, gates verdes después.

## ⓪ `medio_pago` — el supuesto del buzón de A no se cumple

A escribió: *«Hay elección en la pantalla y no hay registro en la fila.»*
🔴 **Medido: NO hay elección en la pantalla.**

```ts
// apps/cliente/src/components/seccion-medio-de-pago.tsx
{ medio: 'deuna' } | { medio: 'tarjeta', tarjetaId }
```

La superficie conoce **dos** medios, no cuatro. ⇒ el dato **no existe desde el
origen**: no es que se pierda al escribir la fila. Cablear la puerta no alcanza.

**Lo demás medido:** `tarjetas_guardadas.marca` es la RED (`vi`/`di`), no el
instrumento · Nuvei **no devuelve `card_type`** · pero **`bin` SÍ se guarda**, y
el BIN determina el tipo por estándar de industria.

**Dos caminos, servidos a A** (buzón
`S115-B-para-A-la-pantalla-no-ofrece-la-eleccion`): derivar del BIN —sin
fricción, y **retroactivo sobre las tarjetas ya guardadas**, pero exige un
dataset de rangos que no tenemos— o preguntar **al guardar la tarjeta**, no en
cada checkout. ⚠️ Con su advertencia: es un dato **fiscal auto-declarado**, y si
la familia se equivoca el XML sale **emitido con un dato falso**, que es peor
que `pendiente_manual` porque no avisa.

**Los dos bordes quedan confirmados** (mixto → medio del RIEL · recurrente →
hereda del mandato). Y el segundo hueco es el mismo: **el mandato tampoco lo
tiene**, porque nadie lo capturó nunca.

## ① `sinCorreo`

`CampoIdentificacion` gana la prop. Precedente `Campo.sinPie`. **Su modo de
falla es silencioso y se dice:** apaga el campo **y su validación**, así que
quien la use valida con `esCorreoValido`.

## ② La validación de correo: UNA, no «alineada»

**Censo: TRES regex**, los tres en el prestador, **ninguno en `packages/ui`** —
y mi propia pieza **no validaba el correo**. Dos coincidían **por copia**.

**Ganó el más estricto** (`perfil.tsx`): exige punto en el dominio y ningún
segmento vacío; las otras dos aceptaban `a@b.` y `a@.b`. *Entre tres que ya
viven en producción, gana la que rechaza más: el costo de rebotar un correo raro
es que la persona lo corrija; el de aceptarlo es que no le llegue la factura.*

**`verify:correo` · 23 comprobaciones**, y su primer positivo es el caso real:
**`karina charry@gmail.com` tiene que dar rojo**. *Si algún día deja de darlo, el
defecto volvió.* Incluye el control que evita el validador que rechaza todo: 6
correos válidos que deben pasar.

**`R90` DURA EN 0** — un cuarto regex sale en rojo. **Atada a la FORMA y no al
valor (`L-534`)**: afinar el regex no la rompe.

⚠️ **No hace `trim()` a propósito:** un correo con espacios al borde **es** un
correo mal escrito, y limpiarlo en silencio esconde el error justo donde la
función existe para mostrarlo.

## Gates

`verify:diseno` **VERDE · 80 reglas** · `verify:correo` **23** ·
`verify:plata` **29** · `verify:identificacion-ec` **652** ·
`verify:contrast` **436 pares** · typecheck en los tres paquetes.
**Sin publicar**, como rige.
