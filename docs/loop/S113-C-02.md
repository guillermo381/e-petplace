# S113 · PISTA C — D-1017 «el prestador no abre» · parte

> Rama **`pista/s113-c-02`** desde `main` **`d29b34f1`**.
> Con **`pista/s113-e-03` mergeada, fijada a `f25bc92c`** (`verify:hooks`),
> control de ancestría verde.

---

## ① EL ROJO, REPRODUCIDO POR MÍ — no heredado

Web contra este árbol, **sesión real de prestador** (`demo-prestador`, Paseos
Andres), transición `verificando → resuelta`:

```
Error: Rendered more hooks than during the previous render.
    at Object.useState (…entry.bundle…:430839:18)
    at TabsLayout (…entry.bundle…:74721:59)
    at renderWithHooks / updateFunctionComponent / beginWork
The above error occurred in the <TabsLayout(./(tabs)/_layout.tsx)> component.
React will try to recreate this component tree from scratch using the error
boundary you provided, Try.
[caida] render roto (raíz): Rendered more hooks than during the previous render.
```

El arnés vive en `scripts/repro-d1017.mjs` (playwright-core + Chrome, molde de
los smokes web de la casa; las credenciales salen de `.env.local`, **nunca del
código**).

**Un matiz sobre el diagnóstico de A, que no lo contradice:** su aviso de orden
nombra el hook 28 como `useContext` (`useSafeAreaInsets`); **mi stack nombra
`useState`**. Son las dos caras del mismo hecho — el que **difiere** primero es
el `useContext`, el que **lanza** es el `useState` de `altoBarra`, el primero en
intentar leer un slot que no existe. *El que rompe no es necesariamente el
primero que difiere.*

**Y son SEIS returns tempranos, no uno:** `verificando` · `repartidor` ·
`sala_espera` · `bienvenida_pendiente` · `invitacion_pendiente` · `sin_sesion`.
Por eso **hacía falta sesión real**: sin sesión la transición es
`verificando → sin_sesion`, **los dos salen temprano** y no hay crash.

## ② LA CURA — se subieron los HOOKS, no se bajó ningún return

Los **tres** (`useSafeAreaInsets` · el `useState` de `altoBarra`, que depende de
él · `useEffect(escucharPendientes)`) viven ahora en `:240`, `:241` y `:250`; el
primer return de guarda está en `:543`. El efecto queda condicionado **por
dentro** —no lo está: ya era incondicional—, **nunca por fuera**. Corre también
mientras se verifica la sesión, y es correcto: el lector rebota solo sin sesión y
el contador queda en cero.

## ③ EL VERDE, EN EL MISMO ARNÉS

```
Hola, demo-prestador · Paseos Andres · Te quedan 5 · terminas 18:00 · EN RUTA · $44.13 DEL DÍA
frontera de caída: no se comió el árbol
barra de tabs: 5 pestañas — Hoy · Datos · Atender · Negocio · Cuenta
cero errores de hooks
```

⚠️ **La burbuja no se pudo ver dibujada, y no es un defecto:** la cuenta que
puedo abrir es un **paseador** y sus dos clases están en cero ⇒ *la pieza calla
por diseño*. Intenté **una vez** con `guillo381+refugio@gmail.com` y la clave del
keychain: rebota («El email o la contraseña no coinciden») — su clave es propia
del founder, como el arranque declara. **No insistí.**

**Lo que sí queda probado con cualquier cuenta:** el **subárbol del `tabBar`
existe otra vez**. `BurbujaDelShell` es hermano de `BarraTabs` adentro del mismo
`tabBar`, y el crash se llevaba el árbol **entero** — no había barra ni burbuja.
Con la barra dibujada, que la burbuja aparezca ya sólo depende del dato, que su
propio gate (`verify:abanico`) cubre.

## ④ EL CENSO — `scripts/verify-hooks-bajo-return.mjs`, con su rojo probado

**395 ámbitos · 68 con return de guarda · 0 con hooks debajo.** Revirtiendo la
cura, lo caza y lo nombra:

```
✗ apps/prestador/src/app/(tabs)/_layout.tsx:776  TabsLayout() · useSafeAreaInsets
  — el primer return de guarda está en :541
```

**Los cuatro layouts con guarda**, medidos uno por uno:

| layout | guarda | hooks debajo |
|---|---|---|
| `apps/cliente/src/app/(tabs)/_layout.tsx` | `:272` | **0** |
| `apps/cliente/src/app/_layout.tsx` | `:134` | **0** |
| `apps/prestador/src/app/(tabs)/_layout.tsx` | `:541` | **0** (era 3) |
| `apps/prestador/src/app/_layout.tsx` | `:95` | **0** |

Los otros ocho layouts **no tienen return de guarda**.

### 🔴 El censo se corrigió TRES veces antes de dar un número

Y las tres las cazó su auto-prueba o el objeto, no yo leyéndolo:

1. Buscaba `use[A-Z](` **en cualquier parte de la línea** ⇒ gritaba sobre
   `const f = () => { const c = useState(2); }`, que es un hook de **otra**
   función. Ahora el hook tiene que ser **la sentencia**.
2. Sólo reconocía `function X(` **con mayúscula**, así que un hook propio entre
   dos componentes se le atribuía al de arriba: **cuatro falsos positivos**
   (`videollamada-piezas.tsx:205` y `:216`, idénticos en las dos apps) acusando a
   `VideoPropioEnLlamada` de hooks que son de `useCamara`, tres líneas más abajo.
   *Un instrumento que acusa a la función equivocada con una línea real manda a
   curar código sano.*
3. La **auto-prueba medía con otra frontera que el censo** y daba rojo sobre su
   propio fixture sano. Ahora comparten la regla: *un arnés que no la comparte
   con lo que mide, mide otra cosa.*

## ⑤ `usarMoneda` → `useMoneda` — un nombre en español apagaba un guard

Con `verify:hooks` de E corriendo, sus **tres** violaciones eran las tres del
mismo archivo: *«React Hook "useState" is called in function "usarMoneda" that is
neither a React function component nor a custom React Hook function»*.

**La razón no es de estilo.** La regla reconoce **por prefijo** (`use…` o
mayúscula), así que un hook en español es **invisible** para ella — y lo que se
pierde no es un aviso cosmético: es **la regla entera sobre esa función**. *El
día que alguien llame a este hook después de un return —exactamente el defecto
que acaba de dejar al prestador sin abrir— el gate no lo ve.* **Un nombre que
apaga un guard cuesta más que la consistencia de idioma que compra.**

**El censo de llamadores: CERO.** Dos ocurrencias en todo el monorepo, las dos de
su propio archivo; nadie importa `usar-moneda`. ⇒ **no hubo llamadores que
actualizar y no aparecieron rojos nuevos, porque no había dónde.** Se renombró
igual —el nombre ya estaba apagando el guard— y queda dicho en el archivo: *este
hook está escrito, documentado y no lo usa nadie.*

Se renombró **también el archivo** (`git mv` a `use-moneda.ts`): *un hook que se
llama `useMoneda` en un archivo `usar-moneda.ts` deja la mitad del problema, y es
la mitad que se busca por nombre de archivo.*

## ⑥ GATES

| gate | resultado |
|---|---|
| `verify:hooks` (de E) | **VERDE · 612 archivos · 0 violaciones · 0 sin medir** |
| `verify:hooks-bajo-return` (nuevo) | **VERDE · 395 / 68 / 0** |
| `verify:diseno` | **VERDE, 61 reglas** |
| `verify:razon-muda` · `verify:puerta-unica` | **VERDE** · **VERDE** |
| `verify:nexo` · `coach` · `glifos` | **314/0** · **74/0** · **31/0** |
| typecheck | **4 tsc en 0** |

## ⑥bis 🔴 EL GATE QUEDÓ REGISTRADO — Y ME LO EXIGIÓ UN GATE DE LA CASA

Iba a declarar la línea del `package.json` **sin agregarla** —«no es `apps/`», la
misma disciplina territorial que sostuve dos veces en el lote 0 con
`verify:nexo` y `verify:glifos`—. **`verify:gates-existen` abortó el commit:**

```
✗ EL CANON NOMBRA UN GATE QUE NADIE PUEDE CORRER — el commit NO ocurre.
🔴 SIN LÍNEA en package.json — el archivo existe, el nombre no invoca (1):
   verify:hooks-bajo-return
```

**Y tiene razón.** *Un gate nombrado y ausente no da rojo: NO CORRE, y su
silencio se lee como salud.* Es exactamente el defecto que yo mismo medí dos
veces en este lote —`verify:puerta-unica` primero, `verify:glifos` después— **y
las dos veces lo dejé para otro en nombre del territorio.** La casa ya tiene un
instrumento que dice que eso no alcanza.

⇒ **la línea entra**, y con ella `gates-existen` vuelve a verde: *36 nombres · 33
`verify:*` · todo gate nombrado existe como script invocable.*

## ⑦ LO QUE NO TOQUÉ

`packages/` (sólo lo que trajo el merge de E) · `supabase/` · el `package.json`
raíz · **ningún `return`** del layout —sólo se movieron hooks— · el cliente,
salvo el rename de `useMoneda` · `videollamada-piezas.tsx` (**el censo lo acusó y
la acusación era del instrumento, no del archivo**) · **cero dependencias
nuevas**. No publiqué OTA. **Cero aparato: el repro es WEB con sesión real, no un
teléfono.**

---

# ⏪ LOTE 0.2 — EL CABLEADO DEL ORBE (`918cddc0`)

## ⑧ EL MERGE DE B: NO HABÍA NADA NUEVO, y se declara con la medición

En origin hay **dos** ramas de B —`pista/s113-b` (`4e944cb3`) y
`pista/s113-b-01` (`edf9091a`)— y **las dos son ancestros de mi rama**: llegaron
por `main`. *No es que no hiciera falta pedirlo; es que ya estaba.* Lo que sí
mergeé es **`main` `0a1986ef`** (el 0.1 publicado), para construir sobre lo que
el founder ya tiene en el teléfono.

## ⑨ LA FILA «Pregúntale» ABRE LA HOJA — ya estaba, y se midió

Medido en web con **sesión real de familia** (`scripts/medir-orbe-s113c.mjs`):
tocar la etiqueta abre la Hoja del Coach —aparece **«Soy Nexo»**, la
presentación de primera vez—. Los cuatro dedos se dibujan: **Peso · Vacuna ·
Antiparasitario · Foto**. Cero errores de página.

## ⑩ 🔴 «EL ORBE ABRE Y CIERRA» NO SE PUEDE CABLEAR DESDE `apps/`

La pieza tiene **dos productores de `onPreguntar`, y son el mismo callback**:

```
PresenciaCoach.tsx:592   la etiqueta «Pregúntale»  → onPreguntar
PresenciaCoach.tsx:621   el orbe: onPress={abierta ? onPreguntar : onAbrir}
```

Desde el montaje **no hay forma de distinguirlos**: si hago que `onPreguntar`
cierre, la etiqueta deja de abrir la Hoja; si abre la Hoja, el orbe abierto no
cierra. *Es una prop para dos nodos.*

⚠️ **Y el cambio contradice una decisión que B escribió con su razón**, en el
mismo archivo: *«Abierta, el orbe abre la HOJA — no cierra la fila. Cerrar es
tocar afuera o el botón atrás. Un mismo toque que a veces abre y a veces cierra
enseña a no tocarlo.»*

### 📮 Pedido a B, por nombre
Que el orbe abierto llame a **`onCerrar`** en vez de `onPreguntar` (una línea en
`:621`), **o** que la pieza exponga un callback propio para el orbe abierto.
**Es de mesa, no mío:** hay una razón firmada del otro lado.

## ⑪ EL ORBE SÍ SE ENCIENDE — la captura del founder NO es del montaje

La pieza decide el violeta en `:522`:

```js
const violeta = abierta || estado === 'despierta' || estado === 'hablando'
```

⇒ con `abierta` en `true` **ya alcanza**. Y **`abierta` llega**, medido en el DOM:

```
antes   · aria-label="Abrir a Nexo"
después · aria-label="Pregúntale a Nexo"
```

que es literalmente `abierta ? voz.preguntar : voz.orbe`.

**Se mira eso y no un píxel a propósito:** *un screenshot diría «no se ve
violeta» sin decir de quién es.* El label es **la prop cambiando de valor**.

⇒ **Con evidencia: el montaje hizo su parte.** Si en la captura el orbe no se
veía encendido, la causa está del lado del **dibujo de la pieza** —el
`withTiming` de `encendido`, el degradé radial o el color— y **no** en que
`abierta` no llegue. **No lo curo: es `packages/ui`.**

## ⑫ EL ESTADO, MEDIDO POR LOS DOS CAMINOS

No se puede leer del DOM —*no es una prop que se dibuje, es un cálculo del
shell*— así que se asierta por sus dos mitades: **el literal del cableado**
(`huellaAbierta: abierta` · `hojaAbierta: hojaCoach !== null`) y **la función
pura** con los casos del encargo:

| caso | estado |
|---|---|
| al abrir la pata | **`despierta`** |
| con la Hoja | **`hablando`** |
| con las dos | manda la Hoja |

⚠️ **Y el caso REAL de este montaje, que es el que importa:** `tocar('coach')`
**cierra la pata y abre la Hoja**, así que la pieza recibe `'hablando'` con
`huellaAbierta: false`. *Si el assert sólo mirara `true/true`, mediría un caso
que este montaje no produce.*

**El rojo del cableado** (cambiando `huellaAbierta: abierta` por `false`):

```
ROJO · `estado` deriva la pata de `abierta` — estado={estadoNexo({ … huellaAbierta: false … })}
```

## ⑬ GATES DEL 0.2

`verify:nexo` **323 · 0** · `verify:hooks` **VERDE** · `hooks-bajo-return`
**VERDE** · `gates-existen` **VERDE** · `diseno` **VERDE, 61 reglas** ·
`razon-muda` **VERDE** · `puerta-unica` **VERDE** · `coach` **74/0** · `glifos`
**31/0** · `tipos:rutas` **OK** · **4 tsc en 0**.

## ⑭ LO QUE NO TOQUÉ EN EL 0.2

`packages/ui` —**incluido el `:621` que el encargo pediría cambiar**, porque es
de B y tiene razón escrita— · `supabase/` · el prestador · las keys · la
pantalla del recuerdo · el montaje de los dedos. **Cero dependencias nuevas.**
**No publiqué. Cero aparato: la medición es web con sesión real.**

---

# ⏪ CIERRE DEL 0.2 — EL ORBE ES UN INTERRUPTOR (`23e450b8`)

Mergeado **`pista/s113-b-02`** fijado a **`439f0694`**, ancestría verde.

## ⑮ EL CAMBIO DE PROPS: NINGUNA FIRMA CAMBIÓ

B lo dice y lo verifiqué en el objeto: `onPreguntar` es **la misma prop y se
pasa igual**; lo que cambió es **desde dónde se llama**.

```
:740  el orbe:  onPress={abierta ? onCerrar : onAbrir}   (era onPreguntar)
:663  la fila «Pregúntale»:  onCerrar() + onPreguntar()
```

⇒ **el montaje no necesitaba tocar una prop.**

## ⑯ LO QUE SÍ CAMBIÉ, Y LO PIDIÓ EL OBJETO

`tocar()` abría con un **`setAbierta(false)` propio**, y hoy sería **el segundo
lugar que cierra la misma pata**: medido, la pieza ya llama a `onCerrar` antes de
disparar el atajo (`:786`), la pastilla (`:677`) y la fila (`:663`).

*Dos lugares que cierran lo mismo son dos que algún día no van a estar de
acuerdo* — **y ya hay un caso donde difieren a propósito: con un dedo apagado la
pieza NO cierra**, para que la razón se lea con la fila a la vista. Lo mismo en
`onPendiente`. Asertado, con su rojo:

```
ROJO · `tocar` NO cierra la pata: eso es de la pieza
```

## ⑰ LA MEDICIÓN, EN WEB CON SESIÓN REAL DE FAMILIA

```
reposo      · orbe {"nombre":"Abrir a Nexo"} · nodos con esa voz: 1
un toque    · dedos: Peso · Vacuna · Antiparasitario · Foto
              abrió: SÍ   · ¿abrió TAMBIÉN la Hoja? no ✓
otro toque  · dedos: ninguno
              cerró: SÍ   · ¿abrió la Hoja al cerrar? no ✓
la fila     · nodos «Pregúntale»: 1 (sólo la fila) · Hoja: «Soy Nexo» · cerró la pata
errores de página: 0
```

⇒ **el orbe abre y cierra, la fila abre la Hoja, y ningún toque hace dos cosas.**

### 🔴 Y el arnés se corrigió a sí mismo antes de que le creyera

La primera corrida reportó *«un toque hizo dos cosas: al cerrar se abrió la
Hoja»*. **Era falso, y el que hacía dos cosas era el instrumento:** el locator
buscaba `/^Abrir a |^Pregúntale a /` y tomaba `.first()`, y **con la fila abierta
el primero en el DOM es la fila** —la pieza la dibuja antes que el orbe—, así que
el segundo toque le pegaba a la fila.

Medido en la pieza: el orbe lleva `accessibilityLabel={voz.orbe}` **siempre**
(`:729`) y la fila lleva `voz.preguntar` ⇒ **se distinguen por nombre exacto**.
Curado. *Un rojo por la razón equivocada está tan roto como un verde por la razón
equivocada.*

## ⑱ ⚠️ OBSERVACIÓN MEDIDA, PARA B (no la curo: es `packages/ui`)

El orbe se llama **«Abrir a Nexo» en los dos estados**, y ahora también cierra ⇒
**dice «Abrir» cuando el toque va a cerrar**. Y en web **`aria-expanded` sale
`null`**: la pieza pasa `accessibilityState={{ expanded }}` pero RN-web no lo
emite, así que ahí **el estado no se anuncia por ningún lado**. En nativo el
estado sí viaja; la etiqueta, no.

## ⑲ EL GATE DEL Z-ORDER SE ROMPIÓ AL MERGEAR, OTRA VEZ, Y ESTÁ BIEN

Murió **`RESPLANDOR`**: era una **sombra** y ahora es un **círculo dibujado**,
porque *«`shadowRadius` no existe en Android y **era la mitad de por qué esto no
se veía**»* (B, medido).

⭐ **Eso confirma con la fuente lo que este lote había concluido midiendo el
DOM:** el montaje hacía su parte y la causa estaba en el dibujo.

Reescrito contra `LIENZO` y `RESPLANDOR_RADIO` (barra = **119**):

| qué | holgura |
|---|---|
| cuerpo del orbe | **+20** |
| resplandor visible | **+8,0** |
| lienzo (transparente) | **−8,8** — declarado |
| *sin `aireInferior`* | **−99** — el control negativo |

## ⑳ GATES DEL CIERRE

`verify:nexo` **326 · 0** · `hooks` · `hooks-bajo-return` · `gates-existen` ·
`razon-muda` · `puerta-unica` **VERDE** · `diseno` **VERDE, 61 reglas** · `coach`
**95/0** · `glifos` **31/0** · `contrast` **OK** · `tipos:rutas` **OK** · **4 tsc
en 0**.

## ㉑ LO QUE NO TOQUÉ

`packages/ui` (sólo lo que trajo el merge) · `supabase/` · el prestador · las
keys · la pantalla del recuerdo · los glifos · `voz-hecho.ts` y `timeline.ts`
(**el hueco del texto del recuerdo sigue abierto y es pedido a A**). **Cero
dependencias nuevas. No publiqué. Cero aparato.**

---

# ⏪ LOTE 0.3 — EL ORBE DICE LO QUE EL TOQUE VA A HACER (`c7c74e03`)

## ㉒ B NO TIENE SHA NUEVO, y se declara con la medición

Sus tres ramas en origin son `pista/s113-b` (`4e944cb3`), `-b-01` (`edf9091a`) y
`-b-02` (`439f0694`), y **la última ya era ancestro de mi rama** desde el cierre
del 0.2. Lo que sí mergeé es **`main` `b92491d0`** (el 0.2 publicado).

⇒ **ninguna prop cambió:** el contrato de `voz` sigue siendo
`{ preguntar, orbe, chat, pedidos, cerrar }` y la pieza sigue usando
`accessibilityLabel={voz.orbe}` **en los dos estados**.

## ㉓ LO QUE SÍ HABÍA QUE CABLEAR, Y SE PUDO SIN PEDIR NADA

En el cierre del 0.2 dejé declarado que el orbe decía «Abrir a Nexo» **también
abierto** — o sea, *«Abrir» cuando el toque iba a cerrar*. **La cura no era
pedirle a B una prop nueva:** `abierta` es estado de este shell, así que **el
VALOR de `voz.orbe` puede cambiar con él**.

```js
orbe: abierta ? t('nexo.cerrarOrbe', { nombre }) : t('nexo.etiqueta', { nombre })
```

*Una prop que ya recibe una cadena no necesita volverse dos para decir dos
cosas.*

⚠️ **Y no se reusa `voz.cerrar`, que ya existe:** ésa es la voz del **velo** —lo
que se lee al tocar afuera— y **son dos actos distintos que hoy coinciden en la
palabra**. *Compartir la key ata el día que una de las dos cambie.* Key nueva:
**`nexo.cerrarOrbe`** (es «Cerrar a {{nombre}}» · en «Close {{nombre}}»), con su
assert y su rojo:

```
ROJO · `voz.orbe` cambia con el estado: abierta dice cerrar
```

## ㉔ LA MEDICIÓN, EN WEB CON SESIÓN REAL

```
① reposo   · 1 nodo · aria-label="Abrir a Nexo"       dice «Abrir»  ✓
② abierto  ·          aria-label="Cerrar a Nexo"      dice «Cerrar» ✓
             dedos: Peso · Vacuna · Antiparasitario · Foto
③ la Hoja  · marcas: «Soy Nexo»
             cabecera («Nexo»): hallada · svgs adentro: 1
             stops de la paleta EN LA CABECERA: #AE59FF · #9E3AFF · #7C2DD4
             ⇒ el orbe violeta SE DIBUJA ✓
errores de página: 0
```

**Se mira el DIBUJO y no un píxel:** `CabeceraCoach` monta un `<svg>` con un
`radialGradient` cuyos tres stops son la paleta del Coach. *Un screenshot diría
«se ve algo violeta» sin decir si es el orbe o el velo.*

### 🔴 Y el arnés se corrigió otra vez antes de que le creyera

La primera corrida del ③ dijo **«el orbe violeta NO se dibuja»**. Era **del
instrumento**: el selector era `[role="heading"], [aria-label="Nexo"]` y el
`role="heading"` **matcheó primero otro nodo de la Hoja**, que no tiene svg.
Curado midiendo en **dos ámbitos** —el documento entero y la cabecera— para poder
distinguir *«no está dibujado»* de *«lo busqué en el lugar equivocado»*.

⚠️ **Es la tercera vez en este arco que el arnés se corrige solo** —el `.first()`
ambiguo del 0.2, el locator por regex, y ahora el `role="heading"`— **y las tres
veces lo que fallaba era la medición, no la app.** *Un rojo por la razón
equivocada manda a curar código sano, que sale más caro que no medir.*

## ㉕ GATES DEL 0.3

`verify:nexo` **333 · 0** · `hooks` · `hooks-bajo-return` · `gates-existen` ·
`razon-muda` · `puerta-unica` **VERDE** · `diseno` **VERDE, 61 reglas** · `coach`
**95/0** · `glifos` **31/0** · `contrast` **OK** · `tipos:rutas` **OK** · **4 tsc
en 0**.

## ㉖ LO QUE NO TOQUÉ

`packages/ui` (sólo lo que trajo el merge de `main`) · `supabase/` · el prestador
· la pantalla del recuerdo · los glifos · **`voz-hecho.ts` y `timeline.ts`** — el
hueco del texto del recuerdo **sigue abierto y es pedido a A**. **Cero
dependencias nuevas. No publiqué. Cero aparato.**
