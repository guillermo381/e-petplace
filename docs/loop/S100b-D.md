# S100b-D · PARTE — la vuelta de la forma: la barra, los pedidos y el camino

> ## 🔖 HANDOFF DE RELEVO — LEER ESTO PRIMERO
>
> ### CÓMO SE LEE EL ESTADO (comandos, no SHAs tecleados)
> **No hay SHA escrito acá a propósito:** *el commit que registra su propio SHA
> lo cambia al registrarlo.* Se leen del objeto:
> ```
> git ls-remote origin pista-d          # el mío, y contra él se compara lo local
> git rev-parse pista-d                 # local — tienen que coincidir (L-239)
> git status --porcelain | wc -l        # 0 al cierre
> ```
> ⚠️ **Y lo mismo vale para lo ajeno:** el SHA de otra pista se re-mide al usarlo.
> **Yo escalé un bloqueo con el SHA de despedida de B que ya estaba vencido** —
> ella había pusheado después. *Un mensaje de despedida describe el mundo del
> instante en que se escribió* (**L-166**).
>
> ### VERIFICACIONES, CORRIDAS AL CERRAR (18-ago-2026)
> `tsc apps/cliente` **exit 0** · `verify:diseno` de main **VERDE 44 reglas** ·
> `verify:diseno` de `origin/pista-b` (la que trae R53/R54) **VERDE 46, con mi
> ruta declarada y el baseline de 11 intacto** · guard propio
> `scripts/verify-s100b-d-el-estado-se-dice.ts` **VERDE, 42 casos, discriminador
> en 6** · **cero migración · cero dato vivo movido**.
>
> ---
>
> ### ① LO ENTREGADO — con archivo y línea
>
> | qué | dónde |
> |---|---|
> | **el pedido sin recorrido dice su estado** y deja de prometer | `lib/despensa/escalera.ts:138` (`portadorDeEstado`) |
> | la **insignia** en la lista · y la misma forma en el detalle | `despensa/pedidos.tsx:232` · `despensa/pedido/[pedidoId].tsx:360` |
> | **EN CAMINO: mapa de FONDO + hoja encima**, alto derivado | `en-camino/[pedidoId].tsx:113` (`PISO_DEL_MAPA`) |
> | **la placa manda** sobre el nombre | `en-camino/[pedidoId].tsx:410` |
> | la **declaración de R53** | `en-camino/[pedidoId].tsx:255` |
> | **G-14** la canasta con su disco · **el buscador en la fila** | `despensa/index.tsx:661` · `:616` |
> | **G-15** «Tus pedidos» sale de abajo de la vitrina | `despensa/index.tsx:737` |
>
> ### ② LO MEDIDO — con su número Y SU FECHA (separado de lo que es impresión)
>
> **MEDIDO (17-18 ago 2026), contra la base o contra la fuente:**
> - `pagando` **= 4 · con promesa = 4 · con pago confirmado = 0`** ⇒ la promesa
>   **nace antes del pago**, así que la red de seguridad era inalcanzable **por
>   construcción**. *(query sobre `v_pedidos_narrativa`)*
> - los dos únicos `entregado` de la base tenían **0 ítems y 0 envíos con
>   `entregado_en`** — cabeceras huérfanas ⇒ la ceremonia **no era gateable desde
>   ninguna cuenta**. Curado por la siembra de A: pedido con **2 ítems**,
>   `sedimentado` **true→Thor · false→Jack** (verificado ítem por ítem).
> - mis dos pantallas: **cero `position:'absolute'`** antes de S100b ⇒ H-105 no
>   aplicaba, y bajó de cinco a tres.
> - el mapa se comía **380 de ~700 dp (54 %)** ⇒ el rango arrancaba a **~19 dp**
>   del filo. *(aritmética sobre la fuente + el filo de 699 dp medido por B)*
> - `EscaleraEstados`: la vertical usa **`PUNTO`=10**, `NODO` solo en la tira.
>
> **IMPRESIÓN, rotulada como tal — NO son mediciones:**
> - que el código de la puerta deba ir arriba *«porque es lo único que se usa en
>   la puerta»* (argumento de B, que tomé).
> - que `PISO_DEL_MAPA = 0.4` sea el reparto correcto. **Es número de gate.**
> - que la canasta con su disco alcance como **acuse** de haber agregado algo.
>
> ### ③ 🔴 LAS CURAS DE DOS MITADES — quitar una reintroduce el defecto sin verlo
>
> - **`portadorDeEstado()` + su guard.** La función **no es refactor**: el defecto
>   era **el ORDEN de las ramas**, que no tiene forma y **ningún typecheck ve**.
>   *Si alguien vuelve a poner esos `if` adentro de la pantalla, el guard deja de
>   medir la función real y pasa a medir su eco.* **Las dos mitades van juntas.**
> - **el discriminador barre `tienePromesa` en los DOS valores.** Con `false` el
>   orden viejo y el nuevo **coinciden** ⇒ *recortar el barrido a un solo valor
>   deja el guard en verde sobre el defecto exacto que vino a prohibir.*
> - **la hoja + su declaración R53.** El escape es **POR PIE, no por archivo**: hoy
>   hay UN pie y UNA razón. *Agregar un pie de verdad ahí sin su razón hace que la
>   regla vuelva a morder — y tiene que morder.*
>   🔴 **Y OJO CON ESTA, que es la más fácil de romper sin darse cuenta (dato de
>   C, re-medido acá: `grep` de `function r53` sobre `scripts/verify-diseno.mjs`
>   de mi árbol da **0**): R53 y R54 NO ESTÁN EN `main`** — viven en la rama de B.
>   ⇒ **hoy mi comentario `R53-DECLARADO:` es inerte**: no lo respalda ninguna
>   regla corriendo. *Alguien puede leerlo como decoración y borrarlo sin que nada
>   se ponga rojo* — y el día que R53 entre a `main`, la pantalla muerde. **La
>   declaración y la regla son las dos mitades, y ahora mismo viven en ramas
>   distintas.**
>
> ### ④ LO QUE DECIDÍ **NO** HACER, con su razón (para que no se re-intente)
>
> - **NO monté `PantallaConPie` en mis dos pantallas.** No tienen pie fijo ⇒ sería
>   **un no-op que da verde y deja el defecto vivo**. Y en `en-camino` sería
>   **regresión**: mete a sus hijos en un `ScrollView` y devolvería el mapa a ser
>   la banda que el gate vio cortada.
> - **NO subí la placa a `CodigoAEscala`.** En esa misma hoja vive el **código de
>   la puerta** en esa escala, y dos números mono grandes —uno que se DICE, otro
>   que se VERIFICA— es cómo alguien le dice la placa al repartidor. *Una
>   jerarquía que se arregla creando una confusión peor no está arreglada.*
> - **NO pinté el badge del contador a mano.** `Texto` no tiene color inverso ⇒
>   habría sido **inventar contraste sin medirlo**. Se pidió la pieza; **de ese
>   freno nació `GlifoConContador`**.
> - **NO compensé los 53 dp** de A. Eran **cero** (B, aparato) ⇒ *compensar habría
>   metido el defecto que creía prevenir.* Y salió la cura inversa: **saqué el
>   `insets.bottom` de las tres pantallas**, que la barra ya absorbe.
> - **NO construí el afinamiento de R53 por anatomía** (orden de mesa) — ver ⑤.
> - **NO moví dato vivo** para gatear la ceremonia: se pidió siembra con el caso.
> - **NO inventé el portador del estado** mientras B decidía su forma.
>
> ### ⑤ DEUDAS, CON DUEÑO Y DISPARO
>
> | qué | dueño | disparo |
> |---|---|---|
> | **los dos gestos sobre la hoja** (¿scrollea con el pulgar abajo? ¿el código se ve sin desplazar?) y el **`PISO_DEL_MAPA` al 40 %** | **B, con aparato** | **el primer publish** |
> | **R53 por ANATOMÍA** en vez de sumar declaraciones (*no dispara si el bloque absoluto contiene un `ScrollView`*) | quien tenga `verify-diseno.mjs` | **la SEGUNDA hoja con scroll propio** que dispare R53 |
> | los **puntos de 10 dp desnudos** de la escalera vertical del detalle | **B** (anatomía de pieza) | **no se mueven sin aparato** — condición escrita por ella |
> | **H-113**: una narrativa llega a `entregado` con **0 ítems y sin `entregado_en`** | **sin dueño asignado** (motor) | *«un pedido entregado sin ítems no puede ser evidencia de nada»* — y la evidencia sostiene una disputa (H-10) |
> | **el `initApi` en RN-web**: toda carga completa hace correr los efectos antes del entry ⇒ el instrumento mide una pantalla sin datos | sin dueño | cualquier instrumento que navegue por recarga |
> | **la entrada a «Tus pedidos» desde Cuenta** (la tabla de B era exacta: ahí no está) | sin asignar | — |
> | `scripts/medir-s100b-d-la-hoja.mjs` **NO CORRE ENTERO** (para en el login) | mío, sin dueño al cerrar | **NO es un gate y no debe leerse como verde** |
>
> ### ⑤bis · 🔴 LA LEY QUE C Y YO ENCONTRAMOS POR SEPARADO — y que no vivía en ningún lado
>
> **Formulación de C, al cerrar:**
> > ***Cuando el instrumento y la pantalla no coinciden, la primera pregunta no
> > es cuál de los dos miente — es si están mirando el mismo objeto.***
>
> **Sus dos mitades, y C tiene razón sobre cuál es peor:**
> - **la de C** — `expo-router` conserva la pantalla anterior **viva en el DOM**
>   (la engañó cuatro veces) ⇒ produce **un número raro, que llama la atención**;
> - **la mía** — toda carga completa corre los efectos **antes** de que el entry
>   inicialice la API ⇒ produce **una pantalla vacía perfectamente plausible**, y
>   su síntoma (*«no encontré el scroller»*) **se lee como defecto de layout**.
>
> **Y es la misma familia que nos mordió a los tres hoy en otras formas:** los 53
> dp de A (*aritmética correcta sobre premisa no medida*) · mi rojo declarado
> sobre un riesgo que era cero · y **el «corte del detalle» que sobrevivió en la
> frase que explicaba su retiro**. *Cuatro caras del mismo animal: el instrumento
> —o la prosa— respondiendo sobre otro objeto.*
>
> ⚠️ **Entra acá porque NO es un duplicado.** Mi pedido sobre la ceremonia sí lo
> era —ya es regla firmada en el registro del gate, y C hizo bien en no copiarlo:
> *un tercer lugar que lo afirme es un lugar más que puede quedar viejo*—. **Esta
> ley no vive en ninguna parte**: cada uno tenía su mitad y la síntesis existía
> solo en un mensaje. *Un aviso que vive solo en un mensaje se pierde con la
> sesión* (L-237).
>
> ### ⑥ LAS PREGUNTAS DEL GATE QUE NINGÚN INSTRUMENTO CONTESTA
> ① el mapa de fondo con la hoja encima, ¿se lee «tu pedido se acerca»? · ② la
> placa manda en su ficha y el código manda en la pantalla, **¿se distinguen?**
> —*el riesgo que este cambio administra es que alguien diga la placa en la
> puerta*— · ③ la canasta, ¿alcanza como acuse? · ④ el código segundo y la
> escalera tercera, ¿es el orden correcto? **Lo movió un argumento, no una
> medición: una línea lo devuelve.** · ⑤ **el CTA de avanzar al pie**, que maté:
> **patrón fuerte en tiendas y NO lo medí en Laika** ⇒ sube al gate como
> **pregunta reversible explícita**.

---

## §0 · 🔴 LOS DOS ENCARGOS QUE LA MEDICIÓN DIO VUELTA

**Ninguno de los dos era un error de quien los escribió: los dos eran
descripciones correctas del SÍNTOMA con el MECANISMO equivocado adentro.**

### ① «Montá `PantallaConPie`: el pie tapa el LLEGA ENTRE» — la cura era de otro defecto

Medido en la fuente de mis dos pantallas: **no tienen pie fijo.** Cero
`position:'absolute'`; en `en-camino` el único `Boton` vive adentro del
`accion={}` del `EstadoVacio` de error, y en el detalle los tres están
adentro de dos `Hoja` (modales), después de que el `ScrollView` cierra.

⇒ **montar la pieza habría sido un no-op que da verde y deja el defecto
vivo** — la propia cabecera de B lo dice: sin `pie` se comporta como un
`ScrollView` común.

**La causa real es aritmética:**

```
status ~48 + Encabezado ~56 + mapa 380 + 20 + escalera vertical ~176 ≈ 680 dp
filo de la barra de tabs (medido por B con aparato)                  =  699 dp
⇒ «LLEGA ENTRE» arrancaba a ~19 dp del borde.
```

**El mapa solo se comía el 54 % del alto**, y lo que quedaba abajo era el
dato por el que la familia abre la pantalla.

> **LA LEY QUE LA MESA SUBIÓ AL CANON, y salió de acá:** *la palabra
> elegida al nombrar un hallazgo carga un diagnóstico.* La captura de B se
> llamaba **`…-eta-CORTADO.png`**, no «tapado» — **el nombre ya traía el
> diagnóstico correcto y la cura mandada era la del otro defecto.** Cuando
> el nombre y la cura no coinciden, gana el nombre y se re-mide.

### ② «"Tus pedidos" no tiene entrada» — la tenía, enterrada

B llegó por deep link y reportó ausencia. Medido: **la entrada EXISTÍA** —
`despensa/index.tsx:760`, la misma `CeldaNavegacion`— **al fondo del
scroll, detrás de la vitrina entera** (hasta 50 productos en grilla de dos
≈ 25 filas).

⇒ *no estaba ausente: estaba enterrada.* **En la práctica es lo mismo; en
la cura no lo es: no había que construirla, había que subirla.** La tabla
de Cuenta de B sí era exacta — ahí sigue sin haber entrada, y queda vivo.

### ③ Y una tercera que era MÍA, del gate

**«Cuatro de seis pedidos no dicen en qué estado están.»** No era la pieza
—la regla de existencia de `EscaleraEstados` es correcta—: era **mi orden
de ramas**. La red que caía a `narrativa_nombre` estaba detrás del brazo
de la promesa, y **la promesa nace con el pedido, antes del pago**:

```
censo v_pedidos_narrativa (17-ago-2026):
  pagando = 4 · con promesa = 4 · con pago confirmado = 0
```

⇒ **la rama era inalcanzable por construcción**, y el pedido **no solo
quedaba mudo: prometía una ventana de entrega sin tener el pago.**

Es la misma clase que S100 curó para el desvío —*prometer una entrega que
ya no va a pasar*— **llegando por el otro lado**. *Curar una y dejar viva
la otra es lo que pasa cuando la regla es un orden de `if` y no un objeto
que se pueda medir.*

> **Y el detalle sufría lo mismo.** Eso **solo aparece midiendo las dos
> superficies** — el hallazgo llegó por la lista. Por eso la decisión salió
> de las pantallas a `portadorDeEstado()`, que es una función con cinco
> brazos y su razón, y las pantallas solo le ponen voz (Ley 3).

---

## §1 · LO ENTREGADO, en orden de commit

| commit | qué |
|---|---|
| `95289817` | **el pedido sin recorrido dice su estado** y deja de prometer · `portadorDeEstado()` + guard con discriminador |
| `925144f7` | **EN CAMINO: el mapa deja de ser banda y pasa a FONDO**, con la hoja encima · el rango preside · **la placa manda** |
| `33fefb2a` | **G-14 la canasta con su contador** · **G-15 «Tus pedidos» sale de abajo de la vitrina** · ☠️ muere la barra fija |

### El guard, y por qué hacía falta uno

`verify-s100b-d-el-estado-se-dice.ts` — **42 casos** (7 narrativas ×
promesa × método, con el `null` del método adentro), **3 brazos**, y su
**discriminador reproduce el orden viejo y exige que falle: 6 fallas.**

**Ni `tsc` ni `verify:diseno` ven este defecto:** los cuatro `if`
compilaban, las cuatro voces existían y el espejo es↔en estaba entero. **El
defecto era el ORDEN de las ramas, que no tiene forma.**

> Y barre `tienePromesa` en **los dos valores a propósito**: con `false`
> el orden viejo y el nuevo **coinciden** ⇒ probar solo ese caso daría
> verde sobre el defecto. *El discriminador solo discrimina con el dato
> real.*

### La placa

Medido por B en aparato: **nombre 24.2 dp · placa 18.8 dp en gris** — el
elemento más chico y más apagado de la ficha, cuando nuestro propio README
dice de Uber *«la ficha de quien llega, con LA PLACA MANDANDO … porque la
placa es lo que se verifica en la calle, no la cara»*. Y **nuestra ficha no
tiene foto** (vive en el bucket de las cédulas), así que el nombre no
alcanza ni para reconocer a nadie. Placa a `datoMd`, nombre a `apoyo`.

> **⚠️ Y NO SUBE MÁS, A PROPÓSITO.** La escala que manda de verdad es
> `CodigoAEscala` (mono `2xl`) — **pero en esa misma hoja vive el CÓDIGO DE
> LA PUERTA en esa escala.** Dos números mono grandes en una pantalla donde
> **uno se DICE y el otro se VERIFICA** es cómo alguien le dice la placa al
> repartidor. *Una jerarquía que se arregla creando una confusión peor no
> está arreglada.*

### La barra del carrito

La canasta va en `accionDer` —el slot que la pieza YA tiene, no un
override— y es **permanente**: antes el único acceso existía solo con
`unidades > 0`, o sea que **un carrito vacío no tenía puerta**. No va en la
barra de tabs **por un dato y no por gusto**: la barra ya tiene sus cuatro
slots y el cuarto es del ciclo del trono.

☠️ **Murió la barra fija «Ver carrito (N)»** — es el «botón de texto» que
G-14 nombra, y con la canasta arriba era **una segunda puerta al mismo
cuarto**. Su muerte paga tres cosas: se va el pie fijo, con él el aire
muerto contra la barra de tabs, y **la vitrina recupera ese alto — que es
lo que G-04 necesita**. De paso muere el `+ 72` que estimaba el alto de ese
pie.

*Lo que se pierde y se declara:* la barra era también el **acuse** de haber
agregado algo. Ese trabajo pasa al contador, que queda siempre a la vista
porque el encabezado no colapsa (B: 156.4 dp). **Si en el gate el acuse no
se siente, el arreglo es dar señal al contador, no resucitar la barra.**

---

## §2 · 🔴 LOS LÍMITES DE PIEZA QUE **NO** FORCÉ, con su razón

**Los tres son casos donde lo correcto no era expresable con lo que hay, y
forzarlo habría producido algo peor que la ausencia.**

| # | qué | por qué no lo hice |
|---|---|---|
| ☠️ ~~**El disco del contador**~~ **— PEDIDO Y CONSTRUIDO** | El badge pide número en **color inverso** sobre acento pleno; `Texto` **no tiene color inverso** y el par `accent.cta`/`accent.ctaTexto` vivía encerrado en `Boton` | *Pintarlo con un color crudo habría sido **inventar contraste sin medirlo**, en la pieza más pública de la tienda.* **Se pidió la pieza y por eso existe** (`GlifoConContador`, reusando el par del timbre `+` ya gateado ⇒ **WCAG 368/0 sin pares nuevos**). *El freno produjo la pieza; forzarlo habría producido un contraste que ningún gate mide* |
| **La insignia del pedido sin recorrido** | B decidió bien —**`Insignia`, no que `EscaleraEstados` crezca**— pero al montarlo: **`TarjetaPedido` no tiene slot.** No es que `Insignia` quede corta: **la tarjeta no tiene puerta**, y `desvio` no sirve (significa «el camino se interrumpió», y `pagando` no se interrumpió) | Orden de mesa explícita: ***«mientras B decide, no lo inventes»***. Queda el `apoyo`, que **es honesto aunque pese menos** que sus vecinas con escalera |
| ☠️ ~~**Los 53 dp de la barra de tabs**~~ **— MURIÓ MEDIDO, y el no-forzarlo se pagó solo** | A midió sobre la fuente que faltaba una constante de **53 dp** en toda pantalla de tab. Yo lo dejé como **rojo abierto** porque, de ser cierto, **lo tapado en mi hoja era el CÓDIGO DE LA PUERTA** | **B lo midió en aparato: son CERO.** El `ScrollView` de una pantalla de tab tiene bounds `y=[254, 1966]` ⇒ viewport hasta **699.0 dp = el filo exacto de la barra**: *el navegador ya reserva ese alto.* **La aritmética era correcta y la premisa no** — lo que A había advertido de su propio número. ⇒ **Si lo hubiera compensado sumando 53, habría metido el defecto que no existía.** Y de la medición salió la cura contraria: **la barra ya absorbió el `insets.bottom`, así que sumarlo en mi hoja lo contaba dos veces** — el aire muerto de G-12. **Se sacó** (`ebfbfe04`) |

---

## §3 · 📥 LO QUE LAS OTRAS PISTAS ME CORRIGIERON — y una es sobre mí

- **🔴 B · MI ADVERTENCIA SOBRE G-15 ERA FALSA, y la midió él.** Yo avisé
  que agrandar el nodo empujaría el rango fuera de cuadro. **Medido contra
  `origin/main`: `PUNTO = 10` en las líneas 375-376 y 429-430 (la vertical
  del detalle) y `NODO = 32` solo en la 312 (la tira compacta).** Subirlo
  **no toca el detalle**. *Mi aritmética del corte era correcta y le
  atribuí el empuje a la pieza equivocada* — el mismo error que le señalé a
  la mesa, cometido por mí un lote después.
- **B · el hallazgo que mi pregunta equivocada destapó igual:** la escalera
  vertical del detalle **no tiene glifo y su punto es de 10 dp**, y G-15
  dice *«el círculo de cada nodo CON SU GLIFO»*. **B lo rotuló
  `[IMPRESIÓN]` y no lo adivinó**: esa frase no puede describir la vertical
  porque ahí no hay glifos ⇒ el founder describía **la lista**. **Va a la
  mesa como pregunta, no como trabajo.**
- **A · los 53 dp** (arriba). Su propio pedido lo declara como aritmética
  sobre la fuente y **no como medición en aparato**, que es lo que lo hace
  utilizable sin ser peligroso.

---

## §4 · 🔴 LO QUE NO SE PUDO GATEAR, Y NO ES POR FALTA DE TRABAJO

**«ENTREGADO» NO TIENE SUJETO EN LA CUENTA DEL FOUNDER.**

Medido: los **2** pedidos `entregado` de la base son de
`demo-prestador@epetplace.dev`. La cuenta del gate (`guillo381+8`) tiene
**17 pedidos y ninguno entregado**.

⇒ **la ceremonia de la entrega que S100 construyó (`897e6f19`) nunca pudo
mirarse**, y con ella el tercer acto del sedimento. *No es que esté sin
verificar: es que no hay nada que abrir.*

**No moví dato vivo para forzarlo** (orden de mesa, y es uno de los cinco
frenos). **Pedido a A: siembra de un pedido entregado en la familia del
founder**, con la mascota atada para que el tercer acto tenga qué decir.
*Sin eso, la ceremonia entra al gate como una pantalla que nadie puede
alcanzar — que es exactamente cómo se pierde un trabajo terminado.*

---

### 🔴 ADDENDUM · MEDÍ ANTES DE INSISTIR CON LA SIEMBRA, Y APARECIÓ ALGO PEOR

A frenó el pedido de siembra **y tenía razón en algo que a mí me faltó ver**:
lo que yo pedía para «demostrar» el tercer acto era, literalmente,
**escribir historia en la línea de vida de Thor o Zeus** — el acto solo
habla si el evento se depositó de verdad. *No es fixture: es su mascota.*

Antes de insistir fui a medir si el gate podía correrse desde la cuenta
demo, que tiene los dos únicos `entregado`:

```
narrativa = 'entregado'  (los DOS de demo-prestador@epetplace.dev)
  f69620bf…   items = 0   envíos con entregado_en = 0
  3cf70071…   items = 0   envíos con entregado_en = 0
```

**Son cabeceras huérfanas** — la forma exacta que S94 censó (*«`pedidos`
tiene 137 filas con `pedido_items` en CERO»*), reaparecida en dos filas.

⇒ ① **la ceremonia no se puede gatear desde NINGUNA cuenta hoy**: no es que
al founder le falte uno, es que **no existe ninguno completo en la base**.
La siembra queda necesaria **medido, no supuesto**. · ② **H-113 (dueño: el
del motor):** una narrativa llega a `entregado` **con cero ítems y con el
envío sin `entregado_en`**. No lo diagnostico —el motor no es mi
territorio— pero *un pedido entregado sin ítems ni fecha de entrega no
puede ser evidencia de nada*, y la evidencia de entrega es lo que sostiene
una disputa (H-10).

### 🔴 ADDENDUM · EL GESTO DE LA HOJA — acotado, y no lo puedo cerrar yo

B halló que **la banda del pie fijo es zona muerta de gesto**. Mi hoja
nueva es también un `View` absoluto, así que la mesa mandó verificarlo.
**Medido en la fuente, NO es su misma clase:**

- debajo de mi hoja está **el mapa**, y en `vivo` **no consume gestos**
  (`MapaRecorrido.tsx:145-146` → `scrollEnabled={!esVivo}`);
- **adentro** de mi hoja **está el `ScrollView`** — el contenedor no tapa a
  quien debe recibir: lo contiene.

⇒ **`pointerEvents="box-none"` acá NO corresponde**, y ponerlo por analogía
sería *copiar la cura sin la causa*.

**Pero queda un riesgo DISTINTO que no se settlea sin aparato:** la hoja
tiene `maxHeight` y **ninguna altura fija**, y un `ScrollView` necesita
altura **acotada** para scrollear. Si el padre no lo acota, el contenido se
recorta y **no scrollea** — y en pantalla **se ve idéntico a la zona muerta
de B**. Lo que quedaría bajo el recorte es **el código de la puerta**, así
que se trata como rojo hasta que alguien lo mire. **Pedido a B, acotado a
dos gestos** sobre el envío vivo `474e6ff6`.

---

## §4bis · ☠️ EL «CORTE DEL DETALLE DE PEDIDO» — FALSADO Y CERRADO CON SU NÚMERO

La mesa me lo adjudicó como *«hallazgo sin dueño, y nadie lo está mirando»*,
con la orden correcta: **medir antes que curar y no heredar la hipótesis de
nadie.** Medido, **el hallazgo no sobrevive a su propia procedencia.**

### ① DE DÓNDE VENÍA — y su fuente ya lo había retirado

El ítem nació en **H-105** (*«el CTA fijo tapa contenido en CINCO pantallas»*),
cuya tabla listaba **④ el detalle del pedido** y **⑤ en camino** como míos. Yo
lo falsé midiendo la fuente, y **B corrigió su propia lámina**. Su literal, hoy
en `origin/pista-b`:

> *«④ Y ⑤ SALEN DE ESTA TABLA — corrección de la pista D, verificada. Listé
> aquí el detalle del pedido y en camino. D midió la fuente: ninguno de los dos
> tiene pie fijo … Montar `PantallaConPie` ahí sería un no-op.»*

Y su tabla vigente de tapados tiene **tres**, ninguna mía: **ficha de producto
(C) · carrito (A) · dirección y entrega (A)**.

🔴 **Lo que pasó después es lo interesante:** B escribió *«la causa real ahí es
ARITMÉTICA»* refiriéndose al **par ④/⑤**, y la aritmética que dio es **solo la
de «en camino»** (mapa 380 + escalera ≈ 680 contra 699). **El detalle del
pedido nunca tuvo síntoma medido propio** — entró a la tabla por la
mis-atribución y salió con ella, pero **la frase que resumía la corrección lo
arrastró como si siguiera vivo**.

> *Un ítem retirado de una tabla puede sobrevivir en la frase que explica el
> retiro.* Es la familia del día: **el instrumento —o la prosa— respondiendo
> sobre otra cosa.**

### ② LA PANTALLA, MEDIDA POR SÍ MISMA — no hay de dónde salga un corte

- **Cero `position:'absolute'`**: no hay pie fijo ni nada que se pinte encima.
  Los `Boton` de las líneas 501/507/536 viven adentro de **dos `Hoja`**
  (modales), después de que el `ScrollView` cierra.
- **El visor está acotado por el navegador**, medido por B: el `ScrollView` de
  una pantalla de tab llega a **699.0 dp**, el filo exacto de la barra.
- ⇒ **sin superficie fija y con el visor acotado, no existe el mecanismo que
  produce un corte.** Lo que quede abajo es *scroll*, no *tapado* — y la
  distinción es de A, confirmada por B: **un `paddingBottom` garantiza
  ALCANZABILIDAD, no NO-SOLAPE.**
- **Y el dato que más importaría si algo se cortara ya está arriba:** el
  **CÓDIGO DE LA PUERTA es el bloque 2 de 6** (tras la escalera y la puerta a
  «en camino»). *El argumento que movió el código en «en camino» acá no aplica:
  ya estaba donde tiene que estar.*

### ③ ⚠️ LO QUE **NO** PUDE HACER, DECLARADO

**No hay aparato de mi lado, y el sustituto tampoco corrió:** el RN-web de mi
worktree **falla ya en el login** (`getClient: initApi() no fue llamado`), así
que no pude poner número propio de píxeles. **La conclusión de arriba se apoya
en la fuente y en las mediciones de B, no en una corrida mía.**

⇒ **Lo que va al gate NO es «¿está cortado?»** —eso está contestado— **sino, si
acaso: ¿la pantalla se siente larga?** Eso es composición, no corte, y lo dice
un ojo.

---

## §4ter · 🔴 EL CHOQUE CON C EN `index.tsx` — servido a la mesa, no resuelto por mí

A lo encontró ensamblando en una rama descartable **antes del GO**, y frenó sin
adjudicar: *«la mano que publica no adjudica contenido ajeno»*. **Cuatro
bloques, y no es textual.**

| # | qué | resolución |
|---|---|---|
| **②** | yo reservaba `insets.bottom + spacing[8]`, C `spacing[8]` pelado | ✅ **CONCEDIDO Y APLICADO** (`763d0951`) — y **en mis tres pantallas**, no solo en la que choca |
| **③** | C apaga la etiqueta del buscador (G-04) · yo subo «Tus pedidos» (G-15) | ✅ **no es elegir: la cura de C SOBREVIVE** — su `etiquetaVisible={false}` está literal en mi versión, dentro de `Encabezado.busqueda` |
| **④** | **C monta `PantallaConPie`; yo maté el pie** | 🔴 **hay que elegir. Voté la mía y declaré qué se pierde** |
| **①** | imports | cae solo con ④ |

### ② · POR QUÉ CONCEDÍ, Y ES SOBRE MÍ

Yo lo defendía como *«aire de cola al final de un scroll»*. **Era una
racionalización:** `spacing[8]` **es** el aire de cola; el `insets.bottom`
encima era **la cara «sobra» del malentendido de los 53 dp** — el mismo que ya
me había hecho sacarlo de `en-camino`. *A lo leyó bien: una línea vieja, no una
posición.* **Se unificó en las tres** porque dos reglas para lo mismo en
pantallas vecinas es lo que esta sesión viene pagando toda la vuelta.

### ③ · LA CURA DE C NO MUERE, Y SE DICE EN VEZ DE DEJARLA MORIR

Mi `3cc0189d` sacó el `Campo` del cuerpo, así que **parecía** que la cura de C
quedaba sin objeto. **No queda: queda aplicada al mismo `Campo`, en otro
lugar.** Y **el slot de B nació de la medición de C** —su cabecera la cita—.
*No es la cura de C contra la mía: es la de C, más lo que B construyó con su
número.*

### ④ · EL PIE — LO ÚNICO QUE ES JUICIO Y NO MEDICIÓN

**El argumento de C no es «tiene que haber pie»: es «la reserva se DERIVA, no
se estima»** (la cura del `+72`), y coincido entero. La diferencia es anterior:
**maté el pie**, y sin pie no hay reserva que derivar — *la misma forma que A ya
me aceptó en H-105.*

**Lo que se pierde con la mía, declarado como A pidió:**
- **el ACUSE** de haber agregado algo ⇒ pasa al contador, siempre a la vista
  porque el encabezado no colapsa (B: 156.4 dp). *Si en el gate no se siente,
  la cura es dar señal al contador — no resucitar la barra.*
- **el CTA de avanzar al pie**, patrón fuerte en tiendas — **y NO lo medí en
  Laika.** *Es lo único de esta decisión que es juicio y no medición: va al
  gate como reversible.*
- **`PantallaConPie` queda sin consumidor acá** (vive en la ficha de C). *Una
  pieza correcta puede quedar sin uso porque el problema que curaba se
  disolvió; eso no la invalida.*

⚠️ **Y el aviso de orden, de A y reforzado por mí:** las **capturas de C
«regeneradas con el estado vigente»** están hechas contra SU versión. Si entra
la mía, **se miran DESPUÉS del merge** — *poner la imagen equivocada como vara
es el modo de falla que esta sesión pasó ocho gates aprendiendo.*

---

## §4quater · 🔴 R53 DISPARA SOBRE MI HOJA, Y BLOQUEA EL PUBLISH

A lo encontró ensamblando: **`R53: 1 pantalla NUEVA dibuja un pie fijo a mano`**
→ `despensa/en-camino/[pedidoId].tsx`. Es **nueva** porque la rehice en esta
vuelta: antes esa pantalla no tenía ningún `absolute`.

**Leí la regla entera en `origin/pista-b`. Su detector:**

```js
const aMano = /position:\s*'absolute'[\s\S]{0,160}?bottom:\s*0\b/.test(limpio)
if (/\bPantallaConPie\b/.test(limpio)) continue
```

⇒ **mi hoja calza el patrón y NO tiene el defecto.** El defecto que R53
persigue está en su propio mensaje: *«un pie que el consumidor posiciona es un
pie cuyo alto el consumidor tiene que ESTIMAR»*. **Acá nadie estima nada:** la
hoja se dimensiona por su contenido, topa contra un `maxHeight` **derivado del
lienzo medido**, y **el scroll vive adentro de ella**. Debajo hay **un mapa de
alto fijo, no contenido que scrollea** ⇒ nada que reservar y nada que tapar.

**Y montar `PantallaConPie` ahí sería una REGRESIÓN**, no un cumplimiento: esa
pieza mete a sus hijos en un `ScrollView` ⇒ devolvería el mapa a ser **la banda
que el gate vio como el rango cortado.**

### ⚠️ LO QUE NO HAY QUE HACER, escrito para que no sea la salida fácil

**Meter mi ruta en `BASELINE_R53` estaría MAL.** Ese baseline significa
*«pantallas que todavía tienen el defecto y deben migrar»* —su propio `info` lo
dice: *«la migración es de C/A/D»*—. Mi ruta no debe nada. **Registraría una
deuda falsa y dejaría lugar para que un pie de verdad se esconda dentro del
número.** Es el intercambio silencioso que A ya rechazó con `SALTAR_GATE`, por
la puerta de al lado.

### ✅ RESUELTO — pero NO con mi propuesta, y eso importa

**La mesa adjudicó el escape POR DECLARACIÓN (patrón R45/R46) y B lo
construyó.** Mi línea va en mi archivo —*la forma es de B, el motivo es de la
pantalla*— y **B hizo bien en no escribirla**: una razón escrita por quien no
compuso la pantalla envejece mal sin que nadie lo note.

**Verificado contra la regla REAL de B, no contra mi lectura de ella:**

```
R53 · 10 con pie fijo a mano · 1 declarado(s) ([pedidoId].tsx) · baseline 11
verify:diseno — VERDE (46 reglas)
```

**El baseline de 11 no se tocó.** Y R53 baja de 11 a 10 **por curación, no por
escondite**: el que salió es `despensa/index.tsx`, donde maté la barra fija.

⏪ **Mi propuesta de anatomía NO se construyó, por orden de mesa, y queda como
deuda declarada:** el día que R53 dispare sobre una SEGUNDA hoja con scroll
propio, se afina la anatomía en vez de sumar declaraciones. *Que esté escrito
evita que la tercera declaración parezca normal.*

### 🔴 Y UN ERROR MÍO EN ESTE MISMO ÍTEM, QUE NO SE TAPA

**Escalé un bloqueo con un SHA vencido.** B había pusheado después de
despedirse (`3471dc17` → `99debdd0`) y yo reporté *«no puede contestar»* **sobre
un dato que no re-medí antes de usarlo**. Es **L-166** en su forma más barata
—*todo dato vivo se lee al momento de usarlo, jamás de un reporte anterior*— y
el reporte anterior era **el mensaje de despedida de B**, que describía el mundo
en el instante en que se escribió.
*Agravante: escalar un bloqueo falso es más caro que no escalar, porque mueve a
la mesa.* **Es la segunda vez en la sesión que me pasa con la misma clase** (la
primera fue atribuirle a la pieza equivocada el empuje del nodo).

### 📐 LA PROPUESTA QUE QUEDÓ COMO DEUDA — estructural, no una lista blanca

> **R53 no debería disparar cuando el bloque absoluto CONTIENE un
> `ScrollView`.** Si adentro del absoluto hay un scroller, **eso no es un pie
> sobre el contenido: ES la superficie de contenido.** Un pie de los que R53
> caza nunca lleva un scroller adentro: lleva botones.

Mantiene la regla dura y **sin excepciones nominales** —que es por lo que B la
hizo por RUTA— y no le abre un escape que después tape un pie real.

### 🔴 Y EL HECHO QUE LO BLOQUEA: **B CERRÓ Y NO PUEDE CONTESTAR**

A dejó el ensamblado *«en pausa esperando a B»*, y B se despidió con *«no voy a
poder contestar más»* (`origin/pista-b` `3471dc17`). ⇒ **una pausa que espera a
alguien que no va a volver deja de ser una pausa y pasa a ser un bloqueo
silencioso.** Avisado a A; **la mesa adjudica quién mueve R53.**

⚠️ **Y una advertencia sobre mí mismo:** `verify-diseno.mjs` **no es mi
territorio** y no la toco por mi cuenta. Si me la adjudican, va **con su
discriminador** —una prueba en rojo que muestre que un pie de verdad SIGUE
cazándose después del cambio—, porque *aflojar una regla sin probar que sigue
mordiendo es cómo un lint se vuelve decorativo.*

---

## §5 · LA COLA VIVA — con su próximo paso

| # | ítem | próximo paso · bloqueo |
|---|---|---|
| ① | ✅ **La insignia del pedido sin recorrido — MONTADA** (`5bc2e6fc`) | B entregó `TarjetaPedido.estado` con la forma CERRADA (`{etiqueta, tono}`) y no un slot libre — *un slot libre entrega la forma, y ahí cada pantalla resuelve «el estado de un pedido» a su manera*. **El invariante «si hay pasos, no pases estado» se cumple por construcción** y lo vigila el guard. **El detalle usa la misma forma**: quien vio la insignia en su fila encuentra la misma figura al abrir |
| ② | ✅ **El disco del contador — MONTADO** (`5bc2e6fc`) | Nació `GlifoConContador` **porque se pidió la pieza en vez de inventarla**. Reusa el par del timbre `+` de `TarjetaProducto`, ya gateado ⇒ **WCAG 368/0 sin pares nuevos**. ☠️ **Y el límite que declaré ya murió:** la pieza fijaba `accessible` + `role="image"` adentro ⇒ anidada en un tocable quedaban **dos nodos accesibles**. Dejé la voz en el `Pressable` —*el que tiene que estar nombrado es el que se toca*— y **lo declaré en vez de esconderlo**. B lo cerró con una **unión discriminada y no un flag**: con un boolean opcional seguían siendo expresables **los dos** estados malos (*suelta y sin nombre* · *anidada con nombre*); con la unión **ninguno compila**. Hoy va `dentroDeTocable` y sin `etiqueta` (`ff652458`) |
| ③ | ☠️ **Los 53 dp — CERRADO POR MEDICIÓN**, no eran | B en aparato: el navegador ya reserva el alto. **No hace falta `useAltoBarraTabs()` para mi caso.** Lo que sí quedó es la cura inversa: el inset contado dos veces, ya sacado |
| ④ | ✅ **La ceremonia de entrega — DESBLOQUEADA** | **El founder firmó y A corrió la siembra.** Sujeto: pedido `21fb1284-032c-41eb-8aa2-a8c9fdb15544`, familia del founder, **DOS ítems con el par que discrimina** — alimento→**Thor** (`entra_al_expediente = true`) y higiene→**Jack** (`false`). ⇒ **por primera vez el tercer acto tiene un caso donde puede equivocarse.** Verificado contra la base que **no se equivoca**: lee `sedimentado` y jamás `destino`, así que dirá «Quedó en la historia de Thor» en singular. **Falta el ojo del founder, no el dato** |
| ⑤ | **¿G-15 alcanza también a la escalera del DETALLE?** | **Del founder.** Si sí, es anatomía nueva y es de B. Hoy: punto de 10 dp sin glifo |
| ⑥ | **El gate del glifo `carrito`** | **Del founder** (§2.9: verlo a su tamaño y a 21 px). Montado con su firma declarada como faltante |
| ⑦ | **La entrada a «Tus pedidos» desde Cuenta** | Sin dueño asignado. La tabla de B es exacta: ahí no está |

---

### ✅ ADDENDUM · EL DATO DE APARATO QUE B **SÍ** PUDO DAR, y el que frenó

B **se negó a probar mi hoja nueva y frenó bien**: el teléfono corre el OTA de
S100 y mi pantalla **no está publicada** ⇒ un verde suyo sería *un instrumento
respondiendo sobre otro objeto* (L-235), y encima sobre un rojo.

**Pero midió lo que sí existe hoy:** «en camino» **scrollea con un swipe que
arranca abajo** — desplazamiento real de **1059 px** (`Confirmado` de
`y=[1379,1447]` a `y=[320,388]`). ⇒ **el mapa no se come el gesto**, que es
exactamente lo que `scrollEnabled={!esVivo}` predice. *Mi razón para NO copiar
su `box-none` deja de apoyarse solo en la fuente y pasa a tener evidencia.*

**Y el orden de la hoja cambió por un argumento suyo, rotulado `[IMPRESIÓN]` y
tomado por mí:** el **código de la puerta sube de cuarto a segundo**, porque *es
el único dato de esa pantalla que se usa EN LA PUERTA, con el repartidor delante
y el teléfono en la mano* — todo lo demás se consulta desde el sillón. **Lo que
se necesita bajo presión no puede estar al final de una hoja que hay que
desplegar.** Es un reordenamiento, no una pieza: **reversible en el gate**.

---

### 🔴 ADDENDUM · EL INSTRUMENTO QUE NO CORRIÓ, Y LO QUE ENCONTRÓ IGUAL

C ofreció su aparato de RN-web y lo adapté para cerrar **la mitad de layout**
de mi rojo (¿la hoja acota su `ScrollView`, o recorta?). **No lo pasé del
login** y se commiteó **con su estado en la puerta** (`0201b3e5`), jamás como
un gate.

**Pero encontró algo que no es de ninguna pantalla:**

> **Toda navegación por RECARGA COMPLETA hacia una ruta de la app hace que los
> efectos de la pantalla corran ANTES de que el entry inicialice la API** ⇒ la
> pantalla monta, pide datos y revienta (`getClient: initApi() no fue
> llamado`). **Pasa igual en `/despensa`, que no es mía.**

⇒ **no es de una pantalla: es del MÉTODO DE MEDICIÓN.** Y su síntoma es *«no
encontré el scroller»*, **que se lee como un defecto de layout** — o sea que un
instrumento así puede reportar un rojo de forma sobre una pantalla que **nunca
tuvo datos**. *Un rojo del instrumento que significa «no pude medir» no se lee
como «está roto».* Avisado a C y a A.

**Y la primera corrida tuvo además un error mío que vale anotar:** pasé un id
de **envío** donde la ruta pide el de **pedido**. *Dos rojos apilados, ninguno
de la pantalla.*

---

### ✅ ADDENDUM FINAL · EL ARRIBA DE DESPENSA QUEDA EN UNA SOLA FILA

B entregó **`Encabezado.busqueda`** justo antes de cerrar por contexto, y se
consumió acá: **`[isotipo] [buscador] [carrito]`**, que es literalmente la
referencia de Laika. *Una pieza entregada y sin consumidor muere sin que nadie
se entere.*

**La medición que lo justifica es de C y viaja adentro de la pieza:** apilado
debajo, el buscador costaba **76 dp para una caja de texto de 26**. Y **el
título no se apaga: deja de gastar píxeles** —sigue anunciándose al lector en
un nodo sin alto—, patrón ya firmado en `Campo.etiquetaVisible`. *La barra de
tabs ya dice en qué pantalla estás.*

⚠️ **El alto que libera es de la VITRINA, o sea de C (G-04).** Se montó desde
acá porque el encabezado es «Despensa de arriba» —mi territorio— con **C parada
por orden de mesa y B ya cerrada**. Declarado para que no se lea como invasión:
si C repiensa la composición, esto se mueve; **lo que no vuelve es el apilado.**

⇒ **Con esto el arriba de Despensa resuelve las tres cosas del gate en una
fila:** el carrito con su disco (**G-14**) · el buscador sin su costo · y «Tus
pedidos» alcanzable sin cruzar la vitrina (**G-15**).

---

## §6 · LAS TRES PREGUNTAS DEL GATE — ninguna la contesta un instrumento

> ① **El mapa ahora es fondo y la hoja se apoya encima: ¿se lee «tu pedido
> se acerca»?** Y con el piso del mapa en 40 %, ¿el mapa preside o la hoja
> se lo come?
> ② **La placa manda dentro de su ficha y el código sigue mandando en la
> pantalla: ¿se distinguen?** *El riesgo que este cambio administra es que
> alguien diga la placa en la puerta en vez del código.*
> ③ **La canasta con su disco: ¿alcanza como acuse** de haber agregado algo,
> ahora que la barra de abajo no está? *Si no, la cura es dar señal al
> contador — no resucitar la barra.*
> ④ **El código segundo y la escalera tercera: ¿es el orden correcto?** Lo
> movió un argumento de uso, no un ojo. **Una línea lo devuelve.**

---

## §7 · LO QUE ESTE TRAMO DEJA COMO MÉTODO

① **El nombre de un hallazgo carga un diagnóstico** — `…-CORTADO.png` no
es `…-tapado.png`, y la diferencia era la cura entera. **Ya está al canon.**
② **Un comentario que declara una intención no la implementa.** La cabecera
de S100 decía *«el mapa preside y es FONDO»* y el mapa era un bloque de 380
dp adentro de un `ScrollView`. *La prosa del código envejece igual que la
del canon, y con menos testigos.*
③ **Una rama de seguridad que el DATO vuelve inalcanzable es peor que no
tenerla:** dice que el caso está cubierto. La mía llevaba una sesión entera
escrita, comentada y muerta.
④ **Le señalé a la mesa un error de atribución de mecanismo y cometí el
mismo un lote después** (G-15 y el nodo). *Ver la clase de error no vacuna
contra ella; medir, sí.*
⑤ **DECLARAR UN RIESGO EN VEZ DE COMPENSARLO SE PAGÓ SOLO, Y EN HORAS.** Los
53 dp parecían un faltante y eran cero. *Si los hubiera sumado «por las
dudas», habría introducido el defecto que creía estar evitando* — y su
número habría quedado ahí, plausible, sin nadie que lo re-midiera. **Y la
advertencia falsa no se borró: se contó**, porque un rojo muerto que queda
escrito es letra que sigue diciendo lo contrario.
⑥ **Declarar un límite de pieza en vez de esconderlo lo hace curable.** Las
dos veces —el disco y la voz duplicada— *la declaración fue lo que produjo la
pieza*; y la segunda B la cerró **volviendo inexpresables los dos** estados
malos, no solo el mío. *Un flag opcional habría curado mi caso y dejado vivo
el inverso.*
⑦ 🔴 **UN ÍTEM RETIRADO DE UNA TABLA PUEDE SOBREVIVIR EN LA FRASE QUE
EXPLICA EL RETIRO.** B corrigió H-105 y sacó mi detalle de la lista; **el
resumen de esa corrección lo arrastró** («la causa real ahí es aritmética»),
y así llegó a la mesa como un hallazgo vivo sin dueño. *Corregir la tabla no
alcanza: hay que corregir el resumen de la corrección, que es donde el
lector siguiente busca el porqué.* **Depositada por B en su lámina.**
⑧ **`paddingBottom` garantiza ALCANZABILIDAD, no NO-SOLAPE** (de A,
confirmado por B en aparato). *Son dos propiedades distintas y cambian el
remedio:* contra el solape de primera pintura la reserva no puede nada — lo
que lo arregla es **menos contenido arriba**.
