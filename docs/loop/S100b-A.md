# S100b-A — CARRITO · «¿PARA QUIÉN ES?» · CHECKOUT · RESUMEN

---

# 🔴 HANDOFF DE RELEVO — leé esto antes que nada

> 🔴 **EL SHA DE ESTA RAMA NO SE ESCRIBE ACÁ: SE MIDE.** `git ls-remote origin pista-a`.
> *Un commit que registra su propio SHA lo cambia al registrarlo* — el número quedaría vencido desde su
> primer segundo. Misma cura que el canon usa para el contador de migraciones: **declarar el COMANDO,
> jamás el número.**
>
> **Escrito con el repo a la vista.** Cada número de acá se midió corriendo su comando **el 18-ago-2026**,
> después de publicar. *Lo derivado decae mientras el objeto no* — si leés esto más tarde, re-medí.

## ⓪ DÓNDE QUEDA TODO — con su comando

| | |
|---|---|
| `origin/pista-a` | `git ls-remote origin pista-a` · **contiene `origin/main`** · árbol en **0** |
| `origin/main` | `git ls-remote origin main` · **fui la mano publicadora de S100b** |
| OTA cliente | `cd apps/cliente && npx eas-cli update:list --branch preview --limit 1 --json` |
| OTA prestador | `cd apps/prestador && npx eas-cli update:list --branch preview --limit 1 --json` |
| ancla de una OTA | `update:view <id> --json` → `gitCommitHash` — **jamás del texto del mensaje** |

**Al publicar (18-ago): las dos OTA anclaron al MISMO commit, árbol limpio, sin asterisco.** cliente
runtime **1.0.3** · prestador **1.0.5**. Los dos groups se distinguen desde el primer carácter (no aplica
la trampa de D-785 esta vez).

## ① LO HECHO — mis ocho hallazgos, siete cerrados

| # | qué | dónde |
|---|---|---|
| **G-16** | «Programar otra fecha» **fuera**, con lápida. Murieron montaje, `CampoFecha`, estado y **las 4 voces en los 2 idiomas** | `checkout.tsx` · lápida ~línea 700 |
| **G-03** | destino filtrado por especie: el caso del gate pasa de **6 destinos a 2** | `lib/despensa/destinos.ts` (69 líneas) |
| **G-10** | la pregunta del destino se hace **una vez**, no una por producto | `carrito.tsx` |
| **G-08** | el menos en 1 es **papelera**; murió el botón «Quitar» y su voz | `carrito.tsx` |
| **G-09** | la donación como pastilla + su detalle detrás de una «i» | `carrito.tsx`, Hoja al final |
| **G-11** | dirección = línea con chevron · «quién recibe» se **muestra** · «instrucciones» único campo | `checkout.tsx` · Hoja en **1033** |
| **G-12** | cada entrega es **carta** · **un solo sólido** (pagar); volver a editar baja a label | `checkout.tsx` |
| **G-13** | los 2 párrafos detrás de una «i» (**verbatim**) · frecuencias solo con el interruptor encendido | `checkout.tsx` |

**Archivos vivos** (líneas medidas hoy): `carrito.tsx` **403** · `checkout.tsx` **1080** ·
`destinos.ts` **69** · `scripts/verify-s100b-destinos.ts` **142** ·
`supabase/dev/seed-s100b-pedido-entregado.sql` **292**.

## ② 🔴 LO MEDIDO, con su fecha — separado de lo que es impresión

**Todo esto es MEDICIÓN (18-ago-2026), no lectura de código:**

| qué | número | cómo |
|---|---|---|
| `verify:diseno` sobre el ensamblado | **VERDE · 46 reglas** | `node scripts/verify-diseno.mjs` |
| R53 | **7 con pie fijo a mano · 1 declarado** (bajó de 11 **por curación**) | ídem |
| R52 | **0 ofensores · DURA EN 0** | ídem |
| typechecks | **los 4 en exit 0** | `npx tsc --noEmit` en cada uno |
| destinos | **12/12** · con el filtro sacado, **10 de 12 caen** | `npx tsx scripts/verify-s100b-destinos.ts` |
| migraciones | **349 local = 349 remoto · 0 solo-local · 0 solo-remoto** | `supabase migration list --linked` |
| siembra `21fb1284` | `entregado` · **2 ítems** · $28.21 · envío con estampas · código `7361` | consulta a la base |
| **el par que decide** | alimento→**Thor** `true` ⇒ **1 depósito** · higiene→**Jack** `false` ⇒ **0** | `evento_producto_asignacion` |
| envío `474e6ff6` | `hacia_destino` · **6 puntos** · destino ✓ · placa **`PBA-0142`** | consulta a la base |
| H-113 | **2 casos**, los dos de demo · **0 pasos de historia** contra los 11 de un pedido que sí caminó | censo de 22 pedidos |
| ofertas de `juguete` | **CERO publicadas** en toda la base | censo por `familia_codigo` |

**⚠️ Y ESTO ES IMPRESIÓN, NO MEDICIÓN — rotulado aparte a propósito:**
- **Nada de mi territorio se vio en un teléfono.** Mis verdes son typecheck, lint, aritmética y consultas.
  **No dicen que se vea bien.**
- **oscuro y memorial: NO medidos**, ni por instrumento ni por ojo.
- la causa del corte en el detalle de pedido: hipótesis de B, `[IMPRESIÓN]`, **sin medir**.

## ③ 🔴 CURAS DE DOS MITADES — quitar una reintroduce el defecto SIN VERLO

**① G-16 = montaje + voces.** Se quitó el control **y sus cuatro claves** en los dos idiomas.
⚠️ **Si alguien repone solo las voces «porque no molestan», deja la mitad del control esperando** — es
exactamente por lo que volvió **cuatro veces**. **R52 vigila el montaje, NO las voces.**

**② G-08 = `onBorrar` + la muerte del botón.** La papelera va **solo en el carrito** (eBay `[SPEC]`: solo
donde el tile desaparece). ⚠️ **Si alguien pasa `onBorrar` en la grilla**, promete un borrado que no
ocurre; **si alguien repone «Quitar» sin quitar `onBorrar`**, vuelven los dos controles para una intención.

**③ G-09 = pastilla + `onExplicarDonacion`.** ⚠️ **Sin el callback la «i» no se dibuja y `detalleDonacion`
queda sin salida** — el texto de §6.4 (jamás entra a un expediente, jamás otorga beneficio) **desaparece
en silencio**. Está cableado en **los DOS montajes** (por-ítem y pregunta única): quitar uno deja la mitad.

**④ G-13 = intención + cadencia.** `cadencia` nace **`null`** a propósito.
⚠️ **Reponer un default de 30 rompe el motor, no la pantalla:** `configurar_recurrencia` **no toma clave de
idempotencia**, así que prender con default y dejar cambiarlo **crea DOS recurrencias**.

**✅ Y LO QUE VUELVE MECÁNICAS A LAS TRES DE ARRIBA — medido sobre `main` puro el 18-ago, DESPUÉS del
push: `verify:diseno` enciende 46 reglas, con R53 y R54 VIVAS.**
*C avisó al cerrar que R53/R54 estaban solo en la rama de B y que las curas de dos mitades dependían de
que alguien leyera un documento. **Su medición era correcta cuando la tomó** —`main` valía `22e5fc3e`— y
**quedó vencida con el push del ensamblado**. Es la misma clase de dato que nos mordió cinco veces esta
vuelta: cierto al escribirse, falso al usarse.* **Hoy las tres tienen guard mecánico en el canon.**

## ④ LO QUE DECIDÍ **NO** HACER, con su razón

*Un camino descartado sin su razón se vuelve a intentar.*

- **NO curé el faltante de 53 dp de la barra de tabs.** Mi aritmética era limpia y **la premisa era falsa**:
  B midió que el navegador **ya acota** (`ScrollView` de una pantalla de tab termina en **699.0 dp**).
  **Curarlo habría metido 53 dp de hueco muerto en tres pantallas.** ☠️ **El pedido de exportar el alto de
  la barra queda RETIRADO.**
- **NO monté `PantallaConPie` en el detalle de pedido.** No tiene pie fijo ⇒ la pieza no reservaría nada.
  *Montarla donde no hay pie es verde y no cura nada.*
- **NO clone `ControlTelefono`** (D-498: la casa tiene UNA). Por eso G-11 quedó sin indicativo.
- **NO bypasseé R53 con `SALTAR_GATE`**, por dos razones: saltar un gate para publicar es lo que el gate
  impide, **y habría metido el pie nuevo de D dentro del baseline de 11** — el intercambio silencioso que
  el baseline-por-ruta existe para evitar.
- **NO adjudiqué el choque C↔D** ni toqué R53 (regla de B). *La mano que publica no decide contenido ajeno.*
- **NO curé H-113**: orden era medir. Y **no es alcanzable por el camino real**.
- **NO sembré un juguete inventado** para calzar con el pedido de D. *Habría fabricado la evidencia que la
  siembra viene a dar.*

## ⑤ DEUDAS, CON DUEÑO Y DISPARO

| deuda | dueño | disparo |
|---|---|---|
| **G-11 · selector de indicativo** — `ControlTelefono` sigue en `apps/prestador`; hueco declarado en `checkout.tsx:1022` | **B / quien tome `packages/ui`** | **después del gate** — diferido POR DECISIÓN de mesa, no olvido |
| **el corte del detalle de pedido** — **no era la barra de tabs**; causa sin medir | **sin dueño asignado** | cuando alguien con aparato pase por esa pantalla |
| **H-113** — 2 cabeceras `entregado` sin ítems, residuo de test E2E de S96, hallables por `clave_idempotencia LIKE '%__e2e_real_g4%'` | limpieza de residuo | ⚠️ **primero medir si el test que las produjo sigue vivo** — si sigue, limpiar es barrer sin cerrar la canilla |
| **catálogo: 0 ofertas de `juguete`** y ninguna familia `false` aplicable a perro | **founder / catálogo** | antes de necesitar otro par demostrativo |
| **catálogo: 46 % de tarjetas sin foto** (medición de C) | **founder / catálogo** | ninguna cura de layout lo tapa |
| **la siembra `21fb1284`** — su retiro **está probado** y vive en la cabecera de su `.sql` | quien limpie | cuando la ceremonia ya se haya mirado |

## ⑥ LO QUE UN INSTRUMENTO DE ACÁ **NO** PUEDE DECIR

*Se escribe para que nadie lea mis verdes de más.* `verify:diseno` mide **forma**; los typechecks miden
**tipos**; mis 12 asserts miden **una función pura**. **Ninguno mide jerarquía visual.**
La ley de la vuelta, y la frase es de C: **sus cuatro números decían que la ficha estaba curada — y la
ficha no tenía precio.** *Los instrumentos declaran ausencia de defecto conocido, jamás presencia de
calidad.*

---

> Territorio: `apps/cliente` — carrito, destino del ítem, dirección y entrega, resumen, pago, listo.
> Worktree `e-petplace-s100-a`, rama `pista-a`. Hallazgos del gate: **G-03 · G-08 · G-10 · G-11 · G-12 ·
> G-13**, más **G-16**, que no estaba en mi reparto y sí en mi archivo (ver §L1).

## ⓪ LÍNEA BASE, MEDIDA ANTES DE TOCAR

| instrumento | al abrir |
|---|---|
| `verify:diseno` | ✅ VERDE · 44 reglas · **R52 = 1 ofensor** (baseline 1, solo-baja, «dueño A») |
| `tsc --noEmit` (cliente) | ✅ exit 0 |
| `PantallaConPie` en `main` | ❌ **no estaba** — L1 arrancó bloqueada |

---

# L1 · G-16, G-03 Y G-10 — commit `9e438c1a`

## 🔴 G-16 · EL CONTROL QUE VOLVIÓ TRES VECES

**No estaba en mi lista y es mío**: el acta §4 no se lo asignó a nadie porque §3 lo trató como firma de
mesa, pero `onProgramarOtra` vivía en **mi** `checkout.tsx`, y R52 nombra a A como dueño.

Murió el montaje, el `CampoFecha`, el estado `mostrarCalendario` y **las cuatro voces en los dos
idiomas**. Lo segundo importa tanto como lo primero: *una voz sin consumidor es la mitad del control
esperando que alguien la vuelva a usar* — y éste ya volvió tres veces.

**Lo que NO se barrió, escrito en el código para que nadie lo barra después:** el cupo por día futuro y
el `p_fecha_programada` de `calcular_promesa_despensa`. Se quitó la puerta, no el motor. `SelectorVentana`
conserva su prop opcional intacta por lo mismo.

**R52 pasa de baseline 1 a DURA EN 0.** La tolerancia no era una excepción de diseño: era la orden de
mesa de «no curar nada», y esa razón se agotó cuando el control salió. *Lo que muere es la tolerancia,
jamás el diente.*

| | ofensores | veredicto |
|---|---|---|
| antes | 1 (`checkout.tsx`) | verde por baseline |
| después | **0** | verde |
| **discriminador** — reintroduje `onProgramarOtra` | 1 | 🔴 **ROJO** |

*Una regla en 0 que no se puede poner en rojo es decoración; ésta se probó en las dos direcciones.*

## 🔴 G-03 · EL DESTINO OFRECÍA ESPECIES IMPOSIBLES

El producto ya declaraba `especies_aplicables`; el selector no lo miraba. **El caso exacto del gate
—alimento de perro— pasa de 6 destinos a 2.**

**La decisión que decide el caso:** `especies_aplicables: []` = **«no se declaró»**, jamás «ninguna».
Una cama o un juguete no declaran especie y sirven para toda la casa. *Leerlo al revés habría vaciado el
selector de media vitrina, y el modo de falla sería mudo: «¿para quién es?» sin una sola cara y ningún
error en ningún lado.*

## 🔴 G-10 · LA PREGUNTA SE HACE UNA VEZ

**La tensión, porque toca letra firmada:** §4 dice *«la app nunca adivina de quién es una compra»* y la
cabecera de `SelectorDestinoItem` prohíbe preseleccionar. Heredar una respuesta sería exactamente eso.

**La salida: no se hereda una respuesta que nadie dio — se deja de REPETIR la pregunta.** Nada nace
elegido. Lo que cambia es que la respuesta de la familia vale para la compra hasta que ella decida
repartirla.

**Y cuándo no se puede preguntar una sola vez SE DERIVA, no se decide:** con comida de perro y comida de
ave en el mismo carrito ninguna respuesta única sería verdad, así que la pantalla se abre repartida sola.
El reparto se OFRECE solo con más de un producto — ofrecer «repartir» un ítem es un control que no puede
hacer nada.

## EL INSTRUMENTO, Y POR QUÉ ES UN ARCHIVO Y NO DIEZ LÍNEAS

La regla vive en `apps/cliente/src/lib/despensa/destinos.ts` y la pantalla la consume. **El test importa
LA función que la pantalla usa.** *Un instrumento que re-escribe el filtro adentro del test mide su
propio eco: probaría que sé escribir dos veces lo mismo, no que la pantalla filtra.*

`scripts/verify-s100b-destinos.ts` — **12/12 verde**, y con el filtro sacado **10 de 12 en rojo**.

---

# L2 · G-11, G-12 Y G-13 — commit `263bed5a`

## G-11 · LA ENTREGA SE LEE, NO SE EDITA

· la dirección deja de tener botón propio y pasa a ser **la línea con chevron** (`CeldaNavegacion`) —
  eran dos bloques para una cosa. E14 lo autoriza: no navega, pero **abre el formulario que la resuelve**.
· **«quién recibe» y el teléfono se MUESTRAN.** Eran dos campos de edición abiertos en la pantalla donde
  la familia revisa antes de pagar. *Un campo abierto invita a escribir; acá el trabajo es leer.* Se
  siguen pudiendo cambiar, por un toque explícito, en su Hoja.
· **cuando el dato falta, la línea lo dice y el toque lo resuelve.** Mostrar fijo un vacío sería un
  callejón — peor que el campo que se sacó.
· «Instrucciones de entrega» queda como **el único campo**, y es justo el que el CTA tapaba (solape ③).
· el placeholder pasa a enseñar el **formato** (`+593 99 123 4567`, N11′) y el teléfono dice para qué se
  pide: el benchmark mide que **49 %** de los sitios no lo explica y **más del 70 %** de la gente es
  reticente a darlo.

⚠️ **HUECO DECLARADO — el selector de indicativo NO entra.** `ControlTelefono` (selector + campo con UN
pie, porque lo que se valida es el E.164 que forman juntos) vive en `apps/prestador` y **no está en
`packages/ui`**. Pedido a B (precedente D-498). **No se clona de este lado**: un segundo teléfono con su
propia idea de E.164 es lo que esa deuda cerró.
*Y lo que NO es defecto, medido por B: un `+57` sobre una dirección de Quito está bien — P21.*

## G-12 · CADA ENTREGA ES UNA CARTA, Y HAY UN SOLO SÓLIDO

Sin superficie, N entregas se leen como una lista larga en vez de N cosas separadas — que es justo lo
que la división tiene que comunicar.
Y el resumen tenía **dos sólidos apilados**. La frase de B es la vara: *dos bloques del mismo peso
significan que nadie decidió cuál importa.* Acá importa pagar; volver a editar es la salida, y una salida
no compite con el destino ⇒ baja a label (19.7 / 22c).

## G-13 · LAS FRECUENCIAS NO EXISTEN HASTA QUE EL INTERRUPTOR ESTÁ ENCENDIDO

Los dos párrafos explicativos se van detrás de una «i» — **VERBATIM, no resumidos**: *una condición de
cobro que se resume deja de ser la condición.* Cambia dónde se lee, no qué dice.

**El interruptor pasa a expresar la INTENCIÓN y la cadencia la cierra**, y la razón es del motor:
`configurar_recurrencia` **no toma clave de idempotencia**, así que prender con un default de 30 y dejar
cambiarlo después crearía DOS recurrencias, o exigiría apagar y volver a crear dejando una fila muerta.
Por eso `cadencia` nace `null`: *el 30 era una decisión tomada en nombre de la familia que no quedaba
registrada en ningún lado.* Mientras falta, la pantalla lo dice.

---

# L3 · `PantallaConPie` — commit `038513e6`

**Dos de tres, y la tercera tiene otra causa.**

| pantalla | qué se hizo |
|---|---|
| **carrito** | la cura literal: acá vivía `insets.bottom + spacing[8] + 120`, y ese **120 era la estimación** que tapaba el aviso del total por 9.6 dp. Ya no hay número |
| **checkout** | se monta **igual aunque ya derivaba** su reserva con un `onLayout` propio — para dejar UNA anatomía de pie en la despensa (dos mecanismos divergen) y para meter el pie DENTRO del `EvitaTeclado`, que es la hipótesis viva de su solape ③ |
| **detalle del pedido** | **NO se monta, y se declara por qué**: no tiene pie fijo, así que la pieza no tendría nada que reservar. Montarla ahí sería copiar el gesto sin la causa |

**De paso murió la barra vacía:** con el carrito vacío el pie es `undefined`, la pieza no lo dibuja y no
reserva. Antes pintaba una barra sin botones y le robaba el alto al contenido igual.

## 🔴 EL HALLAZGO DE L3, Y CORRIGE MI PROPIO PROMPT

Mi arranque decía que el hueco muerto de G-12 *«lo cura la pieza de B»*. **La cabecera de la pieza dice
lo contrario y tiene razón: «NO cubre la barra de tabs».** Medí y el defecto es más ancho que G-12:

**`insets.bottom` no es el alto de la barra de tabs, y media casa los usa como si lo fueran.**

· `BarraTabs.tsx:688` → `altoTotal = ALTO_FILA + insets.bottom`, con `ALTO_FILA = 85`
· las pantallas de tab reservan `insets.bottom + spacing[8]`, y `spacing[8] = 32`
· **el `insets.bottom` se cancela de los dos lados ⇒ el faltante es una CONSTANTE de 53 dp**

Eso explica el corte de ④ sin interpretación. **Y la otra cara es el hueco muerto:** donde SÍ hay pie
fijo, el pie suma un inset que la barra ya pintó ⇒ sobra. *Un mismo malentendido produce «falta» donde no
hay pie y «sobra» donde sí lo hay — por eso no se curan por separado.*

**No lo curé**: el `85` vive en la pieza de B, y copiarlo a las pantallas sería volver a poner dos números
que deben coincidir saliendo de dos lugares — el defecto que `PantallaConPie` acaba de desarmar.
⚠️ **Y la cuenta es aritmética sobre la fuente, no medición en aparato**: si react-navigation ya descuenta
algo por su cuenta, el número cambia y mi cuenta no lo vería. Va a B, que tiene el teléfono.

### ☠️ FALSADO EN APARATO POR B — LOS 53 dp SON CERO

**B midió el árbol de una pantalla de tab: el `ScrollView` scrollable tiene bounds `y=[254, 1966]` ⇒ su
viewport termina en 699.0 dp, exactamente el filo de la barra.** El navegador YA acota el contenedor. **No
falta ninguna constante.**

**Mi aritmética era correcta y mi premisa era falsa** — que es literalmente el riesgo que yo mismo había
declarado al pasarlo (*«una aritmética correcta sobre una premisa no medida da un número exacto y
equivocado»*). Lo que salvó el día no fue acertar: fue **no curarlo**. Si hubiera sumado 53 dp «porque la
cuenta cerraba», habría metido un hueco muerto de 53 dp en tres pantallas para arreglar un faltante que no
existe.

**Consecuencias, todas escritas para que nadie las herede al revés:**
- **El pedido a B de exportar el alto de la barra QUEDA RETIRADO.** Exportar un número para curar un
  faltante inexistente es acoplamiento nuevo a cambio de nada. *Un número exportado mal es peor que uno
  tecleado, porque se propaga* (B).
- **El corte del detalle de pedido (④) sigue vivo y su causa es OTRA.** D y yo estábamos los dos mirando
  el lugar equivocado. Hipótesis de B, rotulada `[IMPRESIÓN]` y no medida: el `paddingBottom` del
  `contentContainer` de esa pantalla es corto para su contenido, y es alcanzable scrolleando — o sea la
  misma familia que ③, no una constante faltante.
- **D quedó avisado**, porque había declarado el mismo faltante en su `en-camino` sin compensarlo.

### 🔴 LA DISTINCIÓN QUE RECONCILIÓ LAS DOS MEDICIONES — y cambia el remedio

B midió que `Instrucciones de entrega` **sí** estaba bajo el CTA en reposo (solape 15.3 dp), **sin teclado
y sin scroll**, o sea que mi hipótesis del teclado también era falsa. Pero el checkout **ya derivaba** su
reserva. Las dos mediciones eran ciertas, y lo que las reconcilia es:

> **UN `paddingBottom` AL FINAL DEL SCROLL GARANTIZA ALCANZABILIDAD, NO NO-SOLAPE.**

Reservar el alto del pie asegura que el ÚLTIMO elemento pueda subir por encima del pie si scrolleás. **No**
asegura que un elemento del medio no quede debajo del pie en la primera pintura. **Confirmado por B en
aparato:** al fondo del scroll `Instrucciones` sube a `y=254` y el contenido termina en 1529, muy por
encima del CTA (1701) ⇒ **la reserva estaba haciendo su trabajo; el trabajo no era ése.**

**Y por eso la gravedad de los tres casos es distinta, aunque los tres números se parezcan:**

| caso | scrollea | qué era | gravedad |
|---|---|---|---|
| **ficha** (①, de C) | ❌ **no** | solape = **inalcanzable** | 🔴 real — por eso subió solo |
| **carrito** (②) | ✅ | el elemento era **el último** ⇒ inalcanzable | 🔴 real, y el `120` era la causa |
| **checkout** (③) | ✅ | elemento del **medio** ⇒ solo primera pintura | cosmético |

**El corolario que cambia el remedio: contra el solape de primera pintura la reserva no puede nada — lo
que lo arregla es MENOS CONTENIDO ARRIBA.** Y eso ya pasó, pero **por el motivo equivocado**: G-11 sacó dos
campos de esa pantalla. *Con contenido más largo vuelve.*

---

## ⚠️ UN ERROR MÍO, DECLARADO — LOS DOS PRIMEROS PEDIDOS FUERON A LA SESIÓN EQUIVOCADA

`ListAgents` devuelve nombres (`e-petplace-dd`, `e-petplace-95`, `e-petplace-59`) que **no dicen qué pista
es cada uno**. Elegí uno y le mandé los dos pedidos con el caso. **B era `e-petplace-59`**, y me enteré
porque B me escribió primero.

*El costo real fue chico —B ya estaba haciendo lo que yo iba a pedir— pero pudo no serlo: un pedido con
el caso que nunca llega se lee, desde el otro lado, como que nadie lo pidió.* Reenviado a `e-petplace-59`
condensado.

**Lo que dejo escrito para quien siga:** el canal entre pistas se identifica **antes** de mandar algo que
bloquea, o el pedido viaja por el repo, que es el único canal cuyo destinatario no hay que adivinar.

## LO QUE B AVISÓ Y NO ME MORDIÓ POR SUERTE, NO POR CUIDADO

`PantallaConPie` lleva `pointerEvents="box-none"` **una sola capa**: envolver el pie en un `View` propio
reabre una zona muerta de gesto en el tercio inferior — justo donde se apoya el pulgar. Mecanizado en su
`R54`, que todavía no llegó a `main`.

**Verificado contra mi código: mi pie usa fragmentos y `<Boton>` pelado en las dos pantallas, cero `View`
envolvente.** *Salió bien porque el pie por fase pedía un fragmento, no porque yo supiera de la trampa* —
si el resumen hubiera necesitado un `gap`, lo envolvía y lo rompía sin enterarme.

---

# L4 · G-08 Y G-09 CIERRAN — commit `45bea29d`

Las dos piezas que B entregó, montadas.

**G-08 · el menos en 1 es la papelera, y el botón «Quitar» MURIÓ con él.** Eran dos controles para la
misma intención (stepper 144 dp + botón 89.6 dp, medidos). `onBorrar` se pasa **solo acá**, y el matiz de
eBay es la razón: *la papelera se usa cuando el stepper está pegado a un tile que va a desaparecer.* **En
la grilla no se pasa** — allá bajar de 1 devuelve la tarjeta a su «+», el producto sigue existiendo, y una
papelera prometería un borrado que no ocurre. Sin confirmación: eBay, Baymard y NN/g convergen en acción
inmediata.
**Y con el botón se fue su voz:** `despensa.quitar` quedó sin consumidor en los dos idiomas y se retiró —
misma razón que las cuatro de G-16.

**G-09 · la donación ya es pastilla**, y se cableó `onExplicarDonacion` en **los dos montajes** (el
por-ítem y el de la pregunta única): sin el callback la «i» no se dibuja y el detalle no tendría por dónde
salir. La Hoja la monta la pantalla, no el selector — *un selector que además abre modales empieza a
conocer la navegación de su pantalla* (B). El texto de §6.4 va **verbatim**.

---

## ESTADO DE MIS SEIS HALLAZGOS

| # | estado |
|---|---|
| **G-03** | ✅ cerrado — filtro por especie (mío) + la salida de la donación (pieza de B) |
| **G-08** | ✅ cerrado |
| **G-09** | ✅ cerrado |
| **G-10** | ✅ cerrado |
| **G-11** | 🟡 **dos de tres** — el selector de indicativo queda **DIFERIDO POR DECISIÓN DE MESA, después del gate** (ver abajo). No es deuda olvidada: es orden |
| **G-12** | ✅ cerrado — cartas + un solo sólido. *El hueco muerto no existía* (ver P6 falsado) |
| **G-13** | ✅ cerrado |
| **G-16** | ✅ cerrado, con R52 **DURA EN 0** |

## COMPROMISOS ABIERTOS

**Le debo a B — nada.** **Espero de B (pedidos enviados con el caso):**

| # | qué | estado |
|---|---|---|
| P1 | `onCambiar` emite `null` ⇒ la donación sale | ✅ **entregado** — entró sin tocar una línea mía: `fijarDestino` ya aceptaba null desde S96 |
| P2 | la donación como pastilla | ✅ **entregado** (G-09) |
| P3 | `StepperCantidad.onBorrar` | ✅ **entregado** (G-08) |
| P4 | `ControlTelefono` a `packages/ui` | ☠️ **DIFERIDO POR MESA, después del gate** — ver la nota de abajo. **B no lo hace ahora** |
| P6 | el alto real de la barra de tabs | ☠️ **RETIRADO POR MÍ**: B lo midió y el faltante no existe |

**El pedido P6 lo retiré yo, no lo rechazó B.** Mi premisa cayó en aparato, y *exportar un número para
curar un faltante inexistente es acoplamiento nuevo a cambio de nada*.

## LO QUE NO SE HIZO, SIN MAQUILLAR

- **Nada de esto se vio en un teléfono.** Todo lo de acá es typecheck, lint y aritmética sobre la fuente.
  El aparato lo tiene B — y la ley de esta vuelta es justamente que *un instrumento verde sobre una
  pantalla fea es un instrumento midiendo otra cosa*. **Mis verdes no dicen que se vea bien.**
- **Oscuro y memorial: no medidos**, ni por instrumento ni por ojo.
- El **indicativo** de G-11 no entró (P4) — lo único de mis seis que queda abierto, **y queda abierto POR
  DECISIÓN, no por falta de tiempo**: la mesa difirió la mudanza de `ControlTelefono` a después del gate,
  con su razón — *el selector de indicativo no cambia si la app se ve premium, y todo lo que suma tiempo
  antes del ojo hoy cuesta más de lo que rinde*. **Se escribe acá porque una decisión que no queda escrita
  se vuelve a proponer** — que es exactamente por qué G-16 volvió cuatro veces. Quien retome: no es deuda
  olvidada ni pieza trabada; es orden, y el disparo es el gate.
  Y sigue rigiendo lo otro: **no se clona** (D-498).
- **🟡 DEUDA DE CATÁLOGO, NO DE CÓDIGO — sube al founder:** **no existe una sola oferta de `juguete`
  publicada** en toda la base (medido: 0), y **ninguna familia con `entra_al_expediente = false` tiene
  producto aplicable a perro**. Por eso la siembra usó higiene de gato para el lado que no sedimenta.
  *D pidió un caso que no existía y no se inventó* — pero el hueco es real y no lo arregla ninguna
  pantalla.
- **El detalle del pedido sigue cortado**, y ahora se sabe que **NO** es por la barra de tabs. Causa
  todavía sin medir; hipótesis de B rotulada `[IMPRESIÓN]`: `paddingBottom` corto para su contenido, de la
  misma familia que ③. *Lo declaro sin dueño asignado porque nadie lo midió todavía.*
- **La ceremonia de entrega quedó sembrada pero NO mirada**: el pedido existe y está completo; que se vea
  bien lo dice el founder o D con aparato, no yo.

## ✅ LA SIEMBRA — FRENADA, FIRMADA, Y CORRIDA

**El freno funcionó como debía: no la ejecuté, la elevé, el founder la firmó, y recién ahí corrió.**

**Corrida el 17-ago: pedido `21fb1284-032c-41eb-8aa2-a8c9fdb15544`** — `entregado`, 2 ítems, $28.21, envío
con `entregado_en`, `verificado_en`, foto, «Repartidor de Pruebas» y código de puerta `7361`. Todo por el
camino real (`crear` → `iniciar_pago` → `confirmar_pago` → escalera → `empacar` → `factura` → `despachar`
→ `entregar`), **cero UPDATE a mano sobre `envios`**.

**🔴 EL NÚMERO QUE LA JUSTIFICA — `eventos_expediente = 1` con DOS ítems, los dos con mascota:**

| ítem | destino | `entra_al_expediente` | depósitos reales |
|---|---|---|---|
| alimento (ADULTO SMALL BREED) | **Thor** | `true` | **1** |
| higiene (CANADA LITTER) | **Jack** | `false` | **0** |

*El tercer acto de la ceremonia tiene por primera vez un caso donde puede equivocarse — y no se equivocó.*
Con un solo alimento la pantalla se habría visto igual de bien sin probar nada.

**⚠️ EL PAR SE CORRIGIÓ CONTRA LA BASE:** D pidió un **juguete** para el lado `false`. **No hay una sola
oferta de `juguete` publicada** (medido: 0), y **ninguna familia `false` tiene producto aplicable a perro**
— por eso el lado `false` es higiene de gato → Jack. Cumple la misma función y es real. *Sembrar un
juguete inventado para que calzara con el pedido habría sido fabricar la evidencia que la siembra viene a
dar.*

**Lo que la corrida enseñó de paso, y no estaba en ningún plan:** la escalera real tiene más escalones que
la letra. `preparado` **no existe** en el motor (once estados internos bajo la narrativa `preparando`), el
empaque **exige lote** —*sin lote no se puede responder un retiro de fabricante*— y `empacado →
documentado` **lo da la factura**, no un cambio de estado. **Los tres me rebotaron y los tres tenían
razón.** También rebotó `confirmar_pago_pedido` con sesión de persona (la cura de S95): la siembra entró
por la rama del webhook en vez de saltear el gate. *Saltear un gate de plata para poder mirar una pantalla
es exactamente lo que no se hace.*

**El retiro está PROBADO, no solo escrito:** corrió entero con ROLLBACK contra la siembra viva, dejó
`quedan_pedidos = 0` y `quedan_eventos = 0`, y la siembra quedó intacta. **Falló dos veces antes** —una
columna que no existe y el padre borrado antes que el hijo con FK RESTRICT—, y **ninguna de las dos la
encontró leyendo: las encontró correrlo.** El orden sale del censo de `pg_constraint` (ocho tablas
RESTRICT), no de intuición.

**Lo que el retiro NO deshace, declarado:** los `inventario_movimientos` de tipo `consumo` son un ledger y
no se borran — igual que el ledger de S95 no se corrige borrando filas.

## 🔴 EL FRENO, TAL COMO SE EJECUTÓ (queda por su valor de precedente)

**D pidió una siembra: un pedido en `entregado` dentro de la familia de `guillo381+8@gmail.com`**, con dos
ítems (uno con `entra_al_expediente = true` y otro con `false`) para poder mirar la ceremonia de entrega
que construyó en S100 — hoy **inalcanzable**, porque los dos únicos `entregado` de la base son de la
cuenta demo y la cuenta del gate no tiene ninguno.

**No la ejecuté hasta tener firma.** Dos razones, y la segunda pesa más que la primera:
1. **Es «dato vivo», uno de los cinco frenos del acta §7**, y la DB no está en mi territorio de S100b (sí
   lo estuvo en S100 — de ahí, creo, que el pedido me llegara a mí).
2. **La cuenta es la REAL del founder, no una demo.** Un `entregado` sembrado ahí, si el ítem entra al
   expediente —que es justo lo que el tercer acto quiere demostrar—, **deposita un evento en la línea de
   vida de Thor o Zeus**. *Eso no es fixture: es historia de su mascota.*

**El pedido de D está bien armado y no se rebotó por forma:** el par true/false es lo que separa «el
tercer acto discrimina» de «el tercer acto siempre afirma». **Va al founder con su retiro escrito de
antemano como condición**, y el SQL lo escribe D — la ceremonia es suya y adivinar sus campos es cómo se
siembra un fixture que verifica otra cosa.

## EL DATO DE D SOBRE MIS PANTALLAS — medido, y no se materializa

D avisó que *la promesa de entrega nace antes del pago* (4 de 4 `pagando` con promesa y
`pago_confirmado_en` en NULL) y que si alguna de mis pantallas la muestra sobre un pedido sin pagar,
estaría prometiendo una entrega que todavía no se compró.

**Medido: el único uso de `promesaVentana` vive dentro de `fase === 'armado'`** — antes de crear el
pedido. En `resumen` (pedido `creado`, impago) y en `exito` no se dibuja ninguna ventana. ⇒ **no se
materializa en mi territorio.** Lo que muestro es la estimación previa a comprar, que es la que el
benchmark exige (41 % de los sitios falla por no dar fecha).

**Lo que queda anotado como medición y no como hallazgo:** la voz es declarativa —«Te llega el {{dia}}…»—
antes de que exista compra, y el benchmark advierte que la gente lee la fecha como *«a clear promise or
guarantee»*. **No lo toqué: la voz está firmada en la letra**, y ablandarla es decisión de producto, no de
forma. El fondo —que la promesa nazca antes del pago, y si consume cupo ahí— es del MOTOR y de S101.

---

# H-113 · MEDIDO, NO CURADO (asignado por mesa)

> *«Una narrativa puede llegar a `entregado` con cero ítems y sin `entregado_en`»* — hallazgo de D al
> buscar un sujeto para la ceremonia. **Orden: medir. Con el número se decide si entra antes o después
> del gate.**

## ① CUÁNTOS CASOS — DOS, y ninguno del founder

Censo de los **22 pedidos** de la base, por narrativa:

| narrativa | pedidos | **sin ítems** | sin envío |
|---|---|---|---|
| pagando | 4 | **0** | 4 |
| preparando | 11 | **0** | 9 |
| en_camino | 2 | **0** | 0 |
| **entregado** | 3 | **2** | 2 |
| cancelado | 2 | **0** | 2 |

**La forma «sin ítems» existe SOLO en `entregado`, y solo dos veces.** Cero en las otras cuatro
narrativas. Los dos son de **`demo-prestador@epetplace.dev`**; **la cuenta del founder no tiene ninguno**
⇒ **el gate no los va a ver.**

## ② 🔴 ¿ALCANZABLE POR EL CAMINO REAL? — **NO.** Cuatro evidencias que convergen

| evidencia | los dos huérfanos | mi siembra (que sí caminó) |
|---|---|---|
| **pasos en `pedido_estados`** | **0** | **11** |
| ítems | 0 | 2 |
| envíos | 0 | 1 |
| `created_by_sistema` | **true** | false |
| `clave_idempotencia` | **`…:__e2e_real_g4-…`** | `S100B-SIEMBRA-GATE` |

**El discriminador es `pasos_historia = 0`**, y es el que cierra la pregunta: **el único camino que lleva
un pedido a `entregado` es `_mover_estado_pedido`, y ése escribe SIEMPRE en `pedido_estados`.** Cero pasos
⇒ **el pedido nunca pasó por el motor: lo pusieron en `entregado` con un INSERT directo.**

Y los dos guards que lo vuelven inalcanzable están **leídos del cuerpo de las funciones**, no recordados:
- `crear_pedido_despensa` → `IF jsonb_array_length(p_items) = 0 THEN RAISE 'pedido_sin_items'`
- `entregar_pedido` → `IF v_env.id IS NULL THEN RAISE 'envio_no_existe'`

⇒ **H-113 NO es un agujero del motor: es residuo de un test E2E de S96** (el marcador `__e2e_real_g4` es
inequívoco) **que entró por debajo de las puertas.** Misma familia que los 137 pedidos huérfanos que S94
censó y que las 64 sondas de S92.

## ③ QUÉ VE LA FAMILIA SI OCURRE — y es lo peor del hallazgo

`v_pedidos_narrativa` los devuelve **completos y creíbles**:

> **«Entregado» · `P-20260812-ba6a5d` · $122.00 · terminal**

**Un pedido entregado que cobró $122 y no tiene un solo ítem adentro.** En la lista se ve **normal** —
título, número de orden, precio y estado—; el vacío recién aparece al abrir el detalle. *No es una fila
rota que se nota: es una fila que miente bien.*

## VEREDICTO — NO ENTRA ANTES DEL GATE

Tres razones, en orden de peso: **no es alcanzable por el camino real** (el motor lo rebota por dos
puertas distintas) · **no está en la cuenta del founder**, así que el gate no lo cruza · **no bloquea
nada**.

**Es limpieza de residuo de test, con dueño y disparo**, no cura de motor. Y lleva su advertencia: *un
pedido entregado sin ítems no puede ser evidencia de nada, y la evidencia de entrega es lo que sostiene
una disputa* (H-10). **El día que se limpien, se limpian los dos por `clave_idempotencia LIKE '%__e2e_real_g4%'`,
que es lo que los hace hallables sin depender de fechas.**

⚠️ **Lo que NO medí, declarado:** si el test de S96 que los produjo sigue vivo y los volvería a crear. Eso
es del territorio del motor y no lo abrí — *si el productor sigue corriendo, limpiar los dos es barrer sin
cerrar la canilla.* Es la primera pregunta de quien tome la limpieza.

---

## VERIFICACIÓN AL CIERRE

| instrumento | resultado |
|---|---|
| `verify:diseno` | ✅ **VERDE · 44 reglas** · R52 **0 ofensores, DURA EN 0** |
| `tsc --noEmit` (cliente) | ✅ exit 0 |
| `verify-s100b-destinos.ts` | ✅ **12/12** · rojo producido: 10 caen sin el filtro |
