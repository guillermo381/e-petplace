# S113-D · LOTE 1.2 — `sugerir-raza` y el contenido por raza

**Rama:** `pista/s113-d-1.2` · **base:** `4fda7858` (mi 1.0 — para heredar la
puerta con `modelo` como parámetro). **Nada desplegado. El Batch NO se mandó.**

---

## Lo medido antes de construir

| | medición |
|---|---|
| `cat_razas` | **existe** · `especie · slug · nombre · ruta_imagen · activo · …` |
| tamaño | **105 filas, 100 activas** — perro 44 · gato 20 · pez 10 · ave 10 · roedor 8 · conejo 8 · reptil 5 |
| `raza_codigo` del contrato | es **`cat_razas.slug`** (no hay columna `codigo`) |
| `mascotas.raza` | **`text` libre** — coherente con `D-379`: el catálogo **sugiere y jamás impone** |
| `ia_uso` | CHECK **sólo sobre `resultado`**, ninguno sobre `pieza` ⇒ la pieza nueva entra **sin migración** |

🔴 **Los slugs traen tipeos reales y son el valor válido:** `pitbul-terrier`
(una L), `jack-rusell` (una S), `shnauzer`, `stanffordshire`. *Un modelo que los
escribe «bien» devuelve un código que no existe.* El arnés prueba ese caso.

---

## D5 · `sugerir-raza`

### La desviación del mandato, declarada

El mandato dice que el catálogo entra **en el cuerpo**. Acá lo **lee la edge**
de `cat_razas` con `service_role`. Tres razones, y **la segunda es la que
decide**:

1. El catálogo es del servidor: un bundle viejo mandaría una lista vieja.
2. 🔴 **La lista blanca de SALIDA se valida contra la MISMA fuente que la
   generó.** Si el catálogo viaja en el cuerpo, **el cliente define su propia
   lista blanca** — y «nunca fuera del catálogo» deja de significar algo.
3. Cuesta una consulta de ~100 filas.

*Si la mesa prefiere lo otro son cinco líneas — pero entonces ② deja de ser
cierto, y hay que decirlo.*

### Las decisiones con su razón

- **`confianza` es `alta|media|baja`**, no un número: la casa tiene **una**
  escala de confianza, no una por pieza, y un `0.87` de un modelo es precisión
  falsa que la pantalla mostraría como si fuera medida.
- **Especie sin razas activas ⇒ no se llama al modelo.** Pedirle que elija de
  una lista vacía es gastar una llamada para que devuelva nada.
- **La lista blanca se exige en el VALIDADOR, no en el prompt.** *Un vocabulario
  cerrado que sólo vive en el prompt es una sugerencia; el que vive en el
  validador es un vocabulario cerrado.* El control lo prueba: **sacándolo, la
  raza inventada sale con 200.**
- **Haiku por defecto** (mandato). Es plausible porque *no es lo mismo leer un
  carnet que mirar un perro*: reconocer de una lista de 44 no es la atribución
  espacial fina donde S48 midió que Haiku topaba. **E lo confirma con número.**

### Verde y rojo — `verify:raza`, **26 verdes · 0 rojos**

Los dos rojos que el mandato pidió, más seis del validador:

| caso | resultado |
|---|---|
| **foto sin animal** | `sin_animal: true` · candidatas **vacías** · 200 (no es error: es respuesta) |
| **gato con especie «perro»** | candidatas **vacías** · **`sin_animal: false`** (hay animal, no es de la especie) |
| raza inventada · raza de otra especie · **slug «corregido»** | 422 sin datos |
| 4 candidatas · repetida · confianza inventada · `mestizo` no booleano · se contradice | 422 sin datos |
| especie sin razas | 400 · **cero llamadas al modelo** · cero filas en `ia_uso` |
| la palanca y el techo | cliente no elige modelo · `service_role` sí · > 2 MB rebota |

**Control:** sacando la lista blanca del validador → **tres rojos**, la raza
inventada saliendo con 200.

---

## D6 · El contenido por raza

`scripts/ia/contenido-razas.mjs` — `--control` · `--construir` · `--enviar` ·
`--recoger <id>`.

**Construido y medido:** 100 peticiones, **`custom_id` únicos 100/100**, Sonnet
5 con `thinking: disabled`, entrada ≈ 50.697 tokens, **~$0,35** a mitad de
precio. **NO SE MANDÓ**: no hay credencial en esta pista, y `--enviar` para en
seco antes de gastar.

**Lo que el prompt prohíbe, y es lo que más importa:** las predisposiciones se
escriben **como algo para conversar con el veterinario**, jamás como
diagnóstico ni indicación — sin tratamientos, sin medicamentos, sin dosis. *Que
la raza tenga una predisposición no significa que ESTE animal la tenga.*

**El rojo pedido, y su forma:** una raza que el modelo no reconoce devuelve
`conocida: false` y **todo en null**. El validador exige la contrapartida: un
`conocida:false` **con texto adentro** es rojo — *sin eso, «vacío» no significa
nada.* **`criollo` va a volver vacío y está bien:** no se puede decir la talla
adulta de un mestizo.

`--control`: **9 verdes · 0 rojos**, sin gastar una llamada. Corre sin
credenciales a propósito (import perezoso): *un control que necesita
credenciales para correr es un control que no se corre.*

---

## 🔴 El defecto que encontré, y es mío

Al agregar la pieza `raza` quedaron **dos tablas incompletas**: `TIMEOUT_MS` sin
`raza` y `CACHEAR_SISTEMA` sin `presencia`. La segunda porque mi reemplazo se
comió la línea; **la primera porque escribí el patrón con los números VIEJOS de
los timeouts, sin mirar que A ya los había cambiado.** Dato heredado de memoria
contra el objeto, otra vez.

### La consecuencia no era cosmética, y la medí

`TIMEOUT_MS.raza` en `undefined` hace `setTimeout(fn, undefined)`, **que dispara
a los 0 ms**. Medido: `abortado = true` a los 5 ms. ⇒ **toda sugerencia de raza
se habría abortado al instante, en producción.**

### Lo grave es quién no lo vio

| instrumento | veredicto |
|---|---|
| `deno check` | **lo dijo**: dos `TS2741` explícitos |
| `verify:edge-deno` | 🔴 **VERDE** — `TS2741` cae en su bucket «fuera de clase» |
| **mi propio arnés de `sugerir-raza`** | 🔴 **26/0** — el proveedor falso responde de forma **sincrónica** y el timer nunca alcanza a disparar |
| `verify:ia-puerta` | lo cazó **por el otro lado** (el caché de `presencia`) |

***Un proveedor falso instantáneo no puede ver un timeout roto.*** Y que lo
cazara el gate de al lado, por el otro defecto, no es un instrumento: es suerte.

### La cura es estructural

**`PIEZAS` pasa a ser un ARRAY en runtime y el tipo se DERIVA de él** — *un tipo
que sólo existe en compilación no se puede recorrer*. Con el array, el arnés
**censa las siete tablas contra las cinco piezas**, más un brazo que exige que
**ningún `TIMEOUT_MS` sea 0 ni `undefined`** — el modo de falla exacto.

- **Control positivo del censo**: sabe ver un hueco cuando lo hay.
- **Control contra el defecto REAL**: sacando `raza` de `TIMEOUT_MS` otra vez,
  el censo lo nombra **en dos brazos**.

`verify:ia-puerta` pasa de **33 a 42 verdes**.

---

## Para A o E — una ficha con su número medido

> 🟠 **Gatear `TS2741` en `verify:edge-deno`.** Con lo mío curado, **`TS2741`
> está en CERO en toda la casa** (89 fuera de clase: `TS2339`×77, `TS2345`×9,
> `TS7006`×3). ⇒ **gatearlo no cuesta un solo falso rojo hoy** — que es
> exactamente lo contrario de `TS2339`, donde ensanchar se rechazó bien por 77
> de línea base. *«Declara una tabla y no la completa» es la misma familia que
> «usa algo que no existe»: las dos compilan mal y revientan en runtime.* El
> número se pide con `pnpm proximo:ficha` al depositar.

## Gates

`verify:ia-puerta` **42/0** · `verify:raza` **26/0** · `verify:carnet-v2`
**27/0** · `ia:razas-contenido --control` **9/0** · `verify:edge-deno` **35 en
verde** · **4 typechecks en 0**.

Los cinco gates de IA quedan declarados en `package.json` con su comentario.
**Ninguno llama al proveedor**, así que se pueden correr siempre.

## Lo que NO hice

- **Nada desplegado. El Batch no se mandó.**
- **Ningún modelo elegido** — Haiku es el default de `raza`; E confirma.
- **Ninguna migración**: la pieza nueva no la necesita (medido).
- **Ninguna pantalla**: `sugerirRaza` tiene wrapper, no UI. **C confirma.**
- **No toqué `mascotas.raza`** ni ninguna escritura.

---

# ADDENDUM · D5 LISTA PARA DESPLIEGUE (S113-D, 5-sep)

## 🔑 SHA: `e5181fa6` — rama `pista/s113-d-1.2`

Árbol limpio, verificada en origin por SHA. **A la despliega con el veredicto
del founder.** Es una edge NUEVA (`sugerir-raza`), no toca ninguna existente.

## El contrato, para A y para C

```
POST /functions/v1/sugerir-raza     (verify_jwt + exigirSesion)

  { imagenBase64: string,           // la foto, achicada por C
    especie: string,                // la que la persona DECLARÓ
    mediaType?: 'image/jpeg' | 'image/png' | 'image/webp' | 'image/gif',
    modelo?: string }               // 🔒 SÓLO service_role, lista blanca de 2

  200 → { candidatas: [{ raza_codigo, confianza }],   // máx 3, del catálogo
          mestizo: boolean,
          sin_animal: boolean }

  error → { codigo, mensaje }
    cuerpo_invalido 400 · imagen_invalida 400 · especie_desconocida 400
    configuracion_faltante 500 · error_modelo 502 · sugerencia_fallida 422
```

Wrapper: **`sugerirRaza`** en `@epetplace/api`, con
`SugerenciaDeRaza · CandidataRaza · ConfianzaRaza · InputSugerirRaza ·
CodigoErrorRaza`.

## Lo que A tiene que saber antes de desplegar

1. **La edge LEE `cat_razas` con `service_role`.** Necesita `SUPABASE_URL` y
   `SUPABASE_SERVICE_ROLE_KEY` en el ambiente de la function — ya están (las
   usa `despachar-push`, entre otras).
2. **Modelo por defecto `claude-haiku-4-5`.** Es decisión de mesa y **E lo
   confirma con número**; el override existe para que E compare **sin dos
   despliegues**, y sólo lo honra un token `service_role`.
3. **Escribe en `ia_uso`** con `pieza: 'raza'` — la pieza no necesita migración
   (medido: `ia_uso` sólo tiene CHECK sobre `resultado`).
4. **No escribe en ninguna otra tabla.** `D-379` rige: el catálogo **sugiere y
   jamás impone**, y `mascotas.raza` es texto libre.

## Para C

**Nunca se guarda sola.** El wrapper devuelve candidatas y **la persona elige**.
`candidatas: []` con `sin_animal: false` es una respuesta legítima y frecuente:
**hay un animal y no es de la especie declarada.**

⚠️ Y el detalle que muerde: **los slugs del catálogo traen tipeos reales**
(`pitbul-terrier` con una L, `jack-rusell` con una S). Son el valor válido —
"corregirlos" produce un código que no existe. El validador de la edge los exige
verbatim contra el catálogo leído en esa misma llamada.

## Gates de D5

`verify:raza` **26 verdes · 0 rojos**, con proveedor falso: los dos rojos que el
mandato pidió (foto sin animal → `sin_animal: true`; gato con especie «perro» →
candidatas vacías con `sin_animal: FALSE`) y seis del validador —raza inventada,
raza de otra especie, **slug «corregido»**, cuatro candidatas, repetida,
contradicción—. **Control:** sacando la lista blanca del validador, la raza
inventada **sale con 200**.

⚠️ **Nada de esto midió al MODELO**: el arnés corre con proveedor falso y prueba
el contrato. **Que Haiku reconozca una raza lo mide E**, con las fotos reales —
y su conjunto tiene **3 casos aptos, los tres perro, cero gato**, así que alcanza
para probar que la pieza corre y **no alcanza para elegir un modelo**.

---

# ADDENDUM 2 · D6 CORRIDO Y ② LA CURA DE `sugerir-raza` (S113-D-2.7)

## D6 · el Batch, corrido con la llave de medicion

`msgbatch_01JWQKMDurB5ELNs3cm9cE1m` · 100 peticiones · Sonnet 5, `thinking:
disabled`.

### 🔴 EL COSTO REAL CONTRA MI ESTIMACION: 49 % MAS CARO

| | estimado | **real** | desvio |
|---|---:|---:|---:|
| tokens de entrada | 50 697 | **95 047** | **+87 %** |
| tokens de salida | 60 000 | **85 761** | **+43 %** |
| **costo** | **$0,351** | **$0,5239** | **+49 %** |

Por ficha: **950 tokens de entrada** (estime 507) y **884 de salida** (estime
600). *La entrada la calcule con `chars/4`, que es la regla para ingles; en
castellano son ~3 caracteres por token y el prompt lleva ademas el esquema
entero.* **El error no fue de mas o menos suerte: fue usar una constante que no
era la de este idioma.**

**Las 100 filas quedaron en `ia_uso`**, con `pieza: 'contenido_raza'` y
`edge: 'batch:contenido-razas'`. 🔴 Este script **no pasa por `llamarModelo`**
—habla directo con la API de Batches, que la puerta no cubre— asi que sin esta
escritura **el gasto no existiria para la casa**. El costo va **a la mitad**
(la tabla de E es de precio estandar) y `latencia_ms` va **null**: *un batch no
tiene latencia por peticion, y poner la del lote entero en cada fila mentiria
cien veces.*

### El resultado: 97 fichas · 3 vacias · 3 rechazadas

**Depositado en `docs/loop/S113-D-contenido-razas.json`** — 🔴 **A lo carga con
`activo=false`. Nada de esto se publica sin revision humana.** Cada ficha
declara su modelo y su fecha.

**Las 3 vacias son exactamente las que no son razas:** `gato-comun`, `criollo`,
`cobaya-o-cuy`. *La regla del prompt funciono: `conocida:false` y todo en null,
sin un parrafo de relleno.*

**Las 3 rechazadas las cazo el validador, y las tres son fallas reales del
modelo** — mire el crudo para no acusar a mi propio validador:
- `shnauzer`: **rompio el contrato a proposito**, contesto en prosa pidiendo si
  es Miniatura, Mediano o Gigante. *Semanticamente razonable; el contrato pedia
  `conocida: false`.*
- `rex` y `pez-betta`: devolvieron `cuidados_por_etapa` **con solo `cachorro`**,
  sin `adulto` ni `senior`.

⚠️ Sospeche que el vocabulario `cachorro/adulto/senior` fuera el problema (rex es
conejo, pez-betta es pez) **y lo medi: las 52 fichas validas de no-perro tienen
las tres etapas llenas.** No es sistematico.

**El muro §, verificado en la salida:** las predisposiciones vuelven como *«algo
para preguntarle al veterinario»*, *«conviene consultarlo»*. Ninguna prescribe.

---

## ② `sugerir-raza`: una candidata que no sirve se descarta, no tumba la respuesta

**El caso que lo obligo, visto en vivo por el founder:** el modelo devolvio
**«American Bully»** y el codigo del catalogo es **`american-bully`**. *La raza
estaba BIEN y la respuesta entera se rechazaba por la forma del texto.*

**Dos curas, y la segunda es el cinturon de la primera:**
1. **El catalogo viaja al prompt como `slug — nombre`**, no como una lista pelada
   de slugs. El modelo ve contra que elegir y que codigo devolver.
2. **La salida se NORMALIZA antes de validar** (minusculas, sin acentos,
   espacios y guiones bajos a guion medio). *Exigir la forma exacta de un slug
   es exigirle al modelo que sepa una convencion de base de datos.*
3. **Lo que igual no encuentra su codigo se DESCARTA solo**, con su motivo, y
   viaja en `descartadas`. Si no queda ninguna: `candidatas: []` **con 200**.
   **Nunca 422 por una candidata.**

**Lo que SI sigue tumbando la respuesta** es la FORMA rota, no una candidata:
`mestizo` no booleano, `candidatas` que no es lista, y la contradiccion
`sin_animal: true` con candidatas.

### 🔴 EL ROJO: LA FOTO REAL DE ZEUS

Corrida contra `claude-haiku-4-5`, foto real de Storage, catalogo real de
`cat_razas`: **1.555 ms · 76 tokens de salida.**

```
{"candidatas":[{"raza_codigo":"american-bully","confianza":"alta"},
               {"raza_codigo":"pitbul-terrier","confianza":"media"},
               {"raza_codigo":"boxer","confianza":"media"}],
 "mestizo":false,"sin_animal":false}
```

**`american-bully` exacto, primera y con confianza alta.** La verdad del
conjunto dice «American Bully». Y `pitbul-terrier` vuelve **con una sola L**,
que es el slug real del catalogo.

*Con el catalogo bien mandado, el modelo devolvio el slug sin ayuda: el
normalizador no hizo falta esta vez, y esta ahi para la vez que si.*

⚠️ **Y la perdida que hay que MEDIR, no suponer:** un modelo que escriba
«pitbull-terrier» (con dos L, que es la ortografia correcta del ingles) **NO
normaliza al mismo slug** y esa candidata se descarta. Es correcto —ese codigo
no existe— pero es exactamente lo que `descartadas` esta ahi para contar.

**Gates:** `verify:raza` **48 verdes · 0 rojos** · `ia:razas-contenido --control`
**9/0** · `verify:edge-deno` **35 verde** · typechecks en 0.
