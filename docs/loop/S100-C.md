# S100 · PISTA C — LA VITRINA Y LA FICHA (la familia entra y elige)

**Rama:** `pista-c` · **Worktree:** `e-petplace-s100-c` · **Base:** `main` en `57aa526d`
**Territorio:** `apps/cliente` (vitrina Despensa, búsqueda, ficha de producto) + lectores de
catálogo del cliente.

> **⚠️ CORRECCIÓN MEDIDA a la premisa heredada: el canal entre pistas NO está caído en
> S100.** El cierre de S99 lo declaraba caído y yo abrí con esa premisa; **D me escribió
> por socket durante esta tanda y el mensaje llegó entero**, así que la premisa está
> vencida. *Se corrige acá y no solo en mi cabeza: cuatro pistas operando con un canal que
> creen muerto se mandan handoffs por archivo que podrían resolver en un mensaje.*
>
> **Lo que NO cambia: el archivo sigue siendo la fuente.** Un mensaje se pierde con la
> sesión que lo recibió; un archivo mergeado a `main` no. *El canal sirve para avisar, no
> para recordar* — y el precedente de S99 (un lote entero que vivió solo en un disco) es
> sobre lo segundo, no sobre lo primero.

---

## §0 · LAS ENMIENDAS DE LA MESA EN VUELO — registradas ACÁ para que no vivan solo en el chat

Llegaron durante el arranque, gobiernan L1 y L2, y **ninguna de las tres es mía**: se
depositan verbatim para que sobrevivan a una compactación.

| # | La enmienda |
|---|---|
| **E1** | **EL ACORDEÓN NO ES UN ESCONDITE.** Solo colapsa **prosa descriptiva** (beneficios, características, descripción de marca). **N19 ①-⑥ vive SIEMPRE ABIERTO** — y la **ADVERTENCIA DE ALÉRGENO jamás, bajo ninguna circunstancia, dentro de un bloque colapsado**: *un dato de salud detrás de un `+` es un dato que la familia no leyó.* Si algo puede lastimar a la mascota, **se ve sin tocar nada**. |
| **E2** | El **buscador** va con **lupa + placeholder («Nombre o marca»), sin etiqueta arriba** — exención N11′. |
| **E4 · FIRMA F5** | **Un carrito, N pedidos independientes en v1.** **La división se DECLARA antes de pagar y se EXPLICA después.** |
| **E3** | **LA PRUEBA DE LAS DOS COLUMNAS.** Dos columnas es correcto para navegar catálogo con foto, **pero si en una categoría el nombre curado NO entra en 2 líneas junto con su presentación, esa categoría te está diciendo que la tarjeta le queda chica.** **Medilo y reportalo (H-nnn), no lo resuelvas** encogiendo la letra ni truncando el dato que decide la compra. |

**E3 está ejecutada en este mismo documento** — su medición es §1.5 y su hallazgo es **H-005**.

---

## §1 · L0 — EL CENSO. TODO MEDIDO CONTRA LA BASE VIVA, NADA DEDUCIDO

Instrumento: `npx supabase --experimental db query --linked --file` (solo lectura, cero
escritura). Lo de la cara vendedor se midió **contra `origin/main` entero**, no contra mi
worktree.

### §1.1 · 🔴 LOS DUPLICADOS — **es fuga de ofertas de vendedor, y el lector afirma por escrito lo contrario**

**El caso del founder, medido fila por fila:**

| oferta_id | producto_id | variante_id | cuenta_comercial_id | precio |
|---|---|---|---|---|
| `e88fdea3…` | `6f77dbbb…` | `0e210866…` | `eec12ef3…` | **70.90** |
| `e54f29e8…` | `6f77dbbb…` | `0e210866…` | `61a28501…` | **75.86** |

**Mismo producto, MISMA VARIANTE, dos vendedores.** No es catálogo canónico duplicado: es
**una variante con dos ofertas publicadas**, y el modelo lo permite **a propósito**.

**El índice real, leído de `pg_indexes`:**

```
uq_oferta_publicada_por_cuenta_variante
  UNIQUE (cuenta_comercial_id, variante_id) WHERE estado = 'publicada'
```

⇒ garantiza **una oferta por VENDEDOR por variante**. **N vendedores por variante es legal.**

**🔴 Y acá está lo que nadie estaba mirando.** `obtenerFichaProducto`
(`despensa-catalogo.ts:531`) dice, en un comentario:

> *«El UNIQUE parcial `uq_oferta_publicada_por_variante` garantiza que hay **una sola**
> publicada por variante — no hay que elegir entre varias.»*

**Ese índice NO EXISTE** (el real lleva `_por_cuenta_` en el medio) **y lo que el real
garantiza es lo contrario de lo que el comentario afirma.** Sobre esa premisa falsa, el
código hace:

```ts
const of: unknown = publicadas[0];   // sin .order() — arbitrario
```

⇒ **la ficha abre con un vendedor arbitrario.** *La tarjeta puede decir $70.90 y la ficha
$75.86 del mismo toque: un precio que cambia cuando lo tocás.* **Y no tiene síntoma** —
las dos pantallas se ven correctas por separado; solo el par las delata. *Es la clase de
defecto que no se encuentra leyendo una pantalla, porque cada una está bien.*

**La escala, medida:**

| medida | valor |
|---|---|
| filas de `v_vitrina_publicada` | **563** |
| variantes distintas | **538** |
| **variantes con más de una oferta publicada** | **25** |
| productos distintos | 470 |
| vendedores | 5 |

**⇒ 25 productos se ven DOS VECES en la vitrina, con dos precios y sin decir por qué.**

**Y la cadena que elegiría entre esos dos: NO EXISTE COMO MOTOR.** Censo por nombre de
función en `public` (`elegir%`/`selec%`/`mejor%`/`ranking%`/`calific%`): devuelve **solo**
`elegir_modo_horarios` y `update_prestador_calificacion`, **las dos de prestador**. Nada de
despensa. **Coincide con lo que el acta de S99 §8 ya declaraba** (*«la cadena de selección
entre vendedores está CONSTRUIDA SIN DATOS»*) — y mi medición **estrecha** esa declaración:
no es que corra sin datos, **es que no hay función que correr**.

⇒ **H-001 (dueño A) + BLOQUEO de la firma «UN PRODUCTO, UNA VEZ» de L1.** Ver §3.

**🔴 Y LA FIRMA F5 (E4) SUBE LA APUESTA DE H-001, no la baja.** *Un carrito, N pedidos
independientes* significa que **el vendedor de cada ítem decide EN QUÉ PEDIDO cae** — y por
lo tanto cuántos pedidos ve la familia, cuántas entregas espera y cuántas veces le
explicamos la división. ⇒ **elegir vendedor deja de ser un detalle de precio y pasa a ser lo
que determina la forma del checkout.**

Con la vitrina de hoy —**25 variantes duplicadas y la ficha eligiendo `publicadas[0]` sin
`order`**— la familia puede agregar **el mismo producto dos veces creyendo que son dos
cosas**, y F5 se lo devolvería como **dos pedidos de dos vendedores**. *La división se
declararía correctamente y sería incomprensible, porque el error no está en cómo se divide:
está en que nunca debió haber dos.* **F5 se cumple al pie y produce el peor resultado** —
exactamente el modo de falla que esta casa viene midiendo: la regla correcta aplicada sobre
la premisa equivocada.

### §1.2 · EL HUÉRFANO — de qué fila es y por qué se separa

**Es de la fila de ARRIBA.** Causa leída en `index.tsx:351-354`: el aviso se pinta como un
`<View>` **hermano de la `Celda`, FUERA de su caja**, con solo `paddingHorizontal`:

```tsx
<Celda … titulo={p.nombre} … />
{!p.hay_stock ? (
  <View style={{ paddingHorizontal: spacing[5], … }}>
    <Texto variante="apoyo">{t('despensa.filaSinStock')}</Texto>
  </View>
) : null}
```

La `Celda` es un tocable autocontenido; el aviso cuelga debajo. **Como las filas no tienen
borde ni separador, nada lo ata a su fila y la siguiente arranca pegada** ⇒ el texto flota
entre dos productos y se lee como si fuera de ninguno. **Mismo patrón exacto para la
advertencia de alergia** (`filaContiene`, mismo bloque, también fuera de la `Celda`).

**Y no es un caso raro: `hay_stock = false` en 80 de 563 ⇒ aparece en 1 de cada 7 filas.**

⇒ **H-002 (dueño C).** La cura viaja con `TarjetaProducto` de L1: el aviso tiene que vivir
**DENTRO** de la superficie de la tarjeta — *el cartel va en la puerta, no en la vereda*, que
es literalmente la firma de L1.

### §1.3 · 🔴 EL BUSCADOR — busca nombre y marca, y el acento lo rompe **al revés de lo esperado**

**Qué busca de verdad:** `nombre` y `marca`, por `ilike`
(`or(nombre.ilike.*t*,marca.ilike.*t*)`). **La categoría NO se busca** — quien escriba
«arena» o «antiparasitario» no encuentra por familia.

**Mayúsculas:** `ilike` las ignora ⇒ **OK, sin defecto.**

**🔴 Acentos — y la dirección importa:**

| consulta | filas |
|---|---|
| `nombre ilike '%nutricion%'` (sin tilde) | **2** |
| `nombre ilike '%nutrición%'` (con tilde) | **0** |
| nombres con tilde o ñ, sobre 563 | **0** |

**El catálogo entero llegó SIN ACENTOS** — `nutricion`, `arena de bano`, `pequenos
mamiferos`. `ilike` **no** es insensible a acentos. ⇒ **la familia que escribe bien su
propio idioma no encuentra nada.** *No es que el buscador sea estricto con el dato sucio: es
que el dato sucio castiga a quien escribe bien.*

**Y una asimetría de saneo entre las dos puertas a la misma vitrina:**
`aplicarFiltrosVitrina` limpia `,` `(` `)` del término; `buscarProductosDespensa` escapa
`%` y `_` **y no limpia los otros**. Dos puertas, dos saneos.

⇒ **H-003 (dueño A — es motor).** La cura honesta es `unaccent` en el servidor (extensión
o columna normalizada con su índice); **normalizar en el cliente no sirve**: el filtro
corre en Postgres sobre 563 filas y el teléfono nunca las ve.

### §1.4 · D-843 — las dos caras paginan distinto, **y el hallazgo nuevo es peor que la deuda**

| cara | techo declarado | ¿pagina? |
|---|---|---|
| **cliente** (vitrina, mío) | `listarProductosDespensa({ limite: 50 })` — explícito en `index.tsx:161` | **NO** — corte duro, sin cursor y sin «cargar más» |
| **cliente** (búsqueda) | `buscarProductosDespensa`, default **50** | NO |
| **vendedor** (`origin/main`, `despensa-vendedor.ts:1406`) | `opciones?.limite ?? 30` | **SÍ** — cursor real, `hasMore` derivado de `data.length === limite` (`:1487`) |

**D-843 queda confirmada y con número: 50 sin cursor contra 30 con cursor.**

**🔴 EL HALLAZGO QUE NO ESTABA EN D-843, y es el que duele:**
`contarProductosDespensa` **existe**, y su propio comentario dice para qué:

> *«⇒ la superficie lo pasa EXPLÍCITO, y para poder decir «100 de 563» usa
> `contarProductosDespensa` con los MISMOS filtros.»*

**Tiene CERO consumidores en la vitrina** (grep en 0 sobre `index.tsx`). La superficie hizo
**la mitad** de lo que el wrapper pide: pasa el límite explícito ✅ y **nunca cuenta** ❌.

⇒ **La familia ve 50 de 563 — el 8,9 % del catálogo — y la pantalla no lo dice.**
*Una lista completa y una truncada se siguen viendo igual, que es exactamente lo que ese
wrapper se escribió para impedir.* **La cura estaba construida y sin consumir.**

⇒ **H-004 (dueño C) — CURADO EN ESTA TANDA, ver §2.**

### §1.5 · E3 — LA PRUEBA DE LAS DOS COLUMNAS, medida por categoría

**Global, sobre las 563 filas comprables:**

| medida | valor |
|---|---|
| **EN MAYÚSCULAS** (`nombre = upper(nombre)`) | **235 (42 %)** |
| con tilde o ñ | **0** |
| largo promedio | 28 chars |
| largo máximo | **81 chars** |
| > 28 chars | 217 (39 %) |
| **> 40 chars** | **101 (18 %)** |

**Por categoría — y acá está la respuesta que E3 pidió:**

| familia | n | EN CAPS | **> 40 chars** | ¿la tarjeta le queda chica? |
|---|---|---|---|---|
| `acondicionador_agua` | 8 | 0 | **7 (88 %)** | **🔴 SÍ, gritando** |
| `higiene` | 11 | 6 | **5 (45 %)** | **🔴 SÍ** |
| `alimento` | **285** | **168** | 62 (22 %) | ⚠️ en el borde, y es la categoría grande |
| `suplemento` | 72 | 0 | 9 (13 %) | no |
| `antiparasitario` | 108 | 0 | 12 (11 %) | no |
| `dieta_prescripcion` | 68 | **61 (90 %)** | **0** | no por largo — **sí por CAPS** |
| `heno` | 7 | 0 | 2 | no |
| `sustrato` | 4 | 0 | — | no |

**Lectura, sin resolverla:** en una grilla de 2 columnas sobre un teléfono de ~390 pt cada
tarjeta mide ~180 pt; a cuerpo base entran ~20-22 chars por línea ⇒ **2 líneas ≈ 40-44
chars**. **`acondicionador_agua` (88 %) e `higiene` (45 %) son las dos categorías que están
diciendo que la tarjeta les queda chica** — y lo dicen **antes** de que la curaduría agregue
la presentación. `alimento` al 22 % está en el borde **y es el 51 % del catálogo**.

**⇒ H-005 (dueño: MESA — es decisión de forma, no de código).** *No lo resolví encogiendo
la letra ni truncando la presentación, que es el dato que decide la compra.*

**Dato que la curaduría de L1 va a necesitar, y que también se midió:** `etiquetaFamilia()`
(`index.tsx:266`) **solo tiene voz para 4 de las 8 familias vivas** — `higiene`,
`acondicionador_agua`, `heno` y `sustrato` devuelven `null` (**30 productos sin etiqueta**).
Si el nombre curado toma su descriptor de la familia (la vía honesta: sale de una columna
real en vez de inventarse), **faltan cuatro voces**. ⇒ **H-006 (dueño C, entra con L1).**

---

## §2 · LA CURA DE DATO DE ESTA TANDA — el total honesto (H-004)

**Por qué ésta y no otra:** mi mandato dice *«tu receta de forma la deposita B ANTES de
pantalla nueva; mientras, L0 y curas de dato»*. **B no depositó nada de S100** (verificado:
`pista-b` solo tiene los tres commits del handoff de S99; no hay lámina S100 ni
`TarjetaProducto` en `packages/ui`). ⇒ L1 y L2 esperan; esta tanda es censo + la única cura
que es **de dato puro y no toca forma nueva**.

**Qué hace:** la vitrina llama `contarProductosDespensa` con **los mismos filtros** que la
lista y **dice cuántos hay** cuando el techo corta.

**Lo que NO hace, declarado:** no sube el techo, no agrega paginación, no toca el orden.
*Subir el techo sin cursor sería cambiar 50 por otro número igual de mudo.* El cursor es
D-843 y va con su decisión de forma.

---

## §3 · BLOQUEOS Y HALLAZGOS — con dueño y disparo

| # | Hallazgo | Dueño | Estado |
|---|---|---|---|
| **H-001** | **25 variantes con 2+ ofertas publicadas ⇒ la vitrina duplica y la ficha elige `publicadas[0]` sin `order`. La tarjeta y la ficha pueden mostrar precios distintos del mismo producto.** El comentario del wrapper nombra un índice inexistente y afirma la garantía contraria a la del índice real. **No existe cadena de selección entre vendedores en la base.** | **A** (motor + wrapper) | 🔴 **BLOQUEA** «UN PRODUCTO, UNA VEZ» de L1 |
| **H-002** | El aviso de sin-stock y el de alergia se pintan FUERA de la `Celda` ⇒ huérfanos entre filas. 80/563 filas afectadas. | **C** | Cura viaja con `TarjetaProducto` (L1) |
| **H-003** | Catálogo con **0 nombres acentuados**; `ilike` no es insensible a acentos ⇒ **quien escribe bien no encuentra**. Además la categoría no se busca y las dos puertas sanean distinto. | **A** (motor: `unaccent`) | 🔴 abierto |
| **H-004** | `contarProductosDespensa` con cero consumidores ⇒ **50 de 563 sin decirlo**. | **C** | ✅ **curado en §2** |
| **H-005** | `acondicionador_agua` (88 % > 40 chars) e `higiene` (45 %) no entran en 2 líneas con su presentación. | **MESA** | 🔴 servido, sin resolver |
| **H-006** | `etiquetaFamilia()` sin voz para 4 de 8 familias (30 productos). | **C** | entra con L1 |
| **H-008** | **~51 % de lo RECOMENDADO muestra «Sin composición declarada»** (288 `ausente` + 274 `declarada_sin_verificar` = 99,8 % del catálogo; **cero `verificada`**). Correcto por letra —*sin ingredientes no se sostiene «seguro para Thor»*— y es **decisión de producto** si esa densidad de banda ámbar se acepta o se ataca cargando composiciones. | **MESA + founder** | 🔴 servido |
| **H-007** | **⏪ ENMENDADA: son TRES viajes seriales, no dos — y la cura NO es del cliente.** Ver §3septies. | **A** (motor) | 🔴 abierto, cedida |

**🔴 EL BLOQUEO QUE PARA L1, escrito con su razón:** la firma *«UN PRODUCTO, UNA VEZ — el
del vendedor que la cadena elegiría en este momento (puertas STOCK/ZONA · orden CALIFICACIÓN
· desempate ANTIGÜEDAD)»* **describe un motor que no existe** (§1.1). Tengo dos salidas y
**ninguna la tomo sin firma**:

- **(a)** A construye el lector canónico por variante con la cadena adentro, y C lo consume.
  *El motor calcula; el wrapper transporta* — la ley de la casa, escrita en la cabecera de
  `despensa-catalogo.ts`.
- **(b)** C colapsa por `variante_id` en la superficie. **Voto en contra y lo declaro:**
  elegir vendedor es **decisión de negocio**, y resolverla en la pantalla la vuelve
  inauditable y la duplica el día que el checkout elija distinto — *que es exactamente el
  defecto de H-001 con otro disfraz.*

**Y el freno con su medición adentro (L-285):** no colapsé por precio más bajo, que era la
salida obvia. **Medí que no hay criterio firmado**: la letra dice CALIFICACIÓN → ANTIGÜEDAD,
**no precio**, y no hay calificaciones. *Ordenar por el precio más bajo habría parecido
correcto y habría contradicho la letra sin que nadie lo notara.*

---

## §3bis · N16 — EL NÚMERO DE ESTE LOTE CONTRA LA LÍNEA BASE

**N16 pide reportar el número en cada cierre de lote. Lo hago con lo que se
puede medir del objeto, y declaro rojo lo que no.**

| medida | línea base (`main`) | ahora | fuente |
|---|---|---|---|
| llamadas a wrapper en `index.tsx` | **6** | **7** | grep sobre el archivo |
| **viajes de red al ABRIR la vitrina** (sin mascota, sin búsqueda) | **4** | **5** | conteo del camino de montaje |

**🔴 Mi cura AGREGA un viaje, y lo digo en vez de esconderlo.** Lo que lo
vuelve aceptable no es que sea chico — es **cómo** entra:

- **va en PARALELO, jamás encadenado** (N16 §1): sale del mismo efecto que la
  lista, sin esperar su respuesta. **No suma latencia serial**, suma
  concurrencia.
- **es un `head` de PostgREST**: cuenta en el servidor y **no baja una sola
  fila** al teléfono. *Un viaje que no trae payload es el más barato que hay.*
- **su fallo no cuesta nada**: sin número, la pantalla calla y la lista pinta
  igual.

**🔴 LO QUE VA ROJO POR AUSENCIA DE APARATO:** el presupuesto real de N16
—*primer contenido < 1 s en pantalla visitada, < 2 s en fría*— **se mide en
aparato real y no lo tengo.** *Declararlo verde contando viajes sería medir
la variable equivocada, que es exactamente L-285.* El número de viajes es un
proxy, y se reporta como proxy.

**Y el censo destapó, sin buscarlo, el viaje que sí duele: H-007** — dos
lecturas **encadenadas** en el montaje. *Mi viaje nuevo es paralelo y gratis;
los dos que ya estaban son seriales y cuestan ~300 ms.* **La cura de N16 en
esta pantalla no es sacar lo que agregué: es desencadenar lo que ya había.**

---

## §0bis · LA LEY QUE NACIÓ DEL INTERCAMBIO B↔C (depositada por mesa, S100)

> **EL CASO LO TIENE QUIEN CONSUME; EL AGUJERO QUE LA FORMA ABRE LO VE QUIEN
> CONSTRUYE LA PIEZA. Ninguna vista sola alcanza.** Cuando una pista necesite
> ensanchar una pieza ajena: **pide con EL CASO, no con la firma que imagina.** Y
> **quien la construye responde con la FORMA, no con un sí.**

**Su evidencia es esta sesión, y por eso se registra acá con los dos lados:** yo tenía
el caso (§5.4 firmada: la búsqueda **advierte**, y migrar sin eso borraba una
advertencia de salud) y pedí `advertencia?: string`. **B vio el agujero que mi forma
abría y que yo no podía ver desde el consumo:** con un texto opcional, `undefined` es
**silencio legal en todos los casos** — incluido `ausente`, que es justo el que tiene
que hablar. **Salió mejor que las dos propuestas por separado**, y la prueba es que la
forma final comparte `alergiaPuedeCallar()` con `AvisoAlergia`: *dos piezas que no
pueden discrepar sobre cuándo callar.*

## §0ter · MEDICIÓN OPERATIVA QUE SIRVE A LAS CUATRO

La mesa avisó que **en un worktree `packages/*` puede resolver al repo PRIMARIO**, y
que el estado de `node_modules` **se mide, no se deduce**. **Medido en el mío,
resolviendo el symlink a ruta absoluta (no contando `../` a ojo):**

```
apps/cliente/node_modules/@epetplace/ui  → …/e-petplace-s100-c/packages/ui
apps/cliente/node_modules/@epetplace/api → …/e-petplace-s100-c/packages/api
```

⇒ **en `pista-c` resuelven a MI worktree**, y la pieza servida ya trae `marca`
(verificado por grep sobre la ruta resuelta, no sobre la del repo). **La advertencia no
muerde acá — pero se midió antes de decirlo**, que es la diferencia entre un dato y una
suposición cómoda.

## §0quinquies · 🔴 EL CENSO DE ALÉRGENOS — ordenado por mesa, ejecutado antes de L2

**La pregunta que abrió mi hallazgo: ¿dónde MÁS cruzamos alérgenos por coincidencia
exacta?** Censados **todos** los sitios de `apps`, `packages` y `scripts` que comparan
alérgeno contra composición:

| # | sitio | función | conjunto |
|---|---|---|---|
| 1 | `despensa/index.tsx:395` — vitrina · recomendación · **búsqueda** | `cruzarConVigilados` | **EXPANDIDO** ✅ |
| 2 | `despensa/producto/[productoId].tsx:225` — la ficha | `cruzarConVigilados` | **EXPANDIDO** ✅ |
| 3 | `despensa-catalogo.ts` · `recomendarParaMascota` — **el motor** | predicado `not ov` **+** verificación fail-closed | **EXPANDIDO** (`codigosVigilados`) ✅ |

### **EL NÚMERO: CERO sitios cruzan por coincidencia exacta hoy.** Antes de la migración de L2 era **UNO** — la fila de búsqueda.

### 🟢 LA PEOR HIPÓTESIS DE LA MESA, DESCARTADA CON MEDICIÓN

*«Si el motor de recomendación cruza exacto, puede estar RECOMENDANDO un producto que
hace mal.»* **No lo hace, y con dos capas:**

- **excluye** con `codigosVigilados`, que sale de `expandirAlergenosAVigilar` (el
  conjunto con relaciones), pegado a la consulta;
- **verifica fail-closed** contra **el mismo conjunto** y **rechaza la respuesta entera**
  si algo cruza (`exclusion_no_verificable`), en vez de sacar la fila en silencio;
- y **rechaza también si la expansión vuelve vacía** teniendo alérgenos documentados —
  *ante la duda no se ofrece*.

**El motor estaba bien. El defecto vivía solo en la superficie que advertía.**

### ☠️ EL RESIDUO, QUE ERA LO ÚNICO QUE QUEDABA CARGADO — MUERTO

`alergenosQueCruzan` (el cruce **literal**) sobrevivía **exportado y con CERO
consumidores** en todo el monorepo, verificado por grep. **Se borra en vez de dejarse
«por si acaso»:** una función de salud que cruza **de menos**, exportada y sin dueño,
**es el arma que agarra el próximo que necesite «cruzar alérgenos» y encuentre el nombre
más obvio.** *Un residuo inofensivo hoy es un default peligroso mañana.* **El cruce de
alérgenos de esta casa es UNO.**

---

## §0quater · GUARDARRAÍL VIGENTE — main inconsistente hasta el bundle

**A declaró:** las dos migraciones de S100-A **ya están aplicadas en la base** pero
siguen en `pista-a`. `main` tiene los tipos **sin** `envio_eventos` y **no** tiene la
migración que la jubiló. ⇒ **contra la base viva no hay diferencia** (`gen:types` lee la
base); **contra las migraciones de `main`, sí**.

🔴 **HASTA EL BUNDLE NADIE RECONSTRUYE UNA BASE DESDE LAS MIGRACIONES DE `main`** —
recrearía una tabla jubilada. Si hiciera falta: **frenar y avisar** (es reconstrucción
de dato, entra en los cinco frenos).

**Mi compromiso, explícito:** en toda esta sesión **solo leí la base** (`db query`, cero
escritura, cero migración) y **no voy a reconstruir nada**. *Se escribe acá y no solo se
piensa, porque un compromiso que vive en una cabeza no es un compromiso.*

> **LEY AL CANON (mesa, S100):** *una inconsistencia declarada con su ventana de cierre
> es **DEUDA**; la misma sin declarar es una **TRAMPA**. La diferencia no está en el
> estado: está en si alguien puede saberlo.*

---

## §3ter · LAS CUATRO ADJUDICACIONES DE MESA — qué se hizo y qué se cedió

### ① El precio incoherente — **CEDIDO A A, y la suya es mejor que la mía**

**Lo construí y lo tiré.** La mesa me adjudicó el orden determinista; A lo curó en
paralelo y **midió lo mismo que yo antes de tocar**. Comparadas las dos:

| | mía | de A (la que queda) |
|---|---|---|
| criterio | `oferta_id` ASC | **`created_at` ASC** (antigüedad), desempate por `id` |
| `created_at` en el select | no | **sí — sin él la antigüedad no se puede leer** |

**La de A es mejor y por eso la tomé:** *antigüedad es el fallback que el canon ya
nombra* («sin calificaciones el orden colapsa a ANTIGÜEDAD»); mi `oferta_id` era un
desempate defendible pero **arbitrario respecto de la letra**. Ejecutado con
`git checkout origin/pista-a -- despensa-catalogo.ts` y **re-aplicando encima solo mi ③**
⇒ **una cura, no dos capas.** *Dos curas del mismo defecto en dos archivos es cómo nace la
próxima contradicción; ceder la peor cuesta veinte minutos y los ahorra para siempre.*

**Deslinde para el canon, que A pidió explícito:** mi `index.tsx` **NO** colapsa la lista —
no dedupé en pantalla. **La contención vive en una sola capa: el wrapper de A.**

**Y la medición de A es más fuerte que mi censo, lo digo porque me corrige:** yo medí que
*no hay función que correr*; A midió un piso más abajo y encontró que **no hay dónde
leer el criterio** — `cuentas_comerciales` **no tiene columna de calificación**,
`resenas_productos` califica el PRODUCTO y tiene **0 filas**, y los 5 vendedores están en
7·4·3·1 pedidos. ⇒ *la cadena firmada, construida hoy, se comportaría exactamente como la
contención.* **Su freno de no construirla es correcto y lo respaldo.**

### ② H-001 — **de dueña C a dueña A.** No la reimplemento. ✅

### ③ El buscador — **MÍO, y A lo declinó explícitamente.** ✅ CURADO

Una sola función `expresionBusquedaVitrina()` para **las dos puertas** (antes saneaban
distinto: una limpiaba `,()`, la otra escapaba `%_` — la misma búsqueda se comportaba
distinto según por dónde entrara). Normaliza **los dos lados**: busca con el término tal
como se tecleó **y** sin diacríticos. La **categoría entra al alcance**.

**Discriminador, medido contra la base viva:**

| caso | antes | después |
|---|---|---|
| «nutrición» (bien escrita) | **0** | **2** |
| categoría «alimento» | 11 | **285** |
| categoría «higiene» | **0** | **11** |
| «baño» (con ñ) | **0** | **2** |

**⚠️ El hueco que queda, escrito en el código y no disimulado:** término SIN acento contra
dato CON acento no se cubre desde el cliente — exige `unaccent` en el servidor. **Hoy no
muerde porque el dato tiene 0 acentos; muerde el día que alguien cargue bien.**

### ④ H-005 — resuelto por mesa: **no es la tarjeta, son los nombres sin curar** ✅

Nace `apps/cliente/src/lib/despensa/nombre-curado.ts`. **Cambia la CAJA, no inventa una
palabra** — nada de agregar «Arena aglomerante»: *un descriptor inventado desde el nombre
es verosímil y falso, y eso ya tiene nombre en esta casa.* Si el catálogo tiene que decir
«Arena aglomerante», **es DATO y lo carga quien tiene el producto en la mano.**

**🔴 LA MEDICIÓN ENCONTRÓ UN DEFECTO EN MI PROPIA PIEZA, y es la nota de método del lote.**
Corrí la función **importando el archivo real** (no reimplementando la fórmula — S99: *así
mide la pieza y no su eco*) contra 26 nombres vivos. Salió:

> «Control **DE** Peso» · «Ternera Humeda **EN** Estofado» · «Medianas **Y** Grandes»

Mi regla ④ (*siglas de ≤2 letras se conservan*, escrita para salvar `ID`/`KD` de las dietas
veterinarias) **se adelantaba a la regla ③** (*los enlaces bajan*) **y dejaba gritando las
tres palabras más frecuentes del catálogo** — peor que no curar, porque parece descuido en
vez de catálogo crudo. **Leyendo el código las dos reglas parecían independientes; solo
correrlas contra nombres reales lo mostró.** Curado invirtiendo el orden.

**Después de la cura: 20 de 26 cambiados · 26/26 idempotente · los 6 que ya traían caja
propia, intactos.** `Cachorros Razas Medianas y Grandes` · `Esterilizados / Control de Peso
Feline` · `X-Small Adulto 8+` · `Active Mind 7+`.

**⏭️ Lo de ④ que NO es mío:** la presentación en su propia línea es **forma** y va en
`TarjetaProducto` (B). **Y cuando esa pieza suba a `packages/ui`, `nombreCurado` sube con
ella** — N17 exige una fuente y N consumidores, y el espejo del vendedor tiene que leer el
mismo nombre que la familia. *Hoy vive en el cliente porque es donde se consume; declarado
para que no nazca una segunda copia.*

### ⑤ H-007 — no tomada en este lote. Sigue con dueño C y su medición en §3.

---

## §3quater · L1 — LA VITRINA A DOS COLUMNAS, MONTADA

**Consumiendo la pieza de B (`origin/pista-b` `917e5129`), no clonando nada:**
`GRILLA_DE_DOS` + `CELDA_DE_GRILLA` + `<TarjetaProducto/>` + `PrecioText`.

**Lo que entró:** grilla de 2 · foto 4:3 · nombre **curado** en 2 líneas ·
presentación en su propia línea · **el `+` que agrega sin abrir el detalle** y muta a
stepper · **sin stock DENTRO de la tarjeta** (H-002 curado: el aviso dejó de colgar
fuera) · **el precio por kilo** (N19 ③) — el dato que decide una compra de alimento y
que casi ningún catálogo pone. **Sale de `peso_kg` y solo se dice cuando existe:** sin
peso no hay cuenta, y una cuenta sobre un peso ausente es peor que el silencio.

### 🔴 R7 ME FRENÓ, Y NO ERA PROLIJIDAD

Mi primera versión animó la entrada con un `FadeInDown` a mano. **R7 dio rojo: *«la
entrada tiene UN portador (`Entrada`)»*.** Fui a leer la pieza antes de discutir la
regla, y la regla tenía razón por una razón que yo no había visto:

> **`Entrada` mira `useReducedMotion` Y `memorial`; mi `Animated.View` no miraba
> ninguno de los dos.**

⇒ **quien pidió menos movimiento en su teléfono habría visto el desplazamiento igual,
y una familia en memorial habría recibido una vitrina animada.** *El lint no me corrigió
el estilo: me corrigió un defecto de accesibilidad y uno de respeto que yo no sabía que
estaba escribiendo.* Curado usando la pieza; **R7 vuelve a su baseline 2/2**.

El `orden` va **topeado en 8**: sin tope el producto 40 espera detrás de 39 escalones —
*un stagger sin techo deja de ser ritmo y pasa a ser latencia*.

### ✅ EL CORTE SE CERRÓ EN LA MISMA SESIÓN — LAS TRES PUERTAS, UNA PIEZA

**B entregó la prop (`origin/pista-b` `9f2cd2f4`) y CAMBIÓ SU FORMA con mejor razón
que la mía, que es lo que hay que registrar.** Yo pedí `advertencia?: string`. B midió
que eso **reabre un agujero que la casa ya había cerrado**: con un texto opcional,
`undefined` es **silencio legal en TODOS los casos** — incluido `ausente`, que
significa *«no tenemos los ingredientes»* y **tiene que hablar**. `AvisoAlergia` había
cerrado eso por construcción (*«la pantalla no tiene prop para hacerla callar»*), y mi
forma habría creado **una segunda manera de decir alergia con menos estados**.

Lo que quedó recibe **hechos tipados** (`composicion` + `coincidencia` + `senal`) y
**decide el silencio con `alergiaPuedeCallar()`, extraída y COMPARTIDA con
`AvisoAlergia`** ⇒ *las dos superficies no pueden discrepar sobre cuándo callar.*
**Mi argumento un piso más abajo, y B lo vio antes que yo.**

☠️ **Con la prop viva murió el andamio entero (Ley 37): el `modo`, `filaProducto` y su
`Separador`.** Y con ellos **un viaje de red**: `listarAlergenos()` existía para darle
voz a `vozAlergeno` en la fila; **las voces ahora viajan resueltas por el motor**
(`declarado_nombre`/`origen_nombre`, que es `cat_alergenos.nombre_es`). *No se borró un
fetch por prolijidad: se quedó sin consumidor cuando la voz pasó a viajar con el dato.*

**Y de paso subió de calidad, no solo de forma:** la fila cruzaba con
`alergenosQueCruzan` (coincidencia **exacta y nada más**); la tarjeta usa
`cruzarConVigilados`, que distingue **`exacta` de `imprecisa`** — *«contiene pollo»* de
*«podría contener proteína de ave»*. **La búsqueda advertía de menos y nadie lo había
medido.**

### 🔴 LA DECISIÓN QUE TOMÉ CON SU MEDICIÓN: la señal va SOLO con mascota en contexto

`alergiaPuedeCallar` habla en todo lo que no sea `verificada`/`no_aplica`. **Medido
sobre la vitrina viva antes de decidir:**

| `composicion_estado` | n | % |
|---|---|---|
| `ausente` | **288** | 51,2 % |
| `declarada_sin_verificar` | **274** | 48,7 % |
| `no_aplica` | 1 | 0,2 % |
| `verificada` | **0** | 0 % |

⇒ **562 de 563 (99,8 %) hablarían.** Pasar la prop sin mascota pondría una banda en
**prácticamente todas** las tarjetas del catálogo — *y un aviso que aparece siempre
enseña a ignorar los avisos.*

**No es una excusa para ablandar la regla: es dónde la regla APLICA.** La letra de S96
dice que el silencio se lee como *«no tiene pollo»* — **esa lectura solo existe cuando
hay un pollo del que preocuparse**, o sea cuando hay una mascota y estamos afirmando
algo sobre SU seguridad. Sin mascota no se afirma nada, así que no hay silencio que
malinterpretar.

⚠️ **H-008 (dueño: MESA + founder) — LO QUE SÍ QUEDA Y NO LO DECIDO YO:** con mascota,
**~51 % de lo RECOMENDADO va a mostrar «Sin composición declarada»**. **Ahí es correcto
y es lo más importante de todo esto:** *la familia lee «recomendado para Thor» como
«seguro para Thor», y sin ingredientes no podemos sostener esa lectura.* **El número
alto es el dato malo hablando, no la regla fallando** — se cura cargando composiciones,
**jamás callando**. Se sirve porque una vitrina donde la mitad de lo recomendado lleva
banda ámbar es una decisión de producto, no de pista.

### 📎 EL CORTE QUE EXISTIÓ UNA TANDA (se conserva porque la razón sigue valiendo)

| puerta | forma | por qué |
|---|---|---|
| vitrina | **grilla** | se ve sin mascota ⇒ no hay contra qué advertir |
| recomendación | **grilla** | el motor **excluye duro**, con verificación fail-closed |
| **búsqueda** | **filas** | **`MODELO_DESPENSA` §5.4: *«advertencia dura en la BÚSQUEDA»*** |

**`TarjetaProducto` no muestra alérgenos por decisión de su autora** (*«medio dato de
alergia es peor que ninguno»* — correcta para una tarjeta densa). **Migrar la búsqueda
hoy borraría una advertencia de salud FIRMADA**, y colgar el aviso fuera de la tarjeta
sería exactamente **H-002**, el huérfano que mi censo midió en 80 de 563 filas.

**No lo resolví por mi cuenta: pedí la prop a B con el caso concreto** (una línea que
diga que hay conflicto, con el texto ya resuelto por el consumidor — la pieza no conoce
el expediente). **Cuando exista, el `modo` muere y las tres puertas montan la misma
pieza.** *Ir más rápido acá costaba una advertencia de alergia; el corte es feo y es el
correcto.*

**⚠️ Y lo que L1 NO cumple todavía, sin maquillar:** la firma **«UN PRODUCTO, UNA VEZ»**
sigue esperando la cadena (**H-001, dueño A**). La contención de A mata el precio que
cambia al tocarlo; **la lista sigue mostrando los 25 duplicados.** *Eso es de la cadena,
no de la tarjeta.*

---

## §3quinquies · L2 — LA FICHA EN EL ORDEN N19

**El orden quedó ① foto · ② nombre + presentación · ③ precio · ④ advertencia +
composición · ⑤ para quién sirve · ⑥ disponibilidad.** La advertencia **encabeza ④** en
vez de ir después de la composición: quedan pegadas como pide N19 y **el dato de salud
se lee antes**, no después de la lista de ingredientes.

**Lo que entró:** el nombre **curado** (ahora desde `packages/ui`) · las presentaciones
como **chips** (`SelectorOpcion` tira, **colapsan con una sola** — 83 % del catálogo) ·
**el precio con `PrecioText registro="ficha"`** y **el $/kg debajo** · **⑥ disponibilidad**
con `hay_stock` **booleano** y las presentaciones sin oferta **dichas en una línea, no
como filas tocables** (*no son una opción, son una ausencia* — Ley 23).

☠️ **Murió la tabla de presentaciones en `Celda`**: los chips + el bloque de precio hacen
su trabajo sin repetir el precio en cada fila.

### 🔴 DOS PIEZAS DE LA LETRA DE L2 QUE **NO CONSTRUÍ**, Y NO ES PEREZA: NO TIENEN OBJETO

**① EL ACORDEÓN DE PROSA.** La letra pide colapsar *«prosa descriptiva: beneficios,
características, descripción de marca»*. **Medido sobre los 470 activos: `descripcion`
tiene promedio 10 caracteres, máximo 29, y CERO sobre 200.** Sus literales:
«Linea Dorada» · «Veterinary Diet» · «Gatos» · «Canine» · «Premium».

⇒ **no es prosa: es una sub-línea del importador.** **No hay nada que colapsar**, y un
`+` sobre una etiqueta de 10 caracteres **escondería un dato en vez de ordenar una
parrafada**. *El acordeón se construye el día que exista prosa; hoy sería un control sin
trabajo.* **Lo que sí hice: dejar de pintarla como párrafo de cuerpo** —eso le prometía
al lector una descripción que no existe— y moverla junto a la marca.

**② EL CARRUSEL CON DOTS.** Medido: **NINGÚN producto tiene más de una foto**
(`max_fotos` = 1, `galeria_mas_de_una` = **0**). **Un carrusel manual con dots necesita
≥2 fotos** ⇒ serían dots que nunca aparecen y un swipe que nunca se mueve. Se conserva
el hero, que es lo que el dato admite.

### 🔴 H-009 (dueño: FOUNDER) — EL DATO QUE TOCA LA PREMISA DE L1

**309 de 470 productos (66 %) NO TIENEN NINGUNA FOTO.** La firma de L1 dice *«dos
columnas es correcto para navegar catálogo **con foto**»* — y hoy **dos tercios de la
grilla va a mostrar el marcador de ausencia**. *Una grilla de fotos donde 2 de cada 3 no
tiene foto no es una grilla de fotos.* **La decisión de dos columnas sigue siendo
correcta para el catálogo que queremos; se sirve el número porque el catálogo que
tenemos todavía no es ése.** Se cura cargando fotos, y es del mismo lote que las
composiciones (H-008) y los nombres sin curar (H-005): **tres caras del mismo dato
crudo.**

---

## §3sexies · LA UNIÓN `compra`, Y UN ERROR PROPIO DECLARADO

**B rompió mi montaje a propósito y tenía razón**, con mi propio argumento en mi contra:
el espejo del vendedor **no tiene carrito**, y con props sueltas obligatorias había que
pasarle `cantidad={0}` y un `onAgregar` vacío **solo para que el `+` no se dibujara** —
*mentir una prop para lograr una combinación legítima*, que es lo que la cabecera de la
propia pieza daba como razón para no reusar `Baldosa`.

**Y unión en vez de props opcionales, que era la salida que no me rompía:** con
opcionales, **olvidar el carrito en la vitrina real compilaría y el `+` desaparecería en
silencio** — *el mismo agujero que B me rechazó en mi `advertencia?: string`.* **La forma
tiene que hacer imposible el olvido, no confiar en que nadie olvide.**

### 🔴 ERROR PROPIO, DECLARADO: corrí `prettier` sin medir la configuración de la casa

Al migrar corrí `npx prettier --write` sobre el archivo. **No hay `.prettierrc` en el
repo**, así que corrió con **sus defaults** y **cambió las comillas simples de la casa a
dobles en las 780 líneas**. tsc y `verify:diseno` daban **VERDE igual** — *un diff de 780
líneas que pasa todos los semáforos es exactamente la clase de ruido que hace
irrevisable una revisión.*

**Curado revirtiendo el archivo y rehaciendo el cambio a mano: el diff pasó de ~780
líneas a 37/23.** *La lección propia: una herramienta de formato sin configuración del
proyecto no es neutral — impone la suya, y el costo lo paga quien lee el diff.*

---

## §3septies · H-007 — LA MEDICIÓN CORRIGIÓ MI PROPIO HALLAZGO, Y DOS VECES

Fui a curar los *«~300 ms encadenados»* que yo mismo había reportado. **Medir antes de
tocar cambió las dos mitades del diagnóstico.**

### ① NO SON DOS VIAJES: SON TRES

`obtenerMascotasDeFamilia` **hace un segundo viaje adentro** (`cat_razas`, condicional a
que alguna mascota declare raza). La cadena real del montaje es:

```
getEstadoOnboardingDueno()  →  obtenerMascotasDeFamilia()  →  cat_razas
```

⇒ **~450 ms seriales, no ~300.** *Mi propio número estaba bajo porque conté los viajes
que veía en la pantalla y no los que hace el wrapper que llamo.*

### ② 🔴 LA CURA OBVIA ERA UN CAMBIO DE RESULTADO DISFRAZADO DE OPTIMIZACIÓN

La salida evidente era **sacar el `.eq('familia_id', …)`**: si la RLS ya limita a mis
mascotas, el primer viaje existe solo para conseguir un id que el servidor ya conoce.
**Medido en `pg_policies`, las SELECT de `mascotas` son tres y NINGUNA gatea por
familia:**

| policy | predicado |
|---|---|
| `mascotas_select_admin` | `is_admin()` |
| `mascotas_select_codueño` | `_user_es_codueño_mascota(id, auth.uid())` |
| `mascotas_select_prestador_con_acceso` | `user_tiene_acceso_a_mascota(id)` |

⇒ **sacar el filtro le mostraría a un usuario que además es prestador las mascotas que
atiende, mezcladas con las suyas, en su propio hogar.** No es más rápido: **es otra
cosa.** *Es exactamente la trampa de S94-PERF —copiar la fórmula inline daba valores
reales y era un cambio de resultado— y la esquivó leer las policies, no razonar sobre
ellas.*

### ⇒ CEDIDA A A, con su forma

La única cura sana es **una RPC de contexto del hogar** (familia + mascotas + razas en un
viaje), que es motor. **No la construyo desde el cliente y no toco el wrapper**: el
`.eq` es el que hace que el lector signifique *«las mascotas de MI hogar»* y sacarlo
rompe esa promesa en silencio.

---

## §4 · COLA VIVA — con próximo paso EJECUTABLE

**Al cierre no queda trabajo mío en vuelo. Lo que sigue es de otros o del aparato:**

1. **Consumir las DOS TEMPERATURAS de la banda de alergia.** **No cambia mi API** —
   sigo pasando los mismos hechos tipados; la temperatura la deriva la pieza con
   `temperaturaDeAlergia()` (tres valores: `alarma · ausencia · silencio`). **Disparo:**
   el aviso de B de que está servida. **Paso ejecutable:** `git merge origin/pista-b`,
   `tsc`, `verify:diseno`. *No se reinventa: se consume.*
2. **El GATE VISUAL DEL FOUNDER**, que es lo único que yo no puedo emitir:
   · la **grilla de dos columnas** con **66 % de tarjetas sin foto** (H-009) — *¿sigue
     leyéndose como vitrina?*
   · el **nombre curado** en dos líneas y **~18 % cortado** hasta que se curen los datos;
   · la **ficha en orden N19** y la **advertencia encabezando ④**;
   · y en la galería, el caso `espejo` **al lado** de los de vitrina — *si el nombre no
     se truncara ahí, el espejo estaría mintiéndole al vendedor.*
3. **H-006** — las cuatro voces de familia que faltan (`higiene`, `acondicionador_agua`,
   `heno`, `sustrato`: 30 productos sin etiqueta). **Chico y mío**, sin bloqueo.

## §5 · COMPROMISOS — al cierre

**Qué debo:** nada pendiente. Entregado: el censo de alérgenos con su cero medido · los
números del lint a B **con la objeción a mi propio pedido** · H-005/H-008/H-009 servidos
a la mesa · H-007 cedida a A **con su medición y su trampa adentro**.

**Qué me deben, con dueño:**
- **B** → el aviso de las dos temperaturas. ✅ **El veredicto del lint YA LLEGÓ y es NO SE
  ESCRIBE** — ver §8.
- **A** → **H-001** la cadena de selección (frenada con medición: no hay dónde leer la
  calificación) · **H-003** `unaccent` en el servidor · **H-007** la RPC de contexto del
  hogar.
- **FOUNDER** → el gate visual · y **el lote de datos crudos: 66 % sin foto · ~51 % sin
  composición · 42 % con nombre crudo** — *tres caras del mismo dato, no tres defectos de
  forma.*

## §6 · LO NO HECHO, SIN MAQUILLAR

- **La firma «UN PRODUCTO, UNA VEZ» NO se cumple.** La contención de A mata el precio que
  cambia al tocarlo; **la lista sigue mostrando los 25 duplicados**. Es de la cadena, y la
  cadena está frenada **con su medición** (sin columna ni tabla de calificación).
- **El acordeón de prosa y el carrusel con dots NO se construyeron**, y no por falta de
  tiempo: **medidos, no tienen objeto** (descripción de 10 caracteres promedio · ningún
  producto con más de una foto). *El día que exista prosa o una segunda foto, se
  construyen; hoy serían controles sin trabajo.*
- **El techo de 50 sigue en 50.** Ahora **lo dice**; el cursor es **D-843** y necesita su
  decisión de forma.
- **H-007 no se curó por mí** — es motor, y la cura obvia era un cambio de resultado.
- **NADA se probó en aparato.** Todo lo de este parte está verificado por **instrumento**
  (tsc · `verify:diseno` 43 reglas · consultas a la base viva) y por **medición contra el
  objeto**. *El juicio visual es del founder y no me lo atribuyo.*

## §7 · ESTADO OPERATIVO AL CIERRE

- **Rama `pista-c` en origin, verificada POR SHA** (`git ls-remote` = `git rev-parse`).
- **`main` mergeado hasta `92d3d65a`** — sin conflictos, sin re-aplicar la cura cedida.
- **Solo lectura sobre la base en toda la sesión**: cero escritura, cero migración, cero
  reconstrucción (guardarraíl §0quater respetado).
- **⚠️ Metro con `--clear`** en la próxima corrida: la sesión tocó `packages/*` varias
  veces.

## §8 · EL LINT DE CRUCE DE ALÉRGENOS: **NO SE ESCRIBE** — veredicto cerrado

**Pedí la regla y pedí explícitamente que se dijera si no se podía. B lo midió y la
respuesta es que no, por las DOS razones que yo mismo había puesto en el pedido:**

① **Un regex no distingue los dos `.some`.** El del motor —`p.alergenos.some((a) =>
prohibidos.has(…))`— **es la defensa**: la verificación fail-closed sobre el conjunto ya
expandido. El otro sería un cruce a mano que se saltea la expansión. **Misma forma,
propósitos opuestos** ⇒ la regla gritaría **sobre el único lugar donde el patrón
protege**. *Un rojo falso permanente es más corrosivo que un verde falso puntual: enseña
a ignorar el instrumento.*

② **Nacería sin sujeto.** El censo dio **CERO ofensores** — la única que cruzaba de menos
ya está muerta. **Una regla en 0 que además no puede distinguir el caso legítimo no
vigila nada y encima grita.**

> **⇒ LO QUE PROTEGE DE VERDAD NO ERA LA REGLA: ERA MATAR LA FUNCIÓN.**
> *Una función que cruza de menos, exportada y sin consumidores, es una trampa cebada —
> el próximo la encuentra por autocompletado y la usa creyendo que es la de la casa.*

**Lo único mecanizable de verdad** —exigir que las tres superficies usen
`cruzarConVigilados`— **necesita saber qué devuelve la expansión, o sea la base**: su casa
es **`verify:censo`**, no `verify:diseno`. **Servido a quien lo tenga; no lo construyo
desde acá.**

**Y el residuo del intercambio, que queda como aporte y no como mío:** mi nota sobre
`galeria[0]` —la aparición **legítima** de `x[0]`, donde **el orden ES dato** y N19 le da
significado explícito a la primera foto— **entró a la cabecera de R50 como exención
NOMBRADA**. *Un baseline dice «hay 2 y no sé por qué»; una exención nombrada dice cuál y
por qué.* Con eso R50 quedó con su límite escrito: **su verde dice «acá alguien decidió un
orden», jamás «el orden es el correcto».**

---

## §9 · LAS DOS LEYES QUE LA MESA DEPOSITÓ AL CERRAR — con su evidencia

*Se registran acá, en el parte, porque salieron de este trabajo y porque quien lo herede
tiene que encontrar la síntesis, no solo la lista de hallazgos.*

### 🔴 ① LA LEY MADRE DE S100

> **Las tres veces que la medición corrigió a quien la hizo, las encontró CORRER EL
> INSTRUMENTO CONTRA EL OBJETO — no leer el código con más cuidado.**

**Mis tres, con su literal:**

| # | qué afirmaba yo | qué dijo el objeto |
|---|---|---|
| ① | `nombreCurado` estaba lista | corrida contra 26 nombres vivos salió **«Control DE Peso»** — mi regla de siglas se adelantaba a la de enlaces |
| ② | H-007 eran **dos** viajes, ~300 ms | son **TRES** — el wrapper hace un segundo viaje adentro (`cat_razas`): ~450 ms |
| ③ | sacar el `.eq('familia_id')` era la cura obvia | `pg_policies`: **ninguna** SELECT de `mascotas` gatea por familia ⇒ era **un cambio de resultado disfrazado de optimización** |

**Ninguna de las tres aparece leyendo mejor.** Y es la síntesis de la sesión entera: el
índice inexistente citado en un comentario · la tabla con policy y sin escritor · el token
legado con nombre plausible · el grep que dio 0 donde había 21 — **todos son lo mismo: el
código afirmaba, y nadie había corrido el instrumento contra el objeto.**

### ② EL FRENO SE REGISTRA CON SU RAZÓN Y SU MEDICIÓN, O NO ESTÁ REGISTRADO

> **Una decisión de NO-CONSTRUIR que no queda escrita se vuelve a proponer en tres
> sesiones.**

Por eso en este parte **ningún «no» viaja solo**: el lint lleva **sus dos razones** (§8),
el acordeón y el carrusel llevan **sus números** (§3quinquies: 10 caracteres promedio ·
ningún producto con más de una foto), y H-007 lleva **la trampa que esquivó** (§3septies).
*Un «no alcanza» sin medición adentro es permanente por accidente; uno con su número tiene
fecha de vencimiento.*

---

## §10 · CIERRE

**Nada abierto de mi lado.** Esperando el aviso de B (las dos temperaturas, que **no
cambian mi API**) y **el gate visual del founder**, que es la mitad que yo no puedo
emitir. **Nada de este parte se probó en aparato.**
