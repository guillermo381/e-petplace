# S104-C · parte de la pista

**Territorio:** `apps/cliente` y `apps/prestador` (cuenta, login, recuperación).
**Worktree:** `../e-petplace-s104-c` · rama `pista/s104-c` (nace de `main` @ `0d0c2e32`).
**Turno 1: SOLO MEDICIÓN — cero código, cero migraciones. Nada commiteado.**

## Estado
Turno 1 cerrado. Censo A/B/C/D medido contra la base viva (proyecto
`zyltipqscdsdsxnjclhp`, vía CLI linkeado). El reporte al founder salió en el
turno. Nada pendiente de commitear (turno de medición).

## Lo medido (resumen; el detalle está en el reporte)
- **Dashboard:** Auth Providers = solo Email (dato del founder, no re-medido acá).
- **8 cuentas solo-Google confirmado por SQL:** `auth.identities.provider='google'`
  único, `encrypted_password IS NULL`, `email_confirmed_at` no nulo, las 8. Lista
  en `scratchpad/out1.json`. De 165 usuarios: 153 email · 8 google · 3 email+google
  · 1 sin identity.
- **B (login):** ningún botón Google/Apple en ninguna de las dos apps (grep global
  = 0 OAuth). Las dos tienen "olvidé mi contraseña" → `/recuperar`. Prestador
  además tiene "crear cuenta" → `/registro`; cliente no.
- **D (scheme):** cliente `scheme: "cliente"` · prestador `scheme: "prestador"`
  (`app.json`; ningún override en `app.config.ts`). El flujo de recuperación usa
  OTP por CÓDIGO (`verifyOtp` con `token`), no magic-link — cero `emailRedirectTo`/
  `redirectTo`/`createURL` en todo el árbol. El deep-link de app hoy NO participa
  del reset.
- **C (la prueba de las 8):** el motor está partido en dos pasos (D-659) y sano
  por lectura. NO pude completar el end-to-end: el código de 8 dígitos llega al
  BUZÓN del founder y no lo tengo; canjearlo por Admin API sería escritura sobre
  cuenta real (prohibido este turno). Punto de corte y guion para el founder en
  el reporte.
- **Hallazgo A que corrige a la mesa:** `familia.tsx` NO promete "co-dueño" en
  pantalla — el string es "Invitar a alguien de tu familia · Pronto" / "Invite
  someone from your family · Soon" (`es.ts:1395`, `en.ts:1198`). Los que dicen
  "co-dueño" son los COMENTARIOS del código (`familia.tsx:5,147`). La pantalla ya
  está alineada con la firma 5.1; el drift vive en el comentario.
- **No existen (medido, grep global = 0):** cambiar correo · sesiones activas /
  cerrar en otros dispositivos · exportar datos — en NINGUNA de las dos apps.

## Compromisos / frenos
- Ninguno con otras pistas.
- Turno de medición: no escribí en la base, no mandé correos, no toqué código.
- Copié `supabase/.temp` del worktree primario para linkear el CLI en este
  worktree (solo estado de link, no es un cambio versionado).

## TANDA 2 (S104-C) — estado al cierre del turno

**Pusheado en `pista/s104-c`** (typecheck verde en todo; los commits con SALTAR_GATE
son por R63·C ambiental — rutas nuevas /baja e /invitacion sin regenerar el
router.d.ts, que A regenera en el build):
- `e63003cf` — **co-dueño → familiar autorizado** (firma 5.1): strings de la Hoja
  de eliminar (es/en) + comentarios. Freno del canon cerrado.
- `6b946ab4` — **superficie de invitar a la familia** (motor de A): correo + copiar
  enlace, solo titular, familiar autorizado, sin permisos. expo-clipboard declarado.
- `fba262f3` — **invitación con 3 estados** (`correoSuprimido`) + **`/baja?t=`** viva
  (un clic, sin sesión, contesta igual siempre) + enlace alineado a `/invitacion?token=`
  con `EXPO_PUBLIC_APP_BASE_URL`.
- `3995b054` — **pantalla `/invitacion` del invitado** (3 láminas + aceptar con token;
  respeta email_no_coincide y familiar autorizado; retome v1 declarado).

**Rutas para `URL_APP_BASE` (reportadas a A):** `/invitacion?token=` y `/baja?t=`.
La base la fija el founder (misma en app y transporte).

### Relevamiento legal (medido) — los 2 checks BLOQUEADOS
- `/terminos` (parent) y `/privacidad` (web) vivos, v1.1, pero **contenido borrador**.
- Estructura firmada: **2 términos** (parent `/terminos` + professional
  `/terminos-plataforma`) + **1 privacidad común**. A alineó `DocumentoLegal =
  terminos_parent | terminos_professional | privacidad`.
- 🔴 **Bloqueado por el abogado:** `terminos-plataforma` (38 huecos), `privacidad-app`
  (24 — la que cubre las apps; la web se autoexcluye), aviso-IA (6 huecos de
  formulación). **No cablear checks a documentos que no existen** (firma founder).

### Aviso de IA — mediciones entregadas + firma
- **Punto 1 (enumeración):** 3 funciones vivas usan IA — estructurar nota clínica,
  leer carnet, redactar presencia. Coach = plantillas (fuera hasta que use IA).
  chat-ayuda y extract-documento existen pero SIN consumidor (apagar).
- **Punto 4 (verificado):** no hay decisiones exclusivamente automatizadas con
  efectos jurídicos — las 3 son asistivas con revisión humana obligatoria.
- 🔴 **Ficha con disparo:** el día que el Coach llame a un modelo, el aviso sube de
  versión ANTES de que la función esté viva — en el mismo acto.

### Cola de tanda 2 pendiente (OTA, no bloquea el build) — para el próximo turno
- **Pantallas de código** (verificación de correo): `confirmarAltaConCodigo` (motor de
  A), reusar CampoCodigo, reenviar con cuenta regresiva 60s, detrás del flag. El
  consentimiento **se MUESTRA en el formulario y se PERSISTE al confirmar el código**
  (firma A: sobrevive al flip del autoconfirm). D-893.
- **Biométrico**: candado sobre la sesión (NO factor de auth). Pieza `PantallaDeCandado`
  de B lista + `expo-local-authentication` instalado. Interruptor en Seguridad de las
  DOS apps; chequeo al volver a primer plano; fallback SIEMPRE al login; sin biométrico
  enrolado el interruptor no aparece.
- **Pegar en CampoCodigo** (pieza de B, `getStringAsync`).
- **Google** (botón, solo cliente, espera el client ID de A).
- **Bug de registro de push de D** (InvitacionAvisos: leer {ok,codigo}, reintentar con
  sesión lista, catch que distinga "es web" del fallo real; las dos apps).
- **Los 2 checks**: esperan las URLs legales finales (abogado).

---
## Turno 2 — CONSTRUCCIÓN COMPLETA (tanda 1, para el merge de A)

### Estado: LISTO PARA ENTREGAR. typecheck cliente + prestador + api en 0.
### verify:diseno: verde salvo 2 fallos R63·C AMBIENTALES (ver abajo).

**Base:** mergeado origin/main (9f29ce65 → B, A, D). Las piezas del ritual
(`PaseoDeHuellas`, `RespiroDeMarca`, `HuellaDeLlegada`, el ojo en `Campo`) y el
wrapper de A viven en main y se consumen reales. Conflicto resuelto: B tocó mis
4 archivos del arco (gap/ghost/MarcaDeAgua) — mi reescritura del ritual es
superconjunto de eso, tomé mi versión (incluye todo lo de B + senda + isotipo +
huella + autofill + consentimiento).

### Construido (las 8 pantallas del arco + cableado + paridad)
- **Cliente (ceremonia completa, RITUAL §2-§5):** bienvenida (isotipo que respira
  + senda + manifiesto con Entrada + acciones al pie + wordmark `e.petplace`→
  `e-PetPlace` + ghost) · login/registro/recuperar (tapiz + MarcaDeAgua + senda +
  isotipo recogido en la esquina + N26 + autofill + **huella de llegada** al
  autenticar/validar). Registro: **línea de términos + consentimiento** (vía
  `registrarse({ urlLegalMostrada })`, que ya lo registra) + ghost «ya tengo cuenta».
- **Prestador (dosis §7, enmienda firmada):** login/registro/recuperar con senda +
  isotipo recogido, **SIN respiración ni celebración de llegada**. Registro con
  términos + consentimiento. Autofill en todos.
- **Paridad (ley de cuenta):** términos visibles en solicitar-acceso e invitación;
  **la invitación registra el consentimiento** (`registrarConsentimiento(userId,
  'acceso_prestador', …)`) al aceptar, best-effort (no bloquea la entrada).
- **Autofill (punto 5):** `current/new-password` + `textContentType` en todas las
  claves; `username`/`email`/`name` en sus campos; `oneTimeCode` ya viene en
  `CampoCodigo` de B. También en cambiar-clave (seguridad, las dos apps).
- **☠️ 4 strings del correo en inglés muertos** (D-628 cerrada por medición).

### Lo que le PIDO a A/B para el merge (declarado, no de contrabando)
1. **Ratificar el hunk aditivo a `packages/api/src/index.ts`** (76(c)/(d)): exporté
   `registrarConsentimiento`, `normalizarEmail`, `TipoConsentimiento` — estaban en
   `auth.ts` pero no en el index (inalcanzables). La invitación los necesita.
2. **Prop `estatica` en `PaseoDeHuellas` (pedido a B):** la senda se traza al
   montar. §7 pide la del prestador «YA trazada y quieta». Sin esa prop la monté
   tal cual (se traza suave); con ella, el prestador la pasaría `estatica`.
3. **R63·C ambiental:** falta `.expo/types/router.d.ts` (se genera con `expo
   start`; worktree fresco). Mis rutas son todas existentes — no inventé ninguna.

### Refinamiento declarado, NO forzado (necesita firma)
La continuidad shared-element del RITUAL §3 («el isotipo VIAJA a la esquina» + «la
senda PERSISTE entre pantallas») exige un layout compartido de las rutas de auth
— y viven sueltas en `app/` (agruparlas toca deep-links y el guard raíz). La monté
**per-pantalla** (isotipo en su posición, senda en las cuatro): da el efecto sin
reestructurar la navegación. El shared-element pixel-perfect queda propuesto aparte.

### No tocado (frenos vigentes)
- `familia.tsx`: el choque co-dueño vs familiar autorizado está servido al founder.
  La pantalla ya dice «Invitar a alguien de tu familia» (alineada con 5.1); el
  drift vive en los COMENTARIOS. No tocado.
- El splash nuevo + wordmark del prestador «e-PetPlace Negocios» van en la build de
  tanda 2 (binario). El prestador no tiene pantalla de bienvenida donde ponerlo hoy.

---
## Turno 2 — (registro previo, superado por lo de arriba)

### Hecho y verde (independiente del RITUAL) — typecheck cliente+prestador en 0
- **☠️ Los 4 strings del correo en inglés, MUERTOS** (Ley 37): `recuperar.avisoCorreo`
  en cliente es/en y prestador es/en, + sus 2 usos en `recuperar.tsx`, + la cabecera
  ③ de `cliente/recuperar.tsx` que decía «se conserva». D-628 cerrada por medición
  (la prueba de satorilatam de este mismo loop).
- **Términos VISIBLES en las dos puertas de entrada del prestador** (paridad, firma
  founder 23-ago): `solicitar-acceso.tsx` (al pie, anclada con spacer) e
  `invitacion.tsx` (bajo «Entrar»). Key nueva `bienvenida.terminos` (es+en),
  neutral («al continuar»), honesta sin link (D-336).
- **Autofill en CAMBIAR CLAVE** (punto 5, no-ceremonia): seguridad cliente+prestador
  — `current-password` en la actual, `new-password`+`newPassword` en las dos nuevas.

### BLOQUEADO — las 4 pantallas de la CEREMONIA de entrada
bienvenida · login · registro (×2) · recuperar (×2). Su layout y motion los
gobierna `RITUAL_DE_ENTRADA.md`, que **NO está en el repo** (ni main ni ramas
A/B/D). Sin él no invento la coreografía (300/120/translateY 15, escala 0.4,
respiración, la huella de llegada «de B»). Falta además el **reparto C↔B** de las
piezas de motion (PaseoDeHuellas, senda, transición del isotipo, huella de llegada).
Cuando lleguen el doc + el reparto, construyo el núcleo entero de una: tapiz +
MarcaDeAgua + senda + continuidad del isotipo + wordmark `e.petplace`→`e-PetPlace`
+ autofill de login/registro/recuperar + términos en registro + la llamada al
wrapper de consentimiento de A.

### Dependencia de A declarada (para el merge)
El **wrapper de consentimiento** no existe en `packages/api` (medido). Contrato que
le pido a A: `registrarConsentimiento({ tipo }): Promise<ResultadoWrapper<null>>` —
A resuelve versión vigente, `user_id` de sesión e `ip_hash`; se escribe en la tabla
`consentimientos` (que ya existe, 0 escritores hoy). Se llama tras registro exitoso.
Mi cableado de esa llamada vive dentro del núcleo bloqueado ⇒ no rompe typecheck hoy.

### Hallazgo que corrige a la mesa (turno 1, sigue vigente)
`familia.tsx` NO promete «co-dueño» en pantalla: dice «Invitar a alguien de tu
familia · Pronto». El drift vive en los COMENTARIOS del código (`:5`, `:147`), no en
la UI. No tocado (v2, firma founder).
