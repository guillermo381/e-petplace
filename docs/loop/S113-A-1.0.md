# S113-A · LOTE 1.0 «El carnet dice la verdad» — parte de pista

**Rama:** `pista/s113-a-1.0` · **SHA:** se declara al pie · **A1–A5 entregados.
A6 FRENADO: falta E.**

## Lo medido ANTES de tocar, y contra qué

Todo contra los **catálogos de Postgres de la base viva** (9-sep), nunca contra
un archivo de migración ni un arnés. Cada cero con su control positivo.

| pregunta | respuesta | control |
|---|---|---|
| ¿qué columnas tiene `evento_vacuna_aplicada`? | faltaban `laboratorio` y `vencimiento_biologico`; el resto del brief ya estaba (`lote`, `via_administracion`, `fecha_proxima`, `veterinario_nombre_externo`, `dosis`, `tipo_vacuna`) + `vacuna_codigo` | `lote` **está** ⇒ el instrumento lee esta tabla |
| ¿`registrar_vacunas_de_carnet` estampa `extraido_por_ia`? | **SÍ**, verificado en `pg_proc` | — |
| ¿existe `plagas`? | **no** | `lote` sí |
| ¿existe `cat_plan_vacunal`? | **sí**, tabla, perro 5 · gato 4 | `cat_vacunas` existe |
| ¿qué usa la casa para el «hoy» de la familia? | **NADA. No existe.** → `D-1029` | ver la ficha |

## A1 · A2 — el carnet fino (`20260909060000`)

Dos columnas nuevas y la puerta que las acepta. **Dos capas y no una, porque
son dos trabajos:** el CHECK vuelve inexpresable un vencimiento anterior a la
aplicación; el guard tipado lo **explica** con el índice del ítem. Y el CHECK
además cubre al **otro** productor, que existe y está medido —
`registrar_vacuna_mostrador`, DEFINER, que no pasa por esta puerta.

`via` y `via_administracion`, las dos, con su razón: la columna se llama de una
forma, el brief nombra la otra, y **la edge de hoy no emite ninguna** (su
contrato, `index.ts:133`). *Aceptar sólo una obliga a adivinar ahora lo que se
firma después.*

**Fixture 6/6**, con discriminador: el mismo ítem con `venc == aplicada` **pasa**
⇒ T2 rebotó por la regla y no por la forma.

## A3 — la plaga en el evento (`20260909080000`)

`plagas text[]` con CHECK. **`<@` y no `&&`**: con `&&` un array como
`{pulgas, basura}` PASA por compartir un elemento — la casa ya pagó por saberlo
en S95. Y el array vacío se cierra aparte: **`{}` no es NULL** — NULL dice «no
sé qué trataba», `{}` diría «no trataba ninguna», que no es un hecho posible.

`tipo_desparasitacion` **se conserva**: responde otra pregunta (dónde actúa el
producto), y «externa» no distingue pulgas de garrapatas.

**L-119 honrada:** la firma vieja de 6 parámetros se dropea explícita;
`sobrecargas = 1` verificado contra `pg_proc`. **Fixture 5/5.**

⚠️ **Estado del frente:** hay **cero desparasitaciones en toda la base**. La
puerta existe desde S82-A y nadie la usó.

## 🔴 El hallazgo del propio arnés — `L-491`

**La limpieza del fixture era un no-op silencioso.** Borraba como
`authenticated`, y esas tablas **no tienen policy de DELETE**: el `DELETE`
devolvió ok y no borró nada. **Quedaron dos filas en producción** hasta que lo
medí. Limpiadas con el rol que puede, verificado residuo 0 — y **con control
positivo**: las 8 vacunas reales de Thor intactas, o sea que no borré nada de
verdad.

## A4 — lo que ya viajaba y se tiraba en el mapeo

Vacunas ganan seis campos; condiciones y medicación dejan de colapsarse; entran
las restricciones activas con su descripción del catálogo, **en el mismo
`Promise.all`** (S94-PERF: el costo está en la petición, no en los datos).

🔴 **Los shapes de los jsonb NO se adivinaron.** El de `condiciones_cronicas` se
leyó **del productor** — el trigger `_trg_condicion_propagar_perfil` arma
`{condicion, cie_codigo, estado, fecha_diagnostico, evento_id}`. *Adivinar los
nombres de clave habría compilado igual y devuelto `undefined` en cada campo: el
modo de falla más silencioso que hay.* El de medicación, de las **cinco filas
reales** de Thor.

Y algo que se dice en vez de esconderse: **`hasta` viene NULL en las cinco**, y
no se deriva de `duracion_dias` porque `fecha_inicio` también es NULL. *Sumar
días a nada da una fecha inventada con cara de dato.* Viaja `duracion_dias`,
que es el único dato de duración que existe.

**El brief decía «tres lectores» de `tiene_condicion_cronica`. Medido abriendo
cada hit: de ESTE wrapper son DOS** (`hogar/mascota/[mascotaId].tsx` y
`components/coach.tsx`) **y hay CUATRO más** que leen el mismo nombre desde
otros cuatro wrappers — `despensa-catalogo`, `mascotasPrestador`,
`grooming-atencion`, `adiestramiento-antes`. **A ésos este cambio no los
alcanza**, y se declara para que nadie crea que los curó.

**El rojo por campo, con sesión real** (`scripts/s113a-a4-campos-del-perfil.mjs`):
con el select viejo las seis claves están **AUSENTES del objeto**; con el nuevo
están, con `lote='190L-01'` y `archivo_url` reales y el resto en null honesto.

⚠️ **Dos trampas propias, declaradas.** ① Partí los `select` con `+` y
`postgrest-js` devolvió `GenericStringError`, porque **infiere del literal** —
la misma trampa que `D-474` dejó escrita en S72 y que volví a pisar. ② Usé
`like` sobre una columna `uuid` y PostgREST devolvió **vacío sin error**: un
cero por instrumento, no por dato. **Lo cazó el control de esa misma línea.**

## 🔴 A5 — ERROR DE MÉTODO: ya existía, y mi duplicado la rompió

El brief pedía construir `obtenerPlanVacunal`. **Ya existía entero desde S82
r7** — motor `obtener_plan_vacunal(uuid)` y wrapper `salud.ts:196`, exportado.
Escribí una segunda función **antes de preguntárselo a `pg_proc`**, y no fue una
duplicación inocente: dejó **ambigua toda llamada de un argumento**, que es
exactamente lo que el wrapper vivo hace. *La rompí y la arreglé en la misma
hora; si hubiera pasado a un candidato, el plan vacunal dejaba de responder en
las dos apps.* Duplicado retirado, número **revertido en el ledger**.

**Es el mismo error del lote 0** (la puerta de desparasitación). Dos veces la
misma clase: *el brief nombra algo como nuevo y yo no le pregunto al catálogo si
ya está.*

**Y la vieja era MEJOR que la mía en tres cosas, que conservo:**
① gate por `user_tiene_acceso_a_mascota` y no por la RLS de `mascotas` — la mía
era **más angosta** y habría escondido el plan a un familiar legítimo;
② **la capturada gana a la derivada**, con `proxima_es_derivada` diciendo cuál
es cuál — yo derivaba siempre, o sea que **habría pisado con una cuenta nuestra
lo que el veterinario escribió**;
③ `aun_no_corresponde` por edad: *lo que todavía no toca no es una falta.*

**Lo único que agrego:** `exigida_guarderia` (columna que existía y no viajaba) ·
`vence_en` · `p_hoy` · `aplicadas_sin_clasificar`.

**El cruce es por `vacuna_codigo` y jamás por nombre:** lleno en **22 de 32**
filas, y los nombres reales son comerciales («Nobivac DHPPi», «Canigen LR») —
mapearlos es inferencia, no dato. `aplicadas_sin_clasificar` rescata del
silencio a las que quedan afuera; **Thor tiene 2**. *Sin ese número, una vacuna
que la familia SÍ puso se lee como «nunca_aplicada» y nadie se entera.*

**El umbral de `vence_en` son 30 días y va como ELECCIÓN, no como medición** —
no hay ninguno escrito en la casa. *Un número inventado que se presenta como
derivado es peor que uno inventado que se admite.*

### 🔴 La trampa que casi me come, encontrada mirando el wrapper vivo

`ESTADOS_PLAN` en `salud.ts` **es un FILTRO, no una declaración**. Un estado
nuevo en el motor que no entre ahí hace que el guard **descarte la fila entera**
(`continue`), con `ok` y sin log. Medido sobre tres filas: **con la lista vieja
sobreviven 1 de 3** — dos vacunas desaparecen sin error. `vence_en` entra en el
**mismo acto** en que nace.

**Fixture 10/10, con dos discriminadores limpios:** el mismo dato con `hoy` en
2023 da `al_dia` y con `hoy` en 2030 da `vencida` ⇒ **el parámetro rige**; y con
el mismo `hoy`, ventana 1 día da `al_dia` y ventana 3650 da `vence_en` ⇒ **el
umbral rige**. `sobrecargas = 1`, la llamada de un argumento sigue resolviendo,
mascota ajena rebota `no_access_to_mascota`, y **`fecha_proxima` intacta: cero
escrituras**.

## 🔴 A6 — FRENADO, y no por mí

El orden es **E → D → B → C**. Del objeto (`git ls-remote`, 9-sep):
`pista/s113-b-1.0` `522483b2` · `pista/s113-c-1.0` `06ec330a` ·
`pista/s113-d-1.0` `b6b42ca2` — **y de E no hay rama `-1.0`.**

Y el despliegue de `extract-vacuna` v2 pide dos cosas que no tengo: **el
veredicto de E** y **el modelo que E y el founder firmen**. *Desplegar con el
modelo que yo elija sería justamente la decisión que el brief le reservó a otros.*

## Lo que NO hice, sin maquillar

· **No mergeé nada** — falta E, y el orden es suyo primero.
· **No desplegué `extract-vacuna` v2** ni corrí una llamada real: sin veredicto
  ni modelo firmado, la fila de `ia_uso` que pegaría sería de un experimento mío.
· **No corrí el emulador** con un carnet real: la superficie que muestra los
  campos nuevos es de C, y su rama de 1.0 todavía no está en `main`.
· **No toqué `fecha_proxima`** en ninguna fila — verificado, 0 escrituras.
· **Cero backfill**, en las tres migraciones.

## Operativo

**3 migraciones** aplicadas y verificadas **contra el objeto** (no contra el
archivo): `20260909060000` · `20260909080000` · `20260909100000`. **Las tres con
reversa escrita ANTES** y con **76(g) declarada NO RIGE** con su porqué. **L-140
verificada en las tres**: `anon` no ejecuta ninguna. `sobrecargas = 1` en las dos
funciones que cambiaron de firma. Tipos regenerados. **4 typechecks en 0 con 0
bytes.** Fichas nuevas: **`D-1029`** · **`L-491`**.

---

## EL CENSO QUE LA REGLA NUEVA EXIGE, corrido sobre mi propio lote

*La regla nació de este lote, así que se aplica primero acá — retroactiva y
completa, no a partir del próximo.*

### ① Por CUERPO en el motor (`pg_proc`, no por nombre)

**`evento_vacuna_aplicada` — CINCO funciones la tocan:**

| función | qué es |
|---|---|
| `registrar_vacunas_de_carnet(uuid, jsonb, text)` | **la que ensanché.** Familia + IA |
| `registrar_vacuna_mostrador(uuid, text, text, date)` | **el OTRO productor**, DEFINER — por eso el CHECK y no sólo el guard |
| `obtener_plan_vacunal(uuid, date, int)` | lectura. **La que ya existía y casi duplico** |
| `evaluar_requisitos_guarderia(uuid)` | **lector que yo no sabía que existía** — lee vacunas y plan |
| `ejecutar_cierres_vencidos()` | cron |

⚠️ **`evaluar_requisitos_guarderia` es el hallazgo del censo**: lee
`evento_vacuna_aplicada` **y** `cat_plan_vacunal`, o sea que es un **segundo
consumidor del plan** que ninguna parte del brief nombra. No lo toqué —
`exigida_guarderia` viaja ahora por `obtener_plan_vacunal` y esa función tiene
su propia lectura—, **pero queda nombrado**: quien mueva la semántica del plan
tiene que abrirla. *Sin el censo, esto se descubría el día que guardería
rechazara a una mascota por una razón que el plan ya no dice.*

**`evento_desparasitacion_aplicada` — UNA sola**: `registrar_desparasitacion`,
la que ensanché. *Y es exactamente la que dupliqué en el lote 0 antes de tener
esta regla.*

**`cat_plan_vacunal` — DOS**: `obtener_plan_vacunal` y
`evaluar_requisitos_guarderia`.

### ② Por IMPORT en la puerta única (`packages/api`)

**`evento_vacuna_aplicada` la leen SEIS wrappers** —`perfilMascota` ·
`mascotasPrestador` · `vacunas` · `hogar` · `veterinaria-mostrador` y el propio
`salud`—, o sea que **la tabla tiene seis lectores y yo toqué uno**. Los otros
cinco siguen con su select viejo: *no están rotos —las columnas nuevas son
nullable— pero tampoco ven lo nuevo, y eso es un hecho declarado, no un olvido.*

**Ya exportadas en `index.ts` antes de que yo escribiera nada:**
`extraerVacunasDeCarnet` · `registrarVacunasDeCarnet` ·
`registrarDesparasitacion` · **`obtenerPlanVacunal`**.

🔴 **La última línea es la prueba de la regla: `obtenerPlanVacunal` estaba
exportado desde `index.ts`, a la vista, y aun así escribí un duplicado.** *No
hizo falta un censo sofisticado para evitarlo: alcanzaba con leer el índice de
la puerta que yo mismo conduzco.*

---

# CANDIDATO 1.0 — `6cc59936` · **FRENADO por dos rojos medidos en la edge**

## Lo que D declaró bloqueante: **es falso, y su causa tiene nombre**

D dice que la RPC no acepta `laboratorio` ni `vencimiento_biologico`. **Contra
`pg_proc` de la base viva:**

| | |
|---|---|
| acepta `laboratorio` | **SÍ** |
| acepta `vencimiento_biologico` | **SÍ** |
| CONTROL− una clave inventada | **NO** ⇒ el instrumento discrimina |
| columnas en la tabla | **2 de 2** |
| sobrecargas | **1** |
| migración en el ledger | **presente** |

**D midió contra `main` sin mi rama** — y ahí la migración no estaba. *La ley de
la casa es «base = catálogo de Postgres, nunca el archivo de migración», y ésta
es su cara menos obvia: el archivo puede FALTAR y la cosa estar aplicada.* D
aplicó el instinto correcto —declarar el bloqueante en vez de seguir— con el
instrumento equivocado.

## Las ramas del objeto — **29**, y nueve pendientes

Mergeadas en el orden dictado: **E** `11682554` → **D** `4fda7858` → **B**
`522483b2` → **A** `0429bbe4` → **C** `d4f183fe`. Ancestría verificada en las
cinco, **cero conflictos**.

**Quedan FUERA, con su razón:** `b-1.1` `182e6480` · `b-1.2` `6d37ed92` ·
`c-1.1`→ya entró · `d-1.2` `e5181fa6` — **son de sublotes posteriores y este
candidato es el 1.0.**

⚠️ **Sobre «C @ su SHA de C3/C4»: no existe ninguna rama ni commit con ese
nombre.** Lo verifiqué contra el objeto; el único hit de mi grep fue **el propio
hash `06ec330a`, que contiene «c3»** — un falso positivo de mi instrumento, que
declaro. Las ramas de C son `c-1.0` `06ec330a` y `c-1.1` `d4f183fe`, y **la
segunda desciende de la primera**. Mergeé `c-1.1` porque **subsume a `c-1.0` y
trae las superficies que tu propio checklist de emulador exige**
(`FilaConfirmacionVacuna`, `FilaVacunaCarnet`, la pantalla de vacunas): con sólo
`c-1.0` ese paso sería inverificable. *Si «C3/C4» era otra cosa, está a un merge.*

## La edge v2: desplegada, y **la llamada real la reprobó**

Desplegada **con el mismo modelo de hoy** (`claude-sonnet-5`) — lo que cambia es
el prompt y `max_tokens` 16000 → 2000, tal como está firmado. Versión **68**,
después **70** tras restaurar el valor firmado (abajo).

### 🔴 ROJO ①: `plan_impreso` no se llena NUNCA en su caso fundante

El carnet «1 → 12» (`carnet-1783564367515.jpg`) tiene **1 vacuna de verdad** y
11 renglones de plan impreso — es el caso que la v2 existe para resolver.

**Seis corridas reales por la edge, todas con `plan_impreso` en CERO:**

| corrida | HTTP | vacunas | plan_impreso | tokens_salida |
|---|---|---|---|---|
| con techo 8000 ×4 | 200 | **14 · 14 · 15 · 14** | **0 · 0 · 0 · 0** | 1970 · 2041 · 2087 · 2052 |
| con techo 2000 ×2 (las que no truncaron) | 200 | **14 · 14** | **0 · 0** | 1947 · 1945 |

**D reporta «1 en `vacunas`, 11 en `plan_impreso`». No reproduce, y no es
diferencia de método:** leí su arnés —`medir-carnet.mjs:102`— y **llama a la
MISMA edge con el MISMO cuerpo** (`imageBase64` + `mediaType: 'image/jpeg'`).
Mismo camino, misma imagen, resultado opuesto **seis veces**.

*Y el detalle que lo empeora: las 14 vienen con `evidencia: "sticker_con_fecha"`
y `confianza: "alta"` en la mayoría. El modelo no duda — afirma. Eso es peor que
una extracción incompleta: es la trampa L-139 otra vez, verosímil y falso, con
un campo de confianza que le da autoridad.*

### 🔴 ROJO ②: el techo de 2000 no está cerca del borde, **está EN el borde**

La salida real mide **1945–2087 tokens**. Con `max_tokens: 2000`, **una de cada
tres corridas trunca** (medido: 1 de 3, con `resultado: 'error_parseo'` y HTTP
422). *Un techo que cae en la mediana de lo que hace falta no es un techo chico:
es una moneda al aire.* Y falla **según el carnet y según la corrida**, que es
la peor forma de fallar.

✅ **Lo único bueno del rojo: la edge es HONESTA.** No devuelve medio carnet:
dice `extraccion_fallida · "quedó truncada"` y no escribe nada. Es la lección de
S48 funcionando.

### El experimento, declarado

Para separar «el techo» de «el modelo» subí `carnet` a **8000**, desplegué, medí,
y **restauré el valor firmado (2000)** con un redeploy — `git diff` sobre
`modelos.ts` en **cero**: lo desplegado vuelve a ser lo que el repo dice. *No
dejé mi número experimental corriendo: cambiar un valor que vos firmaste no es
mío.* El experimento probó que **el techo explica el 422 y NO explica el
`plan_impreso` vacío** — con 8000 tampoco se llenó.

### La fila de `ia_uso` de la primera llamada real

```
pieza carnet · modelo claude-sonnet-5 · edge extract-vacuna
resultado error_parseo · entrada 3380 · salida 2000 (= el techo, o sea truncó)
cache lectura 0 · escritura 0 · latencia 13.097 ms · costo USD 0,02676
```
y la de una corrida buena: `resultado ok · salida 1970 · 12.773 ms · USD 0,02646`.

## 🔴 Por qué NO sigo al emulador ni al «autorizo»

El paso que pediste es *«el carnet real, la confirmación fila por fila, la ficha
fina, Su plan»*. **La confirmación fila por fila mostraría 14 filas inventadas
con confianza alta, y `plan_impreso` vacío.** *Recorrerlo no sería un gate: sería
fotografiar el defecto y llamarlo verde.* Y publicar pondría en el teléfono una
extracción que falla una de cada tres veces y clasifica mal las otras dos.

**Lo mergeado NO se pierde** — `6cc59936` está listo y sin conflictos. Lo que
falta es la decisión sobre la edge, y es de D y tuya:
① `plan_impreso` no funciona por la edge (seis corridas) — **de D**;
② el techo de 2000 trunca 1 de cada 3 — **firma tuya, porque vos lo firmaste**.

**No publiqué, no corrí el emulador, y no toqué el valor firmado.**
