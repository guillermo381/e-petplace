# S113-D · LOTE 2.1 — miré el carnet, y la premisa no se sostiene

**Rama:** `pista/s113-d-2.1` · **base:** `4fda7858`.
🔴 **CERO CORRIDAS CONTRA EL MODELO REAL: no hay llave.** Todo lo de abajo sale
de mirar las imágenes y de medir la base. **Nada de esto es un verde de modelo.**

---

## 0 · La llave: NO ESTABA — apareció a mitad del lote, y una de las dos está rota

> **Enmienda:** el founder dejó las dos llaves en el llavero (cuenta `medicion`)
> después de que escribí esta sección. **La de Anthropic anda; la `service_role`
> NO.** Ver el addendum del final. Lo que sigue queda como estaba, porque
> describe con qué se trabajó las primeras horas.

## 0bis · Lo que había cuando escribí esto: NO ESTÁ

Buscada en: `ANTHROPIC_API_KEY` de entorno (no) · keychain por servicio y por
cuenta, incluido «medicion» (no) · `~/.anthropic` y `~/.config/anthropic` (no
existen) · `ant` (no instalado) · **todos** los `.env*` de los repos vecinos
(cero nombran `ANTHROPIC`). El secreto `ANTHROPIC_API_KEY` **sí existe en
Supabase**, pero la CLI sólo devuelve su **digest**, nunca el valor.

⇒ El mandato dice *«ningún verde sin el modelo real»*. **No hay un solo verde de
modelo en este lote.** Lo que sigue es otra cosa, y creo que vale más.

---

## 1 · Miré el «1 → 12». **No tiene una vacuna: tiene QUINCE.**

`carnet-1783564367515.jpg` — 1079×767, dos páginas enfrentadas, tabla impresa
con cabecera **FECHA · VACUNA · PRÓXIMA APLICACIÓN · FIRMA MVZ**.

**Qué son esos renglones:** vacunas aplicadas, todas.
**Qué marca tienen:** cada uno lleva **un sticker del frasco pegado**, la fecha
**manuscrita** a la izquierda, la próxima **manuscrita** a la derecha, y la
firma impresa *MVZ. Enrique López Sánchez · Ced. Prof. 11536014*.
**Por qué el modelo los llama sticker:** **porque son stickers.**

Rabimune · GiardiaVax · Bronchicine CAe · Recombitek · Peek N-RB · Peek Puppy ·
Peek'o · Imrab 3TF, entre 2020-08-29 y 2024-10-25. La transcripción completa,
fila por fila, en **`docs/loop/verdad-vista/carnet-1783564367515.json`**.

🔴 **Y NO HAY NINGÚN PLAN IMPRESO.** La tabla nace vacía; no hay una lista
preimpresa de nombres de vacuna en ninguna de las dos páginas.

### La verdad del conjunto para este carnet es UNA fila, y está mal

```
nombre_vacuna "Peck NRB" · fecha 2024-10-25 · lote "11536014"
```

- **El nombre y la fecha vienen de DOS renglones distintos.** El del 25/OCT/2024
  lleva **Imrab 3TF**; «Peek N-RB» es el del **27/OCT/2023**.
- **`lote: 11536014` es la CÉDULA PROFESIONAL DEL VETERINARIO**, impresa en la
  columna FIRMA de **todas** las filas. El lote real de esa fila es `185191`.
- Y es 1 de 15.

---

## 2 · Dos de los cinco carnets son **el mismo documento**

`carnet-1785354131272.jpg` y `carnet-1783694984605.jpg` son **la misma libreta
física, fotografiada dos veces** (mismos stickers, mismas fechas, mismo sello de
la Clínica Protectora de Animales; una foto sobre madera, la otra sobre negro).

El conjunto los cuenta como **dos casos**, con **8 y 7** filas de verdad, y
**coinciden en 1 de 8**:

| | caso A (8) | caso B (7) | lo que dice el carnet |
|---|---|---|---|
| Procyon | `Procoyd Dog Py` | `Procyon DogPv` | **`Procyon® Dog Pv`** ⇒ A mal |
| Vanguard, lote | `56288` | `562887` | **`562887`** ⇒ A mal (le falta un dígito) |
| Nobivac DHPPi | `2023-05-05` | `2023-06-26` | manuscrito **«26 JUN»** ⇒ A mal |
| Nobivac Lepto | `2023-06-04` | `2023-06-05` | manuscrito **«S/JUN»** ⇒ ilegible: los dos inventan el día |
| Recombitek | fila sola | `Recombitek C7 + Bacterina Leptospira` | **dos stickers, una dosis** |
| Canigen | **dos** filas | **una** fila | dos stickers, una dosis |

*El mismo documento, dos lecturas humanas, siete desacuerdos de ocho.*

---

## 3 · Lo que esto le hace a `D-1012` — y a mi propio lote 1.0

### `D-1012` describe un defecto que no existe

Su titular es *«un carnet de UNA vacuna devolvió DOCE»*. **El carnet tiene
quince.** El modelo no inventó once filas: **leyó un carnet lleno y lo
compararon contra una libreta donde un humano guardó una sola fila.**

Y la aritmética de costo que colgaba de ahí — *«un modelo que inventa filas paga
la salida de todas»* — **pierde su causa**: pagó la salida de quince vacunas que
están ahí.

### Las exactitudes están medidas contra una referencia contaminada

`nombre` 65,6 % · `fecha` 62,5 % · `lote` 81,3 % se calculan contra filas que
**un humano creó con la extracción y después editó**. En el caso que pude
arbitrar con los ojos, **la referencia estaba mal en 2 de 4 campos**, y en el
par duplicado, en 7 de 8 filas.

🔴 **Es `L-139` cobrada donde más duele: el conjunto de verdad está contaminado
por exactamente el modo de falla que la casa viene peleando.** Un valor
verosímil y falso —`11536014` como lote— pasó la revisión humana *porque parecía
bien*, y hoy es la vara contra la que se mide al modelo.

### Y mi `plan_impreso` cura una enfermedad que este corpus no tiene

Lo construí en 1.0 razonando que el modelo metía el plan impreso en el canasto
de las aplicadas. **En los tres carnets que miré no hay plan impreso.** Puede
seguir siendo correcto para otros formatos —muchos carnets lo traen—, pero
**la evidencia que lo motivó estaba mal leída, y eso lo digo yo, que lo escribí.**

*No lo retiro: no hace daño y el corpus es de cuatro documentos. Pero deja de
ser «la cura de `D-1012`» y pasa a ser una capacidad sin caso medido.*

---

## 4 · Las tres hipótesis del mandato, y por qué no las corrí

(a) razonamiento encendido · (b) dos pasos: describir marcas y después
clasificar · (c) darle la estructura del carnet como pista.

**Las tres apuntan a la atribución sticker↔fecha.** Con lo medido arriba, esa no
parece ser hoy la falla principal: **la falla principal es que no hay contra qué
medir.** Correr las tres contra la referencia actual daría tres números, y los
tres serían del instrumento, no de las hipótesis.

⚠️ **No las descarto** — (b) en particular es una idea buena y barata, y (c)
tiene un caso real: el segundo formato trae **etiquetas `FECHA` preimpresas y
dos que dicen literalmente `DD MM AAAA`** sin llenar, que sí es «estructura del
formato» y sí puede confundir. Pero **medirlas exige, primero, una referencia
que no mienta.**

## 5 · Los 30 sintéticos: **no existen**

Cero en el repo, cero en `.ia-conjuntos/`, cero menciones en los partes de E.
El conjunto de carnets es **5 fotos · ≤4 documentos distintos · 32 filas**, y de
esas 32, las de los dos duplicados se contradicen entre sí.

---

## 6 · La orden para E, para reproducir cada corrida

```bash
# ① bajar los carnets (service_role por keychain, no imprime la clave)
node scripts/ia-conjuntos/construir-carnets.mjs --verificar-legibles

# ② el "1 -> 12" contra el modelo REAL, con el prompt v2 vigente.
#    la edge acepta `modelo` SOLO con token service_role y de la lista blanca.
ANTHROPIC_API_KEY=<la de medicion> \
  npx tsx scripts/ia-conjuntos/medir-pieza.mjs --pieza carnet --modelo claude-sonnet-5

# ③ la variante con razonamiento (hipótesis (a)): hoy `PENSAR.carnet = false`
#    en supabase/functions/_shared/ia/modelos.ts. Se pone en true, se redespliega
#    y se re-corre ②. UNA variable moviéndose.
```

🔴 **Pero antes de ②, la pregunta que ordena todo:** contra qué se compara.
**Firmado por el founder: se rehace con TRES FIRMAS** — ver el addendum.

## 7 · Lo que NO hice

- **Ninguna corrida contra el modelo real.** No hay llave. Cero verdes de modelo.
- **No toqué el prompt v2**, ni `PENSAR`, ni `max_tokens`. El mandato pedía
  4000; **no lo subí**: subirlo sin poder medir el efecto es mover un número a
  ciegas, y hoy 2000 alcanza para 15 filas sin razonamiento.
- **No construí los 30 sintéticos**: antes hay que decidir qué miden, y con la
  referencia real rota, sintéticos sobre la misma vara serían más números falsos.
- **Nada desplegado.**

---

# ADDENDUM · el founder firmó el protocolo, y miré los otros dos carnets

> *«La verdad no la escribe una sola mano.»* D transcribe a ojo · **E transcribe
> los mismos, por separado y sin ver la de D** · el founder dirime mirando la
> foto. La referencia queda con tres firmas y **es anterior a toda corrida**.

## 🔴 Corrección a lo que yo mismo escribí arriba: no son cuatro documentos. Son DOS.

Miré las cinco fotos, una por una. `carnet-1783632653859` y
`carnet-1783633828265` son **la tercera y la cuarta foto del mismo carnet
plegable** que ya había reconocido duplicado. Mismos lotes (`02031203`,
`562887`, `47087B`, `A468A01`, `0129035B01`, `A127G01`, `9B2F`), mismas fechas,
mismo sello.

| documento | fotos | filas de «verdad» |
|---|---|---|
| **A** — tabla de dos páginas | 1 foto | **1** |
| **B** — plegable vertical | **4 fotos** | **8 + 7 + 8 + 8 = 31** |

**31 de las 32 filas de verdad describen UN solo documento**, cargado cuatro
veces, con desacuerdo en **6 de sus 8 vacunas**:

```
DIFIERE nobivaclep | 2023-06-04 | 2023-06-05 | 2023-05-10 | 2023-04-19
DIFIERE nobivacdhp | 2023-05-05 | 2023-06-26 | 2023-06-05 | 2023-06-26
DIFIERE vanguardda | lote 56288 | lote 562887 | lote 56288.7 | —
DIFIERE procyondog | 02031203  | 02031203   | 18KL10-01   | 190L-01
```

## Y el renglón donde se ve POR QUÉ se contradicen

En el documento B, **dos renglones tienen el campo FECHA SIN LLENAR**: sigue
mostrando el marcador **preimpreso `DD MM AAAA`**. Lo único que hay es un
manuscrito en la zona de firma — «S/JUN» y «26 JUN», **sin año**.

⇒ `Nobivac Lepto` **no tiene fecha en el carnet**, y las cuatro cargas humanas
pusieron cuatro fechas distintas. **Ninguna la leyó: las cuatro la inventaron.**
En mi transcripción va `null` con `confianza: baja`, que es lo que L-139 manda.

*Y de paso: esto le da un caso real a la hipótesis (c) del mandato —«darle la
estructura del carnet como pista»—. El `DD MM AAAA` preimpreso es exactamente
la clase de estructura que confunde.*

## Lo entregado

```
docs/loop/verdad-vista/DOCUMENTOS.md        el protocolo + la orden para E
docs/loop/verdad-vista/documento-A--D.json  mi transcripción · 15 filas
docs/loop/verdad-vista/documento-B--D.json  mi transcripción ·  8 filas
scripts/ia/cotejar-verdad.mjs               el cotejo D vs E (pnpm verify:cotejo)
```

`pnpm verify:cotejo --control` → **7 verdes · 0 rojos**, sin archivos y sin red.
`pnpm verify:cotejo --doc B` → **para en seco**: E todavía no transcribió, y el
instrumento dice literal *«no lo escribas vos: la segunda mano tiene que ser de
otra cabeza»*.

**Lo que el cotejo NO dice:** que lo coincidente sea verdad. Dice que dos
lectores leyeron igual — y dos lectores pueden equivocarse de la misma manera si
el error es verosímil. **Que es exactamente como se contaminó el conjunto
original.**

## `D-1012` enmendada

Por orden del founder, en `docs/DEUDAS_CANONICAS.md`: **el «1 → 12» era un error
de verdad, no de modelo.** Lo que no cambia: los 83 s y los $0,0715 siguen
siendo deuda real de producto. Lo que cambia es su causa.

⚠️ **Toqué `DEUDAS_CANONICAS.md`, que es territorio de A**, por orden directa del
founder y sobre una ficha que nació de mi propia medición. Queda declarado.

---

# ADDENDUM 2 · LA MATRIZ, CONTRA EL MODELO REAL

Las llaves aparecieron en el llavero a mitad del lote. **La de Anthropic anda**
(`count_tokens` HTTP 200); **la `service_role` está truncada** —128 caracteres,
**un solo punto**, `403 Invalid Compact JWS` en storage y `401 Invalid API key`
en PostgREST— y el aviso para E vive en `docs/loop/buzon/`.

> ⚠️ **Y me cobré una:** mi primera prueba de la `service_role` dio **400 de
> forma** (`body must have required property 'prefix'`), que llega **antes** de
> que se evalúe la credencial. **Con ese 400 escribí «service_role VIVA» y
> estaba equivocado.** *Un 400 de forma tapa un 403 de auth.*

## La matriz — 10 celdas, $0,3572, contra el modelo real

`node scripts/ia/matriz-carnet.mjs --correr`. Documento **A** = el «1 → 12»
(**15 filas a mi ojo**) · documento **B** = el plegable vertical (**8 filas**).

| variante | modelo | doc | latencia | tok salida | costo | **filas** | plan_impreso |
|---|---|---|---:|---:|---:|---:|---:|
| v1 | sonnet-5 | A | **63,2 s** | 6 887 | $0,0741 | **10** / 15 | — |
| v1 | sonnet-5 | B | **95,3 s** | 9 831 | $0,1064 | **8** / 8 | — |
| v1 | haiku-4.5 | A | 7,3 s | 833 | $0,0064 | **12** / 15 | — |
| v1 | haiku-4.5 | B | 3,7 s | 298 | $0,0042 | **5** / 8 | — |
| **v2** | **sonnet-5** | **A** | **13,1 s** | 2 015 | **$0,0269** | **14** / 15 | **0** |
| **v2** | **sonnet-5** | **B** | **10,0 s** | 1 248 | **$0,0220** | **9** / 8 | **0** |
| v2 | haiku-4.5 | A | 12,9 s | 1 704 | $0,0114 | **12** / 15 | 0 |
| v2 | haiku-4.5 | B | 6,6 s | 1 224 | $0,0094 | **9** / 8 | 0 |
| v2-razón | sonnet-5 | A | 35,7 s | **4 000** | $0,0468 | 🔴 **TRUNCADO** | — |
| v2-razón | sonnet-5 | B | 39,0 s | **4 000** | $0,0496 | 🔴 **TRUNCADO** | — |

## ① `D-1012` queda desmentida por medición, no por lectura

**v1 + Sonnet devuelve 10 vacunas en el carnet del «1 → 12», y las diez son
reales** — cotejadas una por una contra mi transcripción. **Ninguna inventada.**
Le faltan cinco.

⇒ **El modelo no sobre-extrae: SUB-EXTRAE.** El defecto de la ficha no existe en
ninguna de las dos direcciones. *Lo que la ficha llamó «once filas inventadas»
eran once vacunas que están pegadas en el carnet.*

## ② El razonamiento era el costo, y estaba medido al revés

v1+Sonnet gastó **6 887 tokens de salida para ~600 de JSON**. **~6 300 eran
razonamiento** ⇒ la inferencia del lote 1.0 pasa de inferencia a **medición**.

Y lo que no esperaba: **apagarlo no empeoró la extracción, la mejoró.**

| | v1 (razonando) | v2 (sin razonar) | |
|---|---:|---:|---|
| doc A | 10 filas · 63 s · $0,074 | **14 filas · 13 s · $0,027** | **+4 filas · 4,8× más rápido · 2,7× más barato** |
| doc B | 8 filas · 95 s · $0,106 | 9 filas · 10 s · $0,022 | **9,5× más rápido · 4,8× más barato** |

⚠️ **Y se dice cómo hay que leerlo:** v1 y v2 difieren en **tres cosas a la vez**
—prompt, `max_tokens` y razonamiento—. Esta tabla responde *«¿el pipeline nuevo
es mejor?»*, **no** *«¿fue el prompt o fue el razonamiento?»*.

## ③ La hipótesis (a), MEDIDA Y DESCARTADA — y me corrige a mí

Con los **4 000 tokens que pedía el mandato** salió **truncada en las dos**: el
pensamiento se come el presupuesto y no queda JSON. *Una hipótesis medida sobre
una celda truncada no está medida* ⇒ se re-corrió con el techo de v1 (16 000),
**mismo prompt v2, misma imagen, UNA sola variable**:

| doc | sin razonar | RAZONANDO |
|---|---|---|
| A | **14 filas** · 13,1 s · $0,0269 · out 2 015 | **14 filas** · 57,5 s · $0,0739 · out 6 716 |
| B | **9 filas** · 10,0 s · $0,0220 · out 1 248 | **9 filas** · 96,7 s · $0,1185 · out 10 895 |

🔴 **MISMO NÚMERO DE FILAS. 4,4–9,7× más lento. 2,7–5,4× más caro.** El
razonamiento no aporta **una sola fila** y quema entre 4 700 y 9 600 tokens de
salida por nada.

### 🔴 Y esto corrige lo que yo mismo escribí arriba

Dije *«apagarlo no empeoró la extracción, la mejoró»* — comparando v1 con v2,
que **cambian tres cosas a la vez**. Aislado, el reparto es otro y es limpio:

- **El PROMPT v2 aporta las filas:** 10 → 14. Nada que ver con el razonamiento.
- **Apagar el razonamiento aporta la velocidad y la plata:** 57 s → 13 s ·
  $0,074 → $0,027 — **sin costar una sola fila**.

*Los dos cambios se ganan su lugar, por razones distintas. Atribuirle al
razonamiento la mejora de filas habría sido exactamente el error que esta
sesión vino a corregir en otros.*

### Lo que esto le hace a la advertencia que dejé en el lote 1.0

Escribí que apagar el razonamiento era «la palanca más riesgosa» porque S48
midió que era lo que resolvía la atribución sticker↔fecha. **Medido sobre estos
dos documentos: no lo es.** Con el prompt v2, encenderlo no recupera ni una
fila. **La advertencia era razonable y está refutada** — sobre dos documentos,
que es todo el corpus que existe.

## ④ `plan_impreso`: **CERO en las cuatro corridas de v2**

Es la medición que el lote 1.2 dejó pedida («se decide con datos»). **Los dos
documentos no tienen plan impreso**, y el bucket que construí en 1.0 queda con
**cero casos** sobre el corpus que existe.

## ⑤ Contra MI mano — y sólo contra la mía

`v2 + Sonnet` sobre el doc A coincide con mi transcripción en **14 de 15 filas,
con las 14 fechas correctas**. Se le escapa el sticker beige del 15/03/23.
Dos desacuerdos de criterio, no de lectura, que **van al founder**:
- `Bronchi-Shield Ca` (modelo) vs `Bronchicine CAe` (yo) — mismo sticker azul.
- el modelo cuenta el **diluente** como fila propia; yo lo agrupé con su vacuna.

🔴 **Esto NO es exactitud.** Es coincidencia con **una** mano de las tres. La
referencia se firma con E y el founder, y **recién entonces** se puede decir un
porcentaje.

---

# ADDENDUM 3 · v2 DEFINITIVA (S113-D-2.2)

## La firma, aplicada

`claude-sonnet-5` · **`max_tokens` 4000** · **`thinking: {type:'disabled'}`
ESCRITO en la request**. El techo sube de 2000 a 4000 por firma del founder; el
numero que lo respalda es que el carnet mas denso devolvio **14 filas en 2.015
tokens** sin razonar.

## El invariante nuevo, y hoy lo violaban DOS piezas

**`TECHO_SIN_RAZONAR = 16000`: toda pieza por debajo manda `thinking disabled`
explicito.** Medido desde dos lados: **E** en carnets reales (omitirlo deja a
Sonnet 5 razonar solo, quemarse el techo y devolver **cero caracteres**) y **D**
aislando la variable (6.716 y 10.895 tokens contra 2.015 y 1.248, **mismas
filas**).

Al escribirlo, **`documento` y `presencia` lo violaban**: techo 4000 y
`PENSAR: true`. Corregidas -- y **el cambio es casi un no-op, medido**: en la
corrida real de A devolvieron **35 y 85 tokens de salida** con `thinking`
omitido, o sea que el adaptive ya habia decidido no pensar. *No se les quita un
razonamiento que no hacian; se les saca el riesgo de que un dia lo hagan y se
coman el techo.* `nota_clinica` queda razonando y es legitimo: su techo ES 16000.

**El gate tiene DOS brazos, y el segundo es el que importa.** El control lo
prueba: rompiendo la emision del campo en `mod.ts`, **el brazo de la tabla quedo
VERDE** y el del cuerpo cazo las tres piezas. *La tabla puede decir `false` y el
cuerpo no llevar el campo: ahi el proveedor razona igual y nadie se entera hasta
el truncado.*

## `vacuna_codigo` -- y `D-008` queda pagada

La extraccion devuelve, por fila, el **codigo de `cat_vacunas`** (7 filas,
medidas) o `null`. **Lista blanca en la edge, contra el catalogo leido en esa
misma llamada** -- mismo criterio que `sugerir-raza`.

**Y quita una fragilidad que estaba viva:** el proto-catalogo eran nombres **con
tilde** y el validador exigia la cadena exacta -- **una sola emision sin tilde
rebotaba el carnet entero con 422**. Los codigos no tienen tildes ni espacios.
*En las corridas reales los modelos acertaron los acentos; la fragilidad estaba
igual, esperando.*

`tipo_vacuna` **se DERIVA del codigo** en la edge: un solo juicio del modelo,
dos campos de salida, cero chance de que se contradigan -- y la RPC de escritura
sigue guardando lo mismo sin que A toque nada.

**Catalogo caido = se FALLA**, no se degrada: sin lista blanca, seguir
devolveria codigos sin acotar o `null` en todas las filas **sin que nadie sepa
que fue por una caida**.

## El cotejador: el defecto era mio y era de esquema

Mi doc A llamaba `lote_visible` a lo que el cotejo compara como `lote`. El campo
estaba **ausente en las dos puntas**, y el cotejo **lo contaba como
coincidencia**. *La mitad de la salida era nula por esquema y se leia como
acuerdo.*

- **Unificado**: doc A pasa a `lote`, y gana `confianza` (mi certeza, fila por
  fila) y `vencimiento_biologico` **en null con su razon declarada** -- no los
  transcribi, y es un hueco de mi lectura, no del carnet.
- **Curado en el instrumento**: ahora `null` es una **lectura** y ausente es un
  **hueco de esquema**, que sale en rojo aparte y pone el exit en 1.
- **Verificado**: los dos documentos, **cero campos ausentes**.

Y un control cazo un defecto de mi propio arnes: `cuerpoSalida` no se reseteaba
entre casos, asi que <<no se llamo al modelo>> pasaba por el valor viejo. *Un
instrumento con estado que no se limpia mide la corrida pasada.*

## Lo que NO resolvi, porque no es mio

**Los tres criterios los firma el founder**, y quedan marcados en los JSON: el
**diluyente** (fila propia o parte de la dosis), el **sticker azul**
(`Bronchicine CAe` mio vs `Bronchi-Shield Ca` del modelo) y el **`15/JUL`**
huerfano.

## Gates

`verify:ia-puerta` **43/0** · `verify:carnet-v2` **34/0** ·
`verify:cotejo --control` **10/0** · `verify:edge-deno` **34 en verde** ·
**4 typechecks en 0**.

**`verify:cotejo --doc A|B` sigue parando en seco: E no entrego su
transcripcion.** La referencia **no esta firmada**, asi que sigue sin haber
ningun porcentaje de exactitud en este parte.
