# S105-B · BITÁCORA DE LA PISTA

> **Territorio:** `packages/ui` · tokens · lint y jueces (`scripts/verify-*`).
> **Rama:** `pista/s105-b` · worktree `../e-petplace-s105-b` · nacida de `main` en
> `5959650e`, **árbol en cero verificado al abrir**.
> **Nada de este archivo sale de memoria.** Cada afirmación declara contra qué
> objeto se midió; donde hace falta una cifra va el comando (L-141).
>
> ✅ **NÚMERO FIRMADO: S105** (founder, 24-ago) — rige la numeración de la
> bitácora de A, no la del founder. **Rama, worktree y este archivo renombrados
> de `s106-b` a `s105-b` antes de tocar nada más.**
>
> **Turno 1:** sin código — lo medido, y los dos frenos.
> **Turno 2:** llegaron los assets. La marca producida, el pedido a C y la ley
> propuesta → **§9**.

---

## §0 · DOS COSAS QUE EL PROMPT DA POR EXISTENTES Y NO ESTÁN EN EL OBJETO

**No las adivino ni las reconstruyo. Las declaro y sigo con lo que no depende de ellas.**

> **⏪ ESTADO AL CIERRE DEL TURNO 2 — los dos se movieron y ninguno murió:**
> **①** el founder firmó que **rige la numeración de A: esta mesa es S105**, y el
> renombre se ejecutó. **Lo que sigue abierto es si existe un plan para ella** —
> `PLAN_MESA_105` está en el repo sin ejecutar y no fue citado. **Mesa.**
> **②** llegaron **los assets y dos láminas** del manual, y con eso siete de mis
> diez preguntas quedaron contestadas con cita (§9). **El manual completo sigue
> sin leerse** —Corebook es una aplicación de una sola página y no hay navegador
> conectado— así que **tres preguntas siguen abiertas, y una de ellas puede dar
> vuelta una decisión del gate** (§9, pregunta 2).
> *Lo de abajo queda como se escribió: era cierto al escribirse.*

### ① `PLAN_MESA_106` NO EXISTE en el repo

```
ls docs/ | grep -i mesa      → PLAN_MESA_103 · PLAN_MESA_104 · PLAN_MESA_105 · ORDEN_ARRANQUE_MESA_105
find . -iname "*MESA_106*"   → (nada)
```

Y hay algo más que cambia la lectura: **la mesa 105 nunca corrió.**
`git log --all --grep=S105` no devuelve un solo commit de sesión, y no hay
bitácora `docs/loop/S105-*` ni rama `pista/s105-*`. La última que corrió es
**S104** (bitácoras A · B · C · D, y su acta en `main`).

⇒ **Rige `PLAN_MESA_105` mientras no aparezca otro**, o el plan de esta mesa
todavía no se escribió. **Es pregunta de mesa, no una que yo pueda resolver
midiendo.** *Un plan que se cita y no existe es la clase de premisa que se
hereda tres sesiones sin que nadie la mire.*

### ② El manual de marca de DeUna no está en el repo ni me llegó

El prompt dice *«el founder te pasa el acceso o los assets exportados»* —
**hoy no tengo ni el link Corebook ni los archivos.** Censado: cero assets de
DeUna en el árbol (`find . -iname "*deuna*"` no devuelve ningún binario;
los únicos hits son código y letra).

⇒ **El punto 1 de mi encargo —«leé el manual y medí»— no se puede ejecutar
sin el objeto**, y no lo voy a sustituir por lo que suelen decir los manuales
de marca. **Lo que sí hice es dejar la lectura preparada:** §3 es la lista
cerrada de preguntas que el manual tiene que contestar, cada una atada a una
decisión concreta de la superficie. Cuando llegue, leerlo es **una pasada
contra una lista**, no un descubrimiento.

---

## §1 · LO QUE LA SUPERFICIE NECESITA MOSTRAR — medido, no supuesto

**Contra `LETRA_DEUNA.md` v1.7 (`docs/LETRA_DEUNA.md`), que rige completa y sin
firmas pendientes (§11).**

| exigencia | dónde | estado en el código |
|---|---|---|
| **DeUna es la PRIMERA fila de «Cómo quieres pagar»** | §6bis, firma del founder 22-ago | ✅ **ya ejecutado** |
| **Y el medio POR DEFECTO** (preselección), salvo elección previa del cliente | §6bis | ✅ motor presente (`preferencias.ts`: `medio: 'deuna' \| 'tarjeta'`) |
| **No elegible sin `pointOfSale`** (borde ①) | §6bis | ✅ `DEUNA_ELEGIBLE = false`, un solo interruptor |
| **JAMÁS en cobro recurrente** (borde ②) | §6bis | ✅ declarado en la pieza |
| **La experiencia es idéntica a pagar con tarjeta** | §11 firma ② | — |
| **La UI muestra SOLO el código de 6 dígitos** (deeplink y QR son reserva sin pantalla) | §11 firma ① · §10 | ✅ `CodigoAEscala` + `BotonCopiar` viven en `packages/ui` |

### 🔴 Corrección a la letra: §6bis describe una pantalla que ya no existe

`LETRA_DEUNA` §6bis dice, en negrita, que la fila *«está ÚLTIMA, debajo de las
seis tarjetas guardadas… y queda debajo del fold»*, y ordena reordenarla.

**Medido hoy en `apps/cliente/src/components/seccion-medio-de-pago.tsx:319`:
`<FilaDeUna />` es la PRIMERA fila de la Hoja**, con la regla de la firma
escrita en su propio comentario (*«DeUna es SIEMPRE la primera fila, sin
condición… no se mueve cuando no es elegible»*).

⇒ **Esa ejecución ya ocurrió.** Rige §13ter de la propia letra: *«donde esta
letra y el código discrepen, gana el código»*. Lo anoto porque **§6bis se lee
como trabajo pendiente y no lo es** — y ése es exactamente el modo de falla que
el canon ya tiene nombrado: *lo que dejó de ser cierto no se nota, porque nunca
fue falso al escribirse.*

---

## §2 · 🔴 EL PUNTO EXACTO DONDE EL MANUAL MUERDE — y hay precedente ejecutado

**Hoy la marca DeUna se dibuja con TEXTO, no con su logo.**

`apps/cliente/src/components/logo-franquicia.tsx` resuelve la marca en dos
brazos: si hay archivo vendorizado dibuja el SVG del proveedor; si no, **cae al
fallback y dibuja el nombre en letras de nuestra tipografía**. `deuna` no tiene
archivo ⇒ la fila pinta **«DEUNA»** en 56 px con `Texto variante="dato"`
(`fila-medio-de-pago.tsx:253` → `LogoFranquicia marca="deuna"`).

Su propio comentario lo dice sin eufemismo: *«texto dibuja las marcas sin
archivo, y "DEUNA" entra en los 56 px»*.

> **Un wordmark ajeno tipografiado con la fuente de nuestra casa es lo primero
> que prohíbe cualquier manual de marca.** No hace falta haber leído el de DeUna
> para saber que ése es el estado que hay que salir a cambiar — lo que hace
> falta el manual es para saber **con qué exactamente se reemplaza.**

### El precedente que rige, y está construido y probado: S101-D

La casa ya resolvió este problema **para las cinco franquicias de tarjeta**, y
dejó su doctrina escrita en el código y en
`apps/cliente/assets/marcas/PROCEDENCIA.md`:

| ley | dónde está escrita | por qué |
|---|---|---|
| **Vendored, JAMÁS hotlink** | `PROCEDENCIA.md` | un CDN ajeno cambia o borra un archivo sin avisar · **pedir la imagen en cada fila le cuenta a un tercero cuándo abre la app cada familia** · una pantalla de pago que depende de red externa muestra huecos justo cuando la familia mira su plata |
| **NO se redibuja a mano** (orden de mesa) | `logo-franquicia.tsx:19` | son marcas registradas: el `.svg` del proveedor es la fuente de verdad. *Un logo redibujado a ojo se lee como error, no como marca* |
| **`PROCEDENCIA.md` obligatorio** | el archivo mismo | fuente, URL original, quién lo indicó, fecha |
| **Escala CONTENIENDO, con el aspecto real del `viewBox`** | `logo-franquicia.tsx:67` | los cinco vienen con aspectos distintos; un `width`/`height` fijo **estira marcas registradas y no da error: se ve «casi bien»** |
| **Sin `Math.round` por dimensión** | `logo-franquicia.tsx:120` | medido: hasta 0,032 de desvío de aspecto en Visa |
| **La caja es constante: 56×32, contenido 44×22** | `ANCHO_LOGO`/`ALTO_LOGO` | el set consistente es lo que la pieza garantiza; **lo que cambia adentro es el contenido, jamás la caja** |
| **Jamás un hueco** | `logo-franquicia.tsx:32` | sin marca dibuja el glifo `pagos`; *la rama que devuelve `null` no existe* |

**El pipeline también existe:** `apps/cliente/metro.config.js` +
`react-native-svg-transformer@^1.5.3` compilan un `import Logo from './x.svg'`
a componente de `react-native-svg`. **Un SVG de DeUna entra sin trabajo de
infraestructura nuevo** — siempre que entre por `apps/cliente`. Ver §4.

---

## §3 · LO QUE EL MANUAL TIENE QUE CONTESTAR — lista cerrada, cada una atada a una decisión

**Se lee contra esta lista.** Lo que el manual no conteste, se pregunta a DeUna;
no se resuelve por criterio nuestro — *un uso de marca ajena que decidimos
nosotros es exactamente lo que un manual existe para impedir.*

| # | pregunta al manual | qué decide |
|---|---|---|
| 1 | **¿Qué variante corresponde a un lockup de ~44×22 px?** ¿Isotipo solo, o wordmark completo? | si la caja de 56×32 alcanza, o si esta marca es la primera que rompe el set |
| 2 | **¿Tamaño mínimo de reproducción?** | si a 44×22 el uso es legal. **Si el mínimo es mayor, la caja del set no le sirve y hay decisión de arte** (§5.①) |
| 3 | **¿Área de resguardo / espacio libre mínimo?** (normalmente en múltiplos de un elemento del logo) | el `CONTENIDO_ANCHO`/`ALTO` de 44×22 vive dentro de 56×32: **12 px horizontales y 10 verticales de aire**. Hay que medir si eso cumple |
| 4 | **¿Sobre qué fondos se permite?** ¿Exige fondo blanco/claro propio, o admite fondo neutro? | hoy la caja usa `theme.bg.hundido`, **que cambia entre claro, oscuro y memorial**. Si el manual exige un fondo fijo, la caja de DeUna deja de heredar el tema |
| 5 | **¿Hay versión monocroma / negativa autorizada?** | es la salida si (4) prohíbe nuestro fondo oscuro |
| 6 | **Usos prohibidos:** ¿deformar, recolorear, re-tipografiar, encerrar en caja, agregar efectos? | **el que ya nos toca: el fallback de texto actual es un wordmark re-tipografiado.** Y confirma si «encerrar en un recuadro con fondo» está permitido |
| 7 | **¿Formato entregado?** SVG vectorial vs PNG a densidades | SVG entra por el transformer existente. **PNG obliga a `@2x`/`@3x` y a otra rama** |
| 8 | **¿El nombre se escribe «DeUna», «Deuna» o «deuna»?** | el repo tiene **las tres formas vivas** hoy (`DeUna` en letra y wrappers, `Deuna` en la galería y en la voz, `deuna` como código). Es dato de marca, no preferencia nuestra |
| 9 | **¿Exige leyenda de atribución** («Paga con DeUna», marca registrada, etc.) junto al logo? | si la fila necesita voz además del logo |
| 10 | **¿Hay reglas para el momento del pago** (pantalla de código, botón de acción)? | `CodigoAEscala` y `BotonCopiar` son piezas de `packages/ui` que ya existen y **hoy no llevan marca ninguna** |

---

## §4 · EL REPARTO — un problema medido, y no es menor

**El plan asigna la marca DeUna a B. Pero todo el aparato que la dibuja vive en
el territorio de C.**

| pieza | dónde vive | de quién es |
|---|---|---|
| `LogoFranquicia` (la que resuelve la marca) | `apps/cliente/src/components/logo-franquicia.tsx` | **C** |
| `FilaDeUna` (la que la monta) | `apps/cliente/src/components/fila-medio-de-pago.tsx` | **C** |
| assets vendorizados + `PROCEDENCIA.md` | `apps/cliente/assets/marcas/` | **C** |
| `metro.config.js` + `react-native-svg-transformer` | `apps/cliente/` | **C** |
| `CodigoAEscala` · `BotonCopiar` (la pantalla del código) | `packages/ui/src/components/` | **B (mío)** |

**Y el dato que decide:** `react-native-svg-transformer` está declarado **solo
en `apps/cliente/package.json`**, y **`packages/ui` no importa un solo `.svg`
como archivo** (medido: `grep -rn "from '.*\.svg'" packages/ui/src/` → cero).
Toda la marca propia de `packages/ui` —`Isotipo`, `Huella`, los 52 glifos— es
`react-native-svg` **inline**, jamás un archivo importado.

⇒ **Tres caminos, y la mesa elige uno. No lo elijo yo:**

| | camino | costo | riesgo |
|---|---|---|---|
| **(a)** | **B produce y deposita el asset + su `PROCEDENCIA.md`; C lo monta** en el aparato que ya existe | el más barato: cero infraestructura nueva, el precedente S101-D corre tal cual | el asset cruza territorio ⇒ viaja **autocontenido y en disco** (L-355) |
| **(b)** | La marca DeUna sube a `packages/ui` como pieza propia | `packages/ui` gana pipeline de SVG-archivo que hoy **no tiene**, y con él una config de bundler nueva | infraestructura nueva para **una** marca |
| **(c)** | Se dibuja inline en `react-native-svg` como el resto de `packages/ui` | ninguna infra | 🔴 **prohibido por la orden de mesa de S101-D**: *no se redibuja a mano una marca registrada*. Lo descarto por letra, no por gusto |

**Mi voto: (a).** Es el único que no inventa infraestructura ni rompe una orden
vigente, y respeta lo que la pieza de C ya promete al pie: *«el día que los
assets se depositen, cambia el interior de esta caja y nada más»*.

---

## §5 · LAS DECISIONES QUE NECESITAN EL OJO DEL FOUNDER

**Ninguna se puede cerrar midiendo. Las tres nacen del choque entre un manual
ajeno y una ley nuestra — y en ese choque no hay respuesta técnica.**

### ① Si el manual pide más de lo que la caja del set da

La caja de 56×32 con contenido 44×22 **es la ley del set consistente**: misma
caja, mismo radio, mismo aire para las cinco franquicias, para la desconocida y
para la ausente. Si el mínimo de reproducción o el área de resguardo de DeUna no
entran ahí, hay que elegir:

- **agrandar la caja para todas** (el set sigue consistente, la fila crece), o
- **darle a DeUna una caja propia** (el set deja de serlo justo en la fila que el
  founder puso primera), o
- **usar su variante compacta** si el manual la autoriza.

*No es una decisión de implementación con una respuesta correcta: es cuánto vale
la consistencia del set contra el cumplimiento literal de un manual ajeno.*

### ② El fondo de la caja cambia con el tema, y una marca ajena puede no permitirlo

`theme.bg.hundido` es distinto en claro, oscuro y memorial. Si DeUna exige fondo
fijo o versión negativa, **la caja de DeUna deja de heredar el tema de la casa** —
y va a ser la única. En memorial es peor: ese tema apaga el color por letra.

### ③ Un logo real al lado de cuatro y un fallback de texto

Cuando DeUna tenga archivo, en la Hoja convivirán: **DeUna con su logo (primera,
por firma) · las tarjetas con su logo o con su texto**. El founder tiene que ver
eso montado antes de que se dé por bueno. **Es gate por ojo en `/gallery`, con
las tres temas** — mismo protocolo que el gate por ícono (§6b de `DIRECCION_ARTE`).

### 🔴 Y una que es de letra, no de píxeles

**`DIRECCION_ARTE.md` no tiene una sola ley sobre marcas de terceros.** Medido:
`grep -niE "tercero|ajena|logo ajeno|proveedor|visa|mastercard" docs/DIRECCION_ARTE.md`
→ **un solo hit, y es de otro tema.** Sus 15 secciones son todas sobre la marca
propia.

La doctrina de S101-D existe **pero vive en comentarios de una pieza de
`apps/cliente` y en un `PROCEDENCIA.md`** — no en la dirección de arte. Es decir:
**la ley que va a gobernar el logo de DeUna hoy se sostiene en un comentario que
nadie está obligado a leer.**

⇒ Propongo (**sin firma, no rige**) que la doctrina de marca ajena suba a
`DIRECCION_ARTE` como sección propia, con el molde de **§6ter «LA MARCA DE MAPA —
CLASE APARTE»**, que es el precedente exacto: algo que no pertenece al set de la
casa y por eso se le nombra una clase propia en vez de forzarlo adentro.
*El costo de escribirla es una sección; el de no escribirla ya está medido en la
casa cuatro veces.*

---

## §6 · EL SPLASH DEL RITUAL — declarado, no construido

**Sin cambios desde S104-B. Sigue esperando asset dibujado, y mi spec es el insumo.**

| | |
|---|---|
| **Qué falta** | §1 del ritual: tapiz claro (`#F6F6F6`) + isotipo entintado + wordmark «e-PetPlace» horneado |
| **Bloqueo 1 — vivo** | `expo-splash-screen` toma **una imagen raster** ⇒ el wordmark va horneado en el PNG. Censado en este entorno: **cero rasterizadores de SVG** (`rsvg-convert`·`magick`·`inkscape`·`resvg`·`cairosvg`) y **cero librerías** (`sharp`·`@resvg/resvg-js`·`canvas`). **No puedo producirlo.** |
| **Bloqueo 2 — RESUELTO por firma (23-ago)** | el camino intermedio ya está en el repo: isotipo real sobre `tapizDark`, `imageWidth: 122` derivado para que caiga a **72,0 dp exactos**, el alto del isotipo de bienvenida |
| **Bloqueo 3 — vivo** | «coincidir al píxel» es **geométricamente imposible**: el splash nativo **centra** (no hay prop de posición) y bienvenida **ancla arriba** ⇒ los centros quedan a **~280 dp**. La salida está en el propio §0 del ritual (*«el isotipo se asienta»*): el primer frame del Acto II empieza centrado y se asienta. **Eso es composición de C y decisión de mesa, no de esta pista.** |
| **La spec, completa y medida** | `docs/loop/S104-B-SPLASH-BLOQUEADO.md` — fondo, aspecto `1.456`, alto 72, `imageWidth` 105, tipografía y aire del wordmark. **Nada estimado.** |
| **Dueño** | **Founder / diseño.** Yo no tengo nada que construir acá hasta que exista el archivo |

⚠️ **Y no viaja solo:** el splash es **binario, no se cura por OTA**. En esa build
suben también el `name` del launcher (`e-PetPlace`, ya hecho) y
**`expo-local-authentication`**, sin el cual `PantallaDeCandado` sigue inerte.

---

## §7 · ESTADO DE LA PISTA

**Cero código escrito. Cero archivos tocados fuera de esta bitácora.** El árbol
tiene exactamente un archivo nuevo: éste.

### Bloqueado, esperando objeto
1. 🔴 **El manual/assets de DeUna** — sin ellos §3 no se puede correr. **Founder.**
2. 🔴 **`PLAN_MESA_106`** — no existe en el repo. **Mesa.**

### En la mesa del Checkpoint 1
3. **El reparto de §4** — mi voto es (a), decide la mesa.
4. **Las tres decisiones de ojo de §5**, y la de letra (marca ajena a `DIRECCION_ARTE`).

### Herencia de S104-B que sigue abierta y es mía o me toca
5. **`PantallaDeCandado` inerte** — `expo-local-authentication` no instalado, es nativo. **Tren nativo.**
6. **Las tres piezas del cierre inertes** — no hay motor en `packages/api`. **A.**
7. **El gate por ícono del ojo a 21 px** en `/gallery`. **Founder.**
8. **El gate del biométrico en dispositivo** — el caso que importa es **abrir en frío con sesión guardada y el sensor fallando a propósito**. *Es el único punto donde una equivocación deja a alguien afuera de su cuenta.*
9. 🔴 **Los «30 días» de `MODELO_LOGIN` sin firma, y `MODELO_LOGIN` cita a `P15` diciendo algo que `P15` no dice.** **Mesa / A.**

### Nota de instrumento, para mí mismo
Este worktree es nuevo ⇒ **antes de tocar código hay que correr `expo start` una
vez**, o `R63·C` va a dar rojo con razón: sin `.expo/types/router.d.ts`,
`typedRoutes` degrada `Href` a `string` y **el typecheck sale verde sin medir una
sola ruta.**

---

## §8 · DÓNDE MEDIR (comandos, no cifras)

| qué querés saber | comando |
|---|---|
| Reglas del juez, y cuántas | `node scripts/verify-diseno.mjs` (la última línea las cuenta) |
| Componentes de `packages/ui` | `ls packages/ui/src/components/*.tsx` (restá variantes `.web` y los `.ts` de infra) |
| Glifos del registry | el union `IconoNombre` en `Icono.tsx` |
| Si DeUna ya es elegible | la constante `DEUNA_ELEGIBLE` en `fila-medio-de-pago.tsx` — **una sola** |
| Qué marcas tienen archivo | `ls apps/cliente/assets/marcas/*.svg` + el mapa `ARCHIVOS` de `logo-franquicia.tsx` |
| Numeración libre (`D-`·`L-`·`R-`) | **grep contra `DEUDAS_CANONICAS.md`** — jamás de un parte |

⚠️ **`eas-cli` SIEMPRE desde `apps/<app>/`**, aunque solo estés mirando: desde la
raíz scaffoldea un `app.json` stub y ensucia el árbol — y un árbol sucio saca el
ancla con asterisco.

---
---

# §9 · TURNO 2 — EL MANUAL LLEGÓ. LA MARCA, MEDIDA Y PRODUCIDA.

> **Insumo recibido:** `~/Downloads/Logos DeUna` — 9 archivos, **5 únicos**
> (uno estaba bajado 5 veces, sha256 idéntico) + el link Corebook del manual.
> **Lo que se pudo leer:** los assets y **dos láminas del manual** (la
> arquitectura de marca N1–N6, versión *14 de abril 2025*, y la lámina de usos
> sobre fondo con sus ✅/❌/⚠️).
> **Lo que NO:** el manual completo. Corebook es una aplicación de una sola
> página; sin navegador conectado la descarga directa devuelve solo el shell
> (probado: vuelve el tagline *«Paga, Cobra, Fácil y Seguro»* y nada más).
> **No lo sustituí por lo que suelen decir los manuales de marca.**

## §9.1 · EL INSTRUMENTO, VALIDADO ANTES DE USARSE

Este entorno no tiene **ni una** librería de imagen (censado en S104-B: `sharp`,
`resvg`, `canvas`, `magick`, `inkscape` → ninguna). Y la casa tiene una ley que
exigía medir esto bien: **medí el CUERPO, no el LIENZO** — S104-B se equivocó
comparando lienzos y casi le cuesta al founder una decisión peor.

⇒ Escribí un decodificador PNG en node puro (`zlib` es built-in): decodifica,
mide el bbox del alfa y recorta sin resamplear.

**Y lo probé contra un caso de resultado conocido antes de creerle una sola
medición** (L-330: control declarado junto a cada medición):

```
splash del prestador → lienzo 285×208 · cuerpo 245×168 · aspecto 1.458
S104-B había medido    →                cuerpo 245×168 · aspecto 1.458   ✅
```

*Un instrumento que no reprodujo un resultado conocido no midió nada.*

## §9.2 · LAS DIEZ PREGUNTAS, CONTESTADAS CON SU FUENTE

| # | pregunta | respuesta | fuente |
|---|---|---|---|
| 1 | **¿Qué variante para ~44×22?** | Hay **dos** y las dos son N1: wordmark `deuna!` (1583×418, **aspecto 3.787**) e isotipo `d!` (468×420, **1.114**). *Cuál va es del founder — §9.4* | lámina de arquitectura de marca **N1**; medición del cuerpo |
| 2 | **¿Tamaño mínimo de reproducción?** | 🔴 **SIN RESPUESTA — y es la que más puede cambiar el resultado.** Si su mínimo supera los **11.6 dp** que da el wordmark en nuestra caja, la decisión ① se cierra sola a favor del isotipo | — |
| 3 | **¿Área de resguardo?** | 🔴 **SIN RESPUESTA.** Nuestra caja deja **6 dp** horizontales y **5 dp** verticales de aire | — |
| 4 | **¿Sobre qué fondos?** | ✅ **CONTESTADA Y ES LA CLAVE DEL TURNO.** blanco+morado ✅ · morado+blanco ✅ · verde+morado ✅ · **verde+blanco ❌ prohibido** · morado+verde ⚠️ condicionado | **lámina de usos sobre fondo**, con marcas explícitas |
| 5 | **¿Versión monocroma / negativa?** | **Parcial:** existe el negativo **blanco sobre su morado** (✅ fila 2). **No hay versión autorizada para fondo oscuro genérico** — y ése es justo nuestro caso | misma lámina |
| 6 | **Usos prohibidos** | **Contestado para fondos** (el ❌ y el ⚠️). Los prohibidos generales **no los tengo**. ⚠️ El asterisco de morado+verde remite a una nota al pie **que no está en la lámina** | misma lámina |
| 7 | **¿Formato?** | ✅ **PNG, no SVG** — y eso cambia el camino: el pipeline vectorial existente no sirve, hace falta una rama de `<Image>` | los archivos |
| 8 | **¿La grafía?** | ✅ **CONTESTADA Y RESUELVE LA TRIPLICACIÓN — §9.3** | láminas **N1** y **N3/N4** |
| 9 | **¿Leyenda de atribución?** | 🔴 **SIN RESPUESTA** | — |
| 10 | **¿Reglas para la pantalla de pago?** | **Sin respuesta directa.** Dato adyacente: su nivel **N4** lista *«Botón de cobro»* y *«Cobro con QR Dinámico»* como funciones con nombre propio | lámina N4 |

**Y un color, medido de los archivos y no del manual —porque el manual no me
llegó, pero los píxeles sí—:**

| | |
|---|---|
| **morado oficial** | **`#4C1D80`** — es el **100 % de los píxeles opacos** del wordmark **y** del isotipo |
| verde del chip «Negocios» | `#008081` |

## §9.3 · LA GRAFÍA — el manual la fija, y la pantalla ya la tenía bien

**Lo que el manual establece**, y son **dos cosas distintas que el repo mezclaba:**

- **`deuna!`** — minúscula, con exclamación, tipografía propia. **Es el
  LOGOTIPO.** Existe solo como asset: **jamás se tipografía.**
- **«Deuna»** — capital inicial, **sin exclamación**. **Es el nombre en texto
  corrido**, y así lo escribe su propio manual en los niveles N3 y N4:
  *«Promos **Deuna**», «Club **Deuna**», «Lealtad **Deuna**»*.

**Medido en el repo** (código + letra, sin `node_modules`):

| forma | ocurrencias | veredicto |
|---|---|---|
| `DeUna` | **223** | 🔴 **no existe en el manual. La inventamos nosotros.** |
| `Deuna` | 44 | ✅ correcta para texto |
| `deuna` | 186 | ✅ correcta como clave de código |

> ✅ **LO QUE SALE A PANTALLA YA ESTÁ BIEN.** Las doce voces `pago.deuna*` del
> i18n usan **«Deuna»**. *La forma inventada, que es la más numerosa, es
> justamente la que nadie ve: vive en letra y comentarios.*

⇒ **No pido un barrido de las 223.** No salen a pantalla, no rompen nada, y un
barrido de una marca ajena por 223 archivos es mucho riesgo para cero beneficio
visible. **Se curan al tocarse.** Lo dejo escrito para que nadie escriba una
nueva. ⚠️ **Y el `!` no se escribe nunca en texto.**

## §9.4 · LO PRODUCIDO — byte-idéntico, verificado

En `docs/relevamientos/S105-B-MARCA-DEUNA-para-C/`:

| archivo | qué | sha256 vs original |
|---|---|---|
| `ic_deuna.png` | wordmark `deuna!` | ✅ **idéntico** `c476c26006c186c5…` |
| `ic_deuna_isotipo.png` | isotipo `d!` | ✅ **idéntico** `c29721a65b127159…` |

**No se redibujó, no se recortó, no se resampleó, no se recomprimió.** Los dos
vienen **sin un píxel de padding** —el cuerpo llena el lienzo exacto—, así que no
había nada que recortar: se le pasan `w` y `h` tal cual, igual que el `viewBox`
de cada SVG.

**Lo que NO deposité, y con su razón:** el lockup **`deuna! Negocios`**.
«Negocios» es su producto **para el comercio** —o sea, nosotros—; lo que la
familia elige en la pantalla de pago es pagar con **su** app, que es la marca
principal N1. *Depositar el de Negocios sería ponerle a la familia el logo del
lado del mostrador.*

### Cómo queda en la caja del set (44×22, escala conteniendo)

| marca | aspecto | render | alto |
|---|---|---|---|
| Visa | 3.111 | 44.0×14.1 | 14.1 |
| Mastercard · Diners · Amex · Discover | ~1.52 | 33.4×22.0 | 22.0 |
| **`deuna!` wordmark** | **3.787** | **44.0×11.6** | **11.6** |
| **`d!` isotipo** | **1.114** | **24.5×22.0** | **22.0** |

## §9.5 · 🔴 EL CHOQUE MEDIDO: LA CAJA NO PUEDE HEREDAR EL TEMA

`LogoFranquicia` pinta el fondo con `theme.bg.hundido`. Contraste del morado
oficial contra cada tema:

| tema | `bg.hundido` | contraste |
|---|---|---|
| claro | `#EDEBF5` | 9.92:1 ✅ |
| **oscuro** | `#050508` | **1.74:1** 🔴 |
| **memorial** | `#141A14` | **1.51:1** 🔴 |

**En oscuro y memorial el logo sería prácticamente invisible — y además ninguno
de esos fondos figura entre los que el manual autoriza.** La decisión ② que
levanté a ciegas en el turno 1 **queda confirmada con número.**

### El hallazgo que trasciende este caso

**El manual aprueba morado sobre verde (2.45:1) y prohíbe blanco sobre verde
(4.77:1): aprueba la de menor contraste y prohíbe la de mayor.**

No es un error suyo — **es que no mide lo mismo que nosotros.** Su criterio es
identidad de marca; el nuestro, legibilidad. **Un logo no se lee: se reconoce**,
y el dato que la familia necesita leer lo dice el título de la fila, que sí es
nuestro. ⇒ **sobre una marca ajena manda su manual, y eso incluye a WCAG.** Es la
regla ⑤ de la ley propuesta, y es la única de las cinco que hoy no está escrita
en ningún lado.

## §9.6 · ENTREGADO

| # | qué | dónde |
|---|---|---|
| 1 | Las diez respuestas con cita | **acá**, §9.2 |
| 2 | Los dos assets vendorizados, byte-idénticos | `docs/relevamientos/S105-B-MARCA-DEUNA-para-C/` |
| 3 | **El pedido a C, autocontenido (L-355)** | `…-para-C/PEDIDO.md` + `PROCEDENCIA.md` para pegar |
| 4 | **La ley de marca ajena, SIN FIRMA** | `docs/relevamientos/S105-B-LEY-MARCA-AJENA-propuesta.md` |
| 5 | Las tres decisiones del founder | **no las cerré** — §9.7 |

**Cero código de `packages/ui` tocado.** La marca vive en la pieza de C y el
reparto firmado dice que C la monta. *Lo que produje es el insumo, no la
pantalla.*

## §9.7 · LAS TRES DECISIONES DEL FOUNDER — servidas, no cerradas

**① ¿Wordmark o isotipo?** El wordmark queda **más bajo que cualquier tarjeta
del set** (11.6 contra 14.1 de Visa); el isotipo usa el alto completo pero pide
reconocer `d!`. *Dato duro a favor del wordmark: al leer la lámina, un lector
automático transcribió el isotipo como **«d1»** — si una máquina entrenada en
glifos lo lee mal, alguien que nunca usó la app también puede.* **Recomendé
montar el wordmark como default y dejar el isotipo listo para comparar.**

**② El fondo.** Propuse **blanco `#FFFFFF` fijo** para la caja de Deuna (uso ✅
más literal, 11.70:1). **Y le serví una alternativa que el founder tiene que
ver porque preserva el set:** que **todas** las cajas pasen a blanco fijo — las
cinco tarjetas también son marcas registradas. *Con la primera el set se rompe
justo en la fila que él puso primera; con la segunda no se rompe, pero toca cinco
filas que nadie reportó como rotas.*

**③ Verlo montado** junto a las cinco, en los tres temas. Gate en dispositivo.

## §9.8 · LO QUE SIGUE SIN SABERSE — y hay que preguntárselo a Deuna

Va en el pedido a C y lo repito acá porque **una de las tres puede dar vuelta el
gate**: **el tamaño mínimo de reproducción** (si supera 11.6 dp, la decisión ① se
cierra sola) · **el área de resguardo** · **qué dice el asterisco** de
morado+verde. Más: si tienen **SVG**, si autorizan **variante para fondo
oscuro**, y si exigen **leyenda de atribución**.

⚠️ **Y el estado a matar sigue vivo, por orden:** el fallback de texto
—`deuna` cayendo en el brazo que dibuja «DEUNA» con nuestra tipografía— **muere
recién cuando C monte el asset real**. *La fila no puede quedar sin marca ni un
turno.*

---

# §10 · TURNO 3 — EL PROVEEDOR CONTESTÓ. LA ELECCIÓN QUEDÓ FIRMADA Y MEDIDA.

**Las diez preguntas están CERRADAS:** siete con cita de sus láminas, tres con
respuesta directa del proveedor (grupo de soporte, **25-ago-2026**, con fecha y
canal declarados).

## §10.1 · Las tres respuestas nuevas

| | |
|---|---|
| **tamaño mínimo** | logotipo: 1,9 cm impreso · **50 px** digital — símbolo: 0,5 cm · **16 px** |
| **área de reserva** | **1X a cada lado**, X = *grosor total del punto del signo de exclamación*. Nada gráfico, fotográfico, tipográfico ni de textura invade ese espacio |
| **el asterisco** (morado+verde) | *sujeta a las necesidades del diseño · verificar legibilidad · **no usar en formatos pequeños o reducidos*** |

## §10.2 · ✅ FIRMA ① — GANA EL ISOTIPO, y lo decidió un número del proveedor

| | mínimo Deuna | nuestra caja | |
|---|---|---|---|
| wordmark | **50 px** | **44,0 dp** de ancho | ❌ |
| isotipo | **16 px** | **22,0 dp** lado menor · **66 px** @3x | ✅ **bajo las dos lecturas** |

*El wordmark es ambiguo (44 dp lógicos ❌ / 132 px físicos ✅) y **se descartó por
la lectura restrictiva**. El isotipo entra de las dos formas, así que **la
ambigüedad no hay que resolverla** — mismo criterio que la firma ③ de
`LETRA_DEUNA`, donde elegir el supuesto más restrictivo resultó coincidir con el
dato cuando llegó.*

## §10.3 · X MEDIDO, Y LA CUENTA DEL RESGUARDO

Aislé el punto por **componentes conexas sobre el canal alfa**. Es **un círculo
perfecto**: aspecto **1.00**, llenado de elipse **0.989**.

```
isotipo   → punto de 84×84 px      X = 84
wordmark  → punto de 83×83 px      (control cruzado: el proveedor es consistente)

X = 84 px × escala 0.052381 = 4,400 dp en pantalla
```

| | requerido con 1X por lado | caja | |
|---|---|---|---|
| ancho | 24,51 + 8,80 = **33,31** | 56 | ✅ sobran 22,69 |
| **alto** | 22,00 + 8,80 = **30,80** | 32 | ✅ sobran **1,20** |

> ## 🟢 **ENTRA. Pero el alto manda y la holgura es de 0,60 dp por lado.**
> El aire vertical disponible es **5 dp** por lado; X pide **4,40**.

### ⚠️ Lo que eso obliga, y se lo dije a C con todas las letras

**`ALTO_LOGO` no baja de 32 · `CONTENIDO_ALTO` no sube de 22 · nada más se dibuja
dentro de esa caja.** *El modo de falla es silencioso: mover una constante de
layout no da error, no rompe un test, y viola el manual de una marca registrada
sin que nadie lo note.*

⇒ **Ofrecí mecanizarlo con una regla del juez** (`verify-diseno`, mi territorio):
que falle si esas constantes se mueven sin recalcular el resguardo. **No la
construí**: el founder priorizó que el pedido llegara antes que el gate. *Queda
ofrecida, no hecha — y lo digo acá para que no se lea como pendiente olvidado.*

## §10.4 · ✅ FIRMA ② — FONDO BLANCO FIJO, con mi medición como fundamento

`#FFFFFF` en los tres temas para la caja de Deuna. **La alternativa de pasar las
cinco tarjetas a blanco fue evaluada y DESCARTADA por el founder** con su razón:
*«cambia cinco piezas firmadas para acomodar una nueva»*. Visa, Mastercard,
Diners, Amex y Discover **no se tocan**.

## §10.5 · EL ASTERISCO CONVIRTIÓ MI REGLA ⑤ EN EVIDENCIA

La regla ⑤ que propuse decía que *sobre una marca ajena manda su manual, WCAG
incluido, porque un logo no se lee: se reconoce*. Era **un argumento mío**.

**La respuesta del proveedor lo vuelve una cita:** preguntado por el asterisco,
**delegó en «verificar la legibilidad» y NO dio un número de contraste** — y
agregó una condición de **tamaño**. *Un manual que midiera lo que mide WCAG
habría contestado con un ratio. Contestó con un juicio y un tamaño, porque su
criterio es reconocimiento.* **Depositado en la propuesta de ley con su cita.**

Y de paso: **el asterisco no nos toca por partida doble** — no es la combinación
que montamos, **y nuestra caja es formato reducido**, que es justo donde su
propio manual la desaconseja.

## §10.6 · 🔴 EL BLOQUEO DEL CAMINO CRÍTICO — y no es de contenido

**Mi trabajo existe solo en mi disco.** Medido:

```
git merge-base --is-ancestor HEAD origin/main   → 🔴 NO
git ls-remote origin pista/s105-b               → vacío
```

**`pista/s105-b` no está en `origin` ni en `main`, así que C no puede tomar el
asset.** Y C lo necesita: `apps/cliente/assets/marcas/` no existe todavía en su
worktree.

**El merge y el push son de A** (`METODO_TRES_PISTAS`), así que no los ejecuto.
Lo que sí hice: **traer `origin/main` a mi rama** (main había avanzado a
`9760a6a0` con el rescate de A) — merge limpio, **main no tocó `packages/ui` ni
la superficie de pago**, solo `scripts/deuna` y docs.

⇒ **Pedido a A: mergear `pista/s105-b` a `main`.** Es lo único que separa a C del
asset. *Es L-217 en vivo: un commit que existe no es un commit que el canon
tiene — y acá ni siquiera llegó a `origin`.*

**Y apareció `docs/PLAN_MESA_106.md`**, que el turno 1 declaró ausente. Confirma
el reparto: *«el logo y el manual de marca son de B: la pantalla de pago tiene
que mostrar…»*.

---

# §11 · TURNO 4 — `R65`, LA REGLA FIRMADA. Y ME CORRIGIÓ A MÍ.

**Firmada por el founder (25-ago-2026)** con su razón: *el modo de falla es
silencioso, y acá el que tiene que frenar es un compilador, no un lector.*
**`verify-diseno` VERDE con 56 reglas.**

## §11.1 · No vigila números: rehace la cuenta

R65 **no compara constantes contra un baseline que yo escribí.** Lee las cuatro
constantes de la caja del código, **mide el asset real** —aspecto por
decodificación de alfa, y X por componentes conexas sobre el glifo— y **rehace
la cuenta del resguardo**.

*Vigilar «no toques el 32» protege un número. Rehacer la cuenta protege la regla
del proveedor, que es lo que hay que cumplir.* Si cambian las constantes **o
cambia el dibujo**, la cuenta se rehace sola.

Su salida en verde imprime la medición entera:

```
caja 56×32 · contenido 44×22 · isotipo 24,51×22,00 dp · X 4,40 dp
(punto de 84px, aspecto 1.00, relleno 0.989) · resguardo pide 33,31×30,80,
holgura 0,60 dp por lado en el alto
```

**Reproduce exacto lo que había medido a mano.** Dos caminos, mismo número.

## §11.2 · Los tres brazos, probados EN ROJO contra el código real

*No alcanza el fixture: la lección ② de mi propio traspaso dice que el primer bug
de un juez nuevo está en **dónde mira**.*

| brazo | prueba | resultado |
|---|---|---|
| **A** · la cuenta | `CONTENIDO_ALTO` 22 → **23** | ✅ rojo, con la cuenta impresa |
| **A** · la cuenta | `ALTO_LOGO` 32 → **30** | ✅ rojo |
| **C** · candado del asset | reemplacé el isotipo por el wordmark | ✅ rojo pidiendo re-medición |

Y el guard estructural de la casa **me frenó a mí** en el camino: R65 tenía regla
y fixture pero no estaba en `corridas`, y el lint se declaró inválido en vez de
imprimir un verde. *Es el guard que S82-B escribió después de que le pasara
exactamente eso. Funcionó.*

## §11.3 · 🔴 EL BRAZO C ES UNA ORDEN DEL FOUNDER, MECANIZADA

Deuna entrega el SVG mañana. La orden, literal: *«no reemplaces el PNG por
costumbre… **X es una propiedad del glifo dibujado, no del archivo** — si el SVG
trae otro viewBox, otro padding o el punto con otro grosor relativo, los 4,40 dp
dejan de valer»*.

⇒ **el sha256 del asset medido quedó clavado en la regla.** Cambiar el archivo la
pone roja pidiendo re-medición. *Un swap que «solo cambia el formato» es
exactamente la clase de cambio que no da síntoma — y ahora sí lo da.*

⚠️ **El hash no se actualiza para que el lint se calle: se actualiza cuando la
cuenta se rehizo y dio.** Está escrito en la regla, no solo acá.

## §11.4 · 🔴 LA REGLA CORRIGIÓ MI PROSA — y es el hallazgo del turno

Le escribí a C, en negrita: ***«`ALTO_LOGO = 32` no baja. A 31 el área de reserva
se viola.»***

**Es falso.** Lo probé en rojo esperando rojo y **salió verde, con razón**: el
resguardo pide 30,80 y **30,80 ≤ 31**. Sobra 0,10 dp por lado.

La forma correcta no son dos prohibiciones sino **una desigualdad**, porque X
sale del propio dibujo y escala con el contenido (**X = 0,2 × `CONTENIDO_ALTO`**):

```
CONTENIDO_ALTO × 1,4  ≤  ALTO_LOGO
```

> **Escribí una prohibición redonda donde había una desigualdad, y el mecanismo
> que construí para vigilar el límite fue el que encontró que yo lo había escrito
> mal.** *Es exactamente para lo que se mecanizó — y es más barato que se cobre
> en la primera corrida que en el primer cambio de layout.*

**`PEDIDO.md` corregido**, con la corrección visible y su porqué. *La v2 no se
borra: se marca.*

## §11.5 · LO QUE R65 NO MIDE, DECLARADO

**No verifica que nada más se dibuje dentro de la caja** —el tercer límite del
manual—. Contar hijos JSX sería frágil y su rojo no sería confiable: *una regla
que se puede violar sin que nadie lo note es mala; una que grita cuando no debe
es peor, porque enseña a ignorarla.* Vive como límite escrito en `PEDIDO.md` y en
la procedencia, y **su verde no dice «nadie invadió la caja»**.

## §11.6 · HIGIENE DE MESA

**① La rama, a `origin`.** Se midió hoy que mi trabajo no existía en ningún lado
fuera de mi disco — *el object store compartido lo hace parecer seguro y no lo
es*. **Empujada.** ✅ **A ya había mergeado lo anterior a `main`**, verificado por
contenido (el asset, el instrumento y la propuesta de ley están en `origin/main`).

**② La numeración.** Rige **S105**. Mi primer commit de la mesa dice `S106-B` en
su asunto: **se queda así**, por orden — *el commit viejo conserva su asunto y la
corrección se ve*. De ahí en adelante todo firma S105.

**Mi worktree NO miente:** el directorio es `e-petplace-s105-b` y la rama
`pista/s105-b`. *(El que sí miente es el de D: directorio `e-petplace-s106-d`
sobre la rama `pista/s105-d`. No es mi territorio; lo declaro porque un
directorio que dice otro número es el tipo de cosa que manda a alguien a buscar
al lugar equivocado.)*

## §11.7 · LO QUE QUEDA

| # | qué | dueño |
|---|---|---|
| 1 | **Montar el asset** — el pedido está en `main` y el destino existe | **C** |
| 2 | **El SVG, cuando llegue: medir ANTES de reemplazar.** R65·C frena el swap; el que decide si procede soy yo con la medición, y si difiere vuelve a mesa | **B (yo)** |
| 3 | La **ley de marca ajena** a `DIRECCION_ARTE` — propuesta lista, con la cita del asterisco | **mesa → A** |
| 4 | El **gate en dispositivo** de la pantalla de pago | **founder** |
| 5 | El **splash del ritual** — sigue sin asset dibujado (bloqueos 1 y 3) | **founder / diseño** |
