# S113-E · lote 1.2 — razas, costo del contenido, y la despedida

**Rama `pista/s113-e-1.2` · base `5e8b8982`.** Cero migraciones, cero deploys,
cero aparato. Todo lo que escribe corre en transacción con `ROLLBACK`.

---

## E7 · El conjunto de razas

### 🔴 No son «las fotos del founder»: son datasets públicos

`~/Downloads/fotos_mascotas_muestra/` viene de **dog.ceo / Stanford Dogs,
TheCatAPI, Oxford-IIIT Pet y Petfinder**. Su propio `FUENTES.md` avisa:
*«la mayoría permite investigación pero no uso comercial»* ⇒ sirven para
**evaluar**, que es lo que se hizo, **y nada más**.

### La raza ya está etiquetada: **1759 de 1759**. No hace falta tabla

`manifiesto.csv` trae `raza` en las 1759 filas, y además está en la ruta. La
pregunta del encargo —cuántas la traen en el nombre— se contesta con **todas**.

### 🔴 Pero sólo un tercio es MEDIBLE, y eso decide el conjunto

| | conjunto | catálogo |
|---|---|---|
| perro | **174 razas** (inglés) | **44** (español, regional: Criollo, Pastor alemán) |
| gato | **12** | **20** |

Puntuar contra las etiquetas del dataset mediría al modelo sobre razas que **el
producto no puede representar**: un modelo que contesta «Appenzeller» está dando
una respuesta que nuestro catálogo no sabe guardar. **Se mide la intersección:
48 razas · 143 fotos** (3 por raza).

**El mapa es mi juicio y se declara.** Emparejar por texto falla en las dos
direcciones: por igualdad exacta se pierde `akita` ≠ `akita inu`; por
contenencia se gana `affenpinscher → pinscher`, que **son razas distintas**.
Revisado a mano, raza por raza; **4 rechazadas** (`affenpinscher`, `puggle` —un
cruce—, `husky` —dog.ceo no distingue Siberiano de Alaskan— y `boston bulldog`)
y lo dudoso se excluye en vez de resolverse.

🔴 **Y una corrección a mí mismo:** había rechazado seis más por «no tienen
equivalente». El error era de método — **comparé contra la lista de NOMBRES, que
venía truncada, en vez de contra los SLUGS.** El catálogo sí las tiene, con
**cuatro erratas**: `shnauzer` sin la c · `jack-rusell` con una l ·
`pitbul-terrier` con una l · `stanffordshire-bull-terrier` con una n de más.
*Un censo contra la fuente equivocada da un cero perfectamente creíble.*
(D ya lo anticipó en su prompt: *«copiá el código EXACTO… aunque te parezca que
está mal escrito»*.)

### 🔴 `sugerir-raza` NO está desplegada (404). La corrida va por API

Como con el carnet: el prompt se **lee de `origin/pista/s113-d-1.2`** en cada
corrida, no se copia. Sin razonamiento, `max_tokens` 1000.

## LA TABLA — Haiku 4.5 vs Sonnet 5

| | **Sonnet 5** | Haiku 4.5 |
|---|---|---|
| **top-1 (código exacto)** | **83,9 %** | **20,4 %** |
| **top-3 (código exacto)** | **88,1 %** | 22,5 % |
| top-1 *normalizando el separador* | **86,0 %** | 45,1 % |
| top-3 *normalizando el separador* | **91,6 %** | 50,0 % |
| **códigos inválidos** | **22** | **277** |
| respuestas vacías | 0 | 2 |
| marcadas mestizo | 13 | 6 |
| p95 | 2.230 ms | 1.670 ms |
| **costo por foto** | **$0,00380** | $0,00150 |

### 🔴 El hallazgo: **los modelos aciertan la raza y erran el SEPARADOR**

Los códigos inválidos son casi todos **guión bajo en vez de guión**:
`yorkshire_terrier` por `yorkshire-terrier`, `labrador_retriever` por
`labrador-retriever`, `shih_tzu` por `shih-tzu`.

⇒ **el 20,4 % de Haiku es mitad problema de formato**: normalizar el separador
lo lleva a 45,1 %. Sonnet pierde sólo 2,1 puntos por lo mismo.

**Cura barata para D, y no es de prompt:** la edge **ya valida contra la lista
blanca**. Si normalizara el separador **antes** de validar, Haiku pasa de 20,4 a
45,1 y Sonnet de 83,9 a 86,0 — **exactitud gratis para los dos, con una
normalización en el validador.** *El prompt ya pide el código exacto; pedirlo
más fuerte no va a arreglar un guión.*

**Veredicto propuesto: Sonnet.** 86,0 contra 45,1 en reconocimiento de raza, y
83,9 contra 20,4 sobre el contrato tal como está escrito. Haiku cuesta 2,5×
menos y es 0,5 s más rápido; ninguna de las dos cosas compensa 41 puntos.

### Los casos SIN ANIMAL — y uno de mis rótulos estaba mal

Los tomé prestados del conjunto de carnets, porque un conjunto de fotos de
mascotas **no puede medir qué hace el modelo cuando no hay animal**.

- **`captura-de-la-app.jpg`** — los dos contestaron `sin_animal: false` y
  propusieron `american_bully`. 🔴 **Mi rótulo estaba mal: la captura muestra la
  foto de perfil de Zeus, que es un American Bully.** Los dos modelos
  **acertaron**; el que se equivocó fui yo.
- **`carnet-de-papel.jpg`** — Haiku ✅ `sin_animal: true`. **Sonnet devolvió JSON
  ilegible** y no se pudo puntuar. *Es el único caso del lote donde Haiku
  contesta y Sonnet no.*

---

## E8 · El costo del contenido por raza — **los dos números, y el segundo confirma al primero**

`contenido_raza` **ya corrió**: 100 llamadas, Sonnet 5, 950 tokens de entrada y
858 de salida en promedio. **100 es el catálogo entero** (44 perro + 20 gato +
10 ave + 8 conejo + 10 pez + 8 roedor).

| | |
|---|---|
| **estimado** a tarifa estándar · `100 × (950 ent + 858 sal)` | **$1,048** |
| **real** de `ia_uso` | **$0,52385** |
| por raza | **$0,00524** |

**El real es exactamente el 50 % del estimado ⇒ el descuento de Batch, y el
número lo confirma solo.** La otra huella del Batch está en la tabla:
`latencia_ms` viene **NULL** en las 100 filas, porque el Batch no reporta
latencia por llamada.

⇒ **regenerar el contenido de las 100 razas cuesta medio dólar.** No es una
restricción de nada: si hay que rehacerlo por un cambio de voz, se rehace.

---

## E9 · La despedida

### El push: **la puerta es única, y DISCRIMINA — probado, no leído**

El censo estático daba 5 productores «que no filtran `estado_vida`». **Medido,
no son huecos:** `notificacion_intencion` tiene **una sola puerta**
(`registrar_intencion_notificacion`) y **cero funciones escriben la cola
directo**. Los tres productores reales pasan por ella; los otros dos son
LECTORES. ⇒ **el push está cubierto por construcción, no porque cada productor
se acuerde.**

**Y la puerta se vio negarse**, con su control positivo al lado:

```
control_positivo_viva_enviable    1
ROJO_fallecida_enviable           0
motivo_de_la_fila_fallecida       descartada_memorial
estado_de_la_fila_fallecida       descartada
veredicto  DISCRIMINA: encola para la viva y descarta la fallecida
```

🔴 **Mi primer ensayo dio un rojo FALSO** y lo digo porque es la lección: conté
**filas** de la cola, y **la cola registra también lo descartado, para poder
auditarlo**. Contar filas mide «se registró la intención», no «se va a enviar».
Lo que decide es `motivo IS NULL`. *Un instrumento que mide la magnitud
equivocada da un rojo con toda la autoridad de un hallazgo.*

*(La puerta exime a `categoria = 'seguridad_cuenta'`, y está bien: un aviso de la
cuenta tiene que llegar aunque la mascota haya muerto — es de la persona.)*

### Las ofertas: **15 candidatos → UNO real**, y su rojo producido

De 15 funciones que no nombran `estado_vida`: **12 pasan por
`_mascota_elegible_servicio`** (que sí filtra), **2 no reciben mascota** (el
paquete es del hogar, firma de S107) y queda **una**:

🟡 **`_guarderia_puede_reservar` dice `puede: true` para una mascota fallecida.**
Medido con control positivo —una mascota viva de la misma familia da lo mismo—
así que no es el instrumento: **no discrimina**.

**Su alcance, medido y por eso 🟡 y no 🔴:** la reserva real **rebota**.

```
reservar_dia_guarderia(…, mascota fallecida)  →  rebotó: mascota_no_elegible
```

⇒ **no se puede crear ninguna reserva.** Lo que falla es el **consejero**: es
`EXECUTE`-able por `authenticated`, está en los tipos generados, y una pantalla
que lo consulte va a mostrar «sí, podés reservar» sobre una mascota que se
murió — y recién al confirmar aparece el rebote. **Es la Ley 23 exacta: *la
puerta no ofrece lo que va a rechazar*.** La cura es una línea: que el helper
consulte `_mascota_elegible_servicio`, como hacen sus doce hermanas.

---

## Lo que NO hice

- **No corrí `sugerir-raza` por la edge**: no está desplegada (404). La corrida
  fue por API con el prompt de la rama de D, que es lo que permite variar el
  modelo con todo lo demás igual.
- **No apliqué ninguna migración ni desplegué nada.** El hueco de
  `_guarderia_puede_reservar` queda con su rojo producido y su cura nombrada;
  aplicarla es de A.
- **No elegí modelo**: el veredicto de razas es propuesto.
- **No usé las fotos para nada que no sea evaluar** — su licencia lo pide.
- **Cero aparato.**
