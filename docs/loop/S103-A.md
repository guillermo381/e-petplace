# S103 · PISTA A — EL COBRO QUE LLEGA SOLO · BITÁCORA

> **Mesa 104 del founder = sesión `S103` del repo.** El número se midió, no se
> eligió — la medición y su control positivo viven en la nota de depósito de
> `docs/PLAN_MESA_104.md`. *«104» es numeración de mesas y no entra al repo,
> mismo precedente que la mesa 103 → sesión S102.*
>
> **Estatuto de la tanda 0: CENSO, TODO LECTURA.** Orden literal del plan §2:
> *«las cuatro, en el mismo turno, sin escribir código»* y *«No escribas código
> ni migraciones hasta que la mesa autorice la tanda 1»*.
> **Cero migraciones · cero cableo · cero escritura sobre la DB · cero publish.**
>
> **Frente de A:** el cobro recurrente (motor + cron + avisos) · la tanda de
> S102 pendiente · las actas.
> **Letra que gobierna:** `LETRA_COBRO_RECURRENTE` v1.1 (RIGE COMPLETA, cinco
> firmas) sobre `LETRA_MOTOR_PAGOS_S101` v1.4 y `LETRA_PAGO_CITAS` v1.1.

---

## ⓪ · ARRANQUE — lo que se midió, no lo que se supuso

**Worktree (regla 85, primera decisión):**
`/Users/guillo381gmail.com/proyectos/ePetPlace/e-petplace-s103-a` · rama
`pista/s103-a` · nacida de `main` en **`17b0ff3c`** (árbol de `main` verificado
en **cero** antes de abrir: `git status --porcelain` vacío).

**Por qué A abre worktree en vez de trabajar en el primario:**
`METODO_TRES_PISTAS` §S91-③ — *una pista sin worktree envenena la ventana de
todas*. A es la única que publica, y el bundle se arma desde `main`: si A
escribe en `main` mientras trabaja, su propio WIP le saca el ancla con
asterisco, **y un publish sucio es inauditable después** (medido S91:
`eas update:view` no expone el estado del árbol). Con el worktree, `main` queda
limpio y A mergea a demanda con `--no-ff` (regla 86).

**Las TRES piezas del arranque de worktree** — la tercera **POR APP**, que es la
corrección que S102-B se hizo a sí misma el día que fundó la tabla (`L-332`,
superficie equivocada: *miró la raíz y concluyó sobre el repo*):

| pieza | estado medido | qué se hizo |
|---|---|---|
| `node_modules` | ausente | **no se instala** — la tanda 0 es lectura de DB y `grep`; no monta app ni corre typecheck. *Se instalará el día que haga falta, y ese día se declara.* |
| `supabase/.temp` | ausente | **copiada del primario** — verificado que apunta a `zyltipqscdsdsxnjclhp` |
| `apps/cliente/.env.local` (9 líneas) · `apps/prestador/.env.local` (6) | **ausentes las dos** | **copiadas las dos.** La raíz **no** tiene `.env.local` — que es exactamente el dato que volvió falsa la fila original de S102-B |

**Números vivos del canon, medidos por `grep` sobre el objeto y no leídos de
`CLAUDE.md`** (que es derivado y por eso decae — la casa lleva cuatro registros
del mismo atraso):

| qué | comando | resultado | siguiente libre |
|---|---|---|---|
| deuda más alta | `grep -oE '\bD-[0-9]{3}\b' docs/DEUDAS_CANONICAS.md \| sort -u \| tail -1` | **`D-868`** | `D-869` |
| lección más alta | idem con `L-` | **`L-335`** | `L-336` |

> **Control positivo (L-330):** el mismo `grep` devuelve además `L-714`, que es
> el typo histórico —una `D` escrita como `L`— ya registrado en el canon. *El
> instrumento ve de más, no de menos: eso prueba que no está filtrando, y obliga
> a leer la cola en vez de confiar en `tail -1` a ciegas.* **`L-335` es la más
> alta real.**

**Migraciones:** 375 locales. *No se compara contra el remoto en esta tanda —
la tanda 0 no aplica nada, y la resta local↔remoto es trabajo en vuelo de otras
pistas, no un error (cura P3 de S89).*

---

## ① · CENSO A — EN CURSO

Se reporta crudo, medición por medición, con el control declarado al lado
(L-330). *Las divergencias pantalla ↔ letra van en su propia tabla, una por
fila, sin adjudicar: la adjudicación es de la mesa (`METODO_TRES_PISTAS` §5 —
hallazgos, nunca veredictos).*

> **Hora de las mediciones de base: 22-ago-2026, 14:01 UTC** (`now()` leído del
> servidor, no del reloj local). Proyecto `zyltipqscdsdsxnjclhp`.

### ①.1 · LA PANTALLA — «Que llegue solo» EXISTE, y su texto es literal

**Dónde vive:** `apps/cliente/src/app/(tabs)/despensa/checkout.tsx` — **no es una
pantalla propia: es un bloque del ÉXITO del checkout de despensa.** Se ofrece
*después* de que la compra se pagó, con los ítems y la entrega **congelados en
refs** (el propio código explica por qué: si el carrito se repuebla desde otra
tab, el ternario configuraría la serie con la compra nueva).

**El texto literal, transcrito del diccionario** (`src/i18n/es.ts:1883-1897`):

| clave | literal |
|---|---|
| `recurrenciaTitulo` | **«Que llegue solo»** |
| `recurrenciaHonesta` | «Este pedido se cargará automáticamente al medio de pago guardado. Lo puedes desactivar cuando quieras.» |
| `recurrenciaSinPasarela` | «El cobro automático se activa cuando esté el medio de pago. Hasta entonces, nada se cobra.» |
| `recurrenciaCada` | «¿Cada cuánto?» |
| `recurrenciaElegiCada` | «Elige cada cuánto quieres que llegue y queda configurado.» |
| `recurrenciaDias` | «Cada {{n}} días» |
| `recurrenciaLista` | «Listo: quedó configurado. **Lo manejás** desde Tus pedidos.» |
| `recurrenciaActiva` | «La compra recurrente quedó configurada.» |
| `recurrenciaApagada` | «Listo: la compra recurrente quedó apagada.» |

**Las frecuencias ofrecidas** (`checkout.tsx:120`): `const CADENCIAS = [7, 15,
30, 60]` ⇒ **cuatro**, expresadas como *«Cada N días»*. **No se ofrece día fijo
del mes.**

**Qué botón hay y a qué llama:** un `Interruptor` que **solo declara la
intención** y revela las frecuencias; **el motor se toca recién al elegir la
cadencia** → `activarRecurrenciaCon(dias)` → `configurarRecurrencia(...)` →
RPC `configurar_recurrencia`. *El comentario del código declara el porqué y es
correcto: la RPC **no toma clave de idempotencia**, así que prender con un
default y dejar cambiarlo después crearía DOS series.* El apagado va por
`alternarRecurrencia(id,false)` → RPC `alternar_recurrencia`.

**Lo que la pantalla manda, medido en la llamada:** `cuenta_comercial_id`,
`items`, `entrega`, `frecuencia_dias`, `metodo_entrega`. **NO manda
`aviso_dias`** ⇒ cae al DEFAULT de la columna, que es **2** — o sea **48 h,
exactamente la firma ① de la letra.** *Coincidencia feliz, no diseño: nadie
las conectó.*

> **Control positivo (L-330):** el mismo `grep` de conceptos sobre
> `apps/cliente` corrido con un término que existe seguro (`checkout`) devuelve
> 4 archivos; el de recurrencia devuelve 4 (dos pantallas + los dos
> diccionarios). *El instrumento ve lo que existe: el conjunto chico no es
> ceguera.*

### ①.2 · LOS OBJETOS EN LA BASE — existen, y más de lo que la letra suponía

**Tres objetos**, hallados por `information_schema` con `~* 'recurrenc|suscrip|serie|abono'`:
`pedidos_recurrencias` · `suscripciones` · `suscripciones_servicio`.

> **Control positivo:** el mismo instrumento con `~* '^pedido'` devuelve
> `pedido_items`, `pedidos` y `pedidos_recurrencias` — las tres que existen.

**`pedidos_recurrencias` — 14 columnas.** Lo que TIENE: `user_id` ·
`cuenta_comercial_id` · `items` jsonb · `entrega` jsonb · `metodo_entrega` ·
`frecuencia_dias` **XOR** `dia_del_mes` · `proximo_pedido_fecha` (date, NOT
NULL) · **`aviso_dias` NOT NULL DEFAULT 2** · `aviso_enviado_para` (date,
nullable) · `activo` NOT NULL DEFAULT true · timestamps.

**LAS CUATRO FUNCIONES**, y la cuarta es la que ordena todo:

| función | qué hace, medido en su cuerpo |
|---|---|
| `configurar_recurrencia(7 args)` | **Crea la fila y devuelve `ok:true`.** Valida `auth.uid()`, la cadencia XOR, ítems no vacíos y **acceso a la mascota** por `_user_es_familia_de_mascota`. Calcula `proximo_pedido_fecha`. Devuelve una `nota`: *«El primer cobro real espera a la pasarela (D-778).»* |
| `alternar_recurrencia(id, activo)` | prende/apaga |
| `avisar_recurrencias_proximas()` | **el aviso, y está bien construido** — ver ①.4 |
| **`ejecutar_recurrencias_vencidas()`** | 🔴 **ES UN STUB QUE SE DECLARA.** Cuenta las pendientes y devuelve `ok:false, error:'pasarela_no_afiliada', pedidos_creados: 0`. Su comentario, literal: *«El día que la pasarela exista (D-778 muere), este cuerpo cobra y llama a `crear_pedido_despensa` con los items/entrega guardados.»* |

**POBLACIÓN — todo en cero del lado despensa:**

```
pedidos_recurrencias .......... 0 total · 0 activas · 0 con aviso enviado
suscripciones ................. 0   (y su columna kushki_token: 0 no-nulas)
suscripciones_servicio ........ 1 total · 1 ACTIVA · próximo cobro 2026-09-13
CONTROL: pedidos 66 · bonos 3 · pagos_intentos 43
```

> **El control es lo que vuelve legible el cero:** el mismo `select` que
> devuelve 0 en recurrencias devuelve 66 pedidos y 43 intentos de pago. *No es
> una base vacía ni una consulta rota: esas tablas están genuinamente sin
> poblar.*

**Y por qué están en cero, medido y no supuesto:** `configurar_recurrencia`
**NO rebota** — inserta y devuelve `ok:true`. ⇒ **la población en cero es
porque nadie tocó el interruptor, jamás porque el motor lo impida.**

### ①.3 · LOS CRONES — 14 vivos, y el reparto es el hallazgo

**Todos `active: true`.** Los que tocan este frente:

| jobid | nombre | schedule | qué llama |
|---|---|---|---|
| **11** | **`avisar-recurrencias`** | `0 13 * * *` (**08:00 Guayaquil**) | `avisar_recurrencias_proximas()` |
| **3** | `cerrar-renovar-planes` | `0 8 * * *` (03:00 GYE) | `cerrar_y_renovar_planes()` |
| 14 · 15 | `pagos-conciliar-*` | `0 17` y `15 21` | el precedente de hora declarada del barrido de pagos |

> ### 🔴 **NO EXISTE NINGÚN CRON QUE COBRE LA RECURRENCIA.**
>
> **Medido con su control en la misma consulta:**
> `cron.job` con `command ilike '%ejecutar_recurrencias%'` → **0** ·
> con `'%avisar_recurrencias%'` → **1**. *El instrumento encuentra la hermana:
> el cero no es del instrumento.*
> Y **ninguna otra función de la base la llama**: `pg_get_functiondef` sobre
> todo `public` buscando su nombre → **0** (excluyéndose a sí misma).

**⇒ El aviso está enchufado al reloj; la ejecución no.** *Y eso es coherente
con que la ejecución sea un stub — enchufar un stub a un cron solo quemaría un
tick por día.* **Lo que no es coherente es que el aviso SÍ esté enchufado.**

### ①.4 · EL CANAL DE AVISOS — existe, es el correcto, y **está en sombra**

`avisar_recurrencias_proximas()` es **SECURITY DEFINER con `search_path`
fijo**, y su cuerpo hace lo que la letra pide, con una excepción:

- **Ventana:** `proximo_pedido_fecha - aviso_dias <= current_date` y
  `proximo_pedido_fecha >= current_date` ⇒ con `aviso_dias=2`, **48 h.** ✅ §3.
- **Idempotencia:** clave `'recurrencia:'||id||':'||fecha` + guarda
  `aviso_enviado_para IS DISTINCT FROM proximo_pedido_fecha`. ✅
- **Canal:** `registrar_intencion_notificacion('pedido_recurrente', …)` ⇒ **el
  canal que la letra manda medir EXISTE y es éste.** No nace canal nuevo.
- 🔴 **La carga (`payload`) lleva:** `recurrencia_id`, `proximo_pedido_fecha` y
  `'puede': 'saltar, mover o cancelar'`. **NO lleva monto. NO lleva medio de
  pago.**

**El tipo, medido en `cat_notificacion_tipos`:**

```
pedido_recurrente · categoria=operacion · activo=true · audiencia=cliente
                  · canal_forzado=(null) · ignora_techo=false
                  · en_sombra = TRUE   ← 🔴
```

> **Control positivo, en la misma consulta:** `pago_confirmado` sale
> `en_sombra=false · canal_forzado=email · ignora_techo=true`. *El instrumento
> lee la columna y distingue: el `true` de `pedido_recurrente` es un dato, no
> un default de lectura.*

**⇒ Hoy el aviso se registra y NO se entrega a nadie.**

### ①.5 · LA TANDA DE S102 — intacta, sin aplicar, y su bloqueo se levantó

**Dónde vive:** `docs/relevamientos/2026-08-21-s102b-TANDA-DE-APLICACION.md`
(la orden) + `…-s102b-CURA-3-rls-pagos-intentos-cita.sql` (el paso 1 con su
guard) + `CURA-1-fila-co-14pct.sql` + `CURA-2-ranking-fuera-de-anon.sql`.

**NO es una migración sin aplicar:** medido con `supabase migration list
--linked` → **375 local = 375 remoto · 0 solo-local · 0 solo-remoto.** *La
tanda vive como artefacto en `docs/`, que es donde S102-B la dejó a propósito.*

**Estado medido de sus tres precondiciones, hoy 14:01 UTC:**

| qué | esperado por el guard | hoy | lectura |
|---|---|---|---|
| `pagos_intentos.pagador_user_id` | no existe todavía | **0 columnas** | ✅ **el paso 1 NO está aplicado** |
| policy `pagos_select` conoce al pagador | no todavía | **0** | ✅ el guard no aborta por eso |
| *(control)* la policy `pagos_select` existe | 1 | **1** | ✅ el instrumento lee `pg_policies` |
| snapshot citas | **7** | **8** | ⚠️ **VENCIDO** |
| snapshot pedidos | **34** | **35** | ⚠️ **VENCIDO** |

> **El snapshot se movió UNA vez y está quieto:** S102-B midió 8/35 el **21-ago
> 22:08 UTC**; hoy, **16 horas después**, sigue en **8/35**. *La deriva la
> produjo el gate de S101-D (dos citas reales) y nada más escribió desde
> entonces.* **No se edita** — `L-329`: se re-mide bajo veda en el acto del
> apply, declarando la hora. Los dos números están marcados `⚠️ VENCIDO` en el
> propio `.sql`, con el 8/35 al lado **declarado como contexto y no como el
> valor a poner**.

🔴 **Y LO QUE CAMBIÓ Y NADIE FUE A COBRAR:** el freno que dejó la tanda sin
cerrar era *«las 8 citas pendientes son incobrables por construcción — su
desglose se congela al nacer y nacieron antes del trigger»*, y exigía **una
cita NUEVA por el camino real**. **S101-D la produjo:** su tanda 9 dejó
`5cfec36f` **pagada** y **`4f7a18ce` `pendiente_pago` con su desglose
congelado, declarada explícitamente como insumo de S102-B.**
⇒ **El bloqueante de la tanda está levantado. La tanda no se aplicó porque
nadie volvió a ella, no porque siga trabada.**

### ①.6 · EL GATE DE S101-D — ③ y ⑥ **SIN VEREDICTO**

**Medido en `docs/loop/S101-D.md`**, sección `⏸ ABIERTO ESPERANDO AL FOUNDER`,
literal:

> *«El veredicto del gate — ③ la elegida · ⑥ bajo el fold · el acabado del alta
> con el botón nuevo. **Son juicios de píxeles**: los cinco pasos están verdes
> por instrumento, pero el ojo no lo puede reemplazar un invariante.»*

Y en `⏸ ABIERTO EN COLA`:

> *«**Tanda 2: ③ y ⑥**, que **no se construyen antes del juicio del ojo** —
> construir primero sería elegir por él.»*

**⇒ NO hay veredicto sobre ③ ni sobre ⑥.** Los pasos 3, 4 y 5 del gate **sí**
tienen veredicto verde por instrumento (cobro fresco punta a punta contra
sandbox · `cuenta/pagos` · voseo). **Y queda pendiente también la autorización
de push del código** (regla 82: hubo escrituras después de la última).

### ①.7 · DIVERGENCIAS PANTALLA ↔ LETRA — una por fila, sin adjudicar

*La letra lo ordenó explícitamente en su §10: «la letra se ajusta a la promesa
viva o la promesa se corrige — nunca divergen en silencio». **Acá van los
hechos; la adjudicación es de la mesa.***

| # | qué dice la pantalla / el motor | qué dice la letra | tamaño |
|---|---|---|---|
| **1** | `recurrenciaSinPasarela`: *«El cobro automático **se activa cuando esté el medio de pago**. Hasta entonces, nada se cobra.»* | — | 🔴 **La premisa se cumplió y la consecuencia no.** El medio de pago **existe** desde S101 (motor vivo, 7 tarjetas guardadas). Leída hoy, la frase **promete que el cobro automático ya está activo**, y `ejecutar_recurrencias_vencidas` sigue devolviendo `pedidos_creados: 0`. *La frase no envejeció: se dio vuelta.* |
| **2** | El aviso promete *«saltar, mover o cancelar»* | §2: se corta desde la app | 🔴 **Existe `alternar_recurrencia` (cancelar/reactivar). NO existe saltar. NO existe mover.** Medido: las funciones `~* 'recurrenc'` son **cuatro** y ninguna las hace. *Dos tercios de la promesa no tienen camino.* |
| **3** | `pedidos_recurrencias` **no tiene columna de medio de pago** | §2: *«La autorización nombra un medio de pago concreto (el token guardado)… jamás se cobra a una tarjeta que el cliente no eligió para esto»* | 🔴 Sin esa columna, §6 *(«medio de pago muerto: no se salta a otro»)* **es inexpresable**: no hay «otro» del cual no saltar |
| **4** | No hay columna de **monto esperado** ni de **acto de autorización** más allá de `user_id`+`created_at` | §2: se guarda *«quién autorizó, cuándo, sobre qué medio, con qué cadencia y qué monto esperado»* | 🟠 cadencia ✅ · quién ✅ · cuándo ✅ · **medio ❌ · monto ❌** |
| **5** | `activo` es **un booleano** | §6: **pausa ≠ cancelación**, con 3 días de reintento y aviso el día 0 | 🔴 Con un booleano **no se distingue** «el cliente la apagó» de «la casa la pausó por fallo». **No hay contador de reintentos, ni fecha de fallo, ni causa.** §6 no tiene dónde escribirse |
| **6** | El aviso **no lleva monto** | §3 y §5: *«El monto del aviso ES el monto que se cobra»* | 🔴 el contrato central de la firma ② no puede cumplirse con la carga actual |
| **7** | `pedido_recurrente` · **`en_sombra = true`** | §3: hay aviso 48 h | 🔴 el aviso **no llega a nadie**. El resto de la cadena está bien construida |
| **8** | Pantalla ofrece 4 cadencias `[7,15,30,60]` | — | 🟢 el motor acepta `frecuencia_dias` **7–90** y `dia_del_mes` **1–28**; **el camino de día fijo del mes es inalcanzable desde la pantalla.** *No es defecto: es alcance no ofrecido. Se declara para que nadie lo crea roto* |
| **9** | `recurrenciaLista`: *«Lo **manejás** desde Tus pedidos»* | tuteo neutro (regla de la casa) | 🟠 **voseo vivo.** Y promete una superficie de gestión: hay que verificar que «Tus pedidos» muestre y deje cortar la serie — **eso es censo de C, no mío** |
| **10** | `suscripciones.kushki_token` | Kushki **jubilado** desde S81 | 🟢 columna de un proveedor muerto, **0 filas no-nulas**. Inerte |
| **11** | `cerrar_y_renovar_planes()` **no toca el motor de pagos** | §1: el plan de paseos es sujeto de esta letra | 🔴 medido: su cuerpo **no menciona** `pagos_intentos` ni cobro (**control: sí menciona `suscripciones_servicio` → el instrumento lee el cuerpo**). **Hay 1 suscripción ACTIVA con próximo cobro el `2026-09-13`** ⇒ *en 22 días un plan se renueva solo, gratis* |

**🔴 EL VOSEO, Y POR QUÉ NO ES UNA CONTRADICCIÓN CON S101-D** *(se escribe
porque estuve a punto de reportarlo como tal):* medido con frontera Unicode
(`L-327`, jamás `\b`) sobre `apps/cliente/src/i18n/es.ts` → **7 voseos vivos**
(`manejás` 1 · `tenés` 1 · `podés` 1 · `querés` 1 · `contanos` 3), con **control
positivo `puedes`: 32** — el instrumento distingue las dos voces.
**S101-D declaró «voseo cero» y su literal dice: *«0 en las SEIS SUPERFICIES DE
PAGO»*.** *Su alcance era ése; el mío es el diccionario entero.* **No hay
contradicción: hay una superficie que su barrido no cubría** — y comparar los
dos números sin leer el alcance habría fabricado una acusación falsa. `L-332`
en su forma más barata.

### ①.8 · LO QUE **NO** SE MIDIÓ, declarado

- **La superficie «Tus pedidos»** — si muestra la serie y si deja cortarla. **Es
  territorio de C**, y su censo de tanda 0 la cubre.
- **El cuerpo completo de `cerrar_y_renovar_planes`** — se midió **por
  predicado** (¿menciona el motor de pagos? no; ¿menciona `suscripciones_servicio`?
  sí). *Alcanza para el hallazgo, no para diseñar su cura.*
- **Si el plan de paseos tiene desglose congelado** — no se preguntó.
- **`suscripciones_servicio` columna por columna** — solo población, estado y
  `proximo_cobro_en`.
- **La ficha 🔴 de causas de S102** (`status_detail`), de la que §6 depende para
  su voz por causa. *Se sabe que existe por el plan; no se leyó su estado.*

---

## ② · LO QUE ESTE CENSO DEJA PARA LA MESA — sin adjudicar

1. **La firma ① ya está cumplida por accidente.** `aviso_dias` DEFAULT **2** =
   48 h, y la pantalla no lo manda. *Nadie conectó la letra con la columna: se
   escribieron por separado y coinciden.* **Conviene decidir si el 2 se fija
   como constante de la letra o queda como default editable.**
2. **El aviso está a UNA fila de catálogo de estar vivo** (`en_sombra=false`) —
   **y encenderlo hoy mandaría un aviso de un cobro que no va a ocurrir.**
   *El orden importa: primero la ejecución, después la sombra.*
3. **D-778 tiene su condición cumplida y su ficha no lo dice.** El stub declara
   *«el día que la pasarela exista»* — existe desde S101.
4. **El plan de paseos se renueva sin cobrar, con fecha: `2026-09-13`.**
5. **La tanda de S102 está lista y su bloqueante se levantó.** Solo pide veda +
   re-medición del snapshot.

---

# ✅ CHECKPOINT 1 CERRADO — TANDA 1 AUTORIZADA (founder, 22-ago-2026)

## ③ · EL CONTRATO DE LA TANDA 1 — depositado ANTES de escribir

> **Orden literal del founder:** *«Tanda 1 autorizada con este contrato;
> **depositalo en la bitácora S103-A antes de escribir**»*. Se deposita
> **verbatim**, en seis puntos, tal como llegó. *Lo que la pista agrega va
> marcado como lectura de la pista, jamás mezclado con la orden.*

### ③.0 · PUSH — autorizado y EJECUTADO, con su alcance

> **Literal:** *«Push autorizado (regla 82: la autorización cubre lo commiteado
> hasta ahora; si hay commits después, re-pedís).»*

**Ejecutado ANTES de escribir nada nuevo**, para que el alcance de la
autorización y lo empujado fueran el mismo conjunto. **Verificado por SHA, no
por código de salida** (`L-239`):

```
local main : e96c1be610e754626af32892deaf90f27e410455
origin main: e96c1be610e754626af32892deaf90f27e410455   ✅ idénticos
local  pista/s103-a : 7ece09d825797ba3290774c8a7d43b464f0fb802
origin pista/s103-a : 7ece09d825797ba3290774c8a7d43b464f0fb802   ✅ idénticos
```

**Cuatro commits empujados** (los dos del arranque + los dos de la tanda 0).
🔴 **Todo commit de la tanda 1 queda FUERA de esta autorización y se re-pide.**

### ③.1 · Los seis puntos del contrato, verbatim

| # | contrato (literal del founder) |
|---|---|
| **1** | *Cura `KEY_CLIENT` en `pagos-webhook-stg` + redeploy de staging. Evidencia antes/después (las 8 filas del 21-ago como control).* |
| **2** | *Tanda de S102 bajo veda, con las dos curas de privacidad del insumo D-405 (`v_ranking` expuesta · comprobante a quien pagó). Snapshot re-medido en el acto del apply, hora declarada, reversa antes.* |
| **3** | *Recurrente — migraciones **SIN aplicar**, reversa escrita antes: estado de la serie con tres valores (**activa · pausada · cancelada**) reemplaza el booleano · contador de reintentos, fecha y causa del fallo · columna del medio de pago autorizado (token) · `aviso_dias` fijo en 2 **como cinturón, no editable** · idempotencia por período con UNIQUE.* |
| **4** | *`ejecutar_recurrencias_vencidas` deja de ser stub: cobra por la puerta única con pagador explícito, compuertas E3 enteras y desglose congelado por cobro. `cerrar_y_renovar_planes` pasa por el mismo cuerpo. Cron de ejecución con hora declarada. **Orden: primero el cobro; `en_sombra` del aviso se apaga al final, y el aviso gana monto y medio.*** |
| **5** | *Recibís de D tres migraciones sin número y el contrato del proveedor en el actuador; **las numerás al depositar** (`L-331`).* |
| **6** | *Gates: todo en mi aparato antes de convocarme. Reportá por tanda con control declarado (`L-330`).* |

### ③.2 · LECTURA DE LA PISTA sobre el contrato — declarada, no fusionada

**① El punto 4 responde la pregunta que la tanda 0 dejó abierta, y en el
sentido correcto:** *«primero el cobro; `en_sombra` se apaga al final»*. **Es la
orden inversa a la barata** —encender el aviso es una fila de catálogo, cobrar
es un motor— *y encenderlo primero mandaría el aviso de un cobro que no va a
ocurrir.*

**② El punto 3 fija `aviso_dias = 2` como CINTURÓN, no como default.** Hoy es
`DEFAULT 2` editable por argumento; el contrato lo vuelve inexpresable de otro
modo. *La firma ① deja de cumplirse por coincidencia y pasa a cumplirse por
construcción.*

**③ 🔴 CHOQUE DE TERRITORIO DECLARADO en el punto 1.** `PLAN_MESA_104.md` §1
—depositado por mí esta mañana— asigna **`supabase/functions/pagos-*` a la
pista D**. El punto 1 me manda curar `pagos-webhook-stg`, que es de D.
**Se ejecuta, y la razón se escribe para que se pueda revertir sin discutir:**

- La orden del founder es **posterior y más específica** que el plan.
- **El defecto es una OMISIÓN, no una decisión** — un símbolo usado y nunca
  declarado. Es el caso literal de la excepción de `METODO_TRES_PISTAS` §6
  *(«no era una decisión de A sino una omisión… se verifica y se firma, o se
  revierte»)*.
- **Medido antes de tocar:** `main` limpio y **ninguna worktree de S103 salvo la
  mía** ⇒ nadie tiene ese archivo abierto con cambios sin commitear.
- **Se declara a D en la bitácora.** Si D lo reclama, se revierte sin
  argumentar.

**④ El punto 5 dice «tres migraciones sin número» ⇒ D está viva**, aunque al
momento de escribir esto **no tiene worktree ni parte depositado**. *Se declara
como dato, no como reproche: es información que la mesa necesita para saber a
quién esperar.*

---

## ④ · TANDA 1 · PUNTO 1 — `KEY_CLIENT`

### ④.1 · EL ANTES — medido, con su control y con la causa literal

**Instrumento:** consulta sobre `webhook_events` acotada a `recibido_en` del
**21-ago**, partida por credencial (`application_code` termina en `-SERVER` o
no). *La partición sale del payload del proveedor, no de un supuesto nuestro.*

| credencial | filas | `stoken_valido` | `resultado` | `transaction_id` |
|---|---|---|---|---|
| **CLIENT** (`EPETPLACESTG-EC-CLIENT`) | **8** | **NULL** | **`desconocido`** | **null** |
| SERVER (`EPETPLACESTG-EC-SERVER`) | 18 | `true` | `aplicado` | — |
| SERVER | 2 | `true` | `recibido` | — |

> ### **CONTROL POSITIVO (`L-330`): las 20 filas SERVER del MISMO DÍA, por el MISMO instrumento, salen `stoken_valido = true`.**
>
> *El corte cae exacto sobre la credencial.* **Sin ese control, «8 filas sin
> validar» podría ser un buzón roto, un proveedor mudo o una consulta mal
> escrita.** Con él, las tres quedan descartadas: el buzón recibió, guardó y
> analizó **veinte eventos ese día**.

**Y la causa no se dedujo — está escrita, idéntica en las 8 filas:**

```
detalle: analisis_fallo: ReferenceError: KEY_CLIENT is not defined
```

**Las 8, entre las 05:46 y las 06:46 UTC del 21-ago.**

### ④.2 · EL DEFECTO, medido en el código

`supabase/functions/pagos-webhook-stg/index.ts:82`

```ts
const key = esServer ? APP_KEY : KEY_CLIENT;
```

- **`KEY_CLIENT` aparece UNA sola vez en todo el repo: ésta.** No hay
  declaración, no hay import que lo provea (el archivo importa exactamente dos
  cosas: `createClient` y `node:crypto`).
- El archivo declara `AMBIENTE`, `NUVEI_APP_CODE_SERVER`, `NUVEI_APP_KEY_SERVER`
  — **y ninguna CLIENT.**
- **El secret SÍ existe:** `NUVEI_APP_KEY_CLIENT`, cargado el 19-ago 22:22.
  *Verificado por nombre y digest; **cero valores leídos**.* Su digest coincide
  con el que `scripts/s101b/digest-check.mjs` ya usaba como referencia.

**⇒ El ternario evalúa un identificador inexistente ⇒ `ReferenceError` ⇒ el
análisis entero se cae. Solo en la rama CLIENT: por eso los eventos SERVER
—los que mueven plata— nunca se vieron afectados.**

### ④.3 · 🔴 EL HALLAZGO QUE NO ESTABA EN EL ENCARGO: EL JUEZ DE SÍMBOLOS ESTÁ CIEGO A ESTA CLASE

`node scripts/verify-edge-simbolos.mjs` **corrido hoy, con el defecto vivo en el
árbol**:

```
✓ 22 funciones · ningún símbolo de módulo usado sin importar
```

> ### **VERDE. Y el defecto que ese juez existe para cazar está a dos líneas de donde mira.**

**El juez nació** —dice el canon— *«porque un `createHmac` usado y no importado
tiró un 500 que mató los reintentos del proveedor para siempre»*. **Su cuerpo
mide «símbolo de MÓDULO usado sin importar»**, y `KEY_CLIENT` no viene de un
módulo: es un identificador libre. **La otra mitad de la misma clase de defecto
—usar algo que no existe— le queda afuera.**

*No falló: contestó bien una pregunta más angosta que su propósito.* Es
**`L-333`** en su forma exacta *(«el control cubre menos que el instrumento»)*,
esta vez sobre el instrumento y no sobre su autoprueba. **Y su costo está
medido: ocho eventos del proveedor sin validar durante un día, con el juez en
verde todo ese tiempo.**

**⇒ Entra como pieza de esta tanda, y su dueño es B** (`METODO_TRES_PISTAS` §1:
el lint y los jueces son de B, en exclusiva). *La pista A no lo toca: se
declara con su medición y su caso reproducible.*

### ④.4 · LA CURA Y EL DESPLIEGUE

**Una línea, junto a las otras credenciales** (`index.ts:51`):

```ts
const KEY_CLIENT = Deno.env.get('NUVEI_APP_KEY_CLIENT') ?? '';
```

**El `?? ''` no es adorno:** mantiene el fail-closed que el resto del archivo ya
tiene —sin credencial, `verificarStoken` devuelve `valido: null` y lo dice en
`detalle`— en vez de romper. *Un secreto ausente tiene que producir un nulo
honesto, jamás un 500 que mate los reintentos del proveedor.*

**Desplegado** con `--use-api --no-verify-jwt` (público por necesidad: lo llama
el proveedor). **Verificado contra el objeto, no contra la salida del deploy:**

| | antes | después |
|---|---|---|
| `version` | **29** | **30** |
| `ezbr_sha256` | `878d8f5c…` | `1d7c8f35…` |
| `verify_jwt` | false | false *(sin cambio, correcto)* |

### ④.5 · EL DESPUÉS — dos sondas, y la segunda cierra el hueco de la primera

**🔴 La primera sonda sola NO alcanzaba, y conviene decir por qué:** un POST
sintético prueba que **el código corre**, jamás que **la clave sea la
correcta**. *Con un `NUVEI_APP_KEY_CLIENT` equivocado el resultado habría sido
`stoken_invalido` — exactamente lo mismo que devuelve una firma falsa.* **Dos
causas distintas con el mismo síntoma es un instrumento que no discrimina.**

**① SONDA SINTÉTICA** — `tx = S103A-CONTROL-1`, stoken de ceros, credencial
CLIENT. HTTP **200**.

```
resultado ...... stoken_invalido
stoken_valido .. false
detalle ........ receta=hmac-sha256-v2:… · credencial=CLIENT · autenticado=false
```

**② REPLAY DE UN EVENTO REAL** — `tx = DF-2099068`, **el payload guardado el
21-ago, firmado por el proveedor**, re-inyectado tal cual. *Extraído del buzón
sin imprimirlo: trae `card` y `user` (§9 — el crudo vive en el buzón, jamás en
un log ni en un reporte).* **Verificado antes de replayar que el actuador es
inerte para esto:** `aplicar_evento_de_pago` **gatea por `SERVER` y mira
`stoken_valido`** (medido; control: la función existe). Un CLIENT nunca mueve
plata.

> ### **LA MISMA TRANSACCIÓN, TRES FILAS, ANTES Y DESPUÉS:**
>
> | cuando | `resultado` | `stoken_valido` | `detalle` |
> |---|---|---|---|
> | 21-ago 05:46 | `desconocido` | **NULL** | `analisis_fallo: ReferenceError: KEY_CLIENT is not defined` |
> | 21-ago 06:46 | `desconocido` | **NULL** | *(idem — el proveedor reintentó)* |
> | **22-ago 14:42** | **`recibido`** | **`true`** | `receta=… · credencial=CLIENT · **autenticado=false**` |

**⇒ El evento que ayer se cayó dos veces, hoy VALIDA.** Y eso prueba **las dos
cosas a la vez**: que la declaración curó el `ReferenceError` **y** que
`NUVEI_APP_KEY_CLIENT` contiene la clave correcta.

> ### **EL PAR DISCRIMINA EN LAS DOS DIRECCIONES, que es la forma fuerte del control:**
> **firma falsa → `false`** · **firma del proveedor → `true`**.
> *Si las dos hubieran dado `true`, la comparación estaría rota y el verde sería
> peor que el rojo de ayer.* **El `false` de la sonda ① no es un fallo: es la
> mitad negativa de la prueba.**

**Y `autenticado=false` es CORRECTO y no un residuo:** la credencial CLIENT es
pública por diseño —la sirve nuestra propia página de pago—, así que **un
`stoken` de alta jamás autentica movimiento de plata**. Solo SERVER lo hace. *La
cura restauró la validación; no ablandó la puerta.*

**Precisión sobre el control, medida y no supuesta:** las **8 filas son 4
transacciones distintas**, cada una reintentada. *El proveedor reintentó pese al
200 — y eso importa: el buzón nunca perdió un evento, que es exactamente lo que
su diseño de «persistir antes de analizar» prometía.*

**Lo que esta cura NO cubre, declarado:** las 4 transacciones del 21-ago siguen
con sus filas viejas en `desconocido`. **No se reprocesaron ni se editaron** —
solo se agregó la fila del replay. *Reescribir el pasado borraría la evidencia
del defecto; si la mesa quiere reprocesarlas, es una decisión aparte.*

---

## ⑤ · NOTA DE MESA — 22-ago, en vuelo

> **Firma del founder, literal:** *«todos los datos de la app son de prueba»*.

**Tres consecuencias que se registran, no se ejecutan:**

**① EL ARNÉS DEL RECURRENTE TIENE CASO CANÓNICO, y es uno vivo.** La
suscripción de `suscripciones_servicio` que **renueva el `2026-09-13`** deja de
ser un hallazgo del censo y pasa a ser **el sujeto de prueba del punto 4**:

> *«tiene que renovar **cobrando por el motor**, y una **gemela de prueba** tiene
> que **fallar a propósito** y recorrer los tres días hasta la pausa.»*

*Sin el caso de fallo recorrido, §6 de la letra no está probada — la propia
letra lo dice en su §10.5.* **Y la fecha le pone reloj al frente: el 13-sep es
el día en que esa suscripción se renueva sí o sí; si el cobro no está para
entonces, se renueva gratis otra vez.**

**② EL CORTE SEMILLA/REAL ES LA TANDA DE CIERRE DE S103.** Ya está firmado; se
registra acá con su alcance para que no se pierda:

| qué | volumen medido |
|---|---|
| citas `pago_simulado` | **138** *(heredado de `LETRA_PAGO_CITAS` §6)* |
| series de prueba | las que esta tanda cree |
| pedidos de prueba | `pedidos` = 66 al 22-ago |

**Se ejecuta con su propia autorización, cuando la mesa lo convoque, antes del
soft launch.** *No entra en la tanda 1.*

**③ Lo que la firma de «datos de prueba» NO ablanda:** las compuertas, el
pagador explícito y el desglose congelado **siguen enteros**. *Que el dato sea
de prueba cambia qué se puede borrar; no cambia por dónde tiene que pasar la
plata.*

