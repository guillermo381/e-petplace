# S113 · PISTA C — lote 0 «Nexo despierta» · parte

> **Este archivo es LA fuente del handoff.** Lo que vive sólo en la conversación
> muere con ella.
> Rama **`pista/s113-c2`** · SHA **`cc758565`** · medido contra `main` **`dd40e37e`**
> y con **`pista/s113-b` mergeada, fijada a `58642c81`** (control de ancestría
> corrido: `merge-base --is-ancestor` verde).
> Árbol limpio · pusheado y verificado **por SHA** (`ls-remote` == `rev-parse`).

> ⏪ **ESTE PARTE SE ESCRIBIÓ EN DOS TIEMPOS Y SE ENMIENDA EN VEZ DE
> REESCRIBIRSE.** El primero (`39f72aa6`) construyó contra los NOMBRES de las
> piezas de B, porque todavía no existían. El segundo (`cc758565`) las tiene en
> la mano. Lo que cambió está marcado ⏪ donde estaba; lo que sigue siendo cierto
> queda tal cual. *Un parte que se reescribe entero borra el rastro de qué se
> supuso y qué se midió.*

---

## ① LO MEDIDO ANTES DE CONSTRUIR

### 1.1 La burbuja de pendientes del cliente
- **Se monta en UN solo lugar:** `apps/cliente/src/app/(tabs)/_layout.tsx:362`,
  adentro del `tabBar` de `<Tabs>` — o sea **una vez, sobre las cinco pestañas**.
  Censo de montajes: **uno**. La pieza es `BurbujaPendientes` (`packages/ui`).
- **Qué la alimenta:** `usePendientesAdopcion()` (`lib/pendientes-adopcion.ts`,
  molde `useCarrito`: estado de módulo + `useSyncExternalStore`) sobre
  `contarPendientes()` de `@epetplace/api`, más `useCarrito()` para las unidades.
- **Qué cuenta:** `hilosConSinLeer` (⇒ `conversaciones`) y `solicitudesPorRevisar`.
  En el CLIENTE se dibujan dos clases: `carrito` y `mensajes`.
- **A dónde navega:** carrito → `/despensa/carrito` · mensajes → el hilo
  (`/adoptar/solicitud/[solicitudId]`) **si hay una sola**, si no la lista
  (`/adoptar/solicitudes`). *La regla del toque la decide el dominio, no el shell.*
- **Cómo se refresca:** `escucharPendientes()` ← `suscribirseAMisHilos`, montada
  en el shell sin deps. **Un solo camino**: `'reconectado'` llega también en la
  primera conexión, así que la carga inicial y el refresco son el mismo código.

### 1.2 El layout y el destello
- Layout: `apps/cliente/src/app/(tabs)/_layout.tsx`. Lo que ya se monta encima de
  la barra es la burbuja, con `aireInferior={altoBarra}` **medido** por un
  `onLayout` sobre la barra real (arranca en `ALTO_FILA_TABS + insets.bottom`).
- Destello del Coach, **confirmado en mi SHA** (las líneas del relevamiento
  estaban corridas): `hogar/index.tsx` **:614** (el hueco reservado de 44×44) ·
  **:628** (`pressedCoach`) · **:681** (`coachAbierto`) · **:1716-1755** (el
  `Pressable` absoluto con el orbe violeta→azul) · **:2493** (`<CoachHoja>`).

### 1.3 Las pantallas que abre la huella
| dedo | destino | estado |
|---|---|---|
| **Peso** | `RegistrarPesoHoja` (`components/registrar-peso-hoja.tsx`) → `registrarPesoMascota` | **existe**; era una Hoja con **un solo caller** (la ficha de la mascota) |
| **Vacuna** | `/carnet` con `{ mascotaId, nombre }` | **existe**; 6 callers, el canónico es `hogar/index.tsx:1405` |
| **Antiparasitario** | `/antiparasitario` con `{ mascotaId, nombre }` | **no existía**; construido acá |
| **Foto (recuerdo)** | — | 🔴 **no existe puerta** — ver ⑤ |

### 1.4 La Hoja del Coach v0
`components/coach.tsx`, montada sólo desde el Hogar. Tres preguntas
(`edad · carnet · actividad`) resueltas con plantillas sobre el perfil real.

### 1.5 La mascota en foco y el hogar
- **La ruta ya lo dice:** `mascotaId` viaja como parámetro en
  `/hogar/mascota/[mascotaId]`, `/hogar/vacunas/[mascotaId]`,
  `/citas/[mascotaId]` y `/carnet`. Se lee con `useGlobalSearchParams`
  (existe en expo-router 57.0.4, verificado en `build/exports.d.ts`).
- **Cuántas mascotas:** `obtenerMascotasDeFamilia(familiaId)` tras
  `getEstadoOnboardingDueno()`. ⚠️ **Medido: ese par se repite en más de diez
  pantallas** y no había estado compartido. Nace `lib/nexo/hogar-vivo.ts`,
  **una lectura por sesión** en el shell.
- `MascotaResumen` ya trae lo que hacía falta: `sujeto`, `estado_vida`,
  `raza_ruta_imagen`. **No hizo falta pedir nada.**

### 1.6 Dónde la presencia no debe existir
`carnet` · `videollamada` · `videoconsulta` · `checkout` (y `checkout-plan`,
`checkout-paquete`) · `carrito` · `pagos` · `solicitud`.

🔴 **Hallazgo de paso, medido y no curado (es del carrito, no de Nexo):** el
guard vivo `silenciaCarrito` compara `s === 'checkout'` y **no alcanza a
`checkout-plan` ni a `checkout-paquete`** — dos checkouts reales del paseo.
*Un guard que pasa siempre no se descubre: se descubre el disco encima del
botón de pagar.* Nexo compara **por prefijo** y sí los cubre; la burbuja
—que sigue viva en memorial— **no**. Queda declarado, no tocado.

---

## ② EL ROJO, ANTES DE CONFIAR EN EL VERDE

`apps/cliente/scripts/verify-nexo.mts` llama a **las mismas funciones que
corren en pantalla** (por eso `lib/nexo/*` no tiene un solo import de runtime)
y a una instancia i18next real con los diccionarios reales.

Se rompió a propósito y dio rojo en las dos clases que vigila:

```
ROJO · sin Nexo en el checkout del PLAN — el que el guard viejo NO alcanzaba
ROJO · sin Nexo en el checkout del PAQUETE — ídem
ROJO · es · nexo.almohadilla interpola el nombre — Pregúntale a {{n}}
ROJO · es · nexo.almohadilla no deja una llave cruda — Pregúntale a {{n}}
ROJO · 140 casos, 4 fallos.
```

La segunda mitad es **el defecto exacto que esta misma pista curó ayer en el
prestador** — la llave cruda en pantalla — cazado ahora en el cliente antes de
que llegue a un teléfono. Restaurado: **VERDE · 140 casos, 0 fallos**
(147 con `--control`).

---

## ③ LO CONSTRUIDO

**Nexo** se monta una vez en el shell, sobre la barra, en las cinco pestañas.
Cerrada: un orbe con la **`Huella` canónica** (se importa, no se redibuja — §2.2
de `DIRECCION_ARTE`) sobre el gradiente violeta→azul, **la misma receta del
destello que retira**, y **un arco por clase viva**. Abierta: las pastillas de
pendientes arriba, los **cuatro dedos en orden de pata** (fila que envuelve, así
el orden de lectura sigue siendo izq→der) y **la almohadilla** pegada al disco,
que es donde está en una pata de verdad.

Los estados: `dormida` sin pendientes · `atenta` con alguno · `despierta` con la
pata abierta · `hablando` con la Hoja abierta.

**La Hoja** gana cabecera (orbe chico + nombre) y **la presentación**: la primera
vez en el dispositivo preside sola —la advertencia antes de que la IA conteste
nada—, después vive abajo, chica, siempre. **Es UNA key en dos lugares.** Las
tres respuestas del v0 y su pie quedaron intactos: **ninguna promesa nueva**.

---

## ④ LAS DECISIONES QUE SALIERON DE MEDIR

**① La segunda clase se llama `carrito`, no `pedidos`.** El encargo la nombró
«pedidos»; medido, la segunda clase de la burbuja del cliente **es el carrito**,
y §2.4 manda que la pastilla abra lo mismo que abría la burbuja. Apuntarla a
Pedidos **le habría quitado al carrito su única puerta global**, que es firma del
founder (N28). *Un renombre que retira una puerta firmada no es un renombre.*

**② `avisos` nace en `null`, y es letra, no hueco.** El servidor expone
`hayAvisosSinLeer` —un **booleano**— porque `MODELO_LOYALTY` §3 manda que los no
leídos sean PRESENCIA y jamás número. Una pastilla lleva número ⇒ la clase existe
en el tipo y **no se dibuja**. Eso es «ninguna violeta», y no un pendiente.

**③ No nació `obtenerPendientesHogar`.** El conteo ya lo contaba el servidor
(`contarPendientes` + `useCarrito`). *Pedir un wrapper nuevo para leer dos veces
el mismo hecho es fabricar la divergencia.*

**④ Los cuatro dedos van SIN glifo.** Medido contra el registry (55 nombres):
existe `vacuna`; **no existen** peso, antiparasitario ni foto. Inventar tres se
saltaría el gate por ícono de §6b, y poner glifo en uno solo es peor que en
ninguno. Llevan su palabra. **Los tres glifos quedan pedidos a B.**

**⑤ La pata de la casa tiene TRES dedos y acá se abanican cuatro atajos.** No es
contradicción: **el dibujo no se toca**; lo que se abanica son pastillas.

---

## ⑤ LO QUE PEDÍ POR NOMBRE

### A A — **la puerta del recuerdo** 🔴
`evento_hito_narrativo` **existe en la base** con sus dos claves
(`vida_nueva_empieza`, `mundo_nuevo_empieza`) y **cero filas**; `packages/api`
**no tiene un solo escritor** (el alta lo dejó anotado y apagado a propósito:
*«la voz se firma en el gate de pantalla»*). ⇒ **el dedo «Foto» nace apagado con
su razón a la vista.** Falta: un wrapper que escriba un hito narrativo con foto
(nombre a elección de A) y la firma de su voz. *Un atajo que navega a ninguna
parte es peor que uno apagado.*

### A B — **tres glifos**, con su estudio §6b y su gate por ícono
`peso` · `antiparasitario` · `foto`. Hoy los cuatro dedos llevan texto; el día
que los tres existan, `vacuna` ya está.

### A B — **la cabecera de la Hoja**
El encargo la daba por hecha («orbe violeta chico + nombre»). **No existe como
pieza**: la compuse con `Huella` + el gradiente firma, copiando la receta del
destello que retiro. Si B la quiere como pieza, la reemplaza sin tocar nada más.

### Nada de esto llegó — se construyó alrededor y queda declarado.

---

## ⑥ LO QUE NO HICE

- **Cero aparato.** Nada de esto se vio en un teléfono. El gate ejercita las
  funciones reales contra un i18next real; **no es un render**.
- **No publiqué OTA** ni mergeé a `main`.
- **No toqué `packages/` ni `supabase/`.** Cero dependencias nuevas: el lockfile
  no se movió ⇒ el lote sale por OTA.
- **No curé `silenciaCarrito`** (el guard por igualdad de 1.6). Es de la burbuja,
  que sigue viva en memorial. Declarado arriba.
- **No agregué el gate al `package.json` raíz** — no es apps/. La línea, si A la
  quiere: `"verify:nexo": "tsx apps/cliente/scripts/verify-nexo.mts"`.
- **`verify:puerta-unica` NO EXISTE** (medido: no está en el `package.json` ni en
  `scripts/`). La puerta única se verificó por grep: **cero** `supabase.`,
  `.rpc(` o `.from(` en todo lo nuevo.

---

## ⑦ GATES AL CERRAR

| gate | resultado |
|---|---|
| 4 typechecks (api · ui · cliente · prestador) | **0** |
| `verify:diseno` | **VERDE, 61 reglas** |
| `verify:nexo` (nuevo) | **140 casos, 0 fallos** (147 con `--control`) |
| `verify:razon-muda` | **VERDE — el trinquete no subió** |
| `verify:abanico` | **10/10** |
| `verify:ref-antes-de-uso` | **VERDE** |
| puerta única (por grep) | **0 accesos directos** |

⚠️ **Tres reglas de `verify:diseno` se pusieron rojas y las tres eran mías**, las
tres porque la composición vive en `apps/` y su gemela `BurbujaPendientes` vive
en `packages/ui`, donde esas reglas no llegan:

- **R5** — usaba `theme.accent.cta` para el arco del carrito. Ese slot lo resuelve
  `Boton`. Curado: el arco usa `accent.primary`. *El orbe ya es la marca; el arco
  sólo tiene que distinguir la clase.*
- **R7** — cinco `FadeIn` artesanales. Curado con `Entrada`, el portador único de
  §5, que además trae resueltos **reduce-motion y memorial**, cosa que un
  `FadeIn` a mano no tiene. El disco cerrado **perdió su entrada a propósito**:
  no aparece, está.
- **R53** — el overlay leído como pie fijo. Declarado con `R53-DECLARADO` y su
  razón (dos veces, una por capa): no envuelve contenido, y la cola la exporta la
  pieza (`COLA_NEXO`), igual que `COLA_BURBUJA_PENDIENTES`.

Y **`verify:razon-muda` subió 140 → 141** por el botón Guardar de la pantalla
nueva. **No se subió el baseline**: el botón ahora dice por qué está apagado
(`razonDeshabilitado`), que es lo que el encargo pide y lo que la regla existe
para conseguir.

---

## ⑧ EL PRÓXIMO PASO EJECUTABLE

**El founder valida en el teléfono por OTA** (A mergea hasta `39f72aa6`). Lo que
va a ver primero: la pata en la esquina, y que el destello del Hogar ya no está.

---

# ⏪ ENMIENDA — EL LOTE MONTADO SOBRE LAS PIEZAS REALES DE B (`cc758565`)

> Adenda de la mesa con el cierre de B en la mano. Todo lo de arriba sigue
> siendo lo que se midió; **acá está lo que cambió al tener las piezas.**

## ⑨ LO QUE MURIÓ, Y ES LO MEJOR DE ESTA VUELTA

**☠️ `components/nexo/presencia-nexo.tsx` (395 líneas).** Existió para llenar la
ausencia de la pieza de B y **se retiró en el mismo commit en que la pieza
existe** (Ley 37). Con ella se fueron **sus tres declaraciones contra
`verify:diseno`** —R5 el `accent.cta` a mano · R7 los `FadeIn` artesanales · R53
el overlay leído como pie fijo—: las tres eran **el precio de que la composición
viviera en `apps/` mientras su gemela vivía en `packages/ui`**, donde esas reglas
no llegan. *Los gates estaban diciendo, desde el primer día, que esto era una
pieza; no había forma de escucharlos sin cruzar territorio.*

**☠️ La mitad de `lib/nexo/estado.ts`.** Nació con su propio `ClaseNexo` y
`CuentasNexo`; B trae `ClaseCoach` y `PendientesCoach` **con la misma forma**.
*Dos vocabularios para la misma cosa conviven hasta que alguien lee uno y edita
el otro.* Quedan las dos decisiones que **sí** son del shell: en qué pantallas la
presencia no existe, y en qué estado está.

**☠️ La cabecera compuesta a mano** en `coach.tsx` → `CabeceraCoach`.

## ⑩ LO QUE EL SHELL LE PASA A LA PIEZA

| prop | qué le paso | por qué |
|---|---|---|
| `estado` | `estadoNexo(...)` | la Hoja gana a la pata; la pata gana a los pendientes |
| `pendientes.chat` | conversaciones sin leer | `contarPendientes`, el mismo lector de la burbuja |
| `pendientes.pedidos` | **unidades del CARRITO** | ver 🔴 abajo |
| `pendientes.avisos` | **`null`** | el servidor expone un BOOLEANO, no un número |
| `voz.chat` · `voz.pedidos` | **siempre**, con singular y plural | un número no puede existir sin su palabra |
| `aireInferior` | el alto **medido** de la barra (`onLayout`) | *un número tecleado miente en el primer teléfono distinto* |
| `onRazonApagado` | el aviso de la casa, una línea | la pieza no elige el vehículo |
| `atajos` | tupla de **cuatro**, vivo **o** apagado-con-razón | los dos a la vez **no compilan** |

🔴 **`pedidos` NO se apuntó a la tab de Pedidos, aunque el nombre lo pida.** §2.4
manda que la pastilla abra lo mismo que abría la burbuja, y **la puerta global
del carrito es firma del founder** (N28) **y era su única puerta**. La voz dice
«Carrito · 3», que es lo que la familia lee; **el nombre de la prop es un slot de
la pieza, no una promesa de destino.** *Si la mesa quiere que abra Pedidos, eso
es un pedido a A —no hay conteo de pedidos en curso en el servidor— y una
decisión sobre qué pasa con la puerta del carrito.*

## ⑪ LOS GLIFOS: TRES SON PRÉSTAMOS, Y SE DECLARAN

`AtajoCoach` **exige** `icono`, así que los cuatro dedos tienen dibujo. Medido
contra el registry (55 nombres): **existe `vacuna`; no existen peso,
antiparasitario ni foto.** No se inventó ninguno —un glifo se firma con su
estudio §6b y su gate por ícono—: se prestan los tres más cercanos, **mismo
precedente que `pedido` montando `despensa` durante un commit**.

- **peso → `datos`** (barras que suben). *No es comodidad: el peso ES una serie
  —«pasó de 9 a 12 en cuatro meses» es medicina— y ese glifo dibuja eso.*
- **antiparasitario → `receta`.** ⚠️ Costo: también nombra recetas.
- **foto → `ojo`.** ⚠️ **Es el más flojo, y se declara flojo en vez de
  defenderlo.** Hoy se dibuja atenuado igual: no hay puerta.

⇒ **pedido a B: los tres glifos propios.**

## ⑫ LOS LATIDOS DE LA CABECERA

`pulsos` sube **por frase que llega de verdad**: una respuesta contestada. **La
presentación no cuenta** — no es algo que Nexo averiguó, es la advertencia que la
casa le debe a quien abre esto por primera vez. **Cero temporizadores:** *un
latido por reloj sería la pieza fingiendo actividad, que es exactamente lo que su
propia cabecera prohíbe.*

## ⑬ EL TYPECHECK AHORA SÍ MIDE LAS RUTAS

**Medido, y son DOS causas, no una:** `turbo typecheck` falla **0 de 7** en este
worktree porque ① falta `.expo/types/router.d.ts` —generado, fuera de git— y **el
guard de la casa rebota a propósito** (*«sin él `tsc` NO MIDE las rutas y sale
VERDE POR AUSENCIA»*, L-450), y ② `node_modules` ausente en los paquetes.

⚠️ **Y eso alcanzaba a mi propia medición:** correr `npx tsc --noEmit` a mano
**saltea el guard**, así que mis primeros cuatro verdes **no medían las rutas** —
justo en un lote que **agrega una ruta** (`/antiparasitario`) y la empuja con
`router.push`. Se generó el archivo con `expo start` y **el typecheck propio del
cliente pasa con las rutas medidas**.

**Control negativo corrido:** cambiando el destino a `/antiparasitario-que-no-existe`
el typecheck da `TS2322` con las 150 rutas en el mensaje. *El instrumento ve lo
que antes no podía ver.*

## ⑭ GATES AL CERRAR (los de B, montados en mi pantalla)

| gate | resultado |
|---|---|
| `verify:coach` (de B) | **46 verdes · 0 rojos** |
| `verify:contrast` (de B) | **396 pares · 0 fallos** |
| `verify:nexo` | **170 casos · 0 fallos** (162 sin `--control`) |
| `verify:diseno` | **VERDE, 61 reglas** |
| `verify:razon-muda` | **VERDE — el trinquete no subió** |
| `verify:abanico` | **10/10** |
| `verify:ref-antes-de-uso` | **VERDE** |
| typecheck por app (api · ui · cliente · prestador) | **0**, y el del cliente **con las rutas medidas** |

**Sigue en pie: CERO APARATO.** Nada de esto se vio en un teléfono.

---

# ⏪ SEGUNDA ENMIENDA — LA ADENDA DE LA MESA (`de236a35`)

## ⑮ (a) CONTRA QUÉ GUARDA — y la condición NO aplica

**El botón Guardar llama a `registrarDesparasitacion(mascotaId, {...})` de
`@epetplace/api`, y existe en mi SHA.** No lo trajo este lote: **nació en S82-A**
(`packages/api/src/wrappers/salud.ts:53` · exportado en `index.ts:332` · la RPC
`registrar_desparasitacion` está en los tipos generados).

🔴 **`registrarDesparasitacionFamilia` NO EXISTE — y lo dice el compilador, no
yo.** Control negativo corrido: renombrando el import a ese nombre,

```
TS2724: '"@epetplace/api"' has no exported member named
'registrarDesparasitacionFamilia'. Did you mean 'registrarDesparasitacion'?
```

*Era un nombre de mesa, no una puerta faltante.*

**Cero tipos declarados a mano en la pantalla:** `TipoDesparasitacion`
(`'interna' | 'externa' | 'mixta'`) **se importa del paquete**, y los tres
códigos de rebote que se traducen a una línea —`producto_requerido` ·
`fecha_futura` · `orden_fechas_invalido`— son del wrapper.

⇒ **el dedo «Antiparasitario» queda VIVO.** El único atenuado por falta de
puerta sigue siendo «Foto».

## ⑯ (b) LAS RAZONES NOMBRAN A LA MASCOTA

| key | voz |
|---|---|
| `nexo.razonSinPuerta` | «Pronto vas a poder guardar un recuerdo de **{{mascota}}** desde acá.» |
| `nexo.razonAcuario` | «**{{mascota}}** es un acuario: no lleva vacunas ni antiparasitario.» |

Ni «próximamente» ni «en construcción»: *las dos hablan del producto y no de la
familia.*

⚠️ **El borde que apareció al escribirlas:** con varias mascotas activas y
ninguna en foco **no hay a quién nombrar** — la razón vale para todas. Ahí entra
**`alta.tuMascota`**, el fallback que la casa **ya usa en `/carnet`**. Una key
cada una, como pidió la mesa; el caso sin nombre se resuelve con la palabra que
ya existe **en vez de una segunda frase que algún día diverge** (L-175).

## ⑰ (c) `silenciaCarrito` CURADO POR PREFIJO

Comparaba `s === 'checkout'` y **no alcanzaba a `checkout-plan` ni a
`checkout-paquete`**. Ahora prefijo —la misma forma que Nexo— con la lista en una
constante y su razón escrita.

⚠️ **El corte es en `-` a propósito:** alcanza a `checkout-loquesea` y **no** a
un futuro `checkoutear`. *Un prefijo suelto apaga de más, y apagar de más también
es un defecto, sólo que más difícil de ver.*

⚠️ **Y su comentario decía «cubre los DOS checkouts»**, así que quien lo leía se
quedaba tranquilo — el mismo patrón que esta casa ya cobró con el guard del tab.

**El control que pidió la mesa, en el gate:** en `checkout-plan`, con **sólo**
carrito pendiente, **la burbuja entera no se dibuja** (la clase se calla ⇒ cero
clases vivas ⇒ la pieza devuelve `null`). Con su contra-caso —en la vitrina y en
el Hogar **sigue viva**, el prefijo no apaga de más— y con el reparto por clase:
los **mensajes NO se callan** en un checkout (*un mensaje pendiente ahí sigue
pendiente*), en el **hilo** se callan las dos **por otra razón** (el disco cae
sobre la barra de escribir) y en el **ACTA** no se calla nada.

## ⑱ (d) B NO AVISÓ LOS GLIFOS — NO SE ESPERÓ

`pista/s113-b` sigue en **`58642c81`**, el mismo SHA mergeado (medido con
`ls-remote` después de la adenda). **Los tres préstamos salen tal cual** y se
cambian en el 0.1: `peso → datos` · `antiparasitario → receta` · `foto → ojo`.
Viven en **un solo `Record`** del shell: **el reemplazo son tres líneas.**

## ⑲ EL ROJO, ANTES DEL VERDE

Con el guard viejo y una razón genérica — **7 rojos, y nombran el caso**:

```
ROJO · el carrito se calla en paseo · PLAN — el que la igualdad no alcanzaba
ROJO · clasesVisibles apaga el carrito en paseo · PLAN
ROJO · en paseo · PLAN la burbuja no se dibuja con sólo carrito
   (los tres iguales para PAQUETE)
ROJO · es · nexo.razonSinPuerta interpola la mascota — Próximamente.
```

## ⑳ GATES AL CERRAR LA ADENDA

`verify:nexo` **190 casos · 0 fallos** (198 con `--control`) · `verify:coach`
**46/0** · `verify:contrast` **396 pares · 0 fallos** · `verify:diseno` **VERDE,
61 reglas** · `verify:razon-muda` **VERDE** · `verify:abanico` **10/10** ·
`verify:ref-antes-de-uso` **VERDE** · **4 tsc en 0** y el typecheck propio del
cliente en **0 con las rutas medidas**.

**Sigue: CERO APARATO.**

---

# ⏪ TERCERA ENMIENDA — LOS GLIFOS PROPIOS (`f94c852a`)

Mergeado `pista/s113-b` fijado a **`5dbcc5e2`** (su punta al momento de medir);
control de ancestría corrido y verde.

## ㉑ LOS TRES PRÉSTAMOS MUEREN — tres líneas, como estaba dicho

| dedo | antes (préstamo) | ahora |
|---|---|---|
| peso | `datos` | **`peso`** |
| vacuna | `vacuna` | `vacuna` |
| antiparasitario | `receta` | **`antiparasitario`** |
| foto | `ojo` | **`foto`** |

No se pasó `registro`: el registry lo resuelve solo —peso y antiparasitario
visten IDENTIDAD como `vacuna`; `foto` viste TINTA porque es un verbo—.

**Y la declaración de los préstamos murió en el mismo acto**, donde vivía (la
cabecera de `lib/nexo/atajos.ts`). *Un préstamo que sobrevive a su reemplazo deja
de ser un puente y pasa a ser el camino* — y su texto, si queda, manda al próximo
a defender algo que ya no existe.

**Control negativo:** cambiando `'antiparasitario'` por `'antiparasitarios'` el
typecheck contesta `TS2820 · is not assignable to type 'IconoNombre'. Did you
mean "antiparasitario"?`. *El compilador está mirando el registry, no dejando
pasar cualquier texto.*

## ㉒ LO QUE MI MONTAJE PRODUCE, Y VA AL OJO DEL FOUNDER

**Medido en los dibujantes:** `vacuna` lleva huella (`({ tinta, huella })`) y los
otros tres **no** (`({ tinta })`). B lo declaró al entregar: los suyos son
**glifos de control**, y un atajo que **nombra** algo la lleva. ⇒ **dentro de la
misma pata conviven dos tratamientos.**

No es un defecto: es `N27` con **tres actos y un sustantivo**. Y la salida que B
deja escrita, si al founder le salta: **que el cuarto también sea un acto** —
jamás apagarle la huella a un glifo de capa. Queda anotado en el código, no sólo
acá.

## ㉓ 🔴 `verify:glifos` NO EXISTE COMO COMANDO, y su silencio engaña

`pnpm -s verify:glifos` sale **EXIT=1 con CERO bytes de salida**: el script **no
está en el `package.json` raíz** (medido: el diff de `package.json` entre
`58642c81` y `5dbcc5e2` está **vacío**).

⚠️ *Un gate que se pide por nombre y no existe no dice «no existo» — no dice
nada, y leído con `tail -1` se lee como verde.* **Es el mismo modo de falla que
`verify:puerta-unica` en la adenda anterior**, y por eso se mide el exit y los
bytes, no la última línea.

**Se corrió el arnés directo:** `tsx scripts/_arnes-glifos.mts` → **18 verdes ·
0 rojos**, EXIT=0. La línea, si A la quiere:
`"verify:glifos": "tsx scripts/_arnes-glifos.mts"`. **No la agrego: el
`package.json` raíz no es `apps/`** — misma disciplina que con `verify:nexo`.

## ㉔ GATES

`verify:nexo` **190 · 0** · `verify:coach` **46/0** · arnés de glifos **18/0** ·
`verify:diseno` **VERDE, 61 reglas** · typecheck propio del cliente **0 con las
rutas medidas** · los otros tres tsc en **0**.

## ㉕ LO QUE NO TOQUÉ EN ESTA VUELTA

`packages/` (sólo lo que trajo el merge de B) · `supabase/` · el `package.json`
raíz · las pantallas del prestador · `BurbujaPendientes` (sigue intacta, es la
que manda en memorial) · la pantalla de antiparasitario y sus keys (esta vuelta
fue sólo el `Record` de glifos y la lápida de los préstamos) · **cero
dependencias nuevas**. No publiqué OTA ni mergeé a `main`. **Cero aparato.**
