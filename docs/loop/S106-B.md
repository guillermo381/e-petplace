# S106-B · PARTE DEL LOOP — telemedicina, el quinto oficio

> **Pista B** · territorio: `packages/ui` + los jueces/instrumentos.
> Worktree `e-petplace-s106-b` · rama `pista/s106-b` · nacida de `main` en
> `09def031`.
>
> **Este parte es la fuente del handoff.** Si esta pista compacta o cae, la
> sucesora abre LEYENDO esto — no reconstruyendo de memoria.

---

## ESTADO AL CIERRE DEL TURNO 1

**Turno de MEDICIÓN, sin código, por orden expresa.** Cinco mediciones hechas,
cada una con su objeto. **Nada construido. Nada cableado.**

**Depositado:** `docs/relevamientos/2026-08-25-s106-b-mediciones.md` — **es el
documento largo y ahí está todo el detalle con sus comandos.** Este parte es el
estado y la cola.

---

## LO MEDIDO, en una línea cada uno

| | resultado |
|---|---|
| **B-M1** · la marca visible §7 | `LineaDeVida` (4 pantallas, **todas del cliente**) + `FilaCita` + el parte. **🔴 La casa NUNCA marcó modalidad en un expediente** — el grooming a domicilio deja rastro idéntico al de local. **Dos vías declaradas, ninguna elegida** |
| **B-M2** · el aviso §3 | **🟢 El patrón de TRES acciones YA EXISTE**: `SelectorAvatar.tsx:214-229`, tres `Celda` en una `Hoja`. `useAviso` descartado por medición (efímero 3-6 s, UNA acción). `D-484` fijó el binario primario+secundario |
| **B-M3** · el cero de transporte | **🟢 Se sostiene**: 0 en manifiestos, 0 en lockfile, 12 falsos positivos en producto. Los 108 hits crudos viven en `.agents/skills/`. **`ClipSesion` = `useVideoPlayer(uri)`, playback probado** |
| **B-M4** · pre-join / in-call | Sirven `Cronometro` · `CitaEnVivo` · `EsperaDeMarca` · `Hoja` · `Boton destructivo`. **🔴 Faltan tres piezas** (lienzo remoto · video propio · barra de controles). **🔴 El cliente NO tiene `expo-camera`** ⇒ preview en el cliente = build |
| **B-M5** · el juez del texto | Propuesto: familia `R66`, **lee la LETRA en vez de un baseline transcrito**, **dos anclas**, igualdad exacta + conteo de los cinco signos. **Dos límites declarados: no mide tipografía · el inglés no tiene fuente firmada** |

---

## COLA VIVA — en orden, con el próximo paso ejecutable

1. **🔴 ESPERAR LAS FIRMAS DE CH-1 y CH-2** *(bloqueante para la Hoja del §3,
   NO para su texto ni su juez).*
   **Próximo paso ejecutable sin firma:** construir el juez de B-M5 — el texto
   es el mismo en las dos salidas posibles, y **corre sin `node_modules`**
   (medido hoy).
2. **Instalar dependencias en el worktree.** Hoy no hay `node_modules` ⇒ no hay
   `tsc`. **Precondición de tocar `packages/ui`**, no del juez.
3. **La marca de §7 — BLOQUEADA por A** (ver compromisos). Mi pieza se adapta a
   las dos formas; no supongo cuál.
4. **Pre-join / in-call — NO arranca sin la decisión de transporte de la mesa
   (§9).** Dibujar controles antes de saber sobre qué se pintan es trabajo que
   se tira.

---

## COMPROMISOS ABIERTOS — qué debo · qué me deben

### Me deben

| de | qué | por qué bloquea |
|---|---|---|
| **A (motor)** | ¿la teleconsulta es un **`tipo` distinto** de `eventos_mascota` o un **campo** del mismo tipo? | `LineaDeVida` resuelve por `item.tipo` o por campo propio (patrón `vacuna_nombre`). **Sin eso no puedo marcar nada, y no lo voy a suponer** |
| **C (cliente)** | las claves del aviso §3 en `apps/cliente/src/i18n/{es,en}.ts` | **Mi juez las LEE.** Hasta que existan sale **NO CONCLUYENTE, nunca verde** — por diseño (ancla ②). *Un rojo del juez puede significar «C no llegó», no «alguien rompió el texto»* |
| **La mesa** | **CH-1**: cuál de las tres acciones preside (o si van tres celdas de peso par) | Las leyes obligan a UNA sola primaria. **Es firma, no deducción** |
| **La mesa / el abogado** | **CH-2** (§10.2): ¿aviso o **consentimiento registrado**? | Cambia la anatomía: una `Hoja` se descarta por swipe. **🟢 Si es consentimiento, el riel ya existe**: `registrarConsentimiento` (`auth.ts:229`, S104) |
| **La mesa** | §9: **qué transporte** | Sin eso, la in-call no se dibuja |

### Debo

- **El juez de B-M5**, en cuanto la mesa confirme que el texto de §3 es
  definitivo (o antes, aceptando que su primer verde llega con C).
- **El número `R-`**, medido **por grep al momento de nacer** — hoy solo consta
  que `R66` es el más alto vivo. *No se toma de ningún resumen.*

---

## FRENOS Y SUPUESTOS VIVOS

- **FRENO GENERAL DEL TURNO 1 respetado: cero código.**
- **No toco lo abierto de S105:** deploy de `pagos-web` · guard del IVA · puerta
  de retomar · las siete piezas no ejercidas del acta §②.
- **No toco `clips.tsx`** (prestador, territorio C) — si su anatomía de cámara
  se quiere compartida, es **promoción**, y **D-645 rige: una promoción no es
  una migración**.
- **No toco `LETRA_SALDO`** ni el freno de depósito de A (CH-4): está bien
  declarado y no es de `packages/ui`. **Consecuencia de superficie anotada:** si
  las dos ventanas conviven, la voz que el dueño lee al cancelar depende del
  tipo de cita — **string de C, texto de la mesa**.
- **Supuesto que NO hice:** no supuse que §7 se resolvía con lo que hay. Lo medí
  y **no se resuelve** — ver B-M1 ③.

---

## NOTAS OPERATIVAS PARA QUIEN RETOME

1. 🟢 **`node scripts/verify-diseno.mjs` corre SIN `node_modules`** (node puro).
   Verificado hoy en este worktree: `VERDE — 57 reglas encendieron;
   informativas declaradas: R9`. **58 en el registro, 57 encendidas** — la
   diferencia es `R9` y está explicada.
2. **Los symlinks de `node_modules` son RELATIVOS** (`-> ../../../../packages/*`)
   ⇒ al instalar acá apuntan a **mi** `packages/ui`. Sin riesgo de escribir el
   primario por esa vía.
3. **Este worktree sin `node_modules` es una ventaja para censar:** todo `grep`
   es sobre código versionado **por construcción**, no por acordarse de un
   `--exclude`.
4. **Insumo para la mesa de §9, no una decisión:** está instalada la skill
   **`fishjam`** (WebRTC hosted de Software Mansion) con su SDK de React
   Native/Expo documentado en `.agents/skills/fishjam/`. **Es documentación, no
   dependencia** — 0 en manifiestos y 0 en el lockfile.
