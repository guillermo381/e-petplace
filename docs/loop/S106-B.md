# S106-B · PARTE DEL LOOP — telemedicina, el quinto oficio

> **Pista B** · territorio: `packages/ui` + los jueces/instrumentos.
> Worktree `e-petplace-s106-b` · rama `pista/s106-b`.
>
> **Este parte es la fuente del handoff.** Si esta pista compacta o cae, la
> sucesora abre LEYENDO esto — no reconstruyendo de memoria.

---

## ESTADO: TANDA 1 CERRADA · re-corrida contra `main = 271ece9a` (cable VERDE)

Las tres obras construidas y probadas, **re-verificadas contra la letra
enmendada**. **Gates del founder PENDIENTES** (ver abajo) — nada de esto se
declara verde por haber compilado.

| gate | número | comando |
|---|---|---|
| `verify:diseno` | **VERDE · 58 reglas** (eran 57 — nace `R67`) | `node scripts/verify-diseno.mjs` |
| WCAG | **383 pares · 0 fallos** · **cero pares nuevos**, probado | `pnpm verify:contrast` |
| typecheck | **4/4 VERDE** (ui · api · cliente · prestador) | `npx tsc --noEmit` en cada uno |
| árbol | **solo mi territorio 76(h)**, cero fuera | `git status --porcelain` |

**Enganche re-medido tras `pnpm install`** (precondición del turno):
`apps/{cliente,prestador}/node_modules/@epetplace/ui → …/e-petplace-s106-b/packages/ui`.
**Mi worktree, no el primario** — la predicción de ⓪ (symlinks relativos)
quedó medida.

---

## LO CONSTRUIDO

### OBRA 1 · `AvisoTeleconsulta` (`packages/ui/src/components/`)
Tres `Celda` de peso par en una `Hoja`. Orden fijado **por la pieza**
(`acciones` es objeto nombrado, no array ⇒ el consumidor no puede
reordenarlas). Todo el texto entra por props. **Los cinco signos son una
TUPLA: cuatro no compila.** Sin `numberOfLines` en ningún nodo; `tituloEntero`
en las tres celdas; `HojaScroll` si no entra. Sin `conCerrar` (Chanel: una X
sería una cuarta salida). Sin glifos, declarado con su razón.

### OBRA 2 · la marca visible — **vía (b), cuarta familia de `Insignia`**
`InsigniaModalidad = 'teleconsulta'`. **Decisión: PALABRA, no glifo**, y el
argumento es de la letra: §7 dice *«dentro de tres años alguien va a leer ese
expediente»* — **un glifo hay que saber leerlo, una palabra se lee**. Anatomía
de `capa` sin el punto (no clasifica un dominio) y sin fondo pleno (no es
pertenencia). `LineaDeVida` gana `modalidad?`, se dibuja **debajo del quién**;
ausente = no se dibuja. Voz en el namespace `ui` (precedente `cohorte`):
**«Por videollamada»**, no «Teleconsulta» (Ley 17.2).

### OBRA 3 · `R67` — el juez del aviso
Lee la letra con `readFileSync` **en cada corrida**: cero baseline transcrito.
Dos brazos (los cinco signos · el cuerpo exacto) y dos anclas.
**LOS TRES ESTADOS PROBADOS**, que es lo que lo vuelve un instrumento:

| estado | cómo se probó |
|---|---|
| **NO CONCLUYENTE** | probado el 25, cuando el texto no existía (hoy ya no aplica: C depositó) |
| **ROJO** | fixture con **cuatro** signos (auto-prueba L-192, en cada corrida) |
| **VERDE** | ✅ **contra el repo real** (forma plana de C) **y** en la rama anidada — las dos probadas |

---

## 🔴 LO QUE ESPERA AL FOUNDER — pendientes, NO verdes

1. **Gate visual de la Hoja del §3** (galería, sección ⭐ GATE S106): que las
   tres salidas se lean de peso par y que los cinco signos se lean como
   criterio.
2. **Gate del ícono / la marca** (galería, segunda sección): que
   «Por videollamada» se lea como hecho del acto y se separe de sus tres
   hermanas. *Aunque no sea glifo, la decisión palabra-vs-glifo es suya.*
3. **Lote de strings**: `ui.modalidad.teleconsulta` es **voz nueva sin firmar**
   (es + en).
4. ~~La conjugación de §3~~ — ✅ **firmada en CP1** (v1.1).

---

## ✅ LOS DOS CHOQUES DEL TURNO 2 — CERRADOS, verificados contra el objeto

> **Traído `origin/main = f6482db9`.** Los dos hallazgos que esta pista
> declaró se resolvieron **en la FUENTE**, no ablandando los instrumentos.

### CH-A ✅ · la letra, enmendada — **v1.1**
`LETRA_TELEMEDICINA` v1.1 (CP1, firma founder): §3 pasa a **tuteo neutro sin
tocar el contenido** —«notas» y «llévala»—, con **los cinco signos y las tres
acciones intactos**. Verificado por `git log` sobre el archivo: `72a37951` +
`60a1d4e6`. **El rojo que R67 predijo se disolvió**, y se disolvió como debía:
*la contradicción vivía en la fuente y se curó ahí.*

### CH-B ✅ · el agujero de `R66`, curado por A — **y lo re-verifiqué yo**
`lib-voz.mjs` en `69fb7377`. **No lo cito: lo medí en las dos direcciones**

| caso | esperado | medido |
|---|---|---|
| `notás` | HIT | **HIT** (`notá`) |
| `llevala` | HIT | **HIT** |
| `creés` | HIT | **HIT** |
| tuteo v1.1 completo | **vacío** | **vacío** — cero falso positivo |

*El cruce que lo volvía urgente queda cerrado: hoy el §3 en tuteo pasa R66 y
pasa R67 al mismo tiempo — antes eso era imposible sin que uno de los dos
mintiera.*

## 🔴 EL HALLAZGO NUEVO DEL TURNO 2 — la v1.1 dejó ciego a R67, y el guard lo dijo

La enmienda ② de §3 agregó **un párrafo nuevo** —la **línea de tránsito**,
*«La videollamada no se graba y se transmite a través de la infraestructura de
nuestro proveedor de video»*— **después** del párrafo de los signos.

Mi extractor tomaba los signos del **último** párrafo ⇒ devolvió `null` ⇒ R67
salió NO CONCLUYENTE contra su propio fixture ⇒ **la auto-prueba la declaró
REGLA DECORATIVA en la primera corrida** (`EXIT=1`, lint inválido).

> ### **Un extractor atado a la POSICIÓN se rompe cuando la fuente crece; uno atado al CONTENIDO sobrevive a que la mesa agregue un párrafo.**
> Curado buscando el párrafo **por contenido**. *Y lo que hay que registrar no
> es mi error: es que el lint se declaró INVÁLIDO en vez de quedarse verde con
> un juez ciego adentro. L-192 haciendo exactamente su trabajo, sobre el juez.*

**Dos consecuencias, ya aplicadas:**
- **`AvisoTeleconsulta` gana `texto.transito`**, renderizado en registro de
  **apoyo** al pie: es una nota sobre a dónde viaja la imagen, no una
  advertencia clínica — ponerla al mismo peso que los signos le robaría
  atención a lo que puede salvar a la mascota (Ley 15).
- **R67 la vigila**, pero **solo si la letra la trae** — una regla no puede
  exigir lo que su fuente no firma. ⚠️ La letra la marca **PROVISIONAL**
  (rige hasta la respuesta del abogado a §10 pregunta 4, LOPDP): el día que
  cambie, **R67 lee la letra nueva sola** — que es exactamente para lo que la
  vara se extrae en cada corrida en vez de copiarse a un baseline.

### CH-C · el umbral que la auto-prueba cazó (error propio, declarado)
Mi extractor pedía 4 párrafos y la letra tiene **3** ⇒ `R67` salía NO
CONCLUYENTE contra su propio fixture y **habría nacido decorativa**. Lo cazó el
guard de L-192, no yo. *Registrado porque es el guard haciendo exactamente
aquello para lo que existe.*

---

---

# TURNO 3 · EL CABLE DIO VERDE — qué cambia para esta pista

> **LiveKit Cloud FIRMADO SIN CONDICIÓN** (gate del founder, dos dispositivos,
> red real de Quito, los cinco puntos). **El acta y su evidencia las deposita
> A** — acá va solo lo que el verde mueve en `packages/ui` y en los jueces.

## ⏪ B-M3 VENCIDA Y RE-MEDIDA — el «cero de transporte» murió con el verde

Mi medición de ⓪ decía **cero transporte en manifiestos y lockfile**. **Hoy es
falsa, y su vencimiento es una buena noticia.** Medido tras traer `main`:

```
apps/cliente/package.json   @livekit/react-native 2.12.0 · @livekit/react-native-webrtc 144.1.2 · livekit-client 2.22.0
apps/prestador/package.json  (idénticas)  + @livekit/react-native-expo-plugin 1.0.2 (dev)
```

*Se registra el vencimiento en vez de borrar la medición: era verdadera el 25 y
dejó de serlo el 26. Una medición sin fecha es una trampa para el que la lea
después.*

## 🟢 B-M4 SE ACHICA — el transporte trae DOS de las tres piezas que faltaban

Medido sobre los exports reales de `@livekit/react-native`
(`lib/typescript/src/index.d.ts`): **`VideoView` · `VideoTrack` · `LiveKitRoom`
· `useParticipant` · `useRoom` · `AudioSession` · `BarVisualizer`.**

| lo que ⓪ declaró faltante | hoy |
|---|---|
| superficie de video remoto | ✅ **la trae LiveKit** (`VideoTrack`) |
| el video propio | ✅ **la misma pieza con el track local** — y **ya NO exige `expo-camera`**: la cámara la provee el transporte |
| la barra de controles (mute · cámara · colgar) | 🔴 **sigue faltando** — es la única que es Ley 11 de verdad |

**Y sigue en pie la advertencia de ⓪ (§6ter):** los controles viven sobre
**fondo no controlado**, igual que el mapa. Cuando se dibujen, el camino barato
es **nombrar una clase**, jamás enmendar la Ley 12. *La letra vieja ya nombró
este caso al descartarlo: «todo póster de video es fondo no controlado».*

## 🔴 EL PREVIEW PROPIO: no falta la vista — **está construida y no habla**

La observación del founder («la pantalla del cable no muestra preview propio»)
la medí antes de repetirla como diagnóstico, y **dice otra cosa**:

```
apps/prestador/src/app/cable-livekit.tsx:169
  <VideoTrack trackRef={propias[0]} style={{ flex: 1 }} />     ← bajo el rótulo «Vos»
```

**La pieza existe.** Lo que no existe es su guarda: el bloque remoto **sí** la
tiene (`remotos.length === 0` → voz honesta), y **el propio no** — si
`propias[0]` es `undefined`, pinta un marco vacío y **no dice nada**.

> **Es Ley 13 en su forma exacta: el vacío no se disfraza de contenido.** Y es
> del tipo que no tiene stack trace — nadie se entera salvo mirando.

**Es de C, no lo toco.** Para MI tanda 2 queda como requisito de la pieza: **el
preview propio nace con su guarda**, y la in-call habla cuando un track no
llega.

## 🔴 R67 ESTABA MIRANDO EL LUGAR EQUIVOCADO — curado

**El defecto, medido:** R67 buscaba `avisoTeleconsulta: { … }` —el contrato que
ella misma publicaba— y salía **NO CONCLUYENTE** diciendo *«C todavía no
depositó»*. **C ya había depositado**, en claves planas
(`veterinaria.avisoTele*`), y **el aviso ya se le mostraba a una familia real.**

> ### **El juez estaba protegiendo SU CONTRATO en vez de LA LETRA — y su «no concluyente» inducía la conclusión falsa de que no había nada que vigilar.**
> **Un juez se ata al TEXTO QUE EL USUARIO LEE, jamás a la forma en que un
> consumidor lo guardó.** La forma es de quien la escribe; la letra es de la casa.

**Curado:** lee las **dos formas**, normaliza, y cuenta los cinco signos **sobre
el texto que se muestra** (sirve igual con el párrafo entero o con la tupla).
Gana además la verificación de **las tres acciones**, que §3 también firma.

| rama | estado |
|---|---|
| plana (la de C, viva hoy) | ✅ **VERDE** — 5 signos + 3 acciones + cuerpo verbatim |
| anidada (mi contrato) | ✅ **VERDE** — probada con depósito temporal, revertido |

**El aviso real pasó de no estar vigilado por nadie a estar vigilado.** Y la
auto-prueba sigue roja contra su fixture (58 reglas, verde).

## ⚠️ DOS AVISOS CONVIVEN — y NO es un descuido de C

`apps/cliente/src/components/aviso-teleconsulta.tsx` (local) y
`packages/ui/src/components/AvisoTeleconsulta.tsx` (mío). **El orden de merge lo
explica**: `656806f5` (C construye) → `283ce5a9` (mi pieza entra a main) →
`fe81180a`. **C midió cuando mi pieza no existía**, lo declaró en su cabecera y
**dejó escrito el camino de salida**: *«el día que B publique la pieza, este
archivo muere y las claves i18n se quedan — el swap cuesta un import»*.

*Eso es exactamente lo que la casa pide de una pieza local: nace declarada y con
su lápida escrita.* **Lo que queda no es una culpa: es una decisión de mesa.**

### 🔴 Y hay UNA diferencia que sí toca una firma
| | la local (C) | la de `packages/ui` (B) |
|---|---|---|
| acciones | **1 `primario` + 2 `secundario`** | **3 `Celda` de peso par** |
| los cinco signos | un `Texto` con el párrafo entero | **tupla de 5 — cuatro no compila** |

**El founder firmó que NINGUNA acción preside.** Con un `primario`, una preside.
*El argumento de C —«un aviso que pone continuar arriba está apurando a alguien
que quizás tiene una urgencia»— es bueno y sigue en pie: justifica el ORDEN, que
las dos piezas respetan. La jerarquía es lo que la firma prohíbe.*
**No lo curo: es territorio de C y decisión de mesa.**

---

## COMPROMISOS ABIERTOS

### Me deben
| de | qué | estado |
|---|---|---|
| **A** | ~~la enmienda de conjugación de §3~~ | ✅ **entregada** — v1.1, verificada |
| **A** | de dónde sale `modalidad` (derivada de la cita o columna propia) | mi pieza ya se adapta: recibe el valor resuelto |
| ~~**C**~~ | ~~las claves del aviso~~ | ✅ **depositadas** (forma plana) — **R67 ya las lee y da VERDE** |
| **mesa** | ¿la app monta la pieza de `packages/ui` o se queda la local de C? | la firma de paridad («ninguna preside») **solo la cumple la de `packages/ui`** |
| **C** | la guarda del preview propio (Ley 13) | el marco vacío no habla cuando no hay track |
| ~~**C / mesa**~~ | ~~curar el agujero de `lib-voz`~~ | ✅ **curado por A** (`69fb7377`), re-verificado en las dos direcciones |
| **founder** | los cuatro pendientes de arriba | |

### Debo
- Nada bloqueante. Las tres obras entregadas y probadas.
- **Tanda 2, ya sin condición** (LiveKit firmado): la **barra de controles**
  (mute · cámara · colgar) es lo único que falta por Ley 11 — el video lo
  ponen `VideoTrack`/`VideoView`. **El preview propio nace con su guarda.**

---

## NOTAS OPERATIVAS PARA QUIEN RETOME

1. **`verify:diseno` corre sin `node_modules`** (node puro) — útil para tocar
   jueces antes de instalar.
2. **El contrato de las claves de C lo imprime el propio `R67`** cuando sale NO
   CONCLUYENTE: no hay que buscarlo en ningún doc.
3. **El control positivo de `R67` se hace con depósito temporal + revert**
   (`cp` de respaldo, correr, restaurar, `git status` en 0). Es la única forma
   de probar que el juez puede dar verde sin tocar territorio ajeno.
4. **La galería es donde viven los dos gates** — sección «lo que espera tu
   firma», arriba de todo.
