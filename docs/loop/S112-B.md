# S112-B · el parte — `packages/ui`

Rama `pista/s112-b` · worktree `e-petplace-s112-b` · abre en `main cc55348b`,
**sincronizada a `main f704daa2`** al abrir el LOOP DE ADOPCIÓN (2-sep).
**Nada en aparato: todo espera el lote único.**

---

# 📌 TANDA DEL LOOP DE ADOPCIÓN (2-sep) — B1 → B8, ocho pedidos, ocho respuestas

**Código termina en `8b90c9cc`.** Las ocho, cada una con su rojo producido y
borrado, y con su PUERTA. **Ninguna montada: eso es de C.**

| | pieza | qué aporta, en una línea | puerta |
|---|---|---|---|
| B1 | `ConvivenciaInput` | el estado equivocado no se puede expresar | ficha de edición del refugio (C7) |
| B2 | `FormularioPostulacion` | las seis preguntas son SUYAS, así que un campo de nombre no entra | postulación (C5) |
| B3 | `DocumentoLegalLectura` | «vi todo» como PREDICADO, no evento | condiciones (C4) · acta (C8) |
| B4 | `CodigoFirmaInput` | un código vencido no es un tipeo equivocado | acta, las dos apps (C8) |
| B5 | `HitoUnaVidaNueva` | se celebra con permanencia, no con movimiento | línea de vida · cierre del acta |
| B6 | ☠️ **no se construye** | el memorial de esta casa es un TEMA, no una pieza | — |
| B7 | `TarjetaMascotaRefugio` | el interruptor que no se mueve DICE por qué | tab Mascotas (C7) |
| B8 | `FichaAdoptable` | N19 hecho estructura: no hay prop para mover el orden | ficha del adoptable (C3) |

## Lo que el CENSO cambió, y es lo que hay que leer antes de tocar nada

Tres de los ocho pedidos **cambiaron de forma al medir la casa**, y las tres
veces el trabajo se achicó:

1. **B1 no era un control.** `SelectorSegmentado proposito="eleccion"` ya da
   2-3 alternativas excluyentes con la exclusividad hecha FORMA y semántica de
   radiogroup. *«Del mismo peso» no es una instrucción de pintura: es lo que un
   riel único garantiza y tres botones sueltos no pueden garantizar por más que
   se pinten iguales.*
2. **B4 casi no era trabajo.** `CampoCodigo` ya tenía largo por prop, pegado
   desde el portapapeles por la misma puerta que el tipeo, y mensaje en slot
   reservado. Lo único que faltaba era una decisión — y el ensanche fue en
   `PieDeCampo`, la anatomía que comparten las TRES piezas de campo, no en un
   `CampoCodigo` clonado.
3. **B6 no había que construirla.** El memorial es `<ThemeProvider memorial>`,
   que queda siempre encima y al que todas las piezas ya responden. *La pieza
   que no se escribe es la que hay que justificar mejor, no peor.*

## Los ensanches que dejé en piezas de OTROS frentes — para que nadie los busque

| pieza | qué gana | default |
|---|---|---|
| `Convivencia` | exporta `EstadoConvivencia`: los tres estados, escritos UNA vez para las dos caras | — |
| `PieDeCampo` | `tono: 'alarma' \| 'estado'` | `'alarma'` — nadie cambia |
| `CampoCodigo` | `tono`, que pasa al pie y a las cajas | `'alarma'` |
| `Interruptor` | `deshabilitado` + `razonDeshabilitado` | `false` — nadie cambia |
| `SemaforoSanitario` | tercer estado `no_declarado` (con `voz` obligatoria) + eje `lector: 'responsable' \| 'observador'` | `'responsable'` — los dos montajes de guardería compilan sin tocar un byte |

### 🔴 EL TERCER COBRO DEL MISMO DEFECTO, EN EL MISMO VERTICAL

`SemaforoSanitario` se ensanchó a pedido de C, **y el dato que C mandó al
costado dijo que eran DOS huecos y no uno**:

1. **El que pidió:** en la ficha del adoptable quien mira no puede resolver
   nada, y el tipo la obligaba a inventar un `onResolver`. Se resolvió con el
   eje `lector`, **no** con el nombre que proponía (`falta_informativo`
   nombra la incapacidad del LECTOR, no el hecho: el mismo `esterilizado:
   false` es «falta, cargá el carnet» para la familia e «información» para
   quien decide adoptar).
2. **El que no pidió, y es el grave:** `estadoVacunal: null` significa **«el
   refugio no lo declaró»**, no «no está vacunada». Pintarlo como `falta`
   **afirma algo que nadie dijo** — y sobre un animal que espera un hogar.

⇒ **Es la TERCERA vez que esta casa encuentra el mismo defecto en el mismo
vertical:** `Convivencia` (S111), el código de firma vencido (B4) y ahora la
salud. **Las tres veces era un binario donde el mundo tiene tres estados**, y
las tres se curaron igual: un tercer estado con **voz obligatoria** y **sin el
color del pendiente**. *Un dominio donde «no se sabe» es una respuesta
legítima necesita decirlo, no dejar el hueco.*

⚠️ **Y el hallazgo lo trajo el lado de los DATOS, no el de la pieza.** C lo
vio leyendo el contrato de A; yo no lo habría visto mirando el componente.

### El séptimo estado de `TarjetaMascotaRefugio` — mismo patrón, otra pista

C frenó la tab Mascotas antes de mapear `no_disponible` a `pausada`, y el
motor le da la razón **por constraint**: `chk_no_disponible_coherente` exige
`retirada_en NOT NULL`. **Es un retiro definitivo; `pausada` es temporal y el
catálogo dice literal que «no es un rechazo».** El séptimo **conserva el
nombre del motor**, porque la migración `20260907880000` ya jubiló `retirada`
con el criterio de esta casa: *«la palabra vieja describía el acto y no el
estado»*.

⚠️ **`en_proceso` queda SIN PRODUCTOR, declarado:** el motor no distingue
«aceptada, traspaso pendiente» — la publicación sigue en `publicada` hasta el
traspaso. Es letra firmada del founder, así que no se saca; **su costo lo paga
el consumidor**, que le escribe una voz a un estado que todavía no se dibuja.

🔴 **Y el guard cobró en MI galería:** agregar el séptimo rompió `voces` en los
tres montajes del catálogo con `TS2741`. *Un estado nuevo rompe en la pantalla
que tiene que decidir cómo se llama, no en silencio adentro de la pieza* — la
promesa del comentario, cobrada primero a su autora.

### `EstadoSolicitudAdopcion` gana dos cortes — y el duelo NO lleva escalera

Firma del founder (2-sep): **el acta no se firma con el animal en memorial.**
El motor cerró las solicitudes vivas a un estado nuevo, y el tipo lo cazó
donde tenía que cazarlo.

· **`desistida`** — corte de la FAMILIA. Mismo desvío que `declinada`, otra
  voz. *No se mapea a `declinada`: declinar es un acto del PUBLICADOR, y el
  refugio leería «yo la decliné» sobre alguien que se fue solo.*
· **🔴 `no_concretada_fallecimiento` — dibuja LA NOTICIA Y NADA MÁS.** Sin
  pasos, sin marcas, **sin color de status ni siquiera neutro**. *La escalera
  existe para decir dónde estás en un proceso, y acá no hay proceso en el que
  estar: murió su sujeto.* Dibujarla apagada le informa a alguien que perdió
  al animal que eligió **hasta qué paso del trámite había llegado**.
  **La mitad estructural de `D-3`:** la pieza **no tiene slot de acción** en
  ningún estado — no hay dónde poner un «Ver otros animales».

⚠️ **Y la medición que hay que copiar, no el número:** mis tres typechecks
daban 0 **en mi árbol**, y eso NO probaba nada — los consumidores rojos vivían
en `main`. **Sincronicé y medí contra ellos.** El resultado es el que importa:
los rojos **cambiaron de clase** —de `TS2322` («el valor es más ancho que el
tipo») a `TS2739` («faltan dos voces»), con **cero `TS2322` restantes**— y eso
sí prueba que la unión acepta lo que el motor emite. *Un verde sacado de un
árbol que no tiene el caso es la ceguera que se lee igual que salud.*

Los tres call sites que quedan son de C (`apps/`, no es mi territorio) y lo
que falta ahí es una decisión de VOZ.

### 🔴 EL MISMO ERROR, DOS VECES EN UN DÍA, EN LAS DOS DIRECCIONES

**La clase:** *medir la propia rama y llamarlo «el estado»* — que el canon ya
nombra y que esta tanda cobró dos veces.

1. **Yo:** declaré «typechecks 0/0/0» sobre los dos estados nuevos **desde un
   árbol que no tenía las pantallas de C**. El número era cierto y no
   contestaba la pregunta. Se curó sincronizando con `main` y midiendo contra
   los consumidores reales — ahí el rojo **cambió de clase** (`TS2322` →
   `TS2739`), que es lo que sí prueba la cura.
2. **C:** reportó *«`no_concretada_fallecimiento` no existe en el motor: cero
   ocurrencias»* y *«`desistida` anda por suerte, no por contrato»*. **Medido
   contra `origin/main`: falso las dos.** El estado vive en el CHECK
   (`20260908380000`, 16 ocurrencias) y `packages/api` tiene los SEIS.

**Y el detalle que lo vuelve caro, no anecdótico:** el `TS2367` que C vio
**era real** —su árbol estaba atrás— pero de ahí sacó una conclusión sobre el
MUNDO, y **por esa conclusión revirtió trabajo correcto**: callar «esta
conversación está cerrada» en el estado de duelo, que extiende bien la
decisión de no dibujar escalera. *Perdió trabajo por una medición, no por una
decisión.*

⇒ **El correctivo es el mismo para los dos y es una línea:** un hallazgo sobre
el motor se mide contra **`origin/main`**, jamás contra el árbol propio. *«El
compilador me frenó» y «el motor no lo emite» son dos afirmaciones distintas,
y sólo la primera sale del objeto.*

### ☠️ LA HUELLA SE RETIRA — corrección del founder, 2-sep

**El estado «sin foto» de una mascota es el avatar de la casa por raza, o por
especie si no hay raza declarada. Nunca la huella.**

🔴 **Lo que el censo encontró y cambió el trabajo: el resolvedor YA EXISTÍA y
las piezas no lo estaban usando.** `resolverUrlRaza` /
`resolverUrlGenericaEspecie` viven en `packages/api/wrappers/catalogos.ts`
desde S91 sobre el bucket público `especies-razas` (111 objetos). *No había
que construir nada: había que dejar de dibujar una huella.*

La escalera: **foto propia** (con encuadre de retrato) → **avatar de la casa**
(a sangre, **sin** ese encuadre: el zoom existe porque en una foto de mascota
la cara queda alta, y a una ilustración ya encuadrada la recortaría) →
**monograma**.

**El tercer escalón es decisión de la pieza, no del founder** — él nombró dos
porque para una mascota no debería haber un tercero, pero es alcanzable
(**medido: `reptil` no tiene `generico.webp`, 404**) y hay que dibujar algo.

☠️ Ley 37: mueren `HuellaGenerica`, `HUELLA`, `HUELLA_SIN_FOTO` y sus imports.

⚠️ **La `Huella` de `HitoUnaVidaNueva` QUEDA, y fue el control positivo del
censo** (5 piezas en 0 montajes, ésa en 1): ahí es **ilustración de marca**
dentro de un `Guijarro`, no el fallback de una mascota concreta. *La orden
retira la huella de donde suplanta una identidad, no de donde ES la marca.*

**59 archivos montan `AvatarMascota` y ninguno pasaba `fotoDeEspecie`** el día
que se retiró: por eso las dos piezas AVISAN en `__DEV__` al llegar al
residuo, nombrando el resolvedor que falta. El ruido es del tamaño de la
deuda, y es de C.

### ✅ `VitrinaRefugio` CIERRA EL FRENO DEL CARRUSEL, SIN EXTRAERLO

No es una vitrina nueva: **es `FichaPrestador` con los datos de un refugio**
(N17). *La respuesta a «esta pieza no se puede reusar en partes» a veces es
reusarla completa* — y así no se reabre la propiedad que cerró el ojo del
founder en aparato.

🔴 **No expone zona ni dirección, y no puede:** §5.2 acota la vidriera anónima
a nombre, foto y ciudad, y **a la puerta de un refugio la gente deja
animales.** *No se puede exponer lo que no tiene por dónde entrar.*

### El séptimo estado de solicitud, y la clase que nombró

`no_concretada_otra_familia` **tampoco lleva escalera**, y por una razón
distinta a la del fallecimiento:
· fallecimiento → **no hay PROCESO**: murió su sujeto.
· otra familia → **no hay JUICIO**: nadie evaluó esa postulación.

*Dos caminos al mismo dibujo no es una coincidencia que se acomoda: es que el
dibujo dice «esto es una noticia», y las dos lo son.* El `if` pasó de caso
especial a un mapa `NOTICIA` con nombre.

⚠️ **Rompe tres call sites de C, y el hook me frenó bien.** Lo salteé
**diciendo que el rojo es MÍO y deliberado**, no invocando la cláusula del WIP
ajeno. *Un `SALTAR_GATE` que se escuda en una razón que no es la suya es peor
que no tener el gate.*

### ☠️ R18 JUBILADA — la revisé y la dejo, con una razón mejor que la cláusula

C la retiró de `verify-diseno.mjs` (archivo de esta pista) y pidió que la
revisara. **Medido contra `origin/main`, y la medición es más fuerte que el
argumento:** `git grep "Láminas de gate" -- apps/prestador` da **CERO**. La
entrada que R18 vigilaba **ya no existe**.

Y su polaridad es lo que lo cierra: **R18 exigía que la entrada EXISTIERA** —
su modo de falla era que desapareciera—. Retirada la entrada por firma del
founder, *un guard cuyo corpus es la lista vacía no protege: su verde pasa a
significar «no había nada que mirar»*, que es `L-192` exacto. **La lápida lo
dice mejor de lo que lo habría dicho la cláusula que la mandaba retirar.**
`verify:diseno` pasa de 62 a **61 reglas**, verde.

### El cascarón de `VitrinaRefugio` — un hueco que trajo el MOTOR, no la pieza

A lo encontró desde `uq_prestadores_user_id`: **1 humano = 1 prestador**, así
que *una clínica que además rescata no puede tener fila de refugio* y el
lector devuelve vacío. **Eso convirtió un borde hipotético en uno
estructural** — y es lo que justificó cerrarlo como restricción de tipo
(`vozSinPagina` OBLIGATORIA) en vez de una rama defensiva.

Sin eso la pieza renderizaba **una pantalla en blanco con un nombre**,
indistinguible de un fallo de red (Ley 13). 🔴 **Y el nombre NO cuenta como
«tener página»**: una vitrina es lo que el refugio ARMÓ; el nombre y el logo
los tiene por existir. El `pie` se conserva igual — *un refugio sin vitrina
igual tiene animales esperando.*

⇒ **La lección del día, en las dos direcciones:** los tres huecos reales de
mis piezas los encontró gente mirando OTRA capa — C desde el contrato de
datos (el tercer estado sanitario, el séptimo de solicitud), A desde una
constraint del motor (el cascarón). *Ninguno se veía mirando el componente.*

### `HojaFiltros` — y la cuarta vez del mismo defecto

C pidió los nueve filtros **con la lista blanca del motor medida**, así que no
hubo nada que adivinar. Devuelve `FiltrosAdoptables` **tal cual**, con los
nombres del motor —incluido el `snake_case`—: *una hoja con vocabulario propio
obliga a un mapa en el medio, y ese mapa es la segunda verdad que diverge
sola.* Cero control nuevo: ocho `FiltroPills`.

🔴 **Convivencia otra vez, y acá con una vuelta más:** el filtro tiene
**CUATRO** posibilidades y no tres —los tres estados **más no filtrar**—, que
es `activo: null` sin inventar un valor que el motor rebotaría.

🔴 **Y el que no se pudo curar igual: `esterilizado` sólo trae los declarados
`si`.** Angosta escondiendo una ausencia y la persona no puede saberlo. *Es la
misma asimetría que convivencia resolvió con su tercer estado, y acá NO se
puede porque el motor acepta un booleano* ⇒ lo que sí se puede es **no dejar
que quede callado**: su «i» es OBLIGATORIA. **Un filtro que angosta en
silencio es peor que uno que no existe.**

⚠️ **Y un cobro propio que vale más que la pieza:** mi archivo de rojo probaba
`convive_perros: true` **con un `as unknown as` adentro**. El cast anulaba el
tipo que el test venía a probar — **dio verde midiendo nada**. Re-corrido sin
cast, dispara. *Un archivo de rojo con un cast adentro no prueba el tipo:
prueba que TypeScript obedece un cast.* Es `L-459` otra vez, en la forma que
más fácil se cuela: el instrumento escrito por el mismo que escribió la pieza.

### ⚠️ LO ÚNICO DE MIS PIEZAS QUE SÓLO PUEDE JUZGAR EL OJO

En el brazo **`lector="observador"`**, un requisito en `falta` sigue pintando
la marca en **ochre** (`status.warningText`). Está razonado —en esta casa el
ochre dice *«es una tarea»*, jamás *«es un error»*, y para un adoptable «no
está esterilizada» **sí** es una tarea pendiente y real— **pero el ítem 11
firmado dice que el adoptante lo ve como INFORMACIÓN, no como alerta, y dónde
cae ese límite no lo decide un argumento: lo decide mirar.**

**Ahora hay con qué:** la ficha tiene datos vivos (Tito urgente con 908 días,
Nube con los tres ejes en `no_se_sabe`). **Es una sola pregunta para el
recorrido del founder**, y si el ochre grita de más la cura es de una línea:
el mismo `text.tertiary` que ya usa el tercer estado.

## 🔴 TRES COSAS QUE SON DE OTROS Y ESTÁN DECLARADAS, NO RESUELTAS

**① El carrusel no es una pieza, y por eso `FichaAdoptable.galeria` es slot.**
Vive dentro de `FichaPrestador` (~150 líneas, líneas ~270-440), enredado con
clips, `sobrePortada`, el degradado del nombre y los puntos. N17 manda
extraerlo («una fuente, N consumidores; jamás se duplican») y **no se hizo con
su razón: su propiedad clave la cerró el OJO DEL FOUNDER en aparato** —«no
tironea en Android», group `d139b9c0` / APK 1.0.3—. *Una propiedad que se
cerró mirando no se puede volver a cerrar leyendo.* **Necesita gate en
aparato, no un typecheck.** ⚠️ Si C termina escribiendo un segundo carrusel en
la ficha de adopción, **la deuda se cobró** y hay que extraer.

**② Nace `pnpm verify:vio-todo`** (`scripts/verify-vio-todo.mjs`, tsx, ~1 s).
Está en `package.json` y **NO en el hook** — dónde corre es de A.

**③ R17 tiene una línea vencida.** Su justificación dice *«una pieza que nadie
puede mirar no se puede firmar»*, y esa premisa la derogó el founder el 2-sep:
las piezas se firman MONTADAS. Lo que sobrevive es *«ninguna exportación queda
invisible»*, que sigue siendo cierto — por eso las ocho tienen entrada de
**catálogo**, que **no es su gate**. Re-redactar la razón es de A.

## El gate que se corrigió a sí mismo — `L-459` cobrada por su autor

`verify:vio-todo`, al probarse en rojo, **salió por exit 2 («no pude medir»)
en vez de exit 1 («el caso ① falló»)**: su auto-prueba afirmaba
`pudoVerTodo(800,800,0) === true`, que es EXACTAMENTE el caso que estaba
midiendo. Dijo la verdad y dijo mal cuál. *Una auto-prueba que comparte el
supuesto con lo que mide no verifica el instrumento: lo duplica.* Los dos
casos de ahora los contesta bien hasta la versión rota.

---

# ⭐ PARA QUIEN RETOME ESTA PISTA — se lee esto primero, y nada de acá se deduce

**Soy B · `packages/ui`.** Worktree `e-petplace-s112-b` · rama `pista/s112-b`.
Abrí en `main cc55348b`.

> ### ⚠️ LO PRIMERO, PORQUE ME COSTÓ UN ROJO COMMITEADO EN VERDE
> **El worktree YA TIENE `node_modules`** (`pnpm install`, 3,8 s). **Si abrís uno
> nuevo, instalá ANTES de creerle a un typecheck.** Sin deps, `tsc` no resuelve
> los imports y devuelve **ceguera que se lee igual que un verde**. Y **se corren
> los TRES**: `packages/ui` · `apps/cliente` · `apps/prestador` — cuál caza una
> clase dada **no es estable entre árboles**, así que ninguno se saltea.

## Estado del lote — verificado contra el objeto

| | |
|---|---|
| **código termina en** | **`fdedee75`** |
| tip de la rama | docs encima; `git diff fdedee75..tip -- packages/ui/` = 0 |
| los tres typechecks | **0 · 0 · 0** *(con deps instaladas)* |
| `verify:diseno` | VERDE, 62 reglas |
| `verify:razon-muda` | VERDE (6, baseline 6) — **pero su premisa es falsa, ver abajo** |
| árbol | limpio · local = origin |

**Declaración de lote para A:** `buzon/S112-B-DECLARACION-DE-LOTE.md`.

## LA COLA VIVA — en orden, con el próximo paso ejecutable

1. **NADA QUE CONSTRUIR HOY.** Las cuatro piezas sin puerta esperan **dato de
   otros**, no trabajo mío. *No las «mejores» mientras esperás: una pieza que
   crece sin consumidor es la que después nadie puede montar.*
2. **Cuando C pida `TarjetaAdoptable` montada** (lote de adopción) → nada que
   hacer: ya está entregada y exportada.
3. **Cuando exista `destacado_espera`** → `BloqueConCriterio` se monta tal cual.
4. **Cuando el lector devuelva los tres estados de convivencia** →
   `FiltroPills` modo `varias` + `Convivencia` se montan tal cual.
5. **Si el founder firma la esterilización** → ver el compromiso de abajo.

## COMPROMISOS ABIERTOS — qué debo · qué me deben

**QUÉ DEBO:**

- **A `SemaforoSanitario`, cuando el founder firme la esterilización** (me lo
  pasó C con su freno puesto, **y esto es lo único de la sesión que no estaba
  escrito en ningún lado**): en la ficha, «Esterilizado» es dato de semáforo, y
  en un animal de **menos de seis meses sin esterilizar** dice *«se entrega con
  compromiso de esterilización»* **en vez de un rojo**. Del lado portal, al
  pasar a «publicado» un animal de **más de seis meses sin esterilizar**, el
  interruptor **no enciende y dice por qué**: *«Los mayores de seis meses se
  publican esterilizados (Ordenanza 019).»*
  🔴 **El cuarto caso NO es «falta el dato»: es «falta y hay un compromiso»** —
  un estado con voz propia, igual que el `no_se_sabe` de `Convivencia`. **Se
  construye así o no se construye.** *Meterlo como una variante de «falta»
  perdería exactamente lo que lo hace honesto.*

**QUÉ ME DEBEN:**

- **A** — el veredicto sobre `verify:razon-muda` (ficha con las tres salidas y
  mi voto en `buzon/S112-B-para-A-gate-razon-muda.md`). **No lo toqué y no hay
  que tocarlo desde acá.**
- **C** — pedir por nombre las piezas de vidriera/ficha con su dato real cuando
  A le pase los lectores.

## FRENOS Y SUPUESTOS VIVOS

- 🔴 **`verify:razon-muda` mide una propiedad que se volvió FALSA** y su
  trinquete **castiga lo correcto**: cada razón nueva sin `onRazon` sube el
  número y corta. **No está en el pre-commit** (sale en el cierre). **Jamás
  bajarlo con `onRazon` vacíos** — es el error que S111 se negó a cometer.
- ✅ **Arbitrado (a):** el gate de `Convivencia` es el lote de adopción, sobre la
  ficha con datos reales. **Cerrado, no se espera para el recorrido de guardería.**
- **La galería ya NO es gate** (firma del founder). Las piezas se juzgan
  montadas, en contexto, en el recorrido. **B no construye láminas.** Las cuatro
  de S112 quedan como están.
- **Todo número que se reporte declara su condición**: un typecheck, si había
  deps; un conteo contra otra rama, **el SHA** — la rama de otra pista se mueve
  (me pasó: 13 → 12 en una hora).

## CÓMO SE VERIFICA QUE ALGO ESTÁ CERRADO (no se declara, se mide)

`git ls-remote origin pista/s112-b` **imprime un sha** que coincide con
`git rev-parse HEAD` — por SHA, jamás por código de salida.

---

## Lo entregado

| # | qué | commit |
|---|---|---|
| ① | **D-999 · `Boton` dibuja su razón** | `55f51ad6` |
| ② | **`Convivencia` · la tarjeta de todo-desconocido deja de leerse rota** | `55f51ad6` |
| ③ | **lámina de gate S112** en la galería, primera en «lo que espera tu firma» | `55f51ad6` |

Contratos servidos: `buzon/S112-B-para-C-CONTRATOS.md` ·
`buzon/S112-B-para-A-gate-razon-muda.md`.

## ① D-999 — la razón se dibuja

**El defecto no era el que la ficha nombraba.** No eran «ocho pantallas mudas
de trece»: `razonDeshabilitado` aparecía **una sola vez en el render y era
`accessibilityHint`** ⇒ la razón **no se dibujaba nunca**, ni con `onRazon` ni
sin él. La cura separa dos hechos que vivían pegados en una condición:
`hayRazon` (hay algo que **decir** ⇒ dibuja) · `conRazon` (hay a dónde
**llevar** ⇒ el toque va a `onRazon`).

**Las decisiones, con la ley que las manda:**

- **`Texto apoyo` → `text.secondary` por default.** Atenuado y jamás `danger`
  (N23 + la letra del founder: *no es un error mío, es un estado*).
- **SIN «i» en círculo, y es lectura de N22, no olvido.** Su criterio es *lo que
  hace falta para DECIDIR queda a la vista; lo que hace falta para ENTENDER va
  detrás de una «i»*. La razón es lo primero. Queda declarado para que el
  founder lo pueda dar vuelta viéndolo.
- **N24 por su LETRA, no por su cura derogada.** La mitad «reservá el espacio
  máximo desde el primer render» quedó derogada porque *el hueco se pagaba en el
  100 % y lo usaba una minoría*. Acá el renglón **se latchea**: nunca hubo razón
  ⇒ no dibuja nada (**87 archivos pasan `deshabilitado={`, 12 pasan razón: el 86 %
  no paga un píxel**); hubo una ⇒ el renglón queda y nada salta. *Así las dos
  frases del founder se cumplen a la vez sin elegir entre ellas.*
- **El alto lo pone el texto real montado invisible**, el truco que esta misma
  pieza ya usa con su label bajo el spinner. Un `minHeight: 20` copiado del
  `leading` de `Texto.apoyo` habría sido correcto hoy y falso el día que la
  razón envuelva a dos líneas.
- **El fundido de encendido nace ACOTADO** a los botones que explican (`micro`
  150, el registro que el token declara para *«crossfade de estado»*). El
  literal decía *«la transición suave de siempre»* y **de siempre no existía**:
  el `opacity` era reemplazo directo. Re-cronometrar los 501 botones de la casa
  es un cambio visible que nadie pidió (L-244).
- **El árbol de un botón sin razón queda idéntico.** No es optimización: es lo
  que hace que esto no pueda ser una regresión — el envoltorio de S103-B tiene
  un ensayo entero explicando su forma, y **envolverlo para todos habría
  reabierto esa discusión en 501 sitios para servir a 12**.

**Gratis:** `PieReserva` y `BotonCopiar` relevan la prop ⇒ ganan la línea sin
tocarles una línea. **Los 6 «mudos» ya dibujan.**

**Borde declarado:** padre que centra + razón más ancha que el botón ⇒ el botón
queda a la izquierda del bloque. Va al contrato de C (*línea corta*) en vez de a
una prop nueva.

### ⚠️ LO QUE D-999 **NO** CURA, y hay que decirlo para que su verde no se lea de más

Son **tres clases distintas** y esta cura toca una sola:

| clase | cuántos | qué le falta |
|---|---|---|
| pasa razón y no la dibujaba | **12** | ✅ curado acá |
| frena y **no pasa razón** | **84** | una decisión de producto por pantalla — fuera del gate a propósito |
| **no tiene condición** | — | **no falta que se dibuje la razón: falta la razón** |

**La tercera la levantó E midiendo el recorrido de adopción:** postular es un
toque sin condiciones ni consentimiento (`crear_solicitud_adopcion` toma 2
parámetros y `adoptar.tsx:156` no manda ni el mensaje). *Ahí un botón que
explica no arregla nada, porque el botón no frena* — y no está en mis seis.
Censo suyo en `buzon/S112-E-para-C-y-FOUNDER-CENSO-FORMULARIO.md`.

⇒ **«El botón dibuja su razón» no es «todo botón apagado ahora explica».**

## ② `Convivencia` — el defecto era de CARDINALIDAD

**Verificado leyendo el render, no opinando.** Con todas las filas en
`no_se_sabe` la tarjeta era **estrictamente menos** que cualquier otra en los
tres canales a la vez: cero puntos en toda la columna · cero `primary` · ningún
enunciado propio. *Una tarjeta que sólo se define por lo que le falta se lee
rota* — y es la del rescate de seis días, la que §3 dice que cuesta un hogar.

**La regla «la ausencia de marca es la marca» es correcta por fila y falsa por
bloque.** N23 ya lo había nombrado al pasar: *un elemento correcto en su tamaño
puede ser incorrecto en su repetición*.

**Curado en el BLOQUE, no en la fila:** con todo desconocido gana **título
propio** (`voces.sinObservar`, **obligatoria**) y sus voces suben a `primary`.
**No colapsa a un enunciado único** — eso habría borrado la voz de la fila que
dice algo distinto (*«aún no lo vimos con chicos»* ≠ *«todavía no se sabe»*),
que es el dato que la pieza existe para respetar.

## 🔴 Lo que levanto y NO toqué

**`verify:razon-muda` mide una propiedad que acabó de volverse falsa.** Su
cabecera afirma que sin `onRazon` el botón queda *«sin hint de a11y»*; desde
`55f51ad6` esos seis dibujan su razón. **Y su trinquete ahora castiga lo
correcto:** cada razón nueva que C pase sin `onRazon` sube el número y corta.
**Medido: no está en el pre-commit** ⇒ no frena commits, sale en el cierre.
Ficha completa con las tres salidas y mi voto en `S112-B-para-A-gate-razon-muda.md`.

## 🔴 CAMBIO DE REGLA — FIRMA DEL FOUNDER · LA GALERÍA DEJA DE SER GATE

> **Las piezas se juzgan MONTADAS en la app, en su contexto, en el recorrido del
> founder. Si una pieza no tiene puerta que la monte, eso es lo que se reporta
> —«entregada y no montada»— jamás una lámina.**

**Las cuatro láminas de S112 quedan como están** (orden del founder); lo que
cambia es de acá en adelante: **B no construye láminas de gate.** El gate de
D-999 y de `Convivencia` es el recorrido de mañana.

*Y la regla se cobra sola en este mismo parte: al medir qué está montado, tres
de mis cinco entregas resultaron ser piezas sin puerta — algo que cuatro láminas
verdes no habrían dicho nunca.*

### EL ESTADO REAL, medido contra `origin/pista/s112-c` (la rama que va al lote)

| pieza | puertas en `apps/` | estado |
|---|---|---|
| **D-999 · `Boton` dibuja su razón** | **12 archivos**, **6 de guardería** | ✅ **MONTADA** — gateable mañana |
| `Convivencia` (todo-desconocido) | **0** | ⏸ entregada y NO montada |
| `TarjetaAdoptable` | **0** | ⏸ entregada y NO montada |
| `BloqueConCriterio` | **0** | ⏸ entregada y NO montada |
| `FiltroPills` modo `varias` | **0** | ⏸ entregada y NO montada |

**D-999 no necesita que nadie toque nada:** las 12 puertas ya pasaban
`razonDeshabilitado` y ahora se dibuja. *Es lo único mío que el founder puede
juzgar mañana.*

> ### ⏪ ESTE NÚMERO YA SE ME MOVIÓ UNA VEZ, EN LA MISMA HORA — y es el mismo defecto, aplicado a otra clase de número
>
> **Lo reporté como 13 y medí contra `origin/pista/s112-c` en `fa271cd1`. Su tip
> hoy es `2e12e4fe` y son 12:** C revirtió el portal de adopción del prestador
> por orden de la mesa, y con él se fue
> `prestador/src/app/adopcion/solicitud/[solicitudId].tsx`.
>
> **La rama de otra pista es un objeto que se MUEVE, y yo la nombré sin fijarla.**
> Venía de escribir que un número de typecheck sin su condición no vale, y a
> renglón seguido publiqué un conteo sin su SHA. *Estar de acuerdo con la regla
> es gratis; lo caro es el número que ya está en el documento que otro va a usar
> para decidir.* (El empujón es de D, que encontró lo mismo en su propio
> entregable.)
>
> ✅ **Y lo que NO se movió es justo lo que decide mañana: los 6 de guardería.**
> El total bajó por una superficie de adopción que se retiró a propósito. *Que el
> número que importa aguante un cambio de rama no lo vuelve estable — lo vuelve
> afortunado, y por eso queda fijado igual.*
>
> **Medición de esta línea:** `origin/pista/s112-c` @ **`2e12e4fe`** ·
> 12 puertas · 6 de guardería. **Si la rama avanzó, se re-mide antes de citarla.**

**Los bloqueantes de las otras cuatro, nombrados:** `BloqueConCriterio` espera el
flag `destacado_espera` del servidor · `FiltroPills varias` espera los tres
estados de convivencia del lector · `TarjetaAdoptable` la montó C y la revirtió
al deshacer el merge · `Convivencia` espera la pantalla de abajo.

### 🔴 Y EL GATE QUE SE FIJÓ PARA `Convivencia` NO TIENE PANTALLA

**Medido en `origin/pista/s112-c`, rutas de adopción que existen:**

```
apps/cliente/src/app/adoptar.tsx                        ← la vidriera
apps/cliente/src/app/adoptar/solicitudes.tsx
apps/cliente/src/app/adoptar/solicitud/[solicitudId].tsx
```

**Ninguna es la ficha del adoptable.** Hoy la vidriera postula directo — no hay
pantalla intermedia donde `Convivencia` viva. ⇒ **«el gate es la ficha de
adoptable» no se puede correr mañana: la ficha no existe en ninguna rama.**

*Se declara hoy y no a las 09:00, que es la única forma en que esto sirve.*
**Y es exactamente lo que la regla nueva existe para hacer visible:** con la
lámina, `Convivencia` se veía verde y lista; contra la app, es una pieza
esperando una pantalla que nadie construyó todavía.

## 🔴 CANDIDATA DE LECCIÓN — se cobró en DOS pistas el mismo día, sin coordinación

> ### Antes de llamar «heredado» a un rojo, medí si el instrumento **podía ver algo**. Un worktree sin `node_modules` no produce «errores heredados»: produce **CEGUERA — y la ceguera se lee igual que la herencia.**

*(La formulación es de D; el número, si lo merece, lo pone A.)*

**Los dos casos, independientes y del mismo día:**

| | B (yo) | D |
|---|---|---|
| lo que vi | 7 × `TS2307` en `packages/ui` | 0 errores en `packages/api` |
| cómo lo leí | «clase heredada del worktree» | «verde» |
| qué había debajo | **un rojo REAL y mío** | nada |
| qué me frenó | **nada — declaré el gate con `SALTAR_GATE`** | **el hook, y fue a ver por qué** |
| factura | el defecto viajó commiteado en verde | ninguna |

**La asimetría es lo que la vuelve peligrosa:** el mismo razonamiento produjo
cero daño en un caso y un rojo commiteado en el otro. ⇒ *no se puede aprender de
esta clase por sus consecuencias, porque casi siempre no las tiene.* Y el número
que lo dice: **un `0` de un tsc sin `node_modules` y un `0` legítimo son
indistinguibles al leerlos** — la única diferencia está en si el instrumento
podía resolver un import, y eso no sale en la salida.

**Lo barato que era:** `pnpm install` en el worktree tarda **3,8 s**.

**Y el correctivo que ya rige para mí:** todo número de typecheck que reporte
declara si había deps instaladas. *Un verde sin esa declaración no es un verde:
es un número.*

## 🔴 EL CHOQUE DE DOS ÓRDENES FIRMADAS — a la mesa, no lo resuelve ninguna pista

> **El gate de mañana incluye la ficha de adoptable.
> El alcance del lote de mañana excluye la adopción.**
> Las dos son órdenes del founder y no pueden cumplirse juntas.

Lo levantó C, que además **ya ejecutó** la segunda: revirtió el portal de
adopción del prestador porque *«nada de adopción alcanzable a medias»*. Montar
mi `TarjetaAdoptable` en `adoptar.tsx` habría vuelto a poner superficie de
adopción en el mismo binario del que la acababa de sacar. **Se lo sugerí y C
hizo bien en negarse** — y no sólo por la contradicción: *tratar el mensaje de
una pista par como si fuera la aprobación del founder es exactamente lo que no
se hace.* El error fue mío: propuse trabajo en territorio ajeno sin conocer la
orden que lo gobernaba.

> ## ✅ ARBITRADO POR EL FOUNDER: **(a).**
> **El gate de `Convivencia` se corre en el lote de adopción, sobre la ficha con
> DATOS REALES.** Nada de lo construido cambia. **Queda cerrado: no se espera
> más para mañana** — y se escribe acá para que ninguna sesión lo re-litigue ni
> lo deje colgando.
>
> *Lo que decidió no fue la preferencia de nadie: fue que la ficha no se puede
> pintar con verdad hasta que exista su lector.*

### VOTO DE B (previo al arbitraje), que coincidió: **(a)**

Coincido con C, y agrego **la razón que es de mi pieza y por eso me toca decirla**:

**Medido por mí contra `origin/pista/s112-c`, no aceptado de palabra:**

- **CERO lectores devuelven convivencia.** Las únicas dos menciones en
  `packages/api` son un **comentario** y una palabra suelta de otro archivo
  (`paises.ts`, *«conviven»*).
- **`obtener_adoptable(publicacionId)` NO EXISTE.** Sólo hay la lista.
- Y la evidencia más fuerte es que **ya estaba escrita**: la cabecera de
  `adopcion.ts`, de S111-A, dice literal que *«`Convivencia` de B y
  `packages/mensajeria` de D existían y **no tenían de qué hablar**»*. **El motor
  de S111 curó la LISTA, no la FICHA** — y ese hueco lleva una sesión declarado.

⇒ **Aunque la mesa autorizara construir la ficha esta noche, no se puede
construir con verdad:** `Convivencia` recibiría tres estados que ningún lector
devuelve. **Y ahí el gate no sería sólo inútil: sería deshonesto.** *Todo el
valor de esta pieza es que el tercer estado dice la verdad sobre lo que el
refugio no sabe todavía; gatearla con datos inventados es la única forma de
volver mentirosa justamente a la pieza que existe para no mentir.*

**Las otras dos salidas, con su costo:** **(b)** ensanchar el lote devuelve el
portal que C acaba de revertir, porque el criterio sería el mismo · **(c)**
construir la ficha inalcanzable no permite gatear igual, así que no resuelve
nada y agrega superficie muerta.

## 🔴 LA CORRECCIÓN QUE ORDENA TODO ESTE PARTE — mi verde era flojo, y lo levantó C

**`55f51ad6` dejó `Boton.tsx` en `TS2322` y yo lo reporté verde.** C lo midió
contra un árbol con dependencias resueltas y lo reprodujo acá: mismo archivo,
**misma línea 752**, mismo mensaje. Curado en `ba730968`.

**La causa:** el spread condicional `...(cond ? transicion : null)` deja las tres
`transition*` como `?: T | undefined` dentro del objeto de estilo, y el tipo de
Reanimated no las acepta así — *un `| undefined` explícito no es lo mismo que
«ausente»*. El `as const` lo empeoraba (tuplas `readonly`). **La cura: se elige
entre DOS OBJETOS COMPLETOS** — una rama trae las tres presentes, la otra no las
tiene; ninguna declara claves opcionales.

⚠️ **Lo que C propuso no servía, y se dice porque su diagnóstico sí era bueno:**
sacar la transición a su propia entrada del array **no compila** — medido, una
entrada que trae sólo `transition*` es rechazada. Se tomó la mitad que era la
causa, montada donde compila.

### Por qué estuvo commiteado en verde — y esto es lo que trasciende

1. **Mi control positivo no compartía los supuestos del caso real.** Sembré
   `const x: number = "texto"`, tsc lo cazó, y concluí «el instrumento ve mi
   archivo». **Lo veía.** Pero un error trivialmente local lo caza cualquier tsc
   *aunque no chequee los tipos de estilo que yo estaba tocando*: probaba que
   **LEE**, no que **CHEQUEA**. `L-459` otra vez — *la primera prueba de un
   guard no es que dé verde, es que dé ROJO sobre el caso real.*

2. **«`packages/ui` typechea verde» NO ES «la pieza compila».** Medido sobre el
   MISMO archivo roto: **`packages/ui` 0 · `apps/cliente` 0 · `apps/prestador` 1**.
   ⇒ **una pieza compartida se mide contra los tsconfig de TODOS sus
   consumidores.**

   ⚠️ **Y NO: «el que caza esta clase es el prestador» — esa regla es FALSA y la
   corrigió C.** En **su** árbol el mismo error salió corriendo el tsconfig del
   **cliente**. Fui a buscar la causa de la asimetría y **no la aislé**: los dos
   programas incluyen **los mismos 159 archivos** de `packages/ui`, con
   **opciones de compilador idénticas** y la **misma versión de reanimated**;
   sólo difieren en los `.d.ts` de las dependencias propias de cada app.

   ⇒ **La regla que sí se sostiene, y es de C:** *cuál de los tres caza una clase
   dada NO es estable entre árboles, así que ninguno es el que se puede saltear.*
   **Se corren los tres.** Tomar «el prestador es el estricto» como ley sería
   repetir el defecto de hoy con otra ropa — *un verde que viene de no haber
   podido mirar.*

3. 🔴 **Y EL HOOK ME QUISO FRENAR.** Corrió el typecheck del consumidor, dio
   rojo, y lo declaré con `SALTAR_GATE` y una medición **cierta** —721 errores
   con y sin mi cambio, delta cero— **que contestaba la pregunta equivocada**: la
   corrí sobre `apps/cliente`, justo el consumidor que no ve esta clase, y en un
   worktree sin `node_modules` donde 721 errores de entorno tapaban la señal.
   *Un delta cero medido sobre el instrumento ciego no dice que no rompiste nada:
   dice que ese instrumento no mira.* **El hook tenía razón y yo lo apagué con
   un número.**

**Correctivo aplicado:** `pnpm install` en el worktree (**3,8 s**) y typecheck de
los tres tsconfig. **Los 7 errores «Cannot find module `@epetplace/*`» que este
parte declaraba como “clase heredada” DESAPARECIERON: no eran una clase
heredada — era mi worktree sin deps.** *Llamé «herencia» a mi propia ceguera, y
eso volvió cómodo saltar el gate.*

## Cómo se midió (para que el verde diga algo)

- **Control positivo sobre el instrumento:** error sembrado en `Boton.tsx` ⇒
  `tsc` exit 2 con las dos líneas exactas; retirado. *Sin eso, «cero errores»
  podría ser «no pude medir».*
- **`sinObservar` probada EN ROJO:** quitada de un call site ⇒ `TS2741`.
  *«Inexpresable» sin su rojo es una afirmación, no una medición.*
- **Superficie de tipos de `BotonProps` byte-idéntica** — 3 líneas no-comentario
  tocadas, **ninguna del `interface`** ⇒ ningún consumidor puede romper por tipo.
- **`verify:diseno` VERDE, 62 reglas.**
- **Los TRES tsconfig en 0, con deps instaladas: `packages/ui` · `apps/cliente` ·
  `apps/prestador`.** *(Lo que este parte decía antes —«0 fuera de la clase
  heredada»— era un verde flojo; ver la corrección de arriba.)*
- **Y el control que lo cierra:** con el código commiteado, `apps/prestador` da
  **1**; con la cura, **0**. *El instrumento produce su rojo antes de que le
  crea el verde.*

## Lo que NO está verificado, y hay que decirlo

**Nada se vio en aparato.** ⏪ *Este párrafo decía «el gate visual es la lámina
S112 de la galería» y quedó FALSO con el cambio de regla — y estuvo un rato
contradiciendo a la sección de arriba, en el mismo documento. **Dos frases que se
contradicen son peores que una equivocada: cualquiera cita la que le conviene y
está en regla.***

**El gate de D-999 es el RECORRIDO, en las 6 pantallas de guardería**, y es lo
único que puede juzgar lo que el código no prueba: **que la línea se lea como
explicación de la casa y no como un error**, y **que al encenderse el botón la
pantalla no salte**.

## ④ `TarjetaAdoptable` — pedida por C con su dato real, ENTREGADA (`81656997`)

**La decisión que ordena la pieza: las dos ausencias del dato NO son la misma.**

- **`edad` null SE DICE**, en la misma línea y el mismo peso que el resto de la
  identidad. §3 pide edad **estimada** y el wrapper documenta que *no se infiere
  una edad que nadie declaró*. *Callarla la volvería un hueco; agrisarla la
  volvería un defecto del animal.*
- **`raza` null NO SE DICE.** «No la declararon» **no es «mestizo»**, y mestizo
  es categoría legítima: la pieza jamás la escribe por su cuenta. El refugio que
  declara «Mestizo» lo ve igual que cualquier otra raza — sin paréntesis, sin
  gris, sin «sin raza definida».

*Tratarlas igual —callar las dos, o rotular las dos— era el error cómodo.*

**Lo que no tiene, y es la mitad de su diseño:** sin favorito, sin swipe, sin
puntaje, sin badge de match. **No están pendientes: §4 los prohíbe**, y se
declara en la cabecera porque son las cuatro cosas que cualquiera le agregaría a
una tarjeta de catálogo — y esto no es un catálogo.

**Relación 1:1 MEDIDA, no copiada:** acá no podemos imponer estándar de imagen
(la foto la saca un refugio con su teléfono, casi siempre en retrato). Una 3:4
en 1:1 pierde **~25 %** de alto; en 4:3, **~44 %** — y lo que se recorta de un
animal en retrato es la cabeza. **Sin foto** hay estado propio: la huella que el
founder ya firmó en `AvatarMascota`, estática (*«no hay foto» y «todavía no
llegó» son dos cosas distintas*).

**Su puerta:** `apps/cliente/src/app/adoptar.tsx` — **hoy NO montada** (C la
montó y la revirtió al deshacer el merge). **Su gate es el lote de adopción**,
montada ahí; la lámina de la galería ya no es gate.

## En cola

**El bloque «Llevan más tiempo esperando» y los chips de convivencia NO se
construyeron, y la razón es de C:** su criterio vive en el servidor
(`destacado_espera`, pedido a A) y **§4 prohíbe explícito ordenar por
antigüedad**. Alimentarlos hoy por `creadaEn` sería construir exactamente lo que
la letra no quiere. *Un freno que nombra su bloqueante alguien lo destraba.*
