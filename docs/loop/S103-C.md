# S103 · PISTA C — LAS SUPERFICIES · BITÁCORA

> **Territorio (`PLAN_MESA_104` §1):** `apps/cliente` · `apps/prestador`.
> **Worktree:** `../e-petplace-s103-c` · **rama:** `pista/s103-c` · salida de
> `main` en `e96c1be6`.
> **Veda 76(g) de A:** confirmada — **cero escrituras a la base desde 10:05 -05**,
> y se cumplió sin costo: esta tanda no tocó la base ni una vez.

---

## ⓪ · TANDA 0 — el censo

Depositado en `docs/relevamientos/2026-08-21-mesa104-C-tanda0-SUPERFICIES.md`
(A lo mergeó a `main` en `7ece09d8`). **Su hallazgo principal: de las cuatro
superficies del encargo, DOS ya existían** — la hoja «Cómo quieres pagar»
(vigilada por `R57`) y la espera (que no es una pantalla: es la fase
`confirmando` de las dos puertas). *Diseñarlas de cero habría producido la
segunda copia que `R57` existe para impedir.*

---

## ① · TANDA 1 — lo construido, con su estado real

**Tres commits, todos en `apps/cliente`. Typecheck VERDE · `verify:diseno`
VERDE con 49 reglas · `R57` VERDE.**

| # | commit | qué |
|---|---|---|
| 1 | `1625ee0c` | «Ayuda y legales» |
| 2 | `4718f76b` | DeUna: la fila + la pantalla del código |
| 3 | `4af185aa` | La serie recurrente + la muerte del voseo |

### ①.1 · «Ayuda y legales» — **la única de las cuatro que quedó COMPLETA**

- ☠️ **Muere `legalPlaceholder`**, que afirmaba publicado *«no se cobra dinero
  real»*. **Era cierta cuando se escribió y dejó de serlo sin que nadie la
  tocara** (S101 conectó el motor de cobro). *Es la única pantalla donde la app
  hace una afirmación legal sobre el dinero, y afirmaba lo contrario de lo que
  el motor hace.*
- **El soporte pasa de promesa a botón.** WhatsApp con mensaje pre-escrito,
  patrón del prestador (`solicitar-acceso`, S61-B13). **`lib/contacto.ts` gana
  su PRIMER consumidor** — existía desde S96-D sin uso.
- **UN índice legal, no N documentos.** `https://www.epetplace.com/legales`
  (`/en/legales`), contrato de B vía A y ratificado por el founder.
- **D-336 cerrada del lado del producto.** *(El contenido sigue abierto: D-405.)*

⚠️ **NO GATEADO:** `/legales` **da 404 en producción hoy** — las rutas de B
esperan el gate de despliegue del founder. El verde lo da el juez de B en
tanda 2, no esta pantalla.

⚠️ **Lo que la letra de `contacto.ts` pide y NO se cumple, declarado:** *«el
botón dice a dónde va **y en qué horario contestan»***. **No hay horario
firmado y no se inventó uno**; la voz no promete plazo.

### ①.2 · DeUna — construido contra la letra, con el contrato de D pendiente

- **La fila vive en la hoja**, debajo de las tarjetas y encima de «agregar».
  *`R57` sigue verde: los dos checkouts montan la misma pieza.*
- **Se dibuja y no se puede elegir**, y las dos mitades están medidas:
  se dibuja porque *ningún estado se dibuja mudo* (§0.4); no se elige por la
  **Ley 23** (*la puerta no ofrece lo que va a rechazar*). **Causa: el
  `pointOfSale` que D midió como bloqueante único — dueño el founder.**
- 🔴 **No nació ningún tipo nuevo para «qué medio se eligió», y es
  deliberado.** *Mientras DeUna no se pueda elegir, una unión discriminada es
  un modelo de datos para un caso que no puede ocurrir.*
- **La pantalla del código NO hardcodea 3 minutos**, por la advertencia de D:
  la letra dice *«3 minutos fijos, no configurables»* pero el request tiene un
  campo `expiredTime` **y es un número**. ⇒ el vencimiento entra como
  **instante del servidor**. *La pantalla no puede equivocarse porque no sabe
  la regla — solo lee un reloj.*

### ①.3 · La serie recurrente — construida, y **honesta sobre lo que no puede decir**

- ☠️ **Muere la promesa con dirección** (*«Lo manejás desde Tus pedidos»*
  apuntaba a una superficie inexistente) **y con ella el último voseo vivo** en
  un valor de cadena del cliente.
- **UN botón: cortar.** *El aviso promete «saltar, mover o cancelar» y de las
  cuatro funciones ninguna saltea ni mueve* (censo de A #2).
- **Monto esperado y medio de pago tienen VOZ DE AUSENCIA**, jamás relleno.
  *Un `$0` en el lugar de una plata que no conocemos se lee como «gratis».*
- **El contrato es un PEDIDO ESCRITO EN TIPOS** (`lib/serie/contrato.ts`), no un
  contrato inventado. **A lo aceptó** y entrega estado de tres valores, medio
  autorizado, monto esperado y contador de reintentos.
- **El corte es real; la lectura no existe** ⇒ **no se agregó la entrada en
  `pedidos/index`.** *Una fila que lleva a una pantalla que no puede leer nada
  es un callejón con nombre bonito.*

---

## ② · 🔴 LOS DOS FRENOS DE ESTA TANDA — declarados, no maquillados

**① EL GATE EN DISPOSITIVO NO SE CORRIÓ: no hay aparato conectado.**
`adb devices` → **lista vacía**, medido dos veces. *El encargo dice «device
primero vía USB antes de convocar al founder», y eso no se puede cumplir sin
el aparato.* **Nada de esta tanda tiene ojo.**

**② Y aunque estuviera conectado, la mitad no se podría caminar hoy:** la veda
de A. Llegar a la hoja de medios **crea filas** (`crear_intento_pago` reserva
stock y mueve la compra a `esperando_pago`). *A lo confirmó explícitamente y
avisa cuando levante.*

**Lo que SÍ se podrá gatear apenas haya aparato, sin esperar la veda** —porque
no escribe nada—: **«Ayuda y legales» entera** (el botón de WhatsApp, el
fallback del número, el enlace legal) y **la pantalla de la serie** por URL
directa (su estado vacío). *Lo que espera a la veda es solo la fila DeUna, que
vive dentro del checkout.*

---

## ②bis · 🔴 EL INCIDENTE DEL APARATO — le rompí el gate a A, y las dos lecciones

**Los hechos, medidos y sin matizar:**

| hora | qué |
|---|---|
| ~10:52 | Avisé a A que **soltaba** el aparato: Metro apagado, `adb reverse --remove-all`, app force-stopped. |
| ~11:00 | El founder ordenó enchufar `BotonCopiar` ⇒ **volví a levantar Metro (8081) y a poner el reverse.** ⚠️ **No avisé.** |
| ~11:05 | Terminé y repetí `--remove-all` + force-stop ⇒ **maté el reverse y la app que A estaba manejando.** |

**A lo diagnosticó en vez de suponerlo** (`pidof` vacío · `reverse --list` vacío · su Metro caído) y **estuvo a punto de reportarlo como crash de la pantalla de Despensa.**

### La lección ① — el aviso de liberación sin su aviso de retome

**El error nombrable no es haber tomado el aparato: es haber anunciado la liberación y no anunciar el retome.** *Mi aviso le dio a A un dato correcto que después se volvió falso sin que nada se lo dijera.* Un anuncio crea una expectativa, y **romperla en silencio es peor que no haber anunciado nunca.**

### La lección ② — un instrumento de alcance global para limpiar algo propio

`adb reverse --remove-all` **no dice «los míos»: dice TODOS.** Lo usé leyéndolo como *«limpiar lo que dejé»*.

> *Es la misma forma que esta mesa viene cazando todo el día: **un instrumento cuyo alcance excede su intención no falla — hace de más**, y el daño cae sobre alguien que no lo invocó.*

**Rige desde acá:** `adb reverse --remove tcp:8081`, **jamás `--remove-all`** · nunca `force-stop` de la app sin avisar · **puertos fijos: C en 8081, A en 8082** (para que un `curl` con `200` no obligue a nadie a adivinar de quién es el bundler — `L-336` por tercera vez en el día).

### ⏪ Y LA CORRECCIÓN A MI PROPIO PARTE, que es la parte incómoda

El commit `b712bc68` afirma que mi pantalla negra *«era una recarga de Metro coincidente»*.

**Lo que medí fue: proceso vivo · foco en MainActivity · cero FATAL en logcat.** Eso prueba **que no fue un crash**, y **no prueba que haya sido una recarga** — esa atribución la puse yo.

Sabiendo ahora que estábamos **los dos sobre el mismo aparato**, la causa más probable es **interferencia entre pistas**. ⇒ **mi medición era correcta y mi conclusión iba un paso más allá de lo que medía**, que es exactamente lo que la casa llama *verde por la razón equivocada*. *Se corrige acá y no se deja escrito como estaba.*

**El dato que lo vuelve clase:** a A y a mí nos pasó lo mismo con cinco minutos de diferencia y **los dos lo diagnosticamos por separado como defecto de una pantalla**. No era de ninguna pantalla: era de coordinación.

---

## ③ · COMPROMISOS CRUZADOS

| | |
|---|---|
| **C ← A** | el **lector de series** por `packages/api`, con estado de tres valores · medio autorizado · monto esperado · reintentos. **Aceptado por A.** *No construyo contra un contrato no declarado.* |
| **C ← D** | `numericCode` + su instante de vencimiento. **Bloqueado por el `pointOfSale`** (dueño: founder). |
| **C ← B** | que `/legales` responda en producción. **Hoy 404.** |
| **C → A** | *(dado)* que su divergencia #9 (el voseo) **murió en esta tanda**, y que su #2 (saltar/mover) **me ordenó el diseño**: un botón, no tres. |
| **C → B** | su juez de tanda 2 («ninguna URL del enchufe de C devuelve 404») **tiene un consumidor real desde hoy**: `apps/cliente/src/lib/legales.ts`. |

---

## ④ · COLA VIVA

1. 🔴 **Conseguir aparato y correr el gate** de lo que no escribe (①.1 y ①.3).
2. 🔴 Esperar que **A levante la veda** para caminar el checkout y ver la fila
   DeUna en su hoja.
3. Cuando A publique el lector: conectar `cargarSerie` **y agregar la entrada
   en `pedidos/index`** — nacen juntas, en la misma línea.
4. Cuando D entregue su contrato: montar `EsperaDeUna` en la fase
   `confirmando` de las dos puertas y volver elegible la fila.
5. Cuando `/legales` responda: declarar el enchufe gateado.
