# S100c-B — DIRECCIÓN DE DISEÑO · LA VUELTA DE LAS LEYES QUE FALTABAN

> **Pista B · rama `pista-b` · worktree `e-petplace-s100-b`.**
> Territorio: `packages/ui` · `docs/` (canon y láminas) · `scripts/verify-diseno.mjs`.
> **El SHA de esta rama se MIDE, no se lee de acá:** `git ls-remote origin pista-b`.

---

## ⓪ ARRANQUE — lo medido, no lo supuesto

| instrumento | resultado |
|---|---|
| worktree heredado vs `origin/main` | **0 adelante · 63 detrás** ⇒ estrictamente ancestro. Fast-forward sin pérdida |
| `pnpm install` | exit 0 · lockfile intacto (*el repo declara `pnpm`*) |
| `packages/ui` → `tsc --noEmit` | **exit 0** |
| aparato | SM-S938B `R5CY201ZDVL` por USB. `stay_on_while_plugged_in` original **0** → puesto en **3** |

**🔴 EL OBJETO SE VERIFICÓ ANTES DE MIRAR NADA** (L-138: *el gate de una APK
empieza confirmando qué corre*). El pie de Cuenta declara
`update 01a0130f · preview · 17/08 23:09`; el group más nuevo del canal
(**`fbd7b6e0`**, *«S100b · la vuelta de la forma»*) se publicó a esa misma hora
y minuto — medido *«59 minutes ago»* con el reloj del aparato en 00:08. Su
ancla **`f107eac9`** está **verificada ancestro de `origin/main`**.
⇒ **lo que miré es lo que el founder gateó.**

---

# LOTE 1 · LAS CINCO LEYES AL CANON (N21 → N25)

**`docs/DIRECCION_DISENO_S99.md` §1bis, v1.0 → v1.1.**
**Medición completa: `docs/laminas/2026-08-18-s100c-MEDICION-SUPERFICIES.md`.**
**Instrumento nuevo: `scripts/censar-superficies.py`.**

| | ley | de dónde sale |
|---|---|---|
| **N21** | lo que AGRUPA va en carta; lo que ES la pantalla, no | ① del arranque |
| **N22** | lo que se necesita para DECIDIR queda a la vista; lo que se necesita para ENTENDER va detrás de una «i» | ② |
| **N23** | el color marca CLASE, jamás importancia | mensaje de mesa en vuelo |
| **N24** | un control no cambia el tamaño de lo que lo contiene | ③ (el stepper) |
| **N25** | una sola puerta al carrito, y es la que alcanza el pulgar | ③ (glifo + flotante) |

## 🔴 EL HALLAZGO QUE ORDENA N21, Y DA VUELTA LA CURA OBVIA

**El detalle del pedido tiene CUATRO grupos rotulados y CERO superficies.
El 88,5 % de su área útil es un solo color** — y ahí vive el código de la
puerta (`pedido/[pedidoId].tsx:396`, dentro de un `View` con padding y nada
más). *La frase del founder no es una impresión: es la descripción exacta del
árbol.*

**Y lo que NO es el defecto:**

| | fondo | carta | contraste |
|---|---|---|---|
| **nosotros** | `#FAF2F5` | `#FFFFFF` | **1,10** |
| **Laika (aparato)** | `#F2F2F2` | `#FFFFFF` | **1,119** |

**Cuando ponemos carta separamos igual que la referencia** ⇒ **subir el
contraste no habría curado nada.** Lo que falta es la superficie. Y el reparto
lo dice solo: **carrito de Laika 22,4 % fondo / 45,1 % superficie; el nuestro
69,7 % / 10,2 %** — *siete veces más fondo que superficie.*

**⚠️ Y los dos ceros no son el mismo cero.** `referencia-laika-resumen-pedido`
también da 0 planos, y tiene SIETE cartas separadas por hairline: su censo real
es **0 planos · 23 reglas**; el nuestro **0 · 0**. *Apoyar la ley en esa
equivalencia habría sido una conclusión correcta sobre premisa falsa* (L-294)
⇒ el instrumento cuenta las dos cosas.

## EL INSTRUMENTO SE CORRIGIÓ DOS VECES ANTES DE QUE YO LE CREYERA

1. **Cuantizaba el color** ⇒ `#FAF9F7` salía `#F8F8F0` y el contraste **1.005**
   donde el valor real es **1.052**: **error de 10× en el único número que la
   ley usa.**
2. **Sin máscara de planitud**, la caja de la carta se comía el ruido de al
   lado: **área plausible, caja falsa.**

**Verificado contra geometría plantada, y su `1.052` coincide con el número
que la casa ya tenía escrito en `Tarjeta.tsx:127` por otro camino** — dos
cuentas, un número (L-287).

---

# LOTE 2 · ③ EL CARRITO — y una decisión de S100b que se reabre

- **El glifo lleva RUEDAS.** La distinción `carrito` ≠ `despensa` de S100b
  **sigue en pie**; lo que se corrige es la forma. **La causa es de método:**
  se dibujó desde la palabra *«canasta»* del texto del gate en vez de medirse
  contra el objeto, y una canasta con asa de arco **tiene la silueta de una
  bolsa**. *La referencia se MIDE, no se describe* — la ley de la barra de S99
  en ropa nueva.
- **Una sola puerta: la flotante REEMPLAZA a la del encabezado** (medida en
  `x=[344,368] · y=[80,104]`, el peor ángulo para un pulgar). **Y no necesita
  mecanismo nuevo: `PantallaConPie` la sostiene** —su reserva es derivada, así
  que una flotante montada como su `pie` no puede tapar contenido, y montada a
  mano con `absolute` la muerde R53.
- **El stepper rompe la fila, con número: 309,0 → 353,1 dp = +44,1 dp**,
  exactamente un blanco de 44, **y la tarjeta vecina no se mueve**.
- **La eliminación no da acuse.**

---

## 🔴 UN ERROR MÍO, CAZADO ANTES DE REPORTARLO

Reporté que el carrito del encabezado **violaba N8** (el árbol da
`23,8 × 23,8 dp` contra los 44 de ley). **Es falso:** el `Pressable` lleva
`hitSlop={spacing[3]}` ⇒ **48 dp de blanco efectivo**, que supera el mínimo.

> **`uiautomator dump` NO reporta `hitSlop`.** ⇒ **ninguna auditoría de blancos
> de 44 se puede hacer desde el árbol solo**: sub-reporta por `2 × hitSlop`, y
> la casa usa `hitSlop` como recurso normal.

**Lo cazó que dos cuentas no coincidieran, no una relectura más cuidadosa.**

---

## LO QUE DECIDÍ **NO** HACER, CON SU RAZÓN

- **NO pagué desde el carrito para ver el código renderizado.** Habría creado
  un pedido: es dato vivo, y es freno. Se midió **dónde vive** el código y
  **el censo de la pantalla que lo aloja**, que es lo que la ley necesitaba.
- **NO avancé el checkout de Laika.** Es la cuenta real del founder con un
  carrito real; navegar es mirar, comprar no. Las pantallas que faltaban ya
  están como capturas en el repo y **se midieron con el mismo instrumento**.
- **NO instalé un rasterizador de SVG** para verificar el glifo a 21 px. Es
  una dependencia nueva y el gate real es el aparato. **Se declara que no pude
  verificarlo**, en vez de darlo por bueno.
- **NO toqué las pantallas de C/A/D.** Las cinco leyes son de canon; las curas
  son de sus dueños (L-295).

---

## ⚠️ LO QUE NO SE MIDIÓ, DECLARADO

- **Oscuro y memorial: NO medidos.** Todo lo de acá es tema claro.
- **La pantalla «en camino» y el código EN PANTALLA:** esta cuenta no tiene
  ningún pedido en `en_camino` ni `entregado`. *No se forzó dato vivo.*
- **La legibilidad del glifo nuevo a 21 px** (sin rasterizador local).
- **Nada de lo que se construya hoy tiene ojo hasta un publish**, y publicar
  pide firma.

## ✅ N23 — RESUELTA POR LA MESA: EL SUJETO ERA EL REFERENTE

**El censo no encontró el patrón en nuestra app y lo declaró en vez de
rellenarlo.** La mesa aclaró que **el founder describía LAIKA** (`7 % OFF` en
verde junto al precio en negro, la membresía en gris con corona) y que **lo
transmitió como si el sujeto fuéramos nosotros.**

⇒ **N23 queda depositada y es correcta: no cura, IMPIDE** — y ahora tiene su
caso de estudio con nombre y medido en `docs/diseno/referencias/`.
**Al canon como L-296.**

---

# LOTE 3 · LAS CUATRO LECCIONES AL CANON (L-296 → L-299)

`docs/DEUDAS_CANONICAS.md` §Lecciones. **Techo verificado por grep antes de
numerar: L-295 → siguiente libre L-296** (`L-714` sigue descartado por su
propia ficha: es typo de `D-714`).

| | ley | quién se equivocó |
|---|---|---|
| **L-296** | un hallazgo cambia de significado si se pierde sobre qué objeto se observó | **la mesa**, y lo declaró ella |
| **L-297** | cuando dos objetos miden igual en la variable obvia, la variable obvia no es la causa | — (habría errado la cura) |
| **L-298** | un número igual sobre anatomías distintas no compara nada | **el instrumento**, cazado antes de apoyar la ley |
| **L-299** | ninguna auditoría de blancos se hace desde el árbol solo (`uiautomator` no reporta `hitSlop`) | **yo**, cazado antes de reportar |

**L-299 va también a la ficha del instrumento**, para que nadie repita la
lectura de 23,8 dp. **Y su gemela ya estaba escrita del otro lado:** en S100b
el árbol reportaba precios de filas recortadas que la pantalla no mostraba.
***El árbol miente en las dos direcciones: dice de más lo que no se ve, y de
menos lo que sí se toca.***

---

# LOTE 4 · LAS PIEZAS DE N24 Y N25 — construidas, **sin ojo**

⚠️ **Nada de esto se vio correr: son cambios visuales que no existen hasta un
publish, y publicar pide firma.** Van todos al gate del conjunto.

## ① `PantallaConPie` — el espacio muerto era MÍO, y era doble conteo

**El founder reportó *«~1 cm muerto entre el botón y el menú»*. Medido:**

| | |
|---|---|
| barra de navegación del sistema | **52 dp** |
| reserva de la pieza (`insets.bottom + spacing[3]`) | **64 dp** |
| base del CTA medida en el aparato | **638 dp** |
| filo de la barra de tabs | **699 dp** |
| **hueco** | **61 dp** ≈ **0,97 cm** (1 cm = 63 dp en este aparato) |

**La causa: adentro de `(tabs)` el navegador YA reserva la barra del sistema**
—lo mismo que mi predecesora midió para el scroll—, así que la pieza lo sumaba
**dos veces**. 🔴 *Es el defecto que esta pieza nació para matar, cometido por
la pieza misma: dos números que deben coincidir, saliendo de dos lugares.*

**La cura NO es una prop.** `absorbeInset?: boolean` habría devuelto la
decisión al consumidor —lo que la Ley 8 prohíbe— **y habría dejado el par
descoordinado otra vez**. **Se DERIVA:** el contenedor se mide en la ventana y
reserva **solo lo que falta**. Adentro de tabs eso da **0**; suelta, el inset
entero. *El consumidor no tiene nada que declarar porque no hay nada que saber.*

**El estado inicial es el CONSERVADOR** (`insets.bottom`): si la medición nunca
llegara, el pie queda separado de más, **jamás pegado al borde**. *De los dos
errores posibles se elige el que no tapa nada.*

## ② `TarjetaProducto` + `StepperCantidad` — N24, con las dos cuentas coincidiendo

**Aparato: 309,0 → 353,1 dp = +44,1** al agregar, con la vecina quieta.
**Fuente: `BOTON_COMPACTO` 36 + `gap: spacing[2]` 8 = 44.** *Dos cuentas, un
número.*

⇒ el slot del control **existe siempre**; lo que cambia es si está ocupado.
Y su alto **se deriva**: nace `ALTO_STEPPER_COMPACTO`, exportado desde
`StepperCantidad`, **en vez de teclear un `44`** — si el stepper cambia de
alto, la reserva lo sigue sola (L-284).

⚠️ **Costo declarado: +44 dp en todas las tarjetas.** El acreedor es el que
S100b ya nombró: el bloque de texto y el header.

## ③ El glifo `carrito` — ahora con RUEDAS

La distinción `carrito` ≠ `despensa` **sigue en pie**; lo que se corrige es la
forma. **Las ruedas son el discriminador** porque son el único rasgo que una
bolsa y una canasta no pueden tener, y **sobreviven a 21 px** (dos discos, no
un detalle de trazo). Entra también **la barra de empuje**, que es el otro
rasgo que la industria repite y el que ancla la lectura.

⚠️ **Sin gate de ícono, y sin poder simularlo:** §2.9 pide verlo a 21 px y ese
gate es del founder. **En este entorno no hay rasterizador de SVG** (ni
`cairosvg`, ni `rsvg-convert`, ni Inkscape) ⇒ **no se verificó** su
legibilidad. *Se dice en vez de darse por bueno.*

**Y un carácter invisible cazado antes de subir:** se coló un *zero-width
space* en el comentario del glifo. Se eliminó. *Un carácter que no se ve en el
código es exactamente la clase de cosa que muerde después.*

---

## 🔴 H-115 · EL BORDE LILA VIVE EN UNA PIEZA QUE NO ES MÍA — **dueño: C / A**

**El defecto que el founder ve en el carrito NO está en `TarjetaProducto`**
(ahí ya se curó en S100b: la caja de foto toma `bg.card` cuando hay foto).
**Está en `LienzoProducto`** (`apps/cliente/src/components/despensa-piezas.tsx:79`),
que pinta **`theme.bg.overlay` SIEMPRE** — también detrás de una foto. Con
packshots de fondo transparente, ese lavanda asoma: **ése es el «marco lila».**

**La cura es de una línea y ya está probada del otro lado** — la misma regla
que `TarjetaProducto` aplica:
```
backgroundColor: fotoUrl === null ? theme.bg.overlay : theme.bg.card
```
*El fondo hundido es el ESTADO «sin foto», no el marco de una foto.*

**Sus tres consumidores medidos:** `carrito.tsx:260` (lado 56) ·
`producto/[productoId].tsx:511 y 526` (lado 240 y 56). **Curarlo en la pieza
los arregla los tres; curarlo por pantalla arregla uno.**

⇒ **Y la recomendación que lo cierra de verdad: `LienzoProducto` sube a
`packages/ui`.** Hoy es una pieza de producto viviendo en una app, y por eso
la cura de la vitrina no viajó al carrito. *Mientras viva ahí, cada superficie
nueva vuelve a heredar el defecto.* **No lo curo de este lado** (L-295: curar
código ajeno resuelve el defecto y borra el aprendizaje).

---

# LOTE 5 · MEDICIONES PEDIDAS POR LA PISTA A (tengo aparato; ella no)

## 🔴 A-04 · LA HOJA DE DIRECCIÓN — **es peor que «casi pegado al techo»**

**Camino exacto:** checkout → línea de dirección → Hoja **con el pin ya
montado** → tocar el campo `Dirección` para que suba el teclado.
**Capturas: `docs/laminas/s100c-medicion/A04-hoja-direccion-*.png`.**

| qué | medido |
|---|---|
| inset de la barra de estado (cutout) | **25,6 dp** (72 px) |
| `Sector o barrio (opcional)` con el teclado arriba | **y = [8,5 · 30,6] dp** |
| ⇒ el rótulo entra **bajo la barra de estado** | **17,1 dp por encima del área segura** |
| **el campo ENFOCADO (`Dirección`)** | **NO ESTÁ EN EL ÁRBOL: salió de pantalla por arriba** |
| el título de la Hoja y su asa | **también salieron** |

**Lo que se ve en la captura:** el rótulo `Sector o barrio (opcional)` se
dibuja **encima del reloj del sistema** — dos textos pisándose.

⇒ **La descripción del founder se queda corta: no es que el texto quede
pegado al techo — es que EL CAMPO EN EL QUE ESTÁ ESCRIBIENDO SALE DE LA
PANTALLA, y lo que queda arriba se mete debajo de la barra de estado.**

⚠️ **Medido, NO diagnosticado, y a propósito.** Las cuatro observaciones de
A sobre la fuente (`altura="completa"` 0.9 · `paddingTop: spacing[1]` ·
`padding` para el teclado · las Hojas fuera del `EvitaTeclado`) **son
candidatas, y ninguna está probada como LA causa.** *A pidió medición
justamente para no repetir los 53 dp de la barra de tabs: aritmética limpia
sobre premisa no medida.* **La cura es suya; el número es mío.**

## A-05 · EL SELECTOR DE VENTANA — el número que refuerza el pedido

**Medido en el checkout, con el mismo recorrido:** `Cuándo te llega` arranca
en **y = 595,9 dp**, y el pie fijo (`Ver el total`) ocupa desde ~**593 dp**.

⇒ **no es solo «invisible sin deslizar»: en reposo arranca JUSTO donde
empieza el pie fijo.** El founder tenía razón y la razón es más dura de lo
que suena.

---

# LOTE 6 · LA CURA DE `Hoja` (A-04) Y LA MEDICIÓN DE LA BARRA (pedido de D)

## ✅ `Hoja` — A-04 curado, con el mecanismo PROBADO y no argumentado

**A diagnosticó y me mandó una PREDICCIÓN FALSABLE. La corrí antes de curar.**

**El mecanismo:** no falla ninguna de las dos piezas por su cuenta — **falla
que convivan**. El `KeyboardAvoidingView` usa `behavior="padding"` (decisión
medida de S83-B36) y con el teclado le suma un `paddingBottom` de su alto ⇒ la
caja útil pasa a **`H − K`**; pero `altura="completa"` fija **`height: 0.9·H`**,
y **un hijo de alto FIJO alineado al fondo de una caja más chica no se encoge:
desborda hacia ARRIBA.**

**LA PRUEBA QUE PODÍA TUMBARLO** (`altura="contenido"`, que ya encogía sola):

| | `completa` | `contenido` |
|---|---|---|
| campo enfocado | **fuera de pantalla, ausente del árbol** | visible |
| título y asa de la Hoja | **fuera** | título en **74,7 dp** |
| lo más alto que queda | **8,5 dp** (bajo la barra de estado, sobre el reloj) | 74,7 dp |
| última fila | — | `Listo` en **409,6 dp**, visible |

⇒ **la predicción se sostuvo.** *Si `contenido` también hubiera desbordado, el
mecanismo estaba mal y la cura habría sido la equivocada.*

**Cura: `flexShrink: 1`** — le da con qué ceder, no cambia nada sin teclado y
**no toca la decisión de `padding`**. ⚠️ **Sin ojo**: el borde a mirar en el
gate es que al encoger el `paddingBottom` no se coma la última fila (en la
Hoja de dirección, el `Guardar`).

## 🔴 LA BARRA CON CINCO TABS — el número que faltaba (pedido de D)

**D midió la geometría del valle y declaró honestamente lo que NO podía
afirmar: si el hombro muerde el ícono del vecino, porque eso depende del ancho
de la caja del ícono — que no tenía cómo medir.** Lo medí.

**Aparato (384 dp útiles), medido del árbol:**

| qué | medido |
|---|---|
| ancho por pestaña (4 tabs) | **92,1 dp** · centros en 54,0 · 146,0 · 238,0 · 330,1 |
| **caja del ícono** | **24,2 dp** (Hogar 24,2 · Explorar 23,8 · Cuenta 24,2 · activo 25,2) |
| media caja | **12,1 dp** |

**Con CINCO tabs, el borde interior del ícono vecino cae a `ancho/5 − 12,1`:**

| ancho | pestaña | borde del ícono vecino | valle (41,4) | **hombro (63,4)** |
|---|---|---|---|---|
| **360** | 72,0 | **59,9** | ✅ libre | 🔴 **MUERDE por 3,5 dp** |
| **384** (el del founder) | 76,8 | **64,7** | ✅ libre | ⚠️ **libra por 1,3 dp** |
| **412** | 82,4 | 70,3 | ✅ | ✅ margen 6,9 |
| **430** | 86,0 | 73,9 | ✅ | ✅ margen 10,5 |

⇒ **el valle nunca toca el ícono (sobran ≥23 dp): el problema es el HOMBRO.**
**Muerde en 360 dp y libra por 1,3 dp en el teléfono del founder** — un margen
que no se puede usar como respuesta. **Con 412+ entra cómodo.**

**No decido yo si entra la quinta tab: es de la mesa, y ahora tiene el
número.** *Lo que sí queda dicho: «entra» y «no entra» dependen del teléfono, y
en el más chico de la familia no entra.*

---

# LOTE 7 · `SelectorVentana` GANA `disposicion="tira"` (pedido de A, A-05)

**El número que lo obliga, medido en aparato:** el rótulo `Cuándo te llega`
arranca en **595,9 dp** y el pie fijo empieza en **~593** ⇒ ***no es que haya
que deslizar: NACE DEBAJO DEL PIE.***

**La tensión que A planteó y que había que resolver sin cambiar un defecto por
otro:** hoy la opción llena HABLA —su `motivo` se dibuja y entra al
`accessibilityLabel`— y en una tira angosta ese motivo no entra.

**La salida: el motivo no se achica ni se pliega — se MUDA.** Sale del chip y
vive en **una línea debajo de la tira, a ancho completo y sin truncar**.
***La Ley 23 pide que la negación DIGA POR QUÉ, no que quepa adentro del
chip*** — y a ancho completo dice **más** que hoy.

**Y la línea NO depende de un toque:** arranca mostrando el motivo del PRIMER
día lleno, así que el porqué está a la vista sin tocar nada — que es la
condición que el encabezado de la pieza ya exigía (*un motivo detrás de un tap
es un motivo que nadie lee*).

## ⚠️ EL DETALLE FINO QUE MANTIENE LA LEY 23 EN PIE

En `tira` el chip lleno **sí recibe el toque** —para poder contar SU motivo
cuando hay más de un día lleno— **pero JAMÁS llama a `onElegir`**. *El servidor
sigue sin poder recibir un día sin cupo; lo que cambia es que ahora se puede
preguntar por qué.* **Quien cablee `onElegir` en esa rama rompe la ley.**

## LO QUE LA PIEZA SIGUE SIN HACER, declarado

**No parte el día de la ventana horaria.** La referencia medida
(`referencia-laika-dia-y-hora.jpeg`) usa DOS ejes —tira de días arriba, rango
horario como tarjeta debajo— y **ése es el destino natural**. Pero hoy
`OpcionVentana` trae día y ventana en un solo `etiqueta`: partirlos **cambia el
contrato de datos y es decisión de la pantalla**. *Se declara en vez de
inventarle al modelo una separación que no tiene.*

**Y la superficie es de la pantalla (N21):** el bloque tiene rótulo ⇒ es un
grupo ⇒ va en carta. **La pieza no dibuja su carta.**

⚠️ `disposicion` nace con default `'apilada'`: **ningún consumidor existente
cambia.** Y **sin ojo**, como todo lo de esta vuelta.

---

# ⚠️ UN LÍMITE DEL PARALELO, DECLARADO POR A Y QUE ME TOCA IGUAL

**Los typechecks y el `verify:diseno` de cada pista corren contra el
`packages/ui` de SU worktree.** Los míos son ciertos sobre mi árbol; los de A y
D **no vieron** `PantallaConPie` con el inset derivado, `Hoja` con
`flexShrink`, ni esta enmienda.

⇒ **el typecheck que vale es el del ENSAMBLADO, y lo corre quien mergee.**
*Es la misma clase de dato que mordió cinco veces en S100b: cierto al
escribirse, falso al usarse.* **Ningún verde de pista se lee como verde del
conjunto.**

---

# LOTE 8 · `MarcaDeMapa` (pedido de D) Y EL SÉPTIMO MERGE ACOTADO

## 🔴 EL PEDIDO DE D NO SE CONSTRUYÓ COMO VINO — y la razón es letra firmada

D pidió **el glifo de la moto** para cerrar N14, y **se frenó bien**: no lo
resolvió con `despensa` ni `ubicacion` —*elegir la forma desde la palabra es la
lección de S99*—. **Pero su premisa era falsa: la moto YA EXISTE.**

**`DIRECCION_ARTE` §6ter está FIRMADA** (mesa, 15-ago-2026) y salió justo del
gate de ese glifo: ***el mapa no es interfaz, es MUNDO*** — la marca de mapa es
**masa y no trazo**, lleva anillo de separación y **no lleva huella** *porque
ahí la Ley 12 no gobierna*. Meterla en `Icono` la pondría bajo **R30** e
invitaría a montarla a 21 px junto a una huella: **exactamente lo que §6ter
existe para impedir.** `PinEnMapa.tsx:145` ya lo decía literal.

> **D frenó por la razón buena aunque su premisa era falsa.** Eso es mejor que
> al revés: si hubiera resuelto con `despensa`, hoy habría un glifo de interfaz
> haciendo de objeto del mundo.

## EL HUECO REAL, Y ERA MÍO

`PinEnMapa` **suelda dos responsabilidades**: *dónde va* (traduce x/y, anima el
viaje, `position:'absolute'`) y *qué se dibuja*. **El slot `marcadorVivo` solo
necesita la segunda** — ahí el `<Marker>` ya resolvió el dónde. Montarla adentro
de un `Marker` obliga a pasarle `x={0} y={0}`: **una posición inventada para
satisfacer una prop que ahí no significa nada.** *Eso no es usar la pieza: es
esquivarla.*

⇒ **nace `MarcaDeMapa`, el dibujo solo**, y **`PinEnMapa` la consume**: hay UN
dibujo, no dos que se puedan desincronizar (L-284).

**Y me frenó mi propio lint:** R17 rebotó la exportación —*«una pieza que nadie
puede mirar no se puede firmar»*—. Entró a la galería. **Es la R51 de D del
otro lado: el lint no vigila lo que uno hace mal, vigila lo que uno no puede
ver que está haciendo.**

## ✅ EL SÉPTIMO MERGE ACOTADO DE `packages/ui` A MAIN

**Pedido por A, que estaba bloqueada:** su montaje no podía commitear porque el
gate corre `tsc` y `disposicion` no existía en su árbol. *A verificó trayendo mi
archivo temporalmente y lo revirtió para no dejar una copia divergente — hizo
bien, y por eso el desbloqueo era el merge y no que ella copiara.*

**Procedimiento del acta §7:** solo `packages/ui` · origen declarado
(`origin/pista-b @ 2d5cdf56`) · **verificado EN MAIN antes de commitear** ·
avisado a las cuatro.

🔴 **EL FRENO QUE SE CORRIÓ ANTES DE TOCAR NADA, y es la parte que vale:** se
midió que **`origin/main` es ANCESTRO de `pista-b`** y que **no hay ningún
commit de `packages/ui` en main que `pista-b` no tenga** ⇒ **el merge no podía
revertir trabajo de otra pista.** *Un `checkout -- packages/ui` desde una rama
atrasada es justo el modo de borrar el trabajo de un vecino sin que nadie lo
note.* **Se midió en vez de suponerse.**

**Verificado EN MAIN:** `verify:diseno` **VERDE 46** · los **4 typechecks exit
0**. `origin/main` = `786073b7`.

⚠️ **Y lo que el merge NO cambia: nada de esto tiene ojo.** Son cambios
visuales que no existen hasta un publish, y **publicar pide firma del founder.**

