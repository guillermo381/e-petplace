# S113-C · 1.2 — el alta que propone, la raza que se cuenta, y la despedida que no puede escribirse

**Rama `pista/s113-c-1.2b`.** Base `main`, con `origin/main @ f7405832` y B `7a6722e4` adentro.
**Cuenta de las mediciones:** `guillo381+8@gmail.com`, en web con sesión real. **Cero aparato.**

---

## ① C9 · EL ALTA ENTERA, RECORRIDA

`foto → sugerencia → toque → raza guardada`, y la mascota **nace en el Hogar**.

**Y la mitad que más dice:** la única foto a mano era **un carnet de vacunas**, no un
perro. El modelo contestó *«No pude ver a tu mascota en la foto»* y **dejó el campo
vacío**. *No fabricó una raza plausible* — que es exactamente lo que L-139 existe para
prohibir, y acá se cumplió sin que nadie lo forzara.

| | |
|---|---|
| sin tocar la sugerencia | campo de raza **vacío** ✓ |
| tras tocar «Beagle» | **«Beagle»**, y así queda guardada ✓ |

⚠️ **Lo que NO se pudo ejercer:** que la sugerencia **acierte** una raza. Hace falta una
foto de un animal, y no la tengo. *Se declara en vez de darse por verde.*

---

## ② C10 · LA FICHA DE LA RAZA

Va **pegada a la identidad**, que es de lo que habla, pero **debajo de la franja de
seguridad**: una alerta de alergia o medicación tiene que verse sin scroll, y una
tarjeta de lectura por encima la empuja. *La identidad se lee; la seguridad se atiende.*

### 🔴 El puente que faltaba, y el atajo que no se tomó

`mascotas.raza` guarda el **NOMBRE** en texto libre («Beagle»); `razas_contenido` se
indexa por **CÓDIGO** («beagle»). Medido contra la base:

| puente candidato | casa |
|---|---|
| `cat_razas.slug` = `raza_codigo` | **13 de 13** ✓ |
| `cat_razas.nombre_norm` = `raza_codigo` | **5 de 13** |

*Normalizar el texto a mano acertaría a veces — que es peor que fallar siempre, porque
nadie sabría cuándo.* Se pasa por `obtenerRazasDeEspecie`, que ya devuelve el par
`slug`+`nombre`: **el puente existía y no hubo que pedir nada**.

### Me corregí sobre `revisado`

Mi primera lectura fue que el contrato no exponía `activo` ni `revisado` y que **faltaban**.
Estaba equivocado: **A lo cerró en la RLS** — *la policy de `razas_contenido` sólo deja
salir las filas con `activo`, así que el wrapper no puede leer un borrador aunque se lo
pida*. **El filtro no falta: es inexpresable**, que es más fuerte que un `.eq` que alguien
puede olvidar.

| control | resultado |
|---|---|
| **positivo** · Beagle (de las 13 publicadas) | **dibuja** ✓ |
| **negativo** · Akita Inu (no publicada) | **no dibuja** ✓ |

---

## ③ C11 · LA DESPEDIDA — 🔴 FRENADA POR UN DEFECTO DE `registrar_fin_de_vida`

**El menú de edición no existía**: el lápiz iba derecho a la foto y la raza se editaba
desde otro lado. Ahora los agrupa, y la despedida va **sola, tras un separador y sin
color de alarma** — *el memorial es sereno, y pintarla de rojo la trata como un borrado.*

**Lo que funciona, medido:** el menú ofrece las tres cosas · la pantalla abre con la
fecha de hoy · el primer toque pide confirmar con el nombre · el segundo llama al motor.

### 🔴 Lo que no: el motor rebota

```
RPC 400 · {"code":"23502","details":"Failing row contains (…, fin_vida, identidad,
2026-09-05 …, {}, null, f, null, null, null, null, ← country_code, …)"}
```

**`eventos_mascota.country_code` es NOT NULL y el INSERT lo deja en `null`.**
⇒ **Ninguna mascota puede ser despedida hoy, por ninguna puerta.**

### Cómo se supo, que es la parte que importa

1. La pantalla se quedaba **muda**, con el botón en «Toca otra vez».
2. **Tres corridas lo leyeron como memorial** — porque el arnés medía *después* del
   `back()`, o sea **el Hogar**, que no tiene carnet ni lápiz. *Un discriminador que se
   cumple en la pantalla equivocada no discrimina nada.* Con la ficha como sujeto, el
   rojo salió honesto.
3. El número salió leyendo la **respuesta cruda del servidor**: el wrapper la traduce a
   `desconocido`, y desde la pantalla **no se distingue «error que no mapeo» de «forma
   inesperada»**.

### Y encontré mi propia clase, otra vez

Un **`.then()` sin `.catch()`** deja la pantalla congelada y muda: la promesa rechazaba
y nadie la escuchaba. Es la misma familia que el botón del carnet del 1.1.2 — *un camino
de fallo que no habla es indistinguible de un toque que no llegó.* Curado.

---

## ④ MI PROPIO GATE ME CAZÓ, Y TENÍA RAZÓN

`verify:pide-en-memorial` marcó «Cambiar la foto» y «Cambiar la raza» **aunque el lápiz
que abre el menú ya estaba bajo el guard** — o sea que en memorial era **inalcanzable**.
*Un texto inalcanzable hoy es alcanzable mañana, en cuanto alguien le abra otro camino, y
nadie se va a acordar de este guard.* La Hoja entera cuelga de `!esMemorial`.

**Y el gate se curó a sí mismo:** con el JSX mal cerrado **siguió imprimiendo su tabla y
su veredicto** — el parser de TS tolera errores y devuelve un árbol **parcial**, así que
los guards que no alcanzó a ver salieron como «fuera del guard». *Un instrumento que mide
a medias no da un rojo: da el rojo de otra cosa, y manda a curar lo que no está roto.*
Ahora **sale con 2 y lo dice**. Rojo probado, verde después.

---

## ⑤ EL CHOQUE CON D EN EL CARNET, RESUELTO A FAVOR DE D

Al mergear `main` chocó `carnet.tsx`. **D resolvió el mismo bloqueante del founder** en
`05172ae9` y **su regla es más ancha**: dudosa = sin fecha **o** sin nombre **o** fecha
parcial. La probó con el carnet real y midió lo que yo no vi: *con la ley vieja ese
carnet perdía DOS filas en silencio, el 25 % de sus aplicaciones.*

**Tomé la suya entera.** Dos implementaciones del mismo guard es exactamente el defecto
que diagnostiqué en el 1.1.2 — *dos cuentas sobre lo mismo terminan discrepando*.

⚠️ **Consecuencia declarada:** `FilaConfirmacionVacuna` (pieza de B) y mi
`confirmable.ts` quedan **sin consumidor**. No los borro: elegir cuál vive es de la mesa.
**Y `verify:confirmable` sigue en verde midiendo una regla que ya no rige en ninguna
pantalla** — *un gate verde sobre código muerto se lee igual que uno que protege algo*.

---

## ⑥ PIDO A A, POR NOMBRE

1. 🔴 **`registrar_fin_de_vida`: el INSERT del evento no completa `country_code`**
   (`23502`, detalle arriba). **Bloquea C11 entero.**
2. **Ocho mascotas `PruebaC…` en la familia del founder**, nacidas de las corridas del
   alta. **No las puedo borrar** —no hay puerta— **y despedirlas es justo lo que no
   funciona**. Pido su borrado.

## ⑦ ROJO HEREDADO, DECLARADO

`verify:razon-muda` da **140** contra baseline **139**. **Medido en `origin/main`: también
140** ⇒ **no lo introduje yo**, y la regla dice que el baseline se sube a mano y jamás en
el commit que lo trae. Se declara y no se toca.

---

## ⑧ GATES

`nexo` ✓ · `pide-en-memorial` **18 frases, 0 fuera del guard** ✓ · `hooks` ✓ ·
`hooks-bajo-return` ✓ · `ref-antes-de-uso` ✓ · `gates-existen` ✓ · `puerta-unica` ✓ ·
`coach` ✓ · `glifos` ✓ · `diseno` **VERDE, 61 reglas** ✓ · `tipos:rutas` ✓ ·
`tsc apps/cliente` **0** ✓ · `razon-muda` **rojo heredado** (§⑦).

**Cero aparato. Ningún OTA.**

---
---

# RECONCILIACIÓN · una regla, una pantalla (orden de mesa)

## ① LA PIEZA DE B SE MONTA

Reemplaza a `FichaVacuna` en `carnet.tsx`. Trae lo que la otra no podía: **decir
por qué una fila está frenada, pegado al campo que falta**, y **pedir el nombre
cuando no se leyó** — que es literalmente la pregunta que D dejó escrita en ese
archivo: *«qué se le muestra a la familia cuando el nombre no se lee es diseño
tuyo»*.

## ② ☠️ `esDudosa` MURIÓ, Y NO SE PERDIÓ NADA

Recalculaba en la pantalla lo que la edge ya decide (`dudosa`) y lo mezclaba con
el hecho de la columna. **Las razones de D siguen rigiendo enteras** —nombre
nulo, fecha parcial—: lo que sobraba era que vivieran en un tercer lugar.

| antes | ahora |
|---|---|
| la edge deriva `dudosa` | **la fuente** |
| `esDudosa` en la pantalla | ☠️ |
| `confirmable.ts` con regla propia | **traduce**, no recalcula |

## ③ 🔴 ME CORRIJO SOBRE LA ORDEN: NO ES «NADA HOY»

La orden decía que el cliente agrega sólo lo que el servidor no puede saber, y
que hoy eso es **nada**. **El carnet del founder dice que no**, y con nombre:

> **Fila 5 · «Nobivac KC»** — la edge la leyó **bien**: `dudosa: null`, sin
> invento. Su fecha es **`--06-14`**: un día y un mes **sin año**, que es todo
> lo que el papel dice. *Para la extracción la fila es correcta, y lo es.* Pero
> `fecha_aplicada` es `date` y un `--MM-DD` no entra.

**No es una duda de lectura: es un hecho de la columna**, y la edge no tiene por
qué conocerlo. Queda **una línea**, con su porqué escrito, y **se borra sola** el
día que la columna admita precisión.

## ④ EL PIE GANA SU SEGUNDA CUENTA

Con la pieza montada **confirmar es un acto**; sin exigirlo, `tocada` sería
decorativo — *un botón que se dibuja y no cambia nada enseña a ignorarlo*. Dos
razones, **dos voces, en orden**: primero completar, después revisar. *«Faltan 3
por revisar» sobre una fila sin fecha manda a hacer lo segundo antes que lo
primero.* **Las voces ya existían del 1.0**; no inventé ninguna.

## ⑤ EL GATE SE PRUEBA CON LAS SIETE FILAS REALES

`verify:confirmable` dejó de correr sobre casos que yo inventé —*un fixture
escrito por el mismo que escribió la regla comparte sus supuestos*— y corre
sobre **la respuesta real de la edge al carnet del founder**, más cuatro bordes.
**11 casos, verde, con su control de rojo.**

## ⑥ 🔴 EL CARNET NO SE PUEDE LEER HOY EN `main` — Y NO ES DE C

Al correr el recorrido con el carnet real, la lectura rebota: *«La respuesta del
servidor no tiene la forma esperada»*. **La edge responde 200 con datos
correctos** y **el wrapper la rechaza**. El vocabulario de `evidencia` está
partido en **tres**:

| dónde | acepta / emite |
|---|---|
| edge · su validador (`extract-vacuna/index.ts:146`) | `'sticker'` |
| edge · su **prompt** (`:475`) | `'sticker_con_fecha'` |
| wrapper (`vacunas.ts:184`) | `'sticker_con_fecha'` |

**6 de las 7 filas del carnet del founder traen `evidencia: "sticker"`** ⇒
`datos_inconsistentes`, y **ninguna lectura entra**. *El comentario del wrapper
dice «Espejo EXACTO del validador de la edge. Que las dos puntas exijan lo mismo
es lo que hace que "cumple el contrato" signifique una sola cosa» — y son tres
puntas con dos vocabularios.*

⚠️ **Consecuencia para esta reconciliación:** está **construida y no ejercida en
pantalla**. La regla se probó donde sí se puede (§⑤, con las filas reales); lo
que falta es el recorrido, y **lo destraba A**.

## ⑦ PIDO A A, POR NOMBRE — DOS BLOQUEANTES

1. 🔴 **`evidencia`: el wrapper acepta `'sticker_con_fecha'`, la edge emite
   `'sticker'`** (§⑥). **Ningún carnet se puede leer.**
2. 🔴 **`registrar_fin_de_vida` no completa `country_code`** (`23502`).
   **Ninguna mascota se puede despedir** ⇒ **C11 no se puede correr de punta a
   punta**, y queda esperando esa cura, como ordenó la mesa.

## ⑧ GATES

`confirmable` **11 casos** ✓ · `nexo` ✓ · `pide-en-memorial` ✓ · `hooks` ✓ ·
`hooks-bajo-return` ✓ · `ref-antes-de-uso` ✓ · `gates-existen` ✓ ·
`puerta-unica` ✓ · `coach` ✓ · `glifos` ✓ · `diseno` **VERDE, 61 reglas** ✓ ·
`tsc apps/cliente` **0** ✓ · `razon-muda` **140, heredado** (mismo número en
`origin/main`).

**Cero aparato. Ningún OTA.**

---
---

# CIERRE DEL 1.2 · C11 corrido, el carnet ejercido (sobre `main @ 25aabd5c`)

Con los dos bloqueantes curados por A, **lo que estaba construido y no ejercido
se ejerció** — y caminarlo destapó dos defectos que ningún gate podía ver.

## ① 🟢 C11 DE PUNTA A PUNTA

```
RPC 200 · {"ok":true,"estado_vida":"fallecida","ya_estaba":false,
           "evento_id":"e490c764-…","mascota_id":"c2505009-…"}
```

Sobre **una mascota de prueba nacida en la misma corrida** (alta completa: foto
→ sugerencia → toque → «Beagle»), no sobre una sembrada por atrás: *un fixture
que entra por la puerta de atrás no prueba la puerta.*

| | antes | después |
|---|---|---|
| ¿le pide algo? | **sí**, está viva | **no** ✓ |
| menú de edición | ofrece las tres | **no se dibuja** ✓ |
| ficha de raza (Beagle) | **dibuja** ✓ | **se apaga** ✓ |
| `mascotas.estado_vida` | `activa` | **`fallecida`** ✓ *(medido en la base)* |

`verify:pide-en-memorial` **en cero**, con su control positivo de 10 frases.

## ② 🔴 EL LÁPIZ SE DIBUJABA EN MEMORIAL Y NO HACÍA NADA

La Hoja del menú ya colgaba del guard; **el botón que la abre, no**. *Un control
que se ve, se toca y no hace nada es peor que uno ausente: el ausente no
promete.*

**Ningún gate podía verlo**: `verify:pide-en-memorial` mide **textos que piden
algo**, y «Editar» no pide nada. *Apareció al despedir una mascota de verdad —
no leyendo, no midiendo: caminando.*

## ③ ☠️ MI PUENTE POR EL CATÁLOGO SE FUE ENTERO

A se llevó los tres pasos al servidor (`resolver_ficha_de_raza`), **y su razón es
la misma ley que hoy mató a `esDudosa`**: repartir la regla entre cliente y
servidor es la segunda definición de «igual». Y hay una que yo no había visto:
**el tercer paso ESCRIBE** —registra lo que no casó— así que mi mitad habría
dejado eso sin registrar. *Un viaje, una verdad.*

## ④ 🔴 EL CARNET EJERCIDO — Y LA RECONCILIACIÓN TENÍA UN HUECO

Con la edge y el wrapper hablando igual, **el carnet real del founder LEE**: 7
filas, botón apagado con su razón a la vista. **Pero ninguna fila decía qué le
faltaba.**

La causa: **la pieza cuelga su aviso DEL CAMPO VACÍO** —lo dibuja con borde de
atención y el texto debajo— y mi `camposDe` **filtraba todos los vacíos**. *Mi
filtro y la pieza se contradecían, y el resultado era el mismo síntoma del
bloqueante del founder: una cuenta que nadie podía ver.*

Ahora pasa **uno**: la fecha cuando es lo que falta. Los otros cuatro siguen
fuera — con los cinco, una fila sin fecha mostraba **tres casillas de fecha**.

```
① filas que DICEN qué les falta: 3
③ el pie dice que faltan:        3
⇒ ¿una sola cuenta?: sí ✓
② «Sumar 7 vacunas a su historia» · apagado=true · razón a la vista ✓
```

Y en pantalla se lee la voz: *«Nombre de la vacuna · No pude leer cuál es;
escríbela»*.

## ⑤ MI ARNÉS MINTIÓ DOS VECES, LAS DOS POR MEDIR EN EL ESTADO EQUIVOCADO

1. Contaba **C10 después de despedir**, donde *no dibujar es lo correcto* — un
   control positivo medido en el estado equivocado **no falla: miente**.
2. Buscaba las voces con **palabras que yo suponía** («Falta la fecha») en vez
   de leerlas del diccionario («No pude leer la fecha; escríbela…») ⇒ contó 1
   donde había 3 y **cantó «dos cuentas» sobre un instrumento roto**.

*Las dos curadas, y las dos son la misma familia que el memorial medido en el
Hogar: el discriminador se cumple en otro sitio y el verde no dice nada.*

## ⑥ GATES

`confirmable` **11 casos** ✓ · `nexo` ✓ · `pide-en-memorial` ✓ · `hooks` ✓ ·
`hooks-bajo-return` ✓ · `ref-antes-de-uso` ✓ · `gates-existen` ✓ ·
`puerta-unica` ✓ · `coach` ✓ · `glifos` ✓ · `diseno` **VERDE, 61 reglas** ✓ ·
`tipos:rutas` ✓ · `tsc apps/cliente` **0** ✓ · `razon-muda` **140, heredado**
(mismo número en `origin/main` — no lo introduje yo).

## ⑦ LO QUE DEJO DICHO

**Dos mascotas de prueba quedan en memorial** en la familia del founder
(`PruebaC12896`, `PruebaC82896`): son las dos corridas de C11, y **despedirlas
era la prueba**. No se borran desde acá — si molestan, es de A.

**Cero aparato. Ningún OTA.**
