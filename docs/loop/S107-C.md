# S107-C · PARTE DE PISTA — las superficies de la guardería

> **Estado al cerrar: 🔴 FRENADA ANTES DE LA PRIMERA LÍNEA. Cero código escrito, cero pantalla montada — a propósito.**
> El freno no es de la pista: **la letra que autoriza este trabajo está frenada en su propia puerta**, y su ficha declara como disparo *«antes de cualquier construcción de guardería»*.
> Lo que sigue es la medición que lo prueba, y lo que la medición encontró de paso.

---

## ① EL FRENO, EN UNA LÍNEA

**`LETRA_GUARDERIA.md` dice, en su primer renglón: 🔴 «LETRA FRENADA — NO RIGE, NO SE CONSTRUYE, NO SE PUBLICA».**

Frenada el **25-ago-2026 por el memo del abogado**, horas después de depositarse. Dos fichas 🔴 ALTA la sostienen:

| ficha | qué cae | veredicto |
|---|---|---|
| **D-918** | §3 — la exención total de responsabilidad de e-PetPlace | **nula de pleno derecho frente al consumidor** |
| **D-919** | §6 — el animal a un refugio pasados tres días | **riesgo penal** |

**Y la letra se frena ENTERA, no solo esas dos secciones** — está escrito en su puerta y en la ficha: *«§3 sostiene el reparto de responsabilidad del que cuelga todo el servicio, y dejar el resto vigente invitaría a construir transporte y documentación sobre una línea que no existe.»*

**El disparo de D-918 es literal: «antes de cualquier construcción de guardería». Dueño: mesa + abogado.** No es mío y no se salta.

---

## ② LAS TRES DEPENDENCIAS DEL PROMPT NO EXISTEN — medido, no supuesto

El prompt manda leer cuatro fuentes antes de construir. **Tres no existen en ningún commit de ninguna rama**, y la cuarta está frenada.

| lo que el prompt manda leer | estado medido | cómo se midió |
|---|---|---|
| **BRIEF S107** | **no existe** | `git log --all --diff-filter=A -- "*BRIEF*S107*"` → vacío |
| **`CRITERIO_LEGAL_GUARDERIA.md` §4** | **no existe** | `git log --all --diff-filter=A -- "*CRITERIO_LEGAL*"` → vacío. **No es que no esté en `main`: no fue creado nunca, en ninguna rama.** |
| **los contratos publicados de A** | **no hay ninguno** | worktree `e-petplace-s107-a`: árbol limpio, `HEAD == 0f53f24e == main`. **A no commiteó una línea.** |
| **las piezas de B** | **no hay rama `s107-b`** | `git branch -a` — existe `s107-a` y `s107-cert`; **`s107-b` no** |
| `LETRA_GUARDERIA.md` | **existe y está FRENADA** | su propia puerta |

⚠️ **`s107-cert` sí existe y está trabajando** — pero es otra pista y otro frente (certificación de pagos: D-946 a D-953, reverso, conciliador). **No aporta nada a guardería, y no se la confunde.**

**El prompt describe un mundo donde A y B ya entregaron. Ese mundo todavía no ocurrió.** *Construir contra contratos que no existe es inventarlos — y un contrato inventado por el consumidor es exactamente el modo de falla que el método de tres pistas existe para evitar.*

---

## ③ LO QUE EL FRENO DESTAPÓ — y es lo que hace que este parte valga más que un «no»

Aproveché el freno para medir el terreno, porque **esa medición sobrevive a cualquier reescritura que la mesa haga de §3 y §6**: la línea de responsabilidad cambia el contrato legal, no cambia lo que la base de datos puede hacer hoy.

### 🔴 3.1 · La letra afirma que el motor del paseo alcanza. **Medido: no alcanza, y en tres puntos estructurales.**

`LETRA_GUARDERIA` §1 dice, en negrita: *«hereda su motor: transporte, GPS, fotos del durante y chips de comportamiento. **No se construye nada nuevo para eso.**»*

**Eso es cierto del transporte, el GPS, las fotos y los chips.** No es cierto de la forma del servicio, y los recorridos del prompt lo piden en su primera frase.

| lo que el recorrido pide | lo que el motor tiene hoy | medición |
|---|---|---|
| *«a la mañana veo la lista de hoy: **seis animales**»* — una jornada con N animales | **una cita = UN animal.** `evento_cita_servicio.mascota_id` es una sola columna | columnas de la tabla, leídas de `information_schema` |
| *«recibo **8 animales por día**»* — un techo DIARIO | el techo es **por slot**: `prestador_horarios.max_citas_por_slot`. **No hay ninguna columna de cupo por día** | columnas de `prestador_horarios` y de `prestador_servicios` |
| *«franja de recogida 7:00–9:00 y de devolución 16:30–18:30»* — **DOS franjas por animal por día** | la cita tiene **una `hora`** y una `duracion_minutos` | columnas de `evento_cita_servicio` |
| *«salgo a buscar; en cada puerta…»* — una **ruta** de recogida | **no existe concepto de ruta ni de recogida múltiple en todo el repo** | grep sobre `supabase/migrations/` y `packages/api/src/`: los 2 hits son prosa sobre *la hora* de recogida del paseo (el gate de no-show), **verificados uno por uno antes de afirmar** |

> **La conclusión que le importa a la mesa: la guardería no es un paseo largo — es una jornada con muchos animales y dos franjas, y eso es forma de motor, no configuración.** *La frase «no se construye nada nuevo» hace que este oficio parezca barato, y el número dice otra cosa.* **Esto se declara ahora, antes de que la reescritura legal se presupueste sobre una premisa medida como falsa.**

### 🟡 3.2 · Hay cinco filas vivas de guardería y hotel en `tipos_servicio`, y nadie las declaró

Medidas contra la base viva:

| código | activo | reservable | `especies_elegibles` | ofertas | citas |
|---|---|---|---|---|---|
| `guarderia_dia` | ✅ | ✅ | **NULL** | 0 | 0 |
| `guarderia_mensual` | ✅ | ✅ | **NULL** | 0 | 0 |
| `hotel` · `hotel_dia` · `hotel_noche` | ✅ | ✅ | **NULL** | 0 | 0 |

**Cero ofertas y cero citas ⇒ no hay daño hoy, y no hay motor: son filas del legado, no trabajo hecho.**

🔴 **Pero `especies_elegibles` en NULL es la parte que importa:** §1 firma *«solo perros y gatos»*, y **hoy esas filas están abiertas a toda especie**. La nota de depósito de A ya lo había anticipado con precisión — *«quien construya esto llena una columna; no escribe un `if`»* — **y la columna sigue vacía.** Es latente, no abierto (ninguna superficie ofrece hoy guardería: `explorar` la lista como *«próximamente»*), pero **la primera oferta que alguien cree contra esas filas estrena el servicio sin el guard de especie que la letra firmó.**

⚠️ **Y `hotel_noche` está `reservable = true`** mientras §5 firma que *«la noche NO es guardería: es hotel, y es otro servicio con su propia letra»* — letra que no existe.

### 🔴 3.3 · D-920 toca mi territorio y no está resuelta: **§5 firma 10 %, lo que rige dice 15 %**

`LETRA_GUARDERIA` §5 firma comisión **10 %**. Los T&C **publicados** y `fee_configs` **vivo** dicen **15 %** para servicios, y **coinciden entre sí**. La ficha es 🔴 ALTA y es firma del founder, no cura de pista.

**Por qué es mío:** el recorrido del dueño dice *«veo el precio por día»* y el del prestador *«el precio del día»*. **Una pantalla de precio no se puede construir sobre dos números firmados distintos** — cualquiera de los dos que pinte queda contradiciendo una fuente firmada.

### 🟢 3.4 · Una que sale bien: la mensualidad NO colisiona con el cobro recurrente

Firma ④ del prompt prohíbe prometer cobro recurrente. **Medido: `app_config` no tiene la clave `recurrente_vivo` ⇒ el motor recurrente de S103 sigue INERTE**, tal como nació por diseño. **No hay riesgo de que una superficie de mensualidad se enganche sin querer a un cobro automático vivo.** *Es la única de las cuatro mediciones que no trae trabajo.*

---

## ④ EL PERÍMETRO SE RESPETÓ ENTERO

- **Cero pantalla, FAQ, tooltip o empty-state del protocolo de mora.** No se escribió ninguno — y ahora, además, hay razón de fondo: **D-919 dice que su base tiene riesgo penal.**
- **Cero texto legal en pantalla, ni un placeholder.** No hay documentos versionados de A contra los cuales montar la aceptación, así que la compuerta no existe todavía; **no se simuló.**
- **Cero migración, cero DDL, cero escritura.** Todo lo de arriba salió de `SELECT` sobre `information_schema` y catálogos, y de `git log`.

---

## ⑤ LO QUE ESPERA FIRMA — en orden, con dueño

1. 🔴 **La reescritura de §3 y §6** — *mesa + abogado*. **Bloquea todo lo demás.** Sin línea de responsabilidad no hay acta, y sin acta no hay recorrido del prestador.
2. 🔴 **D-920, el 10 % contra el 15 %** — *founder*. Bloquea toda pantalla de precio, de las dos puntas.
3. 🔴 **La decisión de forma que §1 no toma** — *mesa*: si la guardería es **N citas de un animal** (barato, y la lista de la jornada se compone leyendo) o **una jornada con N animales** (motor nuevo, y es lo que los recorridos describen). **Es la decisión más cara del oficio y hoy no está tomada por nadie.**
4. 🟡 **`CRITERIO_LEGAL_GUARDERIA.md` §4** — el prompt lo cita como fuente del contenido del acta y de las firmas. **Alguien tiene que escribirlo**; hasta entonces el acta no tiene contenido definido.
5. 🟡 **Las cinco filas del legado** — llenar `especies_elegibles` de las dos de guardería, y decidir qué pasa con las tres de hotel. *Barato hoy, invisible mañana.*

---

## ⑥ LO QUE ESTA PISTA ENTREGA

**Este parte, y nada más — porque construir habría sido el error.** Cero commits de código, cero migraciones, worktree `pista/s107-c` con este único archivo.

> *El freno llegó antes de construir, que era exactamente el objetivo de mandar la letra al abogado primero* (D-918). **Este parte es la segunda mitad de ese mismo mecanismo: el freno sirve si la pista siguiente lo encuentra escrito, con su medición al lado, en vez de volver a descubrirlo.**

**Lo que NO se hizo y se declara:** no se recorrió el motor de paseo entero para presupuestar la variante «N citas de un animal» — **no se presupuesta un camino que la mesa todavía no eligió** (③.5). El insumo para elegirlo está en §3.1.

---
---

# S107-C · TANDA 2 — los dos encargos de A (28-ago-2026)

## ① `D-645` · **HECHA Y VERDE** — la pieza generalizada montada, y la vieja muerta

**El traspaso decía: migrar `apps/prestador` a la pieza de `packages/ui` y RETIRAR la vieja.** Las dos mitades están hechas — *retirar la vieja era el trabajo, no la limpieza de después.*

**Lo que se hizo:**
- ☠️ **`apps/prestador/src/components/aceptacion-terminos.tsx` BORRADO** (`git rm`). Cero referencias vivas.
- 🆕 **`apps/prestador/src/lib/aceptacion-prestador.ts`** — la mitad que **nunca fue forma**: el estado, su gate, `urlTycProfesional()` y la voz. 🔴 **No es un rezago:** `verificar-correo.tsx` consume `urlTycProfesional` para anclar la evidencia del arbitraje (§38.10), y **borrar el archivo entero se habría llevado puesta una función que no era del componente.**
- **Tres consumidores migrados:** `registro.tsx` · `invitacion.tsx` (montan `AceptacionDeDocumentos` con `registro="oficio"`) · `verificar-correo.tsx` (sólo cambia de dónde importa).
- **Clave nueva `aceptacion.opcionalRotulo`** (es/en) — la pieza rotula la sección opcional; sin ese rótulo *un opcional mezclado entre obligatorios se marca por inercia*. **Va al lote de strings del founder.**
- **El estado NO cambió de forma** (`{tyc, privacidad, arbitraje}`): se traduce a las claves de la pieza en el borde (`marcadasDe` / `aplicarCambio`). **Así el registro del arbitraje por el motor quedó intacto** — radio de impacto mínimo, que es lo que una migración debe tener.

**Verde:** typecheck prestador **limpio** · typecheck cliente **limpio** · `verify:diseno` **VERDE, 60 reglas**.

### 🔴 Lo que la migración DEGRADA, declarado en el código y acá — pedido a B

`AceptacionDeDocumentos` arma el `accessibilityLabel` del checkbox con **`doc.texto` solo** (`Casilla` lo recibe como `accessibilityLabel`). ⇒ el lector de pantalla anuncia **«Acepto los»** donde la pieza vieja anunciaba **«Acepto los Términos y Condiciones Pet Professional»**.

**No se pierde información** —el enlace queda expuesto como elemento propio, con `accessibilityRole="link"` y su etiqueta— **pero para quien sólo escucha, una casilla de consentimiento legal etiquetada «Acepto los» es peor.**

> **PEDIDO A B, autocontenido:** agregar **`etiquetaAccesible?: string`** a `DocumentoAceptable`, y en `Linea` usar `doc.etiquetaAccesible ?? doc.texto`. Es la línea que cierra la paridad.
> ⚠️ **Las claves `tycAccesible` · `privAccesible` · `arbitrajeAccesible` NO se borraron y están sin consumidor A PROPÓSITO: esperan esa prop.** *Se declara para que nadie las lea como basura y las barra — el día que la prop llegue, se cablean y listo.*

---

## ② 🔴 LA ENTRADA A `/gallery` — **NO SE RETIRÓ, Y EL FRENO ES CON MEDICIÓN**

**La ejecuté, medí el resultado, y la REVERTÍ.** El árbol quedó como estaba y `verify:diseno` volvió a **VERDE**. Dos hallazgos, y cualquiera de los dos alcanza para frenar:

### 🔴 (a) Hay una regla FIRMADA que lo prohíbe, y la ejecución la puso en rojo

`R18` (`D-580`, **enmienda founder S82**) vigila esa entrada. Con el retiro hecho, `verify:diseno` dio **1 fallo** con este literal:

> *«LA ENTRADA A /gallery DESAPARECIÓ. D-580 (enmienda founder S82): queda VISIBLE hasta el gate de producción; **su retiro exige FIRMA EXPLÍCITA, y con la firma se borra esta regla en el mismo acto**.»*

**El pedido de B no es esa firma** — es un pedido de pista. Y la regla **se autotermina por diseño**: quien ejecute el retiro firmado la borra en el mismo acto. **`scripts/verify-diseno.mjs` es territorio de B/A, no mío** ⇒ no la toco.

### 🔴 (b) **Son DOS entradas, no una — y la segunda es la que el founder usa para firmar**

El pedido midió `apps/cliente/.../cuenta/index.tsx:318`. **R18 vigila DOS casas**, y la segunda existe:

| casa | fila | qué es |
|---|---|---|
| cliente | `cuenta/index.tsx:318` | **«Galería de tokens · herramienta de sesión»** — la que B midió |
| **prestador** | `(tabs)/cuenta/index.tsx:847` | 🔴 **«Láminas de gate · para firmar — lo que espera tu ojo esta sesión»** |

> ### La del prestador **no es la misma cosa con otro nombre: es el camino del founder a lo que tiene que firmar** — y B acaba de publicar **diez piezas** que esperan exactamente ese ojo. Retirarla esta sesión sería cerrar la puerta del gate el día que más se usa.

⚠️ **Y corrige un dato que yo tenía por cierto:** mi propia nota decía *«el prestador no tiene entrada»*. **Es falsa, medida.** *La tenía de un reporte viejo y no del objeto* — L-166 cobrada contra mí mismo.

**Retirar sólo la del cliente no cumple el propósito del pedido** (*«mientras esa entrada exista, la sala retirada sigue teniendo puerta»*): la puerta que importa queda abierta, y encima el gate queda rojo.

### Lo que hace falta para cerrarlo — y es del founder, no de una pista

**Una firma que diga las dos cosas:** ① si el retiro alcanza a **una casa o a las dos**, y ② que **`R18` se borra en el mismo acto** (lo pide su propio texto). Con eso, el retiro es una línea por casa y B baja la regla.

**Voto de esta pista:** retirar **sólo la del cliente** (es la herramienta de sesión) y **conservar la del prestador hasta el gate de producción**, porque es el único camino del founder a las láminas. *Pero es voto, no decisión — y sin la firma no se toca una regla firmada.*

---
---

# S107-C · TANDA 3 — la config del prestador (28-ago-2026)

**Desbloqueo ① consumido:** A publicó `guarderia-config` y con eso arrancó la primera superficie del oficio. **Y publicó de más:** `obtenerCupoGuarderia` también estaba, que era mi desbloqueo ②.

## LO CONSTRUIDO — `apps/prestador/src/app/guarderia/taller.tsx`

Cumple la primera mitad del recorrido del prestador: **la capacidad por día y las dos ventanas**, guardadas de verdad contra el motor de A.

- **Capacidad** por `StepperCantidad` (1–60), guardada con `definirEspacioGuarderia` bajo un **nombre estable** (`Principal`) — que es la clave del upsert del motor: **re-guardar edita, jamás duplica.**
- **Las dos ventanas** con la grilla de medias horas de la casa, y el **espejo vivo**: monta `FichaFranja` de B respondiendo al borrador (estándar §15b). **La misma pieza que el dueño va a ver en el perfil del lugar.**
- 🔴 **La puerta no ofrece lo que va a rechazar** (Ley 23): al elegir el «hasta», la grilla **sólo muestra horas posteriores al «desde»**. El motor igual lo rebota (`franja_invertida`), pero la pantalla no lo propone.
- **Orden de guardado: recogida → devolución.** El motor valida contra las filas VIVAS que la recogida termine antes de que empiece la devolución; al revés se choca contra la franja vieja. *El rebote sería correcto; el orden evita el choque falso.*
- **El fallo a medias se dice:** si la devolución rebota, la capacidad y la recogida **ya se guardaron** — y la voz lo declara en vez de sugerir que no pasó nada. *Un «no se pudo guardar» sobre algo guardado a medias manda al prestador a re-hacer lo que ya está.*
- **El día sobrevendido se muestra**, con las dos mitades juntas: *nadie perdió su reserva; el cupo nuevo rige de acá en adelante.* El motor **nunca cancela** por bajar la capacidad ⇒ si la pantalla lo callara, el prestador no se enteraría.
- **Fallo de lectura ≠ «todavía no configuraste»** (Ley 13 / L-178): si el servidor no contesta, la pantalla lo dice y ofrece reintentar. *Pintar un formulario vacío haría que guardar pisara una configuración que sí existe.*
- **Hoy en fecha LOCAL, jamás `toISOString()`** — eso da UTC y después de las 19:00 en Guayaquil devuelve el día siguiente. El motor cuenta por fecha local del lugar.

**🔴 MONTADA, no sólo entregada:** entra como **quinta baldosa del tab Negocio** (capa `cuidado`, glifo propio), alcanzable desde la navegación real. *Se pagó cinco veces en S106 y no se repite.*

**`HORAS` se EXPORTÓ de `seccion-horarios` en vez de duplicarse** — es la lección de `D-645` aplicada el mismo día que la pagué: una sola verdad para la grilla de medias horas de la casa.

**Verde:** typecheck prestador limpio · `verify:diseno` **VERDE, 60 reglas**.

### 🔴 R66 me cazó, y lo registro porque es del método

Escribí los strings **en voseo** y la casa habla **tuteo** (regla 27 · L-148 — *la voz de producto no hereda el acento de la mesa*). El trinquete es **solo-baja** y lo subí de **47 a 51**: cinco cadenas. **Curadas, y el gate volvió a verde.** *Lo interesante no es el error: es que ningún typecheck lo ve y lo cazó el juez que existe para eso.*

---

## LO QUE ESTA PANTALLA **NO** HACE, Y NO ES RECORTE DE DISEÑO

🔴 **El precio, el paquete y la mensualidad no están porque NO EXISTE LA PUERTA.** Medido: hay `veterinaria-oferta.ts` y `adiestramiento-oferta.ts`, y **no hay `guarderia-oferta`**; ningún wrapper escribe `prestador_servicios` para este oficio. *Montar un campo de precio que no guarda sería la pantalla mintiendo.*

**Y arrastra el final del recorrido:** *«Guardo y quedo visible para reservas»* **es la oferta, no la capacidad** ⇒ hoy el prestador configura cupo y ventanas **y su guardería todavía no aparece para reservar**. **La pantalla lo dice en su propia superficie**, en vez de dejarlo esperando clientes que no pueden llegar.

## LOS DOS HUECOS DEL MOTOR QUE ENCONTRÉ AL MONTAR — declarados, no inventados

1. **No hay lector de espacios.** A publicó `definirEspacioGuarderia` (escritura) y ningún lector. La capacidad se deriva de `obtenerCupoGuarderia` (que devuelve la SUMA de espacios confirmados). **Alcanza para v1 —un lugar, un número, que es lo que el recorrido pide— y no alcanza para dos salas.**
2. **No hay camino para retirar una franja.** La tabla tiene `activo`; el wrapper no lo expone. Y el upsert es por `(prestador, tipo, dias_semana)` ⇒ **si la pantalla dejara editar el patrón de días, pasar de L-V a L-S crearía una segunda franja y dejaría viva la primera, sin forma de matarla.** Por eso **v1 usa el default del motor (L-V) y no expone el patrón.** Se abre cuando exista el retiro.

## PEDIDOS A OTRAS PISTAS, EN SU LUGAR

- **B (juez):** `docs/loop/S107-C-PEDIDO-A-B-R18.md` — enmendar `R18` a una sola casa, con la firma de la mesa adentro. 🔴 **C no retira la entrada del cliente hasta que esté hecho** (orden de la mesa: no dejar el gate en rojo).
- **B (pieza):** `etiquetaAccesible?` en `DocumentoAceptable` — ya va por la mesa.
- **A (motor):** el lector de espacios · el retiro de franja · y **`guarderia-oferta`**, que es lo que destraba el precio y el «visible para reservas».

## LO QUE SIGUE EN MI COLA

Con `obtenerCupoGuarderia` ya publicado puedo montar **el calendario del dueño** (desbloqueo ②) sin esperar a A. Después: el semáforo (③) y el día del prestador (④).

---
---

# S107-C · TANDA 4 — el retiro firmado y el calendario del dueño (28-ago-2026)

## ① `/gallery` del cliente — **RETIRADA**, con el juez ya angostado

`R18` quedó enmendado en main (una sola casa, ancla a 1) ⇒ **el retiro se ejecutó sin dejar el gate en rojo**, que era la condición de la mesa. `verify:diseno` **VERDE**, y `R18` ahora informa *«1 entradas vivas y sin `__DEV__`»* — **la del prestador, la que importaba.**

La lápida quedó en el archivo con las dos mitades: **qué se retiró y qué NO** (`R17` sigue exigiendo la galería; la ruta `/gallery` sigue viva por deep link).

## ② EL CALENDARIO DEL DUEÑO — `apps/cliente/src/app/guarderia/[prestadorId].tsx`

Monta `CalendarioCupo` y `FichaFranja` de B sobre `obtenerCupoGuarderia` y `obtenerFranjasGuarderia` de A. **Un solo viaje por mes** — la RPC es de rango a propósito (S94-PERF: *no hay consultas que optimizar, hay viajes que eliminar*).

- **El día lleno se ve lleno y lo dice** (`disponible === 0` → `sin_cupo` con su motivo). **Jamás desaparece**, y **no hay contador de «quedan 2»**: `MODELO_LOYALTY` §7.5.
- 🔴 **El día PASADO sale `sin_cupo` con MOTIVO PROPIO, no mezclado con «se llenó».** El motor responde el cupo de cualquier fecha —es un contador, no una agenda— así que **la puerta no ofrece lo que el servidor va a rechazar** (Ley 23). Por la misma razón **el botón de «mes anterior» no existe cuando estás en el mes de hoy**.
- **Fallo de lectura ≠ «no hay lugar»** (Ley 13): *un mes pintado lleno porque el servidor no contestó le miente a la familia con la cara de un dato.*
- **Meses y cabeceras por `Intl`, por idioma** — precedente de la Línea de Vida (S52): los meses no se cablean en un array en español.

### 🔴 LA SOBREVENTA — la firma se cumple, y la premisa de la orden NO era cierta

**Firma de la mesa, cumplida:** el dueño ve lleno; la sobreventa no llega a su pantalla. `sobrevendido` **se lee y se descarta acá**; no viaja a la pieza.

⚠️ **Pero `revelaSobreventa` NO EXISTE.** Medido: **cero ocurrencias de `revelaSobreventa`, `sobreventa` o `sobrevendid` en todo `packages/ui`.** No hay prop que dejar apagada.

> **Y el resultado es MEJOR que el que la orden pedía:** `EstadoCupo` es `'elegible' | 'sin_cupo'` — **la pieza no tiene canal para expresar una sobreventa aunque alguien quisiera pasarla.** *Un default en `false` se puede cambiar en la próxima pasada sin que nadie lo note; algo inexpresable no.* **La decisión queda sostenida por construcción, no por disciplina.**

## ③ ⚠️ INERTE, DECLARADO — todavía nadie navega a esta pantalla

`explorar` lista guardería en **«próximamente»**, y **eso es cierto hoy**: sin oferta no hay guarderías que reservar. Enchufarla necesita **dos mitades que son de A**:

1. **`guarderia-oferta`** — sin ella no hay prestador que listar (ni precio que mostrar).
2. **Un flag propio en `country_config`** — medido: hoy guardería **comparte el de `hotel`** (`explorar/index.tsx:105` empuja las dos a «próximamente» con `!servicios.hotel`), y `paisConfig` no tiene `guarderia`. **Colgarla de `hotel` conflictúa con la letra**, que separa los dos servicios explícitamente (§5: *«la noche NO es guardería»*).

**Con esas dos, el enchufe es UNA línea en `explorar`.** Es el molde S91: la mitad propia construida e inerte, con su pedido al lado. **Lo declaro fuerte porque «entregada ≠ montada» se pagó cinco veces en S106** — la diferencia acá es que el consumidor no está sin cablear: **todavía no puede existir.**

**Verde:** typecheck cliente y prestador limpios · `verify:diseno` **VERDE, 60 reglas**.

## LO QUE SIGUE

El **semáforo sanitario** (③) y **el día del prestador** (④). ⚠️ El semáforo necesita `evaluar_requisitos_guarderia`, que **todavía no está publicado** — y cuando llegue, nace **informando y sin bloquear** (`D-956`: 5 de 78 animales tienen alguna vacuna).

---
---

# S107-C · TANDA 5 — el flag desacoplado, y la corrección del semáforo (28-ago-2026)

## ① EL FLAG DE GUARDERÍA — mitad propia construida e INERTE (molde S91)

**Y no era prolijidad: había un defecto latente y se cerró.** `explorar` decidía así:

```js
if (!servicios.hotel) proximamente.push({ hotel }, { guarderia });
```

> ### 🔴 Guardería colgaba del flag de **hotel**. El día que hotel abriera, guardería **no pasaba a activa: DESAPARECÍA** — no tiene ficha propia —, y nadie se habría enterado hasta ir a buscarla.

Y además **contradice una firma**: `LETRA_GUARDERIA` §5 separa los dos servicios con todas las letras. *Compartir bandera es unir en el código lo que la letra separó.*

**Lo hecho:** guardería tiene su propia condición (`GUARDERIA_ABIERTA`, hoy `false`) y **sigue en «próximamente», que hoy es cierto**. Cuando A publique el flag, es **una línea** y la constante muere en el mismo acto.

**El pedido:** `docs/loop/S107-C-PEDIDO-A-A-FLAG-GUARDERIA.md` — con el cambio exacto (clave `guarderia`, default `false`, dos líneas en `paisConfig.ts`) y **la advertencia que importa más que el pedido: el flag es la MITAD de la llave.** Con flag y sin oferta, la ficha abre a una lista vacía; con oferta y sin flag, hay guarderías y nadie llega. **Se encienden juntas, y la oferta va primero.**

**Verde:** typecheck cliente limpio · `verify:diseno` **VERDE, 60 reglas**.

## ② 🔴 CORRECCIÓN DE LA MESA AL SEMÁFORO — y corrige un razonamiento MÍO

**Yo escribí que el gate sanitario debía nacer ABIERTO** (informando y sin bloquear), en el contrato de A §⑥ y en la ficha `D-956`, con este argumento: *«5 de 78 animales tienen alguna vacuna ⇒ cerrarlo rechaza a 73 de 78; un gate que corre sobre un criterio sin datos no gatea: rechaza a todos.»*

**La mesa lo corrigió, y la corrección es buena:**

> **La firma ③ es gate DURO y `D-956` v1 lo confirmó: sin carnet cargado y rabia vigente NO SE RESERVA**, con el camino a cargarlo **a un toque**. **Los 5 de 78 no son razón para abrirlo: son el catálogo vacío, y cada familia lo llena en su primera reserva.**

**Dónde falla mi razonamiento, para no repetirlo:** medí el estado del catálogo **hoy** y lo traté como si fuera una propiedad del sistema. **No lo es: es su punto de partida.** Un catálogo vacío que se llena en el primer uso no se parece a un criterio sin datos —la cadena de vendedores de S99, donde el dato *nunca iba a llegar solo*—, y **usé ese precedente donde no aplicaba.** *La diferencia es si el propio recorrido produce el dato que falta; acá lo produce.*

**Y el costo de haberme salido con la mía habría sido peor que rechazar a 73:** un gate sanitario abierto en un servicio donde el animal **viaja y convive con otros** es exactamente lo que la Res. 121 exige que no pase.

⚠️ **QUEDA UNA CORRECCIÓN DOCUMENTAL QUE NO ES MÍA DE EJECUTAR:** el contrato `docs/contratos/s107-contrato-cupo-franja-estadia.md` **§⑥ todavía dice «EL GATE NACE ABIERTO»**, y eso ahora **contradice una firma**. Lo escribí yo cuando conducía como A; hoy `docs/contratos/` es territorio de A. **Pedido a A: enmendar §⑥ al gate duro.** *Dos letras que se contradicen son peores que una equivocada — cualquiera cita la que le conviene y está «en regla».*

**Cuando construya el semáforo (va último):** nace **CERRADO**, con cada faltante nombrado y su camino a resolver a un toque — que es lo que `SemaforoSanitario` de B ya vuelve inexpresable de otra forma (su `falta` no compila sin `onResolver`).

---
---

# S107-C · TANDA 6 — el recorrido del prestador CERRADO y el dueño ENCHUFADO (28-ago-2026)

## ⓪ LAS TRES VERIFICACIONES DE LA MESA, CONTRA EL OBJETO

| lo que se pidió verificar | medido |
|---|---|
| el flag es `guarderia`, no `daycare` | 🟢 **en el código Y en el dato**: `paisConfig` lee `'guarderia'`, y en `country_config` **EC y CO tienen la clave `guarderia`, cero residuo de `daycare`** |
| su valor | **`false` en los dos países** — encendido pendiente, exactamente como pedía mi §5 (*el encendido es un acto propio, con la oferta viva*) |
| R66 en 47 | 🟢 **confirmado por el juez en cada corrida de esta tanda: no creció.** Escribí ~30 cadenas nuevas y ninguna en voseo |

🔴 **La de `daycare` no era trámite:** el código lee `leerBandera(o, 'guarderia')` y **una clave que no coincide devuelve `false` para siempre, en silencio** — la guardería nunca habría aparecido y nada habría fallado. *Por eso se midió el dato y no sólo el archivo.*

## ① EL RECORRIDO DEL PRESTADOR — **CERRADO**

`guarderia/taller.tsx` gana **el precio**, y con eso la frase que faltaba: *«Guardo y quedo visible para reservas.»*

- **Precio del día** por `SliderPrecio` (el riel de la casa, arranca en el piso y jamás en vacío) **con el NETO vivo** (`VozComision`) — que **calla si la comisión no se pudo leer, en vez de inventar un número**.
- **Paquete y mensualidad OPCIONALES**, nacen apagados. El motor guarda `null`, que **jamás cae al precio del día** — su propio contrato lo dice.
- 🔴 **LA OFERTA SE GUARDA ÚLTIMA, y no es orden de conveniencia:** publicar **exige franjas Y capacidad**, y el motor lo rebota hablado (`franjas_no_configuradas` / `sin_espacios_configurados`). Guardarla antes rebotaría **contra lo que esta misma pantalla está por escribir**.
- **Los dos rebotes de la firma llegan con SU voz desde el wrapper y no se re-escriben acá** — dicen QUÉ falta, así el prestador sabe adónde ir.
- **«Visible» sólo se dice cuando el motor lo confirma.** Es la palabra más peligrosa de la pantalla: dicha sin ser cierta, deja al prestador esperando clientes que no pueden llegar. Y cuando lo está, se muestra **la jornada DERIVADA de sus franjas** — el dato que prueba que el motor leyó lo que él configuró. 🔴 **La jornada no se teclea en ningún lado.**
- **La comisión NO entra al guard de carga:** si no se pudo leer, el neto calla y la pantalla sigue usable. *Un dato de apoyo que rompe la pantalla entera es peor que su ausencia.*

## ② EL DUEÑO — ENCHUFADO, y la cadena está completa

**`explorar` → ficha activa → `/hogar/guarderia` (hub nuevo) → `/guarderia/[prestadorId]` (el calendario).** La pantalla que la tanda 4 dejó inerte **ya tiene quién la llame**.

- **`GUARDERIA_ABIERTA` murió** (Ley 37): la ficha se enciende con `servicios.guarderia`, el flag real. **Hoy viene en `false`, así que guardería sigue en «próximamente» — y eso es correcto**: la ficha aparece el día que la mesa encienda el flag, con oferta viva.
- **El hub pregunta a quién y qué día**, y lista con `obtenerGuarderiasDisponibles`.
- 🔴 **LAS DOS PREGUNTAS SON DISTINTAS Y SE PINTAN DISTINTO**, y el wrapper de A lo dice en su cabecera: en el **hub** el prestador que no puede ese día **simplemente no está**; en el **calendario del lugar** el día lleno **se ve lleno y lo dice**. *Un jueves que desaparece se lee como «el jueves no existe»; un prestador ausente no.*
- **Las TRES fases de la elegibilidad, honradas** (L-218 · R34): `ofrecibles()` devuelve `[]` cargando, con error y de verdad vacío — **y esta pantalla no decide con `length === 0`**. Es el defecto que dejó *«el paseo es para perros»* con dos perros vivos. **R34 la contó y pasó: 5 decisiones vigiladas.**
- **La especie es DATO, no `if`:** medido, `tipos_servicio.especies_elegibles = ["perro","gato"]` y la categoría es `hospedaje`. La pantalla lee el catálogo; no lleva la regla adentro.
- 🔴 **`sobrevendido` no se pinta en ninguna de las dos superficies del dueño.** Para la familia, un lugar que puede recibirla es un lugar que puede recibirla.

**Verde:** typecheck prestador y cliente limpios · `verify:diseno` **VERDE, 60 reglas** · **R66 sin crecer**.

## LO QUE SIGUE

**El día del prestador** (`obtenerEstadiasDelDia`) y después **el semáforo, último y CERRADO** — con la corrección de la mesa ya adoptada: sin carnet cargado y rabia vigente no se reserva, con el camino a cargarlo a un toque.

⚠️ **Y queda un acto que no es de código:** el flag está en `false`. **Encenderlo es decisión de la mesa** y necesita al menos una guardería con oferta publicada — si se enciende antes, la ficha abre a una lista vacía.

---
---

# S107-C · TANDA 7 — el semáforo CERRADO y la reserva hasta el checkout (28-ago-2026)

## ⓪ 🔴 CORRECCIÓN DE ORDEN, POR MEDICIÓN — lo que llegó no fue ④

La mesa mandó: *«el día del prestador, después el semáforo»*. **Medido: `obtenerEstadiasDelDia` NO existe** — cero ocurrencias en `packages/api` y en `supabase/migrations`. Lo que A publicó fue **③ (el gate sanitario) y la reserva con su cobro**, no ④.

⇒ **Se hizo el semáforo, que sí tiene motor.** *El día del prestador no se pospuso por preferencia: no tiene con qué construirse.* **Y no se montó contra un contrato inexistente**, que es la regla que esta pista viene sosteniendo toda la sesión.

## ① EL SEMÁFORO — **NACE CERRADO**

- **La compuerta vive en el SERVER** (`_guarderia_puede_reservar`) y **la pantalla la REFLEJA**: no la reimplementa y no la ablanda. *Un gate que decide la pantalla es decorativo — se salta cambiando de cliente.*
- **Cada faltante llega con su código, su estado y su fecha**, y la pantalla le pone **voz** (`estado_nunca_aplicada` · `sin_carnet` · `sin_fecha` · `vencida`) — *el server manda códigos; la voz es de la casa*, contrato §⑥bis.
- **El camino a resolver, a un toque:** cada faltante lleva `onResolver → /carnet`. 🔴 **Y no es disciplina mía: el tipo de `SemaforoSanitario` hace que un faltante sin camino NI SIQUIERA COMPILE.**
- 🔴 **El semáforo entra al GUARD DE CARGA:** si no se pudo evaluar, la pantalla **no sigue**. *Un gate sanitario que falla en silencio deja pasar por omisión* — y acá el animal viaja y convive con otros.
- **El botón de reservar está deshabilitado sin requisitos al día**, con su porqué escrito debajo. La puerta no ofrece lo que el servidor va a rechazar (Ley 23).

**La corrección de la mesa quedó adoptada entera, y está escrita en el código donde se lee:** *los pocos animales con carnet hoy no son razón para abrirlo — son el catálogo vacío, y cada familia lo llena en su primera reserva.*

## ② LA RESERVA — hasta el checkout, sin adelantarse al motor

**El hub pasa la mascota al lugar** (sin ella no hay a quién evaluar, y una pantalla que no puede evaluar el gate no puede ofrecer reservar).

`reservarDiaGuarderia` → **la reserva nace `pendiente_pago` con hold de 15 minutos y el desglose se congela solo** → navega a `guarderia/checkout`.

- 🔴 **El checkout NO es nuevo: monta `CheckoutReserva`, la MISMA pieza que paseo y grooming** (la superficie de pago es UNA, S101, vigilada por `R57` — que sigue midiendo **2 checkouts montando la misma pieza y 0 versiones propias**). *Un segundo checkout sería una segunda forma de equivocarse con la plata.*
- **La espera con voz es de la pieza**, y esta pantalla **no declara nada**: `confirmada` **sólo cuando el motor confirma**.
- **La hora no se inventa:** una estadía-día no tiene hora, tiene dos ventanas. Viaja *«Todo el día»* en vez de un `00:00` que no significa nada — *un dato vacío con forma de dato es peor que su ausencia.*
- **El rebote llega con SU voz del wrapper** («ese día ya no tiene lugar», «faltan requisitos») **y recarga el calendario**: si el cupo cambió mientras la familia miraba, el calendario tiene que decirlo.

**`exitoIcono` ganó `guarderia`** — unión cerrada a propósito (un slot libre dejaría pasar cualquier glifo, y la Ley 12 pide el objeto del oficio). Es `apps/cliente`, territorio propio.

## ③ LA COSTURA DE A, RESPETADA

A declaró que `_guarderia_puede_reservar` **tiene hoy UNA condición y mañana dos** — la compuerta de documentos llega con ⑤.

**No leí esa puerta como completa y NO construí la aceptación de documentos contra ella.** La pantalla refleja lo que el gate responde hoy; cuando ⑤ agregue su condición, el reflejo sigue siendo correcto **sin tocar esta pantalla** — porque no duplica el criterio, lo lee.

**Verde:** typecheck cliente limpio · `verify:diseno` **VERDE, 60 reglas** · **R57 y R66 sin moverse**.

## LO QUE QUEDA

- **El día del prestador** — 🔴 **bloqueado: `obtenerEstadiasDelDia` no existe.** Es lo único que falta para cerrar el recorrido del prestador de punta a punta (hoy configura y cobra, pero no ve su jornada).
- **La aceptación de documentos** — espera ⑤ (compuerta + documentos versionados).
- ⚠️ **El flag sigue en `false`:** el camino del dueño está completo **y todavía no es alcanzable desde `explorar`**. Encenderlo es acto de la mesa, y pide al menos una guardería con oferta publicada.

---
---

# S107-C · TANDA 8 — el día del prestador, y una corrección mía (28-ago-2026)

## ⓪ LAS DOS CORRECCIONES, SEPARADAS PORQUE NO SON LA MISMA COSA

**① `obtenerEstadiasDelDia` — no fue error, fue carrera.** Medido: en **`63d7bde2`** (15:10), que era lo que yo tenía cuando dije que no existía, **el wrapper no estaba** (`grep` en 0 sobre ese árbol). Apareció en **`6cd83518`** (16:28) — que es **el merge de mi propio trabajo**. *Medí bien sobre el mundo que tenía; el mundo cambió una hora después.* **No hay lección acá, sólo una ventana.**

**② El OTA — ese SÍ fue error mío, y es el que vale.** Escribí *«build nueva no hace falta»* midiendo **cero cambios nativos entre `main` y mi rama**. La medición era correcta; **la pregunta estaba mal.**

> ### La pregunta no era *«¿mi rama agrega algo nativo?»* sino **«¿existe un binario para el runtime que `app.json` declara?»**. `app.json` dice **1.0.7** y ese binario **nunca se cortó**: la última terminada es **1.0.6**, del 24-ago. **Un OTA contra 1.0.7 no le llega a nadie.**

*Y el modo de falla era silencioso: el founder habría cerrado y abierto la app dos veces esperando algo que no podía bajar.* **Queda como `L-432`, y el guion de gate ya está corregido en su §0** — no sólo acá.

## ① EL DÍA DEL PRESTADOR — `apps/prestador/src/app/guarderia/dia.tsx`

- **Es una VISTA, no una «jornada»:** un día con seis animales son **seis estadías**. La pantalla compone leyendo; no hay objeto que pedir ni que mutar.
- 🔴 **Solo verdad firme.** El wrapper no trae holds sin pagar y **la pantalla no los pide por otro lado**. *Una lista con reservas que pueden evaporarse en quince minutos manda al cuidador a buscar un animal que nadie compró.*
- **La dirección es la del paseo, la MISMA pieza** (`SeccionDireccion`), sobre el snapshot congelado al reservar (D-339). ⚠️ El wrapper la entrega como **`unknown`** a propósito, así que **se estrecha con un guard de forma y jamás con un `as`**: *un `as` produciría un objeto vacío con forma de dirección, y el cuidador saldría a buscar a una casa en blanco.*
- **El estado se mapea a la familia de `Insignia` con criterio, no por decorar:** `atencion` queda reservado a lo que hay que **mirar** —no recogida, cancelada—; el resto es curso normal. *Si todo gritara, nada gritaría.*
- **El espacio `null` se calla**, no se pinta como «sin sala». **La especie se estrecha** al vocabulario del avatar en vez de pasarle un string que no entiende.
- **Vacío digno:** *un día sin animales no es un negocio muerto.* Y **el fallo no se disfraza de vacío** (Ley 13): el cuidador se quedaría en su casa creyendo que no hay jornada.

🔴 **NO marca nada, y no es recorte:** medido, **los cuatro wrappers de acción no existen**, y las transiciones son **eventos server que llegan con el acta (⑤)**. *Un botón que no mueve el estado sería la pantalla mintiendo sobre lo que puede hacer.* **La pantalla lo dice en su superficie**, en vez de dejar al prestador buscando el botón.

## ② LA PORTADA DEL MUNDO, Y UNA TENSIÓN QUE DECLARO

Nace `/guarderia/index.tsx` con **dos puertas** —«Tu día» y «Configuración»—, misma anatomía que `/paseo` y `/grooming`. **La baldosa de Negocio ahora apunta ahí.**

⚠️ **Tensión con `DISEÑO_EXPERIENCIA` §15b, firmada: «HOY acciona / NEGOCIO gestiona».** *«Tu día»* es de **HOY**. Está bajo Negocio porque **HOY tiene 2.854 líneas y su propia lógica de merge de citas**, y una estadía-día **no es una cita con hora**: es un día entre dos ventanas. **Inyectarla ahí es un cambio a la pantalla que el prestador usa todos los días y merece su tanda.**

> 🔴 **Se declara en el código, en el guion de gate y acá** — no en un comentario que nadie lee. *Una ley firmada que se incumple en silencio se vuelve una ley que nadie sabe que rige.* **El destino correcto es una entrada en HOY.**

**Verde:** typecheck prestador limpio · `verify:diseno` **VERDE, 60 reglas** · R66 sin crecer.

## LO QUE QUEDA DEL RECORRIDO DEL PRESTADOR

**Configura ✅ · cobra ✅ · ve su día ✅ · marca la recogida y la entrega ❌** — eso último es el acta, y es ⑤.

---
---

# S107-C · TANDA 9 — los ajustes del gate (28-ago-2026)

## LO HECHO — cinco de los seis

**① La grilla de Negocio, a TRES columnas.** Con cuatro mundos dos columnas repartían parejo; con el quinto **la última fila quedaba con UNA baldosa sola del doble de ancho**, y *una baldosa más grande se lee como más importante* — justo lo que la grilla no debe decir. Tres columnas parten 5 en **3+2** y **ninguna queda huérfana a lo ancho**: la fila corta se ve corta, no inflada.

**② Los labels de la config.** «Precio por día» → **«Tu precio por día»**; los toggles pasan a **«Paquete de estadías»** y **«Mensualidad»**. *Antes había que encenderlos para saber qué eran — un control que se explica al usarlo ya se usó.*

**④ La portada, con el patrón de los otros oficios y no inventada.** Se reescribió siguiendo `grooming/index.tsx` —que a su vez heredó el del paseo, y lo dice en su cabecera—: **sin configurar** → vacío con su camino; **configurada** → **el resumen primero** (visibilidad con la verdad del motor, precio, jornada, cupo y las dos ventanas) y **recién debajo el botón de editar**. Con `useFocusEffect`, para que al volver del taller el resumen muestre lo recién guardado.

> *Entrar directo al formulario le dice al prestador que su trabajo anterior no existe: cada visita empieza de cero aunque no la haya perdido.*

**⑤ El calendario: HOY jamás se reserva — y de paso se separaron CUATRO razones.** La pieza sólo tiene `elegible | sin_cupo`, así que la distinción vive en el motivo:

| caso | voz |
|---|---|
| anterior a hoy | *«Ya pasó»* |
| **hoy** | **«Las reservas entran desde mañana»** |
| **capacidad 0** | **«Ese día no abren»** |
| lleno | *«Sin lugares para este día»* |

🔴 **Las dos del medio son nuevas, y la tercera no me la pidieron: la encontré al implementar la segunda.** `capacidad === 0` **no es «se llenó»** — es que el lugar no abre ese día. *Decirle «se llenó» a alguien un domingo que la guardería nunca abre es mentirle sobre por qué no puede*, y lo manda a esperar un cupo que no va a existir.

**⑥ La tensión de §15b quedó ACEPTADA por firma**, con su ficha pedida a A (abajo). **Escrita también en la cabecera de la portada**, para que se lea donde vive.

## 🔴 ③ EL PAQUETE 5·10·15 — NO SE MONTÓ, Y ES POR MEDICIÓN

| lo que hace falta | estado |
|---|---|
| **`FichaPaquete`** (pieza de B) | **no existe** — cero archivo en `packages/ui` |
| el contrato del motor (A) | **no existe** — cero `guarderia_paquetes` / `paquete_dias` en `packages/api` y en las migraciones |

**La orden decía «no montes contra el precio único viejo», y no lo hice.** El toggle de paquete **sigue como está** —un precio único— hasta que lleguen las dos mitades. *Cambiarlo a chips contra un motor que todavía guarda un solo número sería una pantalla que promete tres paquetes y guarda uno.*

⚠️ **Y hay una decisión de migración que va a aparecer y conviene verla ahora:** los prestadores que ya guardaron un `precioPaquete` único **tienen un dato que el modelo nuevo no representa**. Cuando A publique, **hace falta decir qué pasa con ese número** — se mapea a uno de los tres chips, o se descarta declarándolo. *No es mío, y no se resuelve solo.*

## PARA A — la ficha de §15b, texto listo para depositar

> **`D-9xx` — 🟡 «TU DÍA» DE GUARDERÍA VIVE BAJO NEGOCIO Y ES DE HOY**
> `DISEÑO_EXPERIENCIA` §15b firma **«HOY acciona / NEGOCIO gestiona»**, y la jornada del cuidador es acción, no gestión.
> **Aceptado para S107 por firma del founder.** La razón medida: **HOY tiene 2.854 líneas y su propia lógica de merge de citas**, y una estadía-día **no es una cita con hora** — es un día entre dos ventanas. Inyectarla ahí toca la pantalla que el prestador usa todos los días.
> **Dueño:** C. **Disparo: después del acta (⑤)**, en su propia tanda.
> ☠️ **Condición de muerte:** «Tu día» alcanzable desde HOY y retirado de la portada de Negocio **en el mismo acto** (Ley 37) — *dejarlo en los dos lados es la duplicación que `D-645` acaba de costar.*

**Verde:** typechecks prestador y cliente limpios · `verify:diseno` **VERDE, 60 reglas**.

---
---

# S107-C · TANDA 10 — los chips 5·10·15 y el calendario que deja de inferir (29-ago-2026)

## ① LOS PAQUETES — tres chips, cada uno con su precio y su espejo

Los tres tamaños **se pintan siempre** y el prestador enciende los que quiera. La cuenta del equivalente por día **la hace la pieza de B y acá sólo se dice**: *el número es uno solo o no sirve* — si esta pantalla lo recalculara, el prestador podría vender un descuento que la familia no ve.

### 🔴 Los DOS estados que se ven distinto, y no es un matiz

| estado | qué significa | cómo se ve |
|---|---|---|
| **no está en la respuesta** | **nunca se encendió** | sin precio — *no hay ninguno que mostrar* |
| **`activo: false`** | **apagado, con su precio guardado** | **muestra su precio** |

> *Si los dos se vieran iguales, volver a encender un paquete se sentiría como empezar de cero sobre un precio que el prestador ya había pensado.*

**Y el guardado lo respeta:** un tamaño que **nunca prendió no se escribe**, para que su ausencia siga significando *«nunca lo ofrecí»* y no *«lo ofrecí en cero»*.

### 🔴 LOS TAMAÑOS NO ESTÁN CABLEADOS — y lo probé en rojo

La orden era no cablear los tres. La pantalla **no puede leer un `CHECK` de Postgres**, así que hice lo siguiente mejor: **que quedarse atrás sea imposible en silencio.**

```ts
const TAMANOS = [5, 10, 15] as const satisfies readonly TamanoPaquete[];
type _FaltaAlgunTamano = Exclude<TamanoPaquete, (typeof TAMANOS)[number]>;
const _GUARD_TAMANOS: _FaltaAlgunTamano extends never ? true : never = true;
```

**`satisfies`** prueba que cada valor listado es válido; **el `Exclude`** prueba que no falta ninguno. **Bidireccional.**

> **Probado, no afirmado:** inyecté `| 20` en `TamanoPaquete` y el typecheck **rompió exactamente en esta pantalla** (1 error, en `guarderia/taller`). Restaurado. *Un guard que nadie vio fallar es una promesa, no un guard.*

⚠️ **Lo que NO reusé, y por qué:** existe `PRESETS_PAQUETE = [5,10,15]` en `packages/api`, pero es **del paseo** (`MODELO_PASEO` §6bis.1). **Coinciden hoy por casualidad**; acoplarlos haría que cambiar el paquete de paseo moviera el de guardería sin que nadie lo pidiera.

## ② EL CALENDARIO — dejo de inferir y consumo los cinco casos

**Se retiró mi lógica de la tanda 9** y ahora se pinta `estado` tal como llega: `pasado` · `mismo_dia` · `no_opera` · `sin_lugar` · `elegible`. Cada uno con su voz.

🔴 **Y la corrección de la mesa era la buena, más allá de la disciplina:** *«no opera» se mide del **patrón** del lugar, no del cupo.* Yo lo había inferido de `capacidad === 0`, y desde la pantalla **«no abre» y «se llenó» llegan los dos como `disponible = 0`** — *dos ceros distintos que sólo el motor puede separar.* **Mi hallazgo era correcto y mi instrumento no alcanzaba.**

Se agregó un sexto caso que el contrato no nombra y la pantalla sí necesita: **un día sin fila en la respuesta**. No es elegible, y **se dice sin inventarle una causa** — sólo el motor sabría por qué.

**Verde:** typechecks prestador y cliente limpios · `verify:diseno` **VERDE, 60 reglas** · R66 sin crecer · **el guard de tamaños probado en rojo y restaurado**.

---
---

# S107-C · TANDA 11 — la config reestructurada y los ajustes de Explorar (29-ago-2026)

## ① LA CONFIG DEL PRESTADOR, EN EL ORDEN FIRMADO

**Capacidad suelta → acordeón «Horarios» (cerrado) → acordeón «Tus precios» (abierto) → «Así lo ve el dueño».**

- **`AcordeonSeccion` nace local**, con **promoción declarada** (precedente `GateRoto`/`GateAjeno`, S79): `packages/ui` **no tiene acordeón** —medido— y esta pantalla necesita dos. **El estado es del consumidor** (`abierto` + `onAlternar`): *un acordeón que se acuerda solo de su estado se lo impone a todos*, y acá la decisión es de producto — los horarios ya están guardados, los precios son a lo que se viene.
- **«Tus precios» en plural**, con las tres modalidades como fichas hermanas: **Diario** (prendido por defecto — es la unidad base del oficio), **Paquetes** (los tres, cada toggle enciende y expande en el mismo acto) y **Mensual**, que **antes tenía su precio en un `Campo` suelto fuera de toda ficha y ahora vive como sus hermanas**, con su riel propio.
- **El espejo del equivalente sólo compara si hay contra qué**: sin precio diario encendido, la pieza **omite la comparación en vez de inventar un 0 %**.
- **«Así lo ve el dueño» monta LAS MISMAS PIEZAS que la familia**, no una maqueta — *si fueran otras, el prestador estaría mirando algo que nadie ve* — y **dice cuándo la familia no vería nada.**

### Los dos avisos de guardar

**Se pregunta cuando hay alternativa; se informa cuando no la hay.** *Preguntar lo que no se puede cambiar entrena a tocar «seguir» sin leer, y ese hábito se paga en el aviso que sí importaba.*

- **Sin ningún precio** → **pregunta**, dos botones, «volver» primero. Guardar así **guarda todo y no publica**, y lo dice.
- **Con precio pero sin diario** → **informa**, un botón, **nombrando lo que queda encendido** — *«no se publica» sin decir QUÉ queda deja al prestador adivinando qué configuró.*

### 🔴 DOS COSAS DE LA ORDEN QUE EL MOTOR NO SOSTIENE — medidas

| lo ordenado | medido |
|---|---|
| **«Especies que recibes», arriba de todo** | 🔴 **`definir_oferta_guarderia` es `(uuid, numeric, numeric, boolean)` — sin parámetro de especies.** Y **cada oficio escribe las suyas con SU wrapper** (grooming · veterinaria · adiestramiento · paseo); guardería **no lo tiene**. **No se montó**: el prestador elegiría «solo perros», guardaría, y **su elección se perdería sin un solo error.** |
| **«el precio del día ya no es obligatorio»** | 🔴 **No llegó.** `precioDia: number` sigue **requerido** y `precio_invalido` sigue diciendo *«el precio por día tiene que ser mayor a cero»*. |

**Lo que hice con la segunda:** la oferta **sólo se llama si hay precio del día**; sin él **se guarda todo lo demás y no se publica**, y el aviso lo dice con esas palabras. **Cuando A cambie la regla, es un string y una condición.**

## ② EXPLORAR — tres columnas, el label muerto y la lista corregida

- **Tres columnas** como Negocio, con `flexGrow: 0` para que **la fila corta se vea corta** y no se estire; **glifos de 34 → 26**, el tamaño que adiestramiento ya usaba.
- ☠️ **«Agendar veterinaria llega pronto» RETIRADO.** *No pertenecía: la sección lista lo que YA se agenda, y una nota que dice lo contrario debajo contradice lo que la pantalla muestra.*
- **«Próximamente» según la corrección:** salen **telemedicina** (ya implementada, vive en veterinaria — *anunciar como futuro algo que ya se usa hace dudar de toda la lista*) y **guardería** (subió a implementados).

🔴 **Hoy la lista tiene TRES, no cinco: faltan `wearables` y `certificaciones` porque les falta su glifo.** **Censo hecho antes de pedir, como ordenaste:** los glifos viven en `packages/ui` (`IconoNombre` es el registry tipado; las apps sólo consumen por nombre) ⇒ **pedido a B**, escrito en `docs/loop/S107-C-PEDIDO-A-B-GLIFOS.md` con la familia que deben seguir y una nota que conviene: **`wearables` nace con DOS consumidores** — el hueco `M-WEAR` de la ficha de mascota ya está reservado desde S51.

**No los listé con un glifo prestado:** *dos servicios con el ícono de un tercero se leen como ese tercero*, y en una lista cuya única función es decir qué viene, eso es la lista mintiendo.

## ③ LA RESPUESTA A TU PREGUNTA DEL FLAG

> ### **Es SOLO el flag. No hay que mover nada de sitio.**

La ficha de guardería **ya vive en el camino de los servicios activos**, gateada por `servicios.guarderia`, y **no cae a «Próximamente»**. El día que lo enciendas, la ficha aparece con las otras y navega a `/hogar/guarderia`. **Cero cambios de código.**

⚠️ **Lo que sí hace falta antes de encenderlo, y no es código:** **al menos una guardería con oferta publicada.** Con el flag en `true` y sin ofertas, la ficha abre a una lista vacía.

**Verde:** typechecks prestador y cliente limpios · `verify:diseno` **VERDE, 60 reglas** · cuatro claves i18n muertas retiradas con sus consumidores (Ley 37), y las de «Próximamente» **conservadas con su nota** — *se retiró la lista, no el plan.*

---
---

# S107-C · TANDA 12 — la verificación, y la tanda corta completa (29-ago-2026)

## ⓪ LAS DOS VERIFICACIONES QUE LA MESA PIDIÓ

**① La migración a `FichaDeOferta` quedó bien, y sobrevivió lo que importaba.** A migró leyendo la pieza entera; el merge trajo tres conflictos —porque migró una versión anterior de mi archivo— y al resolverlos **el `precioDiaSuelto` CONDICIONAL se conservó**. Ese era el punto: A lo había dejado incondicional, y **con la regla nueva el precio del día puede no existir** ⇒ comparar contra un día apagado **inventa un descuento contra nada**.

**Dos cosas que la pieza nueva permite y aproveché:**
- **`tamano` acepta `null`** ⇒ Diario y Mensual dejan de pasar el **`1` inventado** que yo mismo había declarado inerte. *Un dato falso invisible sigue siendo un dato falso.*
- **`onCambio` reporta el estado NUEVO** ⇒ se usa ése y no se invierte el propio. *Invertir a ciegas se desincroniza el día que la pieza decida no alternar.*

**② `SeccionPlegable` reemplazó a mi acordeón, y el local MURIÓ en el mismo acto.** `apps/prestador/src/components/acordeon-seccion.tsx` **borrado**. Nació con su promoción declarada y se cumplió — *es `D-645` aplicada sobre mi propio código: retirar la vieja era parte del trabajo, no limpieza para después.*

## ① EL SEMÁFORO EN DOS MODOS — una sola pantalla

`bloquea` viaja **dentro** de la evaluación ⇒ **no hay rama nueva: hay un dato más en la condición.** Y **el mismo `bloquea` lo lee `reservar_dia_guarderia`** ⇒ *esta puerta nunca se abre para chocar contra otra.*

**El mismo faltante, dos tonos** — la diferencia es **de trato, no de información**: encendido **frena y hay que decirlo**; apagado, *«puedes reservar igual; cuando puedas, carga lo que falta»*. **Una voz de alarma sobre algo que no impide nada enseña a ignorar las alarmas que sí importan.** El camino a cargarlo sigue a un toque en los dos modos — la pieza **no compila un faltante sin camino**.

## ② EL SELECTOR DE ESPECIES — arriba de todo, patrón reusado

Mismo `SelectorOpcion` de grooming. **El universo es del tipo y la elección es del prestador**; el server recorta igual — **la pantalla ofrece, no autoriza**.

🔴 **Lo guardado GANA sobre el default.** Era la razón exacta por la que no lo monté antes: sin el retorno del lector, un prestador que eligió «solo perros» **habría visto las dos marcadas y al guardar habría sobrescrito su propia elección, sin un solo error.**

## ③ LA RUTA SIN PRECIO DEL DÍA — abierta

La oferta **se guarda siempre**. Era condicional porque el motor exigía `precio_dia` **y su lector rechazaba un precio nulo**: guardar sin día dejaba el taller imposible de reabrir. **Las dos mitades están curadas.** 🔴 `null` y jamás `0`: **cero sería «gratis»**.

**El aviso cambió de naturaleza con eso:** antes advertía una imposibilidad; ahora **nombra lo que se publica**. *Informar sin nombrar deja al prestador adivinando con qué salió a la vitrina.*

## ④ EL RIEL DE CLAVES TIPADAS ME CAZÓ

Las cuatro claves de especies **cayeron en el bloque equivocado** —había otro `especiesTitulo` en otro namespace— y **el typecheck lo dijo antes de que nadie mirara una pantalla**. *Una clave en el lugar equivocado no rompe nada visible: muestra el texto de otro oficio.*

## LO DEL CLIENTE YA ESTABA HECHO

Los tres ítems de Explorar se hicieron en la **tanda 11** y **sobrevivieron el merge** (verificado por contenido). **Siguen faltando `wearables` y `certificaciones`**: es el pedido de glifos a B, y sin ellos la lista tiene **tres de las cinco**.

**Verde:** typechecks prestador y cliente limpios · `verify:diseno` **VERDE, 60 reglas**.

---
---

# S107-C · TANDA 13 — los ajustes del gate, y la contradicción medida (29-ago-2026)

## 🔴 ① LA CONTRADICCIÓN — medida contra `main`, y tiene una sola explicación

**Los cinco marcadores, medidos en `origin/main` con `git show` (no en mi worktree):**

| marcador | en `main` |
|---|---|
| `flexBasis: '31%'` (tres columnas) | **presente** |
| `agendarLlegaOtros` (el label suelto) | **ausente** |
| `proxTelemedicina` | **ausente** |
| `proxPrime` | presente |
| `servicioGuarderia` (ficha activa) | presente |

**El código está bien en `main`.** Y la historia del archivo dice por qué el aparato muestra otra cosa:

> ### **Los tres cambios de Explorar viven en UN SOLO COMMIT — `3fa1c4ae`, 28-ago 21:30 — y es el último que tocó ese archivo. Son inseparables: no existe un bundle que traiga las tres columnas del CLIENTE sin traer también el label retirado y la lista corregida.**

⇒ **La combinación que el founder describe no puede venir de mi código.** La explicación que sí encaja, y es verificable: **las tres columnas que está viendo son las del PRESTADOR** (grilla de Negocio, `154d85f8`, 28-ago **16:51** — cuatro horas y media antes), **y su bundle del CLIENTE es anterior a las 21:30.**

**El discriminador, para que no sea una hipótesis:** en la Explorar del cliente, **si la ficha de guardería está entre los servicios activos**, el bundle es posterior a `3fa1c4ae` y las otras dos cosas tienen que estar. **Si el label viejo sigue ahí, el bundle es anterior** — y entonces las columnas que ve son las de Negocio, en la app del prestador.

*No es que la verificación por contenido fallara: verifiqué el repo, y el repo estaba bien. Lo que no verifiqué —porque no puedo— es qué bundle tiene el teléfono.* **Es `L-138` otra vez, y la parte que me toca es decirlo con su discriminador en vez de discutirlo.**

## ② EL GLIFO DE LA BALDOSA — el defecto era mío y es de contrato

`Baldosa` dimensiona su glifo con **`columnas`** (`LADO_GLIFO = {2: 48, 3: 32}`) y su **default es 2**. Yo cambié la celda a 33 % **y no le pasé la prop** ⇒ el glifo siguió a 48 en una celda un tercio más angosta. **Ahora va `columnas={3}`.**

> *Cambiar el ancho del contenedor no achica lo de adentro; hay que decírselo a la pieza.* **Y el cliente estaba bien por otra razón: ahí el glifo no viene de `Baldosa` — lo monto yo con `<Icono tamano={26}>`, y por eso sí se achicó.** Dos caminos distintos para el mismo síntoma.

## ③ LOS LABELS DE EXPLORAR — medido: el tamaño es del consumidor

**No es pedido a B.** El label de la ficha se dibuja en `explorar/index.tsx` con `typography.size.base`; **baja a `sm` y se acota a dos líneas**, y el detalle pasa de tres líneas a dos. *Con la baldosa a 31 % el texto se salía de su espacio.*

## ④ LOS TRES AJUSTES DE LA PANTALLA DEL PRESTADOR

- **El espejo, parejo:** la mensualidad usaba `FichaMensualidad` y **su precio se veía más grande** — dos piezas distintas con dos jerarquías, una al lado de la otra. Ahora las tres montan **`FichaDeOferta`**. *Tres precios que se comparan tienen que pesar igual: el que se ve más grande se lee como el importante, y acá ninguno lo es.*
- ☠️ **El resumen de «cuánto vas a recibir», RETIRADO.** e-PetPlace cobra 10 %, **pero el prestador asume además la comisión del motor de pagos y la bancaria**: el número prometía un neto que no es el que le llega. *Mejor no decirlo que fijar una expectativa errada — un número de plata equivocado no se corrige después: se corrige cuando cobra menos de lo que leyó.*
- **Al guardar vuelve a la portada** (`router.replace('/guarderia')`). *Quedarse en el formulario después de guardar deja al prestador sin saber si pasó algo* — y la portada es la que muestra el resultado, con su resumen recargado por `useFocusEffect`.

### Lo que el retiro del neto dejó muerto, y se retiró con él (Ley 37)

**La cadena entera:** el import de `VozComision`, `obtenerComisionVigenteCita`, el campo `comisionPct` del estado y su lectura en el `Promise.all`. *Un dato que ya no se muestra pero se sigue pidiendo es un viaje de red que nadie mira.*

**Respuesta a la pregunta de la mesa: `VozComision` NO queda huérfana** — la montan **paseo, grooming, adiestramiento y veterinaria**. Lo que sí quedó sin consumidor en `apps/` es **`FichaMensualidad`**: su único consumidor era esta pantalla. **Se declara para B**, no se toca — es su pieza.

**Verde:** typechecks prestador y cliente limpios · `verify:diseno` **VERDE, 60 reglas**.

---
---

# S107-C · TANDA 14 — «Próximamente» completo (29-ago-2026)

**Las cinco, en el orden firmado:** hotel · seguros · **wearables** · **certificaciones** · Prime.

⚠️ **Los nombres se midieron antes de escribirlos, y por poco:** la mesa los nombró **en singular** al avisar del merge (*«wearable y certificacion»*) y **en plural** al pedirlos. **El registry dice `wearables` y `certificaciones`** — plural, como el pedido original. *Con una unión tipada el error habría roto el build y no habría llegado lejos; con un slot libre habría pasado y la pantalla mostraría un ícono equivocado.* **Ésa es la razón por la que la unión sigue cerrada.**

🔴 **Los dos nuevos entran SIN bandera de `country_config`, y es deliberado:** no son servicios que un país encienda todavía — son hoja de ruta. *Inventarles un flag apagado sería fingir un interruptor que nadie puede tocar.* Cuando existan de verdad, entran por su bandera como sus hermanos, y ahí la línea cambia de forma.

**Verde:** typecheck cliente limpio · `verify:diseno` **VERDE, 60 reglas**.

---
---

# S107-C · TANDA 15 — la medición del label, el chevron y la respuesta del flag (29-ago-2026)

## 🔴 ① EL ANCHO REAL DEL LABEL — medido, y el resultado dice que el tamaño de fuente NO alcanza

**La cadena, leída del código:** ScrollView `paddingHorizontal: 16` · `ESTILO_GRILLA marginHorizontal: −8` · `ESTILO_CELDA paddingHorizontal: 8` · `Baldosa padding: 16` · canto `3`.
⇒ **útiles = (ancho − 16) / columnas − 51**

| ancho de pantalla | **3 columnas** | 2 columnas |
|---|---|---|
| 360 | **63,7 pt** | 121,0 |
| 390 | **73,7 pt** | 136,0 |
| 412 | **81,0 pt** | 147,0 |
| 430 | **87,0 pt** | 156,0 |

*(La propia `Baldosa` calcula **155 útiles** en su comentario para dos columnas — coincide con la fila de 430.)*

**Los labels, medidos:** `Paseo` 5 · `Grooming` 8 · `Guardería` 9 · `Veterinaria` 11 · **`Adiestramiento` 14**.

> ### 🔴 **`Adiestramiento` es UN SOLO PALABRA de 14 caracteres. A `cuerpo` (16 pt) mide ~116 pt; a `sm` (14) ~102; a `xs` (11) ~80. El espacio en un teléfono de 390 es 73,7.** **Ningún tamaño de la escala de la casa la hace entrar** — y como es una sola palabra, `numberOfLines={2}` no ayuda: **no hay dónde cortar.** `Veterinaria` (11) también se pasa en 360 y 390.

**⇒ Bajarle el tamaño no resuelve esto, y por eso no lo hice y lo traigo medido.** *Estimarlo habría producido un cambio que se ve como trabajo y deja el defecto igual.*

⚠️ **Y el tamaño no es mío:** el label lo dibuja `Baldosa` con `Texto variante="cuerpo"`, **dentro de `packages/ui`**. A diferencia del label de Explorar —que sí era del consumidor y por eso lo bajé—, éste es de B.

**Las tres salidas, con su costo, para que la mesa elija:**
1. **Volver a DOS columnas** — 136 pt útiles, entra todo con holgura. *Es lo que la pieza fue diseñada para hacer, y su comentario lo dice.* El costo es el que motivó el cambio: cinco baldosas dejan una sola en la última fila.
2. **Acortar el vocabulario del label** — decisión de producto, no de layout, y choca con Ley 17.2 (*los nombres van del lado del usuario*): el mundo se llama Adiestramiento.
3. **Pedirle a B que a `columnas={3}` el label baje de escala y admita corte de palabra** — es la única que conserva las cinco parejas, y es su pieza.

## ② EXPLORAR — la causa acá era otra, y la firma la resolvió

**No era el tamaño: era cuánta información cargaba la baldosa.** Con tres columnas entraban glifo + nombre + descripción + una llamada a la acción en ~100 pt.

☠️ **Fuera la descripción y fuera «Toca para entrar»**, que además *le explica a alguien que ya sabe tocar una tarjeta*. **Entra un chevron `›`**, que dice lo mismo ocupando una fila de nada.

🔴 **Y la accesibilidad se cubrió, porque el chevron no se anuncia:** sin texto, quien no ve la pantalla oiría *«botón»* y nada más. La etiqueta de la tarjeta pasa a **«Entrar a {servicio}»** — dice **a dónde entra**, que es lo que el texto retirado decía peor.

## ③ QUÉ HACE FALTA PARA ENCENDER GUARDERÍA — respuesta medida

> ### **Sólo el flag. `services_enabled.guarderia = true` en `country_config` para EC. Cero código.**

**Verificado contra la base, no supuesto:**

| lo que hace falta | estado |
|---|---|
| una guardería con oferta publicada | ✅ **Clínica Aurora**: oferta activa, **$12/día**, mensual $75, **1 espacio · 2 franjas · 1 paquete**, especies `[gato, perro]` |
| la ficha en el camino de los activos | ✅ ya vive ahí, gateada por `servicios.guarderia` |
| la ruta `/hogar/guarderia` y el lugar | ✅ montadas |
| el flag | 🔴 **`false`** — es lo único |

**Y corrí el cupo real de los próximos seis días para que la lista no sorprenda:**
`29-ago = no_opera · 30-ago = no_opera · 31-ago = elegible · 1-sep = elegible · 2-sep = elegible · 3-sep = elegible`

⚠️ **Los dos primeros son sábado y domingo**, y el espacio de Aurora opera **L-V**. **No es un defecto: es «ese día no abren», la voz que separamos ayer.** *Lo digo antes de que lo veas, para que no se lea como una lista vacía.*

**Verde:** typecheck cliente limpio · `verify:diseno` **VERDE, 60 reglas**.

---
---

# S107-C · TANDA 17 — la mudanza del flujo, y el freno de la Línea de Vida (29-ago-2026)

## ① LA MUDANZA — guardería ya tiene la forma de sus hermanas

| | antes | ahora |
|---|---|---|
| **el log** | 🔴 no existía | **`/hogar/guarderia.tsx`** — `Encabezado` · `FiltroMascotas` · vacío honesto · **CTA al pie** con `razonDeshabilitado` |
| **el flujo** | 🔴 en `/hogar/` y `/guarderia/` | **`/explorar/guarderia/`** — `index` · `[prestadorId]` · `checkout` |
| **la cabecera** | `Encabezado` de hub | **`CabezalOficio`**, con el glifo del oficio presidiendo |

**Y el log es MITAD INERTE declarada:** la lista **no se puede llenar** — el lector de la familia no existe (pedido a A). 🔴 **Su vacío dice la verdad y no finge:** *«todavía no podemos mostrarte tus estadías»*, **no** *«no tienes estadías»*. *Lo segundo sería mentir con cara de dato, y el día que el lector llegue nadie sabría que la pantalla estuvo mintiendo.*

⚠️ **Queda UNA diferencia estructural, declarada y no resuelta:** las hermanas parten el flujo en `index` (el cuándo) + `disponibles` (el quién); guardería lo tiene **junto en el `index`**, porque acá el día **es** lo que filtra a los lugares. *Puede estar bien y puede ser pereza mía — no lo decido solo.*

## ② 🔴 LA LÍNEA DE VIDA — PARÉ, COMO LA ORDEN DECÍA, Y EL DATO CORRIGE LA PREMISA

**La firma pide dos niveles:** sedimenta *«el inicio de la relación con un lugar y los incidentes»*; **el día común no**. Medí si el modelo los distingue.

**① No hay campo de relevancia.** `cat_tipos_evento` tiene `puede_ser_raiz` · `puede_ser_subevento` (**jerarquía**), `visibilidad_default` (**audiencia**), `propaga_a_perfil` (**propagación**), `es_clinico`, `es_mvp`. **Ninguno dice «rutina» contra «hecho de vida».**

**② Y la premisa no se sostiene contra el dato: los otros cuatro SÍ sedimentan la rutina.** Contado en `eventos_mascota` vivas:

| tipo | filas |
|---|---|
| **`cita_servicio`** | **219** |
| `hito_narrativo` | 57 (uno por mascota) |
| `vacuna_aplicada` | 32 |
| **`atencion_paseo_registrada`** | **29** |
| **`atencion_grooming_registrada`** | **7** |

> ### **Cada paseo y cada baño ya están en la línea de vida.** Un baño de rutina es tan rutina como un martes en la guardería — **la distinción que la firma quiere no existe hoy en ninguno de los cinco oficios.**

**⇒ No lo forcé, y ésta es la consecuencia que la mesa debería ver antes de decidir:** si guardería sedimentara sólo inicio e incidentes **sería el único oficio cuya rutina no aparece**, y la línea de vida de un animal que va tres veces por semana a la guardería **mostraría menos** que la de uno que pasea. *La firma es buena; lo que falta es que valga para los cinco.*

**Es pedido a A**, y son dos cosas separadas: **el campo de nivel en el catálogo** (o el criterio que lo reemplace) y **la decisión de qué hacen los otros cuatro con sus 255 eventos de rutina ya escritos.**

## ③ EL RIEL, POR SEGUNDA VEZ — y a la tercera es patrón

Ya van **dos tandas seguidas** en que el riel de claves tipadas caza **la misma clase de error**: *una clave cuyo nombre vive en dos namespaces* (`especiesTitulo`, después `paraQuien`). **A la tercera deja de ser accidente y es patrón**, y la cura no es cuidado: es **acotar siempre la búsqueda al bloque**, nunca al archivo. *Lo anoto así para no descubrirlo una tercera vez.*

**Verde:** typecheck cliente limpio · `verify:diseno` **VERDE, 60 reglas** · `R34` cuenta la decisión nueva del log y pasa.

---
---

# S107-C · TANDA 18 — el guion blando, la decisión de la grilla del cliente, y dos firmas registradas (29-ago-2026)

## ① EL GUION BLANDO — la cura que la medición de la tanda 15 pedía y yo no encontré

En la tanda 15 medí que **ningún tamaño de la escala hacía entrar `Adiestramiento`** en 73,7 pt, y ofrecí tres salidas: dos columnas · acortar el vocabulario · pedirle a B corte de palabra. **B midió la cuarta, y es la correcta: no es de la pieza, es de la CADENA.**

> **RN no hifena español, iOS no expone nada equivalente a `android_hyphenationFrequency`, y aunque lo expusiera, la pieza no sabe silabificar: dónde parte una palabra es propiedad del IDIOMA, no del layout.**

**Aplicado (U+00AD, invisible salvo que el renglón corte ahí):**
- **prestador** — `Adiestra­miento` · `Vete­ri­na­ria`
- **cliente** — `Esté­tica y baño` · `Adiestra­miento` · `Vete­ri­na­ria`

⚠️ **Va con su comentario al lado en los dos diccionarios**, porque **es invisible en el editor**: *quien edite la cadena lo borra sin saber que estaba, y nada avisa.*

🔴 **Y un borde que cuidé: el guion se QUITA de la etiqueta accesible.** Sirve para partir un renglón, y ahí no hay renglón que partir. *La mayoría de los lectores lo ignora — pero «la mayoría» no es garantía cuando asegurarlo cuesta un `replace`.*

## ② LA GRILLA DEL CLIENTE — decidí la cura, no el montaje, y declaro por qué

B midió que **la grilla del cliente no monta `Baldosa`**: la dibuja a mano, así que **la cura de altura que B hizo en la pieza no la alcanza**. Su pedido lo dice bien: *montar la pieza ahí es **decisión**, no arreglo.*

**Mi decisión: aplico la misma cura (el guion blando) y NO monto la pieza.** Las razones, en orden:
1. **La anatomía del cliente ya no es la de `Baldosa`**: perdió su descripción y su chevron por firma del founder. Montar la pieza **reabriría dos decisiones recién firmadas**.
2. **El guion resuelve el síntoma medido** —«Estética y baño» partiendo mal— **sin tocar una anatomía firmada.**
3. *Unificar dos baldosas que hoy dicen cosas distintas es trabajo de diseño con su propia vara, no una consecuencia de arreglar un corte de palabra.*

⚠️ **Queda declarado como decisión abierta de mesa**, con el número de B: **es la tercera baldosa a mano de la casa**, y esa divergencia es la que 19.9 predice.

## ③ DOS FIRMAS QUE LA MESA CORRIGIÓ CON MI MEDICIÓN — registradas

**① Guardería sedimenta CADA ESTADÍA**, como sus hermanas. *La firma anterior pedía dos niveles; mi medición la dio vuelta: los otros cuatro ya sedimentan la rutina (219 · 29 · 7), y guardería siendo el único que no haría que un animal que va tres veces por semana mostrara MENOS que uno que pasea.* **La distinción rutina/hecho-de-vida no se estrena en el oficio nuevo** — va a A como pedido separado, sobre los cinco.
🔴 **Nada de esto es mío de construir:** el evento nace en el servidor, al entregar, como el paseo.

**② La diferencia estructural del flujo queda ADOPTADA, y no era pereza:** guardería tiene el CUÁNDO y el QUIÉN en una sola pantalla porque **el día es lo que filtra a los lugares**. *Partirlo en `index` + `disponibles` inventaría un paso: la familia elegiría el día, tocaría «siguiente», y vería la misma lista que ya podía ver.* **Sus hermanas lo parten porque ahí el QUIÉN depende de una hora que todavía no está elegida; acá no.**

**Verde:** typechecks prestador y cliente limpios · `verify:diseno` **VERDE, 60 reglas**.

---
---

# S107-C · TRASPASO — para retomar después de compactar (29-ago-2026)

> **Lo que sigue es el estado, no la historia.** Quien retome abre por acá y **no reconstruye de memoria**: cada dato vivo se re-lee del objeto al usarlo (L-166), y **el número libre de `D-NNN` se verifica por grep contra `DEUDAS_CANONICAS.md`, jamás de este documento.**

## ⓪ IDENTIDAD Y ESTADO, MEDIDO AL CERRAR

| | |
|---|---|
| **soy** | pista **C** de S107 — las superficies en las dos apps |
| **worktree / rama** | `../e-petplace-s107-c` · `pista/s107-c` |
| **árbol** | limpio |
| **commits fuera de `main`** | **0** — A mergeó todo |
| **rama en origin** | ✅ `ad94999a`, **igual al local**, verificado con `git ls-remote` **por SHA** (L-239) |
| **gates al cerrar** | typechecks prestador y cliente **limpios** · `verify:diseno` **VERDE, 60 reglas** |

**Territorio:** `apps/cliente` y `apps/prestador`. **No son míos:** `packages/ui` (B) · `packages/api`, `supabase/`, `docs/contratos/`, `DEUDAS_CANONICAS.md` (A).

---

## ① LA COLA VIVA — en orden, con el próximo paso ejecutable

| # | qué | estado |
|---|---|---|
| 1 | **El LOG del hub** — llenar la lista de estadías | 🔴 **bloqueado**: no existe el lector de la familia. Mi mitad está construida e inerte, con vacío honesto y CTA al pie. **Pedido:** `docs/loop/S107-C-PEDIDO-A-A-LOG-FAMILIA.md` |
| 2 | **El acta (⑤)** — su superficie en las dos apps | 🔴 no arrancó. Contrato publicado: `docs/contratos/s107-contrato-documentos-y-actas.md` |
| 3 | **La aceptación de documentos** (cliente) | 🔴 bloqueado: faltan `obtenerDocumentosGuarderia` · `aceptarDocumentosGuarderia` · `evaluarDocumentosGuarderia`. **Pedido con dos huecos:** `S107-C-PEDIDO-A-A-DOCUMENTOS.md` |
| 4 | **Marcar «a bordo» / «entregado»** en el día del prestador | 🔴 bloqueado: los cuatro wrappers de acción no existen; llegan con el acta |
| 5 | **El módulo de media del durante** | de **D**, no mío |

**Próximo paso ejecutable si A publica el lector de la familia:** en `apps/cliente/src/app/(tabs)/hogar/guarderia.tsx`, reemplazar el `EstadoVacio` de «listaPendiente» por `FiltroPills` (próximos/historial) + filas `FilaCita`. **El esqueleto y los chips ya están montados.**

---

## ② LO QUE ESPERA FIRMA O DECISIÓN DE MESA — no lo decido yo

1. **Montar `Baldosa` en la grilla del cliente.** Hoy la dibuja a mano — **es la tercera baldosa a mano de la casa** (medición de B). Apliqué el guion blando como cura; **montar la pieza reabriría el chevron y la descripción, que están recién firmados.**
2. **El nivel de relevancia en la Línea de Vida** — no existe para ninguno de los cinco oficios, y los otros cuatro ya sedimentan rutina (**219 · 29 · 7**). Va a A **sobre los cinco**, no sobre guardería.
3. **`FichaMensualidad`** quedó sin consumidor en `apps/` — B ya la retiró de `packages/ui`.

---

## ③ FRENOS Y SUPUESTOS VIVOS

- 🔴 **El flag `guarderia` de `country_config`**: medido en `false` la última vez. **Encenderlo es acto de mesa** y ya hay una guardería completa (**Clínica Aurora**, $12/día, 1 espacio · 2 franjas · 1 paquete). ⚠️ **Re-medir antes de afirmar**: pudo encenderse después.
- ⚠️ **El gate sanitario nace APAGADO** (`bloquea: false`) y la pantalla lo refleja sin decidirlo. **La misma perilla la lee `reservar_dia_guarderia`.**
- ⚠️ **`L-138` rige**: verifiqué el repo y el repo estaba bien, pero **no puedo verificar qué bundle tiene el teléfono**. Ante una contradicción con el aparato, **el discriminador es el commit, no la discusión**.
- ⚠️ **`L-432` (mía)**: comparar mi rama contra `main` responde *qué cambié yo*, **no qué puede recibir el teléfono**. La pregunta correcta es si existe binario para el runtime que `app.json` declara.

---

## ④ EL PATRÓN QUE YA COBRÉ DOS VECES — para no llegar a la tercera

**El riel de claves tipadas me cazó en dos tandas seguidas, con la misma clase de error: una clave cuyo nombre vive en DOS namespaces** (`especiesTitulo`, después `paraQuien` — que se llevó la de veterinaria en vez de la de guardería).

> **La cura no es cuidado: es acotar SIEMPRE la búsqueda al bloque del namespace, nunca al archivo.** A la tercera deja de ser accidente y es patrón.

---

## ⑤ LO QUE ESTA PISTA APRENDIÓ Y CONVIENE NO RE-DESCUBRIR

- **La casa tiene un módulo de piezas de reserva** —`CabezalOficio` · `FiltroMascotas` · `SelectorDia` · `GrillaElegir` · `DiaSinHorarios` · `PieReserva`— y **guardería montaba CERO de las seis**. *Antes de construir una pantalla nueva: censar qué monta la equivalente que ya funciona.*
- **Cada oficio vive en DOS sitios**: `/hogar/<oficio>` = el **log** · `/explorar/<oficio>/` = el **flujo**. *Poner el flujo en el lugar del log fue el defecto raíz de toda esta sesión, y era invisible sin medir.*
- **Medir antes de afirmar dio vuelta tres premisas de la mesa** en esta sesión: `D-920` (ya resuelta), el cupo por día (había molde vivo), y la sedimentación de la rutina (los otros cuatro ya la sedimentan). **Ninguna se habría visto sin contar filas.**

---

## TANDA 19 · EL FLUJO EN DOS ETAPAS Y EL DURANTE DEL DUEÑO *(29-ago-2026)*

**Encargo:** el flujo de reserva del cliente **en dos etapas** + **el durante del dueño**, con
**censo de criterios primero**.

### ⓪ El censo pagó, y no en piezas: en criterios ya escritos

Seis preguntas de este flujo **ya estaban contestadas en la casa**, y ninguna se re-decidió:
la forma del selector de modalidad (`SelectorSegmentado`, la misma de Baño / Baño y corte) ·
qué precio va debajo (uno solo, bajo el riel, del server) · cómo se muestra la mascota sin
re-preguntarla (`detalle` del cabezal, con el `FiltroMascotas` **sobreviviendo guardado**) ·
cómo se dice el saldo (**el singular tiene key propia**) · cómo se agenda contra saldo (Hoja
parejo-parejo, y **saltea el checkout entero**) · y **qué comprobante ve una reserva sin cobro**
—*la pregunta explícita del founder*—: **un toast que nombra el saldo restante + Go home**, y
el rastro es la fila del hub **marcada con su origen**. *No hay pantalla de confirmación, y
está bien: el paseo ya resolvió que no hace falta.*

### ① Lo construido

- **Etapa 1** (`explorar/guarderia/index.tsx`, nueva) y **etapa 2** (`disponibles.tsx`, era el
  `index`). El rótulo del día **habla por modalidad**; el tamaño 5·10·15 con la forma de los
  presets del paseo; el precio de cada lugar **del campo de su modalidad, sin una cuenta**.
- **El durante** (`app/guarderia/[estadiaId].tsx`): media **viva**, estadía con **enchufe
  nombrado**, y el punto vivo montado.

### ② Lo que revoqué de mí mismo, y por qué se dice

El acta de traspaso §⑥① declaraba que guardería **no** se partía en `index`+`disponibles`.
**Era cierto mientras el primer filtro era el día.** La firma de la modalidad puso otro filtro
antes y la primera pantalla dejó de repetir nada. *La razón vieja no era mala: se le movió el
piso.* **Queda escrito en la cabecera del archivo, no sólo acá.**

### ③ Mi error del día, y lo corrigió el founder

Escribí **«las seis voces del calendario»** en el recorrido y en el traspaso. **Son CINCO**
—`sobrevendido` es un booleano aparte y «sin fila» es una ausencia— **y el contrato de A ya lo
había medido y dicho con todas las letras**. *Repetí un número sin medirlo, que es justo contra
lo que ese contrato advertía: «un contrato que repite un número sin medirlo lo vuelve
permanente».* Corregido en los dos documentos.

### ④ Los dos hallazgos que van a A

- ⏪ **[VENCIDO — ver TANDA 23]** ~~`guarderia_tramos` NO EXISTE.~~ `obtenerPuntoVivo`/`registrarPuntoVivo` funcionan, pero
  **nadie puede producir ni obtener un `tramoId`**: no hay tabla, el `tramo_id` no tiene FK y la
  estadía no tiene columna. **`L-318` un piso más adentro** — *lo que no tiene productor es el
  identificador con el que la pieza abre*, y no falla: **devuelve `null`, que se lee como un
  estado normal.**
- 🔴 **Paquete y mensual no tienen cómo cobrarse**, y el filtro **todavía no filtra**.

### ⑤ Lo que un gate encontró y yo no

**R55** cazó un inset cobrado dos veces en el durante (`Encabezado` ya paga el tope). *La regla
existía justo para eso.*

### ⑥ Estado de gates

`tsc` cliente **limpio** · `verify:diseno` **VERDE, 60 reglas**.
⚠️ `verify-i18n.mjs` **no se pudo correr: exige Metro vivo** (es de runtime). La paridad es↔en
la sostiene el typecheck con `Espejo<D>`, que está verde. **Se declara, no se omite.**

---

## TANDA 20 · EL RECORRIDO PROPIO, LA ALCANZABILIDAD Y `D-967` *(29-ago-2026)*

### ① EL RECORRIDO — `scripts/s107/recorrido-guarderia.mjs`

Corre **siete rutas** con playwright sobre la app web, y **declara en su cabecera qué NO mide**:
corre **sin sesión**, así que una pantalla que ahí pinta su vacío honesto **podría pintar otra
cosa con datos de Aurora**. *Un verde de este script no es un gate del founder.*

**Lo que confirmó vivo:** ✅ la redirección de **N=1** (`/explorar/guarderia` →
`disponibles?modalidad=dia`) · ✅ Explorar con **los cinco oficios** y los cinco «Próximamente» ·
✅ las **voces honestas de fallo** en las cuatro pantallas (*«no es que no tengas: no pudimos
preguntar»*).

**⚠️ La primera corrida fue ciega y casi la reporto como verde flojo:** sin
`EXPO_PUBLIC_SUPABASE_*` la app **no inicia la API**, todas las pantallas quedaban en esqueleto
y `innerText` salía vacío. *Seis rutas «rotas» que eran el arnés, no el producto.* Se relanzó
con la `anon` inyectada por entorno.

### ② 🔴 DOS DEFECTOS MÍOS, y los dos se veían SOLO en el texto renderizado

| | qué pasaba | cura |
|---|---|---|
| **«Su día» dos veces** | el cabezal del durante caía a `duranteGuarderia.titulo` = *«Su día»* y **la sección de fotos rotula lo mismo** | el cabezal cae a **«Guardería»**; la clave huérfana **muere** (Ley 37) |
| **el vacío prometía lo que otra sección no podía cumplir** | *«Sus fotos sí las tienes acá abajo»* — y abajo decía *«no pudimos traer sus fotos»*, **en la misma vista** | el vacío **habla sólo de lo suyo** |

*Ninguno de los dos lo ve un typecheck ni un lint: **son incoherencias entre dos estados que
sólo coinciden en pantalla**.*

### ③ 🔴 ALCANZABILIDAD — el riesgo gemelo, y aparecieron dos casos

**Medido ruta por ruta.** La cadena del flujo está **entera**:
`hogar/guarderia` → `/explorar/guarderia` → `disponibles` → `[prestadorId]` → `checkout`.

- 🔴 **GUARDERÍA NO ESTÁ EN EL RAIL «TUS SERVICIOS» DEL HOGAR.** Sus cuatro hermanos sí
  (`paseo` · `estetica` · `adiestramiento` · `veterinaria`). **Se alcanza sólo por la baldosa de
  Explorar** ⇒ *una familia que ya la usó la busca donde viven las otras cuatro y no la
  encuentra.* **La causa es de contrato:** `ResumenServiciosHogar` **tiene cuatro servicios y
  ninguno es guardería** ⇒ **va al mismo pedido a A** (el rail se alimenta de ahí, y su regla es
  «cero actividad = cero celda»: sin lector no hay actividad que mostrar).
- 🟠 **El durante no tiene entrada, y es a propósito** — nace con el lector, en la misma línea.

### ④ `D-967`, LA MITAD DEL CLIENTE — respuesta con su medición

**Las dos mitades son actos distintos, y confundirlos es lo que hace esperar por los dos.**

- **Portar es seguro HOY, medido:** la sonda usa `requireOptionalNativeModule`, que devuelve
  `null` cuando el módulo no está ⇒ **importarla en un APK que no la lleva no rompe nada**.
- **Flipear NO es seguro hoy:** en el binario de cliente instalado la sonda daría `null`,
  `null` cuenta como NO (fail-closed) ⇒ **apagaría el mapa en una app donde funciona**.
- ⚠️ **Y la salida tentadora es la trampa:** *«si no puedo preguntar, caigo al literal»* es
  **fail-OPEN** con mejor nombre — deshace exactamente lo que la ficha restituyó.

> ### ⇒ **Portar ya, inerte. El próximo binario del cliente es el disparo del FLIP, no del PORT.**
> *Si el port espera al binario, se vuelve un paso que alguien tiene que recordar en el peor
> momento — y esta deuda existe justamente porque su cura no puede depender de que alguien
> recuerde algo.* **Portado hoy, el próximo binario lo lleva solo y el flip queda a una línea
> con condición verificable** (`verify-manifest-apk` sobre el APK nuevo).

### ⑤ 🔴 UN HALLAZGO DE SEGURIDAD, DE PASO

**`npx supabase projects api-keys` imprime la `service_role` EN CLARO**, junto a la `anon`.
La corrí para levantar el arnés y **la clave quedó en la salida de una terminal**. **No se
copió a ningún archivo, reporte ni documento** — y se declara porque *los artefactos de una
verificación son un vector nuevo* (`D-712`): esa salida no puede terminar en un log de CI, un
pegado a un chat ni un reporte. **Para levantar la app alcanza la `anon`, y hay forma de
extraerla sin imprimir la otra** (está en el propio script de arranque de esta tanda).

---

## TANDA 21 · LAS TRES FIRMAS *(29-ago-2026)*

### ① `D-967` · LA SONDA, PORTADA E INERTE

`apps/cliente/modules/sonda-manifest` existe. **Copiado byte-idéntico** del prestador
(`diff -r` en cero) — **y no se le agregó ni una nota adentro a propósito**: *así la
divergencia entre las dos copias se caza con un `diff`, y un comentario de más volvería ruidosa
la única señal que tenemos.* La nota del port vive en `apps/cliente/src/lib/mapa-nativo.ts`,
que es donde alguien la va a leer.

**El flip NO se hizo**, y el archivo lo dice con su tabla: portar es seguro hoy
(`requireOptionalNativeModule` devuelve `null` si falta), flipear apagaría el mapa en la app
instalada. **Y la trampa quedó escrita:** *«si la sonda devuelve `null`, caigo al literal»* es
**fail-OPEN con mejor nombre**, y su modo de falla es el peor —la app no degrada, muere en hilo
nativo—. El flip está escrito para que sea **una línea**, con su verificación nombrada
(`verify-manifest-apk` **sobre ESE APK**, no sobre el recuerdo de otro).

### ② EL ARRANQUE, CON SU AVISO — `scripts/s107/levantar-cliente-web.sh`

Levanta la web con la API viva, y **lleva escrito por qué existe**: sin las env vars la app no
llama a `initApi()` y un recorrido automático reporta **seis rutas «rotas» que son el arnés**.

🔴 **Y el aviso que la próxima persona no podía saber:** `npx supabase projects api-keys`
**imprime la `service_role` en claro**. *Para levantar la app alcanza la `anon`*, y el script la
extrae **sin que la otra pase por pantalla**. **La `service_role` no hace falta para nada de
esto.**

### ③ 🔴 LA CLASE DE DEFECTO QUE NINGÚN GATE VE — anotada por orden de la mesa

> ### **LA INCOHERENCIA ENTRE DOS ESTADOS QUE SÓLO COINCIDEN EN PANTALLA.**

Cada mitad es **correcta por separado**. El defecto **no existe en ninguna de las dos**: nace
del hecho de que las dos se muestran **al mismo tiempo**. ⇒ *ningún typecheck, lint ni test de
unidad puede verlo, porque no hay una línea que esté mal.* **Sólo aparece leyendo la pantalla
renderizada, entera, en un estado concreto.**

**Los dos casos del 29-ago, y el segundo es el peor:**

| caso | por qué ningún gate lo ve |
|---|---|
| **«Su día» dos veces** (cabezal + sección) | las dos cadenas son correctas y viven en archivos distintos; **nada las compara** |
| 🔴 **el vacío prometía lo que la otra sección no pudo cumplir** — *«sus fotos sí las tienes acá abajo»* con *«no pudimos traer sus fotos»* debajo | **cada estado es honesto por su cuenta**; juntos, la pantalla **miente**. Y miente en el peor momento: cuando la familia ya está preocupada porque el estado falló |

**La regla que deja, para la próxima superficie con varios bloques:**

> **Un estado vacío habla SÓLO de lo suyo.** En cuanto una voz afirma algo sobre **otra**
> sección, se vuelve una promesa que esa sección puede no poder cumplir — *y el vacío es
> justamente el momento en que las otras secciones también están fallando.*

**Cómo se cazan, ya que no hay gate:** **recorrer la pantalla y leer su texto completo** en cada
estado —vacío, error, cargando, poblado—. El instrumento de esta tanda lo hace: imprime
`document.body.innerText` entero, y **ahí las dos frases aparecen juntas y el defecto salta.**

---

## TANDA 22 · LO QUE LA MESA FIRMÓ *(29-ago-2026)*

**Cuatro firmas, y ninguna abre trabajo nuevo — cierran decisiones que estaban en el aire.**

| | firma |
|---|---|
| **el mensaje de causa** | ✅ **va ENCIMA del botón**, con su razón: `PieReserva` es fijo al borde inferior y debajo no hay dónde vivir. *Cumple lo que la firma perseguía —visible sin tocarlo— y no vale romper el pie fijo por la letra.* **Escrito en la cabecera del archivo**, no sólo acá |
| **el tuteo** | ✅ correcto: `R66` es trinquete solo-baja y **los dictados de mesa viajan en tuteo** (`L-148`). *Los mensajes vinieron en voseo de mesa; en producto van convertidos* |
| **el vacío firmado** | ✅ **no se dice antes de tener el lector** — *decirlo hoy sería mentir con cara de dato, y el día que llegue nadie sabría que la pantalla estuvo mintiendo* |
| **los dos huecos del hub** | ✅ `especie_sin_oferta` y el paquete con saldo **quedan inertes declarados hasta que exista con qué preguntar. No se inventan.** |

### 🔴 POR QUÉ LA PRIMERA SE ESCRIBIÓ EN EL CÓDIGO Y NO SÓLO EN EL PARTE

*Una divergencia firmada que vive únicamente en el reporte de quien la propuso **vuelve como
«defecto» en el próximo gate**, y el que la encuentre no va a tener con qué defenderla.* Es la
misma clase que el canon ya pagó dos veces con letras firmadas que se contradecían: **la razón
tiene que vivir donde alguien la va a leer al tocarlo.**

---

## TANDA 23 · EL DATO VENCIDO, EL LECTOR CONECTADO Y EL RECORRIDO CON SESIÓN *(29-ago-2026)*

### ① ⏪ MI DATO ESTABA VENCIDO — `guarderia_tramos` SÍ EXISTE

Afirmé en **cinco lugares** que la tabla no existía y que el punto vivo era inalcanzable.
**Falso:** A la creó hace varias tandas y **en el mismo acto curó una fuga que el hueco tapaba**
— `obtener_punto_vivo` sólo pedía `auth.uid()`, así que **cualquier logueado con un `tramo_id`
obtenía la ubicación en vivo de un vehículo**.

🔴 **Y la forma importa más que la existencia:** el tramo es **del VIAJE, no de la estadía**
(`prestador_id, fecha, direccion` — **sin `estadia_id`**), y cada estadía apunta con
`tramo_recogida_id` / `tramo_devolucion_id`. *Un tramo por estadía haría que el mismo vehículo
emitiera **N puntos idénticos**: no fallaría — multiplicaría la misma verdad y la volvería N
verdades que hay que mantener de acuerdo.*

> **`L-166` en su forma más cara: un dato medido no es un dato vigente — y una afirmación
> ESTRUCTURAL vencida es peor que una ausencia, porque el que la lee construye contra ella.**

**Corregido en los cinco, marcando y no borrando:** *un hallazgo que afirmó de más y desaparece
deja a quien lo leyó creyendo lo viejo.*

### ② EL LECTOR CONECTADO — `LISTA_DISPONIBLE` murió con su razón

A publicó `obtenerMisEstadiasGuarderia` y el enchufe **fue una línea, como estaba previsto**.
Con él entraron **la lista del log** y **la entrada al durante** (sólo cuando hay `estadiaId`:
*una cita comprada que el prestador no ejecutó no tiene durante que mirar*).
☠️ También murió el `String(codigo)`: **A tipó `fecha_no_ofertable` y otros 16.**

⏪ **[VENCIDO — ver TANDA 28: B lo resolvió y el log ya monta `FilaCita`]** ~~Y una pieza que no
alcanzó:~~ `FilaCitaOficio` era `paseo | grooming | veterinaria |
adiestramiento` — **no conoce guardería**. *Montarla con otro oficio pintaría el canto de un
servicio ajeno; forzar el tipo sería mentirle al compilador para que la pantalla mienta en el
color.* ⇒ **`Celda` con la razón escrita** + pedido a B. **Hasta que entre, esta fila no se ve
igual que la de sus hermanas.**

### ③ 🔴 EL RECORRIDO CON SESIÓN REAL — dos defectos que el founder no va a tener que encontrar

| | qué pasaba | por qué ningún gate lo veía |
|---|---|---|
| **guardería NO estaba en el rail del Hogar** | A publicó su rama del resumen **y no tenía consumidor**: el rail pintaba CUATRO. *Una familia que ya la usó la buscaba donde viven sus hermanas y no la encontraba* | **es «motor sin puerta» del lado de la superficie**: el dato llegaba y nadie lo leía. Compila perfecto |
| 🔴 **el log decía «Sin estadías agendadas» sin que hubiera mascota elegida** | con tres mascotas y ninguna tocada, el vacío **afirmaba sobre algo que nadie preguntó** | **es la clase que anoté ayer**: cada mitad correcta —el vacío dice bien lo suyo, el filtro también— y **el defecto nace de mostrarlas juntas** |

*El segundo lo encontré porque el recorrido imprime el texto entero de la pantalla. Leyendo el
código nunca hubiera aparecido: no hay una línea que esté mal.*

### ④ EL RECORRIDO, Y SUS CONDICIONES

`scripts/s107/recorrido-guarderia-con-sesion.mjs`. **Su cabecera declara qué NO mide** —nada
visual, ninguna escritura, ningún camino nativo, un solo aparato— y **por qué su verde no es un
gate**. **Es de SOLO LECTURA por construcción: no hay un solo `click` en el archivo**, así que
no hace falta subtransacción — *no hay nada que deshacer.* La clave de prueba **sale del
keychain al momento** y no se imprime; **`service_role` no se usa ni se pide.**

### ⑤ LO QUE ENCONTRÉ Y **NO** CURÉ, porque es decisión de copy

El log dice **«Elige de quién»** en el cuerpo **y «Elige de quién primero» en el botón** — la
misma instrucción dos veces en una pantalla. *Es la misma clase del §③, encontrada por el mismo
método.* **No lo toco solo** porque la etiqueta del botón sale de una key COMPARTIDA
(`plan.agendarFaltaMascota`) y cambiarla toca otras superficies. **Voto: que el cuerpo instruya
y el botón lleve la acción, con la razón en su `razonDeshabilitado`.**

---

## TANDA 24 · TRES FIRMAS, DOS EN EL CÓDIGO *(29-ago-2026)*

### ① EL BOTÓN DE CONFORMAR QUE FALTA A PROPÓSITO — firmado, y **escrito donde para**

La razón ya estaba en el archivo; se reescribió **como firma y no como observación**, con su
consecuencia explícita: *«si llegaste acá viendo un hueco donde debería haber una acción: **el
hueco es la decisión**»*.

> **La conformidad existe porque el dueño VIO lo que firma.** Un botón «conforme» sobre un acta
> que no se puede leer **no es una función a medias: es pedirle a alguien que firme a ciegas.**

*El parte no alcanzaba: en tres meses, alguien que abra este archivo y vea un botón faltante lo
va a «completar» — y el único que puede frenarlo es el comentario que tiene delante.* **Misma
razón por la que la divergencia del pie se escribió en su archivo y no sólo acá.**

### ② EL TEXTO DUPLICADO — el cuerpo instruye, el botón acciona

Sin mascota elegida el botón decía **«Elige de quién primero»** mientras el vacío decía **«Elige
de quién»**. *Cada mitad correcta por su cuenta; el defecto nacía de mostrarlas juntas.*

**Rótulo propio de esta pantalla** (`logGuarderia.reservar` → *«Reservar una estadía»*):
`plan.agendarFaltaMascota` es **compartida** y cambiarla movería la voz de superficies ajenas.
✅ **No hizo falta tocar la pieza:** el `razonDeshabilitado` de `Boton` ya existía justo para
esto — *la instrucción sigue disponible al tocar, y deja de competir con el cuerpo.*

**Verificado con sesión real:** *«Choose who · Tap one of your pets to see their stays. · Book a
stay»*.

### ③ EL LECTOR DEL ACTA Y LOS DOS `tramo_id` — van a A, no se montan

`S107-C-PEDIDO-A-A-ACTA-Y-TRAMOS.md`. **Nada se construye contra la puerta saltada.**

### 📌 LA REGLA QUE DEJAN LAS DOS PRIMERAS, y ya cobró tres veces

> **Una decisión que se ve como una ausencia tiene que estar escrita EN el lugar donde se ve la
> ausencia.** Un pie que no se rompió, un botón que no se montó, un vacío que no se llenó:
> **desde afuera los tres se leen como trabajo sin terminar.** *El parte lo explica a quien lo
> lea; el código lo explica a quien lo toque — y el que lo toca casi nunca leyó el parte.*

---

## TANDA 25 · EL ACTA CON SU BOTÓN Y EL MAPA ENCENDIDO *(29-ago-2026)*

**A publicó las dos en el mismo commit, y las dos condiciones que yo había escrito se
cumplieron solas** — que es para lo que estaban escritas.

### ① EL BOTÓN DE CONFORMAR NACIÓ CON SU LECTOR

*La condición vivía en el propio archivo: «el botón nace CON el lector del contenido, no
antes».* **Llegó el lector, nació el botón.** *Una condición escrita donde se ve la ausencia no
sólo frena a quien la quiera «completar»: también dice cuándo deja de frenar.*

### ② 🔴 EL MAPEO DE CONFORMIDAD ESCONDÍA UN DEFECTO SILENCIOSO

**`sin_conformidad` existe en los DOS vocabularios con sentidos opuestos:**

| motor | significa | pieza |
|---|---|---|
| `sin_conformidad` | **todavía no la miró** | `pendiente` — sereno |
| `conforme` | aceptó | `conforme` — success |
| `con_reserva` | aceptó **señalando algo** | `sin_conformidad` — **warning** |

*Pasarlo directo —que es lo que hace cualquiera que vea dos enums con la misma palabra— habría
pintado un **warning sobre un dueño que simplemente no abrió el acta**. Y el warning existe para
decir que alguien señaló un problema: el estado más normal se habría visto como el más grave.*

**Lo cazó comparar los dos vocabularios en vez de confiar en que la palabra igual significa lo
mismo.**

### ③ LAS DOS HORAS, SIEMPRE — firma de A, y su razón es de producto

`cerradaEn` es **la hora de la puerta**; `recibidaEn`, cuándo llegó al servidor. **Se muestran
las dos** porque *la diferencia entre ellas es la cola offline: esconderla haría que un acta
levantada SIN SEÑAL parezca levantada tarde* — y eso es una acusación silenciosa a un cuidador
que hizo su trabajo bien.

### ④ EL LECTOR DEVUELVE HECHOS, NO VOZ

Los ítems del checklist **se componen en la pantalla** con el idioma de la casa: el motor manda
`carnetVerificado: boolean` y `objetos: string`, y *«Carnet a la vista»* es una decisión de la
letra, no del motor.

### ⑤ Y UN VOSEO PROPIO, cazado por mí y no por el gate

Escribí *«¿Qué querés que quede anotado?»* — **la regla que vengo aplicando en cadenas ajenas,
rota en la mía.** Curado antes de commitear. *`R66` lo habría atrapado igual; lo que no habría
atrapado es la ironía.*

---

## TANDA 26 · QUÉ VE EL FOUNDER EN LA PANTALLA DE MODALIDAD *(29-ago-2026)*

**La pregunta era *«¿es la compuerta o falta trabajo?»*, y no se podía contestar leyendo
código.** Se midió con `scripts/s107/sonda-secuencia-modalidad.mjs`, con sesión real y Thor.

### LA RESPUESTA: **ES LA COMPUERTA. NO FALTA TRABAJO.**

| lo que ve | por qué |
|---|---|
| **no hay selector Día · Paquete · Mensual** | **`MODALIDADES_ABIERTAS = ['dia']`** ⇒ **N=1 colapsa y el selector no se dibuja** — *«con un turno nadie ve la palabra»*. Es la regla de la casa, no una pieza sin montar |
| **no hay precio ni botón al abrir** | **revelación progresiva, firmada**: *nada se muestra hasta que elija*. **Aparecen al tocar un día** — medido: **`$ 12.00` + `See who can`** |

⇒ **La pantalla funciona de punta a punta.** Lo que se lee como *«casi toda pendiente»* son
**dos ausencias deliberadas**, y las dos se llenan solas: una con el aviso de A sobre paquete y
mensualidad, la otra con un toque.

### ⚠️ LA ÚNICA COSA QUE SÍ SE ADELANTA, y la declaro sin cambiarla sola

**Los requisitos aparecen ANTES de elegir el día.** Con una sola modalidad, `listoParaDia` es
verdadero desde el arranque. *No es un defecto —los requisitos son de la MASCOTA, no del día, y
verlos temprano deja arreglar el carnet mientras se elige— pero rompe el ritmo de «cada paso
aparece cuando el anterior se contestó», y probablemente suma a la sensación de pantalla a
medio hacer.* **Una línea si la mesa quiere el ritmo estricto.**

### 🔴 LA LECCIÓN DEL INSTRUMENTO, que casi me hace reportar mal

La primera corrida tocaba un nodo `«mon 31»` **que no existe**: `SelectorDia` pinta el día y el
número por separado. **El toque no pasaba, la pantalla no cambiaba, y eso se leía exactamente
igual que un defecto de la pantalla.**

> **Un instrumento que no puede distinguir «no pasó nada» de «no hice nada» no mide: adivina.**

Se le agregó el discriminador —imprime si encontró el nodo **antes** de tocarlo— y ahí la
respuesta cambió de signo. *Es la misma clase que el arnés ciego de la tanda 20: la segunda vez
en esta pista que un instrumento sin control iba a producir un hallazgo falso.*

---

## TANDA 27 · EL RITMO ESTRICTO, LA VITRINA, Y UNA CLASE HECHA MECANISMO *(29-ago-2026)*

### ① LOS REQUISITOS, DESPUÉS DEL DÍA — y la firma gana contra un argumento correcto

Aparecían al abrir la pantalla (con una modalidad, `listoParaDia` es verdadero desde el
arranque). **Ahora esperan al día.**

🔴 **El argumento en contra era bueno y por eso queda escrito en el archivo:** los requisitos son
de la MASCOTA, no del día, y verlos temprano dejaría arreglar el carnet mientras se elige.
**Ganó la firma**, y su razón es más fuerte: *el ritmo es lo que le dice a la familia que la
pantalla va paso a paso — y una excepción bien razonada en medio de una secuencia la vuelve una
pantalla que a veces se adelanta.*

**Verificado:** antes del día, sólo la tira; después, requisitos · `$ 12.00` · «See who can».

### ② LA VITRINA — adoptada como se resolvió

`PreviewPrestador` con un slot `pie` para lo propio del oficio. **Y la razón del slot es la que
la mesa firmó:** *si la pieza aprendiera qué es una «ventana de recogida», el próximo oficio le
agregaría lo suyo y en tres sería un formulario con banderas.*

---

## 📌 CLASE · **UN INSTRUMENTO QUE NO PUEDE DISTINGUIR «NO PASÓ NADA» DE «NO HICE NADA» NO MIDE: ADIVINA**

**No es una anécdota: es una clase, y ya cobró DOS veces en esta pista.**

| # | el instrumento | lo que iba a reportar | lo que pasaba de verdad |
|---|---|---|---|
| **1** | el arnés sin las env vars | *«seis rutas rotas»* | la app **no llamaba a `initApi()`**: todas las pantallas en esqueleto |
| **2** | la sonda tocando `«mon 31»` | *«elegir un día no hace nada»* | **el nodo no existe** — `SelectorDia` pinta día y número por separado; **el toque nunca se emitió** |

### Qué las hace peligrosas y no molestas

**Las dos producen una salida CREÍBLE.** No hay excepción, no hay error, no hay silencio
sospechoso: hay un informe bien formado sobre una pantalla que nadie tocó. *Y el que lo lee no
tiene cómo saberlo — el instrumento y el sujeto fallan con la misma cara.*

### 🔴 LA CURA ES UN MECANISMO, NO UN RECORDATORIO

**La segunda vez la curé a mano, después del susto.** *Un recordatorio escrito no sobrevive a la
tercera sonda apurada.* ⇒ nace **`scripts/s107/sonda-tocar.mjs`**:

- **`tocar()` LANZA si el nodo no existe** — *fallar ruidoso es la mitad del punto: una sonda que
  sigue después de no tocar nada produce un informe con forma de hallazgo.*
- **Devuelve `cambio`**, el segundo discriminador: el toque llegó, ¿y movió algo? **Ese sí es un
  hecho de la pantalla**, y ahora se puede afirmar sin confundirlo con el de arriba.

> **La regla, para cualquier sonda que se escriba en esta casa: no se toca sin verificar que hay
> qué tocar.** Y no por disciplina — *porque la función no te deja.*

**Hermana de la clase de la tanda 20** (*la incoherencia entre dos estados que sólo coinciden en
pantalla*): las dos son **defectos sin línea culpable**, y a las dos las caza el mismo hábito —
**mirar la salida entera y preguntarle al instrumento si de verdad hizo lo que dice.**

---

## TANDA 28 · `FilaCita` YA CONOCE GUARDERÍA — y **la segunda vez que un dato vencido mío llega a un parte**

### ① LA CURA

B lo resolvió en `69c39376`. **Medido contra el objeto:** `FilaCitaOficio` incluye `'guarderia'`.
El log **ya monta la pieza de sus cuatro hermanas** y la `Celda` murió.

✏️ **Y B corrigió mi encuadre, que es lo más útil de su respuesta:** *no era hueco de datos.*
`metadataMono` **nunca exigió una hora**, así que **no nació ninguna prop** — sólo faltaba el
oficio en el vocabulario cerrado. *Yo lo había descrito como si la pieza necesitara aprender
algo; sólo necesitaba una palabra.*

**La fila DESPLIEGA, no navega:** `onPress` y `direccion` son obligatorios, y una estadía sin
`estadiaId` **no tiene a dónde llevar** —la cita se compró y el prestador no la ejecutó—.
*Un chevron que promete una pantalla vacía es justo lo que 19.7 vino a matar.* La acción de
entrar al durante vive **adentro del despliegue**, y sólo cuando existe.

**⚠️ El límite de B, respetado:** *las dos ventanas NO entran en la fila — no son metadata, son
contenido, y su lugar es el despliegue con `FichaFranja`.* **Y el log no las necesita:** la
familia ya reservó; **las ventanas importan al ELEGIR**, y ahí están, en la vitrina.

---

## 📌 CLASE · **UN DATO QUE FUE CIERTO Y DEJÓ DE SERLO TIENE LA MISMA CARA QUE UNO CIERTO**

**Segunda vez en esta pista, y las dos veces sobre trabajo de OTRA pista.**

| # | lo que afirmé | era cierto cuando lo medí | lo que pasó |
|---|---|---|---|
| **1** | *«`guarderia_tramos` no existe; el punto vivo es inalcanzable por falta de entidad»* | sí | **A la había creado.** Mi versión habría mandado a construir **un tramo por estadía** ⇒ el mismo vehículo emitiendo **N puntos idénticos** |
| **2** | *«`FilaCita` no conoce guardería; la fila del log no se ve como sus hermanas»* | sí | **B lo resolvió.** Y mi encuadre además era peor que el hueco: hablé de props que nunca hicieron falta |

### 🔴 POR QUÉ ES PEOR QUE EQUIVOCARSE

*Un error se siente inseguro cuando se escribe. **Un dato vencido se escribe con la confianza de
haberlo medido** — y la medición fue real. No hay nada en el texto que avise: la única
diferencia entre «lo medí» y «lo medí hace tres tandas» es una fecha que casi nunca se pone.*

### LO QUE LO DISTINGUE DE `L-166`, y por eso se anota aparte

`L-166` dice **releer el dato vivo al usarlo**, y eso ya lo hago con lo mío. **Esto es sobre el
trabajo AJENO**: lo que otra pista puede haber curado mientras yo no miraba. **Y no se cura
releyendo mejor: se cura re-midiendo ANTES de afirmar en un parte** — *que es exactamente donde
las dos veces se escapó, porque un parte se escribe al final, cuando ya nadie está midiendo.*

> **Regla que me llevo: toda afirmación sobre territorio ajeno se re-mide contra el objeto en el
> momento de escribirla, no en el momento de descubrirla.** Y si no se puede re-medir, **se
> fecha**: *«medido el 29-ago»* le da al lector lo único que necesita para desconfiar.

**Hermana de las otras dos clases de esta pista** —la incoherencia entre dos estados y el
instrumento sin discriminador—: **las tres producen salidas creíbles y ninguna deja síntoma.**

---

## TANDA 29 · EL N+1 CERRADO, Y UN VEROSÍMIL-FALSO ESPERANDO SU PRIMER PRESTADOR *(29-ago-2026)*

### ① EL N+1 MURIÓ ANTES DE DOLER

A trajo las cuatro columnas a la proyección. **La pantalla ya no llama a
`obtenerFranjasGuarderia`.** *El pedido se hizo cuando todavía era barato, y por eso se pudo
pagar sin apuro.*

**Los cuatro nulos, independientes** — cada ventana se dibuja **sólo si sus dos extremos
llegaron**. *Un lugar con recogida y sin devolución muestra una sola, jamás media.*

### ② 🔴 EL `min`/`max` COLAPSA VENTANAS DE DÍAS DISTINTOS

**Medido, y el índice es la prueba:** `UNIQUE (prestador_id, tipo, dias_semana)` ⇒ **dos
ventanas del mismo tipo no son un borde, son el diseño.**

| tipo | días | ventana |
|---|---|---|
| recogida | L-V | 07:00 – 09:00 |
| recogida | sábados | 09:00 – 11:00 |

**Colapsadas: `07:00 – 11:00`** — *un rango que ese lugar **no ofrece ningún día**.* Un martes
dice que recoge a las 10:30, y no es cierto.

### ③ EL CRITERIO ERA CORRECTO Y AUN ASÍ NO SIRVE — **misma fórmula, distinta pregunta**

A hizo bien en no inventar uno: `obtener_estado_guarderia` usa `min`/`max` **y ahí está bien**,
porque deriva **`jornadaMinutos`** — *«del inicio de la recogida al fin de la devolución»*, un
**LAPSO** de la configuración propia del prestador. **Para un lapso, el mínimo y el máximo son
exactamente la respuesta.**

> **La lista de la familia hace otra pregunta:** *«¿a qué hora pasan a buscarlo ESE día?»* — y
> ahí **un agregado sobre todos los días no describe ninguno.**

*Trasplantar un criterio correcto a una pregunta que no es la suya es cómo un número bien
calculado termina diciendo algo falso.* **Hermana de la ley de S99: una proporción trasplantada
a otra escala deja de ser la misma.**

### ④ Y POR QUÉ RECHACÉ LA SALIDA QUE A OFRECÍA

Ofreció **devolver las franjas una por una**. Con eso, **la pantalla tendría que elegir cuál
aplica según el día de semana** — o sea **filtrar de su lado**, justo lo que el contrato del
filtro prohíbe en su ⓪: *«el filtro corre en el SERVIDOR; la pantalla jamás filtra por su
cuenta»*.

⇒ **Pedí una tercera, más chica que las dos:** el lector **ya recibe `p_fecha`** — que las
columnas salgan de **la franja que rige para ESA fecha**. *Y si para una misma fecha hubiera más
de una, ahí `min`/`max` vuelve a ser correcto: son ventanas del mismo día.*

---

## TANDA 30 · LOS CAMINOS TRISTES CONTRA AURORA *(29-ago-2026)*

`scripts/s107/sonda-caminos-tristes.mjs`, con sesión real y **cero escrituras**.

### 🔴 EL HALLAZGO: **«no abre» se reporta como «se llenó»**

Aurora opera L-V. **El domingo devuelve `causa = sin_cupo_ese_dia`** ⇒ la pantalla diría
*«Ninguna guardería tiene cupo para el domingo 30. Prueba con otro día»*: **le dice a la familia
que se llenó cuando está cerrado, y la manda a buscar otro día para un lugar que nunca abre los
domingos.**

**Y la casa ya firmó esa distinción un piso más arriba:** el calendario de cupo tiene
**`no_opera` como estado propio** — *«No abre» NO es «se llenó», y el server los separa porque
desde la pantalla ambos llegan como `disponible = 0`.* **La lista de causas la pierde.**
⇒ `S107-C-PEDIDO-A-A-NO-OPERA.md`.

### ✅ LO QUE ANDA — cuatro caminos verificados

**día pasado** y **hoy** rebotan `fecha_no_ofertable` (estado propio de víspera) · **pez** →
`mascota_no_elegible` · **gato** → `especie_sin_oferta`. 🔴 **Los dos últimos son casos
DISTINTOS y el motor los separa bien** — *decirle a un pez «estamos trabajando en eso» sería
prometer algo que nadie construye.*

### ⚠️ LO QUE NO SE MIDIÓ, Y POR QUÉ — sin maquillar

- **Pago rechazado a mitad** y **acta con salvedad**: exigen **RESERVAR y LEVANTAR UN ACTA**, o
  sea **escribir en Aurora**. *No se simulan: un camino triste inventado prueba la simulación,
  no el producto.* **Van al gate del founder, o a una corrida con subtransacción.**
- **El colapso del `min`/`max`**: la cura de A **está en el cuerpo y funciona para el caso que
  hay** (lunes da la ventana L-V; sábado no da lugar), pero **ejercer el colapso exige un lugar
  con dos ventanas del mismo tipo**, y crearlo es escribir. **Se declara en vez de darlo por
  probado.**

### 📌 TERCERA VEZ DEL INSTRUMENTO QUE MIENTE — y esta vez la cacé antes de abrir la boca

La sonda leía **`precio_desde`** y el RPC devuelve **`precioDesde`** ⇒ imprimía **`null` sobre
un precio que existía**. *Lo delató que la pantalla ya había mostrado `$12.00`.*

> **La cura fue imprimir el JSON crudo.** *Un instrumento que nombra los campos de memoria está
> adivinando con más pasos.*

**Y la primera corrida tuvo otro:** *«mañana»* cayó **domingo**, así que el «día hábil de
control» medía un día cerrado. **Dos instrumentos mal en la misma sonda, los dos con salida
creíble.** ⇒ la sonda ahora calcula **el próximo hábil real**, no «mañana».

---

## TANDA 31 · LA CORRIDA EN SUBTRANSACCIÓN — los dos caminos tristes que exigían escribir *(29-ago-2026)*

`scripts/s107/corrida-tristes-subtx.sql`, entre `BEGIN` y `ROLLBACK`, **con el residuo medido
después: las cinco tablas en 0.** *Una corrida que dice «deshice todo» sin contar filas es una
promesa, no una prueba.*

### ✅ LOS DOS CAMINOS, VERDES CONTRA EL MOTOR REAL

**① pago rechazado a mitad** — *el rechazo no es una función: es la AUSENCIA de confirmación*, y
lo que había que probar era qué queda cuando el pago no llega. **El hold toma cupo (8 → 7)** ·
**la cita queda `pendiente_pago` con su `expira_en`** · **la familia la VE sin pagar**, que es lo
que le permite volver a pagarla (D-319: el hold propio se muestra, el ajeno no).

**② acta con salvedad** — **levantada por el PRESTADOR y conformada por el DUEÑO, cada uno con
su sesión**, no simulado. Queda `con_reserva` **y el texto vuelve**: *una salvedad sin texto
sería una queja muda.*

### 🔴 LO QUE LA CORRIDA ENCONTRÓ ANTES DE PODER CORRER

**`reservar_dia_guarderia` rebotó `documentos_no_disponibles`** — y al medirlo, **dos de sus tres
motivos no están tipados**:

| motivo | tipado | lo que ve la familia |
|---|---|---|
| `requisitos_sanitarios` | ✅ | su voz |
| **`documentos_no_disponibles`** | ❌ | *«Ocurrió un error inesperado»* |
| **`documentos_sin_aceptar`** | ❌ | *«Ocurrió un error inesperado»* |

🔴 **El segundo es el camino normal de TODA familia nueva.** *Le decimos «ocurrió un error
inesperado» a alguien que sólo tenía que aceptar los términos — y no le decimos cuáles ni
dónde.* ⇒ `S107-C-PEDIDO-A-A-CODIGOS-DEL-GATE.md`.

**Y la precondición se fabricó adentro**: cero documentos y cero aceptaciones en Aurora **no es
un defecto — es la regla de perímetro del founder funcionando** (*«sin documento cargado la
reserva no se abre»*). Se crearon los seis del `CHECK` dentro de la transacción, **igual que A
crea el sábado en su cinturón**, y el `ROLLBACK` se los llevó.

---

## 📌 CUARTA, QUINTA Y SEXTA VEZ DEL MISMO INSTRUMENTO — la clase, en una sola corrida

**Nombrar de memoria falló SEIS veces hoy**, y esta corrida sola aportó cuatro:

| lo que escribí | lo que era |
|---|---|
| `familias` | **`familia`** |
| `familias.titular_id` | **no existe** — el titular vive en `familia_miembro` |
| `rol = 'titular'` | **`'adulto_titular'`** |
| `tipo_servicio = 'guarderia'` | **`'guarderia_dia'`** |
| `data.precio_desde` | **`precioDesde`** (camelCase) |
| `format('… %', x)` | **`%s`** — `%` suelto es de `RAISE`, no de `format` |

> ### **Nombrar de memoria es adivinar con más pasos** — firma de la mesa, y hoy se cobró seis veces.

**Lo que las hace distintas de un typo:** *cuatro de las seis produjeron un ERROR ruidoso y el
cinturón las frenó; **dos produjeron salida creíble*** —el `precioDesde` que imprimía `null`
sobre un precio real, y el `RAISE NOTICE` que el cliente **se come**, dejando una corrida «sin
error» y **sin una sola línea**: un verde mudo, que es la peor forma de verde.

**⇒ La regla operativa que dejo escrita en el script:** *la salida de una corrida va a una tabla
que se pueda LEER, jamás a avisos que el cliente pueda descartar.*

---

## TANDA 32 · DOS FIRMAS Y UNA CORRECCIÓN DE ATRIBUCIÓN *(29-ago-2026)*

### ✅ FIRMA DE LA CASA

> ### La salida de una corrida va a una tabla que se pueda LEER, jamás a avisos que el cliente pueda descartar. **Un verde mudo es la peor forma de verde.**

**Escrita en la cabecera del script**, no sólo acá — *el que corra una sonda no va a leer este
parte.* Y su razón, que es lo que la hace regla y no preferencia: **un fallo se investiga; un
silencio se aprueba.**

### ✏️ LA ATRIBUCIÓN, CORREGIDA POR LA MESA — y vale anotarla

La mesa me atribuyó un segundo hallazgo —**el corte de la media del acta**— que **midió D**.
Al no tenerlo, **lo dije en vez de dejarlo pasar**, y la mesa lo corrigió.

*Se anota porque la tentación era barata: aceptar un hallazgo ajeno no cuesta nada en el
momento y **deja un hueco que nadie sabe que existe** — alguien lo habría dado por cubierto de
mi lado.* **Es la misma familia que el dato vencido:** una afirmación cómoda que nadie tiene
cómo verificar hasta que duele.

### LAS TRES CLASES QUE DEJA ESTA PISTA, juntas

Las tres **producen salidas creíbles** y **ninguna deja síntoma**:

1. **La incoherencia entre dos estados que sólo coinciden en pantalla** — cada mitad correcta;
   el defecto nace de mostrarlas juntas. *Ninguna línea está mal.*
2. **El instrumento que no distingue «no pasó nada» de «no hice nada»** — hoy es mecanismo
   (`sonda-tocar.mjs`), no recordatorio.
3. **El dato que fue cierto y dejó de serlo** — cobrada dos veces, las dos sobre trabajo ajeno.

**Y las tres se cazan con el mismo hábito:** *mirar la salida entera y preguntarle al
instrumento si de verdad hizo lo que dice.*

---

## TANDA 33 · LA SUPERFICIE DE ACEPTACIÓN, Y LOS DOS REBOTES CON DESTINO *(29-ago-2026)*

### ① `no_opera_ese_dia` — una línea, y su voz NO manda a mover el dedo

**Sin «prueba con otro día» pegado:** *el día no es el problema — lo que la familia no sabía es
CUÁNDO sí abren, y mandarla a cambiar el día le esconde el dato que le falta.*

### ② LOS DOCUMENTOS — la pantalla que le da dónde resolver el rebote

`app/guarderia/documentos.tsx`. **Verificada con sesión real:** con cero documentos en Aurora
pinta *«Todavía no podemos abrir reservas de guardería»* **y no monta el botón de aceptar** —
fail-closed, y el problema se dice como **nuestro**, sin «prueba de nuevo».

**🔴 NI UNA PALABRA DE TEXTO LEGAL VIVE EN LA PANTALLA.** El diccionario tiene **rótulos** —cómo
se llama cada documento— y **el contenido sale del server, versionado**. *Un texto legal escrito
en un diccionario es un texto que nadie versiona, nadie fecha y nadie puede probar que la
familia aceptó.* **Se acepta una VERSIÓN**, y por eso la versión viaja de vuelta.

**No es una lista de casillas, y la razón es del motor:** exige el **tope de urgencia** y los
**contactos** sin default ⇒ *el consentimiento y esos dos datos son un solo acto; separarlos
dejaría familias aceptadas y sin contacto.* **La autorización de imagen nace apagada:** *un
default encendido publicaría la foto de un animal porque alguien no tocó un interruptor.*

### ③ EL REBOTE QUE AHORA TIENE DESTINO — y los tres que NO navegan a propósito

`documentos_sin_aceptar` **lleva a la pantalla**. *Nombrarlo fue la mitad de la cura; la otra
mitad es que lleve a donde se resuelve.*

🔴 **Los otros no navegan, y es decisión:** `sin_cupo` y `requisitos_sanitarios` se arreglan en
la misma pantalla o en el carnet, y **`documentos_no_disponibles` no se arregla del lado de la
familia** — *mandarla a una pantalla que va a decirle lo mismo sería pasearla.*

### ⚠️ LO QUE DECLARO Y NO RESUELVO: el protocolo de mora está entre los seis

El `CHECK` del motor incluye **`protocolo_no_retiro`**, y la pantalla **lista lo que el server
devuelve**. **No lo trata distinto ni lo esconde**, y las dos alternativas son peores:
*construirle un caso especial sería la pantalla de mora que el perímetro prohíbe; sacarlo de la
lista sería pedirle a la familia que acepte algo que no le mostramos.* **Decide la mesa.**

### 📌 `L-437` DE A — hermana de mis tres clases, y la cuarta de la familia

> **Un censo por patrón sobre código ACOTA, no cierra.** Su *«0 sin tipar»* era **una propiedad
> del regex, no del motor**, y se leyó como cierre.

**Es exactamente lo mismo que mis tres**, un piso más arriba: *el instrumento y el sujeto fallan
con la misma cara, y la salida es creíble.* **La diferencia es de dónde viene el engaño** — en
las mías el instrumento no hizo lo que dijo; acá **hizo exactamente lo que dijo, y lo que dijo
no era la pregunta.**

**Y su señal para abandonar un instrumento, que me llevo:**

> **Uno que se parchea dos veces sobre el mismo caso está diciendo que no es el instrumento.**

*Mi sonda de la secuencia lleva dos parches —el nodo separado y el `format('%s')`—. **Los dos
fueron sobre casos distintos**, así que por esta regla todavía no toca cambiarla; queda
anotado para no confundir «se parcheó dos veces» con «se parcheó dos veces por lo mismo».*
