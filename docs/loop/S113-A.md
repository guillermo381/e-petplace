# S113 · PISTA A — parte

> **Este archivo es LA fuente del handoff.** Lo que vive sólo en la conversación
> muere con ella. Escrito antes de una compactación de contexto.
> Última actualización: **3-sep-2026 21:05 -05** · `main` = **`f166012b`**, árbol limpio.

---

## 🔴 LO PRIMERO — EL ESTADO NO ESTÁ CERRADO, Y EL MODO DE FALLA ES CONOCIDO

**Cinco commits viven SÓLO en este disco.** `main` está 5 adelante de `origin/main`:

```
f166012b  S113-A · el techo del HOY consume el conteo: la puerta de C, cableada
1028c530  S113-A · obtenerConteoSemanaPrestador: el número lo cuenta el servidor
cbd9d63c  merge: S113-C (fijado a 7a1b0ff7)
5a10a7c5  S113-A · relevamiento del expediente y de la infraestructura para IA
7a1b0ff7  S113-C · (de C — ÉSTE SÍ está en origin)
```

**Lo de C sobrevive** (`git ls-remote origin refs/heads/pista/s113-c` → `7a1b0ff7…`,
verificado POR SHA, no por código de salida). **Lo mío no.**

⚠️ **Y hay una divergencia declarada, que es la parte incómoda:** la migración
`20260908940000` **ya está APLICADA en el remoto** (653 local = 653 remoto, cero
desemparejadas, registrada con su nombre). O sea: **la base tiene la función y el
código que la documenta y la usa existe sólo en un disco.** Si este árbol se
pierde, queda una función viva en producción-preview que ninguna migración del
repo explica.

**El push es del founder** (CLAUDE.md). No se hizo, y se declara — no está pendiente
por olvido. *Es el precedente de `L-217`: «todo en origin» y «todo en el canon» son
dos afirmaciones distintas, y acá ni siquiera la primera es cierta todavía.*

---

## LO HECHO, verificado contra el objeto

### ① El relevamiento (`docs/loop/S113-RELEVAMIENTO.md`, 1055 líneas)
Estado real del expediente de la mascota y de la infraestructura que una pieza de
IA necesitaría. Cada ítem con EXISTE / DÓNDE / MEDIDO CON / CONTROL POSITIVO /
EVIDENCIA. **Seis «no pude medir» con su razón exacta.** Sólo lectura: cero DDL,
cero DML.

**Los números que más cuesta volver a medir** (para no re-correrlos):

| qué | valor medido |
|---|---|
| mascotas totales | **89** |
| con ≥1 evento clínico | **6** |
| con alergias no vacías | **1** · crónicas **0** · medicación **1** |
| con peso clínico | **1** · reportado **5** · microchip **0** |
| con fecha de nacimiento | **61** |
| eventos totales | **566** · `modo_captura` poblada en **2** |
| tipos de evento | **62**, de los cuales **44 con n=0** |
| `evento_desparasitacion_aplicada` | tabla + trigger completos, **0 filas** |
| `producto_ficha_dosificacion` | **143 filas** con `espectro` (el saber antiparasitario vive del lado PRODUCTO) |
| edges desplegadas | **33**; **4 llaman a Anthropic**; `chat-ayuda` tiene fuente y **no está desplegada** |
| telemetría de IA / de errores | **cero** — las 4 funciones no escriben una fila ni guardan `usage` |
| `packages/*` | 6, **ninguno de IA** |

### ② La puerta que C pidió, abierta
- **RPC** `contar_citas_semana_prestador(uuid, date, date)` — migración `20260908940000`,
  **INVOKER a propósito** (el gate es la RLS de `evento_cita_servicio`, la misma que
  ya usan los cuatro lectores; un DEFINER sería una puerta nueva).
  Reversa escrita ANTES: `docs/loop/S113-A-REVERSA-20260908940000.sql`.
- **Wrapper** `obtenerConteoSemanaPrestador({ prestador_id })` en
  `packages/api/src/wrappers/conteoSemanaPrestador.ts`.
- **Cableado** en `apps/prestador/src/app/(tabs)/index.tsx` (import + `useState` + disparo
  + la línea de C).
- `verify:linea-jornada` agregado al `package.json` raíz.

**Los estados y el discriminador son COPIA LITERAL, no equivalentes** (medidos antes
de escribir): `['confirmada','en_curso','completada','no_show']` en los cuatro
lectores · oficio por `tipo.categoria` salvo el médico, que va por **`tipo.es_medico`**
porque abarca `veterinario` + `telemedicina` + `emergencia`.

**Controles, corridos dentro de la migración:**
```
L-140 OK · anon EXECUTE = false
ROJO DEL PREDICADO OK · categoria-sola=2 · con es_medico=3
CINTURON VERDE · positivo=3 · con ruido=3 · vacio=0 (y el otro sigue en 3)
fixture revertido — residuo 0
```
Sobre datos reales: `de300000` → **18** · `de680000` → **0** aunque tiene **21
estadías de guardería confirmadas** en la ventana (la exclusión de guardería queda
probada, no sólo declarada).

---

## COMPROMISOS ABIERTOS

### Lo que YO debo
- **Nada bloqueante.** Los tres pasos del encargo están cerrados y verificados.

### Lo que le debo DECIR a C (no curé, es su territorio)
🔴 **La frase falsa sobre la zona vive en DOS lugares y sólo corregí uno.**
`apps/prestador/src/lib/dia-local.ts` sigue diciendo que la zona *«coincide con la
que usa `hoy_local()` en la base»*. **Medido: `hoy_local()` es
`(now() AT TIME ZONE 'America/Guayaquil')::date`, una CONSTANTE** — no la zona del
prestador. Es `D-1007`. La corregí en el call site del HOY porque ahí la leí;
corregirla en `dia-local.ts` es de C.

### Lo que espera al FOUNDER
- **El push de estos 5 commits** (arriba, §🔴).
- **El recorrido en aparato** del techo del HOY con el número real — nada de esto
  tocó un teléfono.
- Todo lo de `docs/loop/S112-HOJA-DE-DECISION.md`, intacto.

---

## FRENOS Y SUPUESTOS VIVOS

1. **No se publicó ningún OTA.** Orden explícita: el founder autoriza. La veda de
   producción de S112 sigue entera.
2. **Cero aparato.** El conteo se verificó por camino real contra la base y por
   gates, **jamás en una pantalla**. `verify:linea-jornada` cubre la voz (incluido
   el caso «el conteo NO llegó → Hoy libre»), no el render.
3. **15 ramas históricas sin mergear** (master, pista-c, s87-c/d, s90-d, s91-b/d,
   s99-d, s104-c/d, s106-d-t3, s107-b/c/d, s109-b/d). Ninguna es de S113; no son mías.
4. **`D-1007` sólo medida, no curada** — 26 funciones llaman a `hoy_local()`, **62**
   tienen el literal `America/Guayaquil`. ⚠️ El canon dice 58: **está una medición
   atrás, o mide otra cosa.** No lo resolví.
5. **Anotado y NO curado:** `diaDelNegocio` (domain) y `hoyEnZona`
   (`apps/prestador/src/lib/dia-local.ts`) son **la misma aritmética en dos lugares**.
   Hasta este wrapper, `diaDelNegocio` no tenía **ningún** consumidor. Unificarlas
   toca territorio del prestador.
6. **`packages/api` ahora depende de `@epetplace/domain`** (workspace). Sin ciclo
   (domain no depende de nadie de la casa) y con precedente (`packages/ui` ya
   dependía). Si alguien audita deps, esto es deliberado.

---

## EL PRÓXIMO PASO EJECUTABLE

**Pedirle al founder el push de los 5 commits.** Hasta entonces nada de S113-A está
en origin, y la base remota ya tiene una función que sólo este disco explica.

Después, por la hoja de decisión de S112: publicar y gatear **la burbuja del refugio
en el prestador** (mergeada en S112, cero aparato) — es el primer objetivo declarado
de S113 en `docs/loop/S113-ARRANQUE.md`.

---

## GATES AL CERRAR

`verify:linea-jornada` **0 fallos** · `verify:diseno` **verde, 61 reglas** ·
**4 typechecks en 0** (api · ui · cliente · prestador) ·
**653 migraciones local = remoto, cero desemparejadas**.
