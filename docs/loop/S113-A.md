# S113-A · lote 0 «Nexo despierta» — parte de la pista

**Estado al escribir:** `main` local, árbol limpio. Lo de abajo se midió contra
la **base viva** (`zyltipqscdsdsxnjclhp`), el **repo en el SHA declarado** y las
**edges desplegadas**, y se dice contra cuál en cada caso.

---

## 0 · Arrastre — hecho antes de este lote

| ítem | estado | SHA |
|---|---|---|
| `docs/loop/S113-RELEVAMIENTO.md` commiteado | ✅ | `5a10a7c5` |
| `obtenerConteoSemanaPrestador` + merge de C + `verify:linea-jornada` | ✅ | `1028c530` · `f166012b` |

---

## 1 · `modo_captura` en las dos IA que ya existen — migración `20260908960000`

**Rojo de partida**, medido en la base: 564 eventos con `modo_captura` NULL, 2
con `automatico`, **cero** `extraido_por_ia` / `dictado` / `tecleado`.

**Lo que se midió antes de decidir** (`pg_proc`, `pg_policies`, `pg_trigger`):
ninguna de las dos RPC toca `eventos_mascota` — las dos desembocan en
`_crear_evento_padre_auto`, que tiene **19 consumidores**; y `eventos_mascota`
tiene RLS con policies de SELECT e INSERT y **ninguna de UPDATE**.

**Lo construido:** `_marcar_modo_captura_evento` (DEFINER angosta, una columna,
NULL→valor, jamás pisa; su gate es el MISMO predicado que la policy de INSERT)
· el carnet marca `extraido_por_ia` y **assertea que marcó tantos como
insertó** · `sedimentar_nota_clinica` gana `p_modo_captura` validado y marca
**toda la constelación**.

🔴 **Por qué NO se ensanchó la puerta de 19 consumidores:** un parámetro con
DEFAULT **no reemplaza, SOBRECARGA** (L-119), y las 19 llamadas quedarían
ambiguas. *Una migración que hace cirugía de firma sobre la puerta central para
escribir una columna de metadato le está cambiando el alcance a la migración.*

🔴 **Por qué no es un `UPDATE` inline:** el carnet es INVOKER y `eventos_mascota`
no tiene policy de UPDATE ⇒ afectaría 0 filas y devolvería `ok` igual. **Verde
falso perfecto.**

**Superficie declarada:** `_marcar_modo_captura_evento` queda ejecutable por
`authenticated`. Permite marcar **eventos propios** donde hoy hay NULL. No
alcanza ajenos ni pisa marcas.

---

## 2 · Los pendientes del hogar — migración `20260908980000`

La medición dijo qué construir **y qué no**:

- ✅ **CHAT ya lo contaba el servidor** desde S112 (`contar_pendientes`). Nada nuevo.
- 🔴 **PEDIDOS no los contaba nadie.** `(tabs)/_layout.tsx:231` trae la lista
  ENTERA con `listarMisPedidos()` para responder `length > 0` y decidir si la
  tab existe; `hogar/index.tsx:891` la trae otra vez. **Ese es el defecto.**
- ⚪ **AVISOS: `null`, y NO por supuesto.** `notificaciones` existe, tiene 22 sin
  leer y 7 usuarios — y **cero productores**: ninguna función de `pg_proc`,
  ninguna edge, ningún archivo del monorepo la escribe. La fila más nueva es
  del 3-ago. *Un número que ningún acto del producto puede mover enseña a
  ignorar la burbuja.*

**Se ensanchó `contar_pendientes` en vez de nacer otra**, porque su propia
cabecera de S112 lo pide literal: *dos contadores en la misma barra terminan
discrepando*. `obtenerPendientesHogar()` es la puerta con la forma pedida,
**proyectada sobre esa única llamada** — no puede divergir porque no consulta.

El predicado **no se transcribe**: `NOT es_terminal` sobre `v_pedidos_narrativa`,
y `es_terminal` es DATO de `cat_narrativas_pedido` (no terminales hoy: pagando ·
confirmado · preparando · en_camino · no_llego).

---

## 3 y 4 · Las dos puertas de la familia — migración `20260909000000`

**Peso: ya existía y ya corrió.** `registrarPesoMascota` →
`registrar-peso-hoja.tsx`. Control positivo: de 7 filas, 4 son `bascula_casa` y
1 `estimacion`. **No se construyó nada.**

**Recuerdo: el brief apuntaba a la tabla equivocada.** `evento_hito_narrativo`
**no tiene policy de INSERT**, sus 3 productores son DEFINER del sistema con
vocabulario cerrado de 3 claves, no tiene columna de foto y ninguna de sus 69
filas guarda una. Es la tabla de los **hitos del sistema**. La de la familia es
`evento_nota_dueno` — eje `identidad`, CRUD del titular ya escrito, 0 filas —
que gana `foto_url` (path, con CHECK que hace inexpresable la URL) y el valor
`recuerdo` en su categoría.

**Antiparasitario:** nace `registrar_desparasitacion_familia`, con procedencia
`declarado_por_familia` y `modo_captura` `tecleado`. **La plaga por especie NO
entra** — llega en el lote 1 con su columna y su CHECK, no en `notas`.

### 🔴 El hallazgo: `evento_nota_dueno` era INESCRIBIBLE

Lo encontró el cinturón al rebotar con `23503`. Su trigger le pasaba el eje
**`cuidado_diario`**, que **no existe** en `cat_ejes_jtbd`, y hay FK con
`ON DELETE RESTRICT`. ⇒ **todo INSERT fallaba desde que ese trigger se
escribió.** Sus 0 filas no eran «falta la UI»: era una puerta tapiada.
***Un cero se lee como «nadie lo usó» y a veces significa «nadie pudo».***

**Censo de la clase, corrido ANTES de curar** — los 16 triggers
`_trg_*_crear_evento` contra los dos catálogos:

| trigger | qué pasa | qué se hizo |
|---|---|---|
| `_trg_nota_dueno_crear_evento` | 🔴 eje `cuidado_diario` **no existe** | **curado** a `identidad` (lo dice `cat_tipos_evento`) |
| `_trg_peso_crear_evento` | 🟠 pasa `salud`, su catálogo dice `etapa_vida`. Escribe, pero diverge. **7 filas vivas.** | **NO se tocó** — mueve el eje de eventos vivos, es decisión de producto |
| `_trg_microchip_crear_evento` | 🟠 escribe `microchip_asignado`, **sin fila en `cat_tipos_evento`**, y `tipo` también tiene FK ⇒ **SEGUNDA puerta tapiada**, 0 filas | **NO se curó** — su fila exige decidir eje y si es clínico |

---

## 5 · La migración de E — **NO LLEGÓ**

**No existe `origin/pista/s113-e`** ni ningún archivo `*_ia_uso.sql`. Medido con
`git branch -r` y `ls supabase/migrations`.

---

## 6 · Merges, gates, despliegue y OTA

### Control de ancestría (corrido antes de cada merge)

| pista | punta mergeada | base común | resultado |
|---|---|---|---|
| `pista/s113-d` | `08e33a05` | `dd40e37e` | ✅ ancestro de `main`, verificado **por contenido** (`cat-file \| shasum`) |
| `pista/s113-b` | `58642c81` | `dd40e37e` | ✅ ancestro de `main` |
| `pista/s113-c` | `7d81e89b` | `7a1b0ff7` | ✅ ancestro de `main` |

Ninguna declaró un SHA distinto de su punta.

### Despliegue de las cuatro edges — leído del objeto

`extract-vacuna` v64→**v66** · `extract-documento` v31→**v33** ·
`estructurar-nota-clinica` v41→**v43** · `escribir-presencia` v40→**v42**.
Las cuatro **ACTIVE**. Dos vueltas: la primera con el código de D, la segunda
con los timeouts medidos.

### 🟢 Las cuatro llamadas REALES al proveedor

La llave vive en los secrets del proyecto y **sólo la ven las edges**, así que
se midió **llamándolas**, con entradas reales: un carnet de vacunas real de
Storage (320 kB en base64), un dictado de consulta completa y hechos+respuesta
para la presencia. **Las cuatro HTTP 200 con contenido correcto.**

| pieza | edge | HTTP | latencia de pared |
|---|---|---|---|
| carnet | `extract-vacuna` | 200 | **85.132 ms** |
| nota_clinica | `estructurar-nota-clinica` | 200 | **15.564 ms** |
| presencia | `escribir-presencia` | 200 | **3.038 ms** |
| documento | `extract-documento` | 200 | **2.370 ms** |

🔴 **NO HAY FILAS DE `ia_uso` QUE PEGAR: la tabla no existe.** Es de E.
**El verde de IA del lote queda a UN paso, y ese paso no es mío.** El
instrumento quedó versionado (`scripts/s113a-llamada-real-ia.mjs`) y la clave
se lee del keychain al momento, jamás del script.

### Timeouts, fijados con los números medidos

**El muro, citado** (`supabase.com/docs/guides/functions/limits`): wall clock
**150 s Free**, **400 s pagos**, idle timeout **150 s en todos**.
⚠️ **El plan de este proyecto NO se pudo medir** — ni `projects list` ni
`orgs list` lo exponen — y **no se infiere**: rige el piso de 150 s.

| pieza | medido | 2× | queda | por qué |
|---|---|---|---|---|
| carnet | 85.132 ms | 170 s | **140 s** 🔴 | su doble excede el muro. *Un timeout por encima del muro es ficción: la plataforma corta primero.* |
| nota_clinica | 15.564 ms | 31 s | **40 s** | regla aplicada |
| presencia | 3.038 ms | 6 s | **10 s** | regla aplicada |
| documento | — | — | **60 s** | **sin medición representativa**, ver abajo |

⚠️ **`documento` no se movió y se dice por qué:** la única imagen real
disponible era un carnet (`prestador_documentos` tiene 0 filas con archivo), y
la edge correctamente devolvió los tres campos en null en 2.370 ms. *Ese número
puede ser la lectura real o un corto circuito al no encontrar documento, y desde
afuera no se distingue.* Aplicar la regla a una medición inválida da un número
equivocado **con la autoridad de una medición**.

🔴 **LA MEDICIÓN QUE SALVÓ UN NÚMERO MALO.** El primer dictado de prueba era un
párrafo y dio 4.510 ms ⇒ la regla habría fijado **10 s**. Con un dictado
realista de consulta completa dio **15.564 ms**: ese timeout habría cortado
**toda consulta de verdad**. *La regla estaba bien; la magnitud medida estaba
mal — y una muestra no representativa se lee igual que una buena.*

⚠️ **Advertencia que deja el número de carnet:** 85 s de latencia real contra
150 s de muro es **poco aire**. Si el p95 sube, esa pieza empieza a morir por
plataforma y no por timeout.

### Gates

| gate | resultado |
|---|---|
| typecheck `api` · `ui` · `cliente` · `prestador` | **4 en 0** |
| `verify:diseno` | VERDE · **61 reglas** |
| `verify:razon-muda` | VERDE · el número no subió |
| `verify:ref-antes-de-uso` | VERDE · 62 archivos con `useRef` |
| `verify:linea-jornada` | **FALLOS: 0** |
| `verify:mensajeria` | **50/50** |
| `verify:coach` | **46 verdes · 0 rojos** |
| `verify:contrast` | OK · 3 temas + 2 casas de oficio |
| `verify:vio-todo` · `verify:abanico` · `verify:rutas-de-aviso` · `verify:hoisting-nativo` · `verify:fila-memoizada` | VERDES (6/6 · 10/10 · 4 emitibles con destino) |
| `verify-ia-puerta` (D) | **33 verdes · 0 rojos** |
| `verify-ia-discriminador` (D) | **diff vacío** en 3, sólo `cache_control` en presencia |
| `verify:censo` | **28 en rojo** — 🟠 **heredado**: baseline medido en `441015cc` (cierre de S112) = **30**. Este lote lo BAJÓ en 2. Es un inventario de puntos ciegos, no corre en el hook |
| `verify:puerta-unica` | **NO EXISTE** — es de E, que no entregó |

**Migraciones: 656 local = 656 remoto, cero desemparejadas.**

### Discriminador del OTA

| pregunta | respuesta | medido contra |
|---|---|---|
| ¿nativa nueva? | **NO** — el único cambio de dependencia es `@epetplace/domain: workspace:*` en `packages/api`, que **tiene cero dependencias** y ya viajaba (lo consume `packages/ui`) | diff `441015cc..HEAD` de los `package.json` |
| ¿app config, permisos o plugins? | **NO** — cero archivos tocados | diff sobre `app.json`, `app.config.ts`, `eas.json`, `android/`, `ios/` |
| ¿runtime o canal? | **NO** — `version` sigue `1.0.7` en las dos apps | `app.json` |

**⇒ Los tres en NO ⇒ CANDIDATO A OTA.** Ni B ni C declararon dependencia
nativa nueva.

**NO se publicó.** El founder dice «autorizo».

---

## 7 · Lo que quedó fuera, con dueño

| qué | dueño | bloqueante |
|---|---|---|
| Las cuatro filas de `ia_uso` — **el verde de IA del lote** | **E** | la migración `ia_uso` no existe |
| `costo_estimado_usd` (sale NULL) | **E** | su tabla de precios; la costura es UNA línea en `_shared/ia/precios.ts` |
| `verify:puerta-unica` | **E** | no entregado |
| La cura angosta de `D-1010` (gatear sólo `TS2307`) | **E** | — |
| `documento`: timeout con medición representativa | **E**, con percentil sobre `latencia_ms` | necesita `ia_uso` |
| 🟠 `_trg_peso_crear_evento` diverge del catálogo, 7 filas vivas | producto | decisión de eje |
| 🟠 `microchip_asignado` sin fila de catálogo — **segunda puerta tapiada** | producto | decidir eje y si es clínico |
| La pantalla del prestador que manda `modoCaptura` | **C** | la puerta está entregada por nombre: `sedimentarNotaClinica({ …, modoCaptura })` |
| El recorrido en aparato | **founder** | nada de este lote tocó un teléfono |

---

## 8 · Lo que se entrega por nombre

- `obtenerPendientesHogar()` → `{ chat, pedidos, avisos }` — **B y C**
- `sedimentarNotaClinica({ …, modoCaptura?: 'tecleado' \| 'dictado' })` — **C**
- `registrar_recuerdo_familia(mascota, contenido, fecha?, foto_url?)` — **C**
- `registrar_desparasitacion_familia(mascota, producto, tipo, fecha, proxima?, notas?)` — **C**
- `registrarPesoMascota(mascotaId, { peso_kg, metodo })` — **ya existía y ya corría**

---

# ADENDA · el candidato del lote 0 (4-sep-2026)

## Merges — control de ancestría corrido antes de cada uno

| pista | rama | SHA mergeado | verificado |
|---|---|---|---|
| E | `pista/s113-e` | `57657f38` | `git ls-remote` → `57657f38a1a92ec0a04bca6199b4d1371827c686` |
| D | `pista/s113-d` | `08e33a05` | por contenido (`cat-file \| shasum`) |
| B | `pista/s113-b` | **`4e944cb3`** | `git ls-remote`; contiene `5dbcc5e2` |
| C | **`pista/s113-c2`** | **`e3232a35`** | `git ls-remote`; contiene `da752503` |
| C (vieja) | `pista/s113-c` | `7d81e89b` | ya mergeada antes |

⚠️ **El trabajo vivo de C no está en `pista/s113-c`** —que sigue en `7d81e89b`—
sino en **`pista/s113-c2`**. Y la cadena de C ya traía el `5dbcc5e2` de B por un
merge propio; **la punta real de B, `4e944cb3`, NO estaba ahí** y entró por su
propio merge, antes.

## 🟢 EL VERDE DE IA DEL LOTE — las cuatro filas de `ia_uso`

Cuatro llamadas **reales al proveedor**, por las edges desplegadas, con las
mismas entradas: un carnet de vacunas real de Storage (320 kB en base64), un
dictado de consulta completa, y hechos+respuesta para la presencia. **Las
cuatro HTTP 200.** Tokens del `usage` real, latencia del edge, costo con la
tabla de E:

| pieza | modelo | edge | resultado | tok entrada | tok salida | cache lect | cache escr | `latencia_ms` | `costo_estimado_usd` |
|---|---|---|---|---|---|---|---|---|---|
| carnet | claude-sonnet-5 | extract-vacuna | ok | 4 036 | 6 347 | 0 | 0 | **58 647** | **0,071542** |
| nota_clinica | claude-sonnet-5 | estructurar-nota-clinica | ok | 1 907 | 1 792 | 0 | 0 | **15 042** | **0,021734** |
| documento | claude-sonnet-5 | extract-documento | ok | 3 265 | 35 | 0 | 0 | **1 559** | **0,006880** |
| presencia | claude-sonnet-5 | escribir-presencia | ok | 156 | 85 | 0 | **1 248** | **1 329** | **0,004282** |

Dos lecturas que valen más que la tabla:
- **`presencia` escribió 1 248 tokens de caché** ⇒ el caché del `system` que D
  encendió en la única pieza que lo justifica **está funcionando**.
- 🔴 **El carnet cuesta $0,0715, no $0,0075.** E lo había estimado sin `usage`
  real y quedó **~9× por debajo** — y la razón es su propio hallazgo:
  *un modelo que inventa filas paga la salida de todas* (6 347 tokens de
  salida). Va como `D-1012`.

## Timeouts — recalculados con el muro **Pro**

**El muro, citado** (`supabase.com/docs/guides/functions/limits`): wall clock
150 s Free · **400 s en planes pagos** · idle timeout 150 s en todos.
**El proyecto está en Pro** (dato del founder) ⇒ **el muro es 400 s**.

| pieza | latencia medida | 2× | **timeout** | por qué |
|---|---|---|---|---|
| carnet | 85 132 ms | 170 s | **170 s** | regla aplicada; el peor de E fue 114 s y entra |
| nota_clinica | 15 564 ms | 31 s | **40 s** | regla aplicada, subido a múltiplo de 10 |
| presencia | 3 038 ms | 6 s | **10 s** | regla aplicada |
| documento | — | — | **60 s** | **sin medición representativa**, ver abajo |

La primera versión se calculó contra el piso de 150 s porque el plan no se
podía medir desde el CLI y **no se infirió**. Con el plan confirmado la regla
original no colisiona con nada.

⚠️ `documento` no se movió: la única imagen real disponible era un carnet
(`prestador_documentos` tiene 0 filas con archivo) y la edge devolvió los tres
campos en null en 2,4 s. *No se distingue desde afuera la lectura real de un
corto circuito, y aplicar la regla a una medición inválida da un número
equivocado con la autoridad de una medición.*

## Gates — exit code y bytes, nunca la última línea

**Un gate que no existe es ROJO, no silencio.** El runner verifica primero que
la línea esté en `package.json`.

🔴 **La regla se cobró en el acto:** mi corrida anterior reportó
`verify:nexo exit=0` y ese 0 **era del `tail` del pipe, no del gate** (`L-191`).

| gate | exit | bytes | |
|---|---|---|---|
| `verify:diseno` | 0 | 11 602 | ✅ 61 reglas |
| `verify:razon-muda` | 0 | 484 | ✅ |
| `verify:ref-antes-de-uso` | 0 | 102 | ✅ |
| `verify:linea-jornada` | 0 | 1 801 | ✅ |
| `verify:mensajeria` | 0 | 2 283 | ✅ 50/50 |
| `verify:coach` | 0 | 2 569 | ✅ 46/0 |
| `verify:contrast` | 0 | 37 292 | ✅ |
| `verify:vio-todo` | 0 | 582 | ✅ |
| `verify:abanico` | 0 | 862 | ✅ 10/10 |
| `verify:rutas-de-aviso` | 0 | 110 | ✅ |
| `verify:hoisting-nativo` | 0 | 209 | ✅ |
| `verify:fila-memoizada` | 0 | 422 | ✅ |
| `verify:puerta-unica` | 0 | 260 | ✅ 414 archivos · **5/5 controles** |
| `verify:huellas` | 0 | 75 | ✅ |
| `verify:sin-byte-nul` | 0 | 83 | ✅ |
| `verify:voz-por-tipo` | 0 | 395 | ✅ |
| `verify:migraciones` | 0 | 157 | ✅ |
| **`verify:glifos`** | 0 | 2 397 | ✅ **32/0** |
| **`verify:nexo`** | 0 | 197 | ✅ **190 casos, 0 fallos** |
| `verify:frenos` | 0 | 483 | ✅ |
| `verify:rebote-lleva-id` | 0 | 454 | ✅ |
| `verify:jornada-completa` | 0 | 964 | ✅ |
| `verify:edge-deno` | 0 | 683 | ✅ **34 functions · cero `TS2307`** |
| `verify:censo` | **1** | 12 915 | 🟠 **heredado** — baseline en `441015cc` = **30**; hoy **28** |
| `verify-ia-puerta` (D) | 0 | 2 621 | ✅ 33/0 |
| `verify-ia-discriminador` (D) | 0 | 10 238 | ✅ diff vacío |

**Typechecks: 6 en exit 0, todos con 0 bytes de salida** (`api` · `ui` · `i18n` ·
`mensajeria` · `cliente` · `prestador`).

**Migraciones: 658 local = 658 remoto, cero desemparejadas.**

### 🔴 El rojo que los merges destaparon, y era real

`verify:diseno` pasó a exit 1: **R63·C midió que `/antiparasitario` —la ruta
que trajo C— no estaba en `router.d.ts`**, así que el compilador no podía verla
y un `router.push` hacia ella pasaba sin control. Curado con `tipos:rutas`, la
herramienta que E construyó justo para esto.

⚠️ **Y de paso, un hallazgo sobre esa herramienta:** dijo *«ya existía · 39 kB»*
y salió **verde sobre un archivo viejo**, sin la ruta nueva. **Mide presencia,
no vigencia** — verde por ausencia (`L-450`) dentro del script escrito para
curar un verde por ausencia. Se destrabó borrando y regenerando. **La cura es
de E.**

## Discriminador del OTA

| pregunta | respuesta | medido contra |
|---|---|---|
| ¿nativa nueva? | **NO** — el único cambio de dependencia es `@epetplace/domain: workspace:*` en `packages/api`, que **tiene cero dependencias** y ya viajaba (lo consume `packages/ui`) | diff `441015cc..HEAD` de todos los `package.json` |
| ¿app config, permisos o plugins? | **NO** — **0 archivos tocados** | diff sobre `app.json`, `app.config.ts`, `eas.json`, `android/`, `ios/` |
| ¿runtime o canal? | **NO** — `version` sigue `1.0.7` en las dos apps, 0 `app.json` tocados | `app.json` |

**⇒ Los tres en NO ⇒ CANDIDATO A OTA.** Ni B ni C declararon dependencia nativa
nueva. **No se publicó: el founder dice «autorizo».**

## Seguridad — el barrido que la adenda pidió

`verify:huellas` corrió **verde**, pero mide otra cosa: compara las huellas
declaradas contra los `.env.local` para detectar **rotaciones**, no barre
`docs/` ni `scripts/`. Se corrió además el barrido que hacía falta, **con su
control positivo**: 2 597 archivos, cuatro apariciones de credencial, y las
cuatro decodificadas por su claim `role` — **las cuatro `role=anon`**, la clave
pública que ya viaja en el bundle. **Cero `service_role`, cero secretos.**

## Fichas depositadas (`pnpm proximo:ficha`, una por hallazgo)

| ficha | qué |
|---|---|
| `D-1010` | `verify:edge-deno` daba verde con un import que no resuelve — **curada por E en este lote**, `TS2307` en cero |
| `D-1011` 🔴 | **no existe flujo para pasar una mascota a memorial** (`fin_vida` activo, cero llamadores) |
| `D-1012` 🔴 | la lectura del carnet: 83 s de espera, un carnet de una vacuna que devolvió doce, y $0,0715 reales |
| `D-1013` 🟠 | `supabase projects api-keys` imprime las claves en claro — **también como LÍNEA PROHIBIDA en `CLAUDE.md`** |
| `D-1014` 🟠 | `microchip_asignado` sin fila de catálogo — **con mi voto escrito: eje `identidad`, NO clínico**, para que el founder firme en el lote 1 |

## Lo que quedó fuera, con dueño

| qué | dueño |
|---|---|
| `tipos:rutas` mide presencia y no vigencia | **E** |
| `documento`: timeout con medición representativa (necesita un documento real) | **E**, con percentil sobre `latencia_ms` |
| 🟠 `_trg_peso_crear_evento` diverge del catálogo (`salud` vs `etapa_vida`), 7 filas vivas | producto |
| `D-1014` · la fila de catálogo de `microchip_asignado` | **founder**, lote 1 |
| El recorrido en aparato | **founder** — nada de este lote tocó un teléfono |

---

## PUBLICADO — «autorizo» del founder, 4-sep-2026

**Ancla: `9d19de783ce661869efdf6f50c80373b09c90368`**, árbol en **0** antes de
bundlear (regla 82: un árbol sucio saca el ancla con asterisco). `eas-cli`
corrido desde `apps/<app>/`, nunca desde la raíz — verificado que no quedó
stub de `app.json` en la raíz.

**Todo lo de abajo se leyó del OBJETO con `eas update:view --json`, jamás del
texto del publish** — y `update:list` confirmó que el group publicado es el
vigente en el canal. *(`update:list` no expone `gitCommitHash`; sólo
`update:view` lo trae. Límite conocido de la casa.)*

### Cliente

| campo | valor |
|---|---|
| group | **`0a26ae50-908c-4548-b31a-a6752ccb07d1`** |
| runtime | **1.0.7** |
| canal / branch | **preview** |
| `gitCommitHash` | `9d19de783ce661869efdf6f50c80373b09c90368` |
| `isGitWorkingTreeDirty` | **`None`** — sin asterisco |
| android | `01a06aa9-5b7a-7837-baa8-0ed95e43490d` |
| ios | `01a06aa9-5b7a-7d1a-b636-640cc7afb503` |

### Prestador

| campo | valor |
|---|---|
| group | **`d921ba81-c43c-4b12-bc31-61a86b751791`** |
| runtime | **1.0.7** |
| canal / branch | **preview** |
| `gitCommitHash` | `9d19de783ce661869efdf6f50c80373b09c90368` |
| `isGitWorkingTreeDirty` | **`None`** — sin asterisco |
| android | `01a06aab-1f73-7fe4-bd17-6bd8390b7d16` |
| ios | `01a06aab-1f73-7ba6-9877-eb47e7933747` |

**Las dos salieron del MISMO ancla, a propósito**: el lote toca `packages/api`
y `packages/ui`, que viajan en los dos bundles.

⚠️ **La veda de producción sigue entera.** Los dos OTA corren en `preview`. Y
**nada de esto se vio en un aparato**: el recorrido es del founder.

---

## LA PUERTA DEL RECUERDO — **SÍ, está construida en `main`**

C la tiene para el 0.1. Medida en `9d19de78` y contra la base viva:

**Nombre exacto:** `registrarRecuerdoFamilia`, exportada desde `@epetplace/api`
(`packages/api/src/index.ts:335`), implementada en
`packages/api/src/wrappers/salud.ts:331`. **Viaja en el OTA que se acaba de
publicar.**

```ts
import { registrarRecuerdoFamilia, type RecuerdoInput } from '@epetplace/api';

const r = await registrarRecuerdoFamilia({
  mascotaId,            // requerido
  texto?,               // opcional
  fotoPath?,            // opcional · PATH del bucket, NO url, NO bytes
  fecha?,               // opcional · 'YYYY-MM-DD'; omitirla = hoy del SERVIDOR
});
// r.ok === true  → { hitoId, eventoId }
// r.ok === false → codigo ∈ acceso_denegado · sin_acceso_mascota ·
//                  recuerdo_vacio · foto_invalida · fecha_futura ·
//                  error_desconocido   (cada uno con su `mensaje`)
```

**Tres cosas que C necesita saber antes de dibujar:**

1. **`texto` y `fotoPath` son los dos opcionales, pero el servidor rebota
   `recuerdo_vacio` si no viene ninguno.** La pantalla puede dejar guardar con
   sólo foto, o sólo texto — no con nada.
2. **La foto se sube ANTES**, por el camino que ya existe
   (`apps/cliente/src/lib/subir-avatar.ts`, bucket `mascotas`, carpeta del
   dueño), y acá viaja **el path que ese subidor devuelve**. Mandar una URL o
   un path de otra carpeta rebota `foto_invalida`.
3. **No hay que pasar procedencia ni modo de captura**: los estampa el
   servidor (`declarado_por_familia` · `tecleado`). No son parámetros porque
   por esa puerta no entra nadie más.

**Estado en la base viva, medido:** RPC
`registrar_recuerdo_familia(uuid, text, date, text)` **existe** ·
`anon` **no** la alcanza · `authenticated` **sí** · policy
`hito_narrativo_insert_recuerdo` **puesta** · fila de catálogo
`recuerdo_familia` **sembrada** · **0 filas** (nadie escribió todavía: falta la
pantalla, que es de C).

**Y lo que la hace visible:** el evento nace con
`datos.clave_hito = 'recuerdo_familia'`, que es **literalmente** lo que
`packages/api/src/wrappers/timeline.ts:282` lee para reconocer un hito. Por eso
se mudó de `evento_nota_dueno` —que no lo lee ningún lector— a
`evento_hito_narrativo`.

⚠️ **Lo que le falta a C, y no es código mío:** el `clave_hito`
`recuerdo_familia` **no tiene voz en el diccionario** — y no debería tenerla:
*su voz es el texto de la familia*. Está escrito así en la descripción de su
fila de catálogo, para que nadie le invente una frase de la casa.

---

# CANDIDATO 0.1 (4-sep-2026) — **sin publicar**

## Merges — control de ancestría antes de cada uno, SHA verificado por `ls-remote`

| pista | rama | SHA mergeado |
|---|---|---|
| E | `pista/s113-e2` | **`7796fa6e`** |
| B | `pista/s113-b-01` | **`edf9091a`** (contiene `4e944cb3`) |
| C | `pista/s113-c3` | **`a6f68bae`** — verificado que **no hay SHA posterior** |

⚠️ **Seis ramas para cinco pistas.** B usó dos (`s113-b` → `s113-b-01`) y C
**tres** (`s113-c` → `c2` → `c3`). Las seis quedaron ancestro de `main`; *quien
busque el trabajo de C en `pista/s113-c` no lo va a encontrar.*

## Lo hecho

**(a)** E2 mergeada. `tipos:rutas` **ya estaba** desde el lote 0 (la agregué
yo); la línea nueva es `verify:gates-existen`.

**(b) Los cuatro «sin línea», curados — con un matiz medido:** sólo **dos** son
alias (`verify:frenos-por-sujeto` y `verify:huellas-credenciales` apuntan al
mismo script que `verify:frenos` y `verify:huellas`, verificado por
comparación). Los otros dos —`selectores-recurrentes` y
`compuertas-por-sujeto`— **no tenían ninguna línea**: su archivo existía y
nadie podía invocarlo. **No son alias: son su única puerta.**

**(c′) Retirar, firma del founder.** `CLAUDE.md` ya no publica «26 · 10 · 17»:
dice **«sin instrumento»**, con el porqué y con `D-1015`.

⚠️ **Una medición que corrige la instrucción:** `verify:legales` y
`verify:huerfanas` **no aparecen en `CLAUDE.md`** — sólo en el parte de S103-B
y en el de E. Ahí no había nada que anotar.

### 🔴 Lo que eso destapó, y cómo se salió sin hacer trampa

El gate cuenta menciones en `CLAUDE.md` **y** en `docs/loop/*.md`, sin
exclusión. Las que quedaban viven en **`S103-ACTA-CIERRE.md` —acta firmada—** y
en partes cerrados. *Reescribir un acta para que un gate se ponga verde es
ajustar el mundo al instrumento.* **La medición no se reescribe: se marca.**

⇒ El gate gana una **tabla de jubilaciones** (precedente vivo: `R62` de
`verify:diseno` es literalmente eso; `verify-edge-simbolos` quedó como lápida).
**No es una lista de perdón:** cada entrada trae su ficha, el verde **declara**
los apartados en pantalla, y **el gate verifica que la tabla no mienta** — un
jubilado con script o archivo sale ROJO. **Su rojo se produjo antes de
cablearlo** (`L-459`): con una línea plantada para `verify:borradores`, exit 1
nombrándolo.

Y la regla que el founder firmó, ya en `CLAUDE.md`: **todo número medido nombra
su comando y su SHA; sin comando en git, no se publica.**

**Cableado al hook** *después* de curar los siete, no antes: *un gate que frena
por deuda ajena no protege, estorba.*

**(e) El texto del recuerdo ya viaja.** `ItemTimeline` gana
**`texto: string | null`**, por **join** a `evento_hito_narrativo`, en el
**mismo `Promise.all`** ⇒ cero olas de red nuevas (`L-223`). **Un solo cambio
cubre a los dos lectores**: `leerTimelineMascota` y `leerTimelineHogar`
comparten `_timeline`. **C cambia una línea.**

No se filtra por `clave_hito`: hoy sólo `recuerdo_familia` escribe `texto`, así
que el efecto es idéntico — pero filtrar reintroduciría **la deducción desde
`datos` que el join existe para evitar**. El `''` se descarta en el mapa, no en
el consumidor: *una cadena vacía se dibuja como línea en blanco y se lee como
defecto.*

**Rojo, verde y control, por camino real** (sonda entrando por `initApi`, la
puerta que usan las apps, y limpiando por id · residuo **0**):

| brazo | resultado |
|---|---|
| recuerdo **con texto** → el ítem lo trae | ✅ |
| recuerdo de **sólo foto** → `null`, jamás `''` | ✅ |
| los **otros hitos** (del sistema) → `null` | ✅ (1 caso vivo para discriminar) |
| **CONTROL POSITIVO** · `hito_clave`, campo que **ya viajaba en el mismo select** | ✅ sigue llegando |
| **el rojo del instrumento** · mapeo devolviendo `null` a propósito | 🔴 exit 1, nombrando el campo |

⚠️ **Lo que NO trae, por nombre:** `evento_hito_narrativo.foto_url`. Un recuerdo
de sólo foto llega con `texto: null` **y `fotos_count: 0`** —la foto vive en esa
columna, no en `evento_archivo_adjunto`— así que la pantalla no tiene con qué
dibujarlo. **Es un campo más en este mismo select, sin viaje extra.** No se
agregó porque el pedido acotó a `texto`; se declara para que la decisión sea de
la mesa y no un olvido mío.

## Gates — exit code y bytes, nunca la última línea

| gate | exit | bytes | |
|---|---|---|---|
| `verify:diseno` | 0 | 11 602 | ✅ |
| `verify:razon-muda` · `verify:ref-antes-de-uso` | 0 | 484 · 102 | ✅ |
| `verify:linea-jornada` · `verify:mensajeria` | 0 | 1 801 · 2 283 | ✅ |
| `verify:coach` · `verify:contrast` | 0 | 4 278 · 37 470 | ✅ |
| `verify:vio-todo` · `verify:abanico` | 0 | 582 · 862 | ✅ |
| `verify:rutas-de-aviso` · `verify:hoisting-nativo` | 0 | 110 · 209 | ✅ |
| `verify:fila-memoizada` · `verify:puerta-unica` | 0 | 422 · 260 | ✅ |
| `verify:huellas` · **`verify:huellas-credenciales`** | 0 | 75 · 75 | ✅ alias |
| `verify:sin-byte-nul` · `verify:voz-por-tipo` | 0 | 83 · 395 | ✅ |
| `verify:migraciones` · `verify:glifos` | 0 | 157 · 2 401 | ✅ |
| `verify:frenos` · **`verify:frenos-por-sujeto`** | 0 | 483 · 483 | ✅ alias |
| **`verify:selectores-recurrentes`** | 0 | 351 | ✅ **su única puerta** |
| **`verify:compuertas-por-sujeto`** | 0 | 631 | ✅ **su única puerta** |
| `verify:rebote-lleva-id` · `verify:jornada-completa` | 0 | 454 · 964 | ✅ |
| `verify:edge-deno` | 0 | 683 | ✅ 34 functions · cero `TS2307` |
| **`verify:gates-existen`** | **0** | 427 | ✅ **tras (c′)**, con 3 jubilados declarados |
| `tipos:rutas --gate` | 0 | 111 | ✅ cliente 95 · prestador 87 |
| `verify-ia-puerta` (D) | 0 | 2 621 | ✅ 33/0 |
| `verify-ia-discriminador` (D) | 0 | 10 238 | ✅ diff vacío |
| **`verify:nexo`** | **1** | 568 | 🔴 **`D-1016`** |
| `verify:censo` | 1 | 12 895 | 🟠 heredado — baseline `441015cc` = **30**, hoy **28** |

**Typechecks: 6 en exit 0, todos con 0 bytes de salida.**
**Migraciones: 658 local = 658 remoto.**

### 🟢 El gate de E pagó en el acto

Tras mergear C, `tipos:rutas --gate` salió **exit 1**: *«cliente — ESTABA
VENCIDO — entraron 1: `/recuerdo`»*. **Nombró la ruta, curó el archivo y avisó
que un `tsc` anterior había medido contra rutas viejas.** Es exactamente el
defecto que E arregló, cazado en su primer caso real.

### 🔴 `verify:nexo` en rojo — un choque real entre pistas, no un descuido

B retiró `HALO` y `HALO_GROSOR` de `coach-geometria` en `edf9091a`; el gate de
C los importa. **No es un rename:** `HALO = 66` era un **diámetro** y el gate
calculaba `(HALO − ORBE) / 2` = 9; hoy hay `RESPLANDOR = 24`, que se usa como
`shadowRadius` — con él la misma cuenta da **−12**. *Sustituir el nombre pondría
el gate en verde sobre una cuenta sin referente, que es peor que el rojo.*

**Alcance medido: el producto está sano** — el único consumidor de esa constante
es el gate, y los 6 typechecks dan 0. **Lo roto es el instrumento.** Lo curan
**B y C**; A no lo adivina, porque elegir ese número es decidir diseño desde el
gate. Ficha **`D-1016`**.

## Discriminador del OTA

| pregunta | respuesta |
|---|---|
| ¿nativa nueva? | **NO** — único cambio de dependencia: `@epetplace/domain: workspace:*`, cero deps propias, ya viajaba |
| ¿app config, permisos, plugins? | **NO** — 0 archivos tocados |
| ¿runtime o canal? | **NO** — `version` sigue `1.0.7`, 0 `app.json` tocados |

**⇒ Los tres en NO ⇒ candidato a OTA. NO se publicó.**

## Fichas del 0.1

| ficha | qué |
|---|---|
| `D-1015` 🟡 | borradores · legales · huérfanas: **retirados** de `CLAUDE.md`, jubilados por declaración, se re-miden el día que una decisión lo necesite |
| `D-1016` 🔴 | `verify:nexo` mide una geometría que B retiró — **de B y C** |

---

# CANDIDATO 0.1 FINAL (4-sep-2026) — **sin publicar**

## Tabla pista → SHA (ancestría verificada antes de cada merge, SHA por `ls-remote`)

| pista | rama | SHA mergeado |
|---|---|---|
| E | `pista/s113-e2` | `7796fa6e` |
| E | **`pista/s113-e-03`** | **`f25bc92c`** — `verify:hooks` |
| B | `pista/s113-b-01` | `edf9091a` (contiene `4e944cb3`) |
| C | `pista/s113-c3` | `d7d9ed0d` (contiene `a6f68bae`) |
| C | **`pista/s113-c-02`** | **`5be3fbb9`** — la cura de `D-1017` |

Las cinco, ancestro de `main`. **E y C ya usaron la convención nueva**
(`-03`, `-02`), depositada en `CLAUDE.md` en este mismo lote.

## 🟢 `D-1017` CERRADA — el prestador vuelve a abrir

**Alivio, ya ejecutado y aún vigente al escribir:** el canal `preview` del
prestador volvió al lote 7 de S112 — group `101a5999-b7aa-4fb4-8910-723a93eb7ddc`,
ancla `e29238a9`. El cliente no se tocó.

**La cura la hizo C**, y **midió más hondo que mi diagnóstico**: yo dije *dos
hooks bajo UN return*; eran **TRES bajo SEIS**. El `useState` de la línea 755
se me pasó midiendo a mano — **lo encontró el gate de E**.

**Verificado por el camino real, no por el gate solo:** `scripts/repro-d1017.mjs`
entra con `demo-prestador@epetplace.dev` y la jornada renderiza con datos vivos
(*«Te quedan 5 · terminas 18:00»*, `$44.13` del día, las cinco pestañas).
*«¿la frontera de caída se comió el árbol? **no**»*, exit 0.

## El lock, declarado

**No conflictuó ninguna de las dos veces**, y se dice porque se esperaba que sí:
E y C basaron en el `main` del momento. Igual se corrió `pnpm install` sobre el
`package.json` mergeado —**jamás elegir un lado**—:

| merge | resultado del `pnpm install` |
|---|---|
| E `f25bc92c` | **byte-idéntico** al lock que trajo E · su diff: **+1020/−36 líneas, +110 paquetes**, todos de la rama de eslint |
| C `5be3fbb9` | *«Already up to date»*, **0 líneas** — C sólo sumó una línea de script |

**El único conflicto real fue de contenido, no del lock:** C y yo renombramos
`usarMoneda → useMoneda` el mismo día. Yo en sitio; C renombró el **archivo** y
puso la explicación en la cabecera. **Gana la de C** —mejor puesta, incluye el
rename del archivo y **conserva la medición que importaba (cero llamadores)**—
verificado byte-idéntico.

## Discriminador de las tres devDependencies, con control positivo en las dos mitades

| medición | cliente | prestador | control positivo |
|---|---|---|---|
| **árbol de producción** (`pnpm list --prod`) · paquetes con `eslint` | **0** | **0** | `react`: **368** líneas |
| **bundle** (`expo export`, 10 MB / 11 MB) · `eslint-plugin-react-hooks` | **0** | **0** | `supabase`: **15 / 17** |
| · `typescript-eslint` · `eslint-scope` · `espree` · `rules-of-hooks` | **0** | **0** | |

**El `expo export` que E no pudo correr, corrido acá para las dos apps.** Los
3+4 hits crudos de la cadena «eslint» en la pasada anterior **se abrieron uno
por uno**: son concatenaciones de literales de Hermes
(`Properti`+`eslint-plugin-ft-flow`, `obtenerAdoptable`+`slint src`) y nombres
de `@react-native/eslint-config` en la **metadata de otros paquetes**. Ninguno
es código de eslint.

## Gates — exit code y bytes

| gate | exit | bytes | | gate | exit | bytes | |
|---|---|---|---|---|---|---|---|
| **`verify:hooks`** | **0** | 153 | ✅ | `verify:huellas` | 0 | 75 | ✅ |
| **`verify:hooks-bajo-return`** | **0** | 181 | ✅ | `verify:huellas-credenciales` | 0 | 75 | ✅ |
| `verify:gates-existen` | 0 | 427 | ✅ | `verify:frenos` | 0 | 483 | ✅ |
| `verify:puerta-unica` | 0 | 260 | ✅ | `verify:frenos-por-sujeto` | 0 | 483 | ✅ |
| `verify:nexo` | 0 | 197 | ✅ | `verify:selectores-recurrentes` | 0 | 351 | ✅ |
| `verify:coach` | 0 | 4 278 | ✅ | `verify:compuertas-por-sujeto` | 0 | 631 | ✅ |
| `verify:glifos` | 0 | 2 401 | ✅ | `verify:sin-byte-nul` | 0 | 83 | ✅ |
| `verify:diseno` | 0 | 11 602 | ✅ | `verify:voz-por-tipo` | 0 | 395 | ✅ |
| `verify:contrast` | 0 | 37 470 | ✅ | `verify:migraciones` | 0 | 157 | ✅ |
| `verify:razon-muda` | 0 | 484 | ✅ | `verify:rebote-lleva-id` | 0 | 454 | ✅ |
| `verify:ref-antes-de-uso` | 0 | 102 | ✅ | `verify:jornada-completa` | 0 | 964 | ✅ |
| `verify:linea-jornada` | 0 | 1 801 | ✅ | `verify:edge-deno` | 0 | 683 | ✅ |
| `verify:mensajeria` | 0 | 2 283 | ✅ | `tipos:rutas --gate` | 0 | 111 | ✅ |
| `verify:vio-todo` | 0 | 582 | ✅ | `verify-ia-puerta` | 0 | 2 621 | ✅ |
| `verify:abanico` | 0 | 862 | ✅ | `verify-ia-discriminador` | 0 | 10 238 | ✅ |
| `verify:rutas-de-aviso` | 0 | 110 | ✅ | `verify:hoisting-nativo` | 0 | 209 | ✅ |
| `verify:fila-memoizada` | 0 | 422 | ✅ | **`verify:censo`** | **1** | 13 324 | 🟠 |

**33 gates: 32 en exit 0.** El único rojo es `verify:censo`, **heredado** —
baseline medido en `441015cc` (cierre de S112) = **30**; hoy **28**.

**Typechecks: 6 en exit 0, todos con 0 bytes.**
**Migraciones: 658 local. Árbol: 0 cambios.**

## Discriminador del OTA

| pregunta | respuesta |
|---|---|
| ¿nativa nueva? | **NO** — `@epetplace/domain` (cero deps, ya viajaba) y **tres devDependencies de la raíz que NO entran al binario**, probado arriba en las dos mitades |
| ¿app config, permisos, plugins? | **NO** — 0 archivos tocados |
| ¿runtime o canal? | **NO** — `1.0.7` en las dos apps, 0 `app.json` tocados |

**⇒ Los tres en NO ⇒ candidato a OTA. NO se publicó.** Cuando llegue el
«autorizo»: **las DOS apps desde el mismo ancla**, groups y shas leídos del
objeto con `update:view --json`.

## Fichas del 0.1

| ficha | qué |
|---|---|
| `D-1015` 🟡 | borradores · legales · huérfanas: retirados de `CLAUDE.md`, jubilados por declaración |
| `D-1016` ✅ | `verify:nexo` medía una geometría retirada — **cerrada por C** |
| `D-1017` ✅ | el prestador no abría: tres hooks bajo seis returns — **cerrada por C**, verificada por camino real |
| `D-1018` 🟡 | 34 `eslint-disable` de `exhaustive-deps` escritas para un linter que nunca corrió |

---

## PUBLICADO — «autorizo» del founder, 4-sep-2026

**Ancla: `7b35d3952c114cbb0a0f6b20bb6df5c0ddea9a73`**, la MISMA en las dos apps.
Árbol en **0** antes de bundlear; `eas-cli` corrido desde `apps/<app>/` y
verificado que no quedó stub de `app.json` en la raíz.

**Todo leído del OBJETO con `update:view --json`**, y el vigente del canal
confirmado con `update:list` — nunca del texto del publish.

### Cliente

| campo | valor |
|---|---|
| group | **`533a9d64-2642-40dd-8651-dfd66770f21c`** |
| runtime | **1.0.7** |
| canal / branch | **preview** |
| `gitCommitHash` | `7b35d3952c114cbb0a0f6b20bb6df5c0ddea9a73` |
| `isGitWorkingTreeDirty` | **`None`** — sin asterisco |
| android | `01a06b0f-d092-72b4-a518-57ea365a3aa5` |
| ios | `01a06b0f-d092-7768-a7fe-30dfa1aaaf0d` |

`update:list` → **vigente**; debajo queda `0a26ae50` (lote 0).

### Prestador

| campo | valor |
|---|---|
| group | **`96c224ba-c82b-4a32-a1ff-d070d0bbca97`** |
| runtime | **1.0.7** |
| canal / branch | **preview** |
| `gitCommitHash` | `7b35d3952c114cbb0a0f6b20bb6df5c0ddea9a73` |
| `isGitWorkingTreeDirty` | **`None`** — sin asterisco |
| android | `01a06b12-0dfa-7ada-8662-ca64985d2ccb` |
| ios | `01a06b12-0dfa-7e2b-b4c2-6b8882af534f` |

### 🟢 El group del prestador reemplaza al del alivio — confirmado del objeto

`update:list --branch preview` del prestador, en orden:

| # | group | qué es |
|---|---|---|
| **[0]** | **`96c224ba`** | **VIGENTE** — lote 0.1, `D-1017` curada |
| [1] | `101a5999` | el **ALIVIO** (republish del lote 7 de S112, ancla `e29238a9`) |
| [2] | `d921ba81` | el lote 0 — **el que no abría** |

*El alivio cumplió su función y queda debajo: el teléfono que lo tenía aplicado
recibe ahora el 0.1, que es el mismo código del lote 0 más la cura de C.*

⚠️ **La veda de producción sigue entera:** los dos OTA corren en `preview`.
Y **nada de esto se vio en un aparato** — el recorrido es del founder. La
diferencia con el lote 0 es que ahora **hay dos gates que sí lo vieron**:
`verify:hooks` y `verify:hooks-bajo-return`, más el repro que entra por el
camino real y confirma que el prestador abre.

---

# CANDIDATO 0.2 (4-sep-2026) — **sin publicar**

## Tabla pista → SHA

| pista | rama | SHA mergeado |
|---|---|---|
| B | `pista/s113-b-02` | **`439f0694`** — el orbe sin alma, medido en Android |
| C | `pista/s113-c-02` | **`6e18fa39`** — el SHA de cierre que C declaró |

⚠️ **Se mergeó B primero y por separado:** la punta de C anterior
(`d7d4252b`) **no** contenía a B — salieron del mismo `main`. La punta de
cierre (`6e18fa39`) sí lo trae, por un merge propio de C (`cc37172e`).

## 🟢 El rojo que el candidato tenía abierto, curado por C

`verify:nexo` estaba en **exit 1**: importaba `RESPLANDOR`, y B lo **partió en
tres** (`LIENZO` · `RESPLANDOR_RADIO` · `RESPLANDOR_ALFA`, más `ANILLO`,
`LILA_ALFA` y `ORBE_MINI`). Medido: de los 11 exports viejos **desapareció
exactamente uno**.

**C reescribió el gate contra la geometría nueva** en vez de sustituir el
nombre — igual que con `D-1016` y por la misma razón: *cambiar la palabra habría
puesto el gate en verde sobre una cuenta sin referente.* Hoy **326 casos, 0
fallos**.

## Los dos gates nuevos de B — dentro de `verify:coach` (95/0)

| brazo | qué mide |
|---|---|
| `stopColor` sin `rgba` | **Android le come el alpha** en un `stopColor` con `rgba()` |
| el resplandor no es sombra de RN | `shadowColor` / `shadowRadius` / `shadowOpacity` **no existen en Android** |

*Los dos nacieron de una medición en emulador, no de una lectura.* Y confirman
con la fuente lo que C había concluido midiendo el DOM: **el montaje hacía su
parte; la causa estaba en el dibujo.**

## Discriminador del OTA

| pregunta | respuesta | medido contra |
|---|---|---|
| ¿nativa nueva? | **NO** | `@epetplace/domain` (cero deps, ya viajaba) + 3 devDependencies de la raíz que **no entran al binario** (probado en el 0.1: árbol de producción y bundle). **Desde el 0.1 publicado: 0 archivos de dependencias tocados** |
| ¿app config, permisos, plugins? | **NO** | 0 archivos |
| ¿runtime o canal? | **NO** | `1.0.7` en las dos apps, 0 `app.json` tocados |

**`expo-linear-gradient` SALIÓ DEL ORBE:** `PresenciaCoach` importaba de ese
paquete **1 vez** en `04883d92` y **0 ahora**. Lo reemplaza el `LinearGradient`
de `react-native-svg` (alias `LinearGradientSvg`), que **ya era dependencia**.
El paquete sigue declarado y lo usan otros 9 archivos de `packages/ui`: **salió
del orbe, no del repo**. **Ninguna nativa entró.**

**⇒ Los tres en NO ⇒ candidato a OTA.**

## Gates — exit code y bytes

**31 gates: 30 en exit 0.** `verify:hooks` 0 · `verify:hooks-bajo-return` 0 ·
`gates-existen` 0 · `puerta-unica` 0 · **`nexo` 0 (326/0)** · **`coach` 0
(95/0)** · **`glifos` 0 (31/0)** · `diseno` 0 (61 reglas) · `contrast` 0 ·
`razon-muda` 0 · `ref-antes-de-uso` 0 · `linea-jornada` 0 · `mensajeria` 0 ·
`vio-todo` 0 · `abanico` 0 · `rutas-de-aviso` 0 · `hoisting-nativo` 0 ·
`fila-memoizada` 0 · `huellas` 0 · `huellas-credenciales` 0 · `frenos` 0 ·
`frenos-por-sujeto` 0 · `selectores-recurrentes` 0 · `compuertas-por-sujeto` 0 ·
`sin-byte-nul` 0 · `voz-por-tipo` 0 · `migraciones` 0 · `rebote-lleva-id` 0 ·
`jornada-completa` 0 · `edge-deno` 0 · `tipos:rutas --gate` 0 ·
`verify-ia-puerta` 0 · `verify-ia-discriminador` 0.

Único rojo: **`verify:censo` (1)**, heredado — baseline `441015cc` = **30**,
hoy **28**. **Typechecks: 6 en 0 con 0 bytes.**

## 🔴 Una corrección al parte de C, medida

C escribe: *«voz-hecho.ts y timeline.ts — el hueco del texto del recuerdo sigue
abierto y es pedido a A»*. **Ese hueco se cerró en el 0.1** y está en `main`:

| pieza | dónde |
|---|---|
| `ItemTimeline.texto: string \| null` | `timeline.ts:80` |
| `ItemTimeline.foto_path: string \| null` | `timeline.ts:97` |
| el join a la tabla tipada | `timeline.ts:270` — `select('evento_id, texto, foto_url')` |
| el mapeo | `timeline.ts:365-366` |
| la voz sin frase de la casa | `voz-hecho.ts:71` — `if (item.hito_clave === 'recuerdo_familia') return item.texto ?? ''` |
| el dibujante | `useFotosDeRecuerdos` montado en las **dos** pantallas del Hogar |

*El parte de C se escribió contra su rama, que no traía el 0.1 de A.* Es la
misma clase que el canon ya nombra: **una afirmación verdadera para el autor
entra al canon como falsa para todos los demás** — el estado se mide contra
`main`, jamás contra el árbol propio.

## Lo que B deja pedido, y no es de A

⚠️ **El orbe se llama «Abrir a Nexo» en los dos estados y ahora también
cierra** ⇒ dice «Abrir» cuando el toque va a cerrar. Y en web `aria-expanded`
sale `null` — **RN-web no emite el `accessibilityState`**, así que ahí el estado
no se anuncia por ningún lado. Lo midió C y lo declaró sin curarlo: **es
`packages/ui`, o sea de B.**

---

## PUBLICADO EL 0.2 — «autorizo» del founder, 4-sep-2026

**Ancla: `e2e65f35bccc59404672613c9b9daea498c034c8`**, la MISMA en las dos apps.
Árbol en **0** antes de bundlear; `eas-cli` desde `apps/<app>/`, sin stub en la
raíz. **Todo leído del OBJETO con `update:view --json`**, y el vigente de cada
canal confirmado con `update:list`.

### Cliente

| campo | valor |
|---|---|
| group | **`63ed7863-71d7-4b32-ac4f-b065e89dd365`** |
| runtime · canal | **1.0.7** · **preview** |
| `gitCommitHash` | `e2e65f35bccc59404672613c9b9daea498c034c8` |
| `isGitWorkingTreeDirty` | **`None`** |
| android · ios | `01a06d0f-9a9a-7086-ad60-b53377ce7537` · `01a06d0f-9a9a-7ebe-8d2f-a37d017da6e8` |

### Prestador

| campo | valor |
|---|---|
| group | **`d3da9663-654d-4be7-9431-4f8a256fc1f0`** |
| runtime · canal | **1.0.7** · **preview** |
| `gitCommitHash` | `e2e65f35bccc59404672613c9b9daea498c034c8` |
| `isGitWorkingTreeDirty` | **`None`** |
| android · ios | `01a06d10-7a6d-74b5-901f-c6e6e62c0098` · `01a06d10-7a6d-79ea-b75d-93ccea7f671a` |

**Vigentes confirmados**, con el 0.1 justo debajo en los dos canales:
cliente `63ed7863` sobre `533a9d64` · prestador `d3da9663` sobre `96c224ba`.

⚠️ **La veda de producción sigue entera** — los dos en `preview`. Y **cero
aparato de mi lado**: las únicas capturas de dispositivo del lote son las de B
en emulador Android (`docs/loop/capturas-s113-b02/`), que es lo que la línea
nueva de `CLAUDE.md` exige desde hoy.

**Ficha depositada para B: `D-1019`** — el orbe dice «Abrir» en los dos estados
y `aria-expanded` sale `null` en web. **Entra en el 0.3.**

---

# CANDIDATO 0.3 (4-sep-2026) — **sin publicar, y con dos frenos**

## Tabla pista → SHA

| pista | rama | SHA |
|---|---|---|
| B | `pista/s113-b-03` | **`6cdd7637`** |
| C | `pista/s113-c-02` | **`696a7958`** |
| C | `pista/s113-c-03` | **`386561b0`** |

Las tres salieron del mismo `main` (`b92491d0`) y **ninguna contenía a las
otras**: se mergearon por separado, B primero.

## 🔴 FRENO ① — NO borré la exportación de `BurbujaPendientes`

**La instrucción decía borrarla en el merge. No se puede todavía, y lo dice la
propia lápida de B**, citada literal:

> *«**SIGUE EXPORTADA A PROPÓSITO, y esto no es tibieza: es `L-395`.** Si se
> retira antes de que las apps monten la nueva, no queda ninguna puerta… **El
> retiro y el montaje van en el mismo commit, y ese commit es de quien toca
> `apps/`.**»*

**Medido:** las **dos apps siguen montándola** —
`apps/cliente/src/app/(tabs)/_layout.tsx:197` y
`apps/prestador/src/app/(tabs)/_layout.tsx:210`— más 4 montajes en la galería. Y
**`coach: false` no lo monta NINGUNA superficie**: sus 5 apariciones en el árbol
son **todas comentarios**. **El SHA de montaje de C no llegó** (sólo existen
`c-02` y `c-03`, las dos ya mergeadas). *Borrar la exportación hoy dejaría a las
dos apps sin puerta.*

## 🔴 FRENO ② — `verify:nexo` en rojo, y es el residuo del conflicto

**B y C curaron `D-1019` el mismo día, de dos formas incompatibles.** C desde el
montaje (dejar `voz.orbe` y cambiarle el valor según `abierta`); B en la pieza
(**partir `voz.orbe` en `voz.abrir` y `voz.cerrar`**).

**Resuelto por el objeto:** `voz.orbe` **ya no existe** en `PresenciaCoach`
(medido en su tipo, líneas 150-171) ⇒ la versión del montaje **no compilaría**.
Gana la pieza.

**Y queda el residuo:** el gate de C assertea *su* versión —
*«`voz.orbe` cambia con el estado»*— y hoy da **333 casos, 1 fallo**. **No lo
toqué**: cambiar un gate para ponerlo verde sobre un contrato que ya no existe
es lo contrario de medir. **Lo re-apunta C.** También queda **huérfana** la key
`nexo.cerrarOrbe` que C agregó.

## 🟢 El recorrido en emulador Android (D-284) — la línea nueva, en su primer uso

Emulador `Pixel_10_Pro_XL`, cliente contra Metro, cuenta `guillo381+8`.
Capturas en `docs/loop/capturas-s113-a03/`.

| captura | qué muestra |
|---|---|
| `02-hogar-con-orbe.png` | **el orbe SE VE, con su resplandor** — la cura de B funciona en Android |
| `03-orbe-abierto.png` | la pata abre: Foto · Antiparasitario · Vacuna · Peso · «Pregúntale a Nexo», con su velo |
| `04-memorial-sombra.png` | 🔴 **el hallazgo** |

**Sobre «la presencia sin Coach»: no se pudo ver porque no existe dónde.**
Ninguna superficie monta `coach: false` — ni las apps ni la galería, cuya
sección sigue rotulada «lote 0.1». *No es que no lo miré: es que no hay
superficie que lo monte*, y es la misma ausencia que impide retirar la burbuja.

### 🔴 `D-1021` — la ficha de una mascota fallecida se dibuja como si viviera

Entrando a **Sombra** (`estado_vida='fallecida'`, medido): techo vivo, chip
*«Conociéndolo · 1 por resolver»*, **«Registrar el de hoy»**, **«Cargar
carnet»**, y el orbe del Coach montado. *A una familia que perdió a su animal,
la app le pide que registre el peso de hoy.*

**Son dos defectos y hay que curar los dos:** ① **el tema memorial no se
enciende en ninguna pantalla de producto** —los únicos `ThemeProvider` del
cliente son galería, lámina, pantalla-caída y el raíz, que monta `light`/`dark`—
así que `theme.mode === 'memorial'` es un guard correcto colgado de un
interruptor que nadie aprieta · ② **`focoNexo` contradice su propio comentario**:
dice *«Nexo no aparece en memorial»* y sólo cumple cuando `activas.length === 0`,
o sea **sólo en un hogar de una sola mascota**.

**Por qué vivió tanto sin verse, medido:** **89 de 89 mascotas estaban
`activa`** hasta que E sembró a Sombra en este mismo lote. **No lo encontró un
gate: lo encontró mirar** — la primera cosecha de la línea que este lote
depositó.

## Gates — exit y bytes

**31 gates: 29 en exit 0.** `hooks` 0 · `hooks-bajo-return` 0 · `gates-existen`
0 · `puerta-unica` 0 · **`coach` 0 (con los dos brazos nuevos de B)** ·
`glifos` 0 · `diseno` 0 · `contrast` 0 · y los 21 restantes en 0.
**Rojos: `verify:nexo` (1) — freno ②** · `verify:censo` (1) heredado, **28**
contra baseline **30**. **4 typechecks en 0 con 0 bytes.**

## Discriminador del OTA

| pregunta | respuesta |
|---|---|
| ¿nativa nueva? | **NO** — 0 archivos de dependencias tocados desde el 0.2 |
| ¿app config, permisos, plugins? | **NO** — 0 archivos |
| ¿runtime o canal? | **NO** — `1.0.7`, 0 `app.json` tocados |

**⇒ Los tres en NO.** Pero **el candidato NO está listo**: hay un gate en rojo
y una exportación que no se puede retirar. **No se publica.**

## Nota de numeración

`proximo:ficha` daba **`D-1020` libre**, pero **C ya tomó ese número** en su
commit (`8ba48697`) **sin depositar la ficha** — medido: 0 ocurrencias en
`DEUDAS_CANONICAS.md`, 1 en su parte. Se usó **`D-1021`** para no colisionar.
*Es exactamente el modo de falla que `D-1003` documenta: un número usado en
artefactos vivos sin ficha depositada.*

---

# CANDIDATO 0.3 — cierre (4-sep-2026) · **sin publicar**

## Tabla pista → SHA

| pista | rama | SHA |
|---|---|---|
| B | `pista/s113-b-03` | `6cdd7637` |
| C | `pista/s113-c-02` | `696a7958` |
| C | `pista/s113-c-03` | `386561b0` |
| C | **`pista/s113-c-04`** | **`1ffff0b8`** — el montaje |
| E | **`pista/s113-e-04`** | **`066e89b1`** — el fixture parametrizado |

## 🟢 El freno ① se levantó: `BurbujaPendientes` retirada del índice

Llegó el montaje (`1ffff0b8`) y **recién con él se puede** — es lo que B pidió
al dejarla exportada (`L-395`). **Medido antes de borrar:** **cero montajes en
las dos apps** (sus tres menciones restantes son lápidas) y **la galería la
importa directo de `./components/BurbujaPendientes`**, no del índice, así que
sigue pudiendo mostrarla. **4 typechecks en 0 después.**

El archivo **no se borró**: la galería es su último lector. Lo que muere es su
condición de puerta pública, y la lápida dice que el retiro **ya ocurrió**.

## 🟢 El freno ② se levantó, y C se autocorrigió

`verify:nexo` vuelve a **exit 0**. **C llegó a la misma resolución que yo sobre
`D-1019`** —gana el contrato de B— y la explicó mejor, con el porqué que a mí me
faltaba: *«cuando la pieza puede garantizar sola lo que el montaje garantizaba
por disciplina, gana la pieza»*. Se tomó su versión.

## El recorrido en emulador Android (D-284)

| captura | qué muestra |
|---|---|
| `02-hogar-con-orbe.png` | el orbe **se ve con su resplandor** en Android |
| `03-orbe-abierto.png` | la pata abre con sus cuatro dedos y «Pregúntale a Nexo» |
| `04-memorial-sombra.png` | 🔴 `D-1021` |
| `05-bruma-memorial.png` | 🔴 **`D-1021` otra vez, con la segunda memorial** |

### 🔴 `D-1021` sigue viva, y ahora es CLASE y no caso

**Bruma** (`fallecida`, sembrada por E con el fixture de `066e89b1`) se dibuja
**idéntica a Sombra**: techo vivo, *«Conociéndolo · 1 por resolver»*,
**«Registrar el de hoy»**, **«Cargar carnet»** y el orbe del Coach montado.
**Dos mascotas memoriales, el mismo defecto.** El SHA de C con su cura **no
llegó** — medido: la ficha sigue sin montar el tema memorial, y los únicos
`ThemeProvider` del cliente son galería, lámina, pantalla-caída y el raíz.

### ⚠️ La presencia sin Coach en el prestador: NO la vi, y digo por qué

Dos frenos, los dos medidos:
① **`guillo381+refugio` no tiene la clave unificada** — probado **por API**, no
por la UI: `Invalid login credentials`, mientras `duenovet` y `paseo1` entran.
*No es que tipeé mal.*
② Con `duenovet`, la app **queda en el esqueleto de carga sin resolver**
(`No active session` → «Try again» → esqueleto, ~2 min). **No sé si es defecto
o entorno del emulador, y no lo voy a afirmar sin medirlo** — distinguirlo pide
el mismo trabajo que hice para `D-1017` (correrlo en web con sesión real), y no
lo hice.

**Lo que sí está medido del lado del prestador:** su `_layout.tsx` ya **no
monta `BurbujaPendientes`** (lápida en la línea 168) y los 4 typechecks pasan.
*El código está; la pantalla no la vio nadie.*

## Gates — exit y bytes

**31 gates: 30 en exit 0.** `hooks` 0 · `hooks-bajo-return` 0 ·
`gates-existen` 0 · `puerta-unica` 0 · **`nexo` 0** · **`coach` 0** ·
`glifos` 0 · `diseno` 0 · `contrast` 0 · `tipos:rutas --gate` 0 · y los demás
en 0. Único rojo: **`verify:censo` (1)**, heredado — **28** contra baseline
**30**. **4 typechecks en 0 con 0 bytes.**

## Discriminador del OTA

| pregunta | respuesta |
|---|---|
| ¿nativa nueva? | **NO** — 0 archivos de dependencias tocados desde el 0.2 |
| ¿app config, permisos, plugins? | **NO** — 0 archivos |
| ¿runtime o canal? | **NO** — `1.0.7`, 0 `app.json` tocados |

**⇒ Los tres en NO ⇒ candidato a OTA.** **No se publicó.**
