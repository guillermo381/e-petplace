# S106-D — el riel DeUna · bitácora de pista

> **Pista D** · territorio: `supabase/functions/pagos-deuna-*` · edge functions,
> secretos, ambiente QA del proveedor.
> **Abre:** 24-ago-2026 · **Rama:** `pista/s106-d` · **Base:** `main` `5959650e`.
> **Rige:** `CLAUDE.md` · `METODO_TRES_PISTAS` · `CONTRATO_TRABAJO` ·
> `LETRA_DEUNA` v1.7 · `CONTRATO_WRAPPER_DEUNA`.

---

## TURNO 1 — medición, sin código

**Contra qué objeto se midió, declarado una vez:** repo = `main` local
`5959650e` (árbol limpio verificado antes y después) · base y edge functions =
proyecto `zyltipqscdsdsxnjclhp` vía CLI · ramas = `git branch -a` local +
`ls-remote`. **Ningún dato sale de un reporte previo.**

### ⓪ Dos frenos de arranque, declarados antes de nada

1. **`PLAN_MESA_106` NO EXISTE en `main`.** Medido: `docs/` tiene
   `PLAN_MESA_103`, `104`, `105` y `ORDEN_ARRANQUE_MESA_105`; cero coincidencias
   de «MESA_106» en todo `docs/`. *El prompt lo cita como fuente que rige.* Esta
   pista trabajó contra `PLAN_MESA_105` (que en su §5 nombra el `pointOfSale`
   como la llave que destraba «el riel entero») y `LETRA_DEUNA` v1.7. **Si el
   plan 106 existe fuera del repo, se deposita y se re-lee: no se supone su
   contenido.**
2. **El número de mesa NO se adivinó: se midió.** Existen `pista/s106-b` y
   `pista/s106-c` (B ya commiteó su turno 1). De ahí `pista/s106-d`.

---

## §1.1 · LO QUE LLEGÓ vs LA LISTA DEL PLAN

La lista es `LETRA_DEUNA` §12 — **cuatro preguntas abiertas**. Contraste literal:

| # | pregunta de §12 | ¿llegó? | qué dice el dato |
|---|---|---|---|
| 1 | 🔴 `pointOfSale` para canal digital, y quién lo emite | ✅ **SÍ** | **QA = `4262774`** |
| 2 | cómo se **registra y rota** URL + headers en QA y PDN | ⚠️ **PARCIAL** | **registro**: nosotros enviamos URL+headers, **ellos configuran de su lado** |
| 3 | ¿firma más fuerte que headers estáticos? | ✅ **SÍ (y la respuesta es NO)** | **solo headers estáticos** — no hay nada más fuerte |
| 4 | ¿QA permite simular `REVERSED` / `REVERSED_FAILED`? | ✅ **SÍ** | se puede simular contra QA |

### Lo que NO llegó — y no se supone

- 🔴 **El mecanismo de ROTACIÓN.** La pregunta 2 tenía dos mitades y volvió
  media: sabemos **cómo se registra**, no **cómo se cambia después** ni **cuánto
  tarda**. *Y esa mitad es la que dimensiona la ventana ciega* — el buzón está
  escrito con **dos secretos** (`ACTUAL` + `SIGUIENTE`) precisamente porque el
  header lo cambia un tercero. **El diseño de dos secretos queda ratificado; su
  ventana sigue sin número.**
- **El `pointOfSale` de PDN** — declarado explícitamente como «otro», y por
  orden de la mesa **no se pide ahora**.
- **El rate limit**: respuesta **parcial, pendiente**. `LETRA_DEUNA` §2 declara
  «~1 req/s» **tomado de la doc en papel** — sigue **sin confirmar contra el
  proveedor**, y el barrido espacia sus consultas apoyado en ese número.
- **Si el webhook se registra por ambiente** (una URL para QA y otra para PDN).
  Se deduce que sí; **no está confirmado**, y no se escribe como si lo estuviera.

### 🔴 Y un dato que NO se preguntó y CONTRADICE letra firmada

**Ventana de reverso: `24 HORAS`.** `LETRA_DEUNA` §8 dice hoy, textual:

> *«✅ **RESUELTO — ya no es supuesto.** ~~un recuadro dice «dentro de las 24
> horas», otro «solo mismo día»~~. **El proveedor lo confirmó por mensaje:
> MISMO DÍA.***

**Las dos afirmaciones vienen del MISMO proveedor con dos días de diferencia**
(22-ago «mismo día» · 24-ago «24 horas»), **y no son lo mismo**: un pago de las
23:50 tiene diez minutos de ventana con una y veinticuatro horas con la otra.

- **No bloquea hoy**, y se dice por qué: §8 declara que **el reverso por API no
  entra en v1** (camino manual de soporte, se construye después del circuito de
  cobro).
- **Sí deja letra desactualizada**: §8, la firma ③ de §11 y el historial v1.2
  celebran «pasa de supuesto a hecho». **Enmendarla es de A / la mesa**, no de
  esta pista; se levanta acá para que nadie la cite creyéndola vigente.
- **Lo que deja como lección, y es del método:** *un «confirmado por mensaje» de
  un proveedor no es más estable que su propia doc — es la misma fuente con otra
  fecha.* La letra trató el mensaje del 22-ago como cierre definitivo (tachó la
  ambigüedad, cerró la pregunta y movió la firma de supuesto a hecho) y **dos
  días después el mismo emisor dijo lo otro.**

---

## §1.2 · ESTADO REAL — el actuador, el wrapper, el riel

**Se corrieron las DOS preguntas de `L-402`**, porque una sola no alcanza:
*¿está alcanzable?* y *¿corrió alguna vez?*

### ¿Corrió alguna vez? — **NO. Cero veces.**

| tabla | medido |
|---|---|
| `pagos_intentos` por proveedor | `nuvei` 30 · `seed_gate` 9 · `simulado` 4 · `siembra` 1 · **`deuna` 0** |
| `webhook_events` por proveedor | `nuvei` 65 · **`deuna` 0** |
| `webhook_events` por origen | `webhook` 65 · **`barrido` 0 · `consulta_activa` 0** |

⇒ **Todo lo que se afirme «vivo» del lado DeUna es lectura de código, no
ejecución.** Es exactamente la distinción que `L-402` ordena declarar.

### Lo que SÍ existe y está aplicado (medido, no recordado)

| pieza | estado | evidencia |
|---|---|---|
| Migraciones | **411 local = 411 remoto, 0 desemparejadas** | `migration list` parseado |
| `20260822220000` forma DeUna | ✅ **aplicada en remoto** | presente en local **y** remote |
| Columnas del riel en `pagos_intentos` | ✅ **completas** | `codigo_numerico` · `codigo_expira_en` · `referencia_corta` · `proveedor_transaction_id` · **`proveedor_reverso_id`** · `hallazgo` · `hallazgo_en` |
| CHECK de la referencia (§4) | ✅ **`<= 20`, al literal de la letra** | `chk_referencia_corta_cabe` |
| Candado de idempotencia (§4) | ✅ **existe, en dos índices parciales** | `uq_pagos_intentos_tx_por_cita` y `…_por_pedido` sobre `(proveedor, proveedor_transaction_id, sujeto)` + `uq_…_referencia_corta` |
| Generador de referencia | ✅ vive | `deuna_nueva_referencia` + `_deuna_base36` |
| Gate `_evento_autenticado` | ✅ **conoce `deuna`, en COLUMNAS** | `coalesce(stoken_valido,false) AND verificado IS TRUE`; `ELSE false` fail-closed |
| `aplicar_evento_de_pago` | ✅ **conoce `deuna`** (y `proveedor`) | pero ver el hueco abajo |
| Cron del barrido DeUna | ✅ **NO existe** — correcto por `§13bis ③` | 19 jobs, ninguno DeUna |
| `app_config.pagos_actuador_vivo` | `true` | |
| Secretos `DEUNA_API_KEY` / `DEUNA_API_SECRET` | ✅ **ya cargados** | `secrets list` |

### Lo que está escrito y NO desplegado

**Cero funciones `pagos-deuna-*` desplegadas.** Medido contra la API del
proyecto: 24 functions ACTIVE, **ninguna DeUna**. No es solo
`pagos-deuna-solicitud` como decía el `CONTRATO_WRAPPER_DEUNA` §6 — **son las
tres**: `solicitud`, `webhook` y `barrido`.

### Lo que FALTA — con dueño

| # | hueco | dueño |
|---|---|---|
| 1 | **`D-887` · el aplicador del barrido.** Confirmado en la base, no en prosa: `aplicar_evento_de_pago` **no menciona `origen` ni `barrido`** ⇒ el barrido clasifica `confirmado`, loguea *«lo aplica el actuador»*, **y nadie aplica nada** | **A (motor)** |
| 2 | Rama del gate para `origen` (§13ter): **no escrita, a propósito** — sería puerta sin motor, y ⛔ jamás `stoken_valido=true` desde el barrido | A |
| 3 | **Los DOS flips de `§13bis ②`.** Medido: `DEUNA_ELEGIBLE = false` (`fila-medio-de-pago.tsx:272`) **y** `deuna-estado.ts` sigue con el bloque `ENSAYO` vivo, que su propio archivo declara que *«muere entero»* al enchufarse | **C** |

---

## 🔴 §1.2bis · EL HALLAZGO DEL TURNO — `main` TIENE LA VERSIÓN VIEJA DEL BUZÓN

**Salió de medir el repo por ancestría, no por lectura.** `pista/s103-d` **nunca
se mergeó a `main`** (`git merge-base --is-ancestor` → NO) y tiene commits
posteriores a los que sí llegaron.

**El número que lo prueba, con su control:**

```
verificado:  main → 0 ocurrencias   ·   pista/s103-d → 2
origen:      main → 0 ocurrencias   ·   pista/s103-d → 2
```

**Por qué es grave y no cosmético:** el gate vivo **en la base** exige, para
DeUna, `verificado IS TRUE`. **La versión de `main` nunca escribe esa columna.**

> ### Desplegar el webhook desde `main` habría producido una función que responde `200`, guarda el crudo, consulta al proveedor — **y cuyo evento el gate rechaza SIEMPRE**, para todo pago, sin error y sin log.
>
> *El pago entra, la plata se mueve del lado del cliente, y el sujeto no se
> mueve nunca.* Es el modo de falla que `§13bis ①` describe palabra por palabra
> — **y lo habríamos construido nosotros, no el proveedor.**

El commit que falta se llama, literal: *«S103-D · el buzón escribe verificado y
origen: **el requisito bloqueante de A, cerrado**»*. **Se cerró en la rama y no
llegó al canon** — `L-217` en su forma más cara: no es un documento perdido, es
el eslabón que hace funcionar el circuito.

**Alcance completo del rescate** (`main` vs `pista/s103-d`), separando **ausente**
de **divergente**, porque no son el mismo problema:

| | archivo |
|---|---|
| **AUSENTE** | `docs/CONTRATO_APLICADOR_BARRIDO_DEUNA.md` ← **el que la ficha `D-887` cita como «contrato ya escrito»** |
| **AUSENTE** | `docs/INSUMO_D405_ELIMINACION_Y_PORTABILIDAD.md` |
| **AUSENTE** | `docs/relevamientos/S103-D-PAQUETE-ALTA-WEBHOOK.md` |
| **AUSENTE** | `supabase/functions/pagos-deuna-solicitud/_codigos.test.ts` |
| **DIFIERE** | `pagos-deuna-webhook/index.ts` (265 → 306) · `_verdad.ts` · `index.test.ts` |
| **DIFIERE** | `pagos-deuna-barrido/index.ts` (190 → 215) |
| **DIFIERE** | 4 relevamientos S103-D · `simulador-local.mjs` · `sondeo-qa.mjs` · las migraciones sin número |

⇒ **`D-887` tiene dueño A y su contrato NO está donde su ficha dice.** A abriría
ese trabajo buscando un archivo que en el canon no existe.

**Nada de esto se mergeó desde esta pista:** `main` es territorio de A. **Se
reporta con el comando de rescate listo** (`git show
pista/s103-d:<ruta>`), y **el despliegue de esta pista se hará desde la versión
correcta, no desde `main`** — declarado acá para que quede escrito antes de
tocar nada.

---

## §3 · LA LIMITACIÓN DEL PROVEEDOR — registrada, no suavizada

> ### **DeUna NO ofrece firma criptográfica de webhook. Su único mecanismo de autenticación son headers estáticos. Preguntado y contestado por el proveedor, 24-ago-2026.**

**Qué significa exactamente, sin adorno:** cualquiera que conozca la URL **y el
valor del header** puede emitir un webhook indistinguible de uno legítimo. **No
hay HMAC, no hay timestamp firmado, no hay reintento verificable.** Contra Nuvei
—que sí firma— **este riel es estructuralmente más débil, y lo va a seguir
siendo**: no es una etapa, es el techo del proveedor.

**Consecuencia operativa, y por eso la capa ② no es opcional:**

- **El buzón de dos capas es la ÚNICA defensa.** Capa ① secreto propio en header;
  capa ② **consulta activa obligatoria** — *un webhook con el secreto correcto y
  datos falsos muere en `payment/info`.* **Quitar la capa ② deja el riel apoyado
  entero en un string que viaja en texto plano en cada request y que administra
  un tercero.**
- **El secreto lo generamos y lo custodiamos nosotros**, y **lo rota un tercero
  a pedido** — de ahí los dos secretos (`ACTUAL` + `SIGUIENTE`). *Medido en el
  código: `coincide()` es fail-closed con vacío (`if (!esperado || !recibido)
  return false`), así que un `SIGUIENTE` sin cargar no abre nada.* **Verificado,
  no supuesto.**
- 🔴 **Y lo que esto le prohíbe a cualquier sesión futura:** **el webhook de
  DeUna jamás transiciona un sujeto por sí solo.** Con Nuvei se podría discutir;
  acá **no hay discusión posible**, porque no hay nada que verifique el origen
  del mensaje salvo un secreto compartido.

---

## §4 · PREPARADO, NO EJECUTADO

### (a) Los secretos a fijar — medidos del código, no de la letra

Fuente: `Deno.env.get(...)` de las tres functions, contrastado contra
`secrets list`.

| secret | estado | valor | quién |
|---|---|---|---|
| `DEUNA_API_KEY` | ✅ ya cargado | — | — |
| `DEUNA_API_SECRET` | ✅ ya cargado | — | — |
| `PAGOS_AMBIENTE` | ✅ ya cargado | ⚠️ **su VALOR no se midió** (es secret). Si dijera `produccion`, la base salta a **PDN** en las tres functions. **Se confirma antes de desplegar** | founder / dashboard |
| **`DEUNA_POINT_OF_SALE`** | 🔴 **FALTA — es el bloqueante** | **`4262774`** (QA) | dato del proveedor, ya en mano |
| **`DEUNA_WEBHOOK_SECRET`** | 🔴 **FALTA** | **por generar** | 🔑 **founder — `L-409`: ningún secreto se inventa ni se rota sin firma** |
| `DEUNA_WEBHOOK_SECRET_SIGUIENTE` | ⚪ **opcional al arranque** | vacío | fail-closed **medido**; se carga al primer ciclo de rotación |
| `DEUNA_BASE_URL` | ⚪ **NO se fija** | — | es **override del simulador local** y **tiene candado**: en `produccion` se ignora. El default ya es QA |

**Restricción medida para el secreto del webhook:** el proveedor acepta header
**value ≤ 100 caracteres** (§2). Lo que se genere entra en ese techo.

### (b) El mensaje a Gabriela — para que lo mande el founder

Los dos datos salen del **código**, no de la letra:
`HEADER_SECRETO = 'x-epetplace-secret'`
(`pagos-deuna-webhook/index.ts:60`) y el project ref del proyecto linkeado.

**Verificado contra los límites de §2 antes de escribirlo:** URL `https`, **sin
IP**, **73 caracteres** (tope 200) · header key **18** (tope 50) · value ≤ 100.

```
Hola Gabriela, buen día.

Van los datos para que registren nuestro webhook de pagos exitosos en QA:

URL (POST, https):
https://zyltipqscdsdsxnjclhp.supabase.co/functions/v1/pagos-deuna-webhook

Header de autenticación:
  nombre: x-epetplace-secret
  valor:  <PENDIENTE — se envía por canal aparte>

Confirmanos por favor:
1) que quedó registrado y desde cuándo está activo;
2) el procedimiento para CAMBIAR ese valor más adelante y cuánto demora
   (tenemos previsto un cambio de secreto sin corte y necesitamos saber
   la ventana);
3) si para producción se registra una URL distinta o se reemplaza esta.

Gracias.
```

🔴 **El valor del header NO va en este mensaje ni en ningún chat**: se genera
con firma del founder y viaja por canal aparte. *Un secreto pegado en el mismo
mensaje que la URL entrega las dos mitades juntas.*

⚠️ **El punto 2 no es cortesía: es la mitad que faltó de la pregunta §12.2**, y
sin su respuesta el ciclo de rotación no tiene número.

---

## COLA VIVA — próximo paso ejecutable

Nada de esto se ejecuta antes del **Checkpoint 1**.

1. 🔑 **Founder:** generar `DEUNA_WEBHOOK_SECRET` (≤100 chars) y confirmar el
   **valor de `PAGOS_AMBIENTE`**.
2. **A:** el rescate de `pista/s103-d` → `main` (la lista de §1.2bis).
   **Bloquea el despliegue del buzón**, y por eso va antes que la tanda.
3. Fijar `DEUNA_POINT_OF_SALE=4262774` + el secreto del webhook.
4. Desplegar las tres, **el buzón con `--no-verify-jwt`** *(medido: no hay
   entradas `pagos-deuna` en `config.toml`; el precedente es `pagos-webhook-stg`
   con `verify_jwt:false`)*. **Desde la versión rescatada, jamás desde el `main`
   de hoy.**
5. Enviar el mensaje a Gabriela y esperar el alta del webhook.
6. **Probar `REVERSED` / `REVERSED_FAILED`** con la simulación de QA, que recién
   ahora existe. `REVERSED_FAILED` es 🔴 hallazgo con nombre — se prueba que
   **jamás se resuelve solo**.
7. **El cron del barrido NO se enciende** (`§13bis ③`): sin aplicador (`D-887`) y
   sin corrida a mano, un barrido que escala lo mismo cada pasada entrena a
   ignorarlo.

## COMPROMISOS CRUZADOS

| con | qué |
|---|---|
| **A** | debo: el reporte del rescate y la contradicción de §8 · **me deben**: el merge a `main` y el aplicador `D-887` |
| **C** | **me deben**: los dos flips de `§13bis ②` — **y son dos**; el primero solo no conecta la pantalla |
| **mesa** | enmienda de `LETRA_DEUNA` §8 / firma ③ por la ventana de 24 h |

## SUPUESTOS VIVOS — declarados, no dados por ciertos

- `PAGOS_AMBIENTE` = sandbox. **No medido** (es secret).
- Rate limit ~1 req/s. **De la doc en papel**, sin confirmar por el proveedor.
- El webhook se registra por ambiente. **Deducido**, no confirmado.
