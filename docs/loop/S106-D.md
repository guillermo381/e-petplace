# S106-D · PARTE DEL LOOP

> **Pista D — telemedicina · transporte de video y edge functions.**
> Fuente del handoff: si compacto, esta es la única memoria. Todo lo demás
> se relee del objeto.

## Identidad y territorio

- **worktree:** `/Users/guillo381gmail.com/proyectos/ePetPlace/e-petplace-s106-d`
- **rama:** `pista/s106-d` · nació de `origin/main` en `09def031`
- **`main` se movió** durante la tanda (`09def031` → `ecbce6f5`, el plan de
  A). **Mergeado sin conflicto: cero roce de territorio.**
- 🔴 **node_modules: NO EXISTE, re-medido al abrir la tanda 1.** No estoy
  enganchado al primario.
  **`pnpm install` NO se corrió, con razón declarada:** la edge es **Deno**
  (no lee `node_modules`), y el generador de tokens y el arnés los escribí
  **sin una sola dependencia** (Web Crypto + `fetch` nativos).
  *Instalar un árbol de dependencias para firmar un JWT de veinte líneas es
  pagar ruido por nada.* Los tres corren y están probados.

### Hallazgos del paso ⓪ (turno anterior, se conservan)
1. El directorio `e-petplace-s106-d` **ya existía con `pista/s105-d` adentro**.
   Verifiqué que su commit fuera ancestro de `origin/main` **por SHA** antes
   de reapuntarlo. No se destruyó nada.
2. 🔴 **`git worktree add -b` creó la rama y después falló por el directorio**,
   y su mensaje habló **sólo del directorio**. *Un error dice qué falló, no
   qué NO pasó.*

## Estado: TANDA 1 — los cuatro actos

| acto | qué | estado |
|---|---|---|
| **1** | pedido de alta LiveKit al founder | ✅ **entregado** |
| **2** | spec del cable para C + generador de tokens | ✅ **entregado y probado** |
| **3** | `video-token` (edge) + pedido de RPC a A | ✅ **escrita · esperando la RPC** |
| **4** | arnés + `verify-edge-deno` en copia aislada | ✅ **corre · gate VERDE** |

**Entregables:**
- `docs/relevamientos/2026-08-25-s106-d-PEDIDO-ALTA-LIVEKIT.md`
- `docs/relevamientos/2026-08-25-s106-d-SPEC-CABLE-C.md`
- `docs/relevamientos/2026-08-25-s106-d-PEDIDO-RPC-A.md`
- `supabase/functions/video-token/index.ts`
- `supabase/functions/video-token/arnes.mjs`
- `supabase/functions/video-token/generar-token-prueba.mjs`

## TANDA 1bis (26-ago) — las claves llegaron y la letra cambió

**`main` se movió otra vez** (`ecbce6f5` → `991b1e1d`): A depositó
**`LETRA_TELEMEDICINA` v1.1 con las enmiendas de CP1**, el acta de CP1, y
**tres correcciones de mesa dirigidas a C y D**. Mergeado sin conflicto.

### ✅ Mis tres hallazgos de la tanda 1 fueron adoptados
- **firma ⓪ de §7:** la teleconsulta **no se graba en v1**, y la letra cita
  mi `roomRecord: false` **explícito, no heredado del default**.
- **`D-930`:** `cita_telemedicina_detalle` **se mata** — más fuerte que mi
  pedido, que era sólo «no la uso».
- **`D-931`:** los webhooks quedan **«registrar sin juzgar»**, escritos y sin
  construir hasta firma. *Si el sistema decide quién faltó, decide quién
  paga* — choca con la firma ② de §5.

### ✅ Los secretos, verificados CONTRA EL OBJETO
`supabase secrets list` → las tres con **el nombre exacto** que la edge lee,
selladas `2026-08-26T04:30:31Z`. *No se dio por buena la declaración: un
nombre con typo se ve idéntico a uno correcto hasta que la edge devuelve
`video_sin_configurar`.*

### 🔴 EL HALLAZGO DE LA TANDA — mi pedido a A tenía el eje equivocado
La letra **v1.1 §7 ②** firma que la marca de teleconsulta es
**`modalidad='telemedicina'`** (por `BIO_EXPEDIENTE` D13.6). **Mi pedido
decía `tipo_servicio`.** Enmendado.

Y al medirlo apareció algo peor, con literal del hold que midió A:
```
v_modalidad := COALESCE(p_modalidad, 'local');
IF v_modalidad NOT IN ('local', 'domicilio') THEN RAISE 'modalidad_invalida'
```
⇒ **la única puerta que crea citas RECHAZA `'telemedicina'`**, y sin
parámetro la cita **nace `'local'`**.

> **Si la RPC gatea por `modalidad` y el hold no la deja nacer, el gate es
> correcto y ninguna teleconsulta pasa jamás.** `L-318` en su forma exacta —
> con el agravante de que **no falla**: devuelve `no_es_teleconsulta`, que se
> lee como rechazo legítimo.

**Y el atajo es peor:** gatear por `tipo_servicio` deja entrar la cita con
`modalidad='local'`, y §7 dice que **la marca del expediente es la
modalidad** ⇒ una teleconsulta registrada como *presencial en el local*.
⇒ Pedí a A que ① (abrir la modalidad en el hold) y ② (la RPC) **viajen
juntas, o que declare cuál falta.** Las dos son motor y son suyas.

**El caso entró al arnés como discriminador** (`CITA_TIPO_SIN_MODALIDAD`):
si entrega token, el gate está leyendo el eje equivocado. **7 casos ahora.**

### 🔴 Los dos tokens de prueba: NO los genero yo, y es a propósito
`secrets list` devuelve **hashes, no valores** — que es lo correcto. **No
tengo las claves y no debo tenerlas.** Pedirlas para generar dos tokens sería
sacarlas del único lugar donde están seguras y meterlas en un chat (`D-712`).
⇒ **los genera quien las tiene**, con el comando que dejé en la spec de C.
Lo que sale **sí puede viajar**: URL + dos tokens de 1 h de una sala de
juguete que no abre nada más.

## Los tres hallazgos de la tanda 1

### ① 🔴 `cita_telemedicina_detalle` — el token NO se persiste
A midió una tabla del legado con **`token_prestador`** y **`token_cliente`**.
**`video-token` no la lee ni la escribe**, por cuatro razones medidas:
un token es credencial de vida corta y guardarlo lo desacopla de su ventana ·
su policy de SELECT alcanza a quien tenga acceso a la mascota ⇒ **el dueño
podría leer el token del veterinario** · su CHECK de `proveedor` sólo admite
`{daily, whereby, zoom}` ⇒ **rebota `livekit`** · trae `grabacion_url` y la
letra **no menciona grabación**.
⇒ Declarado en el pedido a A y en el encabezado de la edge, **para que nadie
la conecte por prolijidad**. Su adjudicación sigue siendo de la mesa.

### ② 🔴 El arnés tenía el defecto que vino a cazar — y lo destapó CORRERLO
Un 404 de la plataforma (`{"code":"NOT_FOUND"}`) **no trae `ok`**, así que la
primera versión lo leía como «no rebotó» e imprimía
**«¡ENTREGÓ TOKEN A QUIEN NO DEBÍA!»** sobre una function que **ni existe**.
*Un rojo que miente sobre su causa manda a auditar una puerta abierta que
nunca se abrió.* **Curado: tres estados, y el tercero es NO CONCLUYENTE.**
**No lo encontró leer el código: lo encontró correrlo.**

### ③ 🟢 LiveKit SÍ publica webhooks, y uno le habla a §5 de la letra
Medidos en `docs.livekit.io/home/server/webhooks/`: `room_started` ·
`room_finished` · **`participant_joined`** · **`participant_left`** ·
**`participant_connection_aborted`** · `track_published` · `track_unpublished`
· `egress_*` · `ingress_*`. Auth por **JWT en `Authorization` con sha256 del
payload**; se configuran por proyecto en Cloud.

⚠️ **Matiz, no contradicción:** `LETRA_TELEMEDICINA` §5 dice *«el sistema no
mide la calidad de la conexión de nadie, así que no puede atribuirla»* — eso
**sigue siendo cierto**: un evento de corte **no dice de quién fue la culpa**.
Pero `participant_connection_aborted` **sí registra que pasó**, y
`participant_left` diría si el dueño llegó a entrar.
⇒ **La letra suponía que ese hecho no existía en ninguna parte, y existe.**
**Decisión de mesa, no de pista** — no construí nada de esto.

## Frenos respetados

- 🔴 **`pagos-*` congeladas** — no toqué ninguna.
- 🔴 **Ninguna key pedida ni vista.** El alta es acto del founder; el pedido
  le dice cómo cargarlas **desde su terminal**, nunca por chat ni repo.
- 🔴 **`verify-edge-deno` sobre COPIA FUERA DEL REPO** (el script ya aísla) y
  **jamás en pre-commit**. Comando y resultado en el reporte.
- ✅ **76(f)** commit por ruta explícita · **76(b)** los tres pedidos son texto
  completo autocontenido · **L-141** números por grep, no de memoria.
- ✅ **`L-408`/vault no aplica** — lectura aceptada por la mesa; los secretos
  van por `Deno.env`.

## Compromisos abiertos

### Qué debo
- Nada. Los tres pedidos están depositados.

### Qué me deben — y bloquea el verde de `video-token`
- 🔴 **A → la RPC `puede_entrar_a_videollamada`.** Pedido completo en
  `2026-08-25-s106-d-PEDIDO-RPC-A.md`. **La ventana (±15 min) es propuesta
  mía y la firma la mesa con el pedido.**
  ⚠️ **Con un aviso que A midió y le devuelvo:** `acepta_telemedicina` está
  en **false en 11 de 11** prestadores y hay **0 citas de telemedicina** ⇒
  **hoy no existe una sola cita con la que probar el camino feliz.**
- 🔴 **Founder → el alta y los tres secretos** (`LIVEKIT_URL`,
  `LIVEKIT_API_KEY`, `LIVEKIT_API_SECRET`), más **la celda que dejé NO
  MEDIDA**: minutos incluidos y precio del excedente del plan pago, leídos
  del panel real.
- 🔴 **C → la prueba de cable.** El CP1 está firmado **condicionado** a que
  dé verde. **La escalera de caída la decide la mesa, no la pista.**

## Lo que sigue NO MEDIDO
- **Región / latencia Ecuador** — de ningún proveedor. **Sólo la cierra la
  prueba de C en la red real de Quito.**
- **minSdk / target / cuánto crece el APK** — sólo lo dice la build de C.
- **Precio de Vonage** (403) y **plan pago real de LiveKit** (pedido al founder).
- 🔴 **`video-token` NUNCA CORRIÓ contra una cita real.** No está desplegada.
  El arnés lo declara **NO CONCLUYENTE** en vez de dar verde flojo.
  *No se declara nada de esta pieza como probado hasta que el arnés ejerza
  los seis casos.*

## Próximo paso ejecutable

1. Llega la RPC de A → correr el arnés **con datos de verdad** (6 casos).
2. Desplegar `video-token` con `--use-api` (sin Docker).
3. Correr `verify-edge-deno` de nuevo en copia aislada.
4. Reportar el arnés **con su salida literal** — verdes y rojos ejercidos.
