# S106-D · PARTE DEL LOOP

> **Pista D — telemedicina · transporte de video y edge functions.**
> Fuente del handoff: si compacto, esta es la única memoria. Todo lo demás
> se relee del objeto.

## Identidad y territorio

- **worktree:** `/Users/guillo381gmail.com/proyectos/ePetPlace/e-petplace-s106-d`
- **rama:** `pista/s106-d` · nació de `origin/main` en `09def031`
- **node_modules: NO EXISTE** en este worktree (ni raíz ni apps). **No estoy
  enganchado al primario** — a diferencia de S99, donde D fue el único que sí.
  ⚠️ **Consecuencia: no puedo correr typecheck ni verify sin `pnpm install`.**
  En este turno no hizo falta (sin código); **en la tanda 1 sí.**

### Dos hallazgos del paso ⓪ que valen para las otras pistas

1. 🔴 **El directorio `e-petplace-s106-d` YA EXISTÍA con la rama
   `pista/s105-d` adentro** — un worktree bautizado con el nombre de la
   sesión siguiente. Verifiqué contra el objeto que su commit `6f3c1f4c`
   **es ancestro de `origin/main`** y que la rama está en origin **por SHA**
   antes de tocar nada. *No se destruyó trabajo; se reapuntó.*
2. 🔴 **`git worktree add -b` CREÓ LA RAMA y después falló por el
   directorio** — y su mensaje habló **sólo del directorio**
   (`fatal: '...' already exists`). La rama quedó huérfana y sin declarar.
   ⇒ **Es la ley de S105 en su forma exacta: un mensaje de error dice qué
   falló, no qué NO pasó.** La medí (apuntaba a `09def031`, sin commits
   propios, no en origin) y recién ahí la adopté.

## Estado al cierre del turno 1

**Turno 1 = medición pura. CERO código, cero cuentas, cero keys.** ✅ cumplido.

| | qué | estado |
|---|---|---|
| D-M1 🔴 | transporte: 8 candidatos medidos contra npm/GitHub/RN-Directory/pricing | ✅ **entregado** |
| D-M2 | molde de edge function nueva (config, secrets, verify, dónde vive) | ✅ entregado |
| D-M3 | seguridad del token: diseño mínimo, **no construido** | ✅ entregado |

**Reporte:** `docs/relevamientos/2026-08-25-s106-d-transporte.md`

**Recomendación (la mesa decide, yo no elijo): LiveKit Cloud**, con
self-host Apache-2.0 como salida sin lock-in. Segundo real: **Agora**
(más barato, free tier doble, **sin self-host**).

## Lo que quedó NO MEDIDO — y no se rellena de memoria

- **Región / latencia para Ecuador** — de ningún proveedor. Sólo se cierra
  probando desde Ecuador.
- **Precio de Vonage** — su página de pricing devuelve **HTTP 403**.
- **minSdk / target / peso del binario** de cualquier SDK — sólo lo cierra
  una build.
- **TURN de los proveedores cloud** — no verificado en doc. *(Sí medido:
  LiveKit self-host trae TURN embebido, literal en su `config-sample.yaml`.)*
- **Compatibilidad real con Expo SDK 57 de cualquier SDK** — 🔴 **ninguno la
  declara, y cero issues reportadas en los repos.** Ausencia de reportes **no
  es** evidencia de compatibilidad. **Sólo una build lo cierra.**

## Frenos y supuestos vivos

- 🔴 **El freno general del turno se respetó entero:** nada construido,
  ninguna cuenta creada, ninguna key pedida.
- 🔴 **No toqué NADA de S105:** ni `pagos-*` (mi territorio de S105), ni el
  deploy de `pagos-web`, ni el guard del IVA, ni la puerta de retomar, ni
  ninguna de las siete piezas no ejercidas del acta §②.
- ⚠️ **Freno declarado sobre mi propia medición:** usé `codegenConfig` como
  señal de New Architecture y **lo marqué como insuficiente en el reporte** —
  mide «tiene codegen», no «soporta new arch» (el interop layer corre módulos
  legacy). Las celdas sin dato dicen **NO MEDIDO**, jamás ❌.
- ⚠️ **`L-408` (vault) NO aplica al caso de video** y lo dejé escrito: ese
  patrón resuelve el secreto que **`pg_cron`** necesita para llamar una edge
  desde la DB. Una edge llamada por la app lee `Deno.env`. *Copiarlo acá
  sería aplicar un patrón a un problema que no tiene.*

## Compromisos abiertos

### Qué debo
- Nada pendiente del turno 1.

### Qué me deben — **y bloquea la tanda 1**
- 🔴 **A la MESA: la firma del proveedor.** Sin ella no hay pieza que
  construir. Con ella se dispara el pedido §9 al founder (alta de cuenta +
  tres secretos).
- 🔴 **A la MESA: la decisión del tren de build.** `LETRA_TELEMEDICINA` §9 la
  pone explícitamente en sus manos. Nada de esto viaja por OTA.
- 🟡 **A la pista A (escritora única de DB):** la RPC
  `puede_entrar_a_videollamada(p_cita_id, p_user_id)` de §7 del reporte.
  **La migración es territorio de A, no mío** — se la pido como bloque
  autocontenido cuando la mesa firme, jamás la tomo.

### Preguntas servidas a la mesa (§5 del reporte)
Volumen esperado/mes · residencia de datos y privacidad clínica · latencia
Ecuador · ¿se paga la build de prueba? · **¿se graba la teleconsulta?** (la
letra **no lo menciona** — lo declaro como pregunta, no como supuesto) ·
qué hacer con Vonage sin precio.

## Propuesta de territorio para la TANDA 1 (76h) — a confirmar por la mesa

**Míos:**
- `supabase/functions/video-token/**` *(nueva)*
- `supabase/functions/_shared/video.ts` *(sólo si el molde lo pide)*
- `docs/relevamientos/2026-08-25-s106-d-transporte.md`
- `docs/loop/S106-D.md`

**Explícitamente NO míos:**
- `supabase/migrations/**` → **A** (la RPC de §7 se la pido)
- `packages/api/**` → wrapper del token: **de quien la mesa asigne**, no lo tomo
- `apps/**` → C
- `packages/ui/**` → B
- `supabase/functions/pagos-*` → **congeladas este turno** (S105)

## Próximo paso ejecutable

**Estoy bloqueado por firma, y es correcto que lo esté.** Al reabrir:
1. Leer la firma de la mesa sobre proveedor + tren de build.
2. Si firmó: emitir el pedido §9 al founder y el pedido de RPC a A.
3. `pnpm install` en el worktree **antes** de la primera línea de código
   (hoy no hay `node_modules`).
4. Recién entonces `video-token`, con `exigirSesion` + RPC de veredicto.
