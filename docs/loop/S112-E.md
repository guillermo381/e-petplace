# S112 · PISTA E — MEDICIÓN Y EJERCICIO · PARTE

> **Territorio:** ninguno de escritura. **Cero migraciones, cero DDL, cero
> seeds, cero pantallas, cero wrappers.** Esta pista mide contra el objeto y
> ejerce por camino real. Worktree `e-petplace-s112-e`, rama `pista/s112-e`,
> base `main` = **`978666bd`**.
>
> **Todo lo medido lleva su «contra qué y cuándo» adentro de cada entregable.**

---

## LOS CUATRO ENTREGABLES

| # | qué | a quién | archivo |
|---|---|---|---|
| ① | Censo de las **22 variables + 7 condicionales** del acta | A | `docs/loop/buzon/S112-E-para-A-CENSO-ACTA.md` |
| ② | El **formulario de postulación**, literal (lo espera el abogado) | C y founder | `docs/loop/buzon/S112-E-para-C-y-FOUNDER-CENSO-FORMULARIO.md` |
| ③ | Las **cuatro puertas legales**: código contra pantalla | A | `docs/loop/buzon/S112-E-para-A-CENSO-PUERTAS-LEGALES.md` |
| ④ | El **primer traspaso**: estado, los cuatro rojos y la falla predicha | A | `docs/loop/buzon/S112-E-para-A-TRASPASO-ESTADO.md` |
| ⑤ | **El alta de refugio**: la puerta que existe, la que falta y los pasos de mañana | A y founder | `docs/loop/buzon/S112-E-para-A-y-FOUNDER-ALTA-DE-REFUGIO.md` |

---

## LO QUE ESTA PISTA ENCONTRÓ, EN ORDEN DE PESO

1. 🔴 **El verde del ejercicio tiene una falla predicha con ficha:** el
   traspaso mueve `mascotas.familia_id` y **ninguna de las tres policies de
   SELECT de `mascotas` mira la familia**. Es **`D-485`**, abierta desde S73 y
   caída en silencio en S74 — **y el primer traspaso real es su disparo.**
   Predicción registrada antes de correr: el adoptante no ve al animal, el
   refugio lo sigue viendo.
2. 🔴 **De los cuatro rojos pedidos, ninguno tiene guard hoy:** tres porque la
   firma no existe (cero columnas de firma en 299 tablas; la RPC sigue con 4
   parámetros), y el cuarto porque **su condición se curó esta tarde** — el
   acta se cargó a las 21:26 y la puerta fail-closed se abrió sola.
3. 🔴 **La única puerta que frena es la única que nadie puede ver frenar:** el
   acta tiene guard, fail-closed y buena voz — y **cero pantallas**. Términos,
   condiciones y consentimiento tienen texto vigente y **cero exigencia**.
   El consentimiento de adopción es hoy **inexpresable**: el CHECK de
   `consentimientos.tipo` es un vocabulario cerrado de 7 sin valor de adopción.
4. **El formulario de postulación no existe** — cero plantillas, cero campos,
   un solo toque — y **no se pide ningún dato de menores en ninguna parte**
   (el rol `menor` de `familia_miembro` tiene 0 filas y no guarda nombre ni
   edad). Es la respuesta literal que esperaba el abogado.
5. 🔴 **El alta de refugio no se puede completar por ninguna puerta:** pedir la
   naturaleza se puede (el motor acepta el enum entero), **conceder el rol
   `refugio` no tiene función** —censadas las 4 que insertan en `cuenta_roles`—,
   `obtener_mi_cuenta_refugio` **no existe**, y `obtener_contexto_arranque` no
   nombra `refugio` en 4 757 caracteres: el refugio entra a la app y lee **«Tu
   cuenta no tiene un negocio asociado»**, con «Cerrar sesión» como única
   acción. Y al registrarse aceptaría **`terminos_professional`**, el documento
   que sus propios términos declaran que no rige su cuenta.
6. **De 22 variables del acta: 6 llenas o escritas por el motor, 7 vacías o a
   medias, 7 inexistentes** — entre ellas las dos firmas, REMETFU, señas y el
   origen rescate/cesión. **Sólo 2 declaran fallback.**

---

## LO EJERCIDO POR CAMINO REAL (con sus controles)

- **Vidriera sin sesión:** `obtener_adoptables` con anon key → **200 `[]`** ·
  control negativo `obtener_mis_solicitudes_adopcion` → **401**.
- **Instrumento del traspaso:** dos rojos **distintos** sobre la misma función
  (`mascota_no_existe`, `familia_destino_no_existe`) + un tercero
  (`familia_destino_igual_al_origen`) ⇒ la sonda **discrimina qué guard
  contestó**, que es lo que hace falta para no leer un rojo cualquiera como
  «la puerta del acta funciona».
- **Puerta del refugio:** `publicar_adoptable` → **`no_sos_cuenta_de_refugio`**
  · **control positivo** `obtener_mis_solicitudes_adopcion` → **VERDE, 0
  filas** (sin ese verde, cuatro rojos seguidos también los explicaría una
  sonda rota).

---

## MIS PROPIOS ERRORES Y VENCIMIENTOS, DECLARADOS

1. **Mi primer instrumento de consulta imprimía valores sin nombre de columna**,
   y el orden que devuelve la API es alfabético por clave, no el del `SELECT`.
   **Leí mal un conteo de `profiles` y lo detecté al chocar con la consulta
   siguiente.** Curado el instrumento (imprime `clave=valor`) y re-corrida la
   medición antes de escribir nada. *El dato malo no llegó a ningún entregable
   porque el instrumento se arregló, no el dato.*
2. **Este parte se venció tres veces mientras se escribía.** `adopcion_documentos`
   pasó de 0 filas → 2 → 5, y la compuerta del acta pasó de mirar existencia a
   mirar vigencia, todo dentro de mi sesión. **Cada entregable lleva su hora.**
   *`L-166` cobrada en vivo: lo que medí a las 02:05 UTC era falso a las 02:45.*
3. **Un hueco que iba a reportar y A curó antes:** la compuerta del acta miraba
   `EXISTS(codigo, version)` sin `vigente` — lo medí, y cuando fui a escribirlo
   ya estaba curado por `20260907760000`. **Se registra igual**, porque el
   próximo que lea mi censo viejo tiene que saber por qué no está.

---

## LO QUE NO HICE, SIN MAQUILLAR

- **No corrí el traspaso.** Falta la firma, falta la cuenta de refugio (hoy
  **0** con `tipo_actor='refugio'`) y falta la pantalla (**cero consumidores**
  de las cuatro RPC del refugio en `apps/`).
- **No propuse ninguna cura.** Nombro puertas; el `D-485` tiene ficha y dueño
  y no la reabro yo.
- **No verifiqué nada en aparato.** Todo es objeto y camino real por RPC.

---

---

# S112 · SEGUNDA TANDA (2-sep) — E1 · E2 · E4 DEL LOOP DE ADOPCIÓN

> **Mandato nuevo:** `docs/loop/S112-LOOP-ADOPCION.md` §8-E. Worktree propio,
> sincronizado a `main f704daa2`. **CONTRA QUÉ:** base viva + ese `main`.
> **CUÁNDO:** 2-sep, 23:20 del 1 → 01:30 del 2.

## LO ENTREGADO

| # | qué | archivo | SHA |
|---|---|---|---|
| **E1** | auditoría de seguridad (§5), un requisito por fila, **pasada 1 de 2** + addendum | `docs/loop/buzon/S112-E-AUDITORIA-SEGURIDAD.md` | `a3e1f4a4` + `512194b6` |
| **E2** | performance con número y con plan (§6) | `docs/loop/buzon/S112-E-PERFORMANCE.md` | `512194b6` |
| **E4** | el escenario del founder, 20 pasos con su puerta | `docs/loop/buzon/S112-E-ESCENARIO-DEL-FOUNDER.md` | `ec2e313d` |
| **E3** | ⛔ **no corrió** — bloqueado por A9 (firma) y C8 | — | — |

## LOS SEIS ROJOS ABIERTOS, en orden de lo que bloquea

1. **`D-485`** — probado simulando el traspaso: la familia adoptante **no ve** a
   su mascota (0 filas, 11 de control) y **`user_id` queda en el refugio**.
   *La asimetría que lo cierra: el UPDATE de `mascotas` **sí** lee familia, el
   SELECT no.* Censo de las 81 tablas colgadas: **la cura son 4 helpers + 3
   policies**, no 81 puertas. **Bloquea el paso 15.**
2. **La policy `mascotas_select_vidriera_anon` no puede dar verdadero nunca** —
   gatea a `anon` preguntando por filas de tres tablas que `anon` no lee.
   **La vidriera no muestra NINGUNA foto sin sesión. Bloquea los pasos 8-9.**
3. **La rama refugio del arranque no existe** — `obtener_contexto_arranque` no
   menciona «refugio» en sus 4 757 caracteres. **Bloquea los pasos 1-7.**
4. **N4 incompleta** — `otorgar_rol_refugio` no escribe `tipo`.
5. **N1 a medias** — falta el techo de 3 solicitudes activas.
6. **El cron 48 nunca corrió** — 0 filas en `job_run_details`, aunque la función
   sí corre (cable probado con control negativo).
   ➕ 🟠 **`anon` lee `v_adoptables_publicos` directo** y saltea paginación, tope
   y whitelist (1 005 filas · 36 columnas · 1,06 MB · p95 651 ms). *Sin columna
   prohibida ⇒ no es fuga.*

## LO EJERCIDO POR CAMINO REAL, con su control al lado

- **7 DEFINERs rebotan a `anon`** con `42501` · control +: `cat_especies` devuelve 2 filas.
- **6 sondas de inmutabilidad** desde el asiento `authenticated` con JWT real ·
  control +: **27 filas y no 97**.
- **D-485** simulado en transacción · control +: **11 mascotas visibles**.
- **Cable del barrido** · control −: una función inexistente lanza.
- **Predicado de la policy de storage desde los dos asientos** · y el porqué
  impreso al lado (`mascotas=0 · adopcion_publicacion=0 · cat_estados_adopcion=0`).
- **Siembra de 1 005 y su borrado** · residuo **verificado en cero**.

## LA LECCIÓN DE LA TANDA — cobrada DOS VECES el mismo día, en direcciones opuestas

**Un predicado de RLS medido desde un asiento que RLS no alcanza no mide la
policy: mide otra pregunta parecida.** El asiento del superusuario me fabricó
**tres rojos falsos** (borrar el texto legal, editar una aceptación, borrarla) y
después **un verde falso** (el predicado de la policy de storage: `true` como
superusuario, y la policy está muerta). **Falla de más y de menos.**

**Correctivo, ejercido:** toda sonda de permisos lleva `SET LOCAL ROLE <el rol al
que gatea>` + `request.jwt.claims`, y **al lado se imprime qué ve ese rol de cada
tabla que el predicado menciona** — eso convirtió un `false` inexplicable en un
diagnóstico.

## EL APARATO — disponible, medido

`adb 1.0.41` · **`R5CY201ZDVL` (SM-S938B) conectado por USB** · `screencap`
devuelve PNG 1080×2340 · `input` responde · **ambas apps en 1.0.7** · el cliente
corre el update **`01a0600a-c96a-715d-9386-ecaf80853f24`**, `embedded=false`,
canal `preview`. ⇒ **E3 y E4 se pueden correr en el aparato con captura por paso.**

## LO QUE NO HICE, SIN MAQUILLAR

- **E3 no corrió**: no hay firma (cero tablas, cero funciones) ni formulario.
  *El guion completo con sus cuatro rojos ya está escrito en
  `S112-E-para-A-TRASPASO-ESTADO.md` §③ y §⑤ — **no se re-inventa**.*
- **20 refugios de prueba → usé los 15 que existen.** Fabricar 20 cuentas falsas
  ensucia una tabla que el founder ve y no cambia la cardinalidad del join.
  **Declarado en el documento, no escondido.**
- **El hilo paginado a 50** (§6): 0 mensajes. *Sembrar un hilo mediría mi siembra.*
- **El scroll a 60 fps**: es de C con el perfilador.
- **Los brazos «tercer usuario» y «otro refugio» de §5.3**: necesitan una
  solicitud viva. **No los repito de memoria porque D ya los probó.**

---

---

# CIERRE DE LA PISTA E · S112 (2-sep, 21:30)

## 🟢 E3 CERRADO — el primer traspaso real de la historia del producto

Sobre **Nube**, con tres sesiones reales, por camino real de punta a punta:
postular con formulario → hilo → aceptación → acta → dos firmas con código →
**traspaso**. `folio F-2026-000050` (adoptante) y `F-2026-000054` (refugio).

**El paso 15 de §0, verificado desde cada asiento:** la familia la ve
(`adoptada`, `user_id` propio) · **ve los 3 eventos, y uno es de las 13:26, de
antes de la entrega** ⇒ *el expediente que el refugio cargó viajó con ella* ·
procedencia el refugio · **dos hitos que son dos momentos distintos** (el rescate
y la adopción, medidos por su hora) · salió de la vidriera · el refugio dejó de
verla · **y un tercero tampoco la ve**.

## LO QUE ESTA PISTA ENCONTRÓ, y ninguno lo habría visto leyendo

| # | hallazgo | cómo apareció |
|---|---|---|
| 1 | **la policy de la vidriera no podía dar verdadero nunca** — gateaba a `anon` preguntando por tablas que `anon` no lee | midiendo el predicado **desde los dos asientos** |
| 2 | **`D-485`** — la familia adoptante no veía a su mascota y el refugio la seguía viendo | **simulando el traspaso** antes de que existiera |
| 3 | **el traspaso escribía `retirada`**, un estado que su propio CHECK prohíbe ⇒ **nunca pudo terminar** | el **control positivo** del rojo del acta |
| 4 | **el array de faltantes sin castear** ⇒ toda acta con un hueco reventaba con un error crudo | **ejerciendo** la primera acta real |
| 5 | **el `INSERT` del hito nombraba 5 columnas inexistentes** ⇒ la segunda firma no completaba | **firmando** de verdad |
| 6 | **el OTP aceptaba intentos ilimitados** — `UPDATE` y `RAISE` en la misma transacción | **seis intentos y mirar la fila** |
| 7 | **`adopcion-fotos` sin policy de `SELECT`** ⇒ `remove()` decía éxito y no borraba | **yendo a limpiar mi propio residuo** |
| 8 | **el refugio no podía subir fotos** · **el cron 48 nunca había corrido** · **N4 sin `tipo`** · **N1 sin techo** | censo y ejercicio |

**Los cinco del medio (3,4,5) tienen la misma forma, y es la lección de la
jornada:** *una rama que nunca se ejecutó no está probada por existir. Las tres
compilan, las tres están escritas, y las tres revientan la primera vez que
alguien las recorre — y ninguna estaba en un borde: las tres estaban en el camino
feliz.*

## 🔴 MIS PROPIOS FALSOS — SIETE en dos días, y ninguno llegó a un reporte

| # | qué medí mal | qué lo cazó |
|---|---|---|
| 1-3 | tres «agujeros» de inmutabilidad | el **asiento**: el CLI es superusuario y RLS no lo alcanza |
| 4 | un verde falso: la policy de storage «daba true» | el mismo asiento, **en la dirección contraria** |
| 5 | «el refugio no puede subir» | una **bandera del cliente** (`upsert`) cambiaba el camino |
| 6 | «§5.9 rebota `nombre_menor`» | rebotaba por **otra clave mía mal escrita** — *falso VERDE* |
| 7 | «la campana no lleva el código» | mi control positivo **daba 0 también** |

> **La forma que las cubre a las siete, y que A adoptó:**
> **El control positivo va PRIMERO, no al lado. Si el caso que DEBE pasar no
> pasa, ningún rojo de abajo significa nada — y se lee igual de convincente.**
>
> Con sus tres corolarios: *la sonda mide el camino que toma, no el que uno cree
> que toma* · *un regex laxo sobre un corpus ajeno encuentra siempre, y encuentra
> algo verdadero que no es lo que se preguntó* · *el control positivo no es
> cualquiera que esté cerca: es el que, si fallara, explicaría el resultado por
> otra razón.*

## LO QUE FRENÉ Y NO HICE

- **Publicar `acta_adopcion v2`** para el rojo de `acta_cambio_de_version`:
  publicar una versión de un documento legal deja rastro en una tabla inmutable y
  le cambia el texto a cualquiera que la abra en ese instante. **Lo cubre A.**
- **Escribir la clave del refugio en el repo.** *Una credencial viva en un
  documento versionado es el defecto que `D-712` ya cobró en esta casa.*
- **Sembrar fotos** para que la vidriera se vea llena: poner una imagen de stock
  como «ésta es Luna» es inventar evidencia.
- **Tocar la cuenta del founder** de forma persistente: el rojo de
  `pendiente_validacion` se produjo **dentro de una transacción abortada**, que
  hace *imposible* el riesgo en vez de prometer no incurrir en él.

## LO QUE QUEDA ABIERTO, con dueño

- **Que el correo LLEGUE.** Medido hasta donde se puede: las 6 intenciones
  salieron por `email`, con `proveedor_id`, sin gate que las cortara, y
  `correo_suprimido` en 0. **Que un proveedor acepte un mensaje y que una persona
  lo reciba son dos hechos** — el segundo lo verifica el founder abriendo su
  correo, en los pasos 7 y 14.
- **`acta_cambio_de_version`** — de A, con `ROLLBACK`.
- **Las fotos de la siembra** — del founder, paso 4.
- **La pantalla del formulario** — de C.

## ESTADO QUE ESTA PISTA DEJÓ EN LA BASE, declarado

**Nube está adoptada de verdad** y vive en la familia de `guillo381+8`, con su
acta firmada por las dos partes. **A repuso el quinto animal (Mica).** Cédula y
ciudad cargadas en `guillo381+8` y cédula en el titular del refugio — **por la
puerta, no por SQL**, que es lo que probó que §0 paso 14 no es un callejón.
**Luna quedó intacta**, que era el punto de haber usado Nube.

---

# 🔴 HANDOFF — SE ESCRIBE ANTES DE COMPACTAR, Y ES LA FUENTE

> **Lo que vive sólo en la conversación muere con ella.** Todo lo que la pista
> E sabe está acá o en los cinco entregables del buzón. **Nada de lo de abajo
> se re-deriva de memoria: cada número trae el comando que lo produjo.**

## ⓪ IDENTIDAD Y TERRITORIO

- **Pista E de S112** — medición y ejercicio. **No escribe `main`, no aplica
  migraciones, no toma números de deuda, no propone curas: nombra puertas.**
- **Worktree:** `/Users/guillo381gmail.com/proyectos/ePetPlace/e-petplace-s112-e`
  · **rama:** `pista/s112-e` · **base:** `main` = `978666bd`.
- **Cerrado y verificado POR SHA, jamás por código de salida.** ⚠️ **El tip NO
  se escribe acá: se mide** — `git rev-parse pista/s112-e` contra
  `git ls-remote origin pista/s112-e | cut -f1`, y los dos tienen que coincidir.
  *Escribir el sha en el parte lo deja viejo en el commit siguiente: pasó con el
  de las 22:20 antes de que este archivo terminara de escribirse.*
- **Territorio tocado:** `docs/loop/S112-E.md` y `docs/loop/buzon/S112-E-*.md`.
  **Cero líneas fuera de ahí.**

## ① EL ESTADO EN QUE DEJO EL MUNDO, CON SU HORA

Medido el **1-sep-2026 entre 21:05 y 22:10 -05**. ⚠️ **Tres de estos datos ya
cambiaron una vez dentro de mi propia sesión** (`L-166`).

| hecho | comando para re-medirlo |
|---|---|
| `adopcion_documentos`: 5 filas — `acta_adopcion v1` **vigente** (5 350 chars, sha `f788d883…`) · `terminos_refugio` v1 jubilada / v2 vigente · `condiciones_adopcion` v1 jubilada / v2 vigente | `select codigo, version, vigente, length(contenido), left(sha256,12) from adopcion_documentos order by 1,2` |
| **cero cuentas con rol `refugio`** (14 filas: 9 `prestador_servicios`, 5 `seller_productos`) | `select tipo_actor, estado, count(*) from cuenta_roles group by 1,2` |
| **0 publicaciones, 0 solicitudes, 0 mensajes** de adopción | `select count(*) from adopcion_publicacion` (ídem `_solicitud`, `_mensaje`) |
| **83 mascotas** · 32 con `user_id` · 2 con codueño vigente · **51 sin ninguna de las dos** | ver el `select` de conteo en `S112-E-para-A-TRASPASO-ESTADO.md` §④ |
| **0/172 profiles con `identificacion_fiscal`**; 172 con `tipo_identificacion='cedula'` | `select count(*), count(identificacion_fiscal), count(tipo_identificacion) from profiles` |
| **0/83 mascotas con microchip**; 55 con fecha de nacimiento; 28 con sexo | `select count(*), count(microchip), count(fecha_nacimiento), count(sexo) from mascotas` |
| **0 eventos de `esterilizacion`**; 32 de `vacuna_aplicada`; 2 de `bitacora_familia` | `select tipo, count(*) from eventos_mascota where tipo in (...) group by 1` |

## ② LOS INSTRUMENTOS, VERBATIM — para no volver a escribirlos

**El de consulta compacta.** ⚠️ **Su primera versión imprimía valores sin
nombre de columna y la API los devuelve en orden ALFABÉTICO por clave, no en el
del `SELECT`: leí mal un conteo.** Ésta imprime `clave=valor` y por eso es la
que vale:

```zsh
#!/bin/zsh
cd /Users/guillo381gmail.com/proyectos/ePetPlace/e-petplace
npx supabase --experimental db query --linked "$1" 2>/dev/null | node -e '
let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{
 try{const j=JSON.parse(s);for(const r of j.rows){console.log(Object.entries(r).map(([k,v])=>k+"="+(v===null?"~":String(v))).join(" | "))}
 console.log("("+j.rows.length+" filas)")}catch(e){console.log(s.slice(0,4000))}})'
```

**La sonda de sesión real** (RPC por camino real, con sus controles). Va **en
`scripts/` y se borra al terminar** — desde el scratchpad no resuelve
`@supabase/supabase-js`:

```js
import { execFileSync } from 'node:child_process';
import { readFileSync } from 'node:fs';
import { createClient } from '@supabase/supabase-js';
const R='/Users/guillo381gmail.com/proyectos/ePetPlace/e-petplace';
const CLAVE=execFileSync('security',['find-generic-password','-a','siembra','-s','epetplace-siembra-s97','-w'],{encoding:'utf8'}).trim();
const env=Object.fromEntries(readFileSync(`${R}/apps/cliente/.env.local`,'utf8').split('\n').filter(l=>l.includes('=')).map(l=>[l.slice(0,l.indexOf('=')).trim(),l.slice(l.indexOf('=')+1).trim()]));
const cli=createClient(env.EXPO_PUBLIC_SUPABASE_URL, env.EXPO_PUBLIC_SUPABASE_ANON_KEY,{auth:{persistSession:false}});
const { data: ses }=await cli.auth.signInWithPassword({email:'guillo381+8@gmail.com',password:CLAVE});
// … rpc(...) con control positivo Y negativo en la misma corrida
```

🔑 **La clave se lee del keychain AL MOMENTO** (`siembra` /
`epetplace-siembra-s97`) — **jamás se escribe en un reporte, en el repo ni en
el chat.** La anon key sale de `apps/cliente/.env.local`, es pública y viaja en
el bundle; aun así **no se pega en ningún documento**.

## ③ LO EJERCIDO, PARA NO REPETIRLO

| sonda | resultado |
|---|---|
| `obtener_adoptables` con **anon key** (HTTP directo) | **200 `[]`** · control negativo `obtener_mis_solicitudes_adopcion` → **401** |
| `traspasar_mascota_a_familia` con basura / con familia inexistente / con familia = origen | **`mascota_no_existe`** · **`familia_destino_no_existe`** · **`familia_destino_igual_al_origen`** (tres guards distintos ⇒ la sonda discrimina) |
| `publicar_adoptable` con cuenta sin rol | **`no_sos_cuenta_de_refugio`** · **control positivo**: `obtener_mis_solicitudes_adopcion` → **VERDE, 0 filas** |

## ④ COLA VIVA — el próximo paso, si esta pista se retoma

1. **El ejercicio del traspaso, cuando existan sus dos precondiciones.** Hoy
   **la firma no existe** y **hay 0 cuentas de refugio**. El guion completo,
   con los cuatro rojos y su fixture mínima, está en
   `S112-E-para-A-TRASPASO-ESTADO.md` §③ y §⑤. **No lo re-inventes: está escrito.**
2. **Falsear o confirmar la predicción de `D-485`** — el discriminador es una
   sola consulta: el `select` del adoptante sobre `mascotas` **inmediatamente
   después** del traspaso. Si devuelve la fila, mi predicción era falsa y hay
   que decirlo.
3. **El rojo del acta ya no se puede producir sin una escritura** (jubilar
   `acta_adopcion`), y **esa escritura es de A**.

## ⑤ COMPROMISOS

- **Qué debo:** *nada.* Los cinco entregables están en el buzón, commiteados y
  pusheados.
- **Qué me deben, y sin esto la pista E no avanza:** **A** — la firma
  construida, la cuenta de refugio dada de alta a mano, y la decisión sobre
  jubilar el acta para producir su rojo.
- **Mensajes enviados** (por canal entre sesiones, no por repo): a **C**
  (`e-petplace-83`) el aviso de que su censo del formulario está en el buzón y
  que `ADOPCION_ALCANZABLE` **no está en `main` `978666bd`**; a **B**
  (`e-petplace-ab`) que **no soy A** y que en adopción el botón que frena sin
  decir por qué **no está en sus seis**: ahí no falta que la pieza dibuje la
  razón, **falta la razón**.

## ⑥ FRENOS Y SUPUESTOS VIVOS

- **Nada de esto se verificó en aparato.** Todo es objeto y camino real por
  RPC. *Que el motor conteste no prueba que una pantalla lo llame bien.*
- **`D-485` está NOMBRADA, no curada, y no es mía.** Tiene ficha desde S73.
- **Las ramas de B, C y D no estaban en `main`** cuando medí: lo que digo del
  canon no describe sus árboles.
- **Todo número de este parte se vence.** Si al releerlo el `sha256` del acta
  ya no es `f788d883…`, el censo de sus variables describe otro documento.

---

## ADDENDUM · LA LECCIÓN QUE SALIÓ DE CRUZAR MIS FALSOS CON LOS DE C

*Depositada acá sin número: **los números los asigna A** con `pnpm proximo:ficha`,
y dos pistas tomando el mismo es exactamente lo que esa regla evita. C se la pidió
con este texto y con las dos autorías.*

> **Un censo estático entrega candidatos, no veredictos.** Ninguna medición sobre
> el árbol distingue una ausencia que es **defecto** de una que es **criterio
> firmado** — y no porque la firma esté fuera del alcance: puede estar **tres
> líneas arriba de la puerta, en el mismo archivo**, y el instrumento la lee como
> prosa. *Ningún grep puede leer una intención.* La diferencia no se cura con un
> instrumento mejor. **Su consecuencia operativa: el censo NOMBRA los casos y los
> CLASIFICA quien conoce la letra**; un censo que entrega un número como veredicto
> está afirmando algo que no midió.
>
> **Corolario, que es lo que la separa de su vecina:** un instrumento que mide
> desde el asiento equivocado se cura **cambiando de asiento**; éste no se cura
> **midiendo mejor**.

**La corrección es de C y es la que le da el filo.** Yo había escrito *«ninguna
medición sobre el árbol distingue…»*, que suena igual y **es más indulgente con el
instrumento de lo que su caso permite**: en su censo la firma **sí estaba en el
árbol**, tres líneas arriba de la puerta, en el mismo archivo. *Su instrumento
abrió ese archivo, leyó esa puerta y pasó por encima del comentario que la
autorizaba.* No le faltó alcance: le faltó poder leer intención — **y eso es peor,
porque quien audite el censo va a encontrar la evidencia justo al lado del rojo y
va a creer que el instrumento fue descuidado.**

**Por qué la separo de mis siete falsos:** los míos fueron todos *el asiento
equivocado* —el CLI que RLS no alcanza, el predicado evaluado bajo la RLS ajena,
una bandera del cliente, un payload inválido, un regex laxo— y **los siete se
curan cambiando desde dónde se mide**. El de C no: **cinco de sus ocho rojos eran
una decisión firmada**, y ningún instrumento mejor los habría distinguido de un
olvido.

*Son dos familias, y conviene que tengan nombres distintos: si se juntan, la
segunda se lee como «el instrumento estaba mal hecho» y se busca la cura donde no
está.*
