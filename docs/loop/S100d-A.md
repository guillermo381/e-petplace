# S100d-A — LA DIRECCIÓN, LA FICHA DE ENTREGA, Y UN CENSO QUE SE ATRAVESÓ

---

# 🔴 HANDOFF DE RELEVO — leé esto antes que nada

> 🔴 **NINGÚN SHA SE ESCRIBE ACÁ COMO VERDAD PERMANENTE: SE MIDE.**
> `git ls-remote origin pista-a` · `git ls-remote origin main`.
> **Todo número de este documento se midió el 18-ago-2026.** *Lo derivado decae
> mientras el objeto no.*
>
> **Escrito ANTES de que el trabajo se lleve el turno**, por la razón que L-217
> tiene medida: *la última acción de una pista es escribir su cierre, y por eso
> es justo la que se queda afuera.*

## ⓪ EL OBJETO DEL GATE — publicado por esta pista

| | |
|---|---|
| **ancla, leída DEL OBJETO** | **`cce110d37055dd1dae52fd4f6aa8e27c3ecd6022`** |
| OTA cliente | group **`c8d9e549-0eb0-4ea9-8855-f3f915455910`** · runtime **1.0.3** |
| OTA prestador | group **`25cbf163-3a52-411b-b7be-e5f0357d079b`** · runtime **1.0.5** |
| árbol al bundlear | **0 ⇒ SIN ASTERISCO** |
| cómo se relee | `cd apps/<app> && npx eas-cli update:view <updateId> --json` → `gitCommitHash`. ⚠️ **NO acepta `--non-interactive`**, devuelve una **LISTA**, y `update:list` **no expone el hash** |

**Las dos contra el MISMO commit, y las dos anclas coinciden con `origin/main`.**
El resumen de publicación —los 22 puntos con su señal de verificación **en
número**— vive en `docs/loop/S100d-RESUMEN-DE-PUBLICACION.md`.

---

## ① 🔴 LAS DOS PREMISAS DE MI ARRANQUE QUE LA MEDICIÓN DIO VUELTA

*Va primero a propósito: dos de mis cuatro encargos traían una premisa falsa, y
las dos corrigen hacia **distinto trabajo**, no hacia menos.*

### EL ALIAS (28) NO FALTABA: ERA INALCANZABLE

Mi arranque decía *«la tabla ya lo soporta, el RPC no lo toma. Ensanchá el RPC y
la superficie»*. **Medido: el alias estaba ENTERO** —motor, wrapper, libreta y
formulario con `conAlias`— **y viajaba en la OTA que el founder miró**:

    git merge-base --is-ancestor e9390704 e20113b6   →  SÍ

**El candado era de arranque y se cerraba sobre sí mismo:** la fila de dirección
abría la libreta solo con `length > 1` · con UNA dirección abría el formulario de
edición, que no tiene alias ni camino a agregar · y **el único botón «agregar
otra» vivía DENTRO de la libreta**. ⇒ para llegar a la libreta hacían falta dos
direcciones, y para tener dos había que pasar por la libreta. **El founder tiene
una.**

⚠️ **La regla del `length > 1` NO era el error y no la toqué.** Su razón sigue en
pie —*«un selector de un elemento es un paso que no decide nada»*— y es cierta
**para elegir**. Lo que no vio es que la libreta hacía **dos** trabajos: elegir y
agregar. *No lo puso una regla equivocada: lo puso una regla correcta que era la
única puerta de otra cosa.*

### EL ZOOM (27): LA LIBRERÍA CONTRADICE SU PROPIA DOCUMENTACIÓN

    node_modules/react-native-maps/android/.../MapManager.java:266
      @ReactProp(name = "zoomEnabled", defaultBoolean = false)
    dist/src/MapView.d.ts:592-601
      «TODO: Why is the Android reactprop defaultvalue set to false?»  @default true

⇒ **en Android, no pasar `zoomEnabled` deja el pinch APAGADO.** `MapaZona` y
`MapaRecorrido` lo declaran explícito; **el único mapa de la casa donde hacer
zoom ES el trabajo era el único que no lo declaraba.** Curado por B en la pieza.

**Y el segundo defecto del mismo punto:** `initialRegion` es **no controlado** —
se lee solo al montar ⇒ elegir una predicción de Places **no movía el mapa**. Con
un borde que lo hace intermitente: la pieza solo se monta con punto, así que
**creando** parece que funciona y **editando** no. *El defecto se ve solo
editando.*

🔴 **LA PREDICCIÓN FALSABLE SIGUE SIN CORRERSE** (ahora hay aparato): con una
dirección que YA tiene punto, elegir una predicción lejana **no debe** mover el
mapa; sin punto, **sí**. **Si las dos se comportan igual, el mecanismo está mal y
se vuelve a empezar en vez de creerle a la cura.**

---

## ② LO CONSTRUIDO, CON SU COMMIT

| # | qué | verificación |
|---|---|---|
| **17** | **la ficha de entrega**: los cuatro grupos rotulados pasan a UNA carta (N21) | `tsc` · **no visto en render** |
| **20** | `lib/despensa/tope-de-compra.ts` + mi puerta (carrito) | instrumento **10/10**, **rojo producido 8/10** |
| **28** | el deadlock roto donde el que tiene UNA dirección realmente está | `tsc` |
| — | **la fuga entre personas**: `.eq('user_id', uid)` en `listarMisPedidos` | 13 → 12, ajenas 1 → 0 |
| — | **3 migraciones de seguridad** | camino real antes/después con discriminador |

### ⚠️ POR QUÉ **UNA** CARTA Y NO CUATRO (punto 17)

**Los puntos 18 y 19 están ✅ APROBADOS por el founder.** Cuatro cartas los
reestructura; una los deja intactos y solo pone el piso que faltaba.
`relleno="ninguno"` **no es decoración**: es lo que conserva la anatomía de
adentro —con relleno normal, `CeldaNavegacion` dejaría de salir a sangre, que es
el patrón que E14 fijó—. Con `ninguno`, «a sangre» pasa a significar «al borde de
la carta».

**Aritmética declarada porque NO se vio en aparato:** los cinco
`paddingHorizontal` de adentro bajan de 20 a 12 ⇒ el rótulo queda a **32** del
borde de pantalla contra 20, y la celda a **20** contra 0. *Introducir una carta
MUEVE cosas: no hay forma de que no lo haga.*

---

## ③ 🔴 CURAS DE DOS MITADES — quitar una reintroduce el defecto SIN VERLO

**① `decidirTope` tiene `sin_medir` como CLASE PROPIA, no como alias de
`agotado`.** ⚠️ El día que alguien las junte «porque las dos no dejan comprar»,
**una caída de red le va a decir a la familia que el producto se acabó** (Ley 13).
El rojo producido del instrumento es exactamente ese: colapsarlas da **8/10**.

**② El tope vive en `lib/`, y no puede subir a `packages/ui`.** El dato es una
**consulta asíncrona al motor**; una pieza presentacional que consulta la base
deja de serlo. ⚠️ Y **no puede bajar a una pantalla**: curarlo en una lo esconde
ahí y lo deja vivo en las otras dos — que es el estado que encontré.

**③ Bajar la cantidad NO consulta.** ⚠️ Si alguien «unifica» y consulta siempre,
paga un viaje por cada toque del `−` y no decide nada.

**④ El filtro de `listarMisPedidos` es un TAPÓN, no la defensa.** La defensa es
el `security_invoker` de la vista, que ya entró. ⚠️ **Si alguien quita el filtro
«porque la RLS ya cubre», tiene razón hoy y queda sin red el día que la vista se
revierta.** Se quedan los dos: cinturón y tirantes.

**⑤ Heredadas y vivas:** `fichaSinStock` **se mudó** al pie y reponerla en ⑥ la
duplica a 400 dp · `maximo_comprable_de_ofertas` devuelve **`LEAST(pedido,
disponible)`, JAMÁS el stock** —la propiedad está en el `LEAST`, no en el
nombre— · `guardar_direccion_con_alias` escribe **`es_principal = false` SIEMPRE**
(parametrizarlo deja que una oficina desplace al hogar).

---

## ④ SEGURIDAD — el censo que se atravesó, y lo que quedó

**Censo completo: `docs/relevamientos/2026-08-18-s100d-CENSO-RLS.md`.**

**El titular: la alerta apuntaba a un lado y el agujero estaba en otro.** Las
tablas sin RLS son **tres** y son catálogos inofensivos con la escritura cerrada
(decisión S92 ratificada — **la nota de falso positivo está depositada para que
la próxima alerta no dispare la investigación de cero**). **El agujero eran ONCE
VISTAS sin `security_invoker` legibles por `anon`.**

| firma | estado |
|---|---|
| `v_dashboard_logistico` (única con PII) | ✅ **cerrada** · 200 con `cliente_email` → **401** |
| lote de 4 sin consumidores | ✅ **cerrado** · con su **límite declarado** (abajo) |
| `v_pedidos_narrativa` | ✅ **cerrada** · 13 → 12, ajenas 1 → 0 |
| las **6 del admin** | 🔴 **ABIERTAS a propósito** — cerrarlas apaga el tablero del founder **con certeza** |
| `consentimientos` | 🔴 **NO tocada — la firma era condicional y su condición FALLÓ** |

### 🔴 EL LÍMITE DEL LOTE DE 4, y es mi propia advertencia mordiéndome

Solo `v_resenas_todas` está probada cerrada **por su salida**. Las otras tres dan
`200 · 0 filas`, y **medido con privilegio las cuatro vistas están VACÍAS** ⇒ ese
cero no distingue «la RLS bloquea» de «no hay nada». *Es literalmente lo que el
censo escribió antes de que existiera la cura.* **Lo probado es el MECANISMO** (el
cinturón + el 401 de reseñas) ⇒ **se re-mide con la primera venta y la primera
reseña reales.**

### 🔴 `consentimientos`: por qué la cura de una línea ROMPE el registro

`e-petplace-v2/src/pages/Login.tsx:328` inserta **con `user_id`** justo después
de `signUp`, y **cuando el alta exige confirmar el correo `data.session` es null
⇒ el cliente sigue siendo `anon` ⇒ `auth.uid()` es NULL** ⇒ el `WITH CHECK`
firmado rechaza y **el alta falla**. **El flujo legítimo y la forja son
indistinguibles en la base**: los dos son «anon escribiendo un `user_id` ajeno».
**La cura que cierra es estructural** —el consentimiento deja de ser *un dato que
el cliente afirma* y pasa a ser *una consecuencia del registro*— y toca una web
viva fuera de este monorepo.

---

## ⑤ LO QUE DECIDÍ **NO** HACER, CON SU RAZÓN

*Un camino descartado sin su razón se vuelve a intentar.*

- **NO apliqué la cura firmada de `consentimientos`** (④). *Una firma condicional
  cuya condición falla es una firma que dice que NO.*
- **NO toqué los tres catálogos sin RLS.** Activarles RLS sin policy **deja a la
  gente sin poder crear cuenta** — el registro los lee sin sesión.
- **NO cerré las 6 vistas del admin.** No es riesgo: es **certeza** de apagarle el
  tablero al founder. Eso se decide con fecha.
- **NO curé `PinMovible` ni el glifo**: son de B. Le pasé la causa raíz con el
  literal del fuente nativo y **una predicción falsable**, que es lo que le
  permite descubrir que estoy equivocada.
- **NO reestructuré «Quién recibe» ni «Cuándo»** al hacer la ficha de entrega:
  están **aprobados**, y tocarlos por prolijidad es gastar crédito ajeno.
- **NO monté el tope en la vitrina ni en la ficha**: son de C. Le mandé el caso
  con su número y las tres condiciones que no se pueden perder.

---

## ⑥ 🔴 MIS ERRORES, DECLARADOS — los dos son de la misma familia

**① EL PEOR: convertí un negativo medido una vez en una recomendación a la mesa.**
`adb devices` me dio lista vacía, **no volví a mirar**, y le reporté al founder
que *«ninguna pista puede cumplir la regla de cierre»* — casi le hago correr el
gate por un teléfono que estaba enchufado (era `unauthorized`; lo destraba
`adb kill-server`). **Las tres pistas medimos igual de mal; la que lo elevó a
decisión fui yo.** *No falló la medición: falló no re-medirla antes de que
cambiara lo que otro iba a hacer.*

**② Dejé una pieza donde otra pista no podía verla.** Mergeé `pista-a` a `main`
en **local** y no pusheé ⇒ C midió `origin/main`, no encontró el helper del tope,
y declaró el punto 20 como no montable. **Su medición era correcta y su
conclusión también: el hueco era mío.** *Un objeto que existe pero no donde el
otro puede verlo se comporta exactamente como si no existiera.*

**③ Y uno chico de la misma forma:** busqué `zoomEnabled=` con `=` y di por no
curado lo que B había curado como `zoomEnabled` a secas. **Un falso negativo de
patrón se lee igual que la ausencia que uno vino a medir.**

---

## ⑦ LO QUE UN INSTRUMENTO DE ESTA PISTA **NO** PUEDE DECIR

- **No vi un solo píxel.** Fuente, tipos, consultas a la base y camino real por
  HTTP. **`verify:diseno` mide FORMA, el typecheck mide TIPOS: ninguno mide
  jerarquía visual ni si algo se entiende.**
- **Mi instrumento mide una función pura.** Dice que la regla decide bien; **no
  dice que las tres puertas la llamen** (eso lo dice leer el montaje) ni que el
  aviso se lea en un teléfono.
- **Oscuro y memorial: no medidos** por ninguna pista, en toda la vuelta.
- **No sé si la ficha de entrega se lee mejor.** Es composición, y eso lo dice el
  ojo.

---

## ⑧ DEUDAS CON DUEÑO Y DISPARO

| deuda | dueño | disparo |
|---|---|---|
| **Las 6 vistas del admin** siguen legibles por `anon` (GMV, MRR, pitch, ranking) | **founder** | cuando el admin migre a un camino autenticado, y **ANTES de que `/inversores` se comparta con nadie externo** |
| **`consentimientos`: la cura estructural** | **mesa** | turno propio; toca `e-petplace-v2` |
| **El límite del lote de 4** — su salida no está probada | **A / motor** | la primera venta y la primera reseña reales |
| **La predicción falsable de `PinMovible`** sin correr | **B** | hay aparato: corre ya |
| **`zoomControlEnabled`** — si los botones +/− tampoco aparecen, **hay un SEGUNDO defecto y no es el `zoomEnabled`** | **B** | en el recorrido |
| **`obtenerDetallePedido` rebota con voz de red** ante un permiso denegado | **D** | con la vista cerrada, **cualquier link viejo a un pedido ajeno cae ahí** |
| **`uq_direccion_principal` y `uq_direcciones_principal_por_user` son índices DUPLICADOS** | **A / motor** | la próxima migración que toque `direcciones_guardadas` |
| **El techo de 50 ítems** de `maximo_comprable_de_ofertas` | **A** | si un carrito real llega a 50 líneas |

---

## ⑨ LA LECCIÓN DE LA VUELTA, QUE NO ES MÍA SOLA

**El mismo día, cuatro veces, cuatro sujetos distintos:** `adb devices` vacío ·
«no hay rasterizador de SVG» (lo había: `qlmanage -t`) · mi grep con `=` · y el
instrumento de C que buscaba **su paráfrasis** de una voz en vez del literal, y
por eso reportaba *«la app no le dijo nada a la familia»* — **fabricando el
defecto que venía a descartar**.

> **Un negativo medido una vez y citado toda la mañana no es una medición: es una
> cita.** Y su gemela, que apareció el mismo día: **una presencia PREDICHA
> tampoco es una medición** (el toast del `<button>` anidado, que B midió en el
> teléfono y no existe en preview).

**Lo que las cuatro tienen en común no es descuido: es que ninguna se re-midió
antes de convertirse en una decisión.**


---

# 🔴 CIERRE DE S100d·bis — HANDOFF

> **Todo número de acá se midió el 19-ago-2026.** *Lo derivado decae mientras el
> objeto no.* Los SHA se re-miden: `git ls-remote origin <rama>`.

## ⓪ EL OBJETO DEL GATE

    CLIENTE     group a69e45f3-4276-4aeb-aa27-fa59f8881569   pie: 01a0189f   rt 1.0.3
    PRESTADOR   group 78ef6a65-29da-4c14-91d9-0fd9cd6d6d65   pie: 01a018a0   rt 1.0.5
    ANCLA (las dos)  1da94f3d  ·  árbol 0, SIN asterisco

⚠️ **El pie muestra el `updateId`, NO el group.** Y **el primer lanzamiento tras
un publish sirve el bundle VIEJO** — con roaming hacen falta **dos aperturas**, y
el pie se lee **con la mano antes de medir nada**. *Si se mide en el primero, todo
lo nuevo sale «no está» y se reportan rojos falsos sobre trabajo que sí está.*

## ① 🔴 LA TABLA DE LO VIVO — con dueño y disparo

| qué | dueño | disparo |
|---|---|---|
| **los contrastes contra el fondo nuevo** — chip magenta, ocre, disco del carrito | C / B | se midieron contra el fondo viejo. Con el ojo sobre `#F6F6F6` los tres **se leen**, **pero «se lee» no es un número de contraste** |
| **el tono `neutro` del chevron: 2,40 / 2,22** | **mesa** | su palette lo llama «decorativo» y **un chevron que es la ÚNICA señal de que algo se despliega no es decorativo**. **Mueve las DOS apps** |
| **`guardarComoOtra`** del modal de dirección | **A** | **construido y NO ejercido.** El fallo fue de mi instrumento: el alias no entró al campo y el botón estaba **correctamente** deshabilitado |
| **las 6 vistas del admin legibles por `anon`** (GMV, MRR, pitch, ranking, funnel, crecimiento) | **founder** | **antes de compartir `/inversores` con alguien externo.** Cerrarlas apaga el tablero **con certeza**, no como riesgo |
| **`consentimientos`: la cura estructural** | **mesa** | su firma era condicional y **la condición falló** — el único escritor inserta con `user_id` **sin sesión** ⇒ el `WITH CHECK` firmado rompía el alta |
| **25 variantes con más de una oferta publicada** | **mesa** | **0 del mismo vendedor · 25 de vendedores DISTINTOS · hasta 48 % de brecha · ningún UNIQUE.** Es el caso **multi-vendedor** contra la firma «una oferta por producto», y **la vitrina curada se apoya en que haya UNA** |
| **el detalle del pedido: ~100 dp de crecimiento** | producto | **+64,4 medido + 36 derivado.** NO es defecto: **es el freno para la tercera cosa que quiera entrar a esa pantalla** |
| **`confirmado` como hito ACTUAL: nunca se vio** | founder | cero pedidos en ese estado (**D-764**, sin pasarela). *No se midió otro estado para reportarlo como éste* |
| **el límite del lote de 4 vistas cerradas** | A / motor | están **vacías** ⇒ su `200 · 0 filas` no distingue «cerrada» de «vacía». **Se re-mide con la primera venta y la primera reseña** |
| **`uq_direccion_principal` y `uq_direcciones_principal_por_user` son índices DUPLICADOS** | A / motor | la próxima migración que toque `direcciones_guardadas` |

## ② LO QUE ESTA PISTA DEJA CONSTRUIDO

**Seguridad:** el censo RLS (`docs/relevamientos/2026-08-18-s100d-CENSO-RLS.md`) ·
**tres migraciones** con reversa escrita antes y camino real verificado —
`v_dashboard_logistico` (200 con `cliente_email` → **401**), el lote de cuatro, y
**`v_pedidos_narrativa`** (la fuga viva entre personas: **13 → 12, ajenas 1 → 0**)
· **`places_id` con su auditoría** (`lat_places`, `lon_places`,
`punto_movido_a_mano`).

**Gate:** el modal de dirección con su flujo completo **verificado en aparato en
sus cuatro pasos y su discriminador** · la ficha de entrega (N21) con la gota ·
el alias alcanzable · el tope de compra en las tres puertas
(`lib/despensa/tope-de-compra.ts`) · el campo tipeable del carrito · la voz del
pedido ausente sin Reintentar · `producto_id` para «Pedir de nuevo».

**Instrumentos:** `verify-s100d-tope-de-compra.ts` (10/10, rojo producido 8/10) ·
**`verify-shell-vs-pantalla.mjs`** — *nació rojo sobre un defecto real y murió
verde cuando se curó*.

## ③ 🔴 EL PASO DE ENSAMBLE QUE AHORA ES OBLIGATORIO

**Cuando dos pistas tocan la misma pieza en la misma vuelta, censar si el montaje
de una vuelve innecesario el de la otra.** *Dos entregas correctas por separado
producen un defecto que no está en ninguna de las dos, y un typecheck no lo ve:
los dos montajes compilan.* **El ensamble es el único lugar donde alguien puede
preguntar por la combinación.**

Mecanizado en `verify-shell-vs-pantalla.mjs`. ⚠️ **La regla cruda sobre-dispara
22 a 2** (`Icono` está en el shell y en 22 pantallas, **y es correcto**); el
discriminador que la vuelve exacta **sin convención nueva** es
**`position:'absolute'` en la fuente de la pieza.**

## ④ LA FORMA QUE LA JORNADA COBRÓ SIETE VECES

> **Un negativo medido una vez y citado después no es una medición: es una cita.**
> Y su hermana, más difícil porque el número **es correcto**: **contesta otra
> pregunta.**

`adb devices` vacío (tres pistas) · «no hay rasterizador de SVG» (lo había) · un
`grep` con un `=` de más · el «no montado» vencido · el «montado» que miraba otro
camino · una **presencia predicha** que no existía · un PNG mal decodificado que
dio negro perfecto en tres botones · la corrida de píxeles que medía el hueco
entre letras · y un `diff` entre **ramas** cuando la pregunta era **contra el
padre**.

**Ninguna la encontró leer con más cuidado. Todas las encontró volver a medir el
objeto.** ⇒ **L-313, L-314 y L-315 en `DEUDAS_CANONICAS.md`.**
