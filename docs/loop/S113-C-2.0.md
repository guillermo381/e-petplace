# S113-C · 2.0 · C1 — Nexo habla

**Rama `pista/s113-c-2.0`** desde `main @ 54e00439`, con **B @ `4dccae6d`**.
Cuenta: `guillo381+8@gmail.com`, en web. **Cero aparato. Ningún OTA.**

---

## ① LO QUE CORRE, MEDIDO

| | resultado |
|---|---|
| el dedo «Pregúntale a Nexo» | abre `/nexo?mascotaId=…&nombre=Thor` ✓ |
| caja, hilo y panel «Lo que sé de Thor» | ✓ |
| los tres chips | salen **del contexto**, no de una lista fija ✓ |
| «¿cuándo le toca la vacuna?» | **«A Thor le falta: múltiple (6 de julio de 2025), antir…»** ✓ |
| Sombra (memorial) | ninguna puerta a Nexo en su ficha ✓ |

**La edge ya estaba desplegada cuando fui a medir** — lo que iba a quedar
«cableado y declarado» quedó **ejercido**.

## ② 🔴 EL ROJO DEL MANDATO, AFINADO POR LA MEDICIÓN

El mandato pedía *«plantilla con la fecha, sin modelo (`ia_uso` no crece)»*.

```
ia_uso  ANTES 805  ·  DESPUÉS 806   (+1 por pregunta)
las dos últimas filas: pieza=coach_router · modelo=claude-haiku-4-5
filas de pieza=coach con Sonnet:  CERO
```

⇒ **La respuesta salió de plantilla, sin redacción.** Lo que crece es **el
router**, que clasifica la intención. Y esa separación **la hizo D a propósito**,
justo para poder verlo: *«leer en `ia_uso` cuánto cuesta pensar y cuánto cuesta
escribir, en vez de un promedio que no dice nada»*.

**La expectativa hay que escribirla así**: «cero llamadas al modelo que redacta»,
no «`ia_uso` no crece». *Un umbral escrito sobre la tabla equivocada da rojo
sobre un sistema que funciona bien.*

## ③ ☠️ `CoachHoja` SE RETIRA EN EL MISMO ACTO (L-395)

336 líneas, tres preguntas con plantillas, cero generación. Lo suyo **lo cubre la
rama `dato` de la edge** —plantilla, cero modelo—: la misma promesa con una caja
de texto delante. *No se deja una puerta vieja al lado de la nueva.*

**Y es pantalla, no Hoja**, por medición vieja de la casa: un hilo con teclado y
scroll dentro de una Hoja **pelea con su gesto de cierre** (`HojaScroll`, S45).
*Una conversación necesita el alto entero.*

## ④ LA PUERTA A LA EDGE LA ABRÍ YO — aditiva, 76(d) declarada

A hizo el motor y sus wrappers, D hizo la edge, y **nadie escribió la puerta
entre las dos**. El contrato salió de **la fuente de D**, no de su parte.

🔴 **`intencion: 'busqueda'` vuelve con `respuesta: null` a propósito**, y la
pantalla tiene que saberlo: la edge **no busca**, porque buscar es leer datos de
la familia y eso pasa por la puerta única con la sesión de quien pregunta.
*Hacerlo en la edge con `service_role` sería reimplementar la RLS adentro de una
edge de IA.*

## ⑤ DOS DEFECTOS MÍOS, LOS DOS CAZADOS ANTES DE ENTREGAR

**El riel tipado me salvó de uno que rompía en silencio:** agregué un namespace
`nexo` **cuando ya existía**, y el objeto nuevo pisaba el viejo entero. Sin keys
tipadas eso se descubre en pantalla, con las voces de la pata en blanco.

**Y mi arnés tocaba la tarjeta de atrás:** el Hogar sigue montado bajo la hoja
del selector, así que buscar «Thor» por texto tocaba **la de atrás** y la URL no
cambiaba. Se distingue **por su caja** (~84 px) — *como ya lo había medido en el
1.1.1 y se me había olvidado*.

## ⑥ GATES

`nexo` ✓ · `pide-en-memorial` ✓ · `hooks` ✓ · `hooks-bajo-return` ✓ ·
`ref-antes-de-uso` ✓ · `puerta-unica` ✓ · `coach` ✓ · `glifos` ✓ ·
`confirmable` ✓ · `diseno` **VERDE, 61 reglas** ✓ · `tsc` los cuatro ✓.

🔴 **`gates-existen` rojo HEREDADO del merge de B**: nombra `verify:pasaporte` y
`verify:todo`, los dos **de la nota de B** en el archivo compartido de pendientes
(líneas 78 y 91), no de mi código. Curarlo sería editar notas ajenas. Declarado
en el commit.

## ⑦ LO QUE NO ENTRA TODAVÍA

- **C2 · la búsqueda desde el Hogar** — la caja en el Hogar. Siguiente.
- **El semáforo sanitario** y la **propuesta de memoria** (`propuesta` de
  `RespuestaNexo`): la edge no las manda todavía en el cuerpo que leí. *Cablearlas
  contra un campo que no llega sería inventar el contrato.*
