# S107 · PISTA D — el módulo de media y el transporte

> **Partes 1 a 5 · el censo, la cola, el motor, la captura, el punto vivo, las actas y la voz.** 28-ago-2026.
> Rama `pista/s107-d`, worktree propio. El plan §2 manda censar antes de montar
> («D corre en paralelo desde su censo; su montaje llega cuando A y B
> publican»): §① y §② son ese censo; §⑤bis y §⑤ter, lo construido de lo que
> **no** dependía de nadie.
> **Nada de esto es letra ni texto de pantalla** (§0 del perímetro).

---

## ⓪ · EL FRENO QUE SE DISOLVIÓ AL RE-MEDIR — y queda anotado porque casi cuesta la tanda

Abrí el censo con `CRITERIO_LEGAL_GUARDERIA.md` **inexistente**: verificado por
`git ls-tree` sobre **todas** las ramas locales y remotas, y por grep de la
cadena `CRITERIO_LEGAL` en `docs/` = **0 ocurrencias**. Con `LETRA_GUARDERIA.md`
🔴 frenada entera desde el 25-ago (D-918 · D-919) y su cabecera diciendo
literalmente que dejar el resto vigente *«invitaría a construir el transporte y
la documentación sobre una línea de responsabilidad que no existe»* — es decir,
**mi tramo, nombrado** —, tenía el freno 76b/L-142 escrito y listo.

**No lo emití.** Al crear el worktree, `main` traía un commit que no existía
cuando empecé a medir: `67ea7a5a merge: S107-A registro de los dos canon`. El
criterio y el plan **llegaron mientras yo medía.**

⇒ **L-166 en carne, y esta vez del lado del que iba a frenar.** Mi medición era
verdadera al momento de hacerse y **falsa al momento de usarse**; la ventana
fueron minutos. *Un freno se emite contra el objeto RELEÍDO, no contra la
medición con la que uno abrió* — y en una sesión de cuatro pistas donde A
mergea todo el tiempo, esa distancia es la regla, no la excepción.
**Nada que curar: el método funcionó porque el worktree obligó a tocar `main`
de nuevo.** Queda anotado para que la próxima pista que abra con un documento
faltante re-mida antes de escribir el freno, no después.

---

## ① · EL CENSO — el motor del paseo contra lo que la guardería necesita

Medido sobre el repo y la DB viva (`db query --linked`), no sobre memoria.

### (a) Lo que sirve TAL CUAL — cero trabajo

| pieza | dónde | por qué sirve |
|---|---|---|
| **Subida en dos pasos con huérfano recuperable** | `subir-evidencia.ts` · `subir-clip.ts` | subir bytes → registrar; si el paso 2 falla con la subida hecha, el reintento **entra por `storagePath` y salta al paso 2 — jamás re-sube**. Patrón de S44, curado en S61. Es exactamente el chasis que la cola necesita debajo. |
| **`leerBytes`** (frontera dual-forma) | `packages/ui/leer-archivo.ts` | L-137 3ª enmienda: ya sobrevivió a los uris pre-codificados de galería. **No se re-implementa.** |
| **Captura de foto con estados** | `EvidenciaFoto.Capturar/.Thumbnail` + `capturaFoto.tsx` | calidad 0.7 + resize; los tres estados de subida ya dibujados. |
| **Grabar → revisar → usar/descartar** | `apps/prestador/src/app/adiestramiento/clips.tsx` | 🟢 **el flujo de revisión que el brief pide YA EXISTE**, incluido el **auto-stop del grabador a los 30 s** (`recordAsync({ maxDuration })`) y el camino a Ajustes ante permiso denegado. |
| **Errores tipados con causa** | ambos uploaders | `lectura · red · servidor · registro`, con el literal al log. La voz honesta de la cola se apoya acá. |
| **Mapa** | `MapaRecorrido` · `PinEnMapa` · `MapaZona` (+ variantes `.web`) | `react-native-maps` está en **ui, cliente y prestador**. |
| **Catálogo de novedades/incidencias** | `cat_novedades_paseo` · `cat_incidencias_paseo` · `evento_paseo_novedades` | molde exacto para los chips de la estadía: catálogo por familia, voz por idioma. |

### (b) Lo que sirve CON PARÁMETRO — reuso con cambio menor

1. **`CLIP_MIN_S = 15`** (`clips-sesion.ts`). En adiestramiento el mínimo tiene
   sentido (un clip de 3 s no muestra progreso). **En guardería no aplica**: un
   animal comiendo cinco segundos es contenido válido. ⇒ **parámetro, y su
   default en guardería es 0.**
2. **`CLIPS_MAX = 3` por sesión.** En guardería el techo no es por sesión: es
   **por día y por animal**. ⇒ parámetro; el número lo firma la mesa.
3. **`videoQuality="720p"`** está fijo en la pantalla. ⇒ sube a parámetro del
   módulo (ver hallazgo ③: hoy es la **única** palanca de compresión que existe).
4. **Bucket y path.** Hoy `cita-archivos` (fotos, `image/jpeg`) y
   `adiestramiento-clips` (video: `mp4 · quicktime · webm`, 50 MB). El path
   `{prestador_id}/…` **no es cosmético**: lo exigen las policies de storage
   (`split_part` del primer segmento) **y** el RPC de registro. ⇒ el bucket de
   guardería nace con la misma forma de path o se reusa; **es contrato de A.**

### (c) Lo que NO existe — construcción nueva, con su número

Los cinco hallazgos que cambian el presupuesto del tramo están en §②.

---

## ② · LOS CINCO HALLAZGOS, CON SU MEDICIÓN

### 🔴 ① UNA MEDIA, N ANIMALES: NO EXISTE — y nunca pasó en la historia de la base

**La pregunta del brief** («¿cómo etiqueta hoy el paseo grupal: por paseo o por
animal?») **tiene respuesta dura: por ANIMAL, y no puede ser de otro modo.**

- `evento_archivo_adjunto.evento_padre_id` es **NOT NULL** y apunta a UN evento
  del bio-expediente; el evento cuelga de `evento_atencion`, que tiene
  **`mascota_id` — una atención, un animal.**
- Medido sobre datos vivos: **4 filas de archivo · 4 paths distintos · `0`
  paths compartidos · 3 eventos padre.** **Ninguna media colgó jamás de dos
  animales.** No hay tabla de etiquetas, ni unicidad que lo contemple, ni
  productor.

⇒ Hoy, una foto de tres perros o **la ve un solo dueño**, o **se sube tres
veces** (tres copias del mismo byte, tres registros, tres URLs firmadas). La
firma ① del founder — *«la foto llega al hilo y al expediente de **cada**
animal etiquetado»* — **no tiene dónde escribirse.** Es el hueco central del
módulo y **es contrato de A** (pedido en §③).

*Por qué el paseo nunca lo sintió, y por qué importa: el paseo es grupal por
norma (P19) desde S59 y la foto igual se sacaba por atención — nadie fue a
mirar si la de grupo llegaba a los otros dueños. **La estructura no falló:
nunca se la ejerció.***

### 🔴 ② LA COLA NO ES COLA — es un `Map` que muere con la app

`clips-sesion.ts` es **estado de módulo**, y su cabecera lo declara sin
maquillar: *«sobrevive a navegar dentro del proceso; **muere con la app**»*,
justificado en `MODELO_ADIESTRAMIENTO` §12.6 (*«captura jamás exigida al
cierre: un clip perdido no bloquea nada»*).

**Esa justificación no se traslada.** En guardería la foto del acta es
**respaldo legal** (criterio §4: *«las dos actas valen más que el contrato»* —
sin foto de entrada, la pregunta de *cuándo apareció la lesión* no tiene
respuesta y la carga cae sobre quien tenía al animal). Una foto de acta que se
pierde al cerrar la app no es una molestia: **es la prueba que faltaba.**

⇒ **Cola persistente = construcción nueva, y es mía.** No persiste, no
reintenta, no tiene backoff, no sobrevive al cierre. Lo que sí está resuelto
debajo es el reintento sin re-subir (§①a). **Cero módulo nativo:
`async-storage` está en las dos apps.**

### 🔴 ③ LA COMPRESIÓN NO ESTÁ MEDIDA — y el número que circula es prosa

El comentario de `clips.tsx` dice *«CameraView 720p + bitrate acotado — un clip
de 30 s pesa ~9 MB»*, y el mensaje de commit de S63 dice *«bitrate 2.5 Mbps»*.
**Medido: `grep` de `bitrate` en `apps/prestador` = 0 fuera de la
videollamada.** Lo único configurado es `videoQuality="720p"`; **el bitrate lo
elige el encoder del dispositivo.**

⇒ El **~9 MB no es una medición: es una estimación heredada**, y el peso real
varía por teléfono. **El dato que el brief me pide declarar al cierre (peso
promedio de foto y de clip + costo por estadía y por mes) no se puede derivar
del repo: sale de capturar en aparato.** Queda como medición pendiente **con
su método**, no como número inventado. *(La videollamada sí fija bitrate — y
lo dice el servidor, con la nota «un bitrate adivinado por la app es una
mentira»; el mismo criterio aplica acá.)*

### 🔴 ④ EL PUNTO VIVO TIENE DOS ANCESTROS Y **LOS DOS LECTORES SON INCOMPATIBLES** — por forma del dato, no por permisos

| ancestro | qué modela | por qué no sirve tal cual |
|---|---|---|
| `eventos_mascota_paseo.track_gps` | traza completa **por mascota** | guarda y expone **la traza entera** |
| `envios.track_gps` + `destino_lat/lon` + `repartidor_id` (despensa, S96) | **un vehículo con destino yendo a una casa** — el más cercano al tramo | su lector (`despensa-seguimiento.ts`) valida, ordena y **entrega al cliente todos los puntos del track** |

Para el envío de despensa está **bien**: un envío, un cliente, un destino — la
traza es la de su propio pedido. **Para el tramo de guardería es
estructuralmente incompatible:** la ruta de recogida pasa por las casas de los
demás, así que **la traza ES la lista de domicilios ajenos**, y las paradas se
leen en la densidad de puntos. Reusar cualquiera de los dos lectores viola el
🔴 de privacidad del brief **sin que ningún permiso ni policy se rompa**: el
dato viaja legítimamente y dice de más.

⇒ **La regla del punto vivo, que B me pide para su pieza 8 y acá queda fijada:**
**se expone el ÚLTIMO punto y nada más** — sin arreglo de puntos, sin
polilínea, sin paradas, sin histórico —, **y solo mientras el animal de ESE
dueño está `pendiente` o `a_bordo` en ESE tramo.** Fuera de esa ventana no hay
punto: no se muestra congelado ni se dice dónde estuvo. *Un mapa que muestra
dos puntos ya es una traza.*

### 🔴 ⑤ EL DIGEST NO EXISTE — y su ausencia falla EN SILENCIO

Medido en el catálogo vivo:

- **Existe** el techo (`cat_notificacion_categorias.techo_ventana_horas` /
  `techo_max`), el dedup (`notificacion_intencion.clave_dedup`), el kill switch
  (`notificacion_config`) y la categoría **`resumen`** — con **`0` tipos que la
  usen**: es el molde vacío que §8 del modelo dejó a propósito para el volumen
  de despensa.
- **No existe** ninguna función que **componga** un aviso agrupado. `techo`
  **corta**, `dedup` **descarta**: **ninguno de los dos agrupa.** «3 fotos
  nuevas de Thor» no tiene productor.

**Y el número que lo vuelve urgente:** la categoría `operacion` tiene techo
**20 en 24 h**. Un día de guardería con 4 animales × 5 fotos = **20 avisos**:
el techo se consume entero, y **lo que se pierde después se pierde callado** —
incluida la marca del retorno («está en casa»), salvo que su tipo lleve
`ignora_techo`. *El modo de falla no es una bandeja llena: es un dueño que no
recibe el aviso que sí importaba.*

⇒ **Es contrato de A** (pedido en §③), y el brief me autoriza a proponer y
declarar, no a inventar: **propongo** que la media entre por `resumen` con un
productor de digest, y que **las marcas de tramo y las de acta entren por
`operacion` con `ignora_techo`** — son operativas e inmediatas y **jamás** al
digest (regla del brief, y ahora también la razón: el techo se las comería).

---

## ③ · LOS TRES PEDIDOS A LA PISTA A — molde S91, autocontenidos

> Cada uno es **bloqueante de una parte de mi módulo** y ninguno lo puedo
> escribir yo (DB y `docs/` son territorio de A). Van con el contrato exacto
> para que no viajen por referencia (regla 75 · L-142).

**PEDIDO D→A ① · EL ESQUEMA DE MEDIA: UNA MEDIA, N ANIMALES.**
Necesito registrar **un archivo subido una vez** y **N etiquetas** sobre él, de
modo que cada animal etiquetado reciba **su** evento en **su** expediente
apuntando **al mismo objeto de storage** (un byte, N lecturas). Hoy
`evento_archivo_adjunto.evento_padre_id` es NOT NULL y fuerza 1:1 (medido: 0
paths compartidos en 4 filas). Lo que consumo desde la app, con estos nombres o
los que A decida **publicados antes de que yo monte**: un registrador que
reciba `storage_path`, `bucket`, `mime`, `duracion_segundos?` y **la lista de
`mascota_id`** (mínimo 1), y devuelva el id de la media + los ids de evento
creados. **Requisitos duros:** ① el conjunto de mascotas se valida contra el
**roster del día de esa estadía** — una mascota ajena rebota tipado; ② es
**idempotente** por `storage_path` (el reintento de la cola no puede duplicar
eventos); ③ **corregir etiquetas el mismo día** es firma del founder ⇒ hace
falta el camino de **re-etiquetar** (agregar y quitar) con la misma validación,
y después del día, cerrado.

**PEDIDO D→A ② · EL LECTOR DEL PUNTO VIVO, QUE DEVUELVE UN PUNTO.**
Un lector para la app del **dueño** que, dado su animal, devuelva **`{lat, lng,
t}` del último punto y nada más** — nunca un arreglo —, y **`null` fuera de la
ventana** (`pendiente` / `a_bordo` en ese tramo). La razón es la §②④: la traza
de una ruta de recogida revela los domicilios de las otras familias, y ninguna
policy lo impide porque el dato viaja legítimamente. 🔴 **La decisión de
recortar tiene que vivir en el SERVIDOR**: si el servidor manda el track y la
app pinta un punto, la traza ya salió del servidor y una versión vieja del
cliente la dibuja entera. *Una autorización que decide el cliente es
decorativa* (precedente de la plata del día, S83). **Del lado escritor**, el
punto del vehículo del tramo debe poder guardarse **sin conservar la traza** —
o conservarla en un lugar que el dueño no lea nunca.

**PEDIDO D→A ③ · EL DIGEST, O SU DECISIÓN.**
Necesito enchufar los avisos de media **agrupados** y no existe productor
(§②⑤). Propongo: tipo nuevo en categoría `resumen` con ventana y clave por
`(destinatario, mascota, día)`, que **componga** («3 fotos nuevas de Thor») en
vez de cortar; y tipos de tramo/acta en `operacion` con **`ignora_techo`**,
porque hoy 20 fotos consumen el techo diario y silencian el retorno. **Si la
mesa decide otra cosa, la respeto — pero el módulo no puede quedar mandando una
push por foto, y tampoco puede quedar con el aviso del retorno cortado por el
techo.** *El estado de hoy no da error: da silencio.*

---

## ④ · EL HALLAZGO QUE ACHICA EL TRABAJO: **EL MICRÓFONO YA EXISTE — el binario no lo obliga mi módulo**

El brief dice *«🔴 micrófono NO existe todavía — el clip lo estrena»* y el plan
§2 ordena cortar el binario *«UNA vez, al final»* por ese permiso. **Medido
contra `app.json` y las builds: es falso.**

- El prestador declara **`NSMicrophoneUsageDescription`**,
  `FOREGROUND_SERVICE_MICROPHONE`, y **`expo-camera` con
  `microphonePermission`** — con la cadena ya escrita y nombrando *«el audio de
  los clips de adiestramiento»*. Entró en **S63** (`77a745c5`, versión 1.0.2) y
  se reforzó en S73 con `expo-speech-recognition`.
- **El clip de adiestramiento ya lo estrenó**: `/adiestramiento/clips` graba
  con audio desde entonces.
- Builds reales (`eas build:list`): prestador **1.0.6 · 24-ago · FINISHED**;
  cliente **1.0.5 · 24-ago · FINISHED**. **Las dos ya llevan esos permisos
  horneados.**

**Y el binario igual hace falta — por otra razón, que ya estaba comprometida:**
el repo declara prestador **1.0.7** y cliente **1.0.6**, subidas en S106 con
`expo-screen-capture`. ⇒ **hay un tren pendiente desde S106 y mi módulo se sube
a él; no lo convoca.**

**Lo que sí decidiría un binario nuevo por mi lado es UNO solo: la compresión.**
Si se mantiene **en captura** (`videoQuality`, como hoy), **mi módulo entero
viaja por OTA** — foto, clip, cola, punto vivo y avisos usan
`expo-camera`/`expo-image-picker`/`expo-location`/`async-storage`/`react-native-maps`/`expo-notifications`,
**todos ya instalados en las apps que los necesitan** (verificado paquete por
paquete). Si se exige compresión **post-proceso** (un `react-native-compressor`
o similar), eso **sí** es módulo nativo nuevo y **sí** obliga al tren.

⇒ **Mi default, declarado y reversible: compresión EN CAPTURA, cero módulo
nuevo.** El founder juzga la calidad en pantalla (como pide el brief); si la
juzga insuficiente, la alternativa cuesta un lugar en el tren que ya sale.

---

## ⑤ · LO QUE ESTÁ BLOQUEADO Y LO QUE NO

| pieza del módulo | estado | espera |
|---|---|---|
| Cola persistente con backoff | **libre — arranca ya** | nada (el registrador entra inyectado; queda INERTE hasta A ①) |
| Compresión + su medición en aparato | **libre — arranca ya** | el aparato para el número |
| Captura foto/clip con el encuadre-ley (criterio §5) | **libre de letra** — la guía de encuadre es **instrucción operativa**, no texto legal (§0 lo prohíbe para lo legal, y esto no lo es) | pieza **4** (selector multi-animal) y **5** (modo clip) de B |
| Publicar la media etiquetada | 🔴 **bloqueada** | **A ①** |
| Punto vivo | 🔴 **bloqueada** en su lector | **A ②** · pieza **8** de B (a la que ya le fijé la regla, §②④) |
| Avisos agrupados | 🔴 **bloqueada** | **A ③** |

**Lo que arranco ahora, sin pisar a nadie:** la cola persistente y la
compresión, en `apps/prestador/src/lib/` (territorio de C por archivo, **no**
por carpeta de librería — lo declaro acá y si C lo reclama, se mueve: es
`lib/`, el mismo lugar donde ya viven `subir-clip.ts` y `clips-sesion.ts`).

---

## ⑤bis · LO CONSTRUIDO EN ESTA TANDA — la cola persistente

`apps/prestador/src/lib/cola-media.ts` + `scripts/verify-cola-media-s107d.mts`.
**Typecheck prestador 0 · arnés 13/13 verde · rojo producido.**

Cura el hallazgo §②②. Persiste en AsyncStorage, reintenta con backoff
**5 s · 15 s · 45 s · 2 m15 · 6 m45 · 20 m** y después queda en `error`
**visible**; **nunca descarta sola** un ítem no publicado — descartar es acto
explícito del cuidador (la foto del acta es prueba, criterio §4). Hereda el
chasis de dos pasos con huérfano recuperable de S44/S61 y lo vuelve **estado
persistido** (`subida_sin_registrar`), así que el reintento salta el paso 1
**también después de cerrar la app**.

**Nace INERTE (molde S91):** el subidor y el registrador entran **inyectados**
— no importa ningún wrapper —, así que no puede publicar nada hasta que exista
el pedido D→A ①; sin motor cableado guarda y lo **dice** (`motor_no_cableado`),
jamás en silencio. El **almacén también se inyecta** (precedente de
`bloqueo-biometrico.ts`), y es lo que la vuelve probable en un banco.

⚠️ **Límite declarado: persiste, pero NO sube con la app cerrada.** La subida en
segundo plano exige módulo nativo ⇒ binario. Lo que la cola garantiza es que
**nada se pierde**: al reabrir, lo pendiente sigue y reintenta solo. *Prometer
background sin el módulo sería la voz verosímil-y-falsa que L-139 prohíbe.*

**El peso, resuelto con método y no con número:** la cola guarda `bytes` del
**ArrayBuffer que el subidor ya lee para subir** — medirlo aparte sería un
segundo número que puede diferir del que viajó — y `pesoMedido('foto'|'clip')`
devuelve promedio y máximo **del uso real**. Sin capturas devuelve `null`:
*el promedio de cero capturas no es cero, es ausencia de dato.* ⇒ el dato que
el brief me pide al cierre **saldrá del gate en aparato, ya instrumentado**.

**🔴 Y el arnés cazó un verde flojo propio, que es lo que vale contar:** la
primera prueba en rojo —romper la escritura del `storagePath` entre el paso 1 y
el 2— **dio VERDE igual**. Causa medida: el path se persiste por **DOS** caminos
(también en `fallo()`), y romper uno no cambia el comportamiento. Hizo falta
romper los dos para que 3b y 3c cayeran (`subidas=2`). ⇒ el arnés discrimina la
**propiedad** «el byte no se sube dos veces», no una línea; la redundancia quedó
**medida y no supuesta**; y el assert 3d se renombró para decir lo que mide y no
lo que uno querría que midiera. *Si me quedaba con el primer verde, archivaba
como probada una línea que ninguna prueba tocaba.*

---

## ⑤ter · TANDA 2 — el motor de subida, y la respuesta medida sobre el APK

### (a) EL APK DEL FOUNDER — contestado con dato, y con tiempo

Los dos ids del pie son **UUIDv7**, así que su prefijo ES un reloj: cliente
`01a04649` ≈ **28-ago 02:53 UTC** · prestador `01a04697` ≈ **28-ago 04:18 UTC**
(±18 min) — o sea updates de **anoche**, de la tanda de S107-CERT.

| gate | ¿sirve el APK de hoy? | medición |
|---|---|---|
| **punto vivo (mapas)** | 🟢 **sí** | el APK instalado pasó `verify-manifest-apk.mjs` **en verde con `✓ meta-data geo.API_KEY`** (registro de S107-CERT, 27-ago) — es lo que hizo volver `MAPA_NATIVO_DISPONIBLE` a `true` |
| **avisos (push)** | 🔴 **probablemente NO** | ver abajo |

**Por qué el push probablemente no está, sin afirmar de más:** el guard exige
**tres** cosas (package · `geo.API_KEY` · FCM: `google_app_id` + listener
`MESSAGING_EVENT`) y **falla si falta una**, así que un verde entero implicaría
FCM. Pero el único registro que encontré nombra **una sola** de las tres, y no
hay acta de esa corrida. Y hay una señal fuerte en contra: **no existe build de
nube 1.0.7 (prestador) ni 1.0.6 (cliente)** — medido sobre 10 builds; las
últimas son 1.0.6 y 1.0.5, del 24-ago. ⇒ **esos APK son locales**, y
`GOOGLE_SERVICES_JSON` es un secret *«que solo el builder de EAS puede leer»*.
**Una build local no lo hornea salvo que alguien lo exporte a mano.**

🟢 **Y la buena, que despeja el gate del final:** medido con `eas env:list
development` —el environment que usa el perfil `preview`— **las DOS apps tienen
`GOOGLE_MAPS_API_KEY` y `GOOGLE_SERVICES_JSON`**. ⇒ **el tren de nube
1.0.7/1.0.6 va a traer mapas Y push en ambas**: los dos gates corren sobre ese
binario, juntos.

**Lo que pido, entonces:** no hace falta que instales nada ahora — el gate de
avisos y el de punto vivo se hacen **los dos sobre la APK de nube del tren
final**. Si querés adelantar el punto vivo, tu APK de hoy alcanza. **Una línea
lo cierra sin discusión:** correr `verify-manifest-apk.mjs` contra el APK
instalado; si sale verde entero, el push también está y el gate se puede
adelantar.

### (b) EL MOTOR DE SUBIDA — `motor-media.ts`, typecheck 0

Completa el módulo **hasta el borde exacto donde empieza A**. El **paso 1 es
real y completo** (bytes a Storage con `leerBytes`, path `{prestador_id}/…`
porque las policies validan el primer segmento con `split_part` — no es
organización, **es el permiso**); el **paso 2 y el aviso entran inyectados.**

Tres decisiones que valen registro:

1. **`upsert: false` con el id del ítem en el path.** Reintentar el mismo ítem
   colisiona **a propósito**, y esa colisión se lee como *«ya estaba subido»* y
   sigue al registro — en vez de re-subir el byte o fallar.
2. **El aviso va después y no puede tumbar la publicación.** Si el aviso falla,
   la media **ya está en el hilo del dueño**; devolver error mandaría el ítem
   de vuelta a la cola y el reintento **registraría de nuevo**. *Un aviso
   fallado no puede convertirse en un evento duplicado en el expediente.*
3. **🔴 La agrupación NO puede vivir en la app, y es estructural:** dos
   cuidadores subiendo fotos del mismo animal desde dos teléfonos no pueden
   coordinar un digest local. **Agrupa el servidor o no agrupa nadie.** La app
   declara el hecho; el reparto firmado por la mesa lo aplica A.

**El contrato de A quedó escrito como TIPO** (`RegistrarMediaEtiquetada`,
`EntradaRegistroMedia`), no como párrafo: *un contrato en un tipo lo verifica el
compilador el día que se cablea; uno en prosa no lo verifica nadie.*

⚠️ **Límite declarado:** `motor-media.ts` **no es importable en node** (arrastra
React Native), así que no entra al arnés de banco — su prueba es el aparato. **El
caso crítico igual está cubierto**: «sin motor cableado, la cola guarda y lo
dice» se prueba en el arnés de la cola (assert 6), y es la misma propiedad.

### (c) LO QUE LA MESA FIRMÓ, REGISTRADO ACÁ

**Reparto de avisos firmado:** media del durante → categoría **`resumen`**,
agrupada, **sin competir por el techo de las operativas**; tramo («subió»,
«está en casa») y acta → **operativas e inmediatas**, que **ni el digest ni un
techo consumido por fotos pueden silenciar**. *Resuelve el hallazgo §②⑤ con su
número: `operacion` tiene techo 20/24h y 20 fotos se lo comían entero.* La
enmienda escrita del modelo va por **A**. · **Punto vivo:** la regla que le fijé
a B queda firmada tal cual. · **Micrófono:** un binario, al final, en la nube,
sobre el tren ya pendiente — **la compresión queda EN CAPTURA** y con eso todo
lo mío viaja por OTA.

---

## ⑤quater · TANDA 3 — contra el contrato de A y las piezas de B

### (a) 🔴 RESPUESTA A LA PISTA A — lo que su contrato §③ me pide devolver

> *«D mide primero cómo lo hace HOY el paseo grupal. **Si el paseo ya tiene la
> forma, se reusa; no se inventa una segunda.** Ese resultado vuelve a A y este
> contrato se enmienda si hace falta.»*

**Medido, y la respuesta es NO: el paseo no tiene la forma, y no puede tenerla.**

- `evento_archivo_adjunto.evento_padre_id` es **NOT NULL** → una fila de
  archivo cuelga de **un** evento.
- El evento cuelga de `evento_atencion`, que tiene **`mascota_id`** → **una
  atención es de UN animal.**
- Datos vivos: **4 filas · 4 paths distintos · `0` paths compartidos · 3
  eventos padre.** **Ninguna media colgó jamás de dos animales.** No hay tabla
  de etiquetas, ni unicidad que lo contemple, ni productor.

⇒ **No hay segunda forma: `guarderia_media` + `guarderia_media_etiquetas` es la
primera.** El contrato **no se enmienda** por esta medición — se confirma.

**Y un hallazgo de paso que no es mío y alguien debería fichar:** el paseo es
**grupal por norma desde S59** (P19) y su foto de grupo **hoy llega a un solo
dueño** — o se sube N veces. *No falló nunca porque nadie fue a mirar: la
estructura no se rompió, no se la ejerció.* El día que exista
`guarderia_media`, el paseo tiene a dónde migrar.

### (b) DOS CORRECCIONES DE CONTRATO, con su medición

1. **§⑤ dice «el permiso de micrófono… **es binario nuevo: no viaja por
   OTA**».** 🔴 **Vencido.** El micrófono está declarado desde **S63**
   (`expo-camera` con `microphonePermission`, `NSMicrophoneUsageDescription`) y
   **las builds del 24-ago ya lo llevan horneado**. El founder firmó el 28-ago:
   un binario al final, y **mi módulo se sube al tren ya pendiente
   (1.0.7/1.0.6), no lo convoca** — con la compresión en captura, **todo esto
   viaja por OTA**.
2. **§② no declara idempotencia de `publicarMedia`.** Era el requisito ① de mi
   pedido. **La cola reintenta por diseño**, así que un timeout ambiguo puede
   registrar dos veces. Queda **anotado en el tipo**, no supuesto — si A no la
   escribe, el defecto aparece como eventos duplicados en el expediente, que
   es la clase que no da error.

### (c) LO CONSTRUIDO

**Motor alineado al contrato oficial.** Lo que yo tenía escrito como tipo era
*mi* propuesta; ahora existe la de A y **manda**: `archivo_url` (no bucket+path
por separado) y ancla por **`fecha`** — el `estadia_id` lo resuelve el servidor,
que es más angosto que mandarlo desde el teléfono. *Dos contratos para la misma
puerta es peor que uno equivocado: cualquiera cita el que le conviene.*

**Y la cola cambió `estadiaId` por `fecha` en la fuente**, porque al alinear
quedé mapeando un id a un campo que se lee como fecha — **eso compila
perfecto**, que es justo por lo que se corrigió donde nace y no en el borde.

**El techo del clip bajó a la PUERTA** (`encolar`), no al consumidor: así lo
respetan todos los caminos, no solo el que lo conoce. Su assert prueba **las
dos mitades**: 34 s no entra · **30,9 s SÍ entra** — *la tolerancia del CHECK
existe para que un archivo honesto no rebote, no para grabar de más.*

**El módulo de captura** (`use-captura-media`), que monta C, con los parámetros
**declarados**: foto **0.7 / 1600 px** — el 1600 no es gusto, es el número que
la casa midió en **D-734** (con 800 la foto que se abre queda blanda, *y una
foto liviana que se ve mal es una regresión con mejor número*) — y clip
**720p en captura**, la vía firmada para que todo viaje por OTA.

**El encuadre viaja como CÓDIGO** (`encuadre.ts`, sin React ni RN para que sea
probable): §0 prohíbe redactar legales en las pistas y el contrato §④ lo
repite. La voz la pone el diccionario de C. Se modula por lugar —en
instalaciones no se guía sobre fachadas, *porque una guía que menciona lo que
no puede pasar enseña a ignorar la guía*— **salvo las dos duras: menores y
personas rigen siempre.** Modularlas sería el hueco por el que entra la foto
que no debía existir.

**El punto vivo** (`use-punto-vivo`) — 🔴 **no acumula, y ésa es toda la
pieza.** El tracker del paseo acumula porque ahí **la traza ES el producto**;
en el tramo **la traza es el riesgo**: es la lista de domicilios ajenos. Por eso
el reuso es **parcial y declarado**: se reusa `expo-location`, se tira la
acumulación. **La privacidad se cura en el ESCRITOR, no solo en el lector** —
un lector prudente sobre un escritor que guarda todo deja la traza escrita
esperando otra consulta (la lección de `D-921`). Y su contrato con A dice
**«pisá el último punto», nunca «agregá uno»**: implementado como INSERT, la
traza vuelve por la puerta de atrás. Corre en **foreground**, y eso es decisión
declarada: el permiso «siempre» ya existe, pero pasarlo a background implica
notificación persistente y emitir la posición del cuidador con la app cerrada
— **no se enciende sin firma**.

**Estado:** typecheck prestador **0** · arnés **27/27 verde**.

### (d) LO QUE SIGUE ESPERANDO

| pieza | espera |
|---|---|
| publicar media de verdad | `publicarMedia` + migración `guarderia_media` (**contrato publicado, wrapper y migración NO existen** — medido) |
| punto vivo de punta a punta | el lector del dueño y el destino de escritura del tramo (pedido D→A ②) |
| avisos agrupados | el productor del digest, del lado del servidor (firma de la mesa) |
| miniatura del clip | ⚠️ **v1 no la genera**: exige módulo nativo (`expo-video-thumbnails`), o sea un lugar en el tren. El contrato §⑤ ya lo contempla — *la miniatura es un dato del esquema, no una promesa* — y `MiniaturaClip` de B lo maneja con póster |
| todo lo de actas | el contrato de documentos/actas, que A todavía debe |

---

## ⑤quinquies · TANDA 4 — las actas, y los dos pedidos que quedan del lado servidor

### (a) LAS ACTAS — el choque que resolvió la pieza, y no era de red

El contrato de actas pide `levantarActaGuarderia({ …, mediaIds[] })` — **fotos
ya publicadas** — y al mismo tiempo firma dos cosas que no se pueden romper:
las fotos del estado **son media** (una sola forma para los dos lados) y 🔴 *«si
no confirma en el momento, **la recogida NO se frena**»*. Y el acta **nace
cerrada**: un trigger rechaza todo `UPDATE` salvo la conformidad.

**Las tres salidas, y por qué queda una:**

| | salida | veredicto |
|---|---|---|
| ① | esperar a que la media suba antes de levantar | 🔴 **frena la recogida** — prohibido por el contrato |
| ② | levantar sin `mediaIds` y completarlos después | 🔴 **imposible**: cerrada no se edita — *y esa inmutabilidad es justo lo que hace que el acta pruebe algo* |
| ③ | **encolar el acta ENTERA junto a sus fotos** y publicarla cuando todas estén arriba | ✅ la única que respeta las dos reglas |

⇒ **En la puerta el acta EXISTE, con su hora**; el viaje ocurre cuando hay
señal. **🔴 La hora que vale es la de la puerta, no la de la subida**, así que
viaja en el payload: *el servidor no la inventa del `INSERT`* — y en un
registro que existe para responder **cuándo** apareció una lesión, esa
diferencia es el registro entero.

**Dos reglas duras más:** **sin fotos no se levanta** (criterio §4 — un acta sin
foto de entrada no responde la única pregunta para la que existe) y **un fallo
NUNCA mata un acta**: vuelve a `lista` y reintenta. *El que se rinde es soporte,
con el registro delante* — una cola no descarta una prueba por seis timeouts.

**Y el orden en el hook no es casual:** primero media, después actas. Al revés,
las actas no encontrarían ningún `mediaId` y esperarían un ciclo entero de más,
**con el cuidador todavía en la puerta.**

**Arnés de actas 12/12, con su rojo producido:** rompiendo `mediaIdsDe` caen 4
asserts y el 3b muestra el **id local** viajando donde va el del servidor.

### (b) 🔴 PEDIDO D→A ④ · `levantarActaGuarderia` TIENE QUE SER IDEMPOTENTE — es el mismo defecto ya adoptado, en el acta

`UNIQUE (estadia_id, direccion)` **ya es su idempotencia natural** — pero sólo
sirve si el segundo intento **no rebota**. Mi cola reintenta por diseño, así que
un **timeout ambiguo** (el acta se creó, la respuesta no llegó) hace que el
reintento choque contra el índice.

**Pido exactamente lo que ya se adoptó para `publicarMedia`:** que el segundo
intento devuelva **el acta que ya existe** con `ya_existia: true`, en vez de un
error de unicidad. *Un reintento que rebota obliga a la cola a distinguir
«falló» de «ya estaba», y ésa es justo la distinción que no puede hacer con un
timeout ambiguo.* Si vuelve un `23505` pelado, **el acta correcta queda marcada
en error para siempre** — y un guard que vive en un índice **sólo puede
negarse** (`L-424`, el caso de `ya_invitada`). Ya está escrito en el tipo
(`LevantarActa`), inerte.

### (c) 🔴 PEDIDO D→A ⑤ · EL PUNTO VIVO: DÓNDE SE ESCRIBE Y CÓMO SE LEE

Mi mitad está construida (`use-punto-vivo`): capta y **no acumula**. Falta el
destino y el lector, y los dos tienen una condición que no es de estilo.

**El destino — una fila por tramo, que se PISA:**

```
guarderia_tramo_punto
  tramo_id   uuid PK        -- 🔴 PK, no id propio: una fila por tramo
  lat        double precision not null
  lng        double precision not null
  visto_en   timestamptz not null
```

🔴 **`UPDATE`, jamás `INSERT`.** Si se implementa como tabla de puntos, **la
traza vuelve por la puerta de atrás** y toda la cura del escritor se pierde:
las paradas se leen en la densidad, y las paradas de una ruta de recogida **son
las casas de las otras familias**.

**El lector del dueño — devuelve un punto o nada:**

`obtenerPuntoDeMiTramo(mascotaId)` → `{ lat, lng, visto_en } | null`.
**Nunca un arreglo.** `null` fuera de la ventana: el animal de ESE dueño tiene
que estar en `recogida_en_curso` o `retorno_en_curso` en ESE tramo (los estados
ya existen en el contrato de estadía, con `a_bordo_en`). Fuera de eso **no hay
punto**: no se muestra congelado ni se dice dónde estuvo.

🔴 **El recorte vive en el SERVIDOR.** Si el servidor manda la traza y la app
pinta un punto, **la traza ya salió del servidor** y una versión vieja del
cliente la dibuja entera. *Una autorización que decide el cliente es
decorativa* (precedente de la plata del día, S83).

⚠️ **Para C, cuando monte el mapa del dueño:** el lector devuelve **un punto**;
no hay polilínea que dibujar y no hay que pedirla. La pieza 8 de B ya nace con
esa regla.

### (d) 🔴 PEDIDO D→A ⑥ · EL PRODUCTOR DEL DIGEST, con la firma de la mesa

Mi disparo está escrito (`AvisarMediaPublicada`) e inerte. Falta el productor, y
**la agrupación no puede vivir en la app** — ya ratificado en el contrato §④bis.

Con la firma del 28-ago, el reparto es:

| qué | categoría | régimen |
|---|---|---|
| media del durante | **`resumen`** | **agrupada** («3 fotos nuevas de Thor»), y **jamás compite por el techo de las operativas** |
| tramo («subió», «está en casa») y **acta** | **`operacion`** | **inmediatas**, fuera del digest, y **no las silencia un techo consumido por fotos** ⇒ `ignora_techo` en sus tipos |

**El número que lo hace urgente, medido:** `operacion` tiene techo **20/24 h**.
Cuatro animales × cinco fotos = **20 avisos**, y lo que se pierde después **se
pierde callado** — incluido el aviso del retorno. *El modo de falla no es una
bandeja llena: es un dueño que no recibe el aviso que sí importaba.*

⚠️ Y el molde ya existe vacío: **la categoría `resumen` está en el catálogo con
`0` tipos que la usen** — nació en el día uno para el volumen de despensa y
nunca tuvo productor.

---

## ⑤sexies · TANDA 5 — el tuteo, medido; y una cadena que promete un límite que dejaría de ser cierto

### (a) EL CENSO DE VOZ, con el instrumento de la casa

Corrido `scripts/censo-voseo.mjs` (el de la casa, no uno mío):

| medido | resultado |
|---|---|
| mis siete archivos del módulo | **0 · 0 · 0 · 0 · 0 · 0 · 0** |
| `apps/prestador/app.json` · `apps/cliente/app.json` | **1 y 1** → **curados, re-corrido en 0** |

**El cero de mis archivos no es mérito: es diseño.** El módulo no tiene voz de
producto — *la voz la pone el diccionario de C* — y sus únicos literales son
mensajes de excepción y de log, que no llegan a ninguna pantalla.

**Lo curado, la misma frase en las dos apps:** `expo-image-picker.photosPermission`
— *«…solo cuando **elegís** una imagen…»* → **«eliges»**. JSON revalidado en
las dos.

🔴 **Y lo que hay que saber para que la cura llegue: una cadena de permiso NO
VIAJA POR OTA.** Vive en el manifest y en el `Info.plist`, o sea que **se
hornea en el binario**. Queda curada en el repo y llega al teléfono **con el
tren 1.0.7/1.0.6 que ya sale** — no antes. *Darla por curada al commitearla
sería exactamente la clase de verde que se cobra en el aparato.*

### (b) 🔴 EL HALLAZGO QUE NO ERA VOSEO: una cadena que promete un límite

Revisando las cadenas de permiso apareció algo más caro que el tuteo. La de
ubicación del prestador dice, hoy:

> `locationWhenInUsePermission`: *«La app usa tu ubicación **durante el paseo**
> para registrar el recorrido que ve la familia.»*
> `locationAlwaysAndWhenInUsePermission`: *«…aunque guardes el teléfono en el
> bolsillo. **Solo se usa mientras dura un paseo.**»*

**Medido:** `use-punto-vivo.ts` —mío— es un **consumidor nuevo de
`expo-location`**, para el tramo de recogida y retorno de guardería.

⇒ La primera queda **incompleta**: el prestador concede el permiso leyendo que
es para paseos. La segunda es peor: **«solo se usa mientras dura un paseo» es
una afirmación de LÍMITE**, y un límite que el código dejaría de respetar no es
una omisión — *es una promesa verosímil y falsa* (L-139), en la pantalla donde
alguien decide si confía.

**Precisión, para no pedir de más:** mi punto vivo corre en **foreground**, así
que **hoy la que queda incompleta es solo la primera**. La de «siempre»
cambiaría únicamente si el tramo pasa a segundo plano — que es la decisión que
ya dejé declarada y sin firmar.

**Propuesta, NO aplicada** (una cadena de permiso declara **alcance de
tratamiento**: curar un voseo es mecánico, ensanchar lo que la app dice que hace
con tu ubicación **es firma**, y el §0 manda los textos al lote del founder):

> *«La app usa tu ubicación durante el paseo y durante el traslado de
> guardería, para mostrarle a la familia el recorrido y dónde va el vehículo.»*

⚠️ **Y tiene fecha, por lo mismo que (a): si no entra ANTES del tren, no entra
hasta el siguiente binario.**

### (c) EL GATE DE LA CASA, VERDE

`verify:diseno` — **VERDE, 60 reglas encendidas**, con mis siete archivos
adentro. Incluye **R66**, la del voseo con baseline solo-baja.

### (d) Y ACÁ SE DEJA DE MONTAR

Orden recibida y ejecutada: **nada más contra motor inexistente.** Lo mío queda
**cableado e inerte**, con sus wrappers inyectados en `null` y diciendo por qué
(`motor_no_cableado`), esperando las migraciones de A.

---

## ⑥ · PREGUNTAS ABIERTAS, CON DUEÑO

1. **¿Techo de clips y fotos por día y por animal?** (§①b.2) — **mesa.** Hoy
   heredaría el `3` de adiestramiento, que es de otro oficio.
2. **¿Retención de la media?** El criterio §5.1 la da por resuelta («cierre de
   cuenta + 30 días», fila de la §18 de la política). **Lo registro como
   resuelto y no lo re-decido** — pero el brief dice *«v1 guarda todo y ninguna
   política de retención se inventa acá»*: **coinciden**, no hay choque.
3. **¿La foto del acta va al expediente del animal, o es documento del acta?**
   — **A**, con la letra. Importa porque la del acta tiene **encuadre distinto**
   (primer plano, sin fachada) y **finalidad distinta** (prueba), y su pérdida
   es más cara. *Hoy no lo decido: lo declaro.*
4. **¿Qué APK tiene el founder hoy?** No es medible desde acá: se lee **en el
   pie de Cuenta** de cada app (`update … · canal`, L-160). Lo pido antes de
   cualquier gate de aparato — **probar avisos o punto vivo en un binario de
   prueba da falso negativo y ya costó una build entera.**

---

**Método:** worktree propio (`e-petplace-s107-d`, rama `pista/s107-d`) · cero
escritura en DB, `packages/ui` ni `apps/` hasta acá · todas las mediciones
contra el objeto (DB viva, `app.json`, `eas build:list`, grep sobre el árbol),
ninguna contra memoria ni contra prosa del repo — **y el §②③ existe justamente
porque la prosa del repo decía un número que el código no tiene.**
