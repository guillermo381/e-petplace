# S99 · PARTE DEL LOOP — PISTA A (motor · DB · `packages/api` · docs maestros · publish)

> Formato: `PLAN_S99.md` §6. Tres secciones fijas. Se actualiza en vivo; la
> mesa consolida al cierre en UNA hoja de decisión.

---

# 🔷 HANDOFF VIGENTE — LA TANDA DEL INVENTARIO (el más nuevo: abre el archivo)

> ⚠️ **Se identifica por ORDEN, no por fecha, y se declara por qué:** las
> fechas de los handoffs anteriores de este parte (18-ago) van **por delante
> del reloj de la máquina** (16-ago). No se corrige ninguno hacia atrás —
> *una fecha reescrita es peor que una fecha rara*—; **el vigente es el que
> abre el archivo, y los de abajo quedan marcados histórico.**

## LO QUE ESTÁ EN EL APARATO Y ESPERA EL OJO DEL FOUNDER
**Ancla única `fbf7e593`, `dirty: None` en las dos apps, y el ancla está en `main`.**
· prestador group `662f30e3-e3a5-4d85-a4a8-0d2261ff4a78` · pie **`01a00bea`** · runtime 1.0.5
· cliente group `c7daf5c1-9e7d-44b8-b78e-2d537ddf7a00` · pie **`01a00beb`** · runtime 1.0.3
**Superseded declarados, en orden y sin ocultar** (cada uno se publicó antes de bajar la tanda siguiente de otra pista): prestador `1f0f8ae3` → `ae6d076e` · cliente `8971fc93` → `c0132f52`. *Tres publicaciones en la misma jornada: el costo de publicar seguido es que el group envejece; el de no publicar es un founder con cuatro juicios sin aparato.*
**Lo que suma esta última:** la consolidación de Stock (deja de ser una segunda lista) · **la ventana de la vitrina, que mostraba 100 de 563 SIN DECIRLO** —un hallazgo que solo aparece con la vitrina llena, o sea que la siembra pagó el mismo día— · y `EntradaDeCruce` montada en las dos.
**Los cuatro juicios que NINGÚN guard puede cubrir:** la barra (¿se hunde y no salta?) · el pin (¿parece una moto y es NUESTRA?) · la transición direccional (¿la ventana viene del lado del botón? ¿32 px se leen como dirección o como temblor?) · el escalón «una» de ATENDER.

## LA LEY DEL DÍA, FIRMADA Y YA EN EL MOTOR: UN SOLO INVENTARIO, DOS BOCAS
Letra en `MODELO_DESPENSA` **§8.6quinquies** (v2.7). Estado medido, punto por punto:
1. **Misma tabla ✅** — las dos bocas escriben `inventario_movimientos`; **el único escritor del saldo es el trigger**, ninguna función lo pisa.
2. **La carrera ✅** — la reserva descuenta del disponible y el mostrador lee ESE disponible ⇒ **no puede vender lo apartado**; los `CHECK >= 0` son el invariante y **ahora el trigger lo DICE** (`stock_insuficiente` / `reserva_insuficiente`, D-827 en su puerta más cara).
3. **La reserva que no se paga ✅ — pero la cura obvia era un arma.** El carrito abandonado ya lo cubría `expirar_pedidos_sin_pago` (cron horario, 91 corridas succeeded, cero pedidos viejos). Lo que faltaba era el reloj de la reserva de quien empezó a pagar. **🔴 `expirar_reservas_vencidas` filtraba SOLO por `expira_en`: las 13 reservas vivas eran las 13 de PEDIDOS PAGADOS, 12 ya vencidas ⇒ un tick devolvía 15 unidades VENDIDAS al disponible.** Cinturón por discriminación: viejo=12, nuevo=0. **Gate primero, reloj después** (cada 5 min).
4. **Ledger ✅** — y el desvío que C midió **NO era el trigger**: 172 de 173 SKU reconcilian exacto; los 2 divergentes son residuo de **S95-K, que corrigió un ledger append-only BORRANDO filas** (tercer cobro de L-231). Reconciliados con rastro en `inventario_reconciliaciones`.
**Y el juez nuevo que evita que vuelva a pasar:** `verificar_coherencia_inventario()` — *el saldo es derivado y NADA lo comparaba con su fuente; por eso una divergencia vivió cuatro días sin que fallara nada.*

## LA VITRINA, LISTA PARA JUZGAR (siembra del founder, ejecutada)
**563 ofertas publicadas: 483 comprables · 80 agotadas.** Reparto a propósito — **361 holgado · 84 escaso (2–3, para agotar EN VIVO) · 87 en cero de verdad** (sin ceros el filtro no se puede probar).
**Y la competencia, que NO existía en ninguna variante (medido: 0): 25 variantes con dos vendedores** — 18 con los dos con stock y precio distinto, 7 donde solo uno tiene. *Recién ahora la cadena de selección se puede ver ejerciéndose.*
**`hay_stock` vive**: booleano en `ofertas`, derivado por trigger, **neto de reservas**, y **se pisa solo** (el UPDATE del cliente no se lee jamás). Ya viaja en `ProductoDeVitrina`. **Falta la superficie ⇒ D-840.**

## 🔴 LO QUE ESPERA FIRMA Y NO TRABAJO
· **El criterio provisional de selección entre vendedores.** La cadena está firmada (gates STOCK/ZONA · orden CALIFICACIÓN · desempate ANTIGÜEDAD · ventana de llegada) **y ninguno de los tres criterios tiene dato**: la calificación de vendedor no existe (0 filas) ⇒ el orden colapsa a ANTIGÜEDAD, que le da la vitrina al que llegó primero. Mi enmienda servida: **rotación determinista que muere sola** el día que exista la calificación.
· **Lo que la mesa decida sobre el agotado en la vitrina** — la letra ya está (mostrar y decir), la pantalla no (D-840).

## COMPROMISOS EN LAS DOS DIRECCIONES
**Debo:** nada bloqueante. **Me deben:** B — la receta de FILA (**el cuello: la ALTURA primero, que destraba tres frentes; la gramática de BLOQUE después, que es lo que la ficha del repartidor necesita**) · C — la voz de la promesa (destrabada: `motivo_corrimiento` distingue `sin_operacion` / `cupo_lleno` / `mixto`) y la superficie de `hay_stock` · D — el seguimiento sobre la receta de B.
**Corregido y pasado a B:** la fila del repartidor **YA navega** (`configuracion.tsx:771`/`:773` contra `origin/main`); lo que falta es **ver el estado SIN entrar**.

## LAS RUTINAS QUE ESTA TANDA VOLVIÓ A COBRAR
· **`git -C <ruta>` SIEMPRE** — el `cd` no persiste entre llamadas y el árbol equivocado muerde (novena vez en la sesión).
· **`eas-cli` SOLO desde `apps/<app>/`**, aunque solo estés mirando.
· **El ancla se lee del OBJETO** (`update:view --json` → `gitCommitHash` + `isGitWorkingTreeDirty`); `update:list` NO expone el hash.
· **El número libre de D-/L- se verifica POR GREP** — y esta tanda cobró una **colisión real**: B depositó un `D-832` que ya existía; se corrió el nuevo a **D-839** y la vieja no se tocó.
· **Cerrado se verifica POR SHA en las dos puntas**, jamás por código de salida.

---

## (histórico) HANDOFF DE A — 18-ago NOCHE — SUPERSEDED por el que abre este archivo

### ⚡ LO QUE NO SE PUEDE PERDER

- **📱 OTA VIGENTE: prestador `5e6cc73e` (pie `update 01a00b6c`) · cliente
  `9f559bcd` (pie `update 01a00b6d`) · ancla `bccd6edf` en las dos, dirty
  None.** ⚠️ **`d7c8d4eb` quedó SUPERSEDED por incompleto** (salió sin C):
  si el pie muestra otra cosa que `01a00b6c`, el bueno no aplicó.
- **🔴 LA CADENA DE SELECCIÓN ENTRE VENDEDORES (firmada, sin construir):**
  **PUERTAS** ① STOCK · ② ZONA — **nada más** · **ORDEN** ③ CALIFICACIÓN ·
  **DESEMPATE** ④ ANTIGÜEDAD · **⑤ VENTANA DE ARRANQUE: 10 pedidos o 30
  días, lo que llegue primero, compitiendo con la CALIFICACIÓN PROMEDIO
  DEL ECOSISTEMA** (cero rama especial: es la misma cadena con un valor
  asumido) · **🔴 EL CUPO NO FILTRA: DETERMINA LA PROMESA** (*no dice «no
  puedo», dice «no puedo hoy»*). **Índice YA movido a `UNIQUE(cuenta,
  variante)`** — la competencia es expresable. **NINGUNO de los tres
  criterios tiene dato confirmado** (calificación de vendedor NO EXISTE:
  0 reseñas; zona es lista de CIUDADES, no distancia; antigüedad sí) —
  **el censo con costo está en el parte y DECIDE QUÉ LLEGA A OCTUBRE.**
  **Mi enmienda servida sin ejecutar:** sin ③, el orden colapsa a
  ANTIGÜEDAD (no a cercanía) ⇒ propongo ROTACIÓN DETERMINISTA como
  desempate provisional, que muere sola cuando ③ exista.
- **🗣️ LA VOZ DE LA PROMESA — CUATRO CAMINOS** (motor listo, voz de C con
  receta de B): ① **corte normal** → dato, sin disculpa ni pregunta · ②
  **`cupo_lleno`** → la frase del founder, terminando en PREGUNTA · ③
  **`sin_turnos_de_entrega`** → SE DICE, jamás un vacío · ④
  **`sin_operacion`** → «no repartimos los domingos»: **NO es escasez y
  no suena a disculpa.** El dato lo da `promesaPorVendedor`
  (`motivoCorrimiento`) — **jamás `saltos_por_cupo`, que no dice la
  causa (L-256).**
- **🧊 D-834 — TIEMPOS CONGELADA:** cuatro sospechosos muertos por
  medición (red · olas · bundle +1,06 % · instrumento); **próximo paso:
  el SEGUNDO APARATO**. Nadie toca rendimiento hasta sesión propia.
- **📅 PDF-factura en SEPTIEMBRE** (§14 rige) · **Excel determinista SÍ
  llega a octubre** (motor entregado; sin §14 porque no es IA).
- **🌱 D-838 — LA SIEMBRA, con su cierre:** ~525 SKUs + multi-seller (5
  vendedores publicando) + 96 SKUs del choque + el segundo pedido pagado
  de duenodes. **Marca contable `SIEMBRA-S99-%`; el cierre es un CONTEO
  EN CERO, medido — y contempla los movimientos de ledger del stock.**
  **Muere antes del primer vendedor real con oferta publicada.**
- **📡 CANAL ENTRE SESIONES: CAÍDO para las cuatro — TODO viaja por repo.**

### La cola de cada pista, con su bloqueo NOMBRADO

- **A (yo):** motor de L5a/L5b **COMPLETO**; el lector de la promesa
  entregado. **Sin bloqueos.** Lo próximo si la mesa lo pide: el criterio
  provisional de selección (espera FIRMA, no trabajo) · el lector de
  selección server-side (espera el criterio) · D-835 🟡 (espera su rojo
  real — el primer sin-rol de verdad lo trae gratis).
- **B — ES EL CUELLO: la receta de FILA destraba CUATRO frentes.** Con
  ella entran las tres de C (conmutación lista⇄íconos · el número real de
  la ventana · **la consolidación de la vitrina sobre STOCK**) y el costo
  ya lo nombró el founder: *«se vuelve complejo cuando un seller tiene
  muchos productos»* — con 399 sembrados, es real. **Y lo demás suyo:**
  la barra (6 correcciones, quedó PEOR: disco al verde del techo, barra
  BLANCA, el hueco que no se ve, etiqueta DENTRO, ícono centrado, más
  metido, transición más lenta) · el pin (falta **la marca**: el magenta
  ya está —`mapaMoto` #681F4C— y **el problema es LUMINOSIDAD 0.26, no
  saturación**) · el techo `Encabezado` (**censo servido: 110 montan, 84
  pasan `titulo` ⇒ ~26 sitios**) · la **ficha del repartidor en
  configuración**, dentro de su gramática, jamás como excepción.
- **C:** la voz de la promesa (ya destrabada) · **ATENDER → Venta de
  productos NO lleva a la vitrina** (dos listas del mismo objeto, una sin
  diseño) · el escalón «una» **que solo cierra el dedo — el suyo** ·
  Stock+vitrina esperando la fila de B.
- **D:** el orden FIFO — **su bloqueo murió: duenodes ya tiene 2 pedidos
  vivos con pago y horas distintas.**

### Compromisos, en las dos direcciones

- **DEBO:** nada bloqueante. (A B: cardinalidades ya servidas · a C: los
  dos lectores entregados · a D: la siembra entregada.)
- **ME DEBEN:** B la fila y sus correcciones · C la voz, la vitrina en
  ATENDER y el escalón caminado · **la MESA: el criterio provisional de
  selección y la ventana** (con mi censo delante) · el gate de aparato de
  la barra y el pin corregidos.

### Rutinas que NO se pierden

Merge de ramas **por SHA** · **la carga de todo OTA-para-gate por
merge-base ANTES de publicar** · **el cwd se verifica ANTES de escribir**
(ocho mordidas de los dos árboles en la sesión) · numeración **por grep**
(vivas: **D-838** · **L-256** · R48) · claves **del keychain al momento**,
jamás en chat ni repo · **L-140** en toda función nueva.

---

## 76(h) · MIS ARCHIVOS DECLARADOS (apertura, 14-ago-2026)

- `supabase/migrations/**` · `supabase/functions/**` · `supabase/dev/**`
- `packages/api/**` (wrappers y tipos generados)
- `docs/**` — maestros, fichas (`DEUDAS_CANONICAS.md`), relevamientos de A,
  `docs/loop/S99-A.md` (este archivo)
- `scripts/**` de instrumentos propios de A (perf, censos)
- Worktree: `../e-petplace-s99-a` sobre rama `pista/s99-a` · Metro NO se usa
  desde acá (C tiene :8081, D :8082; si A necesita Metro, puerto 8099).

---

## 🔴 ROJOS

*(nada — cero roturas; el fixture D-791 y el E2E del rango dieron verde a la
primera salvo un código de catálogo adivinado, corregido leyendo la fuente)*

## 🟡 PARA EL FOUNDER

> ### ✅ HOJA ÚNICA RESUELTA — ADJUDICACIONES DE MESA, 15-ago-2026
> **Los 11 ítems adjudicados; cero abiertos salvo el aparato (acción del
> founder, en curso).** El verbatim con punteros:
> `docs/relevamientos/2026-08-15-s99-ADJUDICACIONES-DE-MESA.md`.
> Veredictos sobre los de esta lista: **#1 dual → HERMANAS** (lápidas
> puestas; D arranca L4) · **#2 alta → aceptar al primer ingreso** (L2 de A)
> · **#3 ATENDER destacada → no se toca** (pregunta de gate) · **#4 ojo N11
> → gate en dispositivo, colapsa con el desbloqueo** · **#5 (=6 abajo) moto
> → candidata ② dos ruedas+manubrio**, casco muerto, al registry tras gate
> visual · **#6 (=7 abajo) huella → SIN huella, razón angosta de A** · más
> los cuatro que no estaban acá: IA tanda ① `extract-documento` a L2 con el
> stock partido (XLSX ya, PDF post-15-sep) · espejo dice propuesto/publicada
> (contrato L5b) · repartidor sin `user_id` cura POR CONSTRUCCIÓN en L2
> (`despachar_pedido` exige cuenta reclamada) · bezier 11 piezas = **D-825**
> solo-baja · N16.2 queda candidata. **Nace L-235** (la ley del instrumento
> que respondió sobre otra cosa) — las cuatro pistas la llevan en su
> arranque. *Los ítems de abajo se conservan como registro de cómo se
> sirvieron; ya no esperan firma.*
>
> ### 🟢 EL LOOP QUEDA EN CERO (palabra de mesa, 15-ago — tercera tanda)
> **0 🔴 · 0 🟡 · cero pendientes entre pistas · cero decisiones esperando
> al founder salvo su ojo en los gates.** La reconciliación de R44 CERRÓ con
> palabra: **las dos que cumplen NO entran — baseline 9 es EL número, no una
> desviación tolerada** (*gana la fuente sobre la directiva; L-236 con sello
> de mesa encima sería su peor forma: un lint que miente con autoridad*).
> Las cuatro pistas liberadas a construir: **C** su cola de L1 tras C4 (el
> aparato abierto) · **B** el gate del moto y la háptica ya no esperan nada
> · **D** L1 cierra con los cuartos de C, L4 con el dual firmado y su
> prioridad de deseable encima · **A** D-791 lado C, el lector del track, y
> D-827 a demanda. **El siguiente gate del founder SE JUNTA, no se reparte:
> `2026-08-15-s99-HOJA-DE-GATE-1.md`** — cuatro preguntas en una pasada,
> dispara cuando C cierre C4 y entregue los dos cuartos.

1. **EL CHOQUE DEL DUAL (relevado por D, consolidado acá): dos firmas de la
   misma semana se contradicen.** `LA_CASA_DEL_PRESTADOR` §3 (13/14-ago) firma
   *«HOY es un feed cronológico: citas, llegadas y despachos CONVIVEN
   ordenados por hora»* — y está CONSTRUIDO (`lineaItems`, S97-D, con su
   argumento en el código. `PLAN_S99` L4 (14-ago, posterior) firma *ventanas
   HERMANAS con puertas espejadas* («Tus pedidos de hoy →» / «← Tus citas»).
   Las dos juntas cuentan el mismo pedido dos veces a un toque de distancia —
   el defecto exacto que S97-D curó. **Opciones:** (a) la línea del HOY queda
   con citas solas y los pedidos viven en su ventana hermana (la letra §3 se
   enmienda por puntero) · (b) el feed fusionado queda y el dual se lee como
   «la ventana de pedidos es el DETALLE», sin puerta espejada desde citas.
   **Voto de A: (a)** — es la firma más nueva, y coincide con el voto de D.
   *Mientras tanto D avanza con D-820 (no depende de esta firma).*
2. **La identidad del alta del repartidor** (propuesta completa en
   `2026-08-15-s99a-L0-CENSOS.md` §A2): el vendedor registra el CORREO del
   repartidor; la persona se crea la cuenta sola (`registro.tsx`, signup
   abierto medido) y el motor la encuentra. **Opciones:** (a) el trigger ata
   `user_id` solo al aparecer la cuenta con ese correo · (b) ata como
   PENDIENTE y el repartidor ACEPTA al primer ingreso (un toque). **Voto de
   A: (b)** — un correo mal tipeado no ata a nadie con (b); con (a) ata a un
   desconocido sin que nadie lo sepa. Jamás una cuenta paralela — la regla
   queda intacta en las dos.

3. **L1 · la tab `ATENDER` destacada con UNA sola baldosa (vendedor puro)** —
   formulada por B vía D, y B ya la miró con su guarda: NO es rojo (§2.1bis
   dice explícito que está bien que sea una sola cosa). La pregunta del gate:
   *¿la destacada se lee como «esto es lo importante» o como «esto está
   vacío»?* Palanca si chirría: condicionar `destacada` por población, sin
   tocar `BarraTabs`. **Es decisión de gate, no cura — se mira en dispositivo
   en el cierre de L1.**
4. **ACCIÓN, no decisión: el gate en dispositivo está BLOQUEADO para las
   cuatro pistas** — C midió que el teléfono no se desbloquea: la entrada del
   keychain que el acta S98 documenta como PIN guarda la clave ALFABÉTICA y
   la pantalla pide PIN numérico. **Lo destraba solo el founder** (desbloquear
   el aparato o corregir la entrada del keychain). Hasta entonces los gates
   de lote corren en RN-web + instrumento, y el dispositivo queda debiendo.
   **✅ RESUELTO (adjudicación 15-ago, segunda tanda): EL FOUNDER ABRIÓ EL
   APARATO — los gates en dispositivo quedan destrabados para las cuatro.**

5. **L3-de-B (campos N11) · LA PREGUNTA DEL OJO, y es la única de N11 que
   ningún instrumento puede firmar:** N11 landa en **121 campos a la vez**
   (96 `Campo` en 35 pantallas del prestador + 25 del cliente, cero
   `TextInput` crudo — cambiar la pieza ES la migración). Los números cumplen
   (contorno 3.34–3.40, R43 vigilando el piso); lo que nadie puede medir:
   *¿un formulario de seis campos con contorno a 3:1 RESPIRA, o se ve
   enrejado?* **Va junto al gate de la primera pasada de B** («¿artesano u
   obrero?») — es EL ojo del founder, no una decisión con opciones.

6. **L6 · EL GLIFO `moto` — gate POR ÍCONO (§6b completo, hoja de contacto
   de B en `docs/laminas/2026-08-15-s99b-HOJA-DE-CONTACTO-moto.md`,
   `04b1c48d`).** Tres candidatas con riesgo declarado: **A** la moto de
   perfil (inequívoca; 4 elementos — llega al techo de la banda, declarado) ·
   **B** el casco (la más limpia a 21px; riesgo terminal: si la mesa lo lee
   como figura humana, muere ahí) · **C** dos ruedas + manubrio (**voto de B**;
   riesgo barato: puede leerse bicicleta, y el pin comunica «alguien viene en
   un vehículo» — sobrevive si mañana entra otro vehículo). Montaje 21/44px
   claro/oscuro junto a 5 vecinos reales. **Se firma mirando, no leyendo.**
7. **L6 · LA HUELLA EN EL PAR DE PINES — decisión de letra (§6b.6, servida
   por B con las dos salidas).** Las tres candidatas van SIN huella, y la
   razón NO es la de los glifos de control (`moto` es objeto del mundo — por
   Ley 12 le tocaría): **en el par de pines la huella ES el discriminador**
   (paseo = la cara de la mascota · entrega = quien trae el pedido; ponerla
   en las dos anula la única diferencia que el pin existe para mostrar).
   **Opciones:** ① la categoría «glifo de control» se ENSANCHA a «donde la
   huella no significa» · **② nace una razón nueva y ANGOSTA** — *la huella
   no se pone donde es el discriminador de un par* — y «control» queda
   intacta. **Voto de B: ② · voto de A: ② también** — ensanchar «control»
   para alojar un vehículo le saca a la palabra lo que la hacía útil, y la
   regla angosta dice la verdad de por qué; es la misma forma con la que la
   casa prefirió `no_aplica` como cuarto estado antes que estirar uno vivo.

## 📋 PARA EL ACTA DEL CIERRE (registro, no decisión)

- **EL PATRÓN DE LA CURA DE D (16-ago), ratificado y depositado:** no
  eligió otra animación — **le sacó a la NAVEGACIÓN el trabajo de
  transportar el día** (`@/lib/dia-en-vista`: un contexto que envuelve a
  las dos ventanas; sin nada que transportar, la vuelta es `back()` REAL
  y la dirección se deriva del gesto). **El remate: «UN día en DOS
  ventanas» pasó de afirmación sobre el comportamiento a PROPIEDAD DEL
  TIPO — las curas cierran por construcción, jamás por margen.** Y su
  re-apuntado del guard queda como CRITERIO: leía `?dia=` y con el día
  compartido ya no hay param — *medir el MECANISMO habría dado rojo sobre
  una cura correcta*; ahora lee el día que la pantalla MUESTRA. **Se mide
  el hecho, no la maquinaria que lo produce.** De este lote nace L-243.
- **La nota que evita una rotura futura (④ del dual):** «entregado» se lee
  del estado NARRATIVO, no del interno — *un contador que mira el estado
  interno se rompe la próxima vez que el motor gane un escalón.* La
  frontera 30→7 defendida desde el consumidor.
- **✒️ GATE 2, RESULTADO (16-ago noche):** ①② VERDES verificados contra
  la base — WhatsApp E.164 ENTERO con su `+593` (la prueba de que el
  selector SE MUDÓ bien, L-240 en positivo) · documento, foto y vehículo
  con placa de moto · la cabecera-espejo actualizándose MIENTRAS se
  escribe (N12.4 y N12.1 con su discriminador en vivo). ③ = el motor sin
  puerta (abajo).
- **🔴 LA CLASE, YA CON TRES MUESTRAS EN UNA SESIÓN: MOTOR DESPLEGADO SIN
  LLAMADORES.** ① `extract-documento` (desplegada, cero llamadas — la
  cazó el censo de C en L0) · ② mis dos wrappers sin re-export en el
  barrel (la cazó C midiendo compilación) · ③ `aceptar_vinculo_repartidor`
  + `mis_vinculos_repartidor_pendientes` con CERO consumidores — la cazó
  un repartidor REAL en el callejón cortés del Gate 2. **Un motor sin
  puerta pasa TODOS los verdes — typecheck, lint, cinturón — porque nada
  de eso mide si alguien lo llama.** (El pariente de la casa: S77 la
  nombró tres veces como «motor sin puerta» — la clase es vieja; lo nuevo
  es que en S99 ya son tres en UNA sesión y una la encontró un usuario
  real.)
- **LA DÉCIMA MUESTRA ES MÍA, cazada por mí antes de escribirla (censo
  L5b):** casi deposito «el precio por kilo no tiene dato» habiendo mirado
  `productos` — estaba en `producto_variantes`, la tabla de al lado.
  **L-238 en carne propia: la ausencia vale lo que vale el universo donde
  se buscó** — y la diferencia entre depositarla y cazarla fue re-medir
  ANTES de escribir.
- **La muestra MÁS FINA del lote de C (16-ago, cazada REPITIENDO y no
  razonando):** el primer frame de cada ráfaga pesaba 2 MB y su umbral lo
  llamó «contenido» — **no era la app: era la pantalla ANTERIOR todavía
  visible.** El instrumento midió bien un objeto que no era el que
  preguntaba.
- **Muestras DÉCIMA y UNDÉCIMA de L-235 (lote de D, sin ficha propia por
  orden de mesa):** el Metro corriendo desde ANTES de mi merge sirviendo
  un grafo sin `obtenerContextoArranque` — *la trampa del puerto tiene una
  hermana: el Metro propio viejo es tan viejo como el de otra pista* — y
  el techo mayusculizando por CSS mientras la aguja buscaba minúsculas.
  En los dos, la sonda mostró la pantalla sana ANTES de tocar una línea.
- **A LA COLA DEL FOUNDER (freno declarado de D):** que la vuelta SE
  SIENTA como vuelta es CUALIDAD del movimiento = gate de aparato (L-243)
  — va en la próxima pasada con lo pendiente.
- **🔴 LA NOVENA MUESTRA DE LA FAMILIA — Y CON FORMA PROPIA: EL DATO DE
  MESA NO SE VERIFICA POR VENIR DE LA MESA (16-ago, el atrape es de B).**
  La mesa aportó *«el anillo separa una silueta oscura del fondo sobre
  calle gris o parque verde»* — **y era FALSO**: B lo midió en vez de
  aceptarlo (silueta tinta 11.54/11.19/9.85 contra asfalto, parque y
  agua — el que desaparece es el ANILLO BLANCO, 1.44/1.48/1.68) — **y el
  cuarto tono donde el anillo sí trabajaría, el mapa oscuro, NO EXISTE**
  (cero `customMapStyle`: era una hipótesis de la propia hoja de contacto
  que nunca estuvo en el mapa). *Las ocho muestras anteriores fueron
  instrumentos; ésta fue LA MESA afirmando sobre un objeto que no había
  medido, y una pista que fue a mirarlo.* **Corolario operativo: cuando
  la mesa aporta un dato técnico, viaja como HIPÓTESIS A VERIFICAR, jamás
  como premisa** — y esta pista lo relayó como «dato técnico de mesa» en
  su nota a B: el corolario me alcanza a mí también. El resultado quedó
  como regla con nombre: **L-242**.
- **L-241 EJECUTADA EL MISMO DÍA QUE SE FIRMÓ (B):** nace `LienzoMapa`
  (mapa real para gatear marcas de mapa, con placeholder web que DECLARA
  que no sirve para juzgar) · murieron las dos hojas de contacto del moto
  (263 líneas, con lápida) · **el dibujo ganador NO se copió a la
  galería: vive en la pieza y la galería lo consume** — *una copia en la
  herramienta de verificación es la primera divergencia, y la herramienta
  que diverge miente con autoridad.* Y su decisión de NO meter la moto al
  registry de `Icono` quedó ratificada: variante de `PinEnMapa` (§6ter —
  el mapa no es interfaz, es mundo; en `Icono` quedaría bajo R30
  invitando a montarla a 21 px junto a una huella). **La ley recién
  firmada gobernando su primer caso, sin que nadie se lo recordara.**
- **LA SEGUNDA MUESTRA DE LA CAUSA QUE FALLÓ EL PRIMER ÍCONO, ahora con
  ley (L-241):** el founder pidió ver el pin EN EL MAPA, no en la lámina —
  *la pieza se gatea donde vive* — y el costo de no hacerlo quedó medido:
  la galería acumula lo que nunca se usó (D-833). De este dictado nace
  L-241 (*cuando montar cuesta menos que ensayar, se monta*), con su
  acotación explícita: no deroga el toque 1 de B.
- **EL PLAN DE MEDICIÓN DE #0a: RATIFICADO por mesa tal como se sirvió**
  (P1 video + P2 d0 en paralelo + P3 RTT, una ventana de ~10 min; P4 solo
  si no discriminan) — con la advertencia contra el candidato obvio AL
  FRENTE: el prólogo ya está curado en este OTA; si la franja persiste,
  curar más olas sería operar al paciente equivocado.
- **LA RATIFICACIÓN A B (mesa 16-ago), con sus dos registros:** el slot de
  pie fijo de `Hoja` y el `tituloEntero` de `Celda` quedan como los
  escribió — el filete que solo aparece con desborde MEDIDO (*un filete
  permanente separaría de nada la mitad de las veces*) y su decisión de no
  poner techo de líneas con su razón (*truncar en dos es la misma falta
  con otro número*). **Su literal al acta:** *«entre un defecto visible y
  uno silencioso, esta casa elige el visible — el silencioso no se
  descubre nunca.»* Y su corrección propia, **octava muestra de la
  familia:** su primer conteo de 239 usos era FALSO porque `<Celda` se
  traga `<CeldaNavegacion` — el 162 de C era el bueno.
- **LA MUESTRA DE B (16-ago): «YA EN MAIN» ERA FALSO CON LOS SEIS
  SEMÁFOROS EN VERDE.** Su handoff declaró «ya en origin/main» — `rev-list
  origin/main..pista/s99-b` daba **6 commits**, incluido el handoff mismo.
  La causa: **verificó por CONTENIDO archivos que YA estaban mergeados de
  una tanda anterior, no sus commits nuevos** — un verde por la razón
  equivocada, y L-217 cobrándose sobre quien la citaba. **El
  discriminador es `rev-list` contra `origin/main`, jamás `cat-file`
  sobre archivos elegidos a mano** (verificar contenido de lo que ya
  estaba da verde sin probar nada). *Con el atenuante que vale: B se
  cazó SOLA (`f630e847`, «corrijo mi propio handoff») antes de que la
  mesa la corrigiera — y su corrección queda registrada COMO ELLA LA
  ESCRIBIÓ, porque es la parte que sirve: **caducó el número, no el modo
  de falla — que un handoff propio afirme un estado que la medición
  desmiente.*** Sin el merge, **la dirección de diseño ENTERA de
  S99 estaba fuera del canon** — mergeada 16-ago con los seis verificados
  por rev-list (post-merge: 0 commits afuera), y con la cura de
  combinación de `sinVer` declarada aparte.
- **LA SEXTA MUESTRA DE L-235, Y LA MÁS LIMPIA (el guard de D, 15-ago):**
  su primera corrida falló porque al empujar la ventana hermana **el HOY
  queda montado debajo y el locator tomó el nodo INVISIBLE de la OTRA
  pantalla** — la sonda mostró que la ventana renderizaba entera y bien:
  **el rojo era del instrumento.** Y su otro atrape vale igual: razonó que
  la puerta debía esperar mi RPC, **fue a medirlo y era falso — la medición
  no confirmó el plan, lo cambió.**
- **`RESET ROLE` BAJO `db push` NO VUELVE AL ROL DE LA MIGRACIÓN — vuelve
  al rol de LOGIN del tool** (medido dos veces en un día: la aserción
  post-RESET y el propio REGISTRO de la migración rebotaron 42501). Es la
  clase L-235 en el motor: el RESET funcionó, sobre OTRO rol. **El patrón
  —capturar `current_user` y restaurar EXPLÍCITO— quedó en el CANON**
  (skill `epetplace-db`), no solo en el archivo. Y la yapa: el mismo SQL
  pasa limpio por `db query` — un fixture verde por una vía no prueba la
  otra.
- **EL GATE FALLÓ UN ÍCONO QUE HABÍA PASADO TODO EL PROCESO INTERNO**
  (§6b completo, tres candidatas, riesgos declarados, voto de mesa
  acompañando). Lo que ninguna instancia vio: **el ícono se estaba
  juzgando en el LUGAR equivocado** — como glifo de interfaz sobre lienzo,
  cuando su destino es un pin sobre mapa. **Es L-235 en su forma de
  diseño: el instrumento respondió sobre otro objeto — la lámina, no el
  mapa.** El aprendizaje es del BRIEF, no del dibujo.
- **C EVITÓ DOS FALSOS POSITIVOS verificando contra la base y contra la
  fuente en vez de contra la captura** (el pedido que parecía entregado
  era `en_reparto`, no terminal; la escalera que se lee como entregado
  llena `hecho` y `actual` igual A PROPÓSITO). **Es L-235 aplicada sin que
  nadie se lo pidiera.**
- **EL GATE CONTESTÓ OTRA COSA QUE LA PREGUNTADA (N11) — y para eso
  existe:** se le preguntó por el contorno y contestó por la ORGANIZACIÓN.
  La clase del barrido de cariño (S98) escaló de pieza a PANTALLA — tres
  muestras: la foto · las dos formas del pedido · la configuración entera.
- **EL GATE DEL FOUNDER ENCONTRÓ EN TRES MINUTOS UN HUECO DE ARQUITECTURA
  QUE NINGUNA DE LAS CUATRO PISTAS VIO EN TODA LA SESIÓN** — la puerta
  asimétrica (D-829: el groomer puede solicitar ventas, el vendedor no
  puede solicitar servicios). **Es la prueba de para qué existe el gate: su
  ojo ve lo que NO está, y ningún instrumento mide una ausencia que nadie
  declaró — L-238 desde el otro lado.** Las cuatro pistas midieron todo lo
  que existía; lo que faltaba no tenía dónde dar rojo.

- **Muere una firma de S81:** el `sinCaja = true` por default de `Campo`
  (*«la affordance la da el RELLENO»*) queda **DEROGADO por N11** (firma más
  nueva, con su razón: *se contornea lo que se fija — un campo vacío todavía
  no existe*). Costo de la derogación: **cero consumidores** (los `sinCaja`
  del árbol eran de `Boton`, otra prop de otra pieza). Marcado por B en la
  pieza y en su parte; la letra S81 no vivía en ningún doc maestro — vivía en
  el default de la pieza, y por eso la marca vive AHÍ. **19.8 NO muere: N11
  es 19.8 bien aplicada** (las dos letras la citaban; la lectura de S81 era
  la invertida).
- **Candidata de lección (sin firma), con TRES muestras de la misma jornada:**
  *el instrumento respondió, y respondió sobre OTRA COSA* — D midió contra un
  Metro huérfano que servía la app vieja (D-769 enmendada) · B midió el
  contraste de una RAMA QUE NO CORRE (el default elegía la otra) · A emparejó
  la cuenta equivocada y el E2E dio 0 filas creíbles. *Las tres salidas eran
  perfectamente creíbles y describían otro objeto.* La forma de la cura fue
  la misma tres veces: verificar QUIÉN/QUÉ responde antes de leer la
  respuesta.
- **L-235 sumó su QUINTA muestra el mismo día, y es la más cara (C, C4):**
  la premisa de la pasada decía «el aparato con el lote S99» y **el aparato
  corría el OTA de S98** (`01a00373` — sin N11, sin los 121 campos). C lo
  cazó aplicando la ley antes de creerle al número: leyó el OTA EN PANTALLA
  antes de medir. *Archivar ese baseline como post-N11 habría anclado toda
  comparación futura a una premisa falsa* — que es justo lo que un baseline
  existe para impedir. Y su instrumento se corrigió a sí mismo además: el
  primer «techo» de 6,9–7,7 s **medía su propio sondeo** (`uiautomator dump`
  a 3,4 s por tiro) — *un instrumento cuyo costo domina la magnitud que mide
  no está midiendo*. El protocolo de keychain que habría evitado el freno
  del aparato quedó escrito en su parte.
- **Cruce de verificación doble del guard de la escalera:** C lo probó en
  rojo por su cuenta y su rojo MAL HECHO (`picking_APAGADO`) destapó el
  límite del parser (curado, `3df95f1e`) — *un rojo torpe puede auditar al
  auditor* (literal de C, su parte `00766524`).
- **Candidata de lección (sin firma; origen B, tras el rescate de su rama —
  refina L-217 con un mecanismo de UN comando):** la tanda N11 entera de B
  quedó fuera del canon aunque su cierre decía *«todo en origin, verificado
  por contenido»* — y ERA cierto contra `origin/pista/s99-b`. Su literal:
  *«un push a la rama de pista deja el objeto en origin, deja el `cat-file`
  verde y deja el árbol en cero — los tres semáforos que uso para declarar
  cerrado prenden igual con la mitad del camino hecho. No falló que nadie
  mirara: falló que mirar salía VERDE.»* ⇒ el mecanismo propuesto: **al
  cerrar tanda, `git merge-base --is-ancestor HEAD origin/main` ANTES de
  decir cerrado — si da rojo, no está cerrado: está pusheado.** B lo aplica
  desde ya; candidata a enmienda de la regla 84 (junta a D-707, que pedía la
  otra mitad: `git branch -a --no-merged main` al cierre de sesión — son la
  misma cura vista desde las dos puntas).

- **La queja de estética del Acto II GANÓ NÚMERO (censo de B, verificado por
  C en su territorio):** S98 decía *«el alta de repartidor reinventó lo que la
  carga de foto ya hacía bien»* — queja sin número. El censo de `HojaCaptura`
  midió que *el picker nunca se duplicó: lo duplicado era la PUERTA*, y **el
  cerrojo anti-doble-tap vive en 2 de los 10 sitios** — ocho puertas sin
  cerrojo en altas donde la foto es obligatoria (una es `FotoDelRepartidor`,
  C la toma como CURA en L1, no como swap). Literal de B que lo explica: *«un
  sitio que copia la anatomía copia lo que se VE; el cerrojo no se ve.»*
  Literal de C que lo completa: *«su censo encontró en mi territorio un
  defecto que dos sesiones mías miraron de frente y llamaron ‹diseño sin
  cariño›.»* ⇒ el barrido de cariño del Acto II tiene su primer caso donde
  la estética era el síntoma y el defecto era de función.

- **La variante más cara de «respondió sobre otra cosa» (origen B, 3er error
  declarado de su jornada):** el net de R44 dio 3, después 4, después 6 —
  regex que no cubría la vocal sin tilde, patrón que no toleraba una palabra
  en el medio. Su literal: *«un baseline mal medido no protege de menos,
  protege de MÁS — deja pasar como preexistente lo que nunca contó»*. Con
  baseline 3, las otras tres voces quedaban bendecidas para siempre con el
  lint en verde. Va con la candidata de la tríada: acá el instrumento era
  propio y la respuesta era UN PERMISO.
- **N16.2 («el spinner muere») queda CANDIDATA declarada, no regla escrita**
  (decisión de B con su razón, registrada para que no pase como obvia): está
  medida en 0, pero su net honesto exigiría exentar `packages/ui` (3
  `ActivityIndicator` legítimos de controles ocupados) y esa exención se
  decidiría sola el día que una pieza nueva necesite uno. *Mejor declarada
  con net propuesto que escrita a medias.* Si la mesa la quiere, es de una
  tanda.
- **R44 nace con DUEÑO DECLARADO (C):** 6 voces genéricas en `apps/prestador`
  prohibidas por N12.4, baseline solo-baja; la cura es decir QUÉ falta y
  bajar el baseline en el mismo commit al tocar cada código. El precedente
  que C va a querer: S75 ya había visto el rebote *«Revisá los datos»* de
  frente, sin ley con qué frenarlo — ahora la ley (N12.4) y el guard (R44)
  existen.
- **El cruce de vuelta de C sobre R44, y la mitad que me toca (COMPROMISO DE
  A, atado al L1 de C):** C verificó el 6 de B contra el objeto (correcto,
  las tres claves con consumidor real) Y midió que la MISMA voz vive 5 veces
  más en `packages/api`, hard-codeada en español (evidencia nueva de D-539) y
  alcanzable por los 34 sitios que muestran `r.mensaje` directo. **Y la cura
  no es copy:** `datos_invalidos` es el fallback de CUALQUIER CHECK
  (`23514` → un solo código). Camino acordado: **① Ley 23 primero** (C valida
  antes de mandar; la genérica queda como último recurso honesto) · **② donde
  la validación no pueda saberlo (constraints de servidor), A agrega códigos
  tipados POR CONSTRAINT a demanda, puerta por puerta cuando el L1 de C
  choque con cada una** — jamás un mapeo genérico de 23514 «mejor escrito».
  El literal del cruce, para el acta: *«su regla encontró en mi territorio
  una deuda con dueño; mi medición encontró que su regla mira la mitad del
  problema. Ninguna de las dos sola alcanzaba.»*
  **CIERRE DEL ARCO (B, `942bec02`, en main):** R44 pasó a mirar TODA la
  casa, y el cinco de C eran TRES (dos de las cinco CUMPLEN: nombran la
  falla antes) — el net ganó su discriminador (*«el defecto no es contener
  ‹revisá› — es que NADA diga qué falló ANTES»*, calibrado 16/16 con dos
  legítimas a propósito). **Baseline 9 = 6 de C (curables escribiendo) + 3
  de A (BLOQUEADAS: son exactamente el compromiso de códigos por constraint
  —`veterinaria-presupuesto:97` · `veterinaria-nota-clinica:194` ·
  `handshake-mostrador:72`— y bajan solas cuando cada puerta gane su
  código).** *Un baseline que mezcla lo curable con lo bloqueado miente
  sobre qué está pendiente.*

## ⚪ SUPUESTOS

1. **El depósito de los dos docs de mesa incluyó marcar la letra vieja del
   mapa v2 EN `LETRA_PANEL_VENDEDOR_S96`** (3 lugares, tachado + puntero a
   `PLAN_S99.md` §4.1). El plan decía *«la letra vieja se marca con puntero
   acá»* — leí «acá» como el plan Y apliqué además el precedente de la casa
   (*las marcas viven en cada lugar afectado* — magenta S83, plata S88).
   **Reversible:** revertir las 3 ediciones es un `git revert` de un commit
   de docs; costo de deshacer ≈ 0.
2. **EL DÍA DEL PEDIDO = `entrega_fecha_objetivo`** (decisión (a) del pedido
   de D, tomada por A): es la columna que `cupo_reparto_del_dia` YA consume —
   una sola verdad del día; la promesa es la ventana horaria adentro de ese
   día. **Reversible:** la vista solo GANÓ una columna (reversa escrita); el
   lector nuevo es el único consumidor.
3. **LOS PEDIDOS SIN DÍA VIAJAN SIEMPRE si están vivos** (decisión (b) del
   pedido de D): `listarPedidosDelVendedorEnRango` los devuelve en `sinFecha`
   en el mismo viaje — precedente D-439/S71 (*lo sin-fecha preside, jamás
   desaparece por un `.gte`*). Terminales sin día no viajan (historia sin
   día = panel). **Reversible:** es forma del retorno del wrapper nuevo,
   cero consumidor todavía.
4. **Push intermedio a `main`** (docs de mesa + D-791 + el lector del rango):
   las letras S99 no existían en main y TRES pistas las necesitan leer
   (freno L-142 relevado por D); además la migración ya está aplicada en el
   remoto — main sin ella dejaría disco≠remoto para las otras pistas.
   **Reversible:** commits atómicos por pathspec, revert limpio.
5. **Corrección de dato a la matriz de cuentas (medida, no aplicada al doc):**
   `guillo381+duenodes@gmail.com` (uid `51c4b19f`) es owner de
   **`054f23aa` «DESPENSA DE PRUEBAS S97 - NO REAL»**, no de `eec12ef3`
   «Despensa de Pruebas (borrable)» (owner `da83d6d8`). El E2E del rango se
   cayó una vez por ese emparejamiento — quien siembre el espécimen del dual
   que no lo herede. La matriz es de C/S97 — se avisa, no se edita.

## 📊 CENSO SERVIDO — EL HOY SIN PEDIDOS (orden de mesa 15-ago; NO se construyó)

**Qué muestra HOY el vendedor con cero pedidos (leído del objeto):**
`EstadoVacio` con *«Todavía no hay pedidos / Cuando entre uno va a aparecer
acá…»* + la línea de cupo (*«X de Y entregas hoy»* / *«Sin reparto
confirmado para hoy»*) + las celdas Configuración·Stock·Mostrador·Entregas.
**Consuela en pasivo; no muestra qué SOSTIENE el día** — exactamente el
hueco que la dirección de mesa nombra (el vacío del día es ausencia de
TRABAJO, no de NEGOCIO).

**Los tres candidatos, contra sus lectores (TODOS ya tienen lector):**
1. **¿Está abierto / recibiendo, con qué horario?** — `listarTurnosEntrega`
   + `listarRecursosReparto` + `cupoRepartoDelDia` EXISTEN (la línea de
   cupo ya se pinta en el HOY). Falta solo la COMPOSICIÓN.
2. **¿En condiciones de recibir?** — `listarSkusDelVendedor` EXISTE
   (publicado · stock · foto): el «12 productos no aparecen en búsqueda
   porque no tienen foto» de N18 es DERIVABLE del mismo lector, sin viaje
   nuevo.
3. **¿Cómo viene contra lo normal?** («ayer a esta hora llevabas 3») —
   derivable EN MEMORIA de `listarPedidosDelVendedorEnRango` (el rango ya
   trae hoy−3): cero lector nuevo, cero ola nueva.

**🔴 El discriminador «alguna vez vendió» NO EXISTE hoy** (ni columna ni
lector — universo: wrappers de despensa + pedidos). **Propuesta con costo:**
un booleano `ha_vendido` (EXISTS pedido ENTREGADO de la cuenta) que viaja
GRATIS en `obtener_contexto_arranque` — el viaje ya existe, es una línea de
SQL cuando la letra del vacío se firme. **Costo total de la propuesta: CERO
lectores nuevos, CERO olas nuevas — es composición de pantalla (dueña C,
con letra de mesa primero) + 1 booleano mío.** Prohibiciones que rigen:
jamás consuelo/checklist/gamificación (LOYALTY §3 · §7.5).

## 📊 EL HOY DE duenotodo — HUECO DECLARADO (a D, antes de que el lote #0 cierre)

El plan resolvió el HOY de duenodes (pedidos) y duenovet (citas); **qué ve
duenotodo AL ABRIR tiene media respuesta** — el reparto «dual → jornada»
del contrato de D NO la contesta. Tres caminos servidos por mesa: (a)
aterriza en citas · (b) aterriza donde hay trabajo · (c) **un techo común
con las dos y él elige** («3 citas · 7 pedidos»). **Voto de mesa: (c) — lo
único que no le miente sobre que tiene dos negocios. ES FIRMA DEL FOUNDER,
no adjudicación.** ⇒ **mi composición deja el brazo dual PARAMETRIZABLE**
(expone las dos capacidades y NO decide el aterrizaje) hasta esa firma; los
brazos prestador-solo y vendedor-puro no dependen de ella y avanzan.

## ✒️ FIRMAS DEL GATE DEL FOUNDER (15-ago — cierran dos 🟡 y abren un frente)

- **ATENDER destacada con UNA baldosa — 🟡 CERRADO, queda como está en v1.**
  Firma verbatim: *«sí se siente vacía, pero para esta versión podemos
  dejarla, no le veo importancia.»* La razón queda escrita: no es olvido.
- **El ojo de N11 — 🟡 CERRADO: LA LEY PASA, LA PIEZA QUEDA.** El founder
  contestó OTRA cosa que la preguntada (para eso existe el gate), verbatim:
  *«La configuración del negocio para el vendedor de despensa hoy se ve muy
  diferente a cómo se estructura el diseño de cómo se configuran los
  servicios. Deberíamos tener una coherencia visual. Diez campos pueden ser
  muy complejos o relativamente fáciles si organizamos la pantalla bien.
  Hoy no está bien organizada.»* ⇒ el contorno NO era el problema; lo que
  falla es la ORGANIZACIÓN y la INCOHERENCIA entre dos configuraciones de
  la misma casa. **NO es una reversión de N11** — se abre un frente de
  organización con dueño B (abajo). Es la TERCERA muestra de la clase que
  el acta de S98 nombró como el arranque del barrido de cariño (*«la misma
  operación se ve distinta en dos lugares y se lee como dos productos»*):
  el componente de foto → las dos formas del pedido en la línea → ahora
  una PANTALLA ENTERA. **La clase escaló de pieza a pantalla.**
- **La spec de L2** (WhatsApp único obligatorio con indicativo ·
  HojaCaptura · ficha completa · máx 2 vehículos POR CONSTRUCCIÓN) quedó
  depositada en `PLAN_S99` §4.4 — la pantalla es de C, la pieza de B.
- **La cola del pedido** (pago = puerta · DOS bandas · FIFO por
  `pagos_intentos.cerrado_en` · reordenar con techo · D-830 · D-831) en
  `PLAN_S99` §4.5.

## 📢 PARA LAS CUATRO PISTAS — RENDIMIENTO CONGELADO (firma 16-ago noche)

**NADIE TOCA RENDIMIENTO hasta la sesión propia.** La investigación de
tiempos está APLAZADA por firma (*«aplacemos la investigación,
concentrémonos en cerrar todo el flujo de despensa»*) y vive CONGELADA en
**D-834** con todo lo medido — la sesión futura no repite nada. **#0b
murió como lote** (disuelto en la ficha). **La cola quedó limpia y con una
sola dirección: L3 → L5a → L5b — cerrar el flujo de despensa entero.**

## 📢 PARA LAS CUATRO PISTAS — LA ENMIENDA DEL ARTESANO (15-ago, firma del founder; LETRA, no arenga)

**`DIRECCION_DISENO_S99` ganó el §0.1** con la firma verbatim del founder y
**las cinco varas en orden — FUNCIONA · SEGURO · EFICIENTE · ELEGANTE ·
ESPECTACULAR — ninguna se salta y la primera no basta.** Lo exigible: **el
founder AUTORIZA ir más lento** (entregar tarde algo bien hecho CUMPLE;
entregar a tiempo algo que «hace por hacer» NO) · el toque 1 de B es
**PRECONDICIÓN** de toda pantalla nueva de L2 y L5b · «funciona y no tiene
intención de forma» es **rojo nombrable** de B · y la pregunta del gate
queda fija: **«¿artesano, u obrero?»**. Se lee EN EL ARRANQUE de cada
pista, de acá en adelante — vive en el doc de dirección, no en esta nota.

## 📬 PARA B — POR REPO (16-ago, muy tarde: confirmación + ratificaciones de tu tanda)

1. **✅ TU LECTURA DEL LITERAL ES CORRECTA — no pierdas la tanda:** «el HOY
   vacío» ES el estado vacío del HOY DEL VENDEDOR, hermano de tu §5bis.
   **La letra está FIRMADA y viaja entera en la cola #7 de mi nota a C**
   (ausencia de TRABAJO no de NEGOCIO · muere «Todavía no hay pedidos…» ·
   lo que SOSTIENE el día en tres candidatos con lector · DOS VOCES con
   `haVendido` que YA viaja en el contexto · ningún contador que no llegue
   a cero). **Y la unión es de TRES, no de dos:** tu §5bis + el HOY vacío
   + el dictado de la configuración-que-muestra-el-negocio — *el mismo
   vendedor el mismo día: los vive los tres en cinco minutos.* La
   gramática nace UNA vez.
2. **Tu ESCALA DE BOTONES: RATIFICADA tal como la serviste** (un sólido
   por superficie · alternativa real solo si la otra rama también es
   consecuencia · lo demás baja a label · `md` es EL botón y `sm` es
   DENSIDAD jamás jerarquía · `lg` gana su caso escrito o muere — 3 usos
   en 501 · `bloque` es del pie). **D-484 quedó PAGADA con tu
   resolución** (primario+secundario, jamás ghost) y **de tu caso nació
   L-244** (el default de una pieza es una decisión que nadie toma) con
   la auditoría hecha: `Boton` era LA única con default enfático.
3. **R47 NO se escribe ahora — tu razón es correcta y quedó registrada:**
   un contador ingenuo mediría de MENOS (el default no aparece en ningún
   grep de `variante=`) y bendeciría como preexistente lo que nunca contó
   — es R44 otra vez. Queda PEDIDA como tanda propia con su costo (parseo
   de JSX multilínea).
4. **Tus dos recetas adelantadas de L5b: RATIFICADAS, con dos decisiones
   que la mesa hizo suyas:** el $/kg EN MONO (*es un cálculo y ese
   registro tipográfico ES su valor — nadie lo pone, nosotros sí*) ·
   «para quién sirve» como TABLA jamás FilaDato (*como fila el diferencial
   deja de leerse como que la app conoce a la mascota*) · **el interruptor
   del espejo = `SelectorSegmentado` FIJO EN EL TECHO, jamás navegación**
   (*si navega, el vendedor sale de su trabajo y el espejo se visita una
   vez*) · sin marco de teléfono (*un mockup dice «simulación» y N17 pide
   lo contrario*). Y tu NO-adelantar de las otras tres (N20 · N18 ·
   grilla) ratificado: *adelantar lo que no depende del censo es ganar la
   carrera; adelantar lo que sí, es correrla dos veces* — mis
   cardinalidades te llegan con el arranque de esos frentes.

## 📬 PARA B — POR REPO (16-ago noche: DOS dictados del founder — el ② es una CLASE)

1. **🔴 LA ESCALA DE BOTONES (dictado verbatim):** *«los botones agregar
   vínculo, agregar foto, y en general todos esos botones que están como
   con gris con un color, no siento que se vea premium. Y el de agregar
   foto de documento, que no aparece botón — es lo que hemos descartado y
   pedido cambiar casi desde la primera sesión. Aparecen muchas veces:
   agregar vehículo, quitar. Es ese que tiene transparencia total y solo
   tiene un borde alrededor, que no aparece un botón. De ese ya hemos
   hablado muchas veces.»*
   **El diagnóstico de mesa, para que no lo trates como tres curas
   sueltas: la casa tiene ley para el botón que CIERRA la acción y NO
   tiene ley para el que la ACOMPAÑA.** Por eso cada pantalla se inventó
   el suyo, ninguno se ve premium, y el mismo defecto vuelve sesión tras
   sesión: no hay nada que los haga verse iguales. **Tu trabajo: censar
   TODOS los botones secundarios de la casa (agregar · quitar · adjuntar ·
   cambiar) y escribir LA ESCALA DE BOTONES** — cuántos registros hay, qué
   dice cada uno, y cuál muere. **El de borde transparente muere CON
   LÁPIDA** (el founder lo rechaza desde las primeras sesiones — 19.7 ya
   lo mató como acción de fila y sobrevivió en otra ropa); su reemplazo
   nace con la vara nueva (ELEGANTE · ESPECTACULAR), con referencia de
   industria concreta, **y por L-241 se verifica DONDE VIVE, no en
   lámina.**
2. **🔴 LA CONFIGURACIÓN DEBE MOSTRAR EL NEGOCIO (dictado verbatim):**
   *«en la pantalla de configuración, debería verse cuáles son los
   horarios, cómo atiendo, cuáles son los repartidores con las fotos, con
   una ficha bien bonita, la capacidad de reparto. Hay que agregarle
   cariño y hay que hacerlo lindo.»*
   **NO es estética, es PRODUCTO:** hoy son tres listas y un enlace — lo
   que vos misma mediste y nombraste (*«el vendedor parado frente a tres
   listas vacías»*). El founder pide que la pantalla MUESTRE EL ESTADO DEL
   NEGOCIO, no que dé acceso a formularios: los repartidores con su foto
   y su ficha, los horarios legibles, la capacidad de reparto a la vista.
   **Se une con tu §5bis y con el HOY vacío — son el MISMO vendedor el
   MISMO día: servilos JUNTOS con la gramática de configuración que ya
   tenés en cola. C construye DESPUÉS de tu receta.**

## 📬 PARA B — POR REPO (16-ago: LA MOTO FIRMADA + el anillo + el orden de la galería)

1. **✅ EL GLIFO `moto`: FIRMADO — LA CANDIDATA D (con caja). El gate por
   ícono está CERRADO.** Incorporala al registry: tu freno de `PinEnMapa`
   (*«cuando `moto` pase su gate por ícono, entra»*) tiene su condición
   CUMPLIDA — la variante entra.
2. **✅ RESUELTO POR TU MEDICIÓN (16-ago, mergeada): el anillo MUERE en la
   moto y SIGUE en la mascota — L-242 con tu número. Y tu corrección al
   dato de mesa quedó depositada como NOVENA muestra (abajo, en el acta).
   La transición direccional de D queda esperando a que D MONTE (L-241:
   se verifica donde vive — montarla en la galería sería reconstruir la
   lámina que la ley acaba de matar); confirmaste que el ancla de la
   puerta no necesita prop nueva.** *(El texto original del pedido, como
   registro de qué se pidió y con qué premisa — hoy corregida:)*
   **🔴 LA DUDA CONCRETA DEL FOUNDER — EL ANILLO — y el método es
   montado-en-mapa (L-241, nacida de este mismo dictado):** *«me gusta el
   ícono, pero no el círculo que la cubre encima.»* **Montá el pin en el
   MAPA REAL y serví la comparación con anillo y sin anillo, sobre los
   cuatro tonos que ya usaste.** El dato técnico de mesa para que no se
   decida a ciegas: **el anillo no es decoración — es lo que separa la
   silueta oscura del fondo** cuando el pin pasa sobre calle gris o parque
   verde; sin él puede desaparecer contra ciertos tonos, **y puede ser
   mucho más sutil que el que el founder vio.** Se sirve el RESULTADO
   medido, no la opinión: **si sin anillo se lee sobre los cuatro tonos,
   muere el anillo.**
3. **LO QUE ESPERA FIRMA VA PRIMERO EN LA GALERÍA — AHORA ES FIRMA DEL
   FOUNDER y NO espera a D-833:** la cura de una línea se ejecuta YA
   (era tu ítem; subió de cura pedida a orden firmada). La limpieza
   grande sigue siendo D-833 con su guarda.
4. **El cambio de método que te toca de cerca (L-241):** la pieza se gatea
   DONDE VIVE — un pin sobre mapa real, un campo dentro de su formulario.
   Tu toque 1 NO cambia (la receta antes de escribir sigue siendo
   precondición); **cambia dónde se VERIFICA la forma.**

## 📬 PARA B — POR REPO (adición 15-ago tarde: DOS piezas nuevas con firma)

**⓪ PRIORIDAD SOBRE TU COLA (orden de mesa con la enmienda del artesano):
la RECETA DE FORMA DE LA FICHA DEL REPARTIDOR (L2) va PRIMERO** — el lote
#0 cierra pronto y L2 arranca detrás SIN decisiones abiertas (la spec del
founder está entera en `PLAN_S99` §4.4). *Si C escribe esa pantalla sin tu
receta, estrenamos la vara nueva incumpliéndola.*

1. **EL CONTADOR DE LA PUERTA HERMANA — PIEZA TUYA, con firma del founder:**
   *«busca un diseño bonito y elegante, que no se vea como una mancha en la
   pantalla; quien esté pendiente del diseño de toda la sesión debe validar
   que se vea bien.»* Nace ADENTRO de `PuertaHermana` (la ley del espejo —
   si el consumidor lo compone, las dos mitades divergen y nadie lo ve
   hasta cruzar), con **tu toque 1 ANTES de que D monte**. La ley que la
   forma tiene que servir: cuenta lo NO VISTO (entra a la ventana → muere)
   · simetría en las DOS puertas · **cero = AUSENCIA de insignia** · la
   insignia roja genérica NO es el estándar de esta casa · entrada y salida
   con el vocabulario N10 (*un número que aparece de golpe es una mancha;
   uno que llega es una novedad*) · tres temas + reduce-motion al nacer ·
   y tu propia ley: **la puerta es un LUGAR, no un aviso** — decir «hay
   algo nuevo del otro lado» sin volverla alarma es exactamente tu oficio.
   **La excepción del EXPRÉS** (promesa de una hora, un contador no
   alcanza): serví a la mesa cómo se anuncia — propuesta tuya.
2. **EL VACÍO DEL VENDEDOR — DOS HUECOS HERMANOS, SERVIDOS JUNTOS (orden de
   mesa: son el mismo vendedor el mismo día):** ① el HOY sin pedidos
   (letra adjudicada — abajo en PARA C — tu receta de forma es toque 1) ·
   ② tus tres listas vacías de configuración (tu §5bis). No se resuelven
   por separado.

## 📬 PARA B — POR REPO (adición 15-ago, del gate — SU OFICIO como Dirección de Diseño)

1. **EL FRENTE DE ORGANIZACIÓN (del ojo N11, arriba):** ① censá las DOS
   configuraciones — la del vendedor de despensa (`ventas/configuracion`)
   y la de servicios — y traé en qué difieren: agrupación, jerarquía,
   orden de campos, uso de secciones, dónde vive cada CTA. ② Traé la
   **GRAMÁTICA ÚNICA de una pantalla de configuración de la casa** (cómo
   se agrupan los campos, cuántos por bloque, qué lleva encabezado, qué se
   pliega, dónde vive el guardar) **con referencia de industria concreta,
   no inventada** — tu mandato literal. ③ **Tu receta va ANTES de que C
   toque la pantalla (toque 1 tuyo), no después.** La vara del founder:
   *diez campos no son muchos si la pantalla está bien organizada* — el
   trabajo es la organización, no el número.
2. **🔴 EL GATE POR ÍCONO DE LA MOTO: FALLADO — ninguna de las tres pasa, y
   ES ORDEN DE MESA PARA VOS con el brief corregido.** Firma verbatim:
   *«estoy viendo los glifos de moto y no me gusta ninguno de los tres,
   copiemos el como los pone Rappi, algo que se vea bien en el mapa. Si me
   toca escoger sería el A, aunque parece más una bicicleta que una
   moto.»*
   **Las dos lecturas de mesa, y la segunda vale más:** ① la candidata ②
   NO pasa — su riesgo declarado («puede leerse como bicicleta») se
   materializó en el ojo del founder · ② **EL BRIEF ESTABA MAL, y por eso
   no es culpa del dibujo:** las tres se pensaron como GLIFO DE LA CASA
   (trazo fino, objeto de interfaz) y su destino es otro — vivir DENTRO DE
   UN PIN, a 21 px, sobre un mapa con calles y colores debajo. A ese
   tamaño y sobre fondo con textura **el trazo fino desaparece y solo
   sobrevive la MASA**; lo que se lee en el mapa de Rappi no es un ícono
   de línea — es una silueta RELLENA de alto contraste dentro de un pin.
   **⚠️ CHOQUE CONTRA LEY FIRMADA, declarado y NO diferido:** la regla
   madre («objeto del oficio en TRAZO + UNA huella rellena») se contradice
   con un pin-moto de silueta llena. **La mesa NO lo resuelve sola — LO
   SERVÍS VOS con las dos salidas y tu voto:** (a) el pin de mapa es una
   **CLASE APARTE** del registry, con su propia física (masa, contraste,
   tamaño mínimo) y la regla de trazo no le aplica — *el mapa no es
   interfaz, es mundo*; (b) la regla se enmienda para admitir relleno
   sobre fondo no-controlado. **El voto de mesa acompaña (a)** por la
   razón angosta que la casa ya usó dos veces: antes que estirar una regla
   viva, se declara la clase nueva.
   **El brief corregido:** · el DESTINO manda — se dibuja PARA el pin,
   sobre fondo de MAPA REAL, y se juzga a 21 px sobre ese fondo, jamás
   sobre lienzo blanco (tu §6b pide vecinos: acá el vecino es EL MAPA) ·
   referencia concreta: el marcador de repartidor de las apps de delivery
   que el founder nombró — silueta, contraste, relación pin↔glifo · **el
   discriminador moto-vs-bicicleta es la MASA del motor y la rueda gruesa,
   no el detalle — a 21 px el detalle no existe** · la huella-discriminador
   ya está firmada: el pin de moto va SIN huella (en el par mascota/moto,
   la huella ES el discriminador) · **nueva tanda de candidatas, nuevo
   gate; el anterior queda registrado como FALLADO con su razón, no
   borrado — el aprendizaje es del brief.**
   *(La cura «la lámina va primero en la galería» sigue en pie para la
   tanda nueva: la lámina que espera firma va ARRIBA, no en la sección 81
   de 95.)*
3. **El rótulo seccionado por el título fijo de la Hoja** — hallazgo
   medible de C, tu territorio (`packages/ui`).
4. **N19 (el nombre del producto no se trunca):** si la fila donde se corta
   es pieza tuya, la cura de la pieza es tuya; la superficie es de C.

## 📬 PARA D — POR REPO (canales caídos; orden de mesa 15-ago, destinatario nombrado: PISTA D)

**🔴 LO PRIMERO DE TU COLA (Gate 2, 16-ago — BLOQUEA EL CIERRE DE L2 y es
UNA LÍNEA):** montá la pieza `reclamo-vinculo` de C en TU cascarón. El
literal del hallazgo: *un repartidor real creó su cuenta con el correo
garantizado y quedó en un callejón cortés — «Avísale a quien administra el
negocio que te invite con ese correo» — YA ESTABA invitado
(`cuenta_existe: 1, sin_atar: true`).* Mi motor (`aceptar_vinculo_repartidor`
+ `mis_vinculos_repartidor_pendientes`) vive desde S99-A con CERO
consumidores y el JSDoc nombraba a su deudor; C entregó la pieza y NO la
montó porque su lugar es tu cascarón. **El montaje es tuyo, hoy.**

**✅ EL ATERRIZAJE DE duenotodo — FIRMADO (founder, 15-ago; la mesa RETIRÓ
su voto (c) y adoptó la del founder):** **duenotodo ATERRIZA EN CITAS
(servicios), y la puerta hacia pedidos lleva un CONTADOR de pedidos nuevos,
como una notificación.** La razón del retiro: el techo común era un peaje
diario para todos por un problema que solo aparece a veces — la del founder
aterriza EN EL TRABAJO y usa la puerta que L4 ya construye: cero superficie
nueva.

**🔴 LA LEY DEL CONTADOR (S91, o se vuelve reproche) — te llega ENTERA, no
solo la firma:**
1. **Cuenta lo NO VISTO, jamás el total del día** — entra a la ventana → el
   número muere · entran pedidos nuevos → vuelve. PUEDE llegar a cero.
2. **SIMETRÍA OBLIGATORIA:** la puerta de vuelta también cuenta (cita nueva
   mientras está en pedidos) — las dos puertas, la misma ley.
3. **Cero = AUSENCIA de insignia**, no un cero dibujado.
4. **Excepción ÚNICA: el pedido EXPRÉS** no es un número más (promesa de una
   hora) — cómo se anuncia sin romper la ley se SIRVE a la mesa, pendiente.
5. duenovet/duenodes no ven puerta ni contador (sin hermana no hay novedad).

**⚠️ EL CONTADOR TE LLEGA COMO PIEZA DE B — NO LO COMPONÉS** (adenda del
founder: *«busca un diseño bonito y elegante… quien esté pendiente del
diseño debe validar que se vea bien»*): nace ADENTRO de `PuertaHermana`
(la razón estructural es TU argumento del espejo — si cada pantalla lo
arma, el espejo se rompe en la primera cura), con toque 1 de B antes de
que montes.

**EL DATO DEL CONTADOR, MEDIDO (mi lector, tu consumo):** marca de visto
**NO EXISTE** (universo: `information_schema.columns` de public,
`%visto%`/`%leido%` — NINGUNA). **Propuesta con costo, NO construida:**
tabla chica `ventanas_vistas(user_id, cuenta_comercial_id, ventana,
visto_hasta timestamptz)` con PK compuesta — **por PERSONA, no por cuenta**
(el mostrador es multi-persona: cada quien su no-visto) — + RPC upsert al
ENTRAR a la ventana + el count viaja en un lector EXISTENTE (cero ola
nueva). «Pedido nuevo» = **pago confirmado DESPUÉS de `visto_hasta`**
(coherente con la puerta de la cola: `pagos_intentos.cerrado_en`). Costo:
1 tabla + 1 RPC + 1 booleano/count en viaje existente. Pedímelo con tu
letra de montaje y lo escribo.

**✅ TU §12: RECIBIDO Y EJECUTADO (15-ago, tarde).** La RPC
`obtener_contexto_arranque()` está APLICADA con todo lo que pediste en
§12.5: `cuenta_comercial` (id·estado·nombre·país) · `moneda` config ENTERA
(D-448) · `es_vendedora` fresco (D-821) · `prestador` (la fila entera de
`obtener_mi_prestador`) · `es_gestor` · `monta_atender` en sus DOS mitades
(`posicion.es_mostrador_o_gestion` + `hay_oficio_local`). Wrapper:
`obtenerContextoArranque()` en el barrel. Tu 12.4 sigue tuyo (la lápida de
`lineaItems` + sacar `listarPedidosDelVendedor` del HOY).

**LA COSTURA DEL LOTE #0 — EL CONTRATO DE COMPOSICIÓN POR CAPACIDAD (orden
de mesa: se acuerda CON VOS antes de escribir, no al mergear; contestá en
tu parte confirmando o enmendando):**
- **Qué se escribe UNA vez:** la composición del HOY por capacidad, con la
  MISMA fuente que ya arma la barra (`barra-prestador-lectura.ts`, tu
  cascarón) — una fuente, N consumidores (barra · HOY · tu L4).
- **Mi propuesta de forma:** ① el dato viene en UN viaje — la RPC de
  contexto (D-738) devuelve identidad + capacidades juntas (hoy son 3 olas
  de identidad + la sonda `contextoVentas` aparte = 4 esperas para saber
  QUIÉN es) · ② la lib de lectura de la barra se ENSANCHA (no nace una
  paralela — L-175) para exponer qué monta el HOY por población:
  prestador → jornada sola · vendedorPuro → panel solo · **DUAL →
  jornada, y los pedidos viven en tu ventana HERMANA de L4 — el HOY NO
  los monta** (tu letra del dual queda consumida, no deshecha).
- **✅ TU ENMIENDA, ACEPTADA POR MESA (16-ago): el contrato del dual NO
  necesita nombre nuevo.** Que el HOY no monte pedidos es la AUSENCIA de
  una rama, no un dato — *un contrato que nombra lo que NO pasa se
  convierte en una bandera que alguien lee al revés.* **Lo que la
  composición expone: `esVendedora` — y YA VIAJA** (`es_vendedora` en
  `obtener_contexto_arranque`, verificado E2E): la puerta se monta sin
  sonda aparte. **⚠️ Y la cura de combinación del 16-ago, declarada:** el
  merge de B trajo `PuertaHermana` exigiendo `sinVer` por tipo y tus dos
  montajes eran anteriores — **puse `sinVer={0}` TRANSICIONAL en los dos**
  (la regla de existencia de B hace que 0 no dibuje nada: puerta limpia,
  la verdad mientras el lector de no-vistos no exista). Cross-territorio
  mínimo para main verde; **el dato real lo cableás vos cuando mi motor
  de `ventanas_vistas` exista.**

**⚖️ EL ORDEN DE LA COLA — ADJUDICADO CON EL VOTO DE D (16-ago noche;
FALTA SOLO LA PALABRA DE C — cómo pinta bandas y reorden — y el reorden se
escribe):**
1. **El REORDEN lo escribe UNA sola superficie: EL PANEL. La ventana
   LEE.** (El mismo argumento con el que la ventana se volvió pieza: dos
   escritores del mismo orden divergen en la primera cura.)
2. **`entrega_fallida` NO entra al FIFO** (la razón de D, correcta: el
   FIFO contesta «¿de quién es el turno?» y ese pedido ya lo tuvo y se
   rompió — por hora de pago quedaría enterrado bajo diez frescos).
3. **El DÍA agrupa por compromiso; el FIFO ordena por pago ADENTRO.**
   Nadie filtra el día por fecha de pago.
4. **La PROMESA es techo, jamás clave de orden** (si ordenara, la ventana
   dibujaría la ruta — que es LA OTRA cola, D-830). `promesa_desde` queda
   vestigial.
5. **🔴 LEY DEL LOTE (el freno de D, elevado): SIN `pago_confirmado_en` NO
   SE ORDENA POR PROXY.** `creado_en` no es pagar — *un FIFO falso es
   peor que ninguno: el vendedor lo lee como justicia y prepara en el
   orden equivocado creyendo que respeta el turno.* Hasta que el dato
   llegue a cada superficie, rige el orden honesto de hoy. **Estado del
   dato: YA VIAJA en `v_pedidos_narrativa` y en los DOS lectores** (mi
   motor del 16-ago) — lo que falta es que las superficies lo consuman.
*(El contrato original, como se sirvió:)*
**⚖️ L3 ARRANCA — EL CONTRATO DEL ORDEN DE LA COLA (16-ago noche; se
ACUERDA con C y con D ANTES de escribirse — contestá en tu parte):**
- **Lo firmado que el contrato ejecuta (Gate 1):** DOS bandas — URGENTE
  (exprés pagado) · **FIFO por hora de CONFIRMACIÓN DEL PAGO** (sin pago
  confirmado NO entra a la cola) — con el vendedor pudiendo REORDENAR y
  **la promesa de entrega como TECHO DURO** (Ley 23: el movimiento que
  saca a otro de su ventana no se ofrece). Los `sinFecha` PRESIDEN.
- **La fuente, medida:** `pagos_intentos.cerrado_en` del intento aprobado
  (`pedidos.pagado_en` está BLOQUEADA y 0/14). **Mi primer paso de motor
  (no contencioso, ya en curso): exponer `pago_confirmado_en` en el
  lector del rango** — el dato viaja, la pieza ordena.
- **Los 6 grupos de acción SE CONSUMEN de `AccionPedido`
  (`escalera-pedido.ts`)** — existen 1 a 1; re-implementarlos dejaría dos
  criterios contando el mismo hecho (adjudicado en L0).
- **Lo que espera SU palabra:** C — cómo la pieza pinta las dos bandas y
  el reorden (su ventana) · D — el orden en SU ventana hermana (su ítem ④:
  hoy ordena por `prioridad()` de trabajo pendiente; el FIFO lo cambia).
  **El reorden manual (persistencia + guard del techo) NO se escribe
  hasta el acuerdo de los tres.**

**🎨 DICTADO DE DISEÑO DEL FOUNDER SOBRE EL DUAL (16-ago — verbatim entero,
tuyo con B; el reparto abajo):** *«El "Tus pedidos del día" con la CTA para
ir a la otra cara del HOY, dejalo a la misma altura de "Tu día". Al tener
dos escalones diferentes queda un espacio inutilizado de la pantalla, que
da un espacio en blanco encima del label de Tu día. Que esto no sea un
problema del lado de la pantalla de las entregas: el label de Tu día podés
ponerlo al costado contrario de la pantalla. Y el efecto de transición de
pantalla, desde pedidos hacia citas, debe ser del lado contrario — se debe
sentir que está regresando. El header de la vista de citas se mantiene en
pedidos; lo que cambia es que en vez de dar el número de citas, da el
número de pedidos. El valor del día y las mascotas es claro; lo que debe
es agregar un campo extra de pedidos entregados — podemos reemplazar el
campo que usa el cuadro de mascota de este lado del HOY.»*
**El reparto:** ① la puerta A LA MISMA ALTURA que «Tu día» (los dos
escalones dejan un blanco muerto) — composición TUYA con receta de B (la
pieza no cambia; cambia dónde se ancla) · ② el label «Tu día» AL COSTADO
CONTRARIO en la ventana de pedidos — tuyo · ③ 🔴 LA TRANSICIÓN DE VUELTA
VA AL REVÉS (de pedidos a citas se debe SENTIR QUE REGRESA) — **tuyo CON
B: verificá el montaje contra su propia receta de simetría** (mismo
número, signo invertido — o está montada en un solo sentido) · ④ el
HEADER de citas SE CONSERVA en pedidos cambiando lo que cuenta: número de
PEDIDOS · valor del día se mantiene · y GANA «pedidos entregados»
reemplazando el cuadro de mascotas (de ese lado no hay mascotas).
**Mi medición para ④ — NO me debés pedido de motor:** los tres números
son DERIVABLES EN MEMORIA del rango que ya traés (`delRango`: count del
día · suma de montos · count con estado `entregado`) — cero viaje nuevo.
Si querés el valor server-side con otra semántica, pedímelo con letra.
**La lectura de mesa que une el dictado:** el vendedor no es un prestador
con otra lista — es OTRO OFICIO en la misma casa; el header espejado con
SUS magnitudes es §2.0 aplicado al techo.

**✒️ TU 🟡 DE `/ventas` — CERRADO POR ADJUDICACIÓN DE MESA (15-ago, sin
re-discusión): SOBREVIVE.** Tu condición de muerte ya está escrita desde la
costura 1 y NO cambió: **la ruta vive mientras exista el empleado-vendedor
no-gestor, que no tiene barra propia.** El dual ganó su día; esa población
sigue sin otra puerta — matarla le sacaría el piso al mismo actor al que
S96 ya se lo sacó una vez. **Puntero: tu lápida de la costura 1 (tu parte,
§ de la costura). Que L5b NO lo reabra: la condición es esa y solo esa.**

**✅ Tu pedido de gate: RATIFICADO por mesa — pediste en vez de publicar,
que es el freno de la casa bien leído. EL PRÓXIMO OTA LLEVA TU TANDA
ENTERA** (la lápida de la línea fusionada · la ventana hermana · el cruce
con navigate) — el disparo del OTA sigue siendo la cura del techo de
Cuenta de C, y tu L4 ya está EN MAIN (mergeada 15-ago, TSC 0 + 37 reglas +
tus dos guards verdes en la combinación).

**La duplicación HOY ↔ raíz `/ventas` se cura (dueño C, vos AVISADA):**
con el HOY del vendedor puro montando la misma pieza, para esa población
hay UNA sola entrada — la tab. **La ruta `/ventas` queda viva SOLO para la
población sin barra (el empleado-vendedor no-gestor)** — exactamente la
condición de muerte que tu lápida de la costura 1 ya tenía escrita. No se
re-litiga: se ejecuta su condición.

**Cura de texto de UNA línea, tu territorio (el histórico dual):** en el
contexto VENDEDOR el histórico dice **«las atenciones y citas que ya
pasaron»** — pero lo que lista ahí es la ventana de PEDIDOS: la pantalla
del vendedor JAMÁS habla de citas ni atenciones. La voz de la población
vendedor debe hablar de **pedidos** (la de servicios queda como está). Es
el hallazgo ② del gate del founder, adjudicado por mesa; entra como cura
anotada, no reabre el dual.

## 📬 PARA C — POR REPO (adición 15-ago, tarde)

**El PIN del aparato YA está en el keychain, corregido:** el founder lo
mandó por canal directo a A y quedó guardado en la entrada
`-a pin -s epetplace-dispositivo-s97` — la desalineación que mediste (la
entrada guardaba la clave de cuentas) está CURADA. Se lee al momento con
el `security find-generic-password` de siempre; no vive en ningún archivo.

**Tu observación de la bienvenida: ASIGNADA A VOS (cura chica, y toca la
puerta de entrada de la casa):** la bienvenida solo le habla al prestador
(«para prestadores» · «¿Eres prestador nuevo? Solicitar acceso») — un
motorizado tiene que tocar «Ingresar» para encontrar «¿Primera vez? Crear
tu cuenta». *El camino existe, pero lo único que lo invita lo manda al
lado equivocado.* La voz nueva pasa por el lote de strings del founder.

**Del Gate 2 (16-ago): tu freno de la Hoja del recurso tiene respuesta —
y ES la respuesta:** el founder no vio la advertencia (*«no tengo en mente
de qué me hablas»*). Si estuviera bien puesta, la habría notado. **La
advertencia necesita su propia cura, no solo un pie fijo: servila a B como
problema de FORMA, no de layout.** Y tus dos pedidos de motor están en mi
cola (#3bis): el correo en `listarRepartidores` y el lector de viajes.

**Tus hallazgos del gate, adjudicados con dueño (mesa 15-ago):**
1. 🔴 **N19 — el nombre del producto se corta en CUATRO superficies: es
   CLASE, no caso, y el nombre es el dato con el que se elige.**
   Adjudicación: *donde el nombre ES el criterio de elección, no se
   trunca — respira a dos líneas o la superficie le da lugar.* Dueña VOS
   (con la pieza de B si la fila es suya).
2. **El CTA que no entra al abrir «Ajustar stock»** — tuyo.
3. **La duplicación HOY ↔ `/ventas`** — tuya, con D avisada (nota arriba):
   una entrada para el vendedor puro; la ruta queda para el
   empleado-vendedor no-gestor.
4. **La spec de L2 está FIRMADA y depositada** (`PLAN_S99` §4.4): WhatsApp
   ÚNICO obligatorio con indicativo (muere el teléfono convencional y
   muere «opcional»), foto por `HojaCaptura` (reemplazo, no construcción),
   ficha completa con máx-2 vehículos POR CONSTRUCCIÓN.
5. **⚠️ LA REGRESIÓN QUE TU MEDICIÓN CAZÓ tiene discriminador (cola viva
   #0):** los viajes crecieron 13→17 olas — tu cura del HOY montó el panel
   con olas propias y la sonda `contextoVentas` corre para todos. NO es
   N11. **Enmienda de mesa: la cura NO es dedup — es COMPOSICIÓN POR
   CAPACIDAD** (el HOY monta por capacidad con la misma fuente que la
   barra; contrato en acuerdo con D por repo). La mitad de motor (D-738 +
   composición) es mía; **no cures olas por tu cuenta** — y el cierre del
   lote lo mide TU instrumento en aparato: el criterio es **<2 s en
   frío**, no volver al baseline (que ya fallaba N16).
6. **Publicación:** cuando tu cura del techo de Cuenta («No pudimos cargar
   tu negocio» al vendedor puro) esté en tu rama Y en origin POR SHA,
   la publico con lo que alcance — palabra del founder ya dada.
6bis. **🟢 L2 ARRANCA YA — CON FIRMA DEL FOUNDER (16-ago):**
   **R46 ya te espera** — el guard aborta el commit si la superficie que
   pide el WhatsApp del repartidor se queda sin `ControlTelefono` (la
   protección exacta del hallazgo del selector). Y arrancá POR EL MOTOR
   MÍO QUE YA ESTÁ LISTO: `correo` con UNIQUE por casa · `p_correo`
   compatible hacia atrás · `aceptar_vinculo_repartidor()` · el guard #9
   vivo · y `correo_requerido` esperando TU pantalla para encenderse.
   La precondición de diseño está CUMPLIDA:
   la receta de forma de la ficha del repartidor de B **está en el canon**
   (`docs/laminas/2026-08-15-s99b-RECETA-FICHA-DEL-REPARTIDOR.md`,
   mergeada 16-ago) y la spec del founder está ENTERA en `PLAN_S99` §4.4
   — L2 no tiene ninguna decisión de producto abierta. **Y el hallazgo de
   B viaja con la receta: al borrar el campo de teléfono convencional, el
   selector de indicativo SE MUDA a WhatsApp — no se va con él.** *El diff
   de un borrado se lee como una resta: lo que el campo borrado portaba y
   otro necesita se MUDA explícito, no se pierde en el diff.* Del lado
   motor todo te espera: `p_correo` compatible, guard #9 vivo,
   `correo_requerido` apagado hasta tu OTA.

7. **EL HOY SIN PEDIDOS — ADJUDICADO, tuyo con la letra de mesa (15-ago),
   receta de B primero (toque 1, junto con su §5bis — orden de mesa:
   juntos):**
   - **El vacío del día es AUSENCIA DE TRABAJO, NO AUSENCIA DE NEGOCIO.
     Muere «Todavía no hay pedidos…»** — consolar en pasivo es lo que la
     ley prohíbe.
   - El HOY vacío muestra lo que SOSTIENE el día: **está abierto** (con su
     horario — `listarTurnosEntrega`/`cupoRepartoDelDia`, ya existen) ·
     **está en condiciones de recibir** (productos vivos y qué queda afuera
     por N18 — `listarSkusDelVendedor`) · **cómo viene contra lo normal**
     (derivable del rango en memoria), sin ranking ni score.
   - **DOS VOCES, un discriminador (ya viaja en el contexto de arranque:
     `haVendido`):** false → ARRANQUE (qué le falta para su primera venta)
     · true → SERENIDAD (todo está bien, es un día tranquilo). **Jamás la
     misma frase para los dos.**
   - La guarda de la casa: ningún contador que no pueda llegar a cero, y
     nada que dependa de e-PetPlace entra al contador (LOYALTY §3 · §7.5).

## 📬 PARA B — POR REPO (su canal de mensajes está caído; orden de mesa: esto reemplaza al mensaje)

- **Tu R45 fue RESCATADA a main por rutina** (`f05b7ea9` → mergeada, verify
  37/37 VERDE sobre el main mergeado, R45 con baseline 0 y «nació antes que
  su primer consumidor»). Desde ahora **mergeo `pista/s99-b` en cada cierre
  de tanda mía, sin esperar aviso** — publicá a tu rama y llega.
- El motor del reclamo (abajo) NO agrega consumidores de `…EnRango` — tu
  baseline de R45 no se mueve con esta tanda.

## 🔧 L2 · EL MOTOR DEL RECLAMO — CONSTRUIDO (adjudicaciones #2 y #9 ejecutadas)

- **Migración `20260817120000`** (aplicada+registrada, cinturón 4 brazos,
  reversa ANTES): `repartidores.correo` (llave de reclamo, lower, UNIQUE por
  casa) + `vinculo_aceptado_en` · `registrar/actualizar_repartidor` ganan
  `p_correo` **compatible-hacia-atrás** (el bundle S98 llama sin él y sigue
  entero) · **`aceptar_vinculo_repartidor()`** (un toque, sin parámetros —
  el correo sale del profile de la sesión: nadie reclama lo de otro POR
  CONSTRUCCIÓN) · **`mis_vinculos_repartidor_pendientes()`** (el lector de
  la pantalla de aceptación) · **el guard #9 en `despachar_pedido`**
  (`repartidor_sin_cuenta`, hablado con el nombre).
- **Fixture 7/7 ROLLBACK residuo 0/0/0** con el par discriminador: el
  despacho REBOTA antes del reclamo y DESPACHA después; contra-caso de
  identidad (otro uid no ve vínculos ajenos); idempotencia del aceptar.
- Wrappers: `aceptarVinculoRepartidor` · `misVinculosRepartidorPendientes` +
  `correo` en registrar/actualizar — **re-exportados en el barrel** (la
  lección de esta misma sesión). gen:types +10. Typecheck verde.
- ⚠️ **CONSECUENCIA DECLARADA (adjudicación #9 obrando): Marco (Aurora, el
  repartidor demo del founder) y «Repartidor duenotodo S97» NO tienen cuenta
  reclamada ⇒ su despacho REBOTA desde hoy** con voz que dice la salida
  (*«registrale el correo y pedile que entre a la app»*). El camino de
  salida ES el gate de L2.
- 📬 **PARA C — POR REPO (el canal de mensajes de A también se cayó; esto
  reemplaza al mensaje):** tu L2 tiene el motor SERVIDO — la pantalla de
  aceptación al primer ingreso consume `misVinculosRepartidorPendientes()` +
  `aceptarVinculoRepartidor()` (los dos en el barrel), y tu alta EXIGE el
  `correo`. **La exigencia `correo_requerido` del motor se enciende RECIÉN
  con tu OTA aplicado** (orden S98: pantalla → merge → OTA → guard) —
  encenderla hoy rompería el alta del bundle vivo. **Declarálo en tu parte
  cuando el OTA esté y yo la prendo.** Y si un ensayo tuyo de despacho
  rebota `repartidor_sin_cuenta` con Marco o «Repartidor duenotodo S97», NO
  es regresión: es el guard #9 obrando — la salida es el reclamo.

## (histórico) HANDOFF DE A del 16-ago noche — SUPERSEDED por el del 18-ago que abre este archivo

### ⚡ ESTADO AL CIERRE — lo que NO se puede perder

- **OTA VIGENTE: group `2d480427` · ancla `f2c839f4` · el pie debe decir
  `update 01a008cd · preview`.** Lleva L2 ENTERA (ficha + reclamo montado
  por D — carga verificada por merge-base: `6fbd54ef`·`1985121e`·`7f28dff3`
  ancestros del ancla). `origin/main` posterior solo agrega DOCS (cero
  código: diff apps/packages vacío). **C corre el par discriminador del
  guard #9** (despachar a Marco REBOTA `repartidor_sin_cuenta` · al
  reclamado PASA — solo el rebote no prueba nada) → **④⑤ del Gate 2
  cierran.**
- **LA COLA FIRMADA: L3 → L5a → L5b** — con **el EXPRÉS FUERA del camino
  crítico de la vitrina** (se construye después de que la vitrina esté en
  pie, y su lectura de `MODELO_FINANCIERO` es PRECONDICIÓN de su
  migración: ¿el 10 % corre sobre el recargo? ¿cómo entra al ledger?).
- **EL ORDEN DE LA COLA, ADJUDICADO** (falta solo la palabra de C sobre
  bandas+reorden): **FIFO por `pago_confirmado_en`** (ya viaja en la vista
  y los DOS lectores) · **el reorden lo escribe SOLO EL PANEL, la ventana
  lee** · `entrega_fallida` FUERA del FIFO · la promesa es techo jamás
  clave · **y la LEY DEL LOTE: sin el dato NO se ordena por proxy —
  `creado_en` no es pagar, un FIFO falso es peor que ninguno.**
- **LA INVESTIGACIÓN DE TIEMPOS: CONGELADA EN D-834** con sus CUATRO
  muertos por medición (red · olas · bundle +1,06 % · instrumento) y **el
  SEGUNDO APARATO como próximo paso** (el discriminador más barato). El
  previo 2,0–2,5 TAMBIÉN fallaba N16. #0b disuelto. **Nadie toca
  rendimiento hasta la sesión propia.**
- **LA CLASE «MOTOR SIN PUERTA» — tres muestras en una sesión** (la
  tercera la encontró un repartidor REAL): pasa todos los verdes porque
  ninguno mide si alguien lo llama.

### ✅⚠️ LA COMPETENCIA — OPCIÓN (b) EJECUTADA + EL CENSO DE LA CADENA, CON UN HALLAZGO QUE CAMBIA EL REPARTO (18-ago noche)

**① EJECUTADO (firmado): el índice se movió.** `uq_oferta_publicada_por_
cuenta_variante` = `UNIQUE(cuenta, variante) WHERE publicada` — migración
`20260818000000`, cinturón con **el discriminador que es la competencia
misma** (dos ofertas de la misma variante de vendedores distintos AHORA
entran; el duplicado del MISMO vendedor sigue rebotando por índice),
sobre los SKUs del choque ya sembrados, con restauración y residuo 0.
**La firma ③ de S94 queda intacta y por fin literal.** ⚠️ Declarado en la
migración: **esto NO elige quién gana** — hasta que la mesa adjudique, la
vitrina puede mostrar N ofertas del mismo producto, y eso es VISIBLE, no
silencioso.

**② EL CENSO DE LA CADENA, CON COSTO — medido del objeto:**

| criterio | ¿dato? | costo | veredicto |
|---|---|---|---|
| **① STOCK** (condición de entrada) | ✅ `vendedor_skus.stock_disponible`, materializado por trigger | **BAJO** — es un `WHERE` | **LLEGA A OCTUBRE** |
| **② CERCANÍA** (puerta) | ⚠️ **hay dato pero NO es distancia**: `reglas_envio.parametros.ciudades_cubiertas` (lista de nombres: Quito + valles + Sangolquí/San Rafael). `zonas_cobertura` es de TRANSPORTISTAS (tarifas), no del vendedor | **BAJO-MEDIO** — un `?` sobre jsonb; **pero la vitrina hoy NO recibe la ciudad de la familia** (el catálogo no pide dirección) | **LLEGA como PUERTA BOOLEANA por ciudad**, que es exactamente lo que la firma pide. *Ordenar por distancia sería imposible hoy — lo que REFUERZA la corrección de la mesa* |
| **③ CALIFICACIÓN** (el orden) | 🔴 **NO EXISTE.** `resenas_productos` es del PRODUCTO (`producto_id`) y tiene **0 filas**; `prestador_resenas` es de PRESTADORES (servicios). **Cero calificación de vendedor** | **ALTO — es un ARCO**: motor + momento de pedirla + superficie + moderación | **NO LLEGA A OCTUBRE** |
| **④ ANTIGÜEDAD** (desempate) | ✅ `cuentas_comerciales.created_at` | **BAJO** | **LLEGA** |

**🔴 ③ EL HALLAZGO QUE CAMBIA EL REPARTO, y es el que hay que decidir: el
criterio provisional y la ventana del vendedor nuevo SON EL MISMO
PROBLEMA — y por eso la propuesta es una sola pieza, no dos.** En octubre
**nadie tiene calificación** (dato medido: 0 reseñas de vendedor, y el
único con historial tiene 2 entregas). Un provisional por **antigüedad**
le da la vitrina al que llegó primero **para siempre** — es exactamente
el modo de falla del ⑤ («el sistema se cierra sobre los primeros cinco»),
pero peor: nace cerrado.

**MI PROPUESTA (una pieza, con su condición de muerte escrita):
ROTACIÓN JUSTA DETERMINISTA entre los elegibles** — con stock (①) y que
llegan (②), el ganador se elige por `hash(producto + día + familia) %
N_elegibles`. Por qué resuelve las dos mitades: **es reparto parejo
mientras nadie tiene calificación** (la ventana del ⑤ sin construir nada
aparte) · **es estable** (la misma familia ve el mismo vendedor el mismo
día — no baila entre recargas) · **es barato** (cero dato nuevo) · y
**muere solo**: cuando ③ exista, la calificación ordena y la rotación
queda SOLO como ventana de arranque para el que aún no tiene reseñas.

**✅ LA VENTANA, FIRMADA (founder + enmienda de mesa, 18-ago): DIEZ
PEDIDOS O TREINTA DÍAS, lo que llegue primero · y la ventaja NO es «gana
siempre»: es COMPETIR CON UNA CALIFICACIÓN ASUMIDA (el promedio del
ecosistema) mientras está en ventana.** *Lo que la vuelve barata: no hay
regla aparte para novatos — es la MISMA cadena con un valor asumido para
quien no tiene historia. Cero rama especial en el ordenamiento.* Y es
autocorrectiva: el nuevo que entrega mal CAE RÁPIDO porque sus primeras
reseñas reales bajan el asumido.

**EL CENSO DE LA VENTANA — con un hallazgo que le corrige el NOMBRE a la
consecuencia:**

| dato | ¿existe? | costo |
|---|---|---|
| **promedio del ecosistema** | 🔴 **NO** — `resenas_productos` visibles: **0 filas**, y **cero vistas de rating** en el esquema | **cuando ③ exista: BAJO si se materializa** (patrón `mascota_perfil_vigente`); computarlo en cada ordenamiento de vitrina sería un AVG global por lectura — se materializa, no se calcula al vuelo |
| **fecha de alta del vendedor** | ✅ `cuentas_comerciales.created_at` (medido: Aurora 18-jul · las otras cuatro 12-13 ago) | BAJO |
| **pedidos entregados por vendedor** | ✅ `pedidos.estado='entregado'` por cuenta (medido: **una** cuenta con 2; las otras cuatro en **0**) | BAJO |

**🔴 LA CONSECUENCIA, CON SU NOMBRE CORREGIDO — y es el punto que hay que
decidir: sin ③ la cadena NO colapsa a cercanía; COLAPSA A ANTIGÜEDAD, que
es peor.** La cercanía es PUERTA (booleana: llega o no llega), no orden.
Si todos están en ventana permanente, **todos empatan en la calificación
asumida** ⇒ manda el desempate ④, y ④ es antigüedad ⇒ **la vitrina se la
lleva el que se dio de alta primero, para siempre.** *Es exactamente el
modo de falla que la ventana viene a evitar — pero peor, porque nace
cerrado en vez de cerrarse con el tiempo.* Con los datos de hoy eso
significa: **Aurora (18-jul) le gana a las otras cuatro en todo producto
compartido, sin haber entregado un solo pedido** (medido: 0 entregados).

**⇒ MI PROPUESTA, MÍNIMA Y CON MUERTE ESCRITA (una línea de la cadena, no
una regla nueva): mientras nadie tenga calificación REAL, el desempate ④
no es antigüedad — es ROTACIÓN DETERMINISTA** (`hash(producto + día +
familia) % N`): reparto parejo, estable para la misma familia el mismo
día, cero dato nuevo. **Muere sola:** cuando ③ exista, la calificación
ordena y la antigüedad vuelve a ser el desempate que la firma dice.
*Sirvo la enmienda, no la ejecuto: toca una cadena firmada.*

### ✅ LOS DOS PEDIDOS QUE FALTABAN — CERRADOS (18-ago noche)

- **① LA SIEMBRA DEL SEGUNDO PEDIDO DE duenodes: HECHA Y VERIFICADA POR
  EL CAMINO QUE D CONSUME** — `v_pedidos_narrativa` trae **DOS vivos con
  `pago_confirmado_en` y HORAS DISTINTAS** (`P-20260816-72c94a` 16:55 ·
  `P-20260813-758c71` del 13). **Su guard del FIFO ya puede
  discriminar.** Por las puertas reales, cero INSERT directo — y **el
  motor me corrigió TRES veces, las tres haciendo bien su trabajo**:
  ① el contrato pide `oferta_id`, no `sku_id` (leí la firma y no el
  cuerpo — regla 40) · ② `confirmar_pago_pedido` rebota con el claim del
  cliente puesto (*«este camino es del webhook, no de una sesión de
  persona»*) · ③ la máquina exige `creado → esperando_pago` **con actor
  `cliente`** (desde `creado` el sistema solo puede cancelar) y la
  reserva ANTES del pago (`pago_sin_reserva`, el orden que S95-K curó).
  *La siembra se adaptó a los gates; jamás al revés.* El primer intento
  fallido no dejó residuo (rollback verificado: 0). Marca de D-838 en la
  clave de idempotencia y en la referencia del pago.
- **② EL TYPECHECK DE `apps/cliente`: VERDE ESTRICTO sobre lo COMBINADO**
  (B+C+D+A mergeados) — con `router.d.ts` presente, que es lo que lo hace
  estricto. **Y el de `prestador` también: los dos en cero.**

### ✅ EL ROJO DE C, CURADO Y CON SU LEY (18-ago noche) — la voz ya se puede escribir

**C frenó la voz antes de escribirla y tenía razón.** Su medición:
`saltos_por_cupo: 1` con el cupo VACÍO. **La causa, medida por mí — y
DESCARTA la hipótesis más probable de la mesa (el borde UTC/local): la
hora local se computa bien (`2026-08-16 11:51 sun`); ERA DOMINGO y
`dias_operacion = [1..6]`** ⇒ cupo del domingo = **capacidad 0, consumido
0**. *No es escasez: el vendedor no reparte los domingos.* **La hipótesis
① de C era la correcta** (el campo cuenta saltos por CUALQUIER causa) y la
② queda cerrada con dato. **Y el censo que la mesa pidió «si es el borde
de hora» NO APLICA — se declara para que nadie lo busque: no hay
comparación UTC/local rota.**

**LA CURA (migración `20260818020000`): `promesa_por_vendedor` DICE LA
CAUSA** — camina los días salteados y clasifica cada `disponible = 0`
(**`capacidad 0` → `sin_operacion`** · **`consumido ≥ capacidad` →
`cupo_lleno`** · las dos → `mixto` · sin corrimiento → `null`). **Sin
tocar `calcular_promesa_despensa`**, cuyo comportamiento es correcto
(saltar días sin capacidad ES lo que debe hacer). Cinturón con **el caso
de hoy como discriminador** (domingo con cupo vacío NO puede reportar
`cupo_lleno`) y **E2E camino real: los dos vendedores devuelven
`MOTIVO sin_operacion`**. Wrapper con guard de vocabulario cerrado y
**fail-closed de SIGNIFICADO: lo desconocido cae en `null`, jamás en
`cupo_lleno`** — *afirmar escasez es el error caro.* (El typecheck cazó
mi estrechado flojo antes de subir.)

**📬 PARA C — LA VOZ YA SE PUEDE ESCRIBIR, y tu contrato queda ratificado
entero:** ① corte normal → *«Lo recibís mañana en la mañana»*, dato sin
disculpa ni pregunta · ② corrimiento **con `motivoCorrimiento ===
'cupo_lleno'`** → la frase del founder, terminando en pregunta · ③
`sin_turnos_de_entrega` → SE DICE, jamás un vacío (*«no vende» y «no
declaró cuándo entrega» se arreglan de maneras distintas y por personas
distintas*). **Y el cuarto camino que la cura habilita: `sin_operacion` →
«no repartimos los domingos», que NO es escasez y no debe sonar a
disculpa.** **L-256 depositada.**

### 📬 REPARTO DE LA REVISIÓN DE STOCK Y CONFIGURACIÓN (18-ago, founder)

- **① El techo sigue diciendo «PEDIDOS»** — de B, con **su censo ya
  servido con número** (110 montan `Encabezado`, 84 pasan `titulo` ⇒ ~26
  sitios). Sigue esperando su enmienda.
- **② Stock sin filtros ni buscador — DESFASE, no hueco:** C ya construyó
  el buscador con autocompletar y la ventana con carga al final **para la
  cara cliente**; lo que el founder ve en Stock es la lista vieja. **Es la
  CONSOLIDACIÓN que C tiene en cola esperando la fila compacta de B — y
  el founder acaba de nombrar su costo: *«se vuelve complejo cuando un
  seller tiene muchos productos»*, con 399 sembrados ese costo ya es
  real.** 🔴 **⇒ SUBE LA PRIORIDAD DE LA RECETA DE FILA DE B: no es
  estética, es lo único que separa a Stock de ser usable.** Con ella
  entran juntas las tres de C (conmutación · el número real de la ventana
  medido en aparato · la consolidación sobre Stock).
- **③ 🔴 EL HUECO REAL, y no estaba en ninguna cola: LA FICHA DEL
  REPARTIDOR DENTRO DE CONFIGURACIÓN.** El dictado era que configuración
  MUESTRE EL ESTADO —*«los repartidores con las fotos, con una ficha bien
  bonita»*— y hoy hay una FILA que hay que tocar. La ficha existe como
  pantalla propia; falta que el estado se vea SIN entrar. **De C con
  receta de B — y B lo resuelve DENTRO de la gramática de configuración
  que ya escribió, jamás como excepción: si el repartidor se muestra con
  ficha y los cortes con fila, la pantalla vuelve a tener dos
  gramáticas.**
- **✅ CERRADOS por confirmación del founder:** cortes y horarios (*«creo
  que ya quedó»*) · «Atiendo en mi local» (*«ya está, pero lo tengo
  apagado»* — **su estado correcto por defecto**: la perilla nace en
  false y solo las 2 cuentas con ventas de retiro nacieron prendidas).

### 🔴 GATE 4 — RESULTADO REPARTIDO (18-ago noche): la barra EMPEORÓ, el pin mejoró y falta la marca, y un rojo nuevo en ATENDER

**📬 PARA B — ① LA BARRA, con las seis correcciones del founder:** el
disco va al **VERDE DEL TECHO** (*`bg.tinta` fue una traducción
equivocada: quedó prácticamente NEGRO*) · **LA BARRA VA EN BLANCO** —
decisión nueva del founder, y es la que resuelve el contraste que
faltaba · **🔴 EL HUECO NO ESTÁ**: lo diseñaste como RECORTE (la barra
pierde su `backgroundColor` para que pase el fondo) y en el aparato no se
ve — **medí por qué: o el recorte no está aplicado, o sobre fondo claro
el hueco no se distingue**; con la barra en blanco esto cambia entero ·
**la ETIQUETA VA DENTRO DEL DISCO**, no abajo sobre la barra · **el ÍCONO
CENTRADO** (hoy no lo está) · **el disco MÁS METIDO** («que salga muy
poco») · **la transición MÁS LENTA** (pedido explícito, para que se vea).

**📬 PARA B — ② EL PIN PASÓ SUS DOS PRUEBAS Y APARECIÓ LA TERCERA:
«ya se ve mucho mejor… ponele el MAGENTA de e-PetPlace, que no quede
como un pin negro».** **L-255 enmendada: se mide que SE APOYE, que SE
RECONOZCA y que SEA NUESTRO** — *un objeto del mundo que no lleva la
marca es de cualquiera.*
**🔴 Y TE LO SIRVO CON EL NÚMERO, porque tu diagnóstico anterior miraba
el eje equivocado y este dato lo mueve:** el pin **YA usa `mapaMoto`**
(`#681F4C`, medido en `PinEnMapa`) — o sea, **el magenta ESTÁ y se lee
negro igual.** Midiendo HSL: **`mapaMoto` sat 0.54 · LUM 0.26** ·
`magentaDark` sat 0.64 · **LUM 0.34** · `pink` sat 1.0 · lum 0.50.
**⇒ El problema NO es la saturación: es la LUMINOSIDAD.** Un color con
lum 0.26 a tamaño de pin sobre mapa claro se lee como negro, esté o no
dentro de la banda de saturación del terreno. **Tu banda era el criterio
correcto para «pertenece al mundo» e INSUFICIENTE para «se ve nuestro» —
es L-254 en el eje del color: un umbral que mide una cosa no mide la
otra.** Sugerencia con dato, tu llamada: subir LUMINOSIDAD conservando la
saturación en banda (un magenta ~0.34-0.40 de lum), o declarar que la
marca sale de la banda a propósito y decir por qué.

**📬 PARA C — ③ 🔴 ROJO NUEVO: LA VITRINA NO ESTÁ DONDE DEBERÍA.**
Verbatim: *«si voy a Atender y le doy Venta de productos, NO ME SALE LA
VITRINA: me sale "Agregar producto", y cuando le doy me sale una lista
desde una pantalla desplegable con todos los productos, pero SIN el
diseño de la vitrina.»* **No es una ruta mal apuntada: hay DOS listas del
mismo objeto y una no tiene diseño** — la clase del barrido de cariño
otra vez. Cruza con tu consolidación en cola: **la vitrina vive en STOCK,
y ATENDER → Venta de productos debe llevar ahí.**

**📬 PARA C — ④ EL ESCALÓN «UNA» QUEDÓ SIN JUZGAR** (el founder no tiene
a mano una cuenta con un solo servicio local). **Caminalo vos en aparato
con la cuenta que corresponda** — es de la clase que solo cierra el dedo,
**y el dedo puede ser el tuyo** (§6bis-B).

### 📱 OTA 9 — EL GATE 4, LAS DOS APPS CON EL MISMO ANCLA (18-ago noche)

- **PRESTADOR: group `5e6cc73e-0246-44bf-8a1e-e913a76bfc5f` · el pie debe
  decir `update 01a00b6c · preview`** · **CLIENTE: group
  `9f559bcd-91ca-4154-aa73-d478a8c7115c` · pie `update 01a00b6d ·
  preview`** *(esta vez los prefijos SÍ difieren en el 8.º char — pero la
  regla no cambia: **se distinguen por el GROUP**, D-785)* · **ancla
  `bccd6edf` en las dos · `dirty: None` en las dos**, leído del OBJETO ·
  ventana con árbol en cero.
- **CARGA VERIFICADA POR MERGE-BASE ANTES:** `bd239bfe` (B: la barra sin
  huella al 34 % en `bg.tinta` + el pin candidata D **verbatim** con solo
  el tratamiento + la casita con su sombra) · `77eb9ad6` (D: el escalón
  «una» sin ratonera) · `fd1b727e` (C: el buscador con autocompletar y la
  ventana con carga al final). **COMBINACIÓN verificada: tres typechecks
  verdes sobre B+C+D+A juntos.**
- **⚠️ UN OTA INTERMEDIO QUEDÓ SUPERSEDED Y SE DECLARA** (`d7c8d4eb`):
  salió con B+D y **sin C** — el pedido de la mesa llegó completo mientras
  la ventana ya estaba abierta. *Un publish incompleto no se esconde: se
  supersede y se dice, porque el pie del teléfono podría mostrarlo.*
- **🔴 ③ EL ESCALÓN «UNA» SOLO SE JUZGA EN APARATO, declarado al
  publicar:** D midió que **el toque de la tab no se puede simular en
  RN-web** (tres caminos probados, y descartó por `git stash` que fuera su
  cambio). **Ningún guard lo cubre** — es de la clase que solo el dedo
  cierra. Las otras tres tienen su pregunta: ① ¿ahora se HUNDE y no salta?
  · ② ¿ahora PARECE una moto? · ④ el buscador sobre los 399.

### 📊 EL CENSO DE `Encabezado` — CORRIDO Y SERVIDO A B (18-ago)

**Adjudicado a B** (la pieza vive en `packages/ui`, su territorio) — pero
el censo es un grep y se lo doy con el número puesto, para que su tanda
propia arranque con dato: **110 archivos montan `<Encabezado`** en
`apps/` + `packages/`, y **84 usos pasan `titulo=`**. *O sea: ~26 no lo
pasan — y ésa es exactamente la superficie donde el techo puede mentir
(L-244 en forma de API opcional: la pieza lo PERMITE).* **Con eso su
enmienda deja de ser «hacerlo obligatorio y ver qué rompe»: son ~26
sitios a mirar, no 110.**

### ✅ MI MITAD DE LA PROMESA — ENTREGADA (18-ago) + EL REPARTO DE LA VOZ

- **`promesa_por_vendedor(uuid[], date)`** (migración `20260818010000`) —
  **COMPONE, no re-deriva**: llama `calcular_promesa_despensa` por
  vendedor y junta. **POR VENDEDOR, jamás por producto** (la promesa
  depende de la CUENTA y el día: ~5 evaluaciones para toda la vitrina, no
  400). Techo 50 que rebota hablando. **Cinturón con los DOS caminos que
  la voz necesita distinguir** — el que promete (trae `fecha`, ventana y
  **`saltos_por_cupo`**, la señal de «no es el corte más cercano») y el
  que NO puede, que **DICE su razón tipada** (jamás un vacío, L-218).
  **E2E camino real: 200 · Despensa `fecha 2026-08-17 · saltos 1` —
  con dato VIVO ya se ve el corrimiento— · Aurora
  `sin_turnos_de_entrega`.** Wrapper `promesaPorVendedor` +
  `PromesaDeVendedor` en el barrel, con la razón de la firma en el JSDoc.
- **📬 LA VOZ ES DE C, CON RECETA DE B — y viaja con su razón adentro:**
  dice **QUÉ NO SE PUEDE Y QUÉ SÍ** (jamás solo el rechazo) y **TERMINA
  EN PREGUNTA, no en aviso** — la familia decide. *«No tenemos
  disponibilidad para entrega mañana en la mañana, te lo podríamos
  entregar en la tarde. ¿Querés continuar?»* **Y EL MOMENTO ES LA MITAD
  DE LA FIRMA: decirlo DESPUÉS de comprar es una disculpa; decirlo ANTES
  es información.** El dato para decirlo ya viaja: `saltos_por_cupo > 0`
  = el corte se corrió porque el vendedor está lleno.

### ⚖️ LA TANDA DE B — L-255 DEPOSITADA Y LOS TRES REGISTROS (18-ago)

- **🔴 L-255 (la clase nueva):** *una corrección puede romper lo que ya
  estaba firmado si no distingue qué se corrige de qué se conserva* — B
  reproyectó la silueta a tres cuartos ejecutando «perspectiva
  compartida», **y la firma decía que la candidata D quedaba firmada como
  silueta**. *Lo encontró ella releyendo la firma.* Con su corolario en
  las dos direcciones (la corrección NOMBRA qué no se toca · quien la
  ejecuta lee esa mitad primero) y el de medición que ya adoptó: **se
  mide que SE APOYE y se mide que SE RECONOZCA — dos pruebas.**
- **Al acta, el ojo del founder midiendo sin ver el número:** el disco iba
  de −46 a +6 con alza 20 contra radio 26 ⇒ **6 px de 52 adentro, el
  11 %** — *«el círculo queda prácticamente afuera»*, dicho sin ver la
  geometría. Ahora 10 contra 28 → 18 adentro, valle a 18.
- **Su lectura del «salta», que es la que vale conservar:** la mitad
  visible era GEOMETRÍA (*un objeto que vive casi afuera parece saltar
  aunque interpole*) y la otra era transporte, que sacó del camino
  poniendo disco y valle en el MISMO grupo. Su literal: **«una animación
  de dos capas donde la crítica cuelga de la frágil no es arriesgada: ya
  falló y todavía no lo sabés.»**
- **El techo — su lectura es correcta y va al acta:** *las dos pantallas
  no lo hicieron mal — hicieron algo que la pieza PERMITÍA.* Es **L-244
  en su forma de API opcional** (`Encabezado` no EXIGE el nombre).
  **ADJUDICADO: el censo de consumidores de `Encabezado` es SUYO** —
  la pieza es de `packages/ui`, su territorio, y la enmienda (hacer el
  nombre obligatorio) se decide con el número de consumidores delante.
  *Yo entro solo si el censo destapa motor, que hoy no se ve.*
- **El umbral pasa a medir DISCRIMINACIÓN** (L-254 ya depositada), con su
  literal: **«hay suficientes» y «sirve» son preguntas distintas, y
  contestó la primera creyendo que contestaba la segunda.**

### ✅🔴 EL CUPO NO ES PUERTA: DETERMINA LA PROMESA (corrección del founder, 18-ago — enmienda la adenda de abajo) — Y EL MOTOR YA SABE HACERLO

**FIRMA VERBATIM:** *«lo que va a cambiar no es que el producto esté o no;
si tienes stock se va a mostrar. Lo que le va a cambiar a la persona es LA
FECHA en la que va a recibirlo… y se le dice honestamente: "no tenemos
disponibilidad para entrega mañana en la mañana, te lo podríamos entregar
en la tarde. ¿Querés continuar?"»*

**La distinción que corrige a la mesa: EL CUPO NO DICE «NO PUEDO», DICE
«NO PUEDO HOY».** Un vendedor lleno hoy tiene cupo mañana — *sacar el
producto sería mentir sobre la disponibilidad para ahorrarle a la familia
una decisión que sabe tomar.* **LA CADENA QUEDA: PUERTAS ① stock · ②
zona, NADA MÁS · ORDEN ③ calificación · ④ antigüedad · ⑤ ventana de
arranque · y EL CUPO NO FILTRA: DETERMINA LA PROMESA** (el vendedor lleno
compite igual; cambia el corte que trae su oferta). **☠️ Muere la pregunta
que la mesa había servido** («si todos están llenos, ¿el producto sale?»)
— *se sirvió, y la firma la resolvió por otro camino: el producto nunca
sale por cupo.*

**🟢 EL CENSO — Y LA NOTICIA ES BUENA: EL MOTOR YA CAMINA LOS CORTES.**
Medido en el cuerpo vivo de **`calcular_promesa_despensa`** (el nombre
real — no existe `calcular_promesa_entrega`): **① consulta
`cupo_reparto_del_dia` · ② si el día está lleno SALTA al siguiente con
capacidad** (loop, techo 14 días — *«donde 'no hay cupo' deja de ser un
corrimiento y pasa a ser un vendedor sin capacidad»*) **· ③ devuelve
`saltos_por_cupo`**, que es EXACTAMENTE la señal que la voz necesita para
decir «no mañana, sino el X» · ④ y ya distingue sus bordes con error
tipado (`sin_turnos_de_entrega` · `sin_cupo_ese_dia` ·
`sin_capacidad_de_reparto`). **⇒ ESTO ES VOZ Y SUPERFICIE, NO MOTOR.**

**LO QUE SÍ FALTA, con su nombre exacto: EL MOMENTO.** La promesa se
computa **al CHECKOUT** (ahí se pueblan `promesa_entrega_desde/hasta`, y
por eso 9/11 pedidos vivos la tienen) — **y la firma dice ANTES DE
COMPRAR.** *Decirlo después es una disculpa; decirlo antes es información,
y la persona todavía puede elegir.* **La cadena de trabajo:** el lector de
la vitrina/ficha llama `calcular_promesa_despensa` **por VENDEDOR** (no
por producto — el mismo hallazgo de costo: escala con vendedores, ~5 hoy)
· la voz dice **QUÉ NO SE PUEDE Y QUÉ SÍ**, jamás solo el rechazo · y
**termina en PREGUNTA, no en aviso: la familia decide.** Es la Ley 23 en
su forma completa. **Mi parte (lector con la promesa por vendedor) es
BARATA y la tomo cuando la mesa reparta la voz/superficie.**

**Al acta, como la mesa lo dictó:** *la mesa proponía sacar el producto de
la vitrina cuando todos estuvieran llenos; el founder corrigió con una
distinción mejor —el cupo no dice «no puedo», dice «no puedo hoy»— y la
solución honesta no era esconder el producto sino DECIR LA FECHA REAL. Es
la ley de la casa aplicada donde la mesa la había perdido: el producto no
miente ni calla, dice la verdad de hoy.*

### ~~(ENMENDADA por la corrección de arriba)~~ LA TERCERA PUERTA — CUPO DE DESPACHO (adenda del founder, 18-ago):
«puede ser que un seller tenga todo para ganarse el pedido, pero ya esté
llena su capacidad de despacho»** ⇒ entra como PUERTA, no como criterio:
sin cupo NO PUEDE CUMPLIR, así que no compite (Ley 23 aplicada al
concurso). ~~La cadena queda: ① stock · ② zona · ③ CUPO — las tres PUERTAS~~ **⚠️ ENMENDADO: el cupo NO es puerta — determina la PROMESA (ver arriba). La cadena vigente: PUERTAS ① stock · ② zona · ORDEN ③ calificación · ④ antigüedad · ⑤ ventana.**

**🔴 EL COSTO, MEDIDO — Y ES EL HALLAZGO QUE LA VUELVE BARATA: la puerta
del cupo NO escala con productos, escala con VENDEDORES.** `cupo_reparto_
del_dia` es **por cuenta y por fecha** — con ~400 productos en vitrina se
evalúa **UNA vez por vendedor**, no una por producto: **5 evaluaciones,
~5 ms acumulados para las cinco cuentas** (medido con `clock_timestamp`
contra el motor vivo). Se resuelve como un CTE de N filas que se cruza
con todo el catálogo. *El «puede ser caro» que la adenda temía no
aparece: lo caro habría sido evaluarlo por producto, y el modelo del
cupo no lo pide.*

**LO QUE SÍ CAMBIA EL DISEÑO, y la adenda lo nombró bien: EL CUPO SE
AGOTA DURANTE EL DÍA** (el stock cambia lento, la zona no cambia, **el
cupo cambia con cada pedido**) ⇒ **el ordenamiento NO se puede cachear
por día — se evalúa contra el cupo VIVO en cada lectura** (y por eso el
número de arriba importa: es barato hacerlo por request). *La vitrina de
las 9 no es la de las 18, y eso es correcto: un vendedor lleno no puede
entregarte HOY.*

**LA PREGUNTA DE PRODUCTO, SERVIDA CON EL VOTO DE MESA Y SU
CONSECUENCIA DECLARADA:** cuando el mejor se llena, **el producto PASA AL
SIGUIENTE** (voto de mesa — es para lo que la cadena existe) ⇒ **la
familia puede ver el mismo producto a DISTINTO PRECIO según la hora, y
eso se declara, no se descubre.** *Mi nota a esa consecuencia: si el
precio cambia entre que la familia mira y compra, el que vale es el del
CHECKOUT — la casa ya tiene esa regla (la escalera del precio honesto,
S61) y acá aplica igual; lo que hay que evitar es que la vitrina prometa
un precio que el carrito no respeta.*

**EL CASO EXTREMO, confirmado con el dato: si TODOS los vendedores de un
producto están llenos, el producto SALE de la vitrina** — misma regla que
el stock (y misma excepción: aparece solo si el cliente lo TIPEA por
nombre). El dato lo permite (`disponible = capacidad − consumido`, ya
computado) y **cuesta lo mismo que la puerta**: cero evaluaciones extra.
⚠️ *Y su borde, declarado: hoy `capacidad` sale de `recursos_reparto`
(D-837) — un vendedor SIN repartidor configurado tiene capacidad 0, así
que la puerta lo sacaría de TODA la vitrina. Medido: 4 de 5 cuentas
tienen recurso; Aurora tiene 20 de capacidad. **Antes de encender esta
puerta hay que decidir qué pasa con el vendedor que no configuró
reparto** — sale de la vitrina, o se lo trata como «sin límite» hasta que
configure. Es decisión de mesa, no de código.*

**Y LA GUARDA DE LA CASA, ratificada y ANOTADA EN EL CONTRATO DEL
LECTOR:** el vendedor **jamás** ve su posición, su percentil ni su
comparación con otros (N18, no se enmienda). Puede ver **su** calificación
y **su** progreso — nunca su lugar en una fila. *Consecuencia técnica que
me toca: el lector de la selección NO devuelve el ranking al panel del
vendedor — devuelve, a lo sumo, si su oferta está visible hoy y por qué
no lo estaría (el hueco/veredicto de N18), jamás «sos el 4.º de 7».*

**⚠️ Y EL COSTO EN LA LECTURA, declarado antes de que alguien lo pida:**
el orden se computa **por familia y por producto** — con ~400 productos
en vitrina, hacerlo en el cliente es N cómputos por scroll. **La cura es
resolverlo SERVER-SIDE en el lector** (una función que ya reciba la
ciudad de la familia y devuelva la oferta ganadora por producto), y eso
es trabajo mío con la firma del criterio. *Hoy el lector devuelve una
fila por oferta: sin selección, la vitrina repetiría el producto N veces
— por eso el criterio provisional no es opcional para octubre.*

### (registro) EL ④ DEL GATE 3 — EL CHOQUE, SERVIDO ANTES DE LA FIRMA (18-ago noche)

**🔴 EL FRENO, medido ANTES de sembrar (freno ④ del LOOP — contradecir
letra firmada):** `uq_oferta_publicada_por_variante` =
**`UNIQUE(variante_id) WHERE estado='publicada'`** ⇒ **dos ofertas
publicadas del mismo producto son INEXPRESABLES hoy.** Y **no es un
descuido: implementa la FIRMA MADRE ③ de S94** (`MODELO_DESPENSA`:
*«UNA sola oferta visible por producto — la vitrina la cura
e-PetPlace»*, con su razón: *la despensa no compite por precio, compite
por criterio*). **El índice NO se tocó.**

**🟡 LA LECTURA FINA QUE LA MESA TIENE QUE ARBITRAR, y es el corazón del
asunto: la firma habla de VISIBLE; el índice cerró EXISTENTE.** Son dos
rigores distintos — se puede tener N ofertas en la base y mostrar UNA en
la vitrina (curaduría), que es lo que la letra literalmente dice. **El
rigor de la BASE nadie lo decidió: lo eligió el índice.** Si la mesa
quiere competencia real, la cura NO es borrar el índice: es moverlo al
lugar correcto (`UNIQUE(cuenta, variante)`) **y escribir el criterio de
cuál gana la vitrina — que hoy NO EXISTE** (ni columna de ranking, ni
regla escrita).

**LAS TRES PREGUNTAS QUE EL FOUNDER YA INTUYÓ, servidas sin resolver:**
① ¿la vitrina muestra el producto UNA vez o una por vendedor? · ② si es
una, **¿cuál oferta gana y con qué criterio?** (precio · stock ·
reputación · cercanía — ninguno existe hoy) · ③ ¿el espejo de cada
vendedor muestra solo lo suyo? *(El ③ ya tiene respuesta parcial: sí —
`listarSkusDelVendedor` filtra por cuenta.)*

**LO QUE SÍ SE SEMBRÓ, y deja el choque VISIBLE en vez de esconderlo**
(`supabase/dev/seed-siembra-multiseller-s99.sql`):
- **① MULTI-SELLER REAL EN LA VITRINA** — las variantes libres se
  repartieron y publicaron: **hoy la vitrina tiene CINCO vendedores**
  (452 · 26 · 26 · 17 · 17 ofertas publicadas). *Es la primera vez que la
  vitrina ejerce multi-vendedor.*
- **② EL ESPEJO DEL CHOQUE — 96 SKUs**: cada cuenta tiene productos que
  OTRO ya publicó, con su precio y su stock propios y **sin oferta
  publicada porque el índice lo impide**. *Eso hace ver la pregunta en
  pantalla: «tengo el producto, tengo stock, y no está en la vitrina
  porque otro lo publicó primero».*
- **③ STOCK DISTINTO POR VENDEDOR a propósito** (11·17·23·29 + variación,
  160 SKUs con stock) — **entrando POR EL LEDGER** (`ajustar_stock_
  vendedor`, motivo obligatorio), jamás UPDATE a la materializada.
- **D-838 SE EXTIENDE** a esta siembra: misma marca contable
  (`SIEMBRA-S99-%`), mismo cierre por conteo-cero, misma muerte antes del
  primer vendedor real. *(Nota del founder ratificando la deuda: «igual yo
  lo tengo superpresente».)*

### 📬 RESPUESTAS DE LA TANDA DE C (18-ago noche)

- **① SU BLOQUEANTE YA ESTÁ CURADO Y ÉL LO TIENE — medido, no dicho:**
  `momentos_aplicables` está en `ProductoDeVitrina` (tipo, select y map),
  el commit `a61409b0` está en main **y su rama ya lo contiene**. Su
  reporte salió de un árbol previo al merge (mismo patrón que su freno de
  `pago_confirmado_en`). **Puede construir el segundo eje ya.**
- **② SU HALLAZGO ES LEY: L-254 depositada** — *un umbral que mide TAMAÑO
  no mide UTILIDAD* (perro 232 con 121 comodines ⇒ «cachorro» devuelve el
  64 %), con el corolario (medir DISCRIMINACIÓN, no tamaño) y la
  interacción escrita: **dos decisiones correctas que se estorban — lo
  resuelve el umbral, jamás el comodín.** **Va a B**, que es la dueña de
  esa receta. Y al acta la nota de método: **L-248 disparó CON alguien
  mirando** porque el founder pidió volumen antes de juzgar.
- **③ LA LEY DE N20 GANA SUS 44 CASOS** — anotado en el plan: ya no
  depende del cruce que la descubrió.
- **④ D-837 CERRADA DE LOS DOS LADOS** (motor mío + su pantalla), con su
  literal al acta: ***no había recursos que agregar — había personas a
  las que preguntarles cuánto llevan.***
- **⑤ Su declaración de no hacer la fusión Stock+vitrina a medias:
  RATIFICADA** — dos listas del mismo objeto conviviendo es peor que
  entrarle con la cabeza fresca.

### 📱 OTA 8 — EL GATE 3 PUBLICADO, LAS DOS APPS CON EL MISMO ANCLA (18-ago noche)

- **PRESTADOR: group `f1a8a14a-0a41-4499-b9a7-11043ec73976` · pie
  `update 01a00b47 · preview`** · **CLIENTE: group
  `765959a9-9eab-4596-a715-ef70e0c7f938` · pie `update 01a00b47 ·
  preview`** (los updateId comparten prefijo POR DISEÑO — UUIDv7, D-785:
  **se distinguen por el group, no por los 8 chars**) · **ancla
  `9f66a7b7` en las DOS, `dirty: None` en las dos, leído del OBJETO** con
  `update:view --json`. Ventana con árbol EN CERO (el `settings.json`
  local salió por stash y volvió intacto).
- **CARGA VERIFICADA POR MERGE-BASE ANTES DE PUBLICAR** (la regla, tercer
  estreno): `042df061` (la barra) · `671e5372` (el pin + destino) ·
  `ea428399` (las curas de C) · `13ac2ecf` (el $/kg de D) · `92785241`
  (mis lectores) — los cinco ancestros del ancla. **Y la COMBINACIÓN
  verificada, no las ramas: tres typechecks verdes sobre B+C+D+A juntos.**
- **① LA BARRA — 🔴 EL LÍMITE, DECLARADO COMO LA MESA PIDIÓ: las dos
  variantes son alcanzables SOLO EN LA GALERÍA** (`GateDeLaBarra
  conHuella` / `conHuella={false}`, montadas una encima de la otra en los
  dos temas — **ahí sí hay comparación, que es lo que el gate necesita**).
  **En la app REAL corre una sola: `huellaEnDisco` tiene default `true` y
  CERO consumidores en `apps/` — la barra viva va CON huella y no se
  puede conmutar sin recompilar.** *Es correcto para juzgar la FORMA (la
  galería las pone lado a lado, que es el juicio que la firma pedía) y es
  INSUFICIENTE para juzgarla EN USO. Si el founder quiere ver la app
  entera con y sin huella, eso es una perilla de gate en la app — pedido
  a B, no algo que A improvise sobre su pieza.*
- **② EL PIN — completo:** `LienzoMapa` con tiles reales, **moto Y
  destino juntos** (el juicio es la comparación) y **el «antes»
  rechazado al lado** con su rótulo. Camino: Cuenta → pie → galería.
- **③ LA VITRINA — 399 productos comprables** con los estados del espejo
  poblados, en el OTA del CLIENTE (runtime 1.0.3) para filtros,
  despliegue y búsqueda con volumen real.
- **📬 PARA D — TU FRENO ESTÁ RESUELTO:** `pago_confirmado_en` llega a
  `PedidoDelVendedorConDia` (medido en select Y map, los dos lectores) y
  **tu rama ya contiene `7bcf3370`**. Podés montar el orden **sin
  proxy**; tu lectura nace `?? null` (L-247).
- **📬 PARA B — LO QUE TU PROPIA DECISIÓN PEDÍA:** «sin paginación, sin
  cargar más, sin encabezados» se decidió con **11** productos. **La
  vitrina publicada tiene ~400 y el founder la va a ver ANTES que vos** —
  la decisión SE RE-MIDE con volumen real (grilla y lista), no se
  defiende con el número viejo.

### ✅ D-836 CERRADA (cura de D, mergeada `45dbaeaa`) + L-253 + LA TRANSICIÓN A B (18-ago noche)

- **D-836 CERRADA:** `/pedidos` declarada en el navegador de tabs (el
  molde de la casa; ruta intacta, cero enlaces cambiados), verde en tres
  poblaciones. Ficha cerrada con la lectura de D al acta (*un cuarto que
  perdió el pasillo — el pasillo que él mismo acababa de construir*).
- **L-253 depositada** (dos pantallas en el mismo DOM: el objeto contenía
  DOS cosas y el instrumento no las separaba) con el corolario de D
  («medí lo que construí, no lo que la persona hace con eso» — todo guard
  de navegación mide QUÉ QUEDA ALREDEDOR, con dos anclas).
- **📬 PARA B — LA DIRECCIÓN DEL MOVIMIENTO VOLVIÓ A QUEDAR SIN DUEÑO, y
  el patrón ya es de DOS veces:** la cura del encierro cambió el SUSTRATO
  del gesto — con la hermana adentro del navegador, cruzar ya no es
  push/pop, y la corrección ③ del founder («desde pedidos hacia citas
  debe SENTIRSE QUE REGRESA») se apoyaba en el POP. Tu receta anterior
  (simetría exacta, mismo número signo invertido, crossfade con
  reduce-motion) SIGUE SIENDO LA LETRA — lo que cambió es sobre qué se
  monta. **Y el patrón, anotado como la mesa lo dictó: es la segunda vez
  que la dirección del cruce se pierde por cambio de sustrato**
  (navigate-con-params que no era POP · el navegador de tabs) — **LA
  DIRECCIÓN DEL MOVIMIENTO NO PUEDE DEPENDER DEL MECANISMO DE
  NAVEGACIÓN: se deriva del GESTO, no de la pila.** D hizo lo correcto al
  no inventar una animación — es tuya y es una pregunta DISTINTA de la
  que ya contestaste.
- **④ El freno de D sobre el orden — VERIFICADO Y RESUELTO del objeto:**
  `pago_confirmado_en` está en los DOS lectores (select Y map — plano
  línea 68/96, EnRango 150/180) y **su rama YA CONTIENE `7bcf3370`** (el
  commit que lo trajo). El freno era correcto como principio; el dato
  LLEGA a `PedidoDelVendedorConDia` — cuando monte el orden, su lectura
  nace `?? null` (L-247). Cero proxy necesario.

### 🔴 LA CAMINATA DE C — REPARTIDA; Y D-837 MOTOR PAGADO EN LA MISMA TANDA (18-ago noche)

- **📬 PARA C — EL ROJO PRIMERO, ALTO y ANTES de las mejoras de forma:**
  **Marco se pinta EXACTAMENTE igual que Diego y no lo es** — `user_id`
  null ⇒ el guard #9 rebota TODO despacho (probado en Gate 2), y la fila
  no lo dice: el vendedor va a asignar y rebotar sin entender. **El dato
  YA VIAJA** (`Repartidor.user_id`, null = sin cuenta) — cura de
  superficie pura. *La clase, depositada con su quinta muestra: un motor
  que rebota bien y una pantalla que no anticipa el rebote son EL MISMO
  DEFECTO visto de los dos lados — Ley 23 lo cubre (no se ofrece lo que
  se va a rechazar) y acá se ofrece. La más cara por frecuencia: CADA
  repartidor nuevo pasa por ese estado; en octubre es un vendedor
  confundido por semana.* El guard NO se toca.
- **⚙️ D-837 — MOTOR PAGADO (migración `20260817230000`), la pantalla de
  C ya no miente sobre el modelo:** `repartidor_id` en `recursos_reparto`
  + backfill solo-inequívoco (3 atados; **Aurora NULL a propósito** — la
  ambigüedad es del vendedor) + `configurar_capacidad_repartidor` (la
  firma S96 hecha puerta: propio → ADOPTA el suelto → crea; jamás
  duplica) + cupo INTACTO por cinturón (idéntico cuenta por cuenta;
  adopción probada y revertida; **medido sobre el LUNES — el primer
  intento cayó en que hoy domingo el cupo es 0 legítimo, y el cinturón me
  cazó a MÍ dos veces: también esperaba 4 atados donde el 1×1 real da
  3**). Lector `Repartidor.capacidad` + wrapper
  `configurarCapacidadRepartidor` en el barrel. **Al acta el literal de
  C: «la pantalla está describiendo con fidelidad un modelo
  equivocado»** — el ① de su caminata cambió de dueño al medirlo.
- **📬 PARA D — EL FRENO DE LA RATONERA (del ③ de C, con el montaje que
  es tuyo):** el escalón «una» de L-251 **NO se monta con un Redirect
  dentro de la tab** — si la tab redirige en cada foco, el atrás del
  destino vuelve a la tab y re-redirige: **el back queda en una
  ratonera** (primo de L-249 y de tu D-836). El `escalonDeAtender()` de C
  está listo (6/6); **EL DESTINO LO DECIDE LA BARRA, que es del cascarón
  — el montaje es una línea tuya, con este freno adentro.**
- **El resto de la caminata, registrado con su reparto:** cerrar-sesión
  sale de configuración · el interruptor «Atiendo en mi local» no existía
  ahí (mi perilla ya está entregada) · el chip «Activa» que contradice al
  párrafo — los tres en la misma pasada de C · «Agregar corte» siempre
  visible · la fila que no dice que es editable · el dato de la fila es
  el DOCUMENTO cuando el vendedor reconoce por VEHÍCULO o WHATSAPP — los
  tres con la gramática de B, no antes. **Y lo que NO se rehace, medido
  por C:** el corte ya lista y reabre (Aurora tiene CERO cortes — la
  pantalla no esconde nada) y la fila del repartidor ya abre su ficha.

### ✅ LA SIEMBRA MASIVA — SEMBRADA CON SU DEUDA ESCRITA ANTES (18-ago noche; D-838)

**El resultado, medido del objeto:** 525 SKUs sembrados en la cuenta
borrable `eec12ef3` por las MISMAS puertas del vendedor real — **445
publicados** (la vitrina pasa de 13 a **399 productos comprables**) · 27
en revisión · **27 rechazados con motivo** · 26 sin precio — **los
estados de la receta 7 de B, POBLADOS: veredicto y hueco por fin tienen
dato con qué verse.** Ledger de la siembra: **CERO** (stock 0, L-231).
Cero fotos inventadas — la vitrina real muestra la realidad del canónico
(dos tercios sin portada), que es exactamente lo que el founder tiene
que ver. Los dos ejes reparten con el reparto real del canónico
(multi-especie y comodines incluidos — el 14>13 y el momento-NULL ya se
pueden VER funcionando). **📬 PARA B, el aviso que tu decisión pedía: «sin
paginación, sin cargar más» se decidió con 11 productos — con ~400 en la
vitrina SE RE-MIDE** (la grilla y la lista con volumen real). **D-838**
con la marca contable (`SIEMBRA-S99-%` = 0 al cierre, medido) y muerte
antes del primer vendedor real.

### ✒️ LAS DOS FIRMAS DE LA BARRA + L-252 (18-ago — depositadas en su letra)

- **① La huella VIVE DENTRO DEL DISCO** — firmada CON salida declarada
  (las dos ramas por adelantado: si se ve mal, se quita la huella y gana
  el dinamismo). En `DIRECCION_ARTE` §6quinquies con la consecuencia
  operativa (B construye LAS DOS variantes y las trae juntas — no
  adivina ni re-pide firma) y el criterio del gate escrito (se LEE como
  huella a ese tamaño, o es ruido).
- **② ☠️ ATENDER deja de estar DESTACADA** — lápida en `LA_CASA` con su
  razón (L-251: un tab que puede no existir no es centro de gravedad
  permanente). **El 🟡 del Gate 1 CERRADO en su hoja** — murió por
  composición Y por forma.
- **③ L-252 depositada** (el hueco se RECORTA, no se pinta — toda pieza
  que depende del color del fondo; el modo de falla es el peor: funciona
  justo donde se probó).
- **④ La tanda ① de B, ratificada con su registro:** «el color pertenece
  al terreno» pasó de criterio a NÚMERO — saturación del mapa real
  medida (banda 0.10–0.58), los tres de marca afuera (pink 1.00 ·
  tealDark 0.84 · magentaDark 0.64), **nace `mapaMoto` CALCULADO para
  caer adentro (0.54, contraste 6.61)** — no se eligió un color bonito:
  se calculó el que entra en el mundo. Y las dos AUSENCIAS que son la
  pieza: sin anillo y SIN boxShadow (la sombra de un objeto del mundo es
  elipse en el suelo, no sombra de tarjeta). Su declaración de NO
  arrancar ②③ a medias, ratificada — declarar gana a dejar una escalera
  a mitad de camino en una pieza que tres pantallas montan.

### 📬 PARA B — LA BARRA LÍQUIDA: EL MECANISMO MEDIDO DEL VIDEO (18-ago tarde; SUPERSEDE a la lectura de la muesca de abajo)

**FIRMA: «me encanta este efecto de animación.»** La mesa extrajo el
video CUADRO POR CUADRO (60fps) — esto es medido, no descrito de memoria;
*la lectura anterior («una muesca restada») era de un cuadro fijo y
estaba incompleta: hay valle Y disco separado, no una sustracción.*

**EL MECANISMO:**
- Barra **PÍLDORA FLOTANTE** — márgenes laterales, radio grande, no
  pegada al borde.
- En el tab activo, el borde superior se hunde en un **VALLE CÓNCAVO**
  con hombros en S a los dos lados.
- Sobre el valle flota un **DISCO DEL MISMO COLOR QUE LA BARRA, separado
  por un hueco DEL COLOR DEL FONDO** — el hueco ES el efecto: el disco no
  está encima; parece que la barra BURBUJEÓ y se desprendió.
- El ícono activo va DENTRO del disco y EN COLOR; los inactivos, planos
  sobre la barra.
- **🔴 LO QUE LO HACE LÍQUIDO: al cambiar de tab, VALLE Y DISCO VIAJAN
  JUNTOS y el valle se deforma ASIMÉTRICAMENTE en el camino — LA BARRA ES
  UN SOLO VECTOR QUE SE DEFORMA**, no un botón que salta. En los cuadros
  intermedios se ve el disco a mitad de camino con el valle torcido.

**LO QUE RESOLVÉS, con tu poder de rojo y su guarda (nombrás la ley o es
sugerencia):**
- **🔴 DOS MARCADORES COMPITEN** — la HUELLA es la marca del activo con
  ley propia (§2.6). El disco elevado es un segundo marcador: uno gana o
  muere. *El camino que la mesa exploraría: la HUELLA VIVE DENTRO DEL
  DISCO. Pero es TU llamada.*
- **🔴 EL CHOQUE QUE LA REFERENCIA NO TIENE Y NOSOTROS SÍ:** ATENDER está
  DESTACADA de forma permanente — si el activo también es disco elevado,
  cuando ATENDER no esté activa hay DOS cosas pidiendo ser el centro. La
  referencia tiene tres tabs iguales; nuestra barra no.
- **LA BARRA SE COMPONE POR CAPACIDAD** (3/4/5 destinos, y con L-251
  ATENDER puede no existir): el valle viaja entre posiciones VARIABLES.
- **EL VALLE COME ESPACIO VERTICAL** sobre la barra, en zona de contenido.
- **El color:** la referencia es negra con ámbar; el teal oscuro es del
  prestador.
- **🔴 LA ADVERTENCIA NUEVA, que solo aparece con el mecanismo correcto:
  EL HUECO ES DEL COLOR DEL FONDO ⇒ la pieza CONOCE el fondo sobre el
  que se apoya.** Si el fondo cambia (tema · memorial · una pantalla con
  otra capa), el hueco deja de recortar y el efecto muere o se ensucia.
  **Declarás cómo se resuelve, o por qué no aplica.**
- **Reduce-motion por el criterio ya escrito:** la persona pidió cambiar
  de tab, no el barrido — la deformación se DEGRADA, el cambio SE QUEDA.

**L-241:** se verifica DONDE VIVE — la app corriendo, con al menos DOS
poblaciones de barra distintas. Jamás lámina.

### (superseded por el de arriba — lectura de un cuadro fijo) LA REFERENCIA DE LA BARRA: LA MUESCA SUSTRACTIVA (18-ago, del founder — CON LA ADVERTENCIA AL FRENTE)

~~**La referencia (llegó como FRAME, no video):** el tab activo es un
CÍRCULO RESTADO DEL CONTORNO de la barra — una MUESCA en el borde
superior donde el círculo entra, no un botón apoyado encima. **El
mecanismo es SUSTRACCIÓN, no superposición** — y de ahí el movimiento: si
la muesca es parte del contorno, al cambiar de tab SE DESLIZA en vez de
saltar.~~ **🔴 ENMENDADO (orden de mesa, 18-ago): esta lectura era de un
CUADRO FIJO y el video la corrigió — NO es una muesca restada: son DOS
formas (valle + disco) con un hueco entre ellas. El mecanismo real vive
en la sección de arriba. La lectura vieja se conserva tachada porque
explica por qué la nueva importa: un frame dice forma; el movimiento
dice mecanismo.** **Lo que la mesa NO pudo ver** (trabajás sobre el mecanismo, no
sobre el detalle que falta): si la muesca viaja con la curva completa o
se desvanece y reaparece · si hay rebote al llegar · qué hace el ícono
activo mientras se mueve.

**🔴 LA ADVERTENCIA, AL FRENTE — la barra es la pieza más cargada de ley
de la casa. Serví qué es adoptable y qué rompe algo firmado, CON TU PODER
DE ROJO Y SU GUARDA (nombrás la ley o es sugerencia):**
- **LA HUELLA ya es la marca del tab activo y tiene ley propia** (§2.6,
  `estadoPorHuella`). La muesca es una SEGUNDA forma de marcar lo mismo —
  dos marcadores compiten: uno gana o muere. **No se agregan los dos.**
- **LA MUESCA RESERVA ESPACIO PERMANENTE** arriba de la barra, en la zona
  del contenido — con la escala de la casa, MEDÍ qué pierde el contenido.
- **🔴 LA QUE NADIE VE MIRANDO LA REFERENCIA: NUESTRA BARRA SE COMPONE
  POR CAPACIDAD.** La muesca viaja entre posiciones que CAMBIAN por
  negocio — cinco, tres, y con L-251 el número de destinos es VARIABLE
  (ATENDER puede no existir). Una muesca calculada para cinco posiciones
  fijas se rompe con el primer vendedor puro. **Tu receta lo resuelve o
  declara que la referencia NO es adoptable, con esa razón.**
- **L-241:** se verifica DONDE VIVE — la app corriendo, con al menos DOS
  poblaciones distintas de barra. Jamás lámina.

### 🔴 📬 PARA B — EL PIN RECHAZADO EN GALERÍA: LA FÍSICA QUE FALTABA YA ESTÁ EN §6ter (18-ago, firma del founder)

**El diagnóstico NO es el dibujo: es la GRAMÁTICA.** «El mapa es MUNDO»
estaba firmado (§6ter, esta sesión) y la pieza seguía siendo un glifo de
interfaz sobre el mundo — un símbolo plano en un círculo es correcto como
ícono, y por eso se ve pegado. **La enmienda vive en `DIRECCION_ARTE`
§6ter con el verbatim y la física entera:** sombra en el suelo ·
perspectiva compartida (tres cuartos, la inclinación del mapa) · color
del terreno · el destino es EDIFICIO, no marcador. **La orden: la moto NO
se ajusta — se RE-DIBUJA como objeto del mundo, y con ella nace el
destino.** La candidata D sigue firmada como SILUETA; cambia el
tratamiento. Se juzga por L-241 sobre `LienzoMapa` con tiles reales, los
DOS objetos juntos — el juicio es la comparación. **Al acta: segunda
falla del mismo ícono por el mismo eje** (lugar equivocado → gramática
equivocada); el brief corregido tenía media verdad y la mitad que faltaba
ya tiene nombre en la ley.

### 🎨 EL SEGUIMIENTO ESTILO RAPPI (18-ago, dictado con capturas) — DEPOSITADO; MI CENSO CERRADO EN LA MISMA PASADA

**En `PLAN_S99` entero (verbatim + lo-que-se-lee + lo-que-NO-se-copia,
incluida la DECISIÓN del pin de entrega ajena: dato de otra persona en la
pantalla de alguien — no entra).** Reparto: **B** la receta (escalera de
NODOS con íconos — los nodos son las NARRATIVAS, jamás estados internos —
+ pin de destino con L-242 sin anillo + composición), verificada sobre
`LienzoMapa` con tiles reales (L-241), jamás lámina · **D** monta · **mi
censo, CERRADO:** el rango de entrega TIENE dato y ya viaja —
`promesa_entrega_desde/hasta` en 9/11 pedidos vivos, poblado por
`calcular_promesa_entrega` al checkout, y el lector de seguimiento del
cliente YA lo expone. **Cero motor nuevo**; los 2 sin rango son la
degradación honesta de la forma.

### 🔴 EL DICTADO DEL FOUNDER (18-ago) — DEPOSITADO ENTERO EN EL PLAN, REPARTIDO, Y MI MITAD EJECUTADA

**El dictado vive verbatim en `PLAN_S99` (bloque 18-ago) — acá el reparto
y lo ejecutado:**

- **📬 PARA D — D-836 🔴 ALTA:** se pierden los cuatro tabs en pedidos
  (verbatim en la ficha). Encierro clase L-249, y peor: una ventana de la
  casa que perdió la casa. Rompe la navegación del dual firmado.
- **📬 PARA C — TRES cosas, en orden:** ① **la caminata de configuración
  ANTES de mejorar** (orden literal del founder: entrar, caminar, traer
  hallazgos) — con la gramática de configuración de B como toque 1 · ②
  el módulo de venta re-compuesto (pedidos al HOY · mostrador →
  interruptor · stock → inventario vivo · configuración muestra ESTADO
  con lápices · cerrar sesión sale de ahí) · ③ **la composición de
  ATENDER por los tres escalones** (L-251) — tu insumo YA ESTÁ en el
  contexto: `oficiosLocales` (la lista, cuenta y nombra) +
  `ventaMostradorActiva` (fresca) + `esVendedora`; capacidades =
  oficiosLocales ∪ (esVendedora && ventaMostradorActiva ? mostrador : ∅).
  Con cero no hay tab; con una, pantalla directa con su nombre; con 2+,
  baldosas. **El 🟡 del Gate 1 murió por composición.**
- **📬 PARA B — receta nueva: la conmutación LISTA ⇄ ÍCONOS de Stock**
  (estilo Finder, verbatim del founder en el plan). Compatible con tu
  «sin paginación, sin cargar más, sin encabezados» — no lo reabre. Y tu
  frente de configuración ya abierto (§5bis + HOY vacío + este dictado)
  son el mismo vendedor el mismo día.
- **⚙️ MI MITAD EJECUTADA (migración `20260817220000`):** nace
  `cuentas_comerciales.venta_mostrador_activa` (la perilla de PRODUCTOS
  que NO existía — medido: cero columnas, 0 reglas retiro;
  `registrar_venta_mostrador` era motor que nada componía) + backfill
  HONESTO (las 2 cuentas que YA vendían por retiro nacen prendidas — una
  migración no quita la pantalla que se usa, clase D-662) +
  `configurar_venta_mostrador` (gate `es_vendedor_de`, L-140) + **el
  contexto de arranque gana `oficios_locales` (LA LISTA — mismo cruce de
  siempre; `hay_oficio_local` pasa a DERIVARSE de ella: una fuente) y
  `venta_mostrador_activa` fresca.** Cinturón entero verde (backfill
  exacto 2 · toggle fresco por camino de rol · ajeno rebota · lista por
  camino independiente: Aurora 7 oficios locales por dos vías · derivado
  == lista · L-140). Wrappers `configurarVentaMostrador` + campos
  fail-closed en `ContextoArranque` (L-247), barrel mismo commit.
- **⑤ EL REORDEN DE LOTES, DECLARADO EN EL PLAN:** L5a y L5b dejan de
  ser secuenciales — **son UNA pantalla (STOCK) con sus acciones**; el
  espejo se ANCLA ahí; la carga Excel ES L5a con motor ya entregado; la
  confirmación previa es N13 literal; **el PDF-factura es caso NUEVO de
  IA: §14 le aplica, POST-15-SEP sin cambio.** **D-837** depositada (la
  capacidad vive en el repartidor — motor real: `recursos_reparto` no
  conoce al repartidor, medido; disparo TRAS la caminata de C).

### ✅ LOS DOS FRENOS DE C + EL PEDIDO DE D — EJECUTADOS (18-ago tarde)

- **Confirmación primero:** los dos pedidos NO se perdieron — llegaron
  con el mensaje de mesa de esta tanda (mi reporte anterior cerró antes).
- **① `momentos_aplicables` en `ProductoDeVitrina`** — al embed, al tipo
  y al map, con la razón en el JSDoc: el segundo eje CONTABA
  (`conteosVitrinaPorEje`) y no podía FILTRAR — pintarlo tocable era una
  puerta que rebota (Ley 23). C frenó bien y lo declaró en pantalla.
- **② `countryCode` en `ContextoVentas`** — en el tipo y en los DOS
  productores (el viejo de `cuenta-ventas` y `contextoVentasDesdeArranque`
  sobre el viaje único), con degradación propia `'EC'` (L-247): la
  vitrina FILTRA por país en vez de adivinarlo (P21: proponer no es
  deducir).
- **③ EL TYPECHECK DE apps/cliente CON EL CAMBIO DE D ADENTRO — VERDE
  ESTRICTO:** su rama mergeada a main, router types del cliente
  GENERADOS en mi worktree (`expo start` efímero puerto 8099 — sin
  `.expo/types` el typecheck es LAXO, la trampa ya medida), `tsc` en
  CERO errores. Los 541/17 de su baseline eran artefactos del entorno
  sin node_modules (TS2307 = módulo no encontrado), no tipos rotos. **El
  «no medido» queda MEDIDO y verde de verdad.** Al acta su método, como
  la mesa lo dictó: *declarar NO MEDIDO con el baseline probado es lo
  correcto cuando el instrumento no está — la alternativa (verde por
  declaración, o un install destructivo que purga el modules dir del que
  dependen prestador y su Metro) rompe algo real. Un «no medido» que
  nadie mide se vuelve un verde por olvido — por eso el pedido viajó con
  dueño.*
- **④ La tercera ley del espejo, a N17** (los dos modos listan conjuntos
  DISTINTOS — la diferencia ES la información) **y el hueco del $/kg
  CERRADO** en la misma letra (D lo montó; cómputo compartido en
  `i18n/moneda`). **Los criterios de C, registrados:** si los conteos
  fallan, LOS EJES NO SE DIBUJAN Y LA LISTA SIGUE SIRVIENDO (degradar el
  filtro es honesto; degradar la lista sería esconder catálogo) · la
  ficha NO se cablea desde stock (Celda renderiza su `fin` dentro del
  Pressable — botón dentro de botón, clase D-311; y la razón de producto
  gana: *stock es el trabajo de adentro, la ficha se abre desde la
  cara*) · **D-829 cerrada del lado de C** con la celda honesta por lo
  que NO hace (no activa nada · no se dibuja si ya la tiene · si el
  lector falla no se dibuja — jamás un «pedilo» sobre algo ya pedido);
  el otorgamiento sigue siendo acto del founder a mano.

### 📬 PARA C — LAS SIETE RECETAS DE B ESTÁN COMPLETAS (18-ago; aviso de mesa por repo)

**L5b tiene recetas COMPLETAS y lectores COMPLETOS — construí.** Los dos
pedidos de pieza que tu §XII dejó, **B los tomó y los puso al frente de su
cola**: ① el carrusel a sangre 4:3 (hoy vive DENTRO de `FichaPrestador`
sin exportar) · ② el portador del fundido de modo (`Entrada` NO sirve —
es entrada escalonada CON desplazamiento, justo lo que la receta
prohíbe). **Mientras tanto construí lo que no depende de esas dos.** De
mi lado tenés: `producto_id` en `SkuDelVendedor` (la puerta de la ficha)
· `motivo_rechazo`/`oferta_estado` (con la receta 7: LITERAL, único
danger) · `razonesDeAlcance` · `conteosVitrinaPorEje` · `correo` en
`listarRepartidores` · `viajesPorRepartidor` · el reorden
(`ponerPedidoPrimero`/`volverPedidoAlOrden` + `movido_al_frente_en` en
los dos lectores — tu lectura nace `?? null`, L-247).

### ✅ LOS DOS LECTORES DE C + EL MOTOR DE L5a — ENTREGADOS (18-ago)

- **① `correo` en `listarRepartidores`:** en el select, el tipo y el map,
  con la razón en el JSDoc (la edición pre-llena; mandar vacío BORRARÍA —
  un lector que no trae el campo convierte la edición en borrado
  silencioso). E2E: viaja con null honesto en los pre-S98.
- **② `viajesPorRepartidor`:** RPC + wrapper — **el HECHO, no el
  vocabulario**: «entregó» = `entregado_en IS NOT NULL` (el vocabulario
  de estados crece sin romper el conteo); el repartidor sin envíos viene
  con ceros REALES del motor, jamás el cero fijo que mentiría. Cinturón
  con camino independiente (en_curso=1 por dos vías) + ajeno rebota +
  L-140; **E2E camino real 200** con datos vivos (`ee94bb10`:
  en_curso 1 · entregados 0). *Nota del E2E que casi miente: mi primer
  400 era IDENTIDAD, no motor — `duenodes` no es vendedor de `eec12ef3`
  (esa es de `+nuevotest2`); el mapa de cuentas quedó medido arriba del
  script.* **El ⑤ de la ficha del repartidor queda montable.**
- **③ L5a MOTOR — `proponer_skus_vendedor_lote`:** cada fila entra por
  la MISMA puerta 1 a 1 (**M21 intacta POR CONSTRUCCIÓN y verificada POR
  RESULTADO en el cinturón**: `creado.producto=false` — el canónico se
  RESUELVE, jamás se crea; leído del cuerpo vivo con `pg_get_functiondef`
  ANTES de escribir, que corrigió mi primer cinturón: yo había armado la
  fila buena CREANDO un producto y el contrato real es MAPEO). **POR-FILA
  jamás todo-o-nada** (una mala no mata 49 buenas; cada una responde con
  índice y causa — *lo que no entra SE DICE*), techo 500 que rebota
  HABLANDO, siembra sin stock (L-231: cero ledger que limpiar), residuo
  0 verificado. Wrapper `proponerSkusVendedorLote` con los tipos
  `FilaLoteSku`/`ResultadoFilaLote` y los códigos `lote_invalido`/
  `lote_demasiado_grande` al vocabulario. **El parseo CSV/XLSX → filas
  es de la PANTALLA (C); §14 no aplica — no hay IA.** La mitad IA:
  POST-15-SEP, sin cambio.
- **Mi cola tras esto:** L5a/L5b de A COMPLETOS (motor) · D-835 🟡
  esperando su rojo real · el resto es superficie (C) sobre recetas (B).

### ✅ L2 CERRADA EN APARATO (17-ago noche) — el lote se cierra; L-249 depositada; D-835 BAJA con decisión

- **L2 CIERRA DE VERDAD:** con app fresca Diego aterrizó en «Mis entregas
  de hoy» — forense `repartidor de Clínica Aurora → /ventas/entregas`,
  vacío honesto, sin barra, sin superficies del negocio, deep link
  devuelto por el resolvedor. El lector (mío) + la rama (D) funcionan en
  el aparato. **El lote L2 queda CERRADO** (spec → motor → guard →
  reclamo → rama → aparato).
- **L-249 depositada** (el repartidor ENCERRADO — borrar datos de la app
  fue la única salida): *toda pantalla-única declara su salida al nacer*
  — la hermana de «motor sin puerta» desde el otro lado (el destino sin
  camino de vuelta). **Al acta el doble registro:** el tipo de
  `Encabezado` (unión discriminada `atras`+`onAtras` o ninguno) frenó EN
  EL COMPILADOR el atajo de la flecha muerta — el mismo defecto que el
  aparato encontró en la pantalla. *Las curas cierran por construcción,
  dos veces en la misma pantalla.*
- **D-835 — MI DECISIÓN (la mesa me la dio): BAJA de prioridad, NO se
  siembra.** El terreno se curó solo (con la rama viva, Diego ya no cae
  al callejón; aceptar ocurre una vez y ocurrió — C hizo lo correcto al
  no fabricar repro). Lo que C sí dejó: **el forense vive y en camino
  feliz resuelve en ~0,5 s — cuando corre, no cuelga.** Sembrar una
  cuenta de auth sin rol para discriminar un defecto de un callejón que
  ya casi no se alcanza es pagar residuo PERMANENTE (la clase que S92
  purgó a 64) por información de baja urgencia. **La discriminación
  queda atada al primer sin-rol REAL** — un recién-registrado de octubre
  cae ahí por el camino normal y el forense lo dice GRATIS (línea sin
  veredicto = cuelga · sin línea = no corre). La ficha se enmienda con
  este estado; el ítem sale del puesto 0 de mi cola.
- **④ EJECUTADO EN LA MISMA TANDA — `producto_id` en el lector del
  espejo:** el pedido de C era real y el hueco era de `SkuDelVendedor`
  (la vitrina y el buscador YA lo tenían — medido antes de escribir):
  desde «Administrar» no había puerta a la ficha. `producto_id` entra al
  tipo, al embed y al map. **C desbloqueada.**
- **📬 PARA D — PEDIDO NUEVO (de la ley del espejo, mesa 17-ago):**
  montar el **$/kg en la ficha de producto de la FAMILIA**
  (`apps/cliente`, la ficha N19 del lado cliente) — el dato ya viaja
  (`peso_kg` + precio en `VarianteDeProducto`), el cómputo es puro
  (precio ÷ peso_kg), la forma es la firmada por B (**mono, secondary**,
  receta N19 §1: `$ 4,16 / kg` bajo el precio). **Hasta que exista, el
  espejo tiene un hueco declarado y correcto** — N17 (ley en el plan):
  el espejo jamás muestra en «Ver como cliente» lo que la familia no ve.
- **📬 PARA B — TU SÉPTIMA RECETA QUEDÓ DESTRABADA por el contrato §XII
  de C (el espejo):** los cuatro puntos firmados que la anclan — ① el
  estado con su PORQUÉ en «Administrar» (los datos ya viajan:
  `estado` + `motivo_rechazo` + `oferta_estado`) · ② el no-publicado NO
  aparece en modo cliente y **el espejo lo explica ahí mismo — el ÚNICO
  caso en que el espejo habla en ese modo** · ③ el contador solo con lo
  suyo (`razonesDeAlcance`, dueño vendedor) · ④ cero huecos NO dibuja
  nada. Con tus cardinalidades ya servidas, nada más la bloquea.

### ⚖️ LA TANDA DE B, DEPOSITADA (17-ago tarde) — N20 enmendada · L-248 · el comodín · sinCaja adjudicada

- **N20 ENMENDADA en el plan** (no como nota de B): **la especie es un
  FILTRO, jamás una carpeta — DOS EJES, DOS FILTROS, cero carpetas.** Al
  acta cómo se encontró: **el dato lo dijo antes que el diseño** — 11
  perro + 3 gato = 14 sobre 13 comprables; 14 no cabe en 13, así que al
  menos un producto vive en las dos especies y una navegación de
  carpetas lo escondería de la mitad de las familias. *(El hallazgo es
  de B, sobre cardinalidades mías — el cruce de pistas trabajando.)*
- **L-248 depositada** (numeración por grep): *una automatización obliga
  a decidir sus casos borde ANTES — no va a haber nadie presente cuando
  dispare.* Con el corolario (el gate de una regla auto-disparada es al
  ESCRIBIRLA) y el criterio del umbral de B como criterio de la casa:
  **un filtro que no reparte no es un filtro, es un adorno con estado.**
- **El comodín, ratificado Y CABLEADO donde se lee:** momento NULL =
  TODAS las edades, jamás bucket propio — el contrato vive ahora en el
  JSDoc de `ConteosVitrina` (quien encienda el eje por umbral suma los
  NULL a cada momento; dejarlos afuera es el caso exacto de L-248).
- **Las dos ratificaciones por número, registradas:** la completitud
  nace cerca de cero con 6+1 — con un hueco la línea narrativa es la
  superficie entera, **con cero huecos NO SE DIBUJA NADA** (felicitar
  por lo normal es la mecánica que LOYALTY §2 prohíbe) y los 457
  canónicos no entran (*un contador que cuenta lo que no es tuyo no se
  puede cerrar nunca*) · la grilla con 11 y 7: **sin paginación, sin
  «cargar más», sin encabezados** — cualquier andamio pesa más que el
  contenido que organiza.
- **📬 PARA B — `sinCaja` SE RENOMBRA AHORA (adjudicación de mesa; tuya
  la ejecución con tu vara):** el propio archivo lo confiesa («NO ES SIN
  CAJA: tiene accent.sinCaja… presencia de superficie») — un nombre que
  contradice lo que la pieza hace es una L-245 esperando a pasar. Es
  enmienda de vocabulario firmado S82 ⇒ va con lápida y razón. Tu
  candidata «apoyada» vale; mejor nombre con su razón también. **⚠️ El
  discriminador para tu barrido, medido: `sinCaja` vive en DOS piezas —
  la variante de `Boton` (LA adjudicada) y el `sinCaja` de `Campo`
  (`caja-de-campo.ts`, candidata S81, OTRA pieza y OTRA semántica). Un
  rename por grep se llevaría los dos; el de Campo NO está adjudicado.**
  Sitios medidos de la variante de Boton: `Boton.tsx` + gallery +
  bienvenida/mascota/invitacion-avisos del cliente +
  equipo/invitacion-avisos del prestador — los de apps son territorio
  C/D: alias compatible primero o coordinación por parte, tu decisión
  declarada.

### ✅ ADJUDICADO (a) POR MESA (17-ago) — N20 Y EL BUSCADOR SOBRE LO COMPRABLE; L5b SALE DE PAUSA

**La adjudicación entera vive en `PLAN_S99` (bloque de adjudicaciones),
que es donde alguien la va a reabrir** — con las tres razones (la tercera
decide: *una vitrina que muestra lo que no se puede comprar miente dos
veces*), la distinción **canónico = inventario de propuesta del vendedor,
jamás vitrina de la familia**, el 🔴 dato de negocio para el founder
(**octubre abre con SEIS productos comprables** — la vitrina limitada por
el catálogo con oferta, no por el software) y L5a reencuadrada después de
L5b. **Los 6 fixtures comprables entraron a D-766 (tercer addendum)** con
la advertencia que importa: el Aceite de Aurora NO muere con las cuentas
borrables — se quita aparte. **Al acta: el «26 curados» que la mesa
repitió varias veces sin verificar** — no existe en esquema ni letra; lo
firmado son SEIS (§4.3). *Segunda vez en la sesión que la mesa afirma
sobre un objeto que no midió (la primera: el anillo del pin). El dato de
mesa no se verifica por venir de la mesa — viaja como hipótesis.*

### (registro de cómo se sirvió) EL NÚMERO QUE DECIDÍA EL ORDEN, MEDIDO (17-ago)

**La pregunta de mesa:** de los 470, ¿cuántos de los curados del
lanzamiento están completos? **Primero, la corrección de dato (L-235: se
dice, no se absorbe): «26 curados» no existe ni en el esquema ni en la
letra** — cero marcas de curaduría en `productos` (todo es
`vendible=true` / `origen_carga='epetplace'`), y la letra medida dice
**SEIS**: `MODELO_DESPENSA` §4.3, *los seis del lanzamiento*, medidos de
la base en S96. Lo comprable HOY (SKU activo + oferta publicada) son
**13 productos**, y se parten limpio:

- **✅ LOS SEIS DE §4.3: COMPLETOS LOS SEIS** — foto ✓ · momento vital ✓
  · composición `declarada_sin_verificar` ✓ · oferta publicada ✓ (Adulto
  Cordero y Arroz · Adulto Pescado y Papa · Bisonte y Venado High
  Prairie · Puppy Pollo y Arroz · Salmon Puppy Pacific Stream · Trucha y
  Salmon Feline). El séptimo completo es ACTIVE MIND 7+ (duenotodo,
  fixture).
- **Los 6 comprables INCOMPLETOS son FIXTURES de cuentas de prueba**
  («Snack de Ave (prueba del gate)» · 3 «Croquetas Demo» · «Arena
  Sanitaria Demo» · el «Aceite de Salmon» de Aurora, sembrado para el
  gate del repartidor — el único con composición AUSENTE). Mueren con la
  limpieza de cuentas borrables (la lista de D-766 creció).
- **LA VITRINA QUE ABRE, ABRE BIEN — y es por CONSTRUCCIÓN, medido:**
  `listarVitrina` parte de **`from('ofertas')` con `!inner`** — los 457
  productos del canónico SIN oferta no se pintan jamás. **El 66 % sin
  foto y el 47 % sin momento son COLA LARGA del catálogo canónico**, no
  la vitrina de octubre.
- **DÓNDE sí muerde la cola larga, y es LA decisión que sirvo:** la
  navegación N20 y el buscador. Si corren sobre lo COMPRABLE, octubre
  abre con 6 productos completos y el canónico queda invisible hasta
  ganar oferta (a). Si corren sobre el CANÓNICO entero —lectura posible
  de la firma S96 *«catálogo amplio con buscador»*— el segundo toque
  esconde media vitrina y el buscador devuelve fichas sin foto (b), y
  entonces L5a + foto canónica SUBEN por encima de L5b. **Mi voto: (a)
  para octubre** — coherente con lo construido (la vitrina ya parte de
  ofertas), con M21 (la foto canónica es de e-PetPlace: 309 fotos no las
  saca un vendedor) y con N18 (el vendedor que propone un SKU ve qué le
  falta para alcanzar). La firma del buscador se lee como «amplio sobre
  lo publicado», no «el canónico crudo» — pero esa lectura es DECISIÓN,
  no medición, y por eso viaja acá.
- **El reencuadre de L5a queda registrado como la mesa lo dijo:** no es
  «la carga por archivo» — es lo que hace que la vitrina EXISTA más allá
  de seis. Cada SKU que un vendedor real proponga mapea a un canónico
  hoy incompleto; N18 le dice cuál campo, y la foto canónica (M21) es
  trabajo de e-PetPlace, no suyo.

### 📱 OTA 7 — L2 ENTERA, LAS DOS MITADES (17-ago; SUPERSEDE al 6) — 📬 PARA C: LA PRUEBA FINAL

- **group `1cc813b8-6951-437b-931e-023b8ab0a8e3` · ancla `7540d06d` (= main
  exacto) · dirty **None** · el pie debe decir `update 01a008ff · preview`.**
- **Carga verificada por merge-base ANTES de publicar** (la regla de los dos
  gates quemados): `49dc4d7a` (lector `repartidor_de` + forense D-835) y
  `0c45e6e8`/`33385020` (la rama de D) ancestros del ancla ✓. El árbol quedó
  EN CERO durante la ventana (el `settings.json` local del founder salió por
  stash y volvió intacto al cerrar — jamás se commiteó).
- **📬 PARA C — la prueba que cierra L2:** aplicar el OTA (pie `01a008ff`),
  entrar como Diego CON APP FRESCA → debe LLEGAR a su pantalla de entregas
  sin tocar nada. *App fresca a propósito: D-835 (el guard que no
  re-resuelve tras aceptar) sigue VIVO y es mío — si probás el camino
  aceptar-en-caliente y muere en esqueleto, eso es D-835, no la rama.* Y de
  paso: una línea de logcat con `[sesion] raíz prestador: resolviendo…` al
  tocar cerrar sesión discrimina D-835 en un minuto (línea sin veredicto =
  la cadena cuelga · sin línea = el efecto no corre).
- **La rama de D, ratificada por mesa:** redirect y no barra POR LETRA
  (§2.0: la casa es del dueño; el repartidor es alguien del vendedor con
  tres acciones) · `?? []` del consumidor → **L-247 depositada** (la ley de
  D, con su auditoría: el `=== null` de `cuentaComercial` en la barra era el
  mismo agujero — curado citándola) · su método del guard registrado: Diego
  consumió su pendiente COMO PERSONA (que es como tenía que pasar); el
  brazo positivo se re-gatea sembrando un pendiente NUEVO, jamás desatando
  a Diego (un fixture que muta producción ya costó un rol en S75).

### 🔍 EL FRENO DE C (`pago_confirmado_en`), MEDIDO EN LAS DOS DIRECCIONES (17-ago) — NO ES LA QUINTA MUESTRA: ES UN ÁRBOL VIEJO

- **Lo medido del objeto:** el campo está en `origin/main` (`7bcf3370`
  ancestro ✓) · en la migración `20260817160000` · en `database.types.ts` ·
  en el TIPO `PedidoDelVendedor` · **en los DOS selects y los DOS maps**
  (plano y EnRango, líneas medidas). El E2E 14/10 fue real.
- **El discriminador que explica el «cero»:** `origin/pista/s99-c`
  (`8b7fcdb9`) **NO contiene `7bcf3370`** (`git branch -r --contains` lo
  lista en main/s99-a/s99-d — no en s99-c). C midió SU worktree, detrás de
  main. Es la ley de D en la dirección inversa: allá «ya en main» era falso;
  acá «no existe» era falso — **el mismo instrumento (rev-list/contains)
  discrimina los dos sentidos, y L-238 lo dice entero: una ausencia vale lo
  que vale el universo donde se buscó.** Cura de rutina ya escrita: merge de
  main al abrir cada ventana.
- **La mitad VERDADERA de C, confirmada y con su cura vigente:** el lector
  plano ordena `created_at DESC` (medido) y el panel no consume el campo —
  el estado declarado. **Rige lo ratificado: el orden se NOMBRA «por
  llegada» mientras tanto** — lo prohibido no es ordenar, es decir «por
  pago» ordenando por creación. Y el flag de urgencia: correcto que no
  llega — el exprés NO existe en el esquema (censo L5b ⑥, fuera del camino
  crítico por firma); la banda «se rompió» sale de la NARRATIVA, no de un
  flag.

### ✒️ LA RATIFICACIÓN A C, SIN ENMIENDA (④ de mesa, 17-ago) — EL REORDEN QUEDA EJECUTABLE

Las cuatro bandas (**sin compromiso preside** · **«se rompió» segunda con
su razón** — lo urgente está por fallar, esto YA falló, y hay alguien que
perdió su tarde esperando — · urgente · los días), separadas con AIRE y
TÍTULO jamás con regla, y **banda vacía no se monta** · el reorden es
**«Poner primero» en el DETALLE**, con el pedido movido diciéndolo
(«Movido a mano» + «Volver al orden») — *un orden alterado que no se
declara es un FIFO que miente* · **no cruza bandas — ordena, jamás
re-promete** · la ventana **AUSENTE, no apagado** (Ley 23) · **sin
arrastre** (si el FIFO es la ley, un drag&drop convierte la excepción en
costumbre). **Y con la palabra en mano, el motor quedó ENTREGADO EN LA
MISMA TANDA (17-ago):** migración `20260817180000` —
`pedidos.movido_al_frente_en` (timestamptz, null = orden natural; N
movidos se ordenan entre sí por marca DESC) + `poner_pedido_primero`
(gates: `pedido_no_existe` · `no_sos_el_vendedor` · `pedido_terminal` —
el respaldo del motor al AUSENTE de la ventana) + `volver_pedido_al_orden`
(sin gate de terminal a propósito: quitar una marca jamás miente) + la
marca en `v_pedidos_narrativa` (aditiva al final). **Cinturón por brazo
entero verde** (duenodes estampa · la vista la expone · Diego rebota
`no_sos_el_vendedor` · un terminal real rebota `pedido_terminal` · volver
limpia · L-140; residuo 0 verificado GLOBAL: cero marcas vivas al salir).
**Wrappers `ponerPedidoPrimero`/`volverPedidoAlOrden` en el barrel EN EL
MISMO COMMIT** + `movido_al_frente_en` en los DOS lectores. ⚠️ Nota L-247
al contrato de C: su lectura de `pago_confirmado_en` y de
`movido_al_frente_en` nace degradando propio (`?? null`).

### 📬 PARA B — LAS CARDINALIDADES DEL CENSO (17-ago; lo que N20/N18/grilla esperaban — llegan AHORA, no al final)

- **470 productos activos · 6 especies con producto:** perro 266 · gato
  131 · pez 50 · roedor 26 · ave 25 · conejo 19 (multi-especie: suman más
  que el total).
- **Momentos vivos: 3** (cachorro · adulto · senior) — perro y gato tienen
  los tres; ave/conejo/pez/roedor UN solo momento. **⇒ N20 con su número
  real: 6 especies × ≤3 momentos, no «doce y quince»** (tu L-246 en carne).
- **🔴 222 de 470 (47 %) SIN momento** → el segundo toque de N20 esconde
  MEDIA vitrina si no existe el camino «todas las edades» — tu receta lo
  decide; mi lector de conteos por eje lo va a decir (Ley 23: la puerta no
  ofrece lo que va a rechazar).
- **309 de 470 (66 %) sin portada** (regla real medida: `imagen_url` ??
  `imagenes[0]`) → la grilla nace mayoritariamente de MARCADORES: la
  receta decide cómo se ve una vitrina de marcadores digna.
- **Composición:** ausente 267 · declarada_sin_verificar 202 · no_aplica 1
  · verificada 0.
- **SKUs por vendedor hoy: 1–7** (13 totales, 4 cuentas) → la completitud
  es LISTA CORTA, no tabla.
- **Los campos de completitud (mi cómputo — tu receta 4): 8 códigos, 3 del
  vendedor** (`sku_rechazado` · `sin_stock` · `sin_precio_propuesto`) **+
  5 de e-PetPlace** (`sku_en_revision` · `oferta_no_publicada` ·
  `composicion_ausente` · `sin_momento_etario` · `sin_foto`). **El
  contador cuenta SOLO los 3 del vendedor** — `razonesDeAlcance` en
  `@epetplace/api`, pura y por-SKU (jamás puede comparar vendedores). Tus
  dos leyes quedaron depositadas: **L-245** (la ley del comentario, con el
  corolario de jubilación y la mitad honesta de R47) y **L-246** (censo =
  contenido, ley = forma).

### ⚙️ EL ARRANCA-YA DE L5b — ESTADO (17-ago)

- **① $/kg: CERO motor** — el dato viajaba desde S95-E (`1631c128`); mi
  censo decía «no viaja» y era FALSO (enmendado en el censo con lápida —
  **11ª muestra de la familia L-235, MÍA, en un censo que se declara
  medido del objeto**). El $/kg es cómputo de PIEZA (mono, firma de B).
- **② la voz del borde: motor ENTREGADO** — `motivo_rechazo` +
  `oferta_estado` en `listarSkusDelVendedor` (el CHECK garantiza rechazado
  ⇒ motivo). La voz es de C con la receta 6 de B.
- **③ completitud: ENTREGADA** — `razonesDeAlcance` (exportada en el
  barrel EN EL MISMO COMMIT — la segunda muestra de la clase fue
  exactamente un wrapper sin re-export) + los campos canónicos en el
  lector POR LA MISMA FUENTE del cliente (`composicionEstado` y
  `fotosDeProducto` exportados del catálogo, jamás re-derivados). **E2E
  1/1 camino real** (duenodes; la muestra DES-CORD-1 demuestra la ley con
  dato: contador vendedor CERO + dos info e-PetPlace).
- **④ D-829: CERO motor, verificado** — `solicitarNaturalezaComercial`
  vivo en `cuentaComercial.ts`; la celda es de C con su ficha.
- **⑤ conteos por eje (N20): ENTREGADO con la adjudicación (a) en la
  mano** — `conteos_vitrina_por_eje()` sobre LO COMPRABLE (migración
  `20260817190000`; cinturón con discriminador de CAMINO INDEPENDIENTE:
  perro=11 por la función y por un join directo escrito distinto; techo
  estructural contra el total comprable=13) + wrapper
  `conteosVitrinaPorEje` con degradación propia L-247, en el barrel en
  el mismo commit. **E2E camino real: 200 con duenodes (perro 11 · gato
  3, dos pares con momento null — el bucket «todas las edades» que la
  receta de B decide CON número) · anon 401 (L-140 por camino real).**
  **Con esto los lectores de A para L5b están COMPLETOS** — lo que sigue
  de L5b es superficie (C) sobre recetas (B).

### 🔴 GATE 2 CERRADO (17-ago): ③⑤ VERDES · ④ ROJO CON MI MITAD YA CURADA

- **⑤ VERDE — el par DISCRIMINA:** Marco rebota `repartidor_sin_cuenta`;
  Diego (reclamado EN EL APARATO, no por script) pasa y nace su envío
  7597, residuo 0. **Al acta: el método de C** — cazó DOS rojos por la
  razón equivocada (`auth_requerido` · la máquina de estados) ANTES de
  contarlos; *un rojo que llega por otra puerta no prueba nada; el orden
  medido de los guards es lo que vuelve sólido al rojo* (la cara-espejo
  de «verde por la razón equivocada», S95).
- **③ VERDE:** un repartidor real entró SOLO y lo primero que vio fue
  «Clínica Aurora te registró como repartidor · Aceptá y entrás», con el
  «nadie te registró» degradado a pie.
- **🔴 ④ ROJO — MOTOR SIN PUERTA, CUARTA MUESTRA Y UN PISO MÁS ARRIBA:**
  Diego aceptó, el vínculo está SELLADO en la base, y el resolvedor
  sigue diciendo «sin rol prestador» — el callejón quedó MUDO (sin
  pendiente no hay tarjeta) y la pantalla del repartidor (S96) es
  inalcanzable. **MI MITAD EJECUTADA (17-ago):** migración
  `20260817170000` — `repartidor_de` entra a `obtener_contexto_arranque`
  (vínculos SELLADOS: `user_id` + `vinculo_aceptado_en` + `activo`;
  cinturón 4/4 con el DISCRIMINADOR contra el lector de pendientes: el
  sin-aceptar NO aparece; L-140) + wrapper `repartidorDe` fail-closed
  (bundle viejo lee `[]` → degrada al callejón, jamás crash). **Es el
  lector que dice QUIÉN ES — jamás derivar de `misEntregasAsignadas`
  (L-218: su `[]` tiene dos significados; medición de C).** **LA MITAD
  DE D: la rama del resolvedor** (📬 abajo). **L2 no cierra hasta que un
  repartidor aceptado LLEGUE a su pantalla.**
- **🔴 SEGUNDO ROJO → D-835 (dueño A, ALTO):** aceptar y cerrar sesión
  dejan el esqueleto PARA SIEMPRE (cero `[sesion]` nuevas) aunque la
  acción ocurre. El mecanismo de re-resolución del guard no re-dispara —
  la puerta DE AL LADO de la clase que el archivo declara curada. Repro
  barata primero (cerrar-sesión en RN-web con el forense en consola).
- **La clase «motor sin puerta» ya no admite muestras sin cura
  estructural** (cuatro en una sesión: extract-documento · wrappers sin
  re-export · el reclamo · el resolvedor). El instrumento va pedido a B
  con su vara (📬 abajo).
- **Al acta, el error propio de C declarado:** su parte guardaba
  «demo-vet» como si fuera el LOGIN y era el NOMBRE — midió el real de
  `auth.users` en vez de adivinar o resetear. *Un dato de cuenta mal
  guardado en un handoff es el que hace perder la siguiente ventana de
  aparato* (pariente directo de la regla de compactación: el resumen es
  mapa, no fuente).

### La cola viva de A, con el próximo paso ejecutable

0. **D-835 — el guard que no re-resuelve (ALTO, mío):** repro web del
   camino cerrar-sesión (`[sesion]` en consola) → discriminar las dos
   hipótesis de la ficha → cura → re-gate en aparato con los DOS botones.
   *Bloquea la última milla de L2 tanto como la rama de D: aunque la rama
   exista, sin re-resolución el aceptar sigue muriendo en esqueleto.*
1. **L5b arranca-YA (mío, motor bajo):** ① exponer `peso_kg`/contenido en
   el lector del catálogo (patrón `pago_confirmado_en`) → el $/kg es
   cómputo de pieza · ② el lector de la completitud-vendedor DERIVADO de
   las razones del cliente (jamás cómputo paralelo) · ③ el lector de
   conteos por eje para N20. El censo entero:
   `docs/relevamientos/2026-08-16-s99a-L5B-CENSO.md`.
2. **El reorden del panel (L3):** se escribe cuando C dé su palabra
   (bandas+reorden) — la persistencia + el guard del techo (la promesa).
3. **Los dos pedidos de C para cerrar L2:** correo en `listarRepartidores`
   · lector de VIAJES por repartidor (sin él, un cero fijo miente).
4. **L5a MOTOR** (carga determinista XLSX/CSV — cero IA, por
   `proponer_sku_vendedor`, M21 intacta). La mitad IA: POST-15-SEP.
5. **Encender `correo_requerido`** cuando C declare pantalla+OTA aplicado.
6. **Rutina:** merge de ramas de pistas en cada cierre POR SHA (L-239) ·
   **la carga de todo OTA-para-gate se verifica por merge-base en el
   ancla** · el directorio se verifica ANTES de escribir (dos árboles —
   CINCO mordidas en la sesión, una con publicación).

### Compromisos, en las dos direcciones

- **CON B:** le debo **las cardinalidades del censo para sus tres recetas
  pendientes** (navegación N20 · completitud N18 · grilla) — le llegan
  con el arranque de esos frentes. Ella me debe: nada — su escala está
  ratificada, D-484 pagada, sus recetas adelantadas firmes.
- **CON C:** le debo los dos lectores (#3) y el contrato de bandas espera
  SU palabra. Ella me debe: su declaración de OTA para `correo_requerido`
  · la voz de la bienvenida (asignada, pasa por el lote de strings del
  founder). ~~El par del guard #9~~ **PAGADO (Gate 2 ⑤ verde, 17-ago)**.
- **CON D:** le debo nada; **D me debe LA RAMA DEL RESOLVEDOR del ④**
  (consume `repartidorDe` del contexto — contrato en 📬 PARA D; es LO
  PRIMERO de su cola por orden de mesa). Su L4+dual+reclamo mergeados y
  publicados; su voto del orden ADOPTADO; el lector del track (L6/v1.1)
  cuando ese lote abra.
- **CON LA MESA:** el exprés espera SU lectura financiera · D-829 espera
  camino del founder (otorgamiento a mano) · el gate de aparato del
  MOVIMIENTO (L-243: la vuelta que se sienta como vuelta) en la próxima
  pasada del founder.

### 📡 CANAL ENTRE SESIONES: CAÍDO PARA LAS CUATRO — TODO VIAJA POR REPO

### Fichas a demanda (no abren frente solas)

**D-826** (r.mensaje crudo) · **D-827** (códigos por constraint, a pedido
de C) · **D-828** (sinFecha — el lint R45 vigila) · **D-829** (la puerta
de servicios: celda en L5b) · **D-830/D-831** (las dos colas · la ruta
del repartidor, v1.1) · **D-832** (mutación de auto-pruebas, dueña B) ·
**D-833** (la limpieza de la galería, dueña B) · **D-834** (🧊 los
tiempos). **D-835 NO es a-demanda: está en la cola como ítem 0** (el
guard que no re-resuelve — dueño A, alto).

## 📬 PARA D — POR REPO (17-ago: LA RAMA DEL RESOLVEDOR — lo primero de tu cola, orden de mesa)

**El ④ del Gate 2 salió ROJO y la mitad tuya es la rama.** Diego aceptó,
el vínculo está sellado, y el guard raíz lo manda a `sin_rol` — el
callejón quedó mudo y la pantalla del repartidor (S96) es inalcanzable.

**Mi mitad YA ESTÁ (17-ago):** `obtenerContextoArranque()` ahora trae
**`repartidorDe: { repartidor_id, cuenta_comercial_id, negocio }[]`** —
solo vínculos SELLADOS (`vinculo_aceptado_en NOT NULL` + `activo`); el
pendiente-sin-aceptar NO aparece (ese vive en el lector del reclamo).
`[]` = no es repartidor en ninguna casa. Fail-closed: contra un motor
viejo el wrapper lee `[]`. Cinturón 4/4 con Diego real (1 vínculo,
Clínica Aurora) y el discriminador del sin-aceptar.

**Tu rama, el contrato (la letra fina es tuya):**
- En el guard raíz (`(tabs)/_layout.tsx`), en la zona post-`esVendedora`
  y ANTES de `sin_rol`: `if (c.repartidorDe.length > 0)` → el repartidor
  ENTRA a su pantalla (la de S96, la que hoy nadie alcanza).
- **El orden entre poblaciones lo decidís vos midiendo** (¿un repartidor
  que además es empleado-pendiente? hoy no existe el caso — declaralo,
  no lo resuelvas a ciegas), pero la regla de la casa rige: un panel
  donde puede TRABAJAR HOY gana a una voz de espera.
- **N>1 casas:** hoy imposible en datos (Diego es el único sellado). Si
  tu rama toma `[0]`, DECLARALO en el código — no es un default mudo
  (L-244), es un borde con su razón escrita.
- ⚠️ **D-835 (mía, alta) es la otra mitad de la última milla:** aunque tu
  rama exista, el `alAceptar` de hoy muere en esqueleto porque el guard
  no re-resuelve. No la esperes para escribir la rama (con app
  reiniciada tu rama YA funciona — así se re-gatea mientras tanto), pero
  sabé que el cierre de L2 exige las dos.

## 📬 PARA B — POR REPO (17-ago: EL INSTRUMENTO DE LA CLASE «MOTOR SIN PUERTA» — pedido de mesa, con tu vara)

**La clase ya no admite más muestras sin cura estructural: CUATRO en una
sesión** (`extract-documento` · los wrappers sin re-export · el reclamo
sin montar · el resolvedor sin rama). El patrón: **un motor sin
llamadores pasa typecheck, lint y cinturón porque ninguno mide si
alguien lo llama.**

**El pedido (la mesa te lo asigna con TU vara — L-236: el instrumento
nace con casos que NO debe atrapar):** diseñá el instrumento que la cace.
Candidato servido, no impuesto: una regla de `verify:diseno` (o script
hermano) que tome los EXPORTS de `packages/api/src/wrappers/*` nacidos
en la sesión (o con marca) y exija ≥1 llamador en `apps/` — con su lista
de EXENTOS DECLARADOS (un lector construido-a-propósito-antes-de-su-
pantalla se exime POR ESCRITO, no por silencio). Los cuatro casos reales
de S99 son tu corpus de calibración: el instrumento debe atrapar los
cuatro EN SU MOMENTO, y NO atrapar los lectores legítimamente
adelantados (los del censo L5b, p.ej.). Si tu diseño concluye que el
lugar no es un lint sino otra cosa (una sección obligatoria del parte,
un paso del cierre), esa conclusión vale — lo que no vale es la quinta
muestra.

## (histórico) HANDOFF DE A del 15-ago — superseded por el de arriba

### ✅ EL DESBLOQUEO EJECUTADO (15-ago, orden de mesa — ANTES de leer la cola)

**El bloqueo mutuo C⇄D está DISUELTO: las dos mitades viven en `main`.**
- **Mergeado `pista/s99-d`** (el CASCARÓN de D-820: el vendedor puro entra a la
  casa de tabs — `barra-prestador-lectura.ts` con `vendedorPuro` + contexto
  exigido por tipo · el histórico dual citas⇄pedidos · 3 guards nuevos
  `verify-s99d-*`) y **mergeado `pista/s99-c`** (la PIEZA `ventana-pedidos`
  — sin fetch, unión discriminada que EXIGE `sinFecha` cuando hay `dia`,
  D-828 por construcción y declarado en comentario — · los DOS cuartos
  honestos de `atender`/`mascotas` · C4 con su premisa PRE-S99 · su parte
  con los avisos que el canal caído no dejó salir).
- **Verificación de la COMBINACIÓN** (los cuartos de C sobre el cascarón de
  D nunca habían compilado juntos): typecheck prestador **0** ·
  `verify:diseno` **VERDE 37 reglas** (R45: 2 superficies consumen el
  lector, todas declaran `sinFecha`, baseline 0) · guard barra vendedor
  puro **VERDE** · guard histórico dual **VERDE** · guard escalera **VERDE**
  (14 estados · 16 activos · 10 orígenes con botón).
- **El rescate de C CERRÓ:** `pista/s99-c` está en origin (`fb3ec4cb`,
  verificada por D por contenido — 16/16 commits por merge-base, 5/5
  artefactos caros por cat-file, incluido el C4 que NO es reproducible).
  Mi primer merge entró en `25aee2c1` (la rama avanzó bajo mis pies con el
  parte del rescate) y el commit final se mergeó aparte — **origin/pista/s99-c
  es ancestro de main, verificado**. De esta jornada nacen **L-238** (una
  ausencia vale lo que vale el universo donde se buscó — el atrape es de D)
  y **L-239** (hay comandos cuyo exit no responde la pregunta — el repro de
  C sobre `ls-remote`; el error de la orden fue de MESA). Fichas en
  `DEUDAS_CANONICAS`.

### 📱 OTA 6 — L2 CIERRA (16-ago noche, EL VIGENTE) — el reclamo montado

- **Group `2d480427-838d-4cbf-9d6e-e0c55a8ff697`** (android `01a008cd-…`) ·
  **ancla `f2c839f4` = origin/main · dirty=None** · **LA CARGA VERIFICADA
  POR SHA ANTES DE PUBLICAR** (el montaje de D `1985121e` y la pieza de C
  `7f28dff3` ancestros del ancla — la regla de los dos gates quemados, ya
  trabajando). **El pie debe decir `update 01a008cd · preview`.**
- **Qué lleva:** el reclamo del vínculo MONTADO donde el callejón vivía
  (arriba del `EstadoVacio` — reclamar es lo que la persona vino a hacer;
  «nadie te registró» pasa a pie) — **el motor-sin-puerta ③ del Gate 2
  gana su puerta.** Guard de D verde en CUATRO poblaciones, corrido acá
  sobre la combinación.
- **⏩ PARA C:** con un repartidor reclamado, **corré el par discriminador
  del guard #9** (`repartidor_sin_cuenta` rebota antes / despacha después)
  — con eso **④⑤ del Gate 2 CIERRAN en minutos.**
- **Ratificado a D (de mesa):** su montaje y su decisión de NO aceptar por
  script — *hay UN pendiente vivo y aceptarlo habría consumido el sujeto
  del guard y hecho por la persona algo que le toca a ella* (el fixture
  que muta producción ya costó un rol en S75).

### 📱 OTA 5 — EL DEL GATE 2 REAL (16-ago noche; superseded por el 6)

- **App PRESTADOR** · `preview` · runtime 1.0.5 · **group
  `9258b0e7-b8ae-44ed-9d4a-4339d8b227b7`** (android `01a0089f-2b39-…`).
- **Ancla del OBJETO: `1f2a05cbf9d7c7dd02e5bb1439893d48d34f06a3`**
  (= `origin/main`) · dirty=None. **El pie de Cuenta debe decir
  `update 01a0089f · preview`.**
- **Qué lleva:** **L2 entera** (la ficha del repartidor caminada) · **el
  pin `moto` firmado en `LienzoMapa`** (verificado en el árbol del ancla:
  LienzoMapa ×6 en galería, moto ×8 en la pieza — el founder lo juzga
  DONDE VIVE, L-241) · **las cuatro correcciones del dual de D**
  (`dia-en-vista`) · todo lo anterior (#0a, R42/R46, curas).
- **⚠️ EL GRUPO HUÉRFANO, DECLARADO: `d2d636ad` (ancla `0139c067`) se
  publicó MINUTOS antes SIN L2** — el merge de C había aterrizado en mi
  rama de pista y no en main; el push dijo «Everything up-to-date» y ESE
  fue el síntoma. *Ancla limpia, carga incompleta: un publish puede estar
  impecable por regla 82 y no llevar lo que vino a llevar.* Superseded por
  éste. **Es la CUARTA mordida de los dos árboles — CON PUBLICACIÓN, y
  la regla escrita horas antes la rompió su propio autor** ⇒ la regla
  gana el brazo que le faltaba: **antes de publicar, LA CARGA SE VERIFICA
  POR SHA EN EL ANCLA** (`merge-base --is-ancestor <sha-de-la-tanda>
  <ancla>`) — no alcanza con que la ventana esté limpia. *(Y la QUINTA
  mordida llegó mientras se escribía esta entrada: un lote de docs editado
  a ciegas del cwd terminó mitad en cada árbol, con un commit directo a
  main — reconciliado por merge, contenido intacto. La regla no falla por
  desconocida: falla por hábito — cada bloque de escritura ABRE con su
  `cd` absoluto, sin excepciones.)*

### ✒️ FIRMAS DEL GATE 2 (16-ago — y DOS leyes evaluadas que SOBREVIVIERON)

- **La cabecera de la ficha PASA** (*«se ve como la tarjeta de alguien»* —
  N17 aplicada a una persona, verificada por el ojo) · **los dos vehículos
  QUEDAN** (*«ya dos es poco convencional, pero puede pasar»* — tope por
  construcción, jamás en pantalla) · **el plan de medición: OK, corre en
  la ventana** · **LA MOTO SIN ANILLO y LA MASCOTA CON ANILLO — L-242
  confirmada por el ojo sobre mapa real**, exactamente como B la midió.
- **N13 RIGE SIN ENMIENDA — evaluada y CONFIRMADA:** el founder consideró
  derogarla para el documento del repartidor **y se retractó**, con la
  razón depositada: *el documento del repartidor es el dato con el que
  alguien responde por la persona que toca el timbre de una familia; si
  la IA lee mal un número y nada lo distinguió, el vendedor guardó un
  dato que nunca leyó y cree que verificó.*
- **📋 AL ACTA:** en este gate el founder consideró derogar DOS leyes
  (N13 · el tope de vehículos) **y se retractó ante la medición y la
  razón** — *una ley que se puso a prueba y aguantó vale más que una que
  nadie discutió.*
- **✒️ FIRMA NUEVA — EL ARCO V3, registrado como DESTINO (no deuda):**
  *«el vendedor es responsable de agregar el dato del repartidor. En V3,
  cuando tengamos flota propia, se hará desde nuestro admin o vía API con
  un proveedor logístico.»* Ratifica L2 entero (el correo como garantía
  del vendedor, el alta por el negocio) y nombra V3: **flota propia +
  integración logística** — saberlo hace que L2 no se construya de forma
  que lo impida. Depositado también en `PLAN_S99` §9.

### 🎯 LA DISCRIMINACIÓN, COMPLETA (16-ago noche) — TRES SOSPECHOSOS MUERTOS POR MEDICIÓN, QUEDA EL DISPOSITIVO

- **LA RED: DESCARTADA (C, los tres caminos del plan):** RTT del aparato
  **39 ms avg (mdev 33)** · escritorio en el mismo minuto **52–96 ms** ·
  la dispersión persiste en AMBOS métodos y **el instrumento explica solo
  0,3–0,5 s** de la franja.
- **LAS OLAS: DESCARTADAS (el cruce que nadie había hecho):** yo mido
  17→14 OLAS del código; C mide que el TIEMPO empeoró. **Los dos son
  ciertos ⇒ la causa NO está en las olas. NI #0a NI #0b arreglan lo que
  el founder siente con el dedo** — curar la cola serial habría sido
  operar al paciente equivocado, exactamente lo advertido.
- **EL BUNDLE: DESCARTADO (mi medición, 16-ago noche — era el candidato
  que nadie había nombrado):** export android por ancla —
  **S98 (`d4613ce9`): 8.029.019 bytes · hoy (`5ee961a5`): 8.113.986 =
  +84.967 bytes, +1,06 %.** Y es Hermes BYTECODE (`.hbc`): en frío no hay
  parseo de JS — el load escala con el tamaño, y **un 1 % no fabrica una
  franja de un segundo.**
- **⇒ LO QUE QUEDA: DISPOSITIVO/ARRANQUE.** La dispersión de ~1 s entre
  corridas consecutivas CON EL MISMO BUNDLE apunta a
  scheduler/térmico/GC del aparato — territorio de traza. **El límite
  honesto, declarado: el siguiente paso es P4 (traza de arranque
  Perfetto), es CARO (una ventana entera + análisis) y SE PIDE APARTE.**
  Hasta esa traza, ninguna cura de código tiene diagnóstico que la
  respalde.

### 🔒 REGLA OPERATIVA (16-ago, de mesa con dos gates quemados en una sesión)

**NINGÚN OTA SE PUBLICA PARA UN GATE SIN VERIFICAR QUE LO QUE EL GATE VA A
MIRAR ESTÉ EN EL SHA PUBLICADO** — `merge-base --is-ancestor
<sha-de-la-tanda-que-el-gate-mira> <ancla>` ANTES del publish. La ventana
limpia (regla 82) no alcanza: *un publish puede estar impecable y no
llevar su carga* (el huérfano `d2d636ad` salió con ancla limpia y sin L2,
y el founder y C miraron pantallas viejas — la Hoja con «Teléfono —
opcional» que la firma manda matar).

### 🔴 #0a NO CIERRA (16-ago, la medición de C en aparato) — Y EL HALLAZGO VALE MÁS QUE EL NÚMERO

- **El número:** C midió **~3,5–4,5 s en frío contra un previo de
  2,0–2,5 — el HOY NO volvió a su estado previo.** El discriminador de
  #0a falló. **La firma del founder sobre #0b se REABRE** (postergó
  suponiendo ~2,3 s; el supuesto no se cumple) — **pero la mesa NO reabre
  la cola hasta saber qué domina** (abajo).
- **EL HALLAZGO (de C, depositado por orden de mesa): LA DISPERSIÓN ES DE
  CASI UN SEGUNDO** — el mismo offset da esqueleto o contenido en
  corridas consecutivas; **el cruce no es un punto, es una FRANJA. La
  variable que domina NO es el código.**
- **La consecuencia de método, que es la que vale:** mis 3 olas ganadas
  son **~450 ms** y la dispersión de C es **~1 s** — **mi mejora EXISTE y
  su instrumento no puede verla. Los dos números son ciertos. Lo que el
  usuario percibe es el de C.**
- **📐 EL PLAN DE MEDICIÓN SERVIDO (no la cura — L-235 manda), con el
  costo de cada camino para discriminar qué domina la franja:**
  1. **P1 — VIDEO en vez de sondeo (mata la ambigüedad
     esqueleto/contenido):** `adb screenrecord` durante 3 fríos y contar
     en el video el frame donde aparece CONTENIDO. Precisión sub-100 ms,
     CERO efecto sonda (el sondeo por dumps costaba 3,4 s por dump — se
     medía a sí mismo, C ya lo cazó una vez). **Costo: ~5 min extra en la
     MISMA ventana de aparato.**
  2. **P2 — el peaje DEL MOMENTO, en paralelo y gratis:** correr `d0`
     desde escritorio EN EL MISMO MINUTO que los fríos de C. Si el p50
     está en ~150 ms estable → la red de la casa no es; si salta →
     **la red del momento domina** y ni #0b la cura. **Costo: 0 (script
     existe, corre en paralelo).**
  3. **P3 — la pata del APARATO separada de la del escritorio:** RTT
     desde el teléfono al host de la base (por `adb shell`, ~20 tiros)
     durante la misma ventana. Si el RTT del aparato varía cientos de ms
     con el escritorio quieto → es la WiFi/radio del teléfono. **Costo:
     ~1 min, mismo cable.**
  4. **P4 — SOLO si P1–P3 no discriminan:** traza de arranque
     (Perfetto/systrace) para separar JS-thread/parse del bundle de la
     red. **Costo: una ventana entera + análisis — se pide aparte, no se
     asume.**
  **Con P1+P2+P3 (~10 min extra en UNA ventana) se sabe si la franja es
  del instrumento (sampling), de la red del momento, o del dispositivo —
  y recién ahí se propone cura.** Nota contra el candidato obvio: el
  prólogo serial de D-738 **ya está curado en este OTA** (17→14 olas
  medidas del código) — si la franja persiste sobre el bundle nuevo, el
  prólogo NO es la variable que domina, y curar más olas sería operar al
  paciente equivocado.

### 📱 OTA 3 — #0a PUBLICADO (16-ago; SUPERSEDE a los de abajo — el DESPUÉS del C4, con el veredicto de arriba)

- **App PRESTADOR** · canal `preview` · runtime **1.0.5** · **group
  `4ad6770e-3a0b-4135-bb44-581af3276bf5`** (android `01a0085d-36be-…`).
- **Ancla del OBJETO: `adc254297ac84506f089b6e4122df77401a440f8`**
  (= `origin/main`) · **dirty=None**. Ventana regla 82 abierta y cerrada.
- **Qué lleva:** #0a COMPLETO (el guard raíz Y el prólogo del HOY en UN
  viaje — absorción verificada por censo: **17→14 olas · la sonda
  `contextoVentas` MUERTA**, cero convivencia) · R46 y R42 10→6 de B/C ·
  UNA entrada del vendedor puro (la duplicación curada) · el pie fijo de
  `Hoja` + `tituloEntero` · la tanda L4 de D entera (su pedido de gate,
  cumplido). **L2 a medias NO viajó: no había — las tres tandas mergeadas
  eran curas terminadas.**
- **⏸️ PARA C — PAUSA DE MEDICIÓN (~20 min, orden de mesa; NO es entrega):**
  el método del C4 ya está escrito; **el pie de Cuenta debe decir
  `update 01a0085d · preview`** antes de medir. **El discriminador de #0a:
  el HOY vuelve a su estado previo O MEJOR** (el ANTES de la regresión:
  2,0–2,5 s frío) — **NO es el <2 s** (ése quedó para #0b, firmado
  post-vitrina). Los tres instrumentos coinciden: mi censo (17→14 olas,
  runtime real 13 — la ola de `obtenerMiPrestador` que queda es un borde
  muerto estático), tus segundos, el dedo del founder. **Y la aritmética
  del founder queda a la vista al reportar:** firmó postergar #0b sobre el
  supuesto de ~2,3 s — si tu número sale mejor, la decisión vale doble; si
  sale peor, la mesa se la vuelve a servir.

### 📱 OTA 2 — LAS CURAS DEL GATE (15-ago, más tarde; SUPERSEDE al de abajo)

- **App PRESTADOR** · canal `preview` · runtime **1.0.5** · **group
  `4e99986c-bc29-4632-ad6d-420ade86380c`** (android `01a00747-8f44-…`).
- **Ancla leída del OBJETO: `1da864accfbc125ef598a018181c4362749382fe`**
  (= `origin/main`, pusheado ANTES de publicar) · **dirty=None**. Ventana
  regla 82: worktree detached, árbol en 0, publicado, verificado, retirado.
  **Sin migraciones en la tanda** (curas JS puras) — motor y bundle juntos
  trivialmente.
- **Qué lleva encima del OTA 1:** la tarjeta roja del HOY del vendedor puro
  CURADA (monta la misma pieza que su panel — el punto ① donde el founder
  se trabó) · «Casos que te confíen» fuera para duenodes · D-829 y las
  notas por repo (docs). **El pie de Cuenta debe decir `update 01a00747`
  antes de re-evaluar.** Este es ahora el DESPUÉS vigente del C4.

### 📱 OTA PUBLICADO (15-ago — freno ③ levantado por palabra del founder; el DESPUÉS del C4)

- **App: PRESTADOR** (todo lo que el gate mira vive ahí; el cliente NO se
  publicó — nada del gate lo necesita, y se declara para que nadie lo dé
  por publicado). Canal `preview` · **runtime 1.0.5** (la build del aparato).
- **EL SHA PUBLICADO, leído del OBJETO con `update:view --json`:**
  **ancla `7e6dc89965b87397b058c883cda1c0dd522f3ff6`** (= `origin/main` al
  publicar) · **`isGitWorkingTreeDirty: None`** — sin asterisco.
- **Group `f690bdc0-46a8-4f97-8653-82ddbb1df2b7`** · android
  `01a00717-74bc-7a5f-…` · ios `01a00717-74bc-7445-…`.
- **La ventana (regla 82):** worktree DETACHED sobre `7e6dc899` creado,
  árbol verificado en 0 antes del bundling, publicado, ancla verificada del
  objeto, worktree retirado. Motor y bundle viajan JUNTOS: las migraciones
  de S99-A ya estaban aplicadas y **la compatibilidad se verificó DEL OBJETO
  antes de publicar** — `p_correo DEFAULT NULL` en las dos puertas
  (`pg_get_function_arguments`) · exigencia `correo_requerido` APAGADA en
  `registrar_repartidor` (prosrc) · guard #9 `repartidor_sin_cuenta` VIVO en
  `despachar_pedido` · las tres `definir_*` con retorno aditivo (`accion`).
- **Para la comparación de tiempos:** el C4 de C es el ANTES (OTA S98
  `01a00373`, ancla `d4613ce9`) — **este group es el DESPUÉS**. La pasada
  del founder se hace sobre este OTA aplicado (doble reinicio; el pie de
  Cuenta debe decir `update 01a00717 · preview` antes de evaluar nada).

### 🔒 REGLA OPERATIVA — LOS DOS ÁRBOLES (16-ago, orden de mesa; al ARRANQUE de las CUATRO pistas)

**EL DIRECTORIO DE TRABAJO SE VERIFICA ANTES DE ESCRIBIR, NO DESPUÉS DE QUE
EL ROJO APAREZCA** (`pwd` / ruta absoluta / `git -C`). Es **la familia
L-235 en el sistema de archivos: el trabajo aplicado sobre el objeto
equivocado** — y mordió TRES veces en una sesión: la lib ensanchada en un
árbol y el guard reescrito en el otro (typecheck rojo por «no exported
member» que era cierto… en el árbol equivocado) · las salidas de perf
escritas al primario · el `worktree remove` con ruta relativa desde
adentro. *El primario y el worktree comparten refs y contenido casi
idéntico: el error no da síntoma hasta que una mitad consume a la otra.*
Cwd tras un `cd` encadenado NO se recuerda: se mide.

### 🔒 REGLA OPERATIVA DE MESA (15-ago — rige al ARRANQUE de las CUATRO pistas)

**NINGUNA PISTA CIERRA VENTANA SIN VERIFICAR QUE SU RAMA ESTÁ EN ORIGIN, POR
SHA** — `git ls-remote origin pista/<rama>` debe imprimir un sha que coincida
con `git rev-parse` local (jamás por código de salida: **L-239**, `ls-remote`
da EXIT 0 con ref inexistente). *Hoy pasó dos veces: la tanda de B vivió sola
en su rama mientras este Loop ya la referenciaba, y el lote de C —con una
medición irrepetible adentro— vivió solo en un disco. Los tres semáforos que
las pistas usan para declarar cerrado prenden igual con el trabajo a medio
camino.*

### La cola viva de A, en orden

0. **🧊 EL LOTE #0: CONGELADO EN D-834 (firma 16-ago noche).** #0a quedó
   CONSTRUIDO y publicado (las olas 17→14 son reales y se quedan); su
   cierre de aparato se SUBSUME en la investigación aplazada — el número
   no volvió y la causa NO es el código (red/olas/bundle muertos por
   medición). #0b DISUELTO. **Lo próximo de A: L3** (abajo). *(Historial
   del lote, como quedó al partirse:)*
   **✒️ EL LOTE #0 PARTIDO — FIRMADO POR EL FOUNDER (16-ago; la letra vive
   en `PLAN_S99` §2, acá el puntero):** **#0b va DESPUÉS de la vitrina** —
   la cola de la sesión queda **#0a → L2 → L3 → L5a → L5b → #0b**. Y el
   discriminador se PARTIÓ con el lote: **#0a cierra volviendo el HOY a su
   estado previo o mejor, EN APARATO con C; el <2 s es de #0b.** Los tres
   instrumentos coinciden en ambos.
   - **#0a — LA RPC CABLEADA + LA COMPOSICIÓN POR CAPACIDAD.** Cierra LA
     REGRESIÓN (devuelve el HOY a su estado previo o mejor). **Sigue
     INDISPENSABLE, va primero, es mío y NO se frena.** Apenas cierre:
     **reporto su número EN APARATO con C.**
   - **#0b — LA COLA DE ~9 OLAS SERIALES (~1,4 s).** Es la que lleva a
     <2 s — y es **DEUDA PREEXISTENTE que la medición destapó de paso**
     (existía antes de S99; no estaba en ningún plan). Compite directo con
     la vitrina: *un vendedor sin vitrina no vende; uno con un HOY de
     2,3 s vende igual que hoy.* **Si #0a deja el HOY cerca de 2,3 s, la
     mesa propone al founder correr #0b DESPUÉS de la vitrina — deja de
     ser bloqueante de octubre por default.** El censo y la cura servida
     quedan abajo, listos para cuando le toque.

   **🔴 LA REGRESIÓN DEL HOY — MOTOR HECHO, FALTA LA COMPOSICIÓN (#0a).**
   **✅ D-738 motor: `obtener_contexto_arranque()` APLICADA y REGISTRADA**
   (15-ago tarde — cinturón 4/4 por brazo, E2E camino real 2/2, contrato
   §12.5 de D cumplido; el prólogo de 4 esperas cabe en UN viaje). **⏳
   Falta:** la composición por capacidad en las libs + el HOY montando por
   capacidad — **el brazo DUAL ya NO espera: FIRMA del founder 15-ago,
   duenotodo aterriza en CITAS con contador en la puerta hermana** (la
   pieza del contador es de B; el aterrizaje es composición mía/D) · el
   cierre en aparato **<2 s en frío** con los tres instrumentos
   coincidiendo. **✅ `ha_vendido` YA viaja en el contexto** (migración
   `20260817150000`, dos brazos verificados: TRUE con el dueño de los 2
   entregados reales, FALSE con la despensa S97 y demovet).
   **⏪ CORRECCIÓN (16-ago, el número de D contra su propio anuncio): EL
   REGALO DE LA LÁPIDA NO APORTA TIEMPO.** Medido: 46→45 y 45→44
   peticiones · **18→18 y 17→17 OLAS** — `v_pedidos_narrativa` viajaba EN
   PARALELO en la última ola: sacó una petición y CERO olas. No aporta un
   milisegundo al <2 s. **Lo que L4 aportó fue el HALLAZGO: la condición
   que parecía discriminador era un UNIVERSAL** (todo prestador con cuenta
   pagaba el fetch). *El párrafo anterior de este mismo Loop decía «ya bajó
   olas» — queda corregido acá, no borrado.*
   **⏰ EL RELOJ ESTÁ EN DOS LUGARES (runtime de D, adjudicado):**
   - **8 OLAS DE PRÓLOGO** antes de que empiece el día, con
     `obtener_mi_prestador` pedido DOS VECES — es D-738 exacto: **mi RPC lo
     come y NO ESTÁ CABLEADA. El 17 de D es el ANTES de mi motor.
     Cablearla es LO PRÓXIMO y es mío.** La composición ABSORBE además la
     sonda `contextoVentas()` (2 peticiones por foco, para todos, sin
     caché) — cero convivencia, verificado con el censo de olas.
   - **🔴 LA COLA: ~9 olas de UNA petición tras la ola grande ≈1,4 s de
     puro esperar en fila. CENSADA (16-ago, contra el código):** la forma
     estática es `resolverUrlsFotos → citasPorCoordinar → franjasHorario →
     equipoNegocio → jornadaRecepcion → plataDelDia(×2 — ya viajó en la
     ola 10)`, cada una en SU efecto esperando un id que YA está resuelto
     (`cuentaR.data.id` · `cuenta_comercial_id` · `prestadorId`). **Sin
     dependencia de DATOS salvo `resolverUrlsFotos`** (necesita las fotos
     de la ola grande — la única cadena legítima, como el `extras` de D).
     **LA CURA SERVIDA (no construida):** plegar la cola A LA OLA GRANDE
     (o a UNA segunda ola paralela tras el contexto) + matar la repetida
     de plata — *la prueba de que es curable está al lado: la ola 10 mete
     16 peticiones en paralelo sin problema; el problema no es cuánto
     pide, es que estas se pidieron de a una.* Sin esta cura, 1,4 s quedan
     intactos y <2 s NO se alcanza. Las repetidas del runtime de D:
     `prestador_servicios` ×6 · `evento_cita_servicio` ×4 ·
     `cuentas_comerciales` ×3 · `prestador_empleados` ×3.
   **⚠️ EL LÍMITE DEL NÚMERO (D lo declaró, la mesa lo hizo suyo, y acá
   rige):** el 17 es RN-web en escritorio — cuenta viajes y muestra la
   forma de la cadena; **un verde ahí NO cierra el lote #0. El cierre
   sigue siendo <2 s en frío EN APARATO, los tres instrumentos
   coincidiendo.**
   *Historial del discriminador abajo.*
   C midió 2,0–2,5 s → 3,8–4,0 s (mismo método/ancla/cuenta/red). Corrí
   los DOS instrumentos sobre main: **d0 por-consulta QUIETO** (Δ+1–4 %,
   peaje 150→162 ms = jitter; el prólogo idéntico 463→466 ms — la red por
   viaje NO cambió) · **el censo del CÓDIGO (instrumento de C) dice que el
   HOY pasó de 13 olas/28 llamadas a 17 olas/35 llamadas.** La causa,
   leída del código: la cura de la tarjeta roja montó el panel de ventas
   ADENTRO del HOY con sus propias olas, y **`contextoVentas()` corre para
   TODA cuenta al montar** (`index.tsx:921`, la sonda `esVendedora`) — y
   Aurora ES vendedora desde el demo S98, así que la cuenta del baseline
   paga las DOS mitades. **NO es N11 ⇒ NO hay bisect por OTA.** 17 olas ×
   ~160 ms ≈ 2,7 s de red encadenada en frío — cuadra con lo que el dedo
   siente («en frío cuesta mucho, en caliente va bien»). **La cura, por
   orden de mesa: es RED, curable sin tocar pantallas — D-738 (el prólogo
   de identidad, 463 ms medidos HOY de cadena pura) ENTRA AHORA como cura
   reina.** *Discriminador reportado ANTES de proponer cura, como estaba
   ordenado.*
   **⚖️ DOS ADJUDICACIONES DE MESA sobre este lote (15-ago, antes de
   arrancar):**
   - **LA CURA NO ES DEDUP: ES COMPOSICIÓN POR CAPACIDAD.** Que
     `contextoVentas()` corra para TODA cuenta contradice §2.0 de frente
     (*«lo que determina mostrar más o menos son los servicios que
     presta»*) — la BARRA ya se compone por capacidad y el HOY no: un
     veterinario sin despensa paga hoy por un panel que jamás verá.
     *Deduplicar cura el síntoma y deja viva la causa: la próxima
     capacidad vuelve a sumarle peso a todas las cuentas.* La cura: **el
     HOY monta por capacidad con la MISMA composición que arma la barra —
     una fuente, N consumidores.** **COSTURA DECLARADA con D:** esa
     composición es la que L4 necesita (con el dual, los pedidos de
     duenotodo viven en su ventana hermana y el HOY no los monta). **Se
     escribe UNA vez, en este lote, de forma que L4 la CONSUMA en vez de
     deshacerla — el contrato se acuerda con D POR REPO antes de escribir,
     no al mergear** (propuesta servida en 📬 PARA D).
   - **EL DISCRIMINADOR DE CIERRE ES <2 s EN FRÍO, NO «volver al
     baseline»:** sacando las 4 olas de la cura quedan 13 × ~160 ms ≈
     2,1 s — **el baseline YA fallaba N16**. Las TRES piezas juntas llegan
     al número (las 4 olas de la cura · D-738 · la composición por
     capacidad) — ninguna sola alcanza. **Y el instrumento del cierre es
     el de C EN APARATO, no solo el censo:** el censo cuenta olas, el
     aparato cuenta segundos, el founder mide con el dedo — **los tres
     tienen que decir lo mismo antes de declarar cerrado.**
1. **L5a MOTOR — la carga determinista de stock (XLSX/CSV, cero IA)** — lo
   próximo que A construye. Diseño adjudicado: parse determinista + el camino
   de propuesta por `proponer_sku_vendedor` (vivo desde S95-F), M21 intacta
   (el vendedor aporta, e-PetPlace publica). La mitad IA (PDF/foto + mapeo)
   tiene FECHA: post-15-sep — no se adelanta.
2. **Encender `correo_requerido`** en `registrar_repartidor` — SOLO cuando C
   declare en su parte que su pantalla de L2 exige correo Y su OTA está
   aplicado (orden S98: pantalla → merge → OTA → guard).
3. **El lector del track del envío** (L6, v1.1 — SIN apuro por prioridad):
   contrato acordado con D — puntos con `t` en ISO string (forma de
   `PuntoTrackMapa`), la conversión epoch→ISO vive en el lector.
3ter. **⚖️ L5B: REPARTO ADJUDICADO SOBRE EL CENSO (mesa 16-ago noche):**
   **ARRANCA YA (motor cero/bajo):** la VOZ del borde propuesto/publicada
   en las dos caras · **el $/kg expuesto en el lector (MÍO, primero)** ·
   la celda de D-829 · la completitud-vendedor DERIVADA de las mismas
   razones del cliente (jamás cómputo paralelo). **EN PARALELO:** la
   extracción de las piezas del cliente (precedente `checkout-reserva`
   S60) — lo que más pesa y no depende de decisiones. **🔴 EL EXPRÉS SALE
   DEL CAMINO CRÍTICO DE LA VITRINA:** se construye DESPUÉS de que la
   vitrina esté en pie, con su lectura de `MODELO_FINANCIERO` hecha ANTES
   de la migración — *si se atrasa arrastra a la vitrina, y un vendedor
   sin vitrina no vende mientras que sin exprés vende igual.* Su regla
   dura sigue siendo de LECTOR.
   **✅ EL CENSO DE L5B: SERVIDO** (`docs/relevamientos/2026-08-16-s99a-L5B-CENSO.md`)
   — las siete piezas con qué existe/lector/falta/costo, las costuras
   declaradas (espejo C · piezas B · lectores A · exprés + finanzas), y
   las SEIS recetas que B debe entregar antes de que C escriba. Los
   titulares: **el $/kg SÍ tiene dato** (`producto_variantes.peso_kg` —
   mi primer veredicto era falso y se corrigió midiendo la tabla de al
   lado) · **el borde propuesto/publicada tiene motor CERO-falta** (el
   lector ya trae los dos estados) · **el EXPRÉS no existe NADA en el
   esquema** («modelado y apagado» era letra) — es el costo alto del lote
   y toca plata: FINANCIERO antes de migrar.
3bis. **LOS DOS PEDIDOS DE C PARA CERRAR L2 (míos; no bloquean el gate):**
   ① `listarRepartidores` devuelve **CORREO** (sin él la edición no
   pre-llena ni exige — mandarlo vacío lo borraría) · ② un **LECTOR DE
   VIAJES por repartidor** (el dato existe en `envios`; sin él el campo ⑤
   de la ficha no se monta — *un cero fijo mentiría sobre alguien que sí
   entregó*). C los declaró EN PANTALLA en vez de disimularlos.
4. **Rutina de cierre de CADA tanda:** `git fetch origin pista/s99-b` +
   merge a main con verificación por contenido (verify:diseno) — **sin
   esperar aviso: B no tiene canal.**

### 📡 EL CANAL ENTRE SESIONES ESTÁ CAÍDO PARA LAS CUATRO PISTAS — TODO VIAJA POR REPO
(partes de Loop, fichas, JSDoc). Los mensajes directos no existen hasta nuevo
aviso.

### Compromisos abiertos, pista por pista

- **CON C:** yo le debo ① encender `correo_requerido` tras su OTA (arriba) ·
  ② códigos tipados por constraint A DEMANDA (**D-827**: me los pide con el
  constraint exacto + el literal del rebote; jamás mapeo genérico de 23514) ·
  ③ **D-791 lado C es SUYO** (reabrir cortes/recursos/regla en el mismo
  formulario consumiendo `accion`/`comprometidos`/`nota` — el motor ya lo
  devuelve todo). C me debe: ~~la pieza `ventana-pedidos` · los dos cuartos~~
  **ENTREGADOS y en main (15-ago)** — queda CAMINARLOS sobre main (paso ③
  del gate) · su declaración de OTA para el punto ①.
- **CON B:** yo le debo el merge de su rama en cada cierre (rutina, sin
  aviso). B me debe: la declaración del resultado de su verificación
  R42/R43 (partición por dueño/curabilidad hacia atrás — orden de mesa: se
  declara en cualquiera de los dos sentidos; su rama ya trajo `a0538332`,
  verificar en su parte si la declaración está completa) · las 3 líneas
  BLOQUEADAS de R44 bajan solas cuando yo entregue D-827 puerta a puerta.
- **CON D:** yo le debo el lector del track (L6/v1.1, contrato `t` ISO). D
  me debe: nada — su L1 cascarón **YA está en main (mergeado 15-ago, guards
  verdes)**; la lápida del comentario `lineaItems` la pone al abrir L4.

### 🚪 EL DISPARADOR DEL GATE 1 — ①② HECHOS, FALTA CAMINAR

**La cadena: ① el cascarón de D entra a main ✅ → ② la tanda de C entra a
main ✅ → ③ C camina sus DOS cuartos ✅ (15-ago, sobre `7e6dc899` — en WEB
con par comparable declarado: el ANTES del redirect se midió igual) → ④ LA
PASADA DEL FOUNDER — YA EMPEZÓ y produjo dos curas (la tarjeta roja del
HOY del vendedor puro · «Casos que te confíen» fuera) que C cerró y esta
tanda mergeó, más el hallazgo D-829. El OTA con las curas se publica sobre
el sha que las contiene (registro abajo).** (`2026-08-15-s99-HOJA-DE-GATE-1.md`, 4
preguntas en orden: L1-como-duenodes+ATENDER-destacada · ojo N11 sobre
`ventas/configuracion` · moto a 21px · el número de tiempos vs N16 con el
ANTES pre-S99 de C4 ya adentro). **C4 está CERRADO** — su número vive en la
hoja ④ con su premisa declarada.

### Fichas de A a demanda (no abren frente solas)

- **D-826** (la pantalla muestra `r.mensaje` crudo — dispara por lote que
  toque esa frontera) · **D-827** (códigos por constraint — dispara por
  pedido de C con constraint+literal) · **D-828** (migración a `…EnRango`
  declara `sinFecha` antes de mergear — el lint R45 de B ya vigila,
  baseline 0) · **D-791 lado C** (la ficha muere cuando C reabra la config
  en el formulario Y el founder camine la línea del compromiso).

## 🔧 OPERATIVO (registro, sin secretos)

- **15-ago · orden directa del founder:** la clave de `guillo381+8@gmail.com`
  (su cuenta pet-parent real) estaba mal — reseteada a la compartida del
  keychain por SQL directo (`crypt bf`, guard de exactamente-1-fila).
  **Camino real: login 400 antes → 200 después.** La clave jamás tocó chat
  ni repo; el archivo temporal (0600) destruido y verificado ausente — con
  un atrape declarado: *el glob de zsh sin match ABORTA el comando entero*,
  el `rm` encadenado no corrió y hubo que volver por él. Durante la urgencia
  el plano postgres estuvo caído ~10 min («connect as temp role» timeout)
  con el REST vivo — se cubrió con recuperación-por-código como plan B (el
  correo enviado quedó sin usar, expira solo) y la base volvió antes.

---

# 🔷 CENSO SERVIDO — LA FICHA DE PRODUCTO EDITABLE (firma «Tu tienda», 16-ago)

**Pedido de la mesa:** *«medí qué falta de motor para editar stock y precio
desde la ficha — el ajuste de stock existe, el precio de la oferta también;
lo que hay que verificar es si las dos puertas están disponibles desde ahí.»*

## ✅ STOCK — COMPLETO. C puede construir el control HOY, sin motor nuevo.
`ajustar_stock_vendedor(p_sku_id, p_cantidad, p_motivo)` — **gate
`es_vendedor_de(cuenta) OR is_admin()`**, `authenticated` puede ejecutarla,
**entra por el LEDGER con motivo** (jamás pisa el saldo: lo mueve el trigger).
El wrapper `ajustarStockVendedor` **ya existe** en `despensa-vendedor.ts`.
Y desde esta jornada **el rebote habla**: `stock_insuficiente` /
`reserva_insuficiente` en vez de una violación de constraint.
⇒ **Falta CERO de motor. Falta la receta de B y la pantalla de C.**

## 🔴 PRECIO — EL VENDEDOR NO TIENE PUERTA, Y NO ES UN OLVIDO
**La única función que escribe `ofertas.precio` es `publicar_oferta_sku`, y su
gate es `is_admin()` PURO** (medido: rebota `no_sos_admin` — me lo cobró la
siembra de competencia de esta misma jornada).
**Es la firma ⑩ de S95 haciendo exactamente lo que fue escrita para hacer:
«el vendedor PROPONE, e-PetPlace PUBLICA».**

**Y el camino de propuesta existe pero no alcanza:** re-llamar
`proponer_sku_vendedor` actualiza `precio_propuesto` y **NO despublica** (su
`ON CONFLICT` lo dice literal: *«reproponer no despublica»*) ⇒ **el vendedor
puede pedir otro precio, y la vitrina sigue mostrando el viejo hasta que
e-PetPlace publique.**

**⚠️ Dicho de frente: la ficha que el founder firmó —«stock y precio editables
ahí mismo»— tiene HOY la mitad de sus controles sin puerta para su dueño.
Y si el control se dibuja igual, «Administrar» miente, que es exactamente lo
que la firma quiso evitar.**

### LA DECISIÓN ES DE MESA (toca letra firmada) — tres opciones con su costo
- **(a) PUERTA PROPIA DEL VENDEDOR — mi voto.** Nace
  `actualizar_precio_oferta(p_oferta_id, p_precio)`, **espejo exacto del gate
  de stock** (`es_vendedor_de OR is_admin`), **solo sobre ofertas YA
  publicadas de su cuenta**. *Razón: la firma ⑩ protege la CURADURÍA —qué
  productos entran a la vitrina—, no el precio de lo que ya fue curado. El
  precio es del negocio del vendedor, y §2.2bis ya construye la comisión
  sobre lo que ÉL cobra.* **Costo: una migración chica** (función + L-140 +
  cinturón con su discriminador: el vendedor ajeno rebota) **y un wrapper.**
  **Lo que NO resuelve y hay que decidir con ella:** si el cambio queda
  registrado (append) o pisa en silencio — *un precio que cambia sin rastro
  es una discusión con un cliente que nadie puede arbitrar.*
- **(b) PROPONER Y ESPERAR.** Respeta la firma al pie y **no cuesta motor
  nuevo**… pero **cuesta lector**: hoy el reader del vendedor trae
  `ofertas(precio, estado)` y **NO trae `precio_propuesto`** ⇒ el vendedor
  cambiaría el precio, no vería ningún cambio, y **la ficha mentiría por
  omisión**. Si gana (b), su precondición es una línea mía: el campo en el
  select y en el tipo, para que la ficha pueda decir *«pediste $X · hoy se
  vende a $Y»*.
- **(c) NO EDITABLE EN v1.** Honesto y gratis, **pero rompe lo que el founder
  acaba de aprobar** — y deja la ficha con un solo control, que era justo el
  hallazgo que decidió fusionar las dos listas.

### Estado del dato hoy, para que nadie decida sobre un fantasma
**563 ofertas publicadas · 1 sola diverge entre propuesto y publicado · 2 sin
propuesto.** **Y la que diverge ES MÍA** —la dejó el cinturón de
`20260819020000`, ya declarada en D-838—: **no hay divergencia de producto,
así que (b) no está pagando una deuda existente: la crearía.**


---

# 🔷 CENSO DE LA BANDA DE PRECIO — antes de la migración (decisión de mesa, 16-ago)

## 🔴 LA PIEZA QUE FALTA EXISTE-CERO: NO HAY PRECIO DE REFERENCIA
Medido: **ni `productos` ni `producto_variantes` tienen ninguna columna de
precio**. **El único precio del sistema es el de cada `oferta`.**
⇒ **Sin referencia no hay banda contra qué medir**, tal como la mesa
anticipó. Es fundación, y es mía.

## ⚠️ Y UNA CORRECCIÓN AL PEDIDO, CON DATO: LA REFERENCIA VA EN LA VARIANTE, NO EN EL PRODUCTO
La decisión dice *«precio de referencia por producto canónico»*. **Medido:
470 productos · 538 variantes activas · 59 productos con MÁS DE UNA variante
(hasta 4).** Un solo número en el producto tendría que servir a la vez a
**una bolsa de 15 kg y a una de 3 kg de lo mismo** — y las ofertas ya viven
por variante (`ofertas.variante_id`, `vendedor_skus.variante_id`).
⇒ **`producto_variantes.precio_referencia`.** *El resto de la decisión no se
mueve: la banda, el registro y la aprobación son idénticos.*

## 🔴 LA DISPERSIÓN REAL DE MERCADO ES CERO — Y CONFIRMA LA CONDICIÓN DE MUERTE
La medición dice «25 variantes con competencia · dispersión media 8,6 % ·
máxima 48,1 %». **Ninguno de esos números es de producto: las 25 las sembré
yo con ±7-8 % a propósito, y la máxima 48,1 % la produjo mi propio cinturón**
(9,99 contra 14,80 — la fila declarada en D-838).
⇒ **el 15 % no tiene contra qué contrastarse hoy**, exactamente como la
decisión declaró. *Mi medición no lo contradice: lo confirma con evidencia.*

## LO QUE FALTA CONSTRUIR, con su costo — TODO MÍO salvo la pantalla
1. **`producto_variantes.precio_referencia numeric`** + quién lo escribe:
   **e-PetPlace (admin), misma puerta que publicar.** *v1 es USD/Ecuador
   (firma S94), así que un número alcanza; el día que haya otro país la
   referencia gana su dimensión — se declara, no se construye.*
2. **`actualizar_precio_oferta(p_oferta_id, p_precio)`** — **espejo del gate
   de stock** (`es_vendedor_de OR is_admin`), solo sobre ofertas publicadas
   de su cuenta. Dentro de banda **aplica**; fuera **guarda la propuesta y
   rebota HABLANDO** (`fuera_de_banda: la referencia es $X · la banda va de
   $Y a $Z`) — *un rechazo que no dice la banda obliga a adivinar por tanteo.*
3. **`oferta_precio_historial`** (append: oferta · anterior · nuevo · quién ·
   cuándo · dentro_de_banda). **Y la mitad que YA está resuelta y no hay que
   volver a construir: `pedido_items.precio_unitario` congela el precio que
   la familia pagó** ⇒ para un reclamo de compra la respuesta ya existe. Lo
   que el historial agrega es la vitrina de quien **miró y no compró**.
4. **El lector gana `precio_propuesto` y `precio_referencia`** — hoy trae
   solo `ofertas(precio, estado)`. Sin los dos, la pantalla no puede decir
   *«pediste $X · hoy se vende a $Y»* ni mostrar la banda **antes** de que el
   vendedor tipee, que es donde el rechazo deja de existir.

## 🔴 LAS DOS PREGUNTAS QUE LA MIGRACIÓN NO PUEDE DECIDIR SOLA
· **¿De dónde sale la primera referencia de las 538 variantes?** Sembrarla
  desde el precio de la oferta que ya existe es barato **y es CIRCULAR**: la
  banda quedaría centrada en el número que ese vendedor eligió, no en un
  criterio de e-PetPlace. **Mi voto: nace NULL y la carga el equipo con el
  catálogo** — con los seis productos curados reales es una tarde.
· **¿Qué pasa mientras la referencia es NULL?** **Mi voto: FAIL-CLOSED — sin
  referencia no hay banda, así que todo cambio va a aprobación.** *Dejarlo
  pasar libre convierte «todavía no calibramos» en «no hay límite», que es
  la lectura opuesta a la que la decisión quiso.*

