# S111-A · REPORTE DE CIERRE — motor y conducción

> **Todo lo de acá se midió contra el OBJETO el 1-sep-2026, 17:27 -05**, sobre
> `main` **`67fec425`** (local = remoto, árbol en 0, **las cinco ramas dentro**).
> **Ningún sha de este documento se cita de memoria**: cada uno declara contra
> qué y cuándo se midió. *Un sha citado en un mensaje es una foto; la rama sigue
> caminando* (`L-469`).
>
> ⚠️ **Nota de forma:** el pedido de cierre decía «reporte por buzón a A» y
> «pedí los números a A». **Yo SOY A** — conductora y escritora única de `main`.
> Cumplo el contenido y las tres reglas; los números los releí yo contra el
> objeto en el mismo acto, y el merge lo hago yo.

---

## ① CONSTRUIDO Y EJERCIDO — y por qué camino se probó

**Ejercido** = corrió por el camino real, no sólo compiló.

| qué | cómo se ejerció |
|---|---|
| **El durante de guardería, los 5 actos** | Cinturones in-migración con **rojo producido primero**; el founder recorrió su gate hasta el acta |
| **Las CINCO voces del aviso** | Medidas ejecutando `_voz_notificacion` contra la base con **control positivo** (`media_resumen` devuelve texto) y **negativo** (`sistema` devuelve `{}`) |
| **La bitácora del prestador** | **10 brazos, 5 rojos primero**, `ROLLBACK`, **residuo 0 medido**. El rojo que vale: `vomito` rebotó sobre **Pepe, un ave** |
| **El orden del día por franja** | Ejecutado sobre **datos vivos**: 2 en `retorno_en_curso` primero con su 16:30, 3 `no_recogida` al final sin franja |
| **El gate del prestador ajeno** | Un `user_id` real que no gestiona ⇒ `no_gestionas_este_prestador` |
| **`ruta` en el `data` de FCM** | Edge **desplegada**; C montó el listener y probó su parser **9/9** |
| **Los dos OTA** | `verify-ota` verde **con su rojo producido** (`--runtime 9.9.9` ⇒ HTTP 204) |

**Gates de cierre, corridos hoy:** 5 typechecks en **0** · `verify:diseno`
**62 reglas** · `verify:mensajeria` **53/53** · `verify:sin-byte-nul` **3549
archivos**. **590 migraciones**, 11 mías en S111-A, **todas con reversa escrita
ANTES**.

---

## ② 🔴 CONSTRUIDO Y NO EJERCIDO — se dice aparte a propósito

> *Lo no construido se sabe. **Lo construido y no ejercido SE LEE COMO HECHO.***

- **Los cinco avisos del durante NUNCA LLEGARON A UN TELÉFONO.** Su intención
  está probada en cinturón y su texto medido; **ninguna push salió**.
- **El deep link nunca se ejerció de punta a punta.** La `ruta` viaja y el
  listener existe; **nadie tocó una push real y llegó a la pantalla**.
- **La bitácora del prestador no se escribió desde una pantalla.** Sólo dentro
  del arnés, que hizo `ROLLBACK`. **Cero filas vivas.**
- **El orden por franja no se vio en el aparato.**
- **El traspaso de adopción nunca corrió de verdad** — no puede hasta que exista
  el acta; sólo dentro de un arnés que la cargó y la deshizo.
- **La vidriera anónima nunca se abrió en un navegador sin sesión.**
- **La mensualidad por DeUna NO se cobró.** Cero intentos del sujeto: el botón
  estuvo apagado toda la jornada y **el reintento sobre el lote curado no se
  reportó**.
- **El wrapper del tramo vivo no tiene consumidor todavía** (ver ③).

---

## ③ ENTREGADO Y NO MONTADO — piezas sin consumidor

| pieza | de quién es la puerta |
|---|---|
| `obtenerTramoVivoDeMiMascota` | **C** — destraba su espejo de ⑦ |
| `registrarBitacoraGuarderia` | **C** — entregado con su contrato; su sección de chips |
| Los tres campos de franja en `EstadiaDelDia` | **C** — ya los consume en `dia.tsx`; **montado** |
| `EvidenciaClip` (B) | **C** — montada por C el mismo día |

⚠️ **`EstadiaEnRango` NO tiene los campos de franja, a propósito**: su RPC no los
devuelve y heredarlos fabricaría tres `null` que se leen como *«no tiene
franja»* en vez de *«este lector no la trae»*. **Con `Omit` la confusión es
inexpresable.** Si alguien los necesita ahí, **se ensancha el lector — no se
simulan con null**.

---

## ④ NO CONSTRUIDO A PROPÓSITO — con su razón

*Sin esta lista se lee como trabajo que faltó.*

- **Ni una palabra de texto legal.** Las puertas se construyeron **fail-closed**
  contra documentos versionados que no existen. **Se abren solas** el día que el
  texto se cargue.
- **El protocolo del animal no retirado: nada.** Ni conteo, ni aviso, ni camino
  a refugio. Frenado por riesgo penal. **Y el aviso de no-recogida lo respeta:**
  su cinturón **aborta** si el texto nombra mora, protocolo, refugio, abandono,
  plazo o retiro.
- **El plan de paseo por DeUna: no se construyó** aunque su disparo se cumplió
  (§⑥). Falta **la rama entera** —`suscripcion_servicio_id` tiene **0
  ocurrencias** en la edge— y **entra con la pantalla del link, no antes**.
- **Cero llaves de `app_config` encendidas.** `guarderia_recurrente_vivo` sigue
  en `false`.
- **Las cinco tablas legado de adopción: no se tocaron**, ni para construir ni
  para borrar (`D-991`).
- **El 5 % a la fundación: nada**, espera al contador.
- **`entregada` sigue bloqueando la bitácora**, como se pidió — con su tensión
  **declarada** y no resuelta sola.
- **Cero backfill** en las 11 migraciones. **76(g) declarada NO RIGE** en todas.

---

## ⑤ FICHAS Y LECCIONES, cada una con su DISPARO

| # | qué | disparo — cuándo se cobra |
|---|---|---|
| `D-997` | DeUna y Nuvei **certificados** 1-sep; la mensualidad por DeUna nunca cobró | **El reintento del founder sobre el lote curado.** Si el botón habilita y cobra, cierra |
| `D-999` | **8 pantallas** pasan la razón sin `onRazon` ⇒ el botón frena **y no explica** | **El gate que lo mide** (`razonDeshabilitado` sin `onRazon`, hoy 8, baseline solo-baja) |
| `D-996` | La ruta del viaje como producto propio; **v1 va sin ruta** | Nada la bloquea |
| `L-467` | El **runtime es la frontera**: el discriminador se corre ANTES de cada corte | **Cada vez que alguien vaya a hornear** |
| `L-468` | Un cinturón que mide que el aviso **nace** no mide que **diga algo** | **Cada cinturón sobre algo que otra pieza va a renderizar** |
| `L-469` | El aviso lleva group **y los shas que contiene**; el control corre **contra el HEAD, inmediatamente antes del publish** | **Cada publish con más de una pista viva** |

⚠️ **`D-998` se tomó DOS VECES hoy** mientras dos pistas medían en paralelo.
**Regla 89 cobrando en vivo:** el número se relee **en el mismo acto** en que se
escribe.

---

## ⑥ LO QUE ESPERA FIRMA O AUTORIZACIÓN — en lenguaje de negocio

| # | qué esperamos de vos | evidencia | mi voto |
|---|---|---|---|
| **1** | 🔴 **Tu pasada de guardería, completa** — y con ella el «autorizo» de publicar. La veda sigue entera. | los dos OTA vigentes | **Primero esto**; nada más importa hasta que camines |
| **2** | **¿El cuidador puede anotar conductas DESPUÉS de entregar?** Hoy no. El animal sí estuvo, y sus manos quedan libres justo ahí. | tensión declarada en la migración | **Abrir la ventana.** Es sacar un valor de una lista y ninguna pantalla cambia |
| **3** | **¿`retorno` es aviso `relacional` u `operacion`?** Lo puse relacional, por coherencia con sus tres hermanos del viaje. | declarado en la migración | **Dejarlo.** Corregirlo es un `UPDATE` de una fila |
| **4** | **El plan de paseo por DeUna: su disparo SE CUMPLIÓ.** DeUna cobró 4 veces hoy, con id de transacción. | `D-997` anexo A | **Todavía no.** Entra **con** la pantalla del link, o el link llega a una pantalla que no sabe leerlo |
| **5** | **La única mensualidad de guardería activa es de un AVE** (`20d025ca`, Pepe), y guardería admite perro y gato. | `D-997` anexo C | **Cerrar la puerta antes de vender.** Hoy es el único sujeto vivo para probar |
| **6** | **El paquete del abogado** — sin ese texto el traspaso de adopción **no ocurre**. La puerta está construida y cerrada. | `adopcion_documentos` vacía a propósito | Es tuyo |
| **7** | **Ocho decisiones más**, con opciones, voto y **construidas fail-closed** | `S111-ESTACIONAMIENTO.md` | — |

---

## ⑦ EL ESTADO OPERATIVO, medido hoy 17:27 -05

| qué | valor |
|---|---|
| `main` | **`67fec425`**, local = remoto, árbol **0** |
| ramas | **las cinco** dentro de `origin/main` |
| migraciones | **590** · **11 de S111-A**, todas con reversa **ANTES** |
| cinturones | **verdes, cada uno con su rojo producido primero** |
| typechecks | **5 en 0** |
| gates | `diseno` **62** · `mensajeria` **53/53** · `sin-byte-nul` **3549** |
| **OTA cliente** | **`0b885c6a-7543-4362-88f5-e6bf9d09562e`** · ancla `df9b1c83` · `updateId` **`01a05eed`** |
| **OTA prestador** | **`9e34d41a-bcbc-48fc-b142-aa682b2faaa0`** · ancla `f86b7e50` · verificado que su bundle **no cambió** desde entonces |
| fichas / lecciones | hasta **`D-999`** · hasta **`L-469`** |
| publish a producción | **CERO** — veda sostenida |

⚠️ **El pie de Cuenta muestra el `updateId`, NO el group.** Tiene que decir
**`01a05eed`** en el cliente. *Dos identificadores parecidos donde uno se lee
como el otro ya costó una vuelta hoy* — `D-785` otra vez.

---

## ⑧ LOS DOS ERRORES DE CONDUCCIÓN, sin maquillar

**① Mergeé estando parado EN la rama** — un no-op. El commit existía, la rama en
origin, el árbol limpio, **y el merge no había ocurrido**. Lo cazó el control de
ancestría. **Pasó dos veces**; la cura es correr `git -C <primario>`, no
acordarse.

**② Publiqué tres veces el mismo lote** porque el trabajo de otra pista llegaba
después. **Ninguna de las tres fue negligencia de nadie**: el control estaba
**después del merge**, y entre el merge y el bundling hay una ventana. **Hoy
corre inmediatamente antes del publish** (`L-469`).

*Las dos las encontró el mismo instrumento, y ninguna tenía síntoma.*
