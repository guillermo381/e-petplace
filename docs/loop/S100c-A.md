# S100c-A — EL ROJO DE CONFIANZA Y LA DIRECCIÓN A MANO

> **Pista A · rama `pista-a` · worktree `e-petplace-s100-a`.**
> Territorio: `apps/cliente` — carrito, «¿para quién es?», dirección y entrega, resumen, pago, listo.
> **El SHA de esta rama se MIDE, no se lee de acá:** `git ls-remote origin pista-a`.
> **Todo número de este documento se midió el 18-ago-2026 con el repo a la vista.** *Lo derivado decae
> mientras el objeto no* — si leés esto más tarde, re-medí.

---

## ⓪ LÍNEA BASE, MEDIDA ANTES DE TOCAR NADA

| instrumento | al abrir |
|---|---|
| worktree | árbol en **0** · rama `pista-a` |
| `node scripts/verify-diseno.mjs` | ✅ **VERDE · 46 reglas** · exit 0 · R52 y R54 **DURAS EN 0** · R53 baseline 11 congelado |
| `tsc --noEmit` (cliente) | ✅ **exit 0** |
| aparato | **NO tengo.** Todo lo de acá es fuente, tipos y consultas al código — **jamás píxeles** |

---

# 🔴 §1 · LAS TRES PREMISAS QUE LA MEDICIÓN CORRIGIÓ

*Va primero a propósito: tres de las seis órdenes que recibí traían una premisa que el objeto no
sostiene, y las tres corrigen hacia **menos trabajo o distinto trabajo**. La lección de la vuelta pasada
—«mi aritmética era correcta y mi premisa era falsa»— se cobra mejor antes de construir que después.*

## ① 🔴 EL GATE DE STOCK NO VALIDA «AL RETOMAR Y AL PAGAR». NO VALIDA EN NINGÚN LADO.

Mi arranque dice: *«el gate que construiste en S100 valida al RETOMAR el carrito y al pagar, pero no AL
AGREGAR»*. **Medido — censo de `revalidarCarrito` en todo el repo, `.ts` y `.tsx`, sin `node_modules`:**

| dónde aparece | qué es |
|---|---|
| `packages/api/src/wrappers/despensa-pedido.ts:387` | **su definición** |
| `packages/api/src/index.ts:1165` | **su export** |

**DOS apariciones, y ninguna es una llamada. CERO consumidores.** El `useFocusEffect` del carrito
(`carrito.tsx:85-116`) carga mascotas y fotos, y **nada más**. El checkout no lo llama.

⇒ **el gate de la puerta se construyó en S100 y nunca se cableó.** Es *motor sin puerta* — el patrón que
esta casa tiene nombrado y que ya cobró en S71 (el picker de receta), en S74 (el arco de equipo) y en S95
(el panel del vendedor). **La cabecera de la función es magnífica y describe un comportamiento que no
ocurre**: 30 líneas de porqué medido, cero llamadas.

**Y eso explica el reporte del founder ENTERO sin ninguna hipótesis:** hoy nada revisa stock entre la
vitrina y `crearIntentoPago`. La primera y única vez que la familia se entera es con el dedo sobre
«Pagar», que es literalmente lo que la cabecera de esa función dice que vino a evitar.

## ② 🔴 LA VOZ QUE VIO EL FOUNDER NO DISTINGUE CUÁL DE LAS DOS MITADES FALLÓ

El mensaje es uno solo (`_despensa-comun.ts:184`):

> `sin_stock: 'No hay stock suficiente para uno de los productos.'`

Y el normalizador **colapsa tres códigos distintos del motor en ese único `sin_stock`**
(`_despensa-comun.ts:295-299`): `stock_insuficiente`, `reserva_insuficiente` y cualquier error que
mencione `stock_disponible`.

**Por eso el reporte del founder, por sí solo, no puede decir cuál mitad se disparó** — y son dos, con
curas de costo muy distinto:

| mitad | qué es | ¿la caza un booleano? |
|---|---|---|
| **(a) se agotó** | `hay_stock = false` | ✅ **sí** — y es mía, hoy |
| **(b) no alcanza para N** | `hay_stock = true` con 1 unidad, la familia pide 3 | ❌ **no puede** — ver abajo |

`ofertas.hay_stock` es, literalmente, `stock_disponible > 0` (trigger `_trg_oferta_deriva_hay_stock`).
**Es un booleano que no mira cantidad**, y eso **no es un descuido: es la firma de S99** —*«`hay_stock`
BOOLEANO y jamás un número — la familia necesita "¿puedo comprar esto?", no el inventario ajeno»*.

⇒ **la mitad (b) no se puede cerrar del lado del cliente sin romper una firma.** Necesita que el motor
conteste *«¿alcanza para N?»* con un booleano, sin revelar el número. **Va como pedido con el caso, no
como parche mío.**

⚠️ **Y la palabra «suficiente» en la voz es engañosa hoy:** dice «suficiente» aunque lo que pasó sea «se
agotó». *Una voz que describe de más el caso frecuente hace que el caso raro parezca el frecuente* —
mordió acá mismo: la palabra empuja a leer el reporte como mitad (b) cuando la mitad (a) está abierta de
par en par.

## ③ 🔴 EL GLIFO DE UBICACIÓN **YA EXISTE** — A-06 NO ES UN PEDIDO A B

Mi arranque dice *«El glifo es de B — pedíselo CON EL CASO»*. **Medido: `Icono.tsx:800` define
`ubicacion`**, registrado en la capa `cuidado` (`Icono.tsx:1406`), con su comentario propio: *«El pin del
hero — la huella vive en la gota (deja de ser placeholder)»*.

⇒ **es montaje mío, cero pedido, cero espera.** *Pedir una pieza que ya está construida cuesta una vuelta
de canal y la respuesta es un `grep`.*

---

# §2 · LOS SEIS HALLAZGOS, CON SU MEDICIÓN

## 🔴 A-01 · EL STOCK FALLA EN EL PEOR MOMENTO — **el founder tiene razón, y por más de lo que dijo**

**Las dos puertas de agregado NO se comportan igual, y eso está medido:**

| puerta | ¿mira `hay_stock`? | evidencia |
|---|---|---|
| **vitrina** (`despensa/index.tsx:442`) | ✅ **sí** | `TarjetaProducto` apaga el `+` y baja la opacidad — líneas 389 · 416 · 547 · 616 |
| **ficha del producto** (`producto/[productoId].tsx`) | ❌ **NO** | `faltaParaAgregar` (329-335) tiene **tres causas y ninguna es stock**: sin oferta · sin presentación · sin entendimiento de alergia |

**La ficha DICE que no hay stock y deja agregar igual.** La línea `despensa.fichaSinStock` se pinta en
la sección ⑥ (`:764`) y **el CTA de al lado sigue encendido**. *Una pantalla que informa el impedimento y
no lo aplica es peor que una que calla: enseña que el aviso no significa nada.*

⇒ **la mitad (a) es una cura de una línea en `faltaParaAgregar`**, y es exactamente la firma de la casa:
*el Confirmar apagado DICE QUÉ FALTA* (S73-B), que esa misma función ya cumple para las otras tres causas.

**Lo que la cura NO alcanza, declarado:** la mitad (b) —cantidad— y el carrito que quedó parado mientras
el mundo cambió. Para eso está el lector huérfano de ①: **cablearlo al foco del carrito es la otra mitad**,
y su cabecera ya declara que sirve para eso.

## A-02 · EL RESUMEN NO CONFIRMA CÓMO NI CUÁNDO LLEGA — **medido, y falta un tercero**

**Lo que la carta del resumen dibuja hoy** (`checkout.tsx:840-884`), en orden: quién lo prepara · los
ítems · subtotal · impuesto · **envío como PRECIO** · total · pago simulado.

**Lo que NO dibuja:**

| falta | dónde está hoy | por qué importa |
|---|---|---|
| **la modalidad confirmada** | solo como etiqueta de una fila de plata (`:877`) | «Envío $2.50» dice cuánto cuesta, **no** que va a tu casa |
| **la fecha probable** | `promesaVentana` vive **dentro de `fase === 'armado'`** (`:759`) — la pantalla anterior | se elige y **no se puede releer al pagar** |
| **la dirección** ⚠️ | también solo en `armado` | *no lo pidió el founder y lo declaro igual: es la misma carta y la misma pregunta* |

**Y el dato ya está a mano, medido:** `entregaCompradaRef` (`:500-526`) captura dirección, ciudad,
receptor, teléfono, lat/lon e instrucciones **al entrar a `resumen`**. ⇒ **no hace falta pedir nada al
motor: hace falta pintarlo.**

## 🔴 A-03 · PLACES **ESTÁ CABLEADO** — y por eso el hallazgo cambia de forma

**Medido: `direccion-hogar-form.tsx` monta `BuscadorDeLugar` y llama `buscarLugares` / `resolverLugar`
desde S79-A4.** Autocompletado hay. Lo que el founder no vio tiene una causa candidata **escrita en el
código**, y es de método (`:132-137`):

```
if (!r.ok) {
  if (r.codigo === 'sin_configuracion') placesApagado.current = true;
  // red/google mientras se tipea: silencio — se sigue a mano.
  setPredicciones([]);
  setBusquedaVacia(false);
  return;
}
```

**Places degrada EN SILENCIO y para siempre**: un solo `sin_configuracion` levanta `placesApagado` y
**no se vuelve a intentar en toda la vida del formulario**. Y el wrapper es explícito sobre cuándo pasa
(`lugares.ts:12`): *«Hasta que el founder cree el secret `GOOGLE_PLACES_API_KEY`, TODO devuelve
`sin_configuracion`»*.

⚠️ **NO PUEDO MEDIR CUÁL ES EL CASO SIN EL AMBIENTE.** Las dos ramas se ven idénticas desde la pantalla:
teclear sin que pase nada. **Es la trampa L-218 en su forma exacta** —una misma pinta para «cargando»,
«no hay» y «está roto»— y la degradación silenciosa la vuelve indistinguible **por diseño**.
**Primera pregunta al founder, y decide el resto: ¿existe el secret `GOOGLE_PLACES_API_KEY`?**

**Lo que sí es defecto pase lo que pase:** el silencio. El wrapper **ya tiene la voz escrita** —
`'La búsqueda de direcciones no está disponible por ahora. Podés escribirla a mano.'` (`lugares.ts:81`)
— y **el formulario la tira a la basura**. *Una voz honesta escrita y no consumida es la mitad de una
cura esperando.*

### El ALIAS — **la tabla está lista; la puerta no**

| capa | estado | evidencia |
|---|---|---|
| **tabla** | ✅ **lista, y de sobra** | `direcciones_guardadas` tiene **`alias` NOT NULL**, `es_principal`, `nombre_receptor`, `instrucciones_entrega`, `places_id`, `lat/lon` |
| **RPC** | ❌ **una sola dirección** | `guardar_direccion_hogar` **no toma parámetro de alias** (`direcciones.ts:133-144`) |
| **lector** | ❌ **una sola dirección** | `obtenerDireccionHogar` hace `.eq('es_principal', true).maybeSingle()` (`:79-80`) |

⇒ **es el segundo «motor sin puerta» del día, y al revés que el primero: acá el esquema se adelantó y la
puerta se quedó.** La casa ya decidió que las direcciones son N con nombre — hace tiempo, en el DDL.

⚠️ **La mitad del RPC NO ES MI TERRITORIO** (mi predecesora ya frenó por esto en S100b: la DB no es de A
en esta vuelta). **Va como pedido con el caso**, no como migración mía.

## A-04 · EL CAMPO PEGADO AL TECHO — **causa candidata sobre la fuente, NO medida en aparato**

**Lo medido, que es geometría de la fuente:**
- la Hoja de dirección se monta con **`altura="completa"`** (`checkout.tsx:1006`)
- `completa` = **`altoVentana * 0.9`** (`Hoja.tsx:236`) — 90 % de la pantalla
- su `ScrollView` interno abre con **`paddingTop: spacing[1]`** (`Hoja.tsx:513`)
- la Hoja usa **`padding` en las dos plataformas** para el teclado (`Hoja.tsx:430`)
- las Hojas se montan **FUERA del `EvitaTeclado`** de la pantalla (`checkout.tsx:998`)

**`[HIPÓTESIS, sobre la fuente — no medida]`:** con el pin montado el contenido crece; a 90 % de alto y
con el teclado abierto el área útil se achica por abajo, el scroll sube el campo enfocado y con
`spacing[1]` de aire arriba queda contra el filo.

🔴 **NO LO CURO CON ESTO.** *Una aritmética correcta sobre una premisa no medida da un número exacto y
equivocado* — es la lección que mi predecesora se comió con los 53 dp de la barra de tabs, y que se
salvó **por no curar**. **Pedido a B, que tiene el aparato**, con el caso exacto: *Hoja de dirección,
punto ya puesto, tocar el campo de dirección* — y con qué medir: **el `y` del campo enfocado con el
teclado arriba**.

## A-05 · «CUÁNDO SE ENTREGA» ES VERTICAL — y la casa ya tiene el eje horizontal

**Medido: `SelectorVentana` apila.** Su `radiogroup` es un `View` con `gap: spacing[2]` y **cero
`horizontal`, cero `ScrollView`, cero `wrap`** (`SelectorVentana.tsx:105`). Cada opción es un `Pressable`
con `padding: spacing[3]` y hasta tres líneas (etiqueta · detalle · motivo). **Cuatro opciones —la más
próxima y los tres días siguientes (`checkout.tsx:125`)— apiladas, debajo de dirección, quién recibe e
instrucciones.**

**Y el precedente existe y está medido:** `SelectorOpcion` tiene `disposicion` **fila / tira / grilla**
desde S55-B4, y **nació justo para esto** — la tira de días del CUÁNDO tipo Teams. *No hay que inventar
un eje: hay que usar el que la casa ya firmó.*

⚠️ **Lo que una tira NO puede llevar, y por eso el pedido va con la tensión adentro:** hoy la opción
llena **habla** (`o.motivo`, y entra al `accessibilityLabel` — Ley 23). En una tira angosta ese motivo no
entra. **Pedido a B con la pregunta explícita**, porque la pieza es suya: ¿la tira conserva el motivo, o
el día lleno pasa a decirse de otra forma? *Ganar visibilidad perdiendo el porqué sería cambiar un
defecto por otro.*

## A-06 · EL PIN — montaje mío, hoy

`CeldaNavegacion` acepta `icono` opcional desde S71 y el glifo `ubicacion` existe (§1③). **Se monta y
se acabó.**

---

# §3 · LO QUE APROBÓ EL FOUNDER Y NO SE TOCA

**«Quién recibe» con su teléfono e instrucciones («está perfecto») y la «i».** Quedan **congelados**.
⚠️ Y con una consecuencia que declaro **antes** de que alguien la barra por prolijidad: la «i» de la
donación y la de las condiciones de cobro **son curas de dos mitades** (montaje + callback); quitarles el
callback deja el texto de §6.4 sin salida **en silencio**. Está en el handoff de mi predecesora §3 y lo
repito acá porque **lo que se lee una vez se olvida y lo que se lee dos veces se respeta**.

---

# §4 · EL PLAN, EN ORDEN, CON SU RAZÓN

*El orden no es por tamaño: es por lo que rompe confianza.*

| # | qué | mío | costo |
|---|---|---|---|
| **1** | **A-01(a)** · `hay_stock` entra a `faltaParaAgregar` en la ficha | ✅ | una línea + su voz |
| **2** | **A-01(cont.)** · cablear `revalidarCarritoDespensa` al foco del carrito | ✅ | el lector ya existe |
| **3** | **A-02** · modalidad + fecha + dirección en la carta del resumen | ✅ | el dato ya está en `entregaCompradaRef` |
| **4** | **A-06** · el pin en la línea de dirección | ✅ | montaje |
| **5** | **A-03(voz)** · Places deja de degradar en silencio | ✅ | la voz ya está escrita |
| **6** | **A-04 · A-05 · A-03(alias) · A-01(b)** | ❌ | **pedidos con el caso** — ver §5 |

**Por qué 1 antes que 2:** son mitades distintas de la misma promesa. **1 cierra la puerta** (que es lo
que el founder pidió con todas las letras: *«ya el producto no me lo debería haber dejado entrar»*);
**2 cubre el carrito que estuvo parado**, que el founder mismo declaró legítimo. *Hacer solo 2 dejaría la
puerta abierta y taparía el síntoma un paso más tarde.*

---

# §5 · LOS CUATRO PEDIDOS, CON DUEÑO Y CON EL CASO

| # | a quién | qué | por qué no lo hago yo |
|---|---|---|---|
| **Q1** | **B** | **A-04**: medir en aparato el `y` del campo enfocado con teclado, en la Hoja de dirección con el pin montado | **no tengo aparato**, y la cura sin medición es el error de los 53 dp |
| **Q2** | **B** | **A-05**: `SelectorVentana` al eje horizontal, **con la pregunta del motivo del día lleno adentro** | la pieza es de `packages/ui` |
| **Q3** | **motor / mesa** | **A-03 alias**: `guardar_direccion_hogar` no toma alias y el lector filtra `es_principal` — **la tabla ya lo soporta** | la DB no es mi territorio esta vuelta |
| **Q4** | **motor / mesa** | **A-01(b)**: un «¿alcanza para N?» que conteste **booleano** sin revelar el número | cerrarlo del lado cliente **rompe la firma de S99** |

---

# §5bis · LO CONSTRUIDO — con su commit y su verificación

| # | qué | commit | verificación |
|---|---|---|---|
| **A-01(a)** | la ficha deja de agregar sin stock; `fichaSinStock` **se muda** al pie | `362647a8` | instrumento **18/18** · **rojo producido: 5 caen** con la regla previa |
| **A-01(cont.)** | `revalidarCarritoDespensa` **cableado al foco** del carrito; cada ítem dice qué le pasó; el CTA no avanza | `362647a8` | ídem |
| **A-02** | modalidad + dirección + fecha en el resumen | `22c4112f` | typecheck · **no verificado en pantalla** |
| **A-06** | el pin en la línea de dirección | `22c4112f` | ídem |
| **A-03(voz)** | Places deja de apagarse en silencio | `22c4112f` | ídem |
| **H-115** | el marco lila: `LienzoProducto` pintaba `bg.overlay` detrás de la foto | `22c4112f` | ídem |

**Instrumento nuevo:** `scripts/verify-s100c-disponibilidad.ts` — importa **la función que la pantalla
usa** (`lib/despensa/disponibilidad.ts`), jamás una copia. **18/18**, y su rojo se produjo devolviendo la
regla a su comportamiento previo: **13/18**.

**Al cierre:** `verify:diseno` **VERDE · 46 reglas** · `tsc --noEmit` (cliente) **exit 0**.

---

# §5ter · LO INTERCAMBIADO CON B — medido por B, no por mí

**B es `e-petplace-4b`, confirmado por él.** *La identificación se hizo ANTES de mandar nada, que es la
cura que mi predecesora dejó escrita después de mandarle dos pedidos a la sesión equivocada.*

## 🔴 A-04 · B MIDIÓ, Y EL REPORTE DEL FOUNDER SE QUEDABA CORTO

| qué | medido por B |
|---|---|
| inset de la barra de estado | **25,6 dp** |
| `Sector o barrio` con el teclado arriba | **y = [8,5 · 30,6] dp** ⇒ **17,1 dp por encima del área segura** |
| **el campo ENFOCADO** | **NO ESTÁ EN EL ÁRBOL — salió de pantalla por arriba** |
| el título de la Hoja y su asa | **también salieron** |

⇒ **no es que el texto quede «casi pegado al techo»: el campo en el que estás escribiendo SALE DE LA
PANTALLA**, y lo que queda arriba se dibuja debajo del reloj del sistema.

### El mecanismo, leído de la fuente — y **ninguna de mis cuatro candidatas era la causa sola**

- `Hoja.tsx:429-441` — el `KeyboardAvoidingView` es de pantalla completa con `justifyContent: 'flex-end'`
  y **`behavior="padding"`** ⇒ con teclado, su caja útil pasa a ser `H − K`.
- `Hoja.tsx:236` — la Hoja hija tiene **`height: altoVentana * 0.9` FIJO**, y `completa` deja
  **`altoMax` en `undefined`** (:237): no hay ni siquiera un tope que la limite.

**Un hijo de alto fijo 0,9·H alineado al fondo de una caja de `H − K` desborda hacia ARRIBA. No se
encoge: no tiene con qué.**

🔴 **Las dos piezas están bien por separado y son incompatibles juntas.** `behavior="padding"` se eligió
**con medición** (S83-B36: `height` encoge contra una ventana que bajo edge-to-edge ya no se achica) y
sigue siendo correcto. Un alto fijo también es correcto sin teclado. *Lo que falla es que un alto fijo no
puede participar del encogimiento que `padding` provoca.*

**Entregado a B con una predicción falsable**, para que no me crea por elegante: el desborde debería ser
≈ `K − 0,1·H` ⇒ **`altura="media"` (0,5·H) y `altura="contenido"` (sin alto fijo) NO deberían mostrar el
defecto.** *Si alguna lo muestra, mi mecanismo está mal y hay que volver a empezar.*
**No lo curé: `Hoja` es de B y la montan las dos apps enteras.**

### ✅ B CORRIÓ LA PREDICCIÓN **ANTES** DE CURAR, Y SE SOSTUVO

Misma Hoja (`Quién recibe`), mismo teclado, cambiando solo la altura:

| | `completa` | `contenido` |
|---|---|---|
| campo enfocado | **fuera de pantalla, ausente del árbol** | visible |
| título + asa | **fuera** | título en **74,7 dp** |
| lo más alto que queda | **8,5 dp** (bajo la barra de estado) | **74,7 dp** |
| última fila | — | `Listo` en **409,6 dp**, visible |

**`contenido` no desbordó nada.** Era la corrida que me dejaba equivocado si fallaba.
**B curó con `flexShrink: 1`** —la candidata que le pasé— **sin tocar `behavior="padding"`**, cuya
decisión está medida (S83-B36) y no se movió.

🔴 **LA NOTA DE MÉTODO, y no es sobre Hojas:** mis cuatro observaciones eran **correctas como piezas** y
**ninguna era la causa sola**. *Un defecto puede no tener un culpable: puede tener dos decisiones buenas
que no pueden convivir.* Leer más fuerte cualquiera de las dos por separado no lo encuentra nunca —
las dos se ven impecables, y cada una tiene su medición defendiéndola.

⚠️ **Sin ojo todavía**, y el borde queda escrito en la pieza para el gate: al encoger, el `paddingBottom`
de la Hoja **no debe comerse la última fila**, que en la de dirección es el **`Guardar`**.

## A-05 · B DECIDIÓ LA ANATOMÍA, Y TRAJO EL NÚMERO QUE LO EMPEORA

**`Cuándo te llega` arranca en y = 595,9 dp y el pie fijo empieza en ~593** ⇒ **no es «invisible sin
deslizar»: nace debajo del pie fijo.**

Su resolución de la tensión que le pasé (visibilidad vs. el porqué del día lleno): **la tira lleva el
día · el lleno se dibuja apagado (Ley 23 intacta) · el motivo vive en UNA LÍNEA DEBAJO de la tira · la
ventana horaria queda como tarjeta aparte.** *Ley 23 pide que diga por qué, no que quepa en el chip* — y
un renglón entero dice más que tres líneas truncadas. **B construye la pieza; yo monto.**

⚠️ **Freno heredado que B declaró: H-204 sigue ABIERTO** — una medición dice que un toque sobre un chip
en `disposicion="tira"` no lo selecciona, y el censo de C dice que sus diez consumidores funcionan. **Dos
mediciones honestas que discrepan.** Si al montar la tira un día no selecciona, **es H-204 y vuelve a B
sin diagnosticar.**

## N24 · N25 · `PantallaConPie` — lo que me toca de su push

- **N25 NO toca mi carrito** (B lo declaró explícito a pedido mío): la puerta que muere es la del
  encabezado de la **vitrina**, territorio de C. **Cero montaje mío.** *Se pidió explícito porque un
  cambio de puerta es invisible desde afuera hasta que alguien lo declara.*
- **N24** (`TarjetaProducto` deja de crecer 44,1 dp) y **`PantallaConPie`** (el `insets.bottom` se
  contaba **dos veces** dentro de `(tabs)` — **61 dp de hueco muerto**, el «~1 cm» del founder):
  **ninguna cambia su API**.
- **Censo pedido por B, corrido: cero compensaciones vivas a mano en mis pantallas.** El único
  `insets.bottom` de mi territorio es la **lápida** del `+ 120` de S100b. ⇒ su cambio no me cuesta nada.

---

# §6 · LO QUE UN INSTRUMENTO DE ACÁ **NO** PUEDE DECIR

*Se escribe para que nadie lea mis verdes de más — y porque el founder ya se comió una vez cuatro números
verdes sobre una ficha sin precio.*

- **No tengo aparato. Nada de lo que haga hoy se va a haber visto.** `verify:diseno` mide **forma**, el
  typecheck mide **tipos**. **Ninguno mide jerarquía visual, y ninguno mide si algo se entiende.**
- **Oscuro y memorial: no medidos**, ni por instrumento ni por ojo.
- **No puedo distinguir «Places apagado» de «Places sin resultados»** sin el ambiente — por eso es
  pregunta al founder y no un diagnóstico mío.
- **No sé cuál de las dos mitades de stock vio el founder.** Cierro la que puedo cerrar y declaro la otra.
- **Un `verify:diseno` verde después de mis curas no dice que el checkout esté bien**: dice que no
  reintroduje un defecto que ya tiene diente.
- **Mi instrumento mide una función pura.** Dice que la regla decide bien; **no dice que la pantalla la
  llame** (eso lo dice leer el montaje) ni que el CTA se apague en un teléfono.
- 🔴 **MIS VERDES CORRIERON CONTRA EL `packages/ui` DE *MI* WORKTREE, o sea el de ANTES de B.** Las curas
  de B a `PantallaConPie` (el inset derivado, **61 dp** de hueco muerto) y a `Hoja` (`flexShrink`) viven
  en `pista-b` y **mi typecheck no las vio**. *No es un descuido: cada pista compila su árbol.* ⇒ **el
  typecheck que vale es el del ENSAMBLADO**, y lo corre quien mergee. **Mis verdes son ciertos sobre mi
  árbol y pueden quedar vencidos con el merge** — la misma clase de dato que mordió cinco veces en S100b:
  cierto al escribirse, falso al usarse.

---

# §7 · 🔴 LO QUE QUEDA ABIERTO, SIN MAQUILLAR

## ① EL ALIAS DE DIRECCIÓN ESTÁ FIRMADO PARA ESTA VUELTA Y **NO ENTRÓ** — con su razón exacta

El founder lo firmó *«entra en esta vuelta»*. **No entró, y no es por tiempo: la puerta no existe.**

| capa | estado |
|---|---|
| **tabla** | ✅ `direcciones_guardadas` ya tiene **`alias` NOT NULL**, `es_principal`, `nombre_receptor`, `instrucciones_entrega` |
| **RPC** | ❌ `guardar_direccion_hogar` **no toma alias** y actualiza LA principal |
| **lector** | ❌ `obtenerDireccionHogar` filtra `.eq('es_principal', true).maybeSingle()` |

**El esquema se adelantó y la puerta se quedó.** Construir la pantalla de «oficina / suegra» sin el RPC
exigiría escribir la tabla a mano desde el cliente, y eso **rompe la puerta única a la DB** — la regla
que esta casa tiene en piedra. *No la rompo por una vuelta.*
**⇒ es lo primero que hay que destrabar, y es de una sola pieza: un parámetro de alias y un lector de
lista.** Escalado al founder en el reporte de esta sesión.

## ② LA MITAD (b) DEL STOCK SIGUE VIVA, Y PUEDE SER LA QUE VIO EL FOUNDER

**Cerré la puerta que estaba abierta** (agregar algo agotado). **No pude cerrar «pedí 3 y hay 1»**: no es
expresable con un booleano, y el booleano es **firma de S99**, no un descuido.

⚠️ **Y lo digo sin adornar: si lo que el founder golpeó fue la mitad (b) —y la palabra «suficiente» de la
voz empuja a leerlo así—, entonces con lo de hoy el síntoma le vuelve a aparecer en la caja.** Lo que
cambió es que ya no puede entrar un producto **agotado**, y que un carrito parado avisa al retomarlo.
**La otra mitad necesita el motor (Q4).**

## ③ LO NO MEDIDO, QUE ES LA MITAD MÁS IMPORTANTE

**Nada de esto se vio en un teléfono.** Oscuro y memorial, sin medir. Y **no sé si el secret
`GOOGLE_PLACES_API_KEY` existe** — de eso depende si el autocompletado del founder aparece o si lo único
que va a ver de A-03 es la voz nueva diciéndole que escriba a mano.

## ④ DEUDA CON DUEÑO Y DISPARO

| deuda | dueño | disparo |
|---|---|---|
| **`LienzoProducto` sube a `packages/ui`** — mientras viva en la app, cada superficie nueva **vuelve a heredar** H-115 (por eso la cura de la vitrina en S100b no viajó al carrito) | **B** (lo propuso; le cedí la decisión) | la próxima superficie que muestre producto |
| **el alias de dirección** (①) | **motor / mesa** | inmediato — está firmado para esta vuelta |
| **«¿alcanza para N?» booleano** (②) | **motor / mesa** | antes de la primera venta real |
| **H-204** · el chip en `tira` que no selecciona: dos mediciones honestas que discrepan | **B** | si al montar A-05 un día no selecciona |
