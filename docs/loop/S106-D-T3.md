# S106-D · TANDA 3 — PARTE DEL LOOP

> **Pista D — EN RESERVA hasta el gate del founder.** Territorio:
> `supabase/functions`. Esta tanda es de motor y superficie, no de transporte.

## Paso ⓪ — y un rescate

- **worktree NUEVO:** `e-petplace-s106-d-t3` · rama `pista/s106-d-t3` desde
  `origin/main = dc0e21b0` · árbol limpio.
- 🔴 **`pista/s106-d-t2` NO estaba mergeada a main**, y con ella **4 archivos
  y 7 commits fuera del canon** — entre ellos **la spec de la vía nativa que
  C necesita para construir el cuadro** y **mi propio instrumento de
  auditoría**. *(Las dos edges sí habían llegado.)*
  ⇒ **Verificado ANTES de crear el worktree y traído por merge.** *Es `L-217`
  en carne: estar en origin no es estar en main — y si el worktree nace de
  main sin mirar, arranca sin su propia herramienta.*
- `supabase/.temp` copiado del worktree anterior (gitignored) — cero cambio
  remoto.

## Línea base ACTUALIZADA — la anterior quedó vieja

| | antes (26-ago) | **ahora** |
|---|---|---|
| hechos de sala | 54 | **232** |
| citas de telemedicina | 8 | **17** |
| adjuntos con `origen_captura` | — | **0** *(el cuadro aún no se construyó)* |

## El instrumento, extendido con los dos chequeos nuevos
`scripts/auditar-hechos-videollamada.sql` — **un resultset, una fila por
chequeo, columna `ok`**.

### ③ EL CUADRO CONGELADO *(listo, sin datos todavía)*
No basta que la marca exista: **`ok` exige `origen_captura='videoconsulta'`
Y que cuelgue de una teleconsulta Y que la mascota sea la de ESA cita.**
> *Una marca que apunta a la cita equivocada es peor que ninguna: es una foto
> en el expediente del animal que no es.*
*(Medido: `origen_captura ∈ {videoconsulta, carga_dueno, carga_profesional}`;
hoy **0 filas** — el chequeo está armado y espera al primero.)*

### ④ LA ASIGNACIÓN AL TITULAR
Mismo criterio que a los hechos de sala: **que haya un asignado no prueba
nada; que sea el titular, sí.**
**Medido para no suponerlo:** `rol='dueño'` son **10 filas y las 10**
coinciden con `prestadores.user_id`; los **21 `empleado`, ninguna**.
⇒ **se exigen LOS DOS criterios**, para que el día que divergan el chequeo lo
cace en vez de elegir uno y no enterarse.

## 🔴 EL DEFECTO QUE ENCONTRÉ EN MI PROPIO CHEQUEO — antes de usarlo
Mi criterio de sala exigía **`personas = 2`**. **Está mal, y tiene una firma
en contra:**

> `LETRA_TELEMEDICINA` **§4**: la consulta se cobra **aunque el dueño no
> asista** — *«si el veterinario entra y determina que el caso necesita
> atención presencial, eso ES el servicio prestado»*.

⇒ **Una sala con UNA sola persona es un caso LEGÍTIMO y firmado.**
🔴 *Mi chequeo marcaba en rojo justo el caso que la letra protege*, y lo
habría reportado como descuadre del registro. **Corregido a `between 1 and 2`:
rojo es 0 (hubo sala y no entró nadie) o 3+ (alguien que no es ninguno de los
dos).**
**Con el criterio corregido: las 9 salas en verde.**

## Estado del pase (corrido hoy, sin el gate)
```
salas      : 9 · todas ok   (joins==lefts, start+finish, sin intrusos)
consumo_sql: 57,4 part-min · 0,732 GB   ← comparar con video-consumo
cuadro     : 0 filas (aún no construido)
asignacion : ver abajo 🔴
```

## 🔴 LO ÚNICO QUE NO CUADRA — y lo reporto sin veredicto
**De las 17 citas de telemedicina de los últimos 2 días, SEIS tienen
`empleado_id` con `rol='empleado'`, no `dueño`.** Empleados
`afdc7fb9…` y `ebd956db…`.

**Y la distribución temporal NO permite concluir:**
```
27-ago 16:31 → dueño      27-ago 16:08 → EMPLEADO
27-ago 15:29 → dueño      27-ago 03:53 → EMPLEADO
27-ago 14:39 → dueño      27-ago 03:12 → EMPLEADO
26-ago 16:23 → dueño ×3   26-ago 16:28 → EMPLEADO ×3
```
⚠️ **No es «antes empleado, ahora dueño»: están intercaladas.**
🔴 **Y no sé cuándo entró el cambio de A**, así que **no puedo decir si son
anteriores a la firma o un defecto vivo.** *Doy el dato y la pregunta, no un
veredicto sin base.*
⇒ **Pregunta a A: ¿desde qué momento las citas nuevas nacen del titular?**
Con esa hora, el chequeo separa solo las de antes de las de después.

## Qué queda para el gate
Correr el script y comparar contra **232 hechos**. Y con las condiciones ya
escritas:
- ⚠️ **el patrón de reconexión**: varias entradas **con UNA sola sesión** es
  señal; **con varias entradas a propósito, no.** *El mismo número significa
  cosas opuestas según lo que hizo la persona.*
- ③ el cuadro, si C llega a construirlo;
- ④ la asignación, con la respuesta de A sobre la hora del cambio.

## Abierto y NO mío
`minSdk` / target / peso del APK — llega de **A** con la build del cierre.

---

# 🏁 CIERRE DE LA PISTA D · S106 — HANDOFF PARA EL GATE

> **El gate final se difiere a la próxima sesión.** Esto es todo lo que hace
> falta para que la auditoría sea **un comando y no una reconstrucción**.
> **Quien lo corra no necesita releer la sesión.**

## 1 · LA LÍNEA BASE — con su fecha
> ### **232 hechos de sala · 17 citas de telemedicina · 0 adjuntos con `origen_captura`**
> **Medido el 27-ago-2026.** Los hechos del gate serán **los que estén por
> encima de 232**, en la sala de esa cita.
⚠️ *Esta línea base **envejece**: si pasan días con llamadas de prueba, hay
que re-tomarla antes del gate. La anterior (54, del 26-ago) quedó vieja en un
día.*

## 2 · EL COMANDO — uno solo
```bash
cd /Users/guillo381gmail.com/proyectos/ePetPlace/e-petplace-s106-d-t3
npx supabase --experimental db query --linked --file scripts/auditar-hechos-videollamada.sql
```
**Un resultset, una fila por chequeo, columna `ok`. Se mira `ok`: si alguna
dice `false`, ahí está el descuadre.**
⚠️ **Va en UN solo `SELECT` a propósito:** *el CLI devuelve sólo el último
resultset — la primera versión tenía tres consultas y descartaba dos en
silencio* (`L-427`).

## 3 · LOS CUATRO CHEQUEOS, con sus criterios ya corregidos

| # | chequeo | `ok` exige |
|---|---|---|
| ① | **sala** | `joins == lefts` · `room_started` **y** `room_finished` · **sin intrusos** · `modalidad='telemedicina'` · **`personas` entre 1 y 2** |
| ② | **consumo_sql** | contra-cuenta en SQL — **se compara A MANO con lo que devuelve `video-consumo`** |
| ③ | **cuadro** | `origen_captura='videoconsulta'` **Y** cuelga de una teleconsulta **Y** `mascota_id` es la de **ESA** cita |
| ④ | **asignacion** | `rol='dueño'` **Y** `pe.user_id = prestadores.user_id` *(los dos)* |

### 🔴 El criterio de ① que estaba MAL y ya está corregido
Exigía **`personas = 2`**, contra una firma:
> `LETRA_TELEMEDICINA` §4 — la consulta se cobra **aunque el dueño no asista**.

⇒ **una sala con UNA persona es LEGÍTIMA.** *Mi chequeo marcaba en rojo justo
el caso que la letra protege.* **Rojo es 0** (hubo sala y no entró nadie)
**o 3+** (alguien que no es ninguno de los dos).

### Por qué ③ pide la mascota y no sólo la marca
> *Una marca que apunta a la cita equivocada es peor que ninguna: es una foto
> en el expediente del animal que no es.*

### Por qué ④ pide los DOS criterios
Medido: `rol='dueño'` **10 de 10** coinciden con `prestadores.user_id`; los
21 `empleado`, **0**. **Son consistentes hoy** ⇒ se exigen ambos **para que el
día que divergan el chequeo lo cace**, en vez de elegir uno y no enterarse.

## 4 · 🔴 LAS 6 CITAS — el discriminador YA CORRIDO, y es concluyente
**Seis citas de telemedicina tienen `rol='empleado'`, no `dueño`.**
La pregunta era si eso es **continuidad clínica** (firma de S78: *el vet que
ya trata a la mascota gana sobre el balanceo*) **o balanceo puro**.

**Corrido contra `caso_clinico`, las seis:**
```
tiene_caso: false   ·   ya_trataba_esa_mascota: false
33800148 · 55e035d8 · 68cb15a2 · 203697fe · c6cdb345 · de1ff727
```
> ### ⇒ **NO es continuidad. Ninguna tiene caso abierto y ninguno de esos empleados trataba antes a esa mascota. Es BALANCEO PURO.**
> *La explicación benigna queda descartada por medición, no por opinión.*

**⚠️ Lo que sigue abierto, y es UNA pregunta a A:**
> **¿desde qué momento rige que las citas nuevas nacen del titular?**

Con esa hora el chequeo separa solo: **anteriores = herencia** ·
**posteriores = defecto vivo**. **Sin ella no se puede concluir**, porque las
horas están **intercaladas**:
```
27-ago 16:31 dueño │ 16:08 EMPLEADO │ 15:29 dueño │ 03:53 EMPLEADO
26-ago 16:23 dueño ×3 │ 16:28 EMPLEADO ×3
```

## 5 · QUÉ MIRAR EL DÍA DEL GATE
1. **Re-tomar la línea base** *(envejece)* y correr el comando de §2.
2. **Mirar la columna `ok`.** Cualquier `false` es el descuadre.
3. **Comparar `consumo_sql` con `video-consumo`** — *dos cálculos
   independientes es la única forma de creerle a un instrumento propio.*
   El endpoint pide `x-despacho-secret` (keychain `epetplace-despacho-secret`).
4. ⚠️ **El patrón de reconexión, con su condición:** varias entradas **con UNA
   sola sesión** es señal y hay que mirarla; **con varias entradas a
   propósito, no.** *El mismo número significa cosas opuestas según lo que
   hizo la persona.*
5. **③ sólo tendrá filas si C construyó el cuadro.** Si da 0 filas, no es
   descuadre: es que no existe todavía.

## 6 · EL ESTADO DE LA PISTA
| pieza | |
|---|---|
| `video-token` | ✅ ejercido entero (6/6, borde de §4 verificado por mí) |
| `video-webhook` | ✅ ejercido · header **`Authorization`** cerrado contra el emisor |
| `video-consumo` | ✅ desplegado · guard con discriminador de 4 casos |
| transporte | ✅ **LiveKit sin condición** |
| el cuadro | 🟡 **candidata sin probar** — vía nativa, con su disparo sonado y su spec en `2026-08-27-s106-d-CUADRO-SPEC-NATIVA-C.md` |

**Abierto y NO mío:** `minSdk`/target/peso del APK (de **A**, con la build del
cierre) · el cron de `video-consumo` (de **A**) · el módulo nativo del cuadro
(de **C**).
