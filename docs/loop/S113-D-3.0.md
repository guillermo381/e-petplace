# S113-D · fase 3 «Encontrar, traer, llevar»

**Rama:** `pista/s113-d-3.0` · **último SHA:** `ca41e878`
**Reparto:** intención de búsqueda (D1) · la respuesta vacía que no es vacía (D2)
· `extract-papel` v2 (D3) · la placa: no participo (D4)

---

## 🔴 TRASPASO · si esta sesión se corta, leer esto primero

**Soy D.** Territorio: `supabase/functions/{coach,coach-parte,buscar-intencion,
extract-papel,_shared/{ia,voz}}` · `packages/api/src/wrappers/{nexo,busqueda-intencion}.ts`
· `packages/domain/src/busquedaPoda.ts` · `scripts/ia/*` · `scripts/verify-{busqueda-poda,arneses-declaran,papel,raza,coach*,carnet-v2,ia-puerta}` · `docs/loop/S113-D-*`.

**Nada bloqueado.** 🔴 **Firma del founder (7-sep): los 38 papeles son
SINTÉTICOS y NO HAY MÁS — deja de pedirse un papel real, hoy no existe.** El
conjunto mide LA LEY y es tablero de regresión; **ningún porcentaje suyo se cita
como exactitud sobre papeles de verdad: es un PISO**, y se dice cada vez.
Ficha: **`D-1047`**, disparo *las primeras familias*.
Mi lote: `scratchpad/gen-d/salida`, semilla `1130744`, con su
`docs/loop/S113-D-CONJUNTO-PAPELES.md`.

**Lo que espero de A:** que despliegue `buscar-intencion` (hasta entonces la
puerta degrada sola, verificado) y que `buscar_en_mi_familia` acepte
`p_tipo`/`p_desde`/`p_hasta` — contrato exacto en `S113-D-PARA-A.md`.

**Claves:** llavero, al momento, nunca impresas.
`security find-generic-password -a medicion -s anthropic-medicion -w` ·
`-s epetplace-service-role` · `-a siembra -s epetplace-siembra-s97` (cuenta de prueba).
🔴 `supabase projects api-keys` **no se corre** (`D-1013`).

**Cómo se corre lo mío:**
```
pnpm verify:busqueda-poda            # 13 verdes · su rojo: agregar `paseo` a la lista
pnpm verify:arneses-declaran         # 42 verdes
node scripts/verify-papel.mjs        # 51 · y raza/coach/coach-parte/carnet-v2/ia-puerta
npx tsx scripts/ia/prueba-busqueda.mts                        # E2E, sesión real, sólo lee
deno run --allow-read --allow-net --allow-env --allow-run \
  scripts/ia/prueba-intencion.ts     # 🔴 MODELO REAL, gasta (~$0,015 la corrida)
```
⚠️ `deno` **corre sobre copia fuera del repo** (escribe `workspaces` en
`package.json`); los `verify-*.mjs` ya lo hacen solos.

---

## Lo hecho, por lote

### ⓪ Lo que estaba roto en `main` antes de empezar
* **`coach-parte` no pasaba por el cinturón del tuteo** y su `system` está en
  voseo. Es la misma clase que ya se cobró con `dejá`. La implementación se mudó
  a `_shared/voz/tuteo.ts`; las dos edges comparten UNA. Control: 132 formas →
  132 corregidas, contra-caso intacto.
* **El wrapper había perdido `general`**: la edge lo emite en sus tres caminos y
  el tipo no lo declaraba. *Un campo que el servidor manda y el contrato no
  declara es indistinguible de uno que no existe.*
* **Mis instrumentos del lote 3 nunca llegaron a `main`.** A mergeó las edges y
  no los scripts. El gate pasó de 6 rojos a 33 verdes al traerlos.

### ① D1 · la intención de búsqueda
Medí antes de construir: con FTS solo, **18/23** de mis búsquedas y **0/20** de
mis preguntas. Con un segundo pase que poda palabras estructurales, **23/23** y
**0/20**, con cero llamadas al modelo.
**E lo volvió a medir con SUS frases: 5/30.** *Mi conjunto no tenía poder de
resolución.* La lista pasó de 15 a **46** palabras — y el brazo derivado del
gate rechazó dos que E proponía (`control` es un producto, `paquete` un
servicio). Con las 46, sus 30 pasan a 9/30.

La edge `buscar-intencion` (Haiku, salida cerrada, razonamiento apagado) extrae
tipo, ventana y término. **$0,000603 por consulta.** Exactitud sobre 24 frases
mías, una corrida: tipo 18/24 · tiempo 23/24 · término 18/24.
🔴 **La ventana la calcula el código**, no el modelo: él elige una palabra de
lista cerrada y las fechas salen del reloj del servidor. 14/14.

### ② D2 · la respuesta vacía no es un vacío
`prefacio_nexo` trae la mitad determinista —«No encontré "X" en lo tuyo, pero»—
en un solo lugar, para que no la invente cada pantalla.

### ③ D3 · `extract-papel`, medido contra 190 analitos
**190/190** filas emparejadas · valor **190/190** (con coma decimal) ·
referencia **190/190** · unidad 176/190 · marca ASCII **6/6** ·
🔴 **marcas inventadas 0 sobre 178 filas sin marca** —incluidas las 13 fuera de
rango SIN marca, que son las que delatarían interpretación— · 🔴 **adjetivos de
juicio 0**. $0,0233 por papel.

🔴 **Y LA VARA TENÍA UN DEFECTO QUE EL MODELO NO.** Mi mano no fue re-teclear el
JSON del generador —esa verdad es del código que hizo los papeles— sino
contrastarla contra la imagen. Albúmina, declarada con `↓`, **en el papel es un
cuadro vacío**. Censo de las 46 fuentes: `⁶` en **0/46**, `⁹` en 0/46, `↓` en
12/46, y **`³` en 46/46** — son subconjuntos latinos de `@fontsource`, llevan
Latin-1 y no el bloque Superíndices. **11 de mis 190 filas** usan una unidad que
ninguna fuente dibuja. *Medir contra una verdad que el artefacto no lleva cuenta
como error del modelo lo que el modelo lee bien.* E encontró la misma clase por
su lado: dos mediciones independientes, misma causa.

### ①bis · la intención contra el conjunto de E, 3 vueltas, vara nueva
**40 frases × 3 vueltas · `temperature: 0` · $0,00079 por consulta:**

| | |
|---|---|
| 🔴 **casos que varían** | **0/40** — el determinismo es una PROPIEDAD, no una coincidencia |
| preguntas siempre bien | 8/10 |
| búsquedas siempre bien | 29/30 |
| `tipo` == etiqueta de E | 19/30 |

Los **3 fallos son los que E ya concedió**: «la del mes pasado» y «lo de ayer»
—*son fragmentos, no preguntas; corrijo yo, no vos*, dicho por E— y «dónde está
la libreta», el caso del borde, que **dejó de ser una moneda al aire**: ahora
sale siempre como pregunta.
⚠️ De las 11 diferencias de `tipo`, **varias me parecen etiqueta y no modelo**
(«veterinario» → `prestador` y no `cita`; «todo lo de Thor» → `cualquiera`).
**No las adjudico yo**: se listan una por una en la salida del arnés para que
las resuelva quien no las escribió. *Adjudicar el propio examen es justo lo que
el protocolo de dos manos existe para evitar.*

### ③bis · el rojo de E contra mi prompt, curado
6 de 10 preguntas de cuidado no salían `cualquiera`, con **0 variación en 3
corridas**. El patrón: falla en las que traen un sustantivo agarrable y acierta
en las que no tienen nada que agarrar. Curado sin insistirle al prompt —eso es
el juego del topo, ya lo pagamos con el voseo—: **el modelo contesta
`es_pregunta` y el código fuerza el resto**. **10/10**, contra-caso **4/4**.
⚠️ Y con eso **acepto una corrección de E a mi propia ley**: «una corrida es una
tendencia» NO aplica a este sujeto — razonamiento apagado + salida cerrada lo
vuelven determinista, y separar por frecuencia escondería el defecto detrás de
un ruido que no existe. La ley sigue valiendo para salidas en prosa.

### ③ter · lo demás de `extract-papel`
El tipo del archivo se lee de los **bytes**. Antes, un `mediaType` fuera de la
lista caía a `image/jpeg` y un PDF viajaba en un bloque `image`. 5/5 formatos.
Al curarlo **se destapó un verde falso** que llevaba una sesión: el caso «el
cliente no puede mentir el modo» mandaba bytes PNG diciendo PDF y **pasaba
porque el código le creía al cliente**. Curado el fixture, con su espejo.
Y **los seis runners no copiaban `declarar-objeto.ts`**: la ley se cumplía en el
archivo y se rompía al correrlo.

---

## 🔴 El hallazgo que vale más que todo lo anterior

**De 520 eventos del expediente, la búsqueda puede ver OCHO.** El índice lee
`datos->>'texto'|'nota'|'mensaje'` y casi ningún tipo guarda ahí su contenido:
`cita_servicio` 364 → 0 · `vacuna_aplicada` 23 → 0 · `alergia_diagnosticada` 7 →
0 (su `datos` es NULL) · `historia_clinica_registrada` 7 → 0 (vive en
`diagnostico_principal`).
Y no falla: aporta filas, la búsqueda se ve completa.

Con los dos rojos que midió E —Despensa invisible por `estado`, techo que corta
antes de priorizar— las tres dicen lo mismo: **el cuello de botella no es la
intención ni la poda, es la COBERTURA de las fuentes.** Todo con sus consultas
en `S113-D-PARA-A.md`.

## Lo que NO cubre nada de esto
* **El tipeo**: «thorr · zeuz · muneca» → 0 en los tres pases. Pide coincidencia
  difusa. Sale honesto por el escalón ③, pero no es encontrar.
* **`tipo`/`ventana` sin término**: «las citas de marzo» no tiene puerta hoy.
  Declarado y no construido; se completa cuando A acepte los tres parámetros.
* **Las apps** salen NO CONCLUYENTE por `router.d.ts` (generado, ausente): con
  uno prestado, de sus 67 errores **0 tocan lo mío**.
* **Una corrida no es un conteo**: con el modelo, entre dos vueltas `tipo` dio
  18 y 17. Los números de modelo son tendencias.


---

## 🔴 EL ENVOLTORIO JSON DE `coach` — lo establecido y lo que sigue abierto

Salió de una rotura que E midió, que yo no reproducía, y que **resultó ser mía**.
Se cierra acá lo que quedó medido, porque el mecanismo sigue sin explicación y
*una investigación que no deja escrito dónde quedó se vuelve a empezar entera*.

### Lo que está establecido, cada uno con su medición

1. **La rotura la introdujo la regla de urgencia y la curó la de formato.**
   A/B/C de E, 3 vueltas por celda, juzgado con el `parsearJson` de la edge:
   `main` 3/3 · **cura de urgencia sola 0 de 6** · con la de formato 3/3.
   ⇒ **Las dos van juntas o ninguna.** `681bf7b4` NO se despliega solo.
2. **El largo, por sí solo, NO rompe.** System MÍNIMO (dos líneas, sólo el
   contrato de salida), techo 4000: **8/8 parsean hasta 2213 tokens** — más del
   triple de donde E vio roturas. *Descarta la explicación que los dos
   creíamos.*
3. **Tampoco son las comillas del ejemplo.** La MISMA regla con el ejemplo entre
   comillas dobles y entre angulares, un solo cambio por vez: **9/9 contra 8/9**
   — si algo, al revés de la hipótesis.
4. **Cuando rompe, el modelo devuelve PROSA desde el primer carácter.** No es
   JSON truncado ni malformado: nunca lo empieza (`Unexpected token 'L', "La
   pregunt"…`, 608 tokens, `stop_reason: end_turn`). *No hay nada que reparar
   en el texto: hay un envoltorio que no salió.*
5. **Con la regla de formato nada llega a 650 tokens** (E: 16 respuestas,
   máximo 450; yo: 12, máximo 400). La regla **acota además de instruir**, y por
   eso ni su experimento ni el mío pueden producir el caso a distinguir.

### Lo que sigue abierto, dicho como hipótesis y no como hallazgo

La única candidata que sobrevive: **un `system` que es él mismo prosa larga con
ejemplos redactados hace más probable que el modelo conteste en prosa** — y la
regla de urgencia agregó exactamente eso. *No está medida.* El experimento que
la probaría es inyectar prosa de relleno equivalente SIN reglas nuevas y ver si
rompe igual; cuesta una tanda y hoy no cambia ninguna decisión.

### Por qué se puede parar acá

La pregunta **operativa** está cerrada: la punta es segura (0 roturas medidas
por E) y el commit del medio no. Lo que queda es **por qué**, y sólo importa el
día que una respuesta legítima necesite pasar de ~650 tokens. Si ese día llega,
la cura no es un prompt: es **reintentar pidiendo sólo el JSON**, porque el
punto 4 dice que el texto no se puede reparar.


---

## ✅ EL CIERRE, con los números que valen

Los tres que cierran el arco clínico, **todos contra la edge desplegada** y
ninguno contra un fixture:

| qué | número |
|---|---|
| **el muro, CON sujeto** (9 ataques + 7 contra-casos × 2) | **0 cruces / 32** · 0 sin derivar · 0 contra-casos cortados |
| **el reintento de contrato** (4 frases × 4, antes y después) | **8/16 → 16/16** |
| **el cableado del reintento** (proveedor falso, determinista) | 8 verdes / 0 rojos |

🔴 **El primero es el único del intercambio que dice «el muro aguanta»**, porque
es el primero que tuvo algo que interpretar: hasta que la bóveda llegó al
contexto, todas las mediciones de la costura —las mías incluidas— corrían sobre
un expediente sin exámenes. *Su cero no decía «aguanta»: decía «no había nada».*
Y el arnés de E lo comprueba antes de medir (`sujeto: ✅ el examen LLEGA a
Nexo`, corta en 2 si no llega), que es esa misma lección hecha guard.

🔴 **El segundo sólo es afirmable por su LÍNEA DE BASE**, tomada antes del
deploy: desde 50 %, sacar 16 de 16 por azar es 1 en 65.536. Y la base pagó dos
veces — al tomarla apareció que **una de las cuatro frases no estaba rota**.
*Sin ella habríamos curado algo sano y contado su acierto como mérito del
reintento: un número inflado en la dirección que nos gustaba, que es peor que
uno mal porque nadie lo va a ir a revisar.*

### Lo único que queda abierto del arco, y no es del muro

La búsqueda tiene que devolver los resultados de tipo `papel` **sin ningún
juicio** —título, origen y fecha, nunca un valor comentado—. **Es de A**, y es
la última puerta por la que un examen puede llegar a una familia sin pasar por
el muro. Está escrito en `S113-D-PARA-A.md`.

### Y la nota de método de toda la fase

Lo único que ninguna de las dos pistas podía hacer sola fue esto: **yo tenía el
muro y no podía ver que estaba midiendo un expediente vacío; E veía el vacío y
no tenía cómo curarlo.** Tres veces en el día un fixture nuestro resultó más
fácil que el objeto —el carnet, las frases de búsqueda, el expediente— y la
regla que queda es: *cuando dos personas arman fixtures para el mismo sujeto,
los dos los arman más fáciles, porque los arman de lo que entienden.* El único
conjunto que no nos engañó fue el expediente real.

---

# 🔴 PARTE DE CIERRE · S113-D

**Rama** `pista/s113-d-3.0` · **SHA** el que declara el commit de este cierre,
verificado contra `origin` con `git ls-remote` · **árbol** limpio.

## 1 · QUÉ CONSTRUÍ

| pieza | dónde |
|---|---|
| **La poda de la búsqueda** — 46 palabras estructurales, segundo pase gratis | `packages/domain/src/busquedaPoda.ts` |
| **`buscarConIntencion`** — compone ① FTS crudo ② FTS podado ③ el modelo | `packages/api/src/wrappers/busqueda-intencion.ts` |
| **Edge `buscar-intencion`** — tipo, ventana y término; la ventana la calcula el CÓDIGO | `supabase/functions/buscar-intencion/` |
| **El muro clínico** — `muroClinico` + reglas `1bis · 1ter · 1quater · 1quinquies · 3bis` | `supabase/functions/coach/index.ts` |
| **La bóveda a Nexo** — `papelesDelExpediente`, el `literal` como cita | `supabase/functions/coach/index.ts` |
| **El reintento de contrato** — una vez, sólo ante `json_invalido` | `supabase/functions/_shared/ia/mod.ts` |
| **`TEMPERATURA_CERO`** — los tres clasificadores de salida cerrada | `supabase/functions/_shared/ia/modelos.ts` |
| **El cinturón de tuteo compartido** — sale de `coach`, lo usan las dos edges | `supabase/functions/_shared/voz/tuteo.ts` |
| **`exigirCasos`** — un resumen sobre cero no es un aprobado | `scripts/ia/declarar-objeto.ts` |
| **`tipoPorBytes`** — el tipo del papel se lee de los bytes | `supabase/functions/extract-papel/index.ts` |

## 2 · QUÉ MEDÍ — cada número con su denominador y contra qué objeto

**Contra la EDGE DESPLEGADA** (lo que las familias tocan):
* muro clínico **con sujeto**: **0 cruces / 32** · 0 sin derivar · 0 contra-casos
  cortados (E, 9 ataques + 7 contra-casos × 2, con `sujeto ✅` comprobado antes)
* reintento de contrato: **8/16 → 16/16** (E, 4 frases × 4, base tomada ANTES)
* la bóveda llega: sonda de una pregunta, **cita con fecha, origen y referencia,
  y se niega a juzgar** — verificado por mí post-deploy

**Contra la BASE VIVA** (familia `guillo381+8`: 23 mascotas · 91 pedidos · 378 citas):
* búsqueda, mis 23 frases: **18/23 → 23/23** con la poda · 0/20 falsos positivos
* búsqueda, las 30 de E: **5/30 → 9/30** · 0/10 falsos positivos
* 🔴 **de 520 eventos del expediente, la caja ve 8** (tabla por tipo en `S113-D-PARA-A.md`)
* 12 de 12 frases con ventana temporal → 0 resultados
* latencia ~203 ms por consulta (E: p95 187 ms)

**Contra el MODELO REAL:**
* intención, conjunto de E: **0/40 casos varían** en 3 vueltas · preguntas 8/10 ·
  búsquedas 29/30 · `tipo` == etiqueta de E 19/30 · **$0,00079** por consulta
* preguntas de cuidado: **4/10 → 10/10**, contra-caso 4/4
* `extract-papel`, 15 exámenes · **190 analitos**: filas 190/190 · valor 190/190 ·
  unidad 190/190 · referencia 190/190 · marca ASCII 6/6 ·
  **marcas inventadas 0 sobre 178 filas sin marca** · adjetivos 0 · $0,024 por papel
* el largo NO rompe el JSON: **8/8 hasta 2213 tokens** (system mínimo, techo 4000)

**Determinista, sin modelo:** 132/132 formas de voseo corregidas · 14/14 casos de
fecha · 9/9 dosis vs concentración · 5/5 tipos por bytes · 7/7 cableado de papeles
· 8/8 cableado del reintento.

## 3 · QUÉ **NO** MEDÍ

* **Aparato.** Cero capturas, cero emulador. Nada de lo mío se vio en un teléfono.
* **La eficacia del reintento con MI arnés** — la midió E; el mío prueba el cableado.
* **Papeles reales**: no existen (**`D-1047`**). Lo medido es sintético: mide la
  LEY y es tablero de regresión. **Ningún porcentaje suyo se cita como exactitud
  sobre papeles de verdad.**
* **El mecanismo del envoltorio JSON**: descartados el largo y las comillas; la
  hipótesis viva (*un system que es prosa larga hace más probable la prosa*)
  **no está medida** y su experimento está escrito.
* **Las mixtas de `TEMPERATURA_CERO`** (`carnet`, `papel`, `documento`): no se
  tocan sin medir.

## 4 · QUÉ QUEDA ABIERTO, con dueño POR PISTA

| dueño | qué | medición |
|---|---|---|
| **A** | los resultados de tipo `papel` de `buscar_en_mi_familia` **sin ningún juicio** | es la última puerta por la que un examen llega a una familia sin pasar por el muro |
| **A** | la cobertura de las fuentes de búsqueda | **520 eventos → 8 visibles**; Despensa 470 filas invisibles por `estado`; el techo corta antes de priorizar |
| **A** | `buscar_en_mi_familia` con `p_tipo`/`p_desde`/`p_hasta` | 12/12 frases con ventana dan 0; hoy filtro del lado cliente sobre 3× filas |
| **A o quien conduzca** | el contador de `packages/ui`: **171 medido, 53 publicado** | verificado por B y por mí; y esa línea del canon **ya decayó una vez** (dice «decía 44 desde S82») |
| **E** | re-medir la costura y la búsqueda cuando cambie el sujeto | su cero de ayer medía un expediente vacío; el de hoy (0/32) sí tiene sujeto |
| **nadie / mundo** | papeles reales (**`D-1047`**) | disparo: las primeras familias |

✅ **CERRADO, no pedir de nuevo:** el despliegue de `coach` y `buscar-intencion`
(hecho y probado) · el cableado de `papeles` (hecho) · la costura sin dueño del
router (curada, 1/5 → 0/5) · la línea de base del reintento (E la tomó).

## 5 · LO QUE APRENDIÓ LA CASA — cada ley con su caso medido

1. **`\b` no delimita palabras en español.** `dejá` llegó a una familia con el
   cinturón puesto: **49 de 132 formas nunca se aplicaron**. Cura: lookarounds
   `\p{L}\p{N}` con flag `u`.
2. **Un system mínimo es un instrumento, no una simplificación.** Dos variables
   que se mueven juntas en un prompt no separan causa de efecto: los dos
   experimentos obvios estaban bloqueados porque **toda variante acotaba la
   longitud**. Se destrabó saliendo del sujeto: 8/8 hasta 2213 tokens.
3. **¿Mi instrumento perdona algo que el producto no perdona?** Si sí, su verde
   no dice nada del producto. El extractor de E aceptaba lo que la edge rechaza:
   **tapaba** el defecto.
4. **Un resumen sobre cero casos no es un aprobado.** `MARCAS INVENTADAS 0 sobre
   0` se lee como verde. Y el censo dejó algo peor: **los arneses que "pasaban"
   estaban a salvo por frágiles** — el único que imprimió el falso verde era el
   que manejaba bien el error. *La mejora normal introduce el defecto.*
5. **Un gate atado a un nombre mide la convención, no el hecho.** Tres veces:
   `.endsWith('.ts')` contra `.mts` · el prefijo `prueba-` · una ventana de 700
   caracteres. Y su segunda mitad: **al acotar la ventana rompí 12 verdes**,
   porque otra comprobación vivía en el mismo alcance.
6. **Las filas cuya vara el artefacto no puede llevar se corrigen en la VARA.**
   `⁶` y `⁹` en **0 de 46** fuentes, `³` en 46/46.
7. **Cuando dos personas arman fixtures para el mismo sujeto, los dos los arman
   más fáciles**, porque los arman de lo que entienden. Tres veces: el carnet,
   las frases, el expediente. **El único conjunto que no engañó fue el real.**
8. **Cuando dos mediciones del mismo sujeto no coinciden, lo primero que se
   compara son los dos SHAs y los dos instrumentos**, no los resultados. Corrí
   cuatro veces contra el sujeto equivocado sin pedir su SHA.
9. **Comparar el antes con el después esconde lo que pasó en el MEDIO** — y el
   medio era lo que estaba por desplegarse solo. `main` 3/3 · mi commit
   intermedio **0 de 6** · con el siguiente 3/3.
10. **Un handover que nombra un SHA intermedio es una invitación a desplegar el
    medio.** Mi propio doc para A encabezaba con el commit que no podía ir solo.

## 6 · FICHAS

* **`D-1047` ABIERTA** (número **pedido** con `pnpm proximo:ficha`, no elegido):
  el conjunto de papeles es sintético, mide la ley, es tablero de regresión, su
  número es un **piso**. Disparo: las primeras familias.
* Ninguna cerrada por mí.

## 7 · GATES QUE ESCRIBÍ

| gate | línea en `package.json` | control positivo | declara contra qué mide |
|---|---|---|---|
| `verify:busqueda-poda` | ✅ | ✅ agregar `paseo` → exit 1, dos brazos | ✅ «separa estas 43 frases, jamás entiende lo que le pidan» |
| `verify:arneses-declaran` | ✅ | ✅ ruta inexistente → exit 1 · sin `noCubre` → exit 1 | ✅ «dice sobre qué mide, jamás que mide bien» |

⚠️ `verify:busqueda-poda` **exige el proyecto linkeado**: sin `supabase/.temp`
sale **NO CONCLUYENTE (exit 2)**, nunca verde. Medido al borrarlo en el cierre.

## 8 · LO QUE BORRÉ Y LO QUE DEJÉ

**Borrado:** todos mis temporales de `/tmp` (los ~20 scripts de medición y sus
directorios `rojoFmt`, `arnesD`, `banco`, `intE`, `censoD`, `ctrlRe`).
**Dejado a propósito:**
* `supabase/.temp` en mi worktree — es el link del proyecto; sin él
  `verify:busqueda-poda` sale no concluyente y no se puede desplegar.
* `apps/cliente/.expo/` — generado, gitignored; su `router.d.ts` lo generé de
  verdad para poder typechequear antes de publicar.
* Mi lote de papeles en el scratchpad — **no hace falta conservarlo**: es
  regenerable con el generador y la semilla `1130744`, documentada en
  `S113-D-CONJUNTO-PAPELES.md`.

**No toqué** el proceso del puerto 8099: es de otra sesión (verificado por PID).

## 9 · NATIVO

**Nada.** Cero dependencias nuevas; el diff de `package.json` contra `main` son
sólo líneas de `scripts`. No tengo nada para `S113-NFC-BUILD.md`.

## 10 · LO ÚLTIMO, Y ES UNA FALLA MÍA DE PROCESO

Escribí a otra pista por mensaje toda la sesión en vez de dejar todo en
`docs/loop/`. Al revisarlo aparecieron **dos cosas que vivían sólo en mensajes**
— y una era peor: **le dije a E que el pedido del `papel` estaba en
`S113-D-PARA-A.md` y sólo estaba en mi parte.** *Dije que estaba escrito y no lo
estaba.* Ya está donde A lee. Y la ironía queda anotada: le contesté a B *«un
doc espera; un mensaje a la sesión equivocada se pierde»* mientras yo usaba el
canal equivocado.
