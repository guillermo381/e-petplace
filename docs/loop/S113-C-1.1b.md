# S113-C · 1.1b · el perfil habla primero

Rama `pista/s113-c-1.1b`, desde main `f987de64`.
Mergeado **`pista/s113-b-1.1` @ `48fb0f2f`**; verificado por ancestría que
**`3c92c185` está adentro**. **Cuenta: `guillo381+8@gmail.com`.**

## ① LA FRANJA DE SEGURIDAD — lo que hay que saber antes de tocar

Montada **entre la identidad y el hoy**: primero quién es, después qué cuidado
necesita, y recién ahí cómo viene el día. Para eso **el bloque «Cómo está hoy»
se movió abajo de identidad** (190 líneas), que era el orden que el mandato pide
y no el que había.

La decisión de **qué entra y con qué nombre** vive en `lib/perfil/seguridad.ts`,
no en la pantalla: es criterio, no render, y así se puede probar sin montar
nada. Cuatro fuentes → cuatro clases: alergias · medicación · condiciones ·
restricciones.

🔴 **Con nombre o no entra.** Un ítem sin nombre legible se descarta: *«tiene
alergias» no le sirve a nadie — el que va a bañarlo necesita saber a QUÉ*, y una
fila que dice «alergia» y nada más ocupa el lugar de la que sí dice algo.

🔴 **`alergias_detalle` llega como `unknown[]`** («el jsonb del snapshot tal
cual»). **Medí la forma contra la base** (`mascota_perfil_vigente.alergias`,
5-sep): `{ estado, alergeno, categoria, evento_id, severidad,
fecha_diagnostico }`. Se lee con guardas de runtime y sin `alergeno` el ítem no
entra. **Pedido a A abajo.**

El resumen de la franja cerrada son **las dos que más pesan**, no todas: si
dijera las cinco, cerrada y abierta dirían lo mismo y abrirla no serviría.

## ② LA FILA DE AUSENCIAS DEJA DE SER INCONDICIONAL

Empujaba «desparasitación» y «alergias» **siempre**, existieran o no. *Una lista
de faltantes que nombra cosas que están enseña a no leerla* — y la de al lado,
que sí falta, se pierde entre las que no. El dato para callarlas estaba en el
perfil y nadie lo miraba: **es el mismo hallazgo que el globo del Hogar en
`D-1026`**.

## ③ C7 — LA VACUNA QUE VENCE ANTES, EN LA TARJETA

Bajo el nombre, **sólo si la hay**: sin próxima registrada no se dibuja nada.
*Una línea que dice «sin datos» bajo cada nombre convierte la tira en un tablero
de faltantes.* La señal ya traía `proxima_vacuna`; no hizo falta pedir nada.

## ④ LOS TRES ROJOS (C8), y su discriminador

```
C7 · la tira            «Quíntuple · 05 ene 2027»

Thor    franja: SÍ · «Alérgico a pollo» · «Toma Enzimax 1 tableta»
        ausencias: «desparasitación»            ← NO dice «alergias»: las tiene
Zeus    franja: no  (control negativo)
        ausencias: «vacunas · desparasitación · alergias»  ← las tres, correcto
Sombra  franja: no · verbos que le piden: NINGUNO · sin fila de ausencias
errores de página: 0
```

**Ese par es la prueba de ②**: la misma pantalla omite «alergias» para quien las
tiene y la nombra para quien no. *Con un solo caso no se probaba nada — una
franja que se dibujara siempre pasaba el de Thor.*

## ⑤ MI PROPIO GATE ME CAZÓ, y eran falsos positivos

`verify:pide-en-memorial` salió **ROJO con 2**: «Lo registró su familia» y «Lo
registró una clínica». **No piden nada**: son atribuciones en pasado que
entraron por la raíz `registr`, la misma del imperativo. Excluidas **por clase**
y con su razón escrita — *el pretérito narra, el imperativo manda* — y el
control positivo sigue viendo «Regístralo» y «Registralo».

## ⑥ LO QUE NO CONSTRUÍ, SIN MAQUILLAR

De la composición de C6 quedan **cuatro piezas montadas y tres sin montar**:

| pedido | estado |
|---|---|
| retrato · identidad · **franja** · hoy · vacunas · documentos · alimento · vida · vitales | ✅ en ese orden |
| fila de ausencias condicional | ✅ |
| **`CeldasHoy`** en lugar de mi grilla | ⏳ no en esta mitad → **✅ montada en la segunda** (⑦) |
| **`PiezaMedicacionActiva`** como sección propia | ⏳ → **✅ montada en la segunda** (⑦) |
| **Antiparasitario** con chips por plaga y puerta a registrar | ⏳ → **✅ montado en la segunda** (⑦) |
| **`FiltrosLineaDeVida`** en «La vida» | 🔴 **sigue sin montar**, y con razón medida (⑧) |

⚠️ *Esta tabla es de la PRIMERA mitad y se marcó al cerrar la segunda: un parte
que dice «no montada» treinta líneas arriba de donde la monta se lee como dos
verdades, y cualquiera cita la que le conviene. Es el mismo desliz que ya me
costó una corrección con «Foto»/«Recuerdo».*

*Lo digo entero en vez de listar sólo lo hecho: las tres piezas están mergeadas
y disponibles, así que lo que falta es trabajo, no bloqueo.*

## LO QUE PIDO, POR NOMBRE

**A A, con su medición:**
1. ~~La plaga en la señal~~ **✅ entregada** en `31cfbdd7` — y al cablearla
   apareció que **falta la otra mitad**: ver ⑩.
2. ~~Tipar `alergias_detalle`~~ **✅ entregado** en `31cfbdd7`; mis guardas se
   retiraron (⑨).

**A B:** `OrbeCoach` exportado y `resumenDeLaTanda` (siguen del 1.0.2).

## GATES

`pide-en-memorial` **16 · 0 fuera** (control 10/10) · `nexo` **344 · 0** ·
`hooks` · `hooks-bajo-return` · `ref-antes-de-uso` · `gates-existen` ·
`razon-muda` · `puerta-unica` · `diseno` **61 reglas** · `coach` **134 · 0** ·
`glifos` **31 · 0** · `contrast` OK · `tipos:rutas` OK · **4 tsc en 0** (B curó
su galería: los 3 errores del 1.0.2 ya no están).

## RUTAS Y KEYS

`hogar/mascota/[mascotaId].tsx` (franja + orden + ausencias) ·
`hogar/index.tsx` (C7) · **`lib/perfil/seguridad.ts` nuevo** · censo con dos
exclusiones nuevas. **10 keys** es/en: `perfil.seguridad*` (9) ·
`hogar.proximaVacunaCorta`. Instrumento: `scripts/medir-perfil-habla.mjs`.

**No toqué** `packages/*` · `supabase/*` · el prestador. **Cero aparato.**

---

# Segunda mitad · las tres piezas, y la plaga que no llega

Mergeado **A `31cfbdd7`** (señal con plaga + `alergias_detalle` tipado).

## ⑦ Lo montado

- **`CeldasHoy`** reemplaza la grilla propia (203 líneas → 112), con **la fila
  de ausencias desenganchada** a su propio bloque: vivía dentro del mismo IIFE,
  así que cambiar la grilla obligaba a tocarla. ⚠️ **Censo antes de mover**:
  `PieRevelar` **no estaba** en ese bloque (0 ocurrencias en sus 190 líneas) —
  la mesa lo nombró y se midió en vez de suponerse.
- **Antiparasitario** con chips por plaga y próxima. Sin registros no se dibuja
  la sección —la celda ya dice «sin registro»— **pero la puerta a registrar
  queda**: es donde está la ausencia.
- **`PiezaMedicacionActiva`**: la franja dice lo urgente, la pieza lista todo.

## ⑧ 🔴 `FiltrosLineaDeVida` NO se montó — el choque es de vocabulario

```
la pantalla hoy:  salud · paseos · estetica · adiestramiento · bitacora   (por OFICIO)
la pieza de B:    salud · vacunas · antiparasitario · peso · cuidado · recuerdos  (por TIPO DE DATO)
```

Sólo «salud» coincide de nombre. Montarla **colapsaría paseos/estética/
adiestramiento en un solo «cuidado»** —tres filtros que hoy existen se vuelven
uno— o me obligaría a inventar un mapeo que nadie firmó. **Es decisión de
producto, no de montaje.**

## ⑨ Las guardas se retiran en el mismo acto que llega el tipo

`alergias_detalle` era `unknown[]`. Con el tipo de A las guardas se van:
sostenerlas «por las dudas» dejaría **dos verdades sobre la misma forma** —la
del compilador y la mía— y la mía envejecería sin avisar.

## ⑩ 🔴 LA PLAGA NO LLEGA, Y NO ES POR FALTA DEL CAMPO

Cablé el campo de A (con su regla: `null` = nadie declaró próxima, **no** «al
día»). Después medí la cadena entera:

1. `evento_desparasitacion_aplicada` tenía **cero filas** en toda la base;
2. sembré una **por la puerta única** → quedó con `fecha_proxima 2026-09-25` y
   **`plagas: null`**;
3. el wrapper de escritura (`salud.ts:53`) acepta producto, tipo, fechas y
   notas — **no acepta `plagas`**;
4. la señal de A **se arma abriendo `plagas` fila por fila**.

⇒ *Mientras la puerta de escritura no exponga las plagas, ninguna fila puede
aportar una.* **El campo de lectura llegó; falta la mitad que escribe.**

## ⑪ Los rojos, y la fila mostró su TERCER estado

```
la tira    «Quíntuple · 05 ene 2027»       (vacuna sola, por ⑩)
Thor       franja SÍ · «Alérgico a pollo» · «Toma Enzimax 1 tableta»
           ausencias: (no aparece)   ← ya tiene las tres cosas
Zeus       franja no · ausencias: «vacunas · desparasitación · alergias»
Sombra     franja no · verbos que le piden: NINGUNO · sin ausencias
0 errores de página
```

**Los tres estados de la misma fila en una corrida**: todo puesto (no se
dibuja), algo puesto (nombra sólo lo que falta), nada puesto (las tres).

## ⑫ El arnés dejó de discriminar al aparecer un vecino

Buscaba la fila por la palabra «desparasitación» y con la sección nueva
encontraba **su rótulo**. Ahora la toma por su ancla, porque «Sin registro» es
también la voz de las celdas vacías.

## PEDIDOS

**A A:** exponer `plagas` en `registrarDesparasitacion` — con eso ⑩ se cierra
solo. **A la mesa:** el vocabulario del filtro (⑧). **A B:** `OrbeCoach` y
`resumenDeLaTanda`.

**Residuo:** una desparasitación sembrada en Thor (Bravecto, 20-ago/25-sep)
para probar la cadena.
