# S107-B · LAS PIEZAS DEL OFICIO — parte de pista

**Territorio (76h):** `packages/ui/**` y este parte. **Nada de `apps/`, nada de
`supabase/`, nada de `docs/` fuera de acá.**
**Rama:** `pista/s107-b` · **worktree:** `../e-petplace-s107-b` · abrió en `0f53f24e`.

---

## ⓪ DOS COSAS QUE SE DECLARAN ANTES DE LO CONSTRUIDO

### 🔴 ① `LETRA_GUARDERIA` ESTÁ FRENADA, Y ESTAS PIEZAS SON DE GUARDERÍA

Medido, no supuesto: la letra tiene en su puerta **«LETRA FRENADA — NO RIGE, NO
SE CONSTRUYE, NO SE PUBLICA»** (memo del abogado, 25-ago-2026). **§3 —el reparto
de responsabilidad— es *nula de pleno derecho frente al consumidor*** y §6 tiene
**riesgo penal**. Fichas `D-918` y `D-919`, las dos **🔴 vivas**: se verificó
contra `DEUDAS_CANONICAS.md` y contra los partes de S106/S107 que **no hay
reapertura** en el repo.

**Se construyó igual, y la razón es el §0 del propio encargo:** *ninguna pieza
escribe texto que reparta responsabilidad; los strings van al lote que el
founder lee aparte.* Eso es exactamente la mitad que el abogado no tocó.

⇒ **La decisión estructural de la tanda: NINGUNA pieza trae texto adentro.**
Todo rótulo, toda voz de estado y hasta el nombre de cada dirección del acta
entran **por prop**. *No es prolijidad de i18n: es lo que hace que una pieza no
PUEDA afirmar un reparto de responsabilidad que la letra no sostiene* (L-222 —
el estado malo es inexpresable; precedente `EscaleraEstados`, *«cero
diccionario adentro»*, y `FilaEntrega`, que no tiene prop de mascota).

**Las dos piezas que rozan la cláusula frenada, declaradas:**
`ActaDeEntrega` (la conformidad es lo que alguien va a citar en una disputa) y
`AceptacionDeDocumentos`. **Las dos registran, ninguna adjudica**: `sin_conformidad`
es un HECHO con fecha, no una sentencia. **Qué significa legalmente es la
reescritura de §3 y es de la mesa** — está escrito en la cabecera de la pieza
para que nadie la cite como si lo hubiera decidido.

### ⚠️ ② EL `BRIEF S107` NO EXISTE EN EL REPO

El encargo manda leerlo, y **no está**: cero archivos, cero menciones en
`docs/`. Tampoco existen «los contratos que A publique» — `pista/s107-a` está en
`0f53f24e`, **sin un solo commit**. Se trabajó con lo que sí está firmado
(la skill, `DIRECCION_ARTE` §6ter/§6septies, `DIRECCION_ARTE_VIDEOCONSULTA`) y
**los contratos de props los definió B**. ⇒ **Quedan a reconciliar con A.**

---

## ① EL CENSO ACHICÓ EL TRABAJO — TRES PIEZAS NO NACIERON

El encargo listaba doce. **Tres no se construyen porque la casa ya las tiene**
(protocolo 1c, pregunta 2: reusar > adaptar > crear). Se declara para que nadie
las construya de nuevo:

| pedido | veredicto |
|---|---|
| **el reproductor** | **es `ClipSesion`** — ya existe, con sus estados y su regla de «jamás autoplay» |
| **el punto vivo** | **es `PinEnMapa`** (§6ter). Sin traza y sin paradas **es cómo lo consume D**, no una pieza nueva |
| **la tarjeta de estadía** | **`Tarjeta` + `EscaleraEstados` + `HiloDelDia`**. `EscaleraEstados` ya es la pieza de «progreso multi-paso» y ya trae *«cero diccionario de estados adentro: las etiquetas llegan por prop»* — la condición que §0 exige. Envolverlas en una cuarta pieza habría fijado un layout que cada superficie quiere distinto |

Y una cuarta, a medias: **la aceptación de documentos NO se inventó — se
GENERALIZÓ.** `apps/prestador/src/components/aceptacion-terminos.tsx` (S104-C,
firma founder) ya tiene la forma exacta: N casillas obligatorias con enlace al
documento inmutable + **una opcional separada abajo que nunca nace marcada**.
Lo único que la volvía irreusable era que sus documentos estaban **cableados**
al par del prestador. La pieza nueva recibe **la lista por prop**.

---

## ② LO CONSTRUIDO — DIEZ PIEZAS, TODAS EN GALERÍA

`CalendarioCupo` · `FichaFranja` · `FichaMensualidad` · `SemaforoSanitario` ·
`SelectorRoster` · `ContadorClip` · `MiniaturaClip` · `HiloDelDia` ·
`ActaDeEntrega` · `AceptacionDeDocumentos`.

**Las decisiones que no son obvias y que C y D necesitan saber:**

- **`CalendarioCupo` — el día lleno se dibuja, recibe el toque y JAMÁS llama a
  `onElegir`.** Solo cuenta su motivo, que vive **a ancho completo bajo la
  rejilla** y **arranca mostrando el del primer día lleno**: el porqué está a la
  vista sin tocar nada. **No se inventó: es la resolución que `SelectorVentana`
  ya firmó** para su disposición `tira`, citada y no re-argumentada. La Ley 23
  queda intacta — el servidor sigue sin poder recibir un día sin cupo.
  🔴 **No existe prop de cupo restante y no va a existir** (`MODELO_LOYALTY`
  §7.5). El matiz: *«sin lugar» ayuda a elegir otro día; «quedan 2» ayuda a que
  no lo pienses.*
- **`SemaforoSanitario` — «falta sin camino» NO COMPILA.** Unión discriminada:
  `estado:'falta'` exige `onResolver` **y** `etiquetaResolver`. *Un
  `onResolver?` opcional habría dejado la regla en un comentario, y un
  comentario no frena a un compilador (L-396).* El faltante va en `warning`,
  jamás `danger`: es una tarea, no una falla.
- **`SelectorRoster` — `seleccionadas` SIN default.** El consumidor declara el
  arranque y el arranque correcto es `[]`. **No compone `SelectorOpcion`** y la
  razón está medida: en `entidad` su señal es el RELLENO, que dice *«éste está
  elegido»* pero **no dice «podés elegir varios»** antes de tocar; y meterle un
  slot a una pieza congelada que montan ~20 pantallas es el precedente que S71
  ya rechazó con `FilaDato`. La casilla vacía es **afordancia**, el relleno es
  **estado** — por eso no es Chanel.
  🔴 **El mínimo de 1 NO se valida acá** (precedente `Casilla`: *«este control no
  valida»*): **lo gatea la pantalla** con `seleccionadas.length > 0`.
- **`ContadorClip`** sube a `techoSeg` y llama a `onTecho()` **una sola vez**
  (guard en `ref`; se re-arma si cambia `inicioTs`). **No apaga la cámara:
  avisa** — quien graba es la pantalla. Corre por diferencia contra el inicio,
  jamás acumulando: *el hilo se traba justo cuando la cámara codifica.*
- **`MiniaturaClip` consume `tokens/sobreVideo` (§6septies) — cero scrim nuevo.**
  Un póster es **el ejemplo literal** que §6ter usó para descartar ensanchar la
  Ley 12. La duración va sobre `banda` (0.72) y no sobre `disco` (0.62), porque
  la clase lo dice: *«la prosa necesita más piso que un glifo»*.
- **`ActaDeEntrega` — el prestador NO PUEDE firmar la conformidad del dueño
  porque la prop no está.** `onConformar` existe solo en `modo:'leer'`; los
  callbacks de edición solo en `'levantar'`. Las fotos son un **slot**: la
  captura y su cola siguen siendo de la pantalla (frontera `EvidenciaFoto`).

---

## ③ GATES — TODOS MEDIDOS, NINGUNO ASUMIDO

| gate | resultado |
|---|---|
| `pnpm --filter @epetplace/ui typecheck` | **VERDE** |
| `cliente` · `prestador` · `@epetplace/api` typecheck | **VERDE los tres** |
| `node scripts/verify-diseno.mjs` | **VERDE · 60 reglas** |
| `pnpm verify:contrast` | **391 pares · 0 fallos** |
| `R17` (la galería no envejece) | `exportaciones=135 · en-galería=126 · pendientes=0/0` |

⚠️ **`@epetplace/domain` falla y NO es de esta tanda:** 10 errores, todos en
`node_modules` (`@types/emscripten`, `@types/dom-mediacapture-record`).
**Verificado corriéndolo en `main` sin tocar: los mismos 10.** El canon ya lo
declara desde S81.

**🔴 `R41` CAZÓ UN DEFECTO REAL, y conviene que quede escrito:** `SelectorRoster`
animaba mirando **solo memorial** y no `useReducedMotion`. Lo encontró el gate,
no una revisión. Curado con la receta firmada de la casa (la que usa `Destape`):
**brazo compartido memorial‖reduce-motion**, el hook **suelto** y combinado
después, y **se quita el VIAJE, no el momento** — el check sigue apareciendo con
el mismo fade y el mismo tempo, sin escalar. *Dejarlo sin confirmación de que
marcó sería curarle el mareo a alguien quitándole la respuesta.*

**Cero pares nuevos de contraste**, y es una consecuencia del diseño, no suerte:
las piezas consumen tokens ya medidos (`accent.control`, `status.*`,
`sobreVideo`), y el check de `SelectorRoster` es **la receta exacta de
`Casilla`** —mismo path, mismo trazo, `palette.white`— en vez de un color a mano.

---

## ④ LO QUE FALTA PARA «HECHO», SIN MAQUILLAR

El encargo define HECHO como *piezas publicadas con su contrato completo **y
consumidas por C y D***. **La segunda mitad NO está**, y no depende de B:

- **C y D no tienen worktree ni rama en este repo.** No hay a quién entregarle.
- **La mitigación que sí se pagó:** la **galería llena TODAS las props de las
  diez piezas**. *Una prop que nadie llena no la ve ningún typecheck* — y hoy la
  galería es lo que las hace ver. **Ése es TODO su papel acá: consumidor mínimo
  del typecheck.**
- 🔴 **La galería NO es la sala de revisión, y esta línea se corrige por
  dirección de la conductora** (firma S106: *la galería está retirada*). ⏪ *Este
  parte decía «están en la galería, listas para que el founder las mire» — era
  falso y se marca en vez de borrarse.* **El gate visual va sobre la app real
  cuando C monte; el founder corrige en el aparato** (Ley 9 / L-153: la web no
  cierra gates de componentes).
- **Ninguna pieza pasó gate en dispositivo.**

---

## ⑤ COMPROMISOS ABIERTOS, CON DUEÑO

| # | qué | dueño |
|---|---|---|
| 1 | **Reconciliar los contratos de props con los de A** — B los definió porque al construir el BRIEF y los contratos de A no estaban en el repo; **A los publicó en `main` mientras B corría**. ⏪ *Este parte decía que no existían: era cierto al medirlo y dejó de serlo.* **La reconciliación la conduce A, y A pushea y mergea los dos commits de B.** | **A** |
| 2 | 🔴 **`D-645` — EN EL PLAN PARA C (dirección de la conductora).** *La promoción no es migración.* `apps/prestador/.../aceptacion-terminos.tsx` sigue vivo ⇒ **hay dos implementaciones de la misma forma y nada señala la vieja.** Que pase a montar `AceptacionDeDocumentos` con su par de documentos, **y la local muera en el mismo acto** (Ley 37). No se hizo acá: `apps/` no es territorio de B. **La declaración de este parte y la de la cabecera de la pieza ALCANZAN como traspaso** | **C** |
| 3 | **El lote de strings**: las diez piezas piden su voz por prop. Los textos de la galería son **relleno declarado**, jamás voz de producto | **founder** |
| 4 | **El mínimo de 1 del roster lo gatea la pantalla** (`seleccionadas.length > 0`) — la pieza no valida | **C y D** |
| 5 | **`CalendarioCupo` no hace cuentas de fecha**: `columnaInicial` y `cabecerasDias` los resuelve el riel de i18n. Ídem el patrón de días de `FichaMensualidad` y las horas de `FichaFranja` | **C y D** |
| 6 | **Gate visual de las diez SOBRE LA APP REAL**, cuando C las monte — no en la galería (ver ④) | **C monta · founder firma** |

---

## ⑥ SUPUESTOS VIVOS — lo que se re-verifica antes de usar

1. **Que §0 del plan es lo que el encargo transcribe.** No hay plan en el repo;
   si su §0 dice otra cosa, la decisión de «todo texto por prop» se re-lee.
2. **Que el freno de `LETRA_GUARDERIA` sigue vivo.** Medido hoy contra el
   objeto. Si la mesa reescribió §3, **estas piezas no cambian de forma** — lo
   que cambia es qué textos les manda la app, que es justamente el punto.

---

## ⑦ DOS MEDICIONES QUE QUEDAN SERVIDAS A LA CONDUCTORA (no son objeción)

Al corregir ④ busqué el texto de la firma para citarlo bien y **no lo encontré
en el repo**: ni en los partes de cierre de S106, ni en `DEUDAS_CANONICAS.md`,
ni en `DIRECCION_ARTE.md`. **Eso NO la desmiente** — una firma de mesa sin
transponer es cosa corriente en esta casa, y este mismo parte ya registra que el
canon viene con deuda de transposición. *Se anota porque un puntero que no se
puede seguir es caro para la próxima pista, no para discutir la firma.*

**Lo que sí está medido contra el objeto, y son dos cabos sueltos:**

1. **La entrada a `/gallery` SIGUE VIVA en el cliente** —
   `apps/cliente/src/app/(tabs)/cuenta/index.tsx:318` (`router.push('/gallery')`).
   Si la galería está retirada como sala de revisión, **ése es el camino que
   todavía la ofrece**. Territorio de C.
2. **`/gallery` devuelve HTTP 500 en web**, y la causa no tiene nada que ver con
   estas piezas: la midió S106-B-T3 —
   `requireNativeComponent is not a function` desde
   `@livekit/react-native-webrtc`, que entra al bundle web por el árbol de rutas
   (`_layout.tsx` del cliente importa `@/lib/livekit` **en el raíz**).
   ⚠️ **Consecuencia para cualquiera que intente verificar ahí:** un censo sobre
   `/gallery` **devuelve cero sin que nada falle a la vista**, y ese cero se lee
   como «no está la pieza» cuando lo cierto es «no está la galería». *El control
   positivo es obligatorio antes de creerle a un vacío en esa página.*

**Ninguna de las dos cambia lo entregado**: el gate visual va sobre la app real
cuando C monte, y la galería queda como lo que es acá — el consumidor mínimo que
hace que el typecheck vea las props.

---

## ⑧ TRES PEDIDOS DE LA CONDUCTORA — TEXTO LITERAL PARA DEPOSITAR

**Los tres caen fuera del territorio de B** (`docs/` fuera de este parte es de
A; `apps/` es de C). **B no los ejecuta.** Viajan como **texto autocontenido
listo para pegar**, que es como esta casa manda que viaje un pedido entre pistas
(regla 76b: nada por referencia a un reporte ajeno).

### ① Retirar la entrada `/gallery` — **YA ESTÁ EN ESTE PARTE**, §7 punto 1
Dueño **C**. Medido: `apps/cliente/src/app/(tabs)/cuenta/index.tsx:318`
(`router.push('/gallery')`). No hace falta re-medirlo al planificar.

### ② LA FIRMA DE S106, PARA QUE DEJE DE SER TRADICIÓN ORAL

> ⚠️ **B NO tiene el literal de esta firma: lo relaya la conductora.** Se
> transcribe tal como llegó, y **quien lo deposite lo coteja contra el acta de
> mesa** — un verbatim de segunda mano se marca como tal (L-142).

```
### LA GALERÍA SALE DE SALA DE REVISIÓN (firma S106)

**«La galería queda retirada como sala de revisión; el gate visual es la app
real en el aparato.»**

⇒ **La galería NO deja de existir ni de ser obligatoria** — `R17` sigue
exigiendo que toda pieza exportada esté montada ahí, y ése sigue siendo el
mecanismo que hace que el typecheck vea las props (*una prop que nadie llena
no la ve ningún typecheck*). **Lo que muere es que el founder JUZGUE ahí.**

**Consecuencia operativa, y es la que cambia los partes:** una pieza de
`packages/ui` **sin pantalla real que la monte NO puede pasar gate visual**.
Su gate **espera al consumidor**, y eso se declara en el parte en vez de
darse por pasado contra la galería.

⚠️ **Cabo suelto medido (S107-B, 28-ago):** la entrada a `/gallery` **sigue
viva** en `apps/cliente/src/app/(tabs)/cuenta/index.tsx:318`. Mientras esté,
la sala retirada sigue teniendo puerta.
```

### ③ LA LECCIÓN — **`L-432`, y el número NO es el que parece**

> 🔴 **VERIFICADO CONTRA EL OBJETO, y el resultado corrige a la vía obvia:**
> `DEUDAS_CANONICAS.md` llega a **L-429**, pero **`L-430` y `L-431` YA ESTÁN
> TOMADAS** por dos fichas de S107-CERT que viven **sin depositar** en
> `docs/relevamientos/` (`S107-CERT-FICHA-D952-y-L430.md` ·
> `S107-CERT-FICHA-D953-y-L431.md`). **Quien grepee sólo el canónico —que es el
> lugar correcto— se lleva un número ocupado.** Es el precedente `D-757` exacto:
> *un número usado en artefactos vivos sin ficha depositada, que la sesión
> siguiente encuentra tomado y sin dueño.* **Re-verificar igual al depositar.**

```
#### L-432 — UN CENSO QUE DA CERO VERIFICA PRIMERO QUE LA PÁGINA VIVA

**Un censo sobre una página rota devuelve CERO sin fallar a la vista** — y ese
cero se lee como *«no está la pieza»* cuando lo cierto es *«no está la
página»*.

**Medido (S106-B-T3):** se fue a `/gallery` a contar cuatro botones y los
cuatro dieron 0. El cero no probaba nada: `curl /gallery → HTTP 500`, body
vacío, **cero errores JS a la vista**. La causa es preexistente y ajena al
censo — `requireNativeComponent is not a function` desde
`@livekit/react-native-webrtc`, que entra al bundle web porque el `_layout.tsx`
del cliente importa `@/lib/livekit` **en el raíz**.

🔴 **Lo que la vuelve cara es que NO HAY SÍNTOMA:** la página responde, el
navegador no protesta, el censo termina bien y su número es falso. *No falla
la medición: falla la superficie sobre la que se mide, y el instrumento no
tiene cómo saberlo.*

**La regla:** todo censo cuyo resultado sea CERO **prueba primero que su
fuente existe** — control positivo contra un caso de resultado conocido, o el
estado HTTP de la página, antes de creerle al vacío. **Un cero sin control
positivo no dice «no hay»: dice «no vi».**

**Familia:** es `L-425` (*un baseline en 0 no dice «no hay»: dice «no vi, con
la lista de hoy»*) un piso más abajo — allá fallaba la LISTA, acá falla la
PÁGINA. Y es `L-192` en su forma de siempre: **una verificación cuyo modo de
falla es el silencio no es una verificación.**

**Re-cobrada el 28-ago (S107-B):** volvió a aparecer al buscar piezas en
`/gallery`. Segunda vez, misma página, misma causa viva.
```

---

## ⑨ 🔴 CORRECCIÓN URGENTE A §8 + EL CENSO DE NUMERACIÓN QUE LA MESA PIDIÓ

### ⚠️ EL NÚMERO DE LA LECCIÓN YA NO ES `L-432` — ES **`L-433`**

⏪ **§8 recomienda `L-432` y quedó FALSO.** Se marca, no se borra.

**Qué pasó, medido:** `L-432` **la depositó A** en `bec55b54` con **otra
lección** (*«la verdad vencida deriva, no afirma»*) **mientras B medía**. La
primera corrida de B dio `L-432 en DEUDAS: 0` y fue **verdadera al correrla**;
minutos después, falsa.

🔴 **Y ESTE CASO ES LA MEJOR EVIDENCIA DE LA ENMIENDA QUE LA MESA QUIERE
PROPONER, porque es de la clase que la enmienda NO cubriría.** Acá el número no
se perdió por reservarlo fuera del canónico: **se perdió por depositarlo bien,
en paralelo.** *Dos pistas que hacen todo correcto colisionan igual si las dos
leen el máximo antes de escribir.* ⇒ **`max+1` leído ANTES de escribir es una
lectura vencida en el momento de escribir** — la única defensa es **releer y
escribir en el mismo acto**, no un grep más ancho.

**Verificado al cierre (excluyendo este parte, para no certificar mi propio
texto — L-220):** `L-427…L-432` tomados · **`L-433`, `L-434`, `L-435` libres.**
⇒ **la lección de §8 va a `L-433`, y se re-verifica igual al depositar.**

### EL CENSO DE `D`, QUE ERA LO PEDIDO — **el patrón se confirma, y son 10, no 2**

| dónde | números |
|---|---|
| **Reservados en `docs/relevamientos/` SIN depositar** | `D-945` · `D-946` · `D-947` · `D-949` · `D-950` · `D-951` · `D-952` · `D-953` (**8**) |
| **Reservados en `docs/loop/` SIN depositar** (`S106-C-CIERRE.md`) | `D-954` · `D-955` (**2**) |
| **Depositados de verdad en el canónico** | `D-948` · `D-956` |

**Con `L-430`/`L-431` son DOCE números tomados fuera del archivo canónico.** El
patrón que la mesa sospechaba **está confirmado y es mucho más ancho que los dos
casos citados.**

### 🔴 PERO LA PREMISA DE LA MESA SE CORRIGE EN UN PUNTO: `D-956` NO FUE UNA COLISIÓN

El pedido dice *«dos colisiones de numeración en un día por la misma causa
(D-956, L-430/431)»*. **Medido: `D-956` no colisionó con nada** — está
depositado, no aparece en `relevamientos/`, y **queda POR ENCIMA de todo el
bloque reservado** (el máximo reservado es `D-955`). Tomarlo fue seguro.

**Y esto no es un detalle: las dos familias fallan en formas OPUESTAS, así que
una enmienda escrita para una no atrapa a la otra.**

| | máximo en el canónico | reservas fuera | qué pasa con `max+1` |
|---|---|---|---|
| **`L`** | `429` — **DEBAJO** de las reservas | `430`, `431` | 🔴 **`L-430` COLISIONA** |
| **`D`** | `956` — **ENCIMA** de las reservas | hasta `955` | ✅ `D-957` es seguro |

⇒ **En `D` el peligro no es `max+1`: son LOS HUECOS.** El canónico tiene un
salto visible de `D-948` a `D-956`, y **ocho de esos diez huecos están
ocupados**. *Quien "rellene un hueco" por prolijidad colisiona con una ficha
escrita.*

### DOS TRAMPAS MÁS PARA QUIEN MIDA ESTO

1. **`D-999` NO es una ficha**: es *«un control negativo de un instrumento
   viejo»* (declarado en `S105-D-FICHA-D923…`) y vive también en `S101-D.md`
   junto a `L-999`. **Un `max` ingenuo sobre `docs/` devuelve `D-999`.**
2. **El censo se corre EXCLUYENDO el parte propio.** La primera corrida de B se
   iba a contar a sí misma (`L-432` aparecía en `docs/` porque B lo había
   escrito en §8) — **L-220 en vivo: certificar el propio resumen y darle
   verde.**

### LO QUE B **NO** HACE, y de quién es

**La enmienda de método NO se propone acá: `docs/CONTRATO_TRABAJO.md` es de A y
la firma es de la mesa.** B entrega el censo y la corrección de premisa; **la
línea la redacta y la propone A en su parte**, como el pedido indica.

*Insumo para esa línea, del caso de arriba:* «el número se toma DEPOSITANDO, no
anotando» **es correcto como causa y no alcanza como defensa** — no cubre la
colisión entre dos depósitos simultáneos, que es la que ocurrió hoy.

---

## ⑩ CIERRE — LO QUE SE CERRÓ CONTRA EL OBJETO, Y LO QUE QUEDA

*Medido el 28-ago tras los merges de A. **Este bloque cierra ítems que otros
bloques dan por abiertos** — donde discrepe, gana éste, que es el más nuevo.*

### ✅ LA RECONCILIACIÓN DE PROPS: CERRADA, Y LAS PIEZAS NO CAMBIAN

`docs/contratos/s107-contrato-cupo-franja-estadia.md` (merge `f43e6b95`)
**nombra las cuatro piezas de B con su prop exacta** y reparte quién arma cada
texto. **Cotejado contra lo construido: coincide en las cuatro, cero cambios.**

Y coincide en lo que más importaba, sin que las dos pistas se hubieran
coordinado: el contrato dice de la franja *«recibe las dos ventanas ya
resueltas… **la misma pieza** en config del prestador y en perfil del lugar»* —
**que es exactamente la razón por la que se construyó una y no dos.** *Dos
pistas llegando al mismo reparto por su cuenta es la señal de que la línea está
bien puesta.*

**🔴 Y un hueco que B creyó encontrar y NO existe** — se deja escrito para que
nadie lo "arregle": `sin_cupo ⟺ disponible === 0` parece frágil ante un día
sobrevendido, **y no lo es**: la función define
`disponible = GREATEST(capacidad − consumido, 0)`, así que **nunca es
negativo**, y la sobreventa viaja aparte en `sobrevendido: boolean`. *La
igualdad estricta es correcta y completa.*

**⚠️ Lo que sí queda como cabo, y es de C/A, no de la pieza:** el contrato dice
que el día sobrevendido *«la superficie del prestador lo muestra»* **sin decir
dónde**. `CalendarioCupo` tiene DOS estados, así que **hoy un día sobrevendido
se dibuja idéntico a uno lleno**. Si tiene que verse *en el calendario*, la
pieza necesita un tercer estado — **y eso es decisión, no arreglo.**

### ✅ LOS TRES PEDIDOS DE §8: DEPOSITADOS POR A (`ff8fab66`)
La entrada `/gallery` al §6 de C · la firma S106 con su marca de relay · **y la
lección, que entró como `L-433` con su colisión declarada** — el número que la
corrección de §9 mandó, no el `L-432` que §8 decía.

### ✅ LA ENMIENDA DE MÉTODO: **FIRMADA POR LA MESA**
⏪ *§9 decía «la línea la redacta y la propone A». Quedó viejo: ya está firmada.*
Se transcribe acá porque **rige sobre B también** — B deposita números:

> **«El número se toma en el acto de depositar en el archivo canónico: releer
> el máximo y escribir el depósito son UN solo acto, en el mismo commit — quien
> deposita re-verifica ahí, no antes. Una ficha sin depositar no tiene número:
> las reservas fuera del canónico no reservan. Todo censo de numeración excluye
> el parte propio, y `D-999` no es ficha.»**

**Su registro en `docs/CONTRATO_TRABAJO.md` es de A y al cerrar este parte
TODAVÍA NO ESTABA** (medido: 0 coincidencias). *Se anota como pendiente
verificable, no como hecho.*

### 🟡 LOS DOCE NÚMEROS EN VUELO — la nota, y de quién es regularizarlos
La lista con **cuáles son y dónde viven** ya está en **§9** de este parte, lista
para levantar tal cual al registro. **La regularización es de la sesión CERT
cuando cierre, no de B ni de A.**

`D-945` · `D-946` · `D-947` · `D-949` · `D-950` · `D-951` · `D-952` · `D-953`
(en `docs/relevamientos/`) · `D-954` · `D-955` (en `docs/loop/S106-C-CIERRE.md`)
· `L-430` · `L-431` (en `docs/relevamientos/`).

### LO QUE SIGUE ABIERTO PARA B — **nada que B pueda avanzar solo**
Las diez piezas están mergeadas en `main`, con typecheck, `verify:diseno` y
`verify:contrast` en verde. **Falta un consumidor real** (C) y, con él, **el
gate visual en el aparato** — que ya NO es la galería. Hasta entonces B no tiene
trabajo propio en esta tanda.

---

## ⑪ LAS TRES ENMIENDAS FIRMADAS (S107, tanda 2 de B)

### ① `DocumentoAceptable.etiquetaAccesible?` — pedido de C, degradación REAL

**El defecto que C destapó:** `texto` y `etiquetaEnlace` son **dos mitades de
una sola frase** (*«Acepto los»* + *«Términos y Condiciones»*), porque el enlace
tiene que ser nodo aparte para abrirse sin marcar la casilla. La pieza le pasaba
a `Casilla` **sólo `texto`** ⇒ el lector anunciaba **«Acepto los»** y paraba.
*En una casilla de consentimiento legal eso no es incomodidad de a11y: es una
aceptación cuyo enunciado el usuario nunca oyó entero* (P23 — la casilla existe
para ser prueba).

**Se hizo lo pedido Y una cosa más, que es la que cierra el defecto:**
· **la prop existe** (`etiquetaAccesible?`), y C ya tiene sus claves esperándola;
· 🔴 **el DEFAULT se curó**: sin la prop, la pieza ahora **compone**
  `texto + etiquetaEnlace`. ⇒ **el caso roto dejó de ser el default.**

*Una prop opcional que hay que acordarse de llenar deja el defecto vivo para
quien no se acuerde. Lo que lo cierra es que el camino de menor esfuerzo ya sea
el correcto* — la prop queda para cuando la composición **no lee bien**, no para
arreglar el caso normal.

⚠️ **Y no contradice el literal de `Casilla`** (*«el TEXTO de la aceptación,
jamás el enlace»*): lo que esa regla prohíbe es anunciar el enlace **como
control**. El enlace sigue siendo responder aparte con su rol; lo que viaja al
label es **la frase que se acepta**.

### ② `CalendarioCupo` gana `'sobrevendido'` — y el default es la mitad de la firma

**Firmado: el tercer estado existe, pero SÓLO en la vista del prestador.** Para
el dueño un día sobrevendido **se ve lleno, igual que hoy.**

🔴 **Se implementó SEGURO POR CONSTRUCCIÓN, no por disciplina:**
`revelaSobreventa` **default `false`** ⇒ un día `'sobrevendido'` se dibuja
idéntico a uno lleno salvo que la superficie **pida** verlo.

*La alternativa —que la app del dueño colapse `sobrevendido → sin_cupo` antes de
pasar los días— deja el defecto a un olvido de distancia, **y no se ve como bug:
se ve como un calendario**. Acá olvidarse es el caso seguro* (L-222).

**Sin pares nuevos de contraste:** el revelado usa `status.warningBg` +
`warningBorder` + `Texto color="warning"`, el trío que `Tarjeta warning` ya monta
y `verify-contrast` ya mide. **Jamás `danger`** (Ley 22): pasarse de cupo pide
atención, no alarma.

**En galería va como PAR DISCRIMINADOR** —la misma data, las dos vistas, una
sobre otra—: *si los dos se vieran igual, la prop no hace nada; si el de arriba
distinguiera el día 12, se le está contando al dueño un problema ajeno.*

### ③ `R18` ENMENDADA, NO BORRADA — y el ancla baja con ella

**Firmado: se retira la entrada del cliente; R18 pasa a vigilar sólo la del
prestador.**

⏪ **La cabecera de R18 mandaba BORRARSE** el día que se firmara el retiro. **Se
marcó y no se obedeció**, con razón medida: la polaridad de R18 es *«que la
entrada no DESAPAREZCA»*, así que **borrarla junto con una de sus dos casas
dejaba sin guard a la que sobrevive** — el hueco exacto que S84-B8 cerró cuando
R18 miraba sólo al cliente, ahora en espejo.

🔴 **Y EL ANCLA BAJÓ DE 2 A 1 EN EL MISMO ACTO.** Sin eso, R18 salía **ROJA por
ANCLA ROTA** sobre una sola casa — un rojo que no habla del defecto que vigila.
*Bajar un ancla es aflojar un guard y por eso se declara: se bajó porque el
corpus se achicó POR FIRMA, no para que pasara un rojo.*

⚠️ **B NO retiró la entrada del archivo:** `apps/cliente/.../cuenta/index.tsx:318`
sigue viva y **es de C**. **El orden importa y esta tanda lo respetó:** primero
la enmienda de R18, después el retiro. *Al revés, R18 se cae en rojo el momento
en que C borra la línea.*

### GATES DE ESTA TANDA
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 60
reglas** (R18 informa `1 entradas vivas y sin __DEV__`) · `verify:contrast`
**391 pares · 0 fallos**, sin pares nuevos.

---

## ⑫ `FichaPaquete` — la pieza nueva (S107-B, tanda 3)

**Qué es:** el paquete con su tamaño, su precio, y **el espejo vivo debajo** —
«10 estadías a $90 = $9,00 por día — 25 % menos que tu día suelto». **La misma
pieza** en la config del prestador (seleccionable, con su campo de precio) y en
el perfil del lugar (solo lectura).

### 🔴 LA DECISIÓN DE FONDO: LA ARITMÉTICA ES DE LA PIEZA, LAS PALABRAS NO

*Si la cuenta viviera en cada pantalla, las dos superficies podrían dar números
distintos — y el día que difieran, el prestador vende un descuento que la
familia no ve.* ⇒ **la división y el porcentaje se calculan una sola vez, acá.**

**Pero el texto NO se arma en la pieza, por dos razones medidas:**
① el orden de las palabras cambia por idioma ⇒ componer la frase de fragmentos
es el anti-patrón clásico de i18n, y la casa ya tiene interpolación tipada con
espejo es↔en · ② 🔴 **el formato de plata es del RIEL**.

⇒ **`vozEquivalente` es una FUNCIÓN**: recibe los números calculados y devuelve
la frase. **La pieza garantiza que el número sea uno solo; el riel garantiza que
la frase esté bien dicha.**

**Y se exporta `equivalenciaDePaquete(...)`** para que una pantalla que necesite
el mismo número (un resumen, un checkout) **no lo vuelva a escribir**.

### ⚠️ UN HALLAZGO DECLARADO Y NO CURADO — `VozComision` revive el patrón que `PrecioText` mató
`PrecioText` nació porque el precio se escribía a mano **en 53 sitios** con la
forma `` `$${x.toFixed(2)}` `` (su cabecera lo cuenta). **`VozComision` la
reintrodujo** en su cuerpo. Esta tanda **no agregó el 54.º** —por eso la voz
sale por función— **y no cura `VozComision`**: tiene consumidores vivos y no es
territorio de esta tanda. *Se nombra para que no se re-descubra.*

### LO QUE LA FIRMA PIDIÓ Y CÓMO SE CUMPLIÓ
- **Si el paquete sale MÁS CARO, el espejo lo dice — en NEUTRO.** Mismo
  `Texto color="secondary"` caiga para donde caiga: ni `warning` ni `danger` ni
  ícono. *Esconder la comparación cuando no favorece convierte al espejo en
  publicidad; pintarla de alarma es la app opinando sobre el precio de un
  negocio ajeno.* **En galería va como PAR: barato arriba, caro abajo — si el
  de abajo se ve distinto, falla.**
- **Sin contadores ni urgencia** (`MODELO_LOYALTY` §7.5): no hay «quedan N» ni
  «ahorrás hasta». *El porcentaje no es gancho: es la unidad que vuelve
  comparables dos precios de distinta forma.*
- **La pieza no lee motor**: los dos precios llegan por props.

### LOS DOS HUECOS HONESTOS (Ley 13), los dos en galería
· **sin `precioDiaSuelto`** → dice el equivalente por día y **omite la
comparación** (`sin_comparacion`) — *inventar un 0 % sería afirmar que sale
igual* · · **sin `precioPaquete`** → no se dibuja el espejo, y **no se dibuja un
`$0,00`** que se lee como dato.
**Y un borde de aritmética:** `tamano <= 0` **no se dibuja** — dividir daría
`Infinity` y el espejo afirmaría cualquier cosa. La igualdad se juzga **al
centavo redondeado**, no al flotante crudo: *dos precios que rinden el mismo
centavo por día son iguales para quien paga.*

### CENSO (Ley 11) — por qué no es `SelectorOpcion`
Es un GRUPO de chips con una etiqueta por chip; acá **cada opción carga su
propio campo de precio y su propia línea calculada** — eso es una ficha por
opción, no un chip en fila. Meterle dos slots por ítem a una pieza congelada que
montan ~20 pantallas es el precedente que S71 rechazó con `FilaDato`. De
`VozComision` se toma **la forma** (presentacional, nulos honestos,
`secondary`); `SliderPrecio` se **monta por slot**, no se reimplementa.

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 60
reglas** (`R17`: exportaciones 136 · en-galería 127) · `verify:contrast`
**391 pares · 0 fallos**, sin pares nuevos.

---

## ⑬ TANDA 4 — SEIS PEDIDOS, Y DOS DEFECTOS PROPIOS QUE APARECIERON MIDIENDO

### ⓪ LA PREMISA SE CORRIGIÓ ANTES DE TOCAR: «la ficha expandible que hiciste»
**B no hizo ninguna ficha expandible.** `FichaPaquete` no expandía. **Lo
expandible era composición de C** (`guarderia/taller.tsx`): su `onElegir`
alterna `activo` y sólo pasa `campoPrecio` cuando está encendido. *Medirlo
cambió el trabajo: no había que agregar expansión — había que darle forma
propia a algo que ya ocurría por composición.*

### ① 🔴 EL MAGENTA ERA UN DEFECTO MÍO, no una preferencia de color
`FichaPaquete` pintaba **el borde según el registro** (`accent.cta` = tealDark
en el prestador) **y el fondo NO**: `capaBg.comunidad`, que es `pinkAlpha08`.
⇒ **en el taller la ficha encendida tenía borde teal con relleno magenta.**

Pasa a **`capaBg.cuidado`** (`tealAlpha16`) **en las dos casas**.
**Por qué `cuidado` y no `identidad`:** `capaBg.identidad` es
`verdeVitalAlpha15`, la capa de **VIDA/salud** — meterla en una pieza de
comercio sería usar la capa clínica para marcar un precio. `cuidado` es la capa
del servicio y su `capa.cuidado` **es el mismo `tealDark` que ya pintaba el
borde**: recién ahora borde y relleno dicen lo mismo.

### ② LA FICHA SE GENERALIZÓ — y con ella murió un nombre que mentía
Nace **`FichaDeOferta`**: pared **blanca** en reposo, etiqueta izquierda,
**toggle a la derecha que ENCIENDE Y EXPANDE en un solo acto**. La usan
**Diario, Mensual y los tres paquetes** — antes eran tres anatomías distintas
para el mismo trabajo (tarjeta seleccionable · `Interruptor` suelto con `Campo`
· slider suelto), que es cómo empiezan a divergir (19.9).

*Por qué el toggle hace las dos cosas:* encender una oferta y abrir su precio
**no son dos decisiones** — nadie enciende «Mensual» para dejarlo sin precio.
Separarlos dejaría existir «encendido sin precio», que es una oferta que no se
puede comprar.

**El espejo queda OPCIONAL** (Diario y Mensual no lo pasan y no se dibuja).

### 🔴 EL ALIAS QUE ME EQUIVOQUÉ, y lo cazó el typecheck
Renombré y dejé `FichaPaquete` como **alias deprecado** «para no romperle el
import a C». **Estaba mal, y el typecheck lo probó en el acto:** un alias
conserva el **NOMBRE**, no las **PROPS** — y en el rename cambiaron cinco
(`rotuloTamano→rotulo` · `precioPaquete→precio` · `onElegir→onCambio` ·
`elegido→encendido` · `clave` retirada). **El consumidor de C rompía igual.**

⇒ **El alias se retiró en el mismo commit que lo creó** y se migró la llamada.
*Un alias que no evita la migración no es compatibilidad: es deuda con buena
intención* — y encima habría pedido un trinquete para matarla después.

⚠️ **CRUCE DE TERRITORIO DECLARADO:** se tocó
`apps/prestador/src/app/guarderia/taller.tsx` (territorio de C) — **el import y
cinco nombres de prop, nada de lógica.** *Dejar `main` sin compilar era peor que
un cruce mecánico y declarado.*

### ③ EL ACORDEÓN — el censo dio PIEZA NUEVA, y con motivo
**`packages/ui` NO tenía acordeón**, y hay **seis implementaciones inline en
apps** (`hogar/grooming` · `hogar/bitacora` · `hogar/adiestramiento` ·
`hogar/index` · `despensa/checkout` · `citas/[mascotaId]`). Nace
**`SeccionPlegable`** con la anatomía **19.7** (sin caja · chevron `⌄`/`⌃` ·
target 44 · jamás `›`, porque no se navega).

**`PieRevelar` NO servía**, y lo dice su propia entrada: revela *el resto de una
lista* desde su **pie**; acá se pliega *una sección entera* desde su **título**.
**Y lleva `detalle`** («3 franjas», «desde $12») porque *si al cerrar no queda
señal de lo que hay adentro, plegar deja de ser ordenar y pasa a ser esconder.*

### ④ 🔴 EL SLIDER NO TENÍA UN PROBLEMA DE SENSIBILIDAD — TENÍA `onBegin`
**La causa, leída del código:** `.minDistance(0)` + `onBegin(irA(...))` ⇒ **el
precio se fijaba en el TOUCH-DOWN**, antes de que el dedo se moviera un píxel. Y
desplazar una pantalla larga **empieza** con un touch-down: si cae sobre el
riel, el precio ya cambió.

***No hacía falta arrastrar para romperlo: alcanzaba con apoyar el dedo.*** Por
eso **«bajarle la sensibilidad» no lo habría curado**: no había gesto que
suavizar — había un compromiso tomado antes de que hubiera gesto.

**La cura, dos gestos en `Race`:** **TAP** que pone el valor (deliberado, y se
cancela solo si el dedo se mueve) · **PAN** con `activeOffsetX([-12,12])` y
🔴 **`failOffsetY([-10,10])`, que es la mitad que no se puede omitir**: sin él un
arrastre mayormente vertical con algo de deriva lateral **igual activa el pan**.
*El umbral vertical es MÁS CHICO que el horizontal a propósito: ante la duda
gana el scroll, porque equivocarse hacia «no moví el precio» es gratis y hacia
«moví el precio» cuesta plata.* `.runOnJS(true)` se conserva en los dos — es el
contrato de la cura de S58 y lo que exime a la pieza de `R68`.

**Rige para TODOS los sliders de precio de la casa**, como se pidió.

**De los tres remedios, los otros dos:**
· **③ campo numérico — YA EXISTE y ya rige en toda la casa:** `edicionNumerica`
  **default `true`** (S68), con affordance visible (subrayado punteado + hint).
  *Confirmado, no reconstruido.*
· **② pasos más gruesos — NO ES DE LA PIEZA:** `pasos: string[]` lo pasa el
  consumidor. La granularidad vive en los arreglos `PASOS_*` de los talleres,
  **territorio de C**. *Se nombra en vez de simularlo acá.*

### ⑤ EL ESPEJO SIN DÍA SUELTO — confirmado, y apareció un borde nuevo
**Confirmado: omite la comparación** (`sin_comparacion`) en vez de inventar un
0 %. **Pero al volver `precioDiaSuelto` opcional apareció una TERCERA forma de
estar ausente —`undefined`— que el guard viejo no miraba**: seguía acertando
**porque `undefined > 0` da `false`**, o sea **acertaba por coerción, no por
diseño**. Es `L-424` con otra ropa. **Guard reescrito explícito**, ahora que el
caso *dejó de ser raro y pasó a ser común*.

### ⑥ LA BALDOSA — se agrega un caso, NO se cambia lo firmado
`LADO_GLIFO` era `48` por **N7** (*«48, no 32: es lo primero que se ve»*) — pero
**se firmó PARA DOS COLUMNAS**. A tres, la baldosa es ~2/3 de ancho *y también
de alto* (`aspectRatio: 1`), y el glifo le come el aire al label.

**El número se derivó, no se eligió a ojo:** 48 × 2/3 = **32**. Y aterriza en un
lugar elocuente: **32 es exactamente el valor que N7 rechazó** ⇒ *32 nunca fue
«chico»: era chico PARA DOS COLUMNAS.* Entra como `columnas?: 2 | 3`
(**default 2**, todo consumidor existente intacto) y **sólo modula el glifo** —
el ancho lo pone el contenedor.

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 60
reglas** (`R17`: 137 exportaciones · 128 en galería) · `verify:contrast`
**391 pares · 0 fallos**, sin pares nuevos.

---

## ⑭ LOS DOS GLIFOS NUEVOS — cuatro candidatos, gate por ícono PENDIENTE

**Dos candidatos por glifo** (molde `prime`/`primeCorona`: el founder elige a
21px y **el perdedor muere con su lápida**), montados en hoja de contacto **a
21 y 44 px, y contra los vecinos que el censo marcó como riesgo** (§6b/§2.9).

### 🔴 CERTIFICACIÓN — el veto se resuelve con una regla que la casa YA tenía
El pedido trae su propio veto: *«el error fácil es dibujar una medalla o un
diploma: eso acredita a la persona equivocada»*. **Y la salida estaba escrita**,
en la entrada de `documento` del registry:

> *«`carnet` es el de vacunas de la MASCOTA — **su huella sobre una cédula diría
> que el documento es del animal**»*

⇒ **La huella no es el adorno de este glifo: es lo que resuelve el veto.** Un
papel con la huella rellena encima dice *«la acreditada es ella»*; una medalla
dice *«alguien ganó algo»*. *No se inventó un criterio — se ejerció el que la
casa ya había escrito para el caso inverso.*

**Censo de objetos ocupados:** `documento` = identificación **con retrato** ·
`carnet` = la **placa** de vacunas · `copiar`/`descargar` = hojas de CONTROL sin
huella. **Ninguno acredita**, y prestarlos sería la sustitución genérica que la
Ley 12 prohíbe.

· **A `certificacion`** — papel + huella como sello. *Riesgo: a 21px los
  renglones pueden empastarse con el borde; si pasa, se van los renglones antes
  que el sello.*
· **B `certificacionSello`** — sello troquelado (anillo) con la huella adentro.
  🔴 *Riesgo, y es el que decide: **Ley 9 — a 21px la huella sobrevive o es
  ruido**. Dentro del anillo tiene menos aire. Si se empasta, gana A.*

### 🔴 WEARABLE — el censo mató al candidato obvio
Veto: *«jamás un corazón médico — un corazón dice consulta, y esto mide todo el
tiempo»*. **Y el candidato natural —la placa del collar— también cae, por
medición:** **`carnet` YA es una placa colgante y `paseo` YA es el lazo del
collar**; a 21px serían el mismo dibujo.

⇒ El objeto es **el DISPOSITIVO** (cuerpo redondeado + dos tramos de correa), y
**la huella va ADENTRO**: *el aparato está mostrando a la mascota, todo el
tiempo* — dice «continuo» **sin órgano y sin ondas**.

**Las ondas se descartaron por precedente, no por gusto:** `contactoOndas` murió
en su gate porque **§1 manda dibujar el OBJETO y «el alcance» es una idea**, y
encima chocaba con `ayuda` a 21px.

· **A `wearable`** — huella adentro. *Riesgo: la silueta se parece a un reloj;
  lo desambigua la huella, que es lo que un reloj no tiene.*
· **B `wearableActividad`** — traza de actividad adentro, huella afuera chica.
  🔴 *Riesgo, y es el veto mismo: **una traza puede leerse como ECG**, o sea
  clínica. Se dibuja igual porque es lo único que dice «mide EN EL TIEMPO»
  siendo objeto y no idea; si a 21px se lee médica, gana A.*

### LAS CAPAS NO SE ELIGIERON: SE LEYERON (L-166)
· **`wearable` → `identidad`** porque **la casa ya lo tenía declarado**:
  `themes/light.ts` dice `services.wearable: verdeVitalDark // Capa 1 ·
  monitoreo de vida`. *Razonarlo de nuevo era arriesgar contradecir una línea
  que ya existía.*
· **`certificacion` → `identidad`** anclado en su vecino: `documento` ya es
  identidad. *No es `cuidado` como `training`: el curso es el SERVICIO; el
  certificado es la CONDICIÓN que queda.*

**Consumidores:** `wearable` nace con dos — el hueco **M-WEAR** de la ficha de
mascota, **que espera desde S51**. *La regla de economía se cumple: no se pidió
un glifo que nadie va a montar.*

### GATES
`typecheck` ui **VERDE** · `verify:diseno` **VERDE, 60 reglas** ·
`verify:contrast` **391 pares · 0 fallos** (los glifos no traen pares nuevos: el
color lo resuelve el `registro` del consumidor).
🔴 **Gate por ícono PENDIENTE — es del founder, a 21px, y elige uno de cada
par.** Hasta entonces los cuatro conviven: *ninguno rige.*

---

## ⑮ RESIDUO PROPIO, CURADO — el borrado que la forma pathspec se comió
El commit del rename **creó `FichaDeOferta.tsx` y NO registró el borrado de
`FichaPaquete.tsx`**: la forma pathspec de la regla 84 commitea **sólo lo que se
le lista**, y listé el archivo nuevo y no el viejo.

**Consecuencia si pasaba:** `main` con **las dos piezas**, la vieja ya sin export
en `index.ts` ⇒ **código muerto que nada señala** — *exactamente la trampa
`D-645` que este parte viene declarando, cometida por su propio autor.*

**Lo destapó un `git merge` que falló por el índice sucio, no una revisión.**
Curado en `320bf589`.

---

## ⑯ ☠️ `FichaMensualidad` MUERE — nació y murió en la misma sesión

**Medido antes de decidir:** cero consumidores en `apps/` (sólo la nombra un
comentario). Su hermana `FichaFranja`, en cambio, **está viva en tres pantallas
—incluida la del cliente—**, o sea que *sí* cumplió la promesa de ser una pieza
para las dos caras.

### LA RAZÓN ES DE C, Y ES MEJOR QUE LA QUE YO LE HABÍA ESCRITO
Su comentario en el taller:

> *«Con `FichaMensualidad` su precio se veía **más grande** que el del día y el
> de los paquetes: son dos piezas distintas con dos jerarquías distintas,
> puestas una al lado de la otra. **Tres precios que se comparan tienen que
> pesar igual — el que se ve más grande se lee como el importante, y acá
> ninguno lo es.**»*

**Es diagnóstico, no preferencia:** `FichaMensualidad` montaba
`PrecioText registro="ficha"` y sus hermanas `"linea"`. *El defecto no se veía
en la pieza sola — sólo aparece al ponerla al lado de aquéllas con las que se
compara.*

### 🔴 Y UN SEGUNDO HALLAZGO, QUE ES SOBRE MÍ: `dias` NUNCA TUVO PRODUCTOR
Medido: **`dias_semana` existe sólo para las FRANJAS** (recogida/devolución), y
**el taller captura del mensual sólo un precio**. La prop `dias` («Lun–Vie») la
inventé **del ejemplo de la orden**, y el motor nunca modeló ese dato.

***Una prop que ningún dato podía llenar*** — el reverso exacto del cuidado que
este parte viene declarando («una prop que nadie llena no la ve ningún
typecheck»): allá el riesgo es que nadie la ejercite; **acá era que nadie
PUDIERA**.

### LO QUE NO SE HIZO, Y ES LA PARTE DISCIPLINADA
**NO se le agregó `detalle?` a `FichaDeOferta`** para heredar el patrón de días.
Hoy **nadie lo produce ni lo consume**, y construirlo sería repetir el error de
`dias` con otro nombre. **Disparo declarado:** el día que el perfil del lugar
muestre la oferta mensual con su cobertura, nace `detalle?` **y su campo en el
motor**, *en ese orden*.

**Lápida en `index.ts`** (Ley 37 + `D-645`: una pieza viva que nada señala es la
trampa que esta sesión ya pagó dos veces).

---

## ⑰ LA BALDOSA — la lección es de CONTRATO, y va contra mi propia decisión

**Lo que pasó:** C no le pasaba `columnas={3}` y el glifo seguía en 48. Ya está
curado del lado de C.

> **Cambiar el ancho del contenedor no achica lo de adentro: hay que
> DECÍRSELO a la pieza. Y un default que sirve al caso viejo no avisa cuando
> llega el nuevo.**

### 🔴 EL DEFAULT LO ELEGÍ YO, Y CON EL ARGUMENTO EQUIVOCADO
Escribí `columnas = 2` razonando *«default 2, todo consumidor existente
intacto»*. **Eso es exactamente lo que lo volvió silencioso:** un default
protege al consumidor VIEJO **al precio de callarle al NUEVO** — y el nuevo era
justamente el que necesitaba el valor distinto.

**Y el modo de falla es invisible:** un glifo grande de más no lanza, no rompe
el build y no sale en ningún gate. **Se ve, y sólo si alguien mira.**

### LA CASA YA TENÍA LA REGLA, Y YO NO LA APLIQUÉ (19.9)
> *«la prop de identidad va **OBLIGATORIA sin default** — `capa=` en
> `CabezalOficio` obligó a cada clon a declararla, y la primera consumidora real
> probó que sin ella el clon salía teal»* · *«`direccion` OBLIGATORIA sin
> default: el tsc obliga a decidirlo»*

**Es el mismo caso, y con el mismo desenlace**: la primera consumidora real
probó que sin declararla salía mal. *Otra vez el criterio estaba escrito y no lo
fui a buscar — ver `censo de criterios, no sólo de piezas`.*

### PROPUESTA — NO EJECUTADA, porque ripplea a dos territorios
**`columnas` debería ser OBLIGATORIA sin default.** Costo medido: **3 call
sites** (`cliente/despensa/index` · `prestador/atender` · `prestador/negocio`).
El cambio **rompe el typecheck a propósito**, que es el punto: *convierte un
defecto invisible en un error de compilación, y obliga a cada grilla a declarar
cuántas columnas tiene.*

**No se ejecuta acá:** son dos territorios ajenos y ya hice un cruce declarado
esta sesión. **La decisión es de la mesa; el trabajo, de quien tenga esas
pantallas.**

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 60
reglas** (`R17`: 136 exportaciones · 127 en galería — bajó una con la muerte) ·
`verify:contrast` **391 pares · 0 fallos**.

---

## ⑱ TANDA 6 — la firma de `columnas`, la ficha, y los glifos con el nombre de C

### ① `columnas` OBLIGATORIA — con dos correcciones a lo que yo mismo reporté

**⏪ Dije «3 call sites». Son SEIS**, y el error fue contar **archivos**:
`atender` ×3 · `negocio` ×3 · y `despensa/index`, que **importa `Baldosa` y no
la monta** (0 usos). *Medí con `grep -rln` —que lista archivos— y reporté como
si fueran llamadas.*

**⏪ Y la segunda corrección la hizo el propio rojo:** al volverla obligatoria
rompieron **8 en prestador y 3 en cliente**, o sea que el cliente también la
monta — por la galería, que ambos typechequean. *El rojo producido midió mejor
que mi grep.*

🔴 **Y una trampa de medición que vale anotar:** mi primer regex para listar los
call sites (`^[^(]+\(`) **se cortaba en el paréntesis de `(tabs)`** y me mostró
sólo la galería. *Un censo sobre rutas de expo-router tiene paréntesis adentro:
el patrón «hasta el primer `(`» miente.*

**Los valores se MIDIERON, no se declararon de memoria** — que es el punto de
una prop obligatoria: declararla mal es peor que el default, porque **ahora
parece decidida**.

| archivo | `ESTILO_CELDA` | ⇒ |
|---|---|---|
| `atender.tsx` | `width: '50%'` | `columnas={2}` (glifo 48, el firmado por N7) |
| `negocio.tsx` | `width: '33.333%'` | `columnas={3}` (glifo 32) |

**Cruce de territorio declarado:** `atender.tsx` y `negocio.tsx` (de C), sólo la
prop y su comentario. **En galería queda el PAR** —la misma baldosa a dos y a
tres— porque *si los dos se vieran igual, la prop no hace nada.*

### ② LA FICHA DE `dias` — **sin número, y eso es la enmienda ejercida**
`docs/relevamientos/S107-B-FICHA-prop-inventada-de-un-ejemplo.md`.

**Va sin número a propósito**, obedeciendo la enmienda firmada horas antes:
*«una ficha sin depositar no tiene número: las reservas fuera del canónico no
reservan»*. **La numera quien la deposite, releyendo el máximo en ese commit.**
Se declara además que **se lee como LECCIÓN (`L-`) y no como deuda**: no queda
nada abierto.

**Su contenido, en una línea:** *una prop nace de un DATO QUE ALGUIEN PRODUCE,
jamás del ejemplo con el que se pidió la pieza.* Y lo que la vuelve clase:
**pasa todos los gates de la casa** —compila, tiene demo, entra a `R17`— porque
la prop **se ejercitaba perfectamente**; lo que faltaba era un productor. *La
pregunta que la delata no la hace ningún instrumento: «¿qué wrapper, tabla o
pantalla produce este valor HOY?».*

### ③ LOS GLIFOS — nombre de C, y **su censo corrigió mi dibujo**
**Renombrados al plural que el pedido usa** (`certificaciones` ·
`certificacionesSello` · `wearables` · `wearablesActividad`). *No es capricho de
C: `seguros` ya es plural en esa misma lista de mundos.*

🔴 **Y su pedido traía un censo que yo no había hecho** — *«si el papel que
acredita ya tiene una forma acá, el tercero no la reinventa»*. Lo corrí, y el
registry lo dice más fuerte de lo que C sospechaba, en la entrada de `fiscal`:

> **«el idioma "rectángulo con renglones" ya está ocupado CINCO veces en este
> registry»**

**Mi candidato A dibujaba el sexto.** ⇒ **fuera los renglones**: el papel queda
con su esquina doblada y **el diferenciador pasa a ser el sello** — estructural,
como el **dentado** de `fiscal` o el **retrato** de `documento`.

**Y la cura arregló dos cosas de una:** los renglones eran también **mi propio
riesgo declarado a 21px** (*«pueden empastarse con el borde»*). *Sacarlos
despeja la colisión y el tamaño chico en el mismo trazo.*

**Lo que C hace cuando entren:** dos `push` en `explorar/index.tsx` y sus dos
voces. **«Próximamente» pasa de tres entradas a cinco.**

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 60
reglas** · `verify:contrast` **391 pares · 0 fallos**.
🔴 **Sigue pendiente el gate por ícono del founder**, ahora sobre los dibujos
corregidos.

---

## ⑲ EL LABEL A TRES COLUMNAS — se PARTE, no se trunca

**La medición de C:** útil = `(ancho−16)/3 − 51` = **73,7 pt en 390**;
«Adiestramiento» ≈ **116 a cuerpo · 102 a sm · 80 a xs**. Es **una palabra de 14
caracteres**, así que `numberOfLines={2}` sola no alcanza: *no hay dónde
cortar*. «Veterinaria» también se pasa en 360 y 390.

### 🔴 LA ELIPSIS SE DESCARTA POR PRECEDENTE, NO POR GUSTO
Antes de elegir, se midió si la casa ya había juzgado esto. **Sí: `D-576`
registra «"Adiestramiento" trunca» como DEFECTO medido por el founder en
dispositivo (S80), y sigue 🟠 abierta.**

⇒ *Elegir elipsis sería reproducir, en otra pantalla, un defecto que él ya
rechazó **sobre esta misma palabra**.* **La palabra se parte y no se corta:
nada se pierde.**

### EL MODELO, DERIVADO DE LOS TRES NÚMEROS DE C — y se cruzan solos
`116/(14·16) = 0,518` · `102/(14·14) = 0,520` · `80/(14·11) = 0,519`
⇒ **ancho ≈ 0,519 × caracteres × tamaño**. *Tres datos que caen en el mismo
coeficiente no son tres datos: son un modelo.*

### LAS DOS PALANCAS, LAS DOS DERIVADAS
· **Escala:** `cuerpo` (16) → **`enfasis`** (14 **bold**). **No `apoyo`**: ahí el
  título quedaría del mismo peso que el `detalle` y se aplanaría la jerarquía.
  `enfasis` está descrito como *«el elemento destacado de una lista, que no es
  un rótulo»* — que es exactamente lo que un título de baldosa es.
· **Aire:** `spacing[4]` (16) → **`spacing[2.5]` (10)**, por **la misma
  derivación que el glifo**: la baldosa a tres es ~2/3 de ancho, y 16×2/3 ≈ 10.
  *Ni el glifo ni el aire se eligen a ojo — los dos salen de la misma
  proporción.* Devuelve **12 pt** de ancho útil.

### EL RESULTADO CONTRA LOS CINCO LABELS REALES
Útil = `(ancho−16)/3 − 39` · bold ≈ +5 %

| label | @14 bold | 360 (75,7) | 390 (85,7) | 430 (99,0) |
|---|---|---|---|---|
| Paseo | 38 | ✓ | ✓ | ✓ |
| Grooming | 61 | ✓ | ✓ | ✓ |
| Guardería | 69 | ✓ | ✓ | ✓ |
| Veterinaria | 84 | **2 líneas** | ✓ | ✓ |
| Adiestramiento | 107 | **2 líneas** | **2 líneas** | ✓ |

**Ninguno trunca en ningún tamaño**, y el peor caso —14 caracteres— entra
holgado en dos renglones.

### LO QUE SOSTIENE LA LEGIBILIDAD CUANDO SE PARTE
**El glifo.** Ley 12: *el glifo dice SERVICIO* — cuando «Adiestramiento» se
parte en dos renglones, **el silbato sigue diciendo cuál mundo es**. *Por eso
partir es aceptable y truncar no: partir reparte la palabra entre dos renglones
y deja el glifo intacto; truncar borra el final y deja al glifo solo.*

⚠️ **El modelo es una ESTIMACIÓN de ancho de glifo, no una medición de
render.** La vara final es el aparato — por eso la galería monta **los cinco
mundos reales, a dos y a tres columnas**, con sus labels tal cual los escribe el
riel. *No se midió contra el más corto.*

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 60
reglas** · `verify:contrast` **391 pares · 0 fallos**.

---

## ⑳ 🔴 LA CURA DEL LABEL NO ALCANZÓ — y el aparato encontró lo que mi modelo no podía ver

**El founder lo vio en dispositivo, que es la vara que yo mismo había
declarado.** Dos defectos, y el segundo es enteramente mío.

### LO QUE MI MODELO NO MEDÍA (y por eso no era suficiente)
El modelo `0,519 × caracteres × tamaño` **predijo bien el ANCHO** y fue **mudo
en las dos cosas que rompieron**:
· **dónde cae el corte** — predice que entra en dos renglones, no que el
  segundo empiece en «ento»;
· **el ALTO** — no dice nada de él, y el alto era el que estaba mal.

*Un modelo que contesta una pregunta se lee como si contestara la vecina.*

### ① EL CORTE A MITAD DE SÍLABA — «Adiestrami / ento»
**Tiene razón: un corte que deja «Adiestrami» no es mejor que truncar.** Y **la
pieza no puede arreglarlo**: RN no hifena español (Android tiene
`android_hyphenationFrequency`, **iOS no expone equivalente**), y **la pieza no
sabe silabificar** — dónde parte una palabra es **propiedad del IDIOMA**.

🔴 **Y NO se le inventó una prop para esto**, que era la tentación: sería el
error que esta misma sesión fichó — *una prop nace de un dato que alguien
produce, jamás del ejemplo con el que se pidió la pieza.* **El dato existe y ya
tiene dueño: es la cadena.**

⇒ **Guion blando (`­`) en el label**, pedido autocontenido a C en
`docs/loop/S107-B-PEDIDO-A-C-GUION-BLANDO.md`. **Montado en galería el roster
CON y SIN guion, lado a lado**, para que se vea que la pieza ya lo respeta y que
lo que cambia es el texto.

### ② EL GLIFO ENCIMA DEL TEXTO — mío, por partida doble
**No era ancho: era ALTO.** Dos causas, las dos mías:

· **El glifo estaba `position: absolute`** ⇒ **fuera del flujo, sin reservar
  altura**, y el texto crece desde abajo. *Nada impedía que subiera hasta
  debajo del glifo: no había espacio reservado, había un hueco que casualmente
  alcanzaba a dos columnas.*
· **`aspectRatio: 1` era el TERCER número firmado PARA DOS COLUMNAS** —después
  del glifo (48) y del aire (16)—, **y es el único que no re-derivé.** *Curé
  los dos que se veían y dejé justo el que causaba el choque.*

**La cuenta, en el peor caso (360):** baldosa `(360−16)/3 − 8 = 106,7` ·
demanda `20 + 32 + 6 + 40 + 6 + 20 = 124` ⇒ **faltan ~17 pt, y en un cuadrado no
hay de dónde sacarlos.**

**Las dos curas:**
· **El glifo vuelve al FLUJO** y un espaciador (`flex: 1`) sostiene la
  alineación firmada. ⇒ **el choque es imposible por construcción**, no porque
  las cuentas den. *Cuando el contenido crece, lo primero que cede es el
  espaciador — jamás el glifo.*
· **A tres columnas la baldosa deja de ser cuadrada** (`0.8`): alto ≈133 en 360
  y ≈146 en 390, con holgura sobre 124. **A dos sigue CUADRADA, intacta.**

### ⚠️ EL CASO DEL CLIENTE **NO ES MI PIEZA**, y se dice en vez de dejar que se descubra
Medido: **`apps/cliente/.../explorar/index.tsx` dibuja su grilla A MANO** — su
único `Baldosa` es *una mención en un comentario*. ⇒ **la cura de altura no
alcanza a «Estética y baño»**, y seguirá partiendo igual.

*Es la tercera baldosa a mano de la casa, y su divergencia es la que 19.9
predice.* Montar `Baldosa` ahí es trabajo real y **es decisión, no arreglo** —
queda declarado, no ejecutado.

### LA LECCIÓN, QUE ES LA DE HOY
**Tres números de esta pieza estaban firmados para dos columnas —glifo, aire y
proporción— y los tres se aplicaron a tres sin re-derivarse.** Yo corregí los
dos primeros *porque se veían en el ancho*, y **el tercero era el que rompía**.

> **Cuando un contenedor cambia de tamaño, no hay UN número que re-derivar: hay
> que censar TODOS los que se firmaron contra el tamaño viejo.** *Curar los que
> se ven deja vivo al que no se ve — y ése es el que aparece en el aparato.*

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 60
reglas** · `verify:contrast` **391 pares · 0 fallos**.
🔴 **La vara es el aparato**: la galería monta los cinco labels reales a dos y a
tres columnas, y el roster con y sin guion blando.

---

## ㉑ LA MEDICIÓN CONTRA EL RENDER — instrumento entregado, resultado NO CONCLUYENTE

**Se pidió medir contra el render, no contra el modelo. Se intentó de verdad y
no se pudo terminar** — se declara en vez de entregar un verde de otra fuente.

### LO QUE SE CONSTRUYÓ
`scripts/verify-baldosa-tres-columnas-s107b.mjs` — playwright contra `:8081`
(el instrumento web de la casa), a **360 · 390 · 430**, con **los cinco labels
reales y el glifo puesto**, midiendo las tres cosas que el modelo no veía:
**① si el texto pisa el glifo** (solape de cajas del DOM) · **② si el contenido
se sale de la baldosa** · **③ en cuántos renglones cae cada label**.

### 🔴 POR QUÉ NO SE PUDO TERMINAR, con su diagnóstico
1. **La extensión de Chrome no está conectada** ⇒ sin browser tools; se fue por
   playwright, que es lo que usan los `verify-*-web-*`.
2. **Sin `.env.local` la app monta el gate de entorno** — y la página **responde
   HTTP 200 igual** (`bodyLen` 106). *Es exactamente el caso que el control
   positivo existe para atrapar, y lo atrapó: **NO CONCLUYENTE en los tres
   anchos**, jamás un verde.*
3. **Con el env puesto, `/gallery` redirige al LOGIN.** ⇒ **B no tipea
   contraseñas** —tampoco las de una cuenta demo local— así que la corrida es de
   quien tenga sesión. **La copia del `.env.local` se borró al terminar**: no se
   dejan credenciales duplicadas en disco por una medición que no se pudo hacer.

**El comando queda en la cabecera del script.** *Se entrega el instrumento con
su resultado real, no con uno fabricado.*

### LO QUE SÍ SE PUEDE AFIRMAR SIN EL RENDER, y es más fuerte que una medición
**El solape dejó de ser una cuestión de números.** Con el glifo **de vuelta en
el FLUJO**, el texto **no puede** ocupar su lugar: no hay caja absoluta que
invadir. ⇒ **la ausencia de solape es una propiedad de la construcción, no un
resultado que dependa de que las cuentas den** (L-222 otra vez: el estado malo
se vuelve inexpresable).

**Lo que sigue dependiendo del aparato** —y por eso el instrumento queda— es si
la baldosa a `0.8` se ve bien, y **dónde cae el corte de «Adiestramiento»**, que
no lo arregla la geometría sino el guion blando en la cadena.

### EL CHEVRON — MEDIDO: no es prop mía
`Baldosa` **no dibuja chevron**. La única mención en su fuente es un comentario
que argumenta **por qué una baldosa no es una fila**: *«una fila con un chevron
dice "hay más adentro"; una baldosa dice…»*. `negocio.tsx` tampoco lo dibuja.
⇒ **No hay prop que retirar ni cambio que hacer en la pieza** — lo que C saque
vive en su consumidor. *Y la firma del founder coincide con lo que la cabecera
de la pieza ya argumentaba.*

---

## ㉒ D-973 — LA COMPARACIÓN MONTADA (preparar la decisión, no tomarla)

En galería, **tres filas con LOS MISMOS cinco labels**, para que *lo único que
varíe sea la anatomía*: `Baldosa` a **dos** columnas (el caso firmado por N7) ·
`Baldosa` a **tres** · y **la baldosa a mano del cliente**, reproducida fiel.

### 🔴 UNA CORRECCIÓN DE PREMISA, Y CAMBIA LA PREGUNTA
La ficha dice *«es la **tercera** superficie de la casa que dibuja una baldosa
por su cuenta»*. **Medido con tres criterios distintos, es UNA.**

| criterio | resultado |
|---|---|
| `aspectRatio` + `flexWrap`, sin `<Baldosa>` | **1** — `cliente/explorar` |
| celdas `50%`/`33%` con glifo, sin `<Baldosa>` | **0** |
| `flexWrap` + `<Icono>`, sin `<Baldosa>` | 8 archivos, **1 es rejilla de cuadrados**; los demás son chips y filas |

**Y el candidato que parecía la segunda NO lo es:** `cliente/despensa` monta
**`TarjetaProducto`**, no una baldosa a mano. *Lo que confunde es que su
comentario CITA la cabecera de `Baldosa` —como argumento sobre otra pieza— y una
mención se lee como un uso.*

⇒ **La pregunta de la mesa cambia de forma.** Con tres superficies divergentes,
el caso sería *«hay tres verdades y ninguna firma»* y empujaría a variantes o a
una pieza hermana. **Con una sola**, y encima **desviada a propósito por firma
del founder**, la pregunta honesta es la que la propia ficha ya formulaba mejor:
***¿esta superficie es una baldosa, o sólo se le parece?***

*Se declara contra qué se midió: si un cuarto criterio encuentra otra, la
premisa vuelve a moverse.*

### LO QUE LA COMPARACIÓN PONE A LA VISTA
| | `Baldosa` (pieza) | el cliente, a mano |
|---|---|---|
| superficie | propia, `radius.lg` + elevación + **CANTO de capa** | `Tarjeta` — **sin canto** |
| glifo | 48 (2 col) / 32 (3 col), **en el flujo** | **26**, con `paddingTop` |
| proporción | 1 · **0.8** a tres | **1.05** |
| título | `Texto` (`cuerpo`/`enfasis`) | **`Text` a mano**: `sans.medium` + `sm` |
| detalle | sí | **no** — retirado por firma |
| chevron | **nunca tuvo** | **no** — retirado por firma |
| celda | `50%` / `33.333%` | `flexBasis: 31%` + `flexGrow: 0` |

**Dos de las siete diferencias son firmas del founder** (detalle y chevron) y
**una no es diferencia**: `Baldosa` nunca tuvo chevron — su cabecera argumenta
*por qué una baldosa no es una fila*. ⇒ **de las siete, cinco son decisiones
abiertas y una ya coincide.**

### ⚠️ LA REPRODUCCIÓN ES UNA COPIA, Y PUEDE ENVEJECER
Se transcribió del `fichasActivas.map` de `explorar/index.tsx`. **No se importó
porque no hay qué importar: vive inline dentro de un `.map`** — *y ése es
exactamente el hecho que `D-973` pone sobre la mesa.* **Si esa pantalla cambia y
la copia no, la comparación compara contra una ficción**, que es lo único que
arruinaría el instrumento. Queda dicho en el código, al lado de la copia.

### DE PASO, MEDIDO: EL GUION BLANDO YA ESTÁ APLICADO
`cliente/es.ts` **3** cadenas y `prestador/es.ts` **2** ya llevan `U+00AD`
(`Adiestra­miento` · `Vete­ri­na­ria` · `Esté­tica y baño`). ⇒ **C ejecutó el
pedido en las DOS apps** y el corte a mitad de sílaba está curado del lado de la
cadena. *Confirmado contra el objeto, no dado por hecho.*

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 60
reglas** · `verify:contrast` **391 pares · 0 fallos**.
🔴 **La decisión NO se toma acá.** Queda montada para la sesión de diseño, con
su vara: *ninguna de las tres se juzga sola.*

---

## ㉓ EL GATE DE LA BALDOSA A TRES — subtítulo fuera, caja medida, estado en color

### ① EL SUBTÍTULO SALE **A TRES COLUMNAS**, no de la pieza — y el alcance lo decidió la medición
La orden decía *«la baldosa queda con glifo y nombre, nada más»*. **Medido antes
de aplicarlo: `detalle` lo pasan `negocio` ×7 y `atender` ×2**, y **sólo UNO es
el conteo de la grilla de mundos**. *Borrar la prop de la pieza se habría
llevado el **dato del día** de `atender` —a DOS columnas— y seis baldosas de
Negocio que el gate no miró.*

⇒ **`detalle` deja de EXISTIR a tres columnas, por tipo**, y sigue intacto a
dos. La firma fue sobre la grilla de tres; el alcance se respeta.

### ② LA CAJA: una ALTURA MEDIDA, no otra proporción
`0.8` estaba dimensionado para alojar el subtítulo — **el mismo error que ya
caché acá dos veces** (glifo 48 · aire 16), *pero esta vez el caso no cambió de
tamaño: **desapareció**.*

🔴 **Y no se re-derivó otra proporción, porque la proporción es la herramienta
equivocada:** `aspectRatio` hace crecer el alto con el ancho, y este contenido
es de **alto FIJO** (un glifo y dos renglones). Un ratio afinado en 360 regala
~23 pt de aire muerto en 430. *«Ni un píxel más» describe una ALTURA, no una
razón.*

`10 + 32 (glifo) + 6 + 40 (2 renglones × 20) + 10` ⇒ **98**, y va como
**`minHeight`**: con altura fija, agrandar el tipo del sistema **CLIPA** — que es
el defecto que acabamos de curar. Con mínimo crece; y como el contenido es igual
en las cinco, **la grilla queda pareja igual**.

### ③ EL ESTADO EN COLOR — y la trampa cerrada por construcción
· **Se atenúa el GLIFO** (`0.45`, no un gris tímido: con el subtítulo afuera el
  color quedó como **único** portador) · **el canto va al TINTE de su capa** —
  mismo hue, menos intensidad; **no a gris**, porque el canto dice CATEGORÍA y
  un gris borraría de qué mundo es · **el NOMBRE no se toca**: el texto jamás
  baja de contraste, y por eso el `opacity` no lo alcanza.

**La trampa que la forma evita:** un gris de *disabled* sobre toda la baldosa
diría *«no podés entrar»* — *y es al revés: ésa es justo la que hay que tocar.*

🔴 **Y el estado viaja en la etiqueta accesible POR TIPO:**
`{ configurado: false; vozSinConfigurar: string }` — **sin la voz no compila.**
*Un estado que sólo existe en el píxel es un estado que para alguien no existe.*
La prop **nace obligatoria sin default** (19.9, y la lección que esta pieza ya
pagó con `columnas`).

### 🔴 DOS CONSECUENCIAS QUE SE DECLARAN EN VEZ DE ABSORBERSE
1. **Se perdió el estado de FALLO DE CARGA.** `conteoDeMundo` tenía una rama
   `fallos → «No cargó»`, y **con el subtítulo afuera ese aviso no tiene dónde
   vivir**: hoy la baldosa no distingue *«no pude leer tu conteo»* de *«todo
   bien»*. **Es Ley 13 en su forma incómoda — un error que deja de decirse.**
   *No se le inventó lugar: la firma fue «glifo y nombre», y meterle un tercer
   color sería decidir por la mesa.*
2. **Dos DESCRIPCIONES cayeron de rebote** (tienda vitrina · tienda local): su
   `detalle` no era un conteo sino copy explicativo, y sale por la regla de tres
   columnas. **Queda marcado en el código para que la mesa lo confirme o lo
   devuelva** — *borrar copy en silencio es peor que dejarlo de más.*

### LO QUE MURIÓ CON SU LÁPIDA
**`conteoDeMundo`** — mi cambio le quitó el último consumidor, así que **sale en
el mismo acto** (Ley 37 · `D-645`). ✅ **Su criterio NO se perdió, se absorbió:**
su propio comentario decía *«`null` NO ES CERO: durante la carga la baldosa no
dice nada en vez de afirmar "Sin configurar"»*, y por eso el estado nuevo se
deriva de **`cargado && n === 0`** — *el apagado se enciende sólo cuando está
MEDIDO en cero, jamás mientras carga.*

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 60
reglas** · `verify:contrast` **391 pares · 0 fallos** (el nombre no cambió de
color; el `opacity` del glifo no entra al gate porque no es texto).
🔴 En galería, **guardería va sin configurar entre sus cuatro hermanas**: es el
par que decide si el apagado se lee «todavía no lo usás» o «no podés entrar».

---

## ㉔ LAS DOS CONSECUENCIAS, DECIDIDAS POR LA MESA

### ① EL FALLO DE CARGA QUEDA NEUTRO — y ya lo estaba, pero por la razón equivocada
**Firma:** *una baldosa que FALLÓ AL LEER no se pinta apagada; queda neutra, como
durante la carga.*

**Medido antes de tocar, y el resultado sorprende para bien:** los cuatro estados
nacen `null` y **en un fallo el setter NUNCA se llama** (`if (r.ok) setX(...)
else caidos.add(...)`), así que quedan en `null`, `n` da `null`, y
`cargado && n === 0` **ya daba falso**. ⇒ **la firma se cumplía sin tocar una
línea.**

🔴 **Y aun así se tocó, porque se cumplía por la razón equivocada.** Se cumplía
**por cómo cae la cadena de derivación**, no por un guard que alguien pueda leer
y confirmar. *Un futuro `ofertas ?? []` —una cura razonable del estado de carga—
lo volvería `0`, y **los fallos empezarían a pintarse «sin configurar» en
silencio**.* Es `L-424` exacto: **un guard que acierta por derivación no es un
guard.**

⇒ La exclusión **se DICE**: `cargado && !fallos.has(bloque) && n === 0`. Hoy es
redundante; **su trabajo es no serlo mañana.** *La condición de la mesa quedó
escrita donde se decide, no inferida de tres archivos.*

### ② LAS DOS DESCRIPCIONES DE TIENDA — NO SE BORRÓ NADA
**Verificado contra el objeto:** `negocio.tiendaVitrinaDetalle` y
`negocio.tiendaLocalDetalle` **siguen vivas en `es.ts` y en `en.ts`** (2 y 2).
Lo único que pasó es que **dejaron de renderizarse**, forzado por la regla de
tres columnas — *no se borró una sola cadena.*

⚠️ **Y quedaron sin uso, que es un estado peligroso por prolijidad ajena:** una
clave huérfana invita a barrerla. **Se marcó en el código, nombrándolas**, con la
orden de la mesa citada: *«el founder decide y te aviso; no las borres mientras
tanto»*. **No son basura que limpiar: son una decisión esperando firma.**

### LA LECCIÓN QUE LA MESA RATIFICÓ, para el registro
> **`aspectRatio` hace crecer el alto CON el ancho. Si el contenido es de alto
> FIJO, la proporción es la herramienta equivocada** — un ratio afinado en la
> pantalla angosta regala aire muerto en la ancha. **Y va como `minHeight`, no
> como `height`: agrandar el tipo del sistema no puede CLIPAR**, que es
> justamente el defecto que se venía de curar.

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 60
reglas** · `verify:contrast` **391 pares · 0 fallos**.

---

## ㉕ `FilaCita` Y LA GUARDERÍA — el censo dijo que NO hace falta una prop

**El pedido traía la pregunta bien planteada** —*¿hueco de datos, o el oficio no
cabe?*— y la respuesta salió de medir el cuerpo de la pieza, no de suponerla.

### LO QUE FALTABA ERA UNA PALABRA, Y LA PIEZA YA DECÍA DÓNDE
`FilaCitaOficio` es un **vocabulario CERRADO de cuatro** y no incluía guardería.
**Y su propia cabecera manda qué hacer:** *«Cero API que permita romper la ley;
**si la taxonomía crece, cambia ACÁ y todas las pantallas heredan**»*.

⇒ `| 'guarderia'` en la unión. **Y el color no se tocó:** el canto resuelve
`veterinaria ? identidad : cuidado`, y guardería **es CUIDADO** (Ley 10, como
sus tres hermanas — *las separa el glifo, no el canto*), así que cae bien sola.

### 🔴 NO ES UN HUECO DE DATOS — `metadataMono` NUNCA EXIGIÓ UNA HORA
Medido: es **string libre y opcional**. *La estadía pasa su DÍA ahí y ya.* **No
hay campo que agregar** ⇒ **no nace ninguna prop**, ni obligatoria ni de otro
tipo. *Inventarle una habría sido el error que esta sesión ya fichó: una prop
nacida del enunciado del pedido y no de un dato que falte.*

### ⚠️ LO QUE **NO** ENTRA, Y NO ES DEFECTO DE LA PIEZA: LAS DOS VENTANAS
`Celda` pinta `metadataMono` **a la derecha, en mono, minúsculas forzadas y
`size.sm`** — el lugar de *«17:30 · 45 min»*. **«Recoge 7:00–9:00 · Devuelve
16:30–18:30» no es metadata de fila: es CONTENIDO**, y ya tiene su pieza
(`FichaFranja`) y su lugar (**el despliegue**, que esta fila soporta desde
siempre con `direccion="abajo"` — y que 19.9 reserva para *prestador + costo*).

*Forzarlo al slot derecho sería meter una frase donde va un dato* — y habría
«resuelto» el pedido dejando la fila peor que con `Celda`.

### ⚠️ UNA TRAMPA LATENTE, DECLARADA Y NO CURADA
El canto es un **ternario de DOS ramas sobre una taxonomía de CUATRO
categorías**. Con cinco oficios sigue siendo correcto —los cinco son salud o
cuidado—, **pero el día que entre uno de COMUNIDAD o CONSUMO se pintará cuidado
en silencio.** No se reestructura en este pedido: **se nombra con su disparo.**

### 🔴 EL VOCABULARIO CERRADO COBRÓ SU DIVIDENDO — y lo cazó el hook, no yo
Agregar un miembro **rompió el typecheck en un lugar que yo no había mirado**:
`historico.tsx` compone su clave de i18n con el oficio
(`historico.oficio_${oficio}`), así que apareció `historico.oficio_guarderia` —
**inexistente en el diccionario**. *Ése es exactamente el trabajo de un
vocabulario cerrado y de un riel de claves tipadas: **el quinto oficio no puede
entrar a medias**.* **El commit lo frenó el hook de pre-commit, no una
revisión mía.**

**Cruce declarado:** se agregó la clave en `es.ts` y `en.ts` del prestador.
**La palabra no se inventó** — es la que ese mismo riel ya usa para el oficio
(`negocio.mundoGuarderia` = «Guardería»), en la forma exacta de sus cuatro
hermanas, y en inglés siguiendo su patrón (`Walking`/`Grooming`/`Training` ⇒
**`Daycare`**). ⚠️ **Igual es VOZ: va al lote que el founder lee aparte.**

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 60
reglas** · `verify:contrast` **391 pares · 0 fallos** (cero pares nuevos: el
canto reusa `capa.cuidado`, que ya se mide).
En galería, **la fila de guardería va contra sus hermanas**: qué decide es que
su canto se lea igual y que **el día solo, sin hora, no deje la fila coja**.

---

## ㉖ D-973 LISTA PARA GATEAR · Y EL APAGADO **NO PASA** EL PISO GRÁFICO EN CLARO

### ① LA COMPARACIÓN, RE-COTEJADA CONTRA LA FUENTE
**El riesgo que declaré se activó y se atrapó a tiempo:** el bloque del cliente
**se movió** (`183 → 200`) y **ganó guardería**. ⇒ **la copia se re-cotejó campo
por campo y SIGUE FIEL**: `aspectRatio: 1.05` · `paddingTop: spacing[1]` ·
glifo **26** · `sans.medium`+`sm`+`numberOfLines={2}` · `flexBasis: '31%'` con
`flexGrow: 0`.

**Y se curó el puntero:** la advertencia ahora cita **el bloque
(`fichasActivas.map`) y no un número de línea** — *el número envejeció mientras
la copia seguía siendo cierta, o sea que el número era el dato frágil.*

### ② 🔴 EL APAGADO — Y LO PRIMERO ES QUE `verify:contrast` **NO LO MIDE**
Aquél recorre **pares de TOKENS declarados**; el apagado es **un `opacity` en
runtime** más un canto cambiado de token. **Ninguno es un par registrado** ⇒
*«391 pares, 0 fallos» no dice nada sobre el apagado.* **Decir que pasó el gate
habría sido un verde de otra fuente.** Se computó aparte
(`scripts/verify-baldosa-apagada-s107b.ts`), **reusando la matemática de
`verify-contrast.ts`** — su propio historial registra a un autor calculando la
luminancia en lineal y obteniendo *números cómodos y falsos*.

**El resultado, y el veredicto está ACOTADO a las capas que los cinco mundos
usan de verdad** (`cuidado` ×4 + `identidad` ×1 — medido del consumidor):

| tema | capa | glifo @0.45 | piso | alfa mínimo | canto |
|---|---|---|---|---|---|
| claro | identidad | **2.06** ✗ | 3 | 0.65 | 1.09 |
| claro | cuidado | **2.02** ✗ | 3 | 0.67 | 1.09 |
| oscuro | identidad | 3.14 ✓ | 3 | 0.45 | 1.32 |
| oscuro | cuidado | 3.28 ✓ | 3 | 0.45 | 1.34 |

⇒ **En CLARO el glifo apagado no llega al piso gráfico; en oscuro sí, y justo.**

### 🔴 Y LA TENSIÓN QUE EL NÚMERO DESTAPA — el canal está agotado
Para llegar a 3:1 en claro hace falta **α ≈ 0.67**. *Un glifo al 67 % ya casi no
se ve apagado* ⇒ **no se puede tener «claramente atenuado» Y ≥3:1 con opacidad
sobre estos colores.** Es la forma exacta del hallazgo de `sobreVideo`:
***cuando un canal se agota, no se insiste con más dosis — se cambia de
canal.***

**Y el canto está peor: 1.09.** El tinte de capa sobre `bg.card` es
**prácticamente invisible** ⇒ *el canto deja de decir CATEGORÍA justo en el
estado donde el prestador tiene que reconocer qué mundo configurar*, que era la
razón por la que se eligió el tinte en vez del gris.

**NO se cambia por mi cuenta:** la forma la firmó la mesa (*atenuar el glifo,
canto más suave, nombre pleno*) y **el founder lo va a juzgar por ojo**. Lo que
esta medición aporta es que **hay un rojo esperándolo en claro**, y las salidas
posibles ya con número: subir α a ~0.67 (pierde el efecto) · cambiar de canal
(p. ej. el glifo al registro `tinta` en vez de a menos alfa) · o declarar el
glifo apagado como decorativo —**el nombre, a contraste pleno, ya identifica el
mundo**— y eximirlo del piso, que es una decisión de mesa y no mía.

### LO QUE EL INSTRUMENTO **NO** DICE
Si el apagado se LEE como *«todavía no lo usás»* y no como *«deshabilitado»*.
**Eso es ojo del founder** — acá sólo se descarta que haya un rojo esperándolo.
*(Y las dos capas `[sin mundo]` se miden y se marcan, pero **no cuentan al
veredicto**: hoy ningún mundo las usa. Entran el día que la trampa del ternario
de `FilaCita` traiga un oficio de comunidad o consumo.)*

---

## ㉗ 🔴 EL PATH IMPRESO COMO TEXTO — dos defectos míos, y la regla que los caza

### EL CENSO — 9 consumidores, 7 bien, 2 rotos y los DOS míos
`CHEVRON` es el mapa de paths `d`. Medido: **7 lo alimentan a un `<Path>`**
(`Boton` · `FichaMascotaHogar` · `AccionQueLleva` · `PuertaHermana` ·
`FilaCita` · `CeldaNavegacion` · `PieRevelar`) y **2 lo metían en un
`<Texto>`** — `SeccionPlegable` y `SemaforoSanitario`, **las dos escritas por
mí en esta sesión**.

⇒ **La pantalla imprimía `M9 18l6-6-6-6` al usuario**, y en `SeccionPlegable`
eso pasaba **en cada acordeón del taller que el founder está usando**.

**Curadas con la PIEZA**, que es lo que `chevron.tsx` ya mandaba en prosa:
*«el path no se exporta; se usa la pieza»*. Ahora montan `<Chevron direccion=…/>`.

### 🔴 POR QUÉ NINGÚN GATE LO VEÍA — y por qué la regla valía más que la cura
**Un path en posición de texto es un `string` puesto donde va un `string`.**
Compila, pasa lint, pasa `verify:diseno` y pasa cualquier typecheck **por
construcción**: *el tipo es correcto; lo que está mal es el SIGNIFICADO.* Es la
familia de `L-192` — **el modo de falla no es un error, es una salida creíble.**

**Nace `R70`** (`verify:diseno`, 61 reglas): `CHEVRON` sólo puede alimentar el
`d` de un `<Path>` —forma directa o por variable intermedia—; cualquier otra
aparición es el defecto. **Mecaniza una frase que hasta hoy era prosa.**

**Dos cosas que la regla se corrigió a sí misma, y las dos en su primera
corrida:**
· **Se acusaba a sí misma:** su primer rojo fue `chevron.tsx`, *el archivo donde
  el mapa se define*. Falso positivo ⇒ el archivo que lo declara queda exento.
· **Sus números de línea mentían:** el `sinComentarios` compartido **borra los
  bloques enteros y corre la numeración**, así que el mensaje apuntaba a una
  línea equivocada. *Un guard que señala mal el lugar manda a buscar donde no
  está.* ⇒ despoja **preservando los saltos**.

**Alcance declarado:** vigila `CHEVRON`, el único mapa de paths con nombre de la
casa. **La CLASE es más ancha** —cualquier constante de path impresa— y
ensancharla pide un censo de constantes que hoy no existe. *Se cierra lo medido.*

### ¿EXPLICA EL SÍNTOMA DEL FOUNDER? — sí, y la anatomía está MEDIDA
Su reporte: *«el botón de cargar carnet no se entiende como tocable»*. **La
anatomía que pidió ya estaba entera, verificada línea por línea:**

| lo pedido | medido en `SemaforoSanitario` |
|---|---|
| la fila tapea entera | `Pressable` envolviendo el cuerpo (`:196`) |
| se anuncia como control | `accessibilityRole="button"` (`:198`) |
| target 44 | `minHeight: 44` (`:209`) |
| chevron sólo donde hay camino | `{falta ? <Chevron/> : null}` (`:177`) |
| el camino en la voz | `etiquetaResolver` al label (`:202`) |

⇒ **Lo único que fallaba era la affordance: en vez del chevron se dibujaba
`M9 18l6-6-6-6`.** *La fila era tocable y lo anunciaba, pero el signo visual de
que llevaba a algún lado estaba reemplazado por ruido* — y eso explica
exactamente «no se entiende como tocable».

⚠️ **Y se dice honesto: es una explicación COMPLETA de lo medido, no una
verificación en el aparato.** Si con la cura sigue sin leerse tocable, los
candidatos que quedan **no son de anatomía** sino de énfasis —la fila no tiene
superficie propia ni fondo, sólo el chevron y el pressed—; eso sería craft y
pide su gate, no otra cura a ciegas.

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 61
reglas** (`R70`: 7 usos en 7 archivos · **0 en posición de texto**) ·
`verify:contrast` **391 pares · 0 fallos**.

---

## ㉘ LAS TRES FIRMAS — tres frentes cerrados

### ① «La vitrina» y «Muy pronto» — MUERTAS, y era MI condición de muerte
`tiendaVitrinaDetalle` y `tiendaLocalDetalle` **borradas de `es.ts` y `en.ts`**,
con su lápida en el riel. *Este parte las había dejado vivas «esperando firma»;
la firma llegó y por eso se borran — **una clave que sobrevive a su decisión
deja de ser reserva y pasa a ser basura con coartada**.*

⏪ **Y el comentario que las protegía quedó VIEJO: se marca, no se reescribe.**
Decía *«no las borres mientras tanto»*; dejarlo tal cual **mandaría a la próxima
pista a proteger de un borrado que ya se firmó.**

### ② El glifo apagado — el instrumento DEJA DE SER GATE
Firmado: se ve bien en el aparato y queda como está. ⇒
`verify-baldosa-apagada-s107b.ts` **sale siempre en 0 y reporta sus números**.

🔴 **Por qué se degrada en vez de dejarlo en rojo, que era lo cómodo:** *un gate
que queda rojo POR DECISIÓN enseña a ignorar el rojo* — y el día que aparezca
uno de verdad ahí, nadie lo mira. **Un instrumento cuyo rojo ya no significa
«hay que arreglar algo» no es un gate: es ruido con autoridad.**

**El dato NO cambió y queda impreso:** en claro el glifo a 0.45 rinde
**2.02–2.06** contra un piso gráfico de 3, y el founder lo miró y lo aceptó.
*Escrito para que nadie lo re-descubra como si fuera nuevo.*

### ③ Los cuatro glifos — ganaron los dos primarios, y **no se dedujo: se midió**
La firma decía *«los que están puestos quedan»*, y eso tenía **una única lectura
contra el objeto**: `explorar/index.tsx` monta `icono: 'wearables' |
'certificaciones'` — **exactamente uno por par**. *No se adivinó cuál ganaba:
se leyó del consumidor.*

☠️ **`certificacionesSello` y `wearablesActividad` MUERTOS**, con su lápida en el
registry — *un candidato que sobrevive a su gate es un dibujo que el próximo va
a encontrar sin saber que ya perdió*. **Y la lápida dice qué perdió cada uno**,
para que no se re-dibuje igual: el sello troquelado (Ley 9, poco aire a 21 px) y
la traza de actividad (leía a ECG, o sea clínica — el veto mismo).

**D-973 cierra sin cambio:** la baldosa a mano del cliente se queda, `Baldosa` no
gana variantes. ⚠️ **Su ficha vive en `DEUDAS_CANONICAS`, territorio de A** —
queda declarado que su condición de muerte (*decisión firmada*) ya se cumplió.

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 61
reglas** · `verify:contrast` **391 pares · 0 fallos**.

---

## ㉙ EL RELLENO DEL SEMÁFORO (freno) · Y EL CENSO DE LA FAMILIA DE R70

### ① EL PEDIDO DEL RELLENO **NO LLEGÓ** — freno 76b, con su medición
Se buscó de verdad antes de frenar:
· **cero** archivos `S107-C-PEDIDO-A-B-*` sobre relleno o padding;
· **cero** `paddingHorizontal` cerca de los dos montajes de `SemaforoSanitario`;
· el único `paddingHorizontal` de esa pantalla es del **rebote del CTA**
  (`[prestadorId].tsx:555`), con su propio comentario y ajeno a la pieza.

**Y lo que C sí declaró del lado del consumidor es otra cosa, y está bien
puesta:** envuelve la pieza en `<Tarjeta>` porque *«`SemaforoSanitario` no
expone superficie — como `FichaFranja`, la decide quien la monta»*. **Eso no es
andamio: es la frontera que las dos piezas ya tenían.**

⇒ **No se ejecuta sobre un literal que no llegó** (76b · L-142). *El principio
que la conductora enuncia es correcto —ese número sería de la pieza— y por eso
mismo conviene verlo escrito antes de moverlo: un relleno movido «de memoria»
en una pieza con dos consumidores se paga en las dos.*

### ② LA FAMILIA DE R70 — **SÍ existe, y mi primer censo la perdió**
**Primera pasada, por NAMESPACES** (`palette`, `typography`, `spacing`,
`radius`, `motion`, `gradients`, `opacity`, `elevacion`, `sobreVideo`…):
**391 `.tsx`, 88 ocurrencias miradas, 3 sospechosos — y los tres de la
GALERÍA**, donde imprimir el valor del token **es** el contenido. *Habría
cerrado con «el chevron era el único caso».*

🔴 **Y habría estado mal, porque el censo buscaba la forma equivocada:** éstas
**no son namespaces, son constantes sueltas**. Buscando por VALOR —lo que
empieza con un comando de path— aparecieron **`ISOTIPO_PATH`**, **`GOTA_D`**,
**`CHISPA`** y **`BLOB`**. *Un censo que busca la forma equivocada devuelve cero,
y ese cero se lee como «no hay».* **Es la lección del control positivo otra
vez, un piso más arriba: no bastaba con probar que la página vivía — había que
probar que la CONSULTA miraba la forma correcta.**

⇒ **Hay familia y valía la regla.** `R70` se ensancha: **DERIVA SU CORPUS** en
vez de llevar una lista — *una lista escrita a mano no sabe del path que se
declare mañana, y el defecto de hoy nació justamente de una constante que esa
lista no habría tenido.* Hoy deriva **5**: `CHEVRON · CHISPA · GOTA_D · BLOB ·
ISOTIPO_PATH`, **20 usos en 14 archivos, 0 en posición de texto.**

**Y se cerró un punto ciego que el propio ensanche destapó:** los paths también
viven en `.ts` (`gota.ts`), y la regla recibía sólo `.tsx` ⇒ **`GOTA_D` no
entraba al corpus y un uso suyo como texto habría pasado EN VERDE.** Ahora
recibe el listado de código completo.

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 61
reglas** · `verify:contrast` **391 pares · 0 fallos**.

---

## ㉚ LA GEOMETRÍA DEL SEMÁFORO — el número era de la pieza, y C tenía razón

### LA MEDICIÓN DE C, RE-VERIFICADA CONTRA EL OBJETO
No se aceptó de palabra. Leído de la fuente:
· `CeldaNavegacion` = **`paddingHorizontal: 12` · `paddingVertical: 8` ·
  `minHeight: ALTURA_MIN` (56)** — exactamente lo que su pedido decía;
· mi `Fila` = `paddingVertical: 12`, `minHeight: 44`, **sin horizontal**;
· 🔴 **y el brazo «al día» no tenía `minHeight` y el tocable sí** ⇒ *las dos
  filas de la misma lista medían distinto.*
· los dos andamios existen, con su comentario declarado y su retiro escrito.

**Su número —~92 contra ~60, 53 % más alta— se sostiene.**

### 🔴 POR QUÉ EL NÚMERO ES DE LA PIEZA — su §5 es el argumento y es correcto
> *«El padding de la fila no es superficie: es la fila.»*

**La pieza no expone SUPERFICIE a propósito** —igual que `FichaFranja`— **y eso
sigue bien**. Pero sin padding horizontal **sólo se puede montar dentro de un
contenedor que la padee**, y el contenedor que la casa usa para listas de filas
es justamente `Tarjeta relleno="ninguno"`, que **no padea**. *Una pieza que sólo
funciona con el contenedor que su propio caso no usa le está pasando su
geometría al consumidor — y cada consumidor la resuelve distinto.* Hoy dos
montajes; iban a ser más.

⇒ Los tres números entran a la pieza, **y son los de `CeldaNavegacion` leídos de
su fuente**, no elegidos acá.

### Y LA DIVERGENCIA SE CURÓ POR CONSTRUCCIÓN, NO REPITIENDO
Los tres van en **una constante compartida** (`GEOMETRIA_FILA`) que usan los dos
brazos. *Repetir tres propiedades en dos lugares es cómo se separaron; con una
constante no pueden volver a divergir.* **Efecto de paso:** con el padding
adentro, el resalte de presión **corre a sangre** como en toda lista de celdas —
antes quedaba inset, *y ese inset era justo lo que delataba al andamio.*

### LOS DOS ANDAMIOS, RETIRADOS EN LA MISMA TANDA
C los dejó pre-declarados (*«cuando B lo mueva, este `View` se retira»*), y **se
retiran acá y no después a propósito**: con el padding puesto en los dos lados
la fila quedaría en **24**. *Un andamio que sobrevive un rato a su obra no es
neutro: dobla el número que vino a arreglar.* Quedan sus lápidas, y el
comentario que los declaraba **se marca en vez de borrarse**.

⚠️ **Cruce de territorio declarado** en las dos pantallas de C: sólo el
envoltorio y su comentario, cero lógica.

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 62
reglas** · `verify:contrast` **391 pares · 0 fallos**.

---

## ㉛ `SelectorDia` — una causa POR DÍA, y la vieja se vuelve inexpresable

### EL DEFECTO, MEDIDO CONTRA LA PIEZA
`cerrados` era un **`Set` con UNA `etiquetaCerrado` para todos**. Conviven dos
causas —*«no abre ese día»* y *«ya la reservaste»*— y con una sola cadena
**sólo se podía decir el neutro**. *Dos hechos distintos contados con la misma
palabra.*

🔴 **Y midiendo apareció por qué importa más de lo que parece:** el día cerrado
**es MUDO por diseño** —se ve sólo como una opacidad de `0.18`— así que
`etiquetaCerrado` se consume **únicamente en el `accessibilityLabel`**. ⇒ *quien
usa lector de pantalla oía «cerrado» sobre un día que él mismo había
reservado.* **No era un matiz de copy: era el único canal donde ese día podía
explicarse.**

### LA FORMA ELEGIDA — `Map` de `iso → motivo`, y por qué no las otras
· **`Set` + etiqueta por función/opcional** — deja **el caso viejo vivo para
  quien no se acuerde**, que es el patrón que esta sesión ya pagó dos veces
  (`columnas`, `dias`).
· **`Map`** — **«cerrado sin porqué» pasa a ser INEXPRESABLE**: una entrada
  siempre trae su valor. Y **dos props se vuelven una**. ☠️ `etiquetaCerrado`
  muere (Ley 37): medido, **cero apariciones vivas**.

🔴 **La pieza no aprende qué es «no abre»** — sigue recibiendo el texto de quien
la monta. **Lo único que cambió es la granularidad:** de uno para todos, a uno
por día.

### LA MIGRACIÓN — 9 sitios, y PRESERVANDO LA VOZ DE HOY
Cada consumidor arma su mapa **con la misma cadena que ya pasaba**
(`[...cerradosISO].map((iso): [string, string] => [iso, t(…)])`), así que **hoy
se ve y se oye exactamente igual**. *El cambio abre la capacidad sin mover una
sola palabra* — y el seam queda declarado en cada sitio: *el día que esa
pantalla distinga las dos causas, sólo cambia el valor de esa entrada.*

⚠️ **Cruce de territorio declarado** en 7 pantallas de C y 2 del prestador:
sólo la construcción del mapa y el retiro de la prop muerta, cero lógica.
*Cambiar el tipo sin migrar habría dejado `main` sin compilar.*

**En galería van los DOS motivos distintos** — es el par que muestra lo que
antes no se podía decir.

### GATES
`typecheck` ui + cliente + prestador **VERDE** · `verify:diseno` **VERDE, 62
reglas** · `verify:contrast` **391 pares · 0 fallos**.

---

# 🔴 TRASPASO — LO QUE UNA SUCESORA NECESITA (escrito antes de compactar)

*Lo que vive sólo en la conversación muere con ella. Esto es el estado, no el
relato: lo de arriba es la historia y se lee si hace falta; **esto se lee
primero.***

## ⓪ IDENTIDAD Y TERRITORIO
**Pista B de S107.** Worktree `../e-petplace-s107-b`, rama **`pista/s107-b`**.
**Territorio (76h): `packages/ui/**`, `scripts/verify-*` de diseño, y este
parte.** Todo cruce a `apps/` que hice está **declarado en su commit** y fue
siempre mecánico (props, imports, retiro de andamios) — **nunca lógica**.

## ① ESTADO CONTRA EL OBJETO — medido, no recordado

🔴 **ESTAS TRES LÍNEAS SON DERIVADAS: se re-miden al leerlas, jamás se citan.**
Los comandos, para que nadie tenga que inventarlos:
`git log --oneline origin/main..HEAD` · `git ls-remote origin pista/s107-b` ·
`git status --porcelain`.

· **3 commits sin mergear a `main`** (`45535583` · `abc52209` · `cae9f7eb`).
· **NO es fast-forward** — traje `main` a mi rama varias veces. *Esto no
  envejece: ya pasó.*
· **Árbol limpio · `origin` = local = `cae9f7eb`**, cotejado por SHA.

⚠️ **Y esta sección ya se cobró sola, el mismo día que nació:** decía *1 commit*
y *`origin` una atrás* — cierto al escribirla, falso dos commits después. *Un
traspaso que guarda un número derivado le miente a la sucesora con la voz de un
documento escrito para que no tenga que recordar* (L-141, en el archivo que
existe justamente para no depender de la memoria). **Por eso ahora declara el
COMANDO además del número.**

## ② LO ÚNICO ABIERTO — y nada de esto lo puede avanzar B solo
🔴 **Todo lo pendiente es GATE DEL FOUNDER**, sobre cosas ya construidas:
1. **La comparación de `D-973`** — tres superficies montadas en galería.
   ⚠️ **La baldosa del cliente ahí es una COPIA inline** de
   `explorar/index.tsx` (bloque `fichasActivas.map`). **Se re-cotejó el 30-ago
   y seguía fiel**, pero *puede envejecer*: quien toque aquélla, mira ésta.
2. **La baldosa a tres columnas** — hay instrumento listo,
   `scripts/verify-baldosa-tres-columnas-s107b.mjs`. 🔴 **Nunca se pudo correr:
   `/gallery` vive detrás del login y B no tipea contraseñas.** Su cabecera
   trae el comando y el diagnóstico completo.
3. **Que el apagado se lea «todavía no lo usás» y no «deshabilitado»** — el
   founder ya lo firmó en el aparato; el dato de contraste queda **declarado,
   no pendiente** (`verify-baldosa-apagada-s107b.ts` **informa, no gatea**).

## ③ SUPUESTOS VIVOS — se re-verifican antes de usar
· **`LETRA_GUARDERIA` sigue FRENADA** (`D-918`/`D-919`). Todas mis piezas
  respetan el §0: **ninguna trae texto adentro; todo rótulo entra por prop.**
  *Si la mesa reescribe §3, las piezas no cambian de forma — cambia qué textos
  les manda la app, que era el punto.*
· **Los números de línea de cualquier cita envejecen** — el del bloque del
  cliente ya lo hizo (183→200). **Citar el bloque, no la línea.**

## ④ LO QUE NO SE HACE, aunque parezca prolijo
· **`negocio.tiendaVitrinaDetalle` / `tiendaLocalDetalle` YA NO EXISTEN** —
  murieron por firma con su lápida. *No revivirlas.*
· **Las claves de i18n sin uso NO se barren sin leer su lápida.**
· **`certificacionesSello` y `wearablesActividad` están MUERTOS** por gate. Sus
  lápidas dicen **qué perdió cada uno**: no se re-dibujan igual.

## ⑤ LA REGLA QUE NACIÓ ACÁ Y HAY QUE NO ROMPER
**`R70`** (`verify:diseno`, 62 reglas) — *un path SVG no va en posición de
texto*. **Deriva su corpus del objeto** (hoy 5 constantes): **no se le pone una
lista a mano** — el defecto que la parió nació de una constante que una lista no
habría tenido.

## ⑥ EL PATRÓN QUE MÁS SE REPITIÓ, por si sirve de vara
**Cinco veces en la sesión el defecto fue el mismo: algo correcto por TIPO
puesto donde el significado era otro, o un guard que acertaba por derivación.**
`columnas` con default · `dias` sin productor · el path como texto · el fallo de
carga excluido por casualidad · el censo que buscó la forma equivocada.
**Ninguno lo encontró una relectura: los encontró un gate, un typecheck o el
founder mirando.** ⇒ *correr los gates antes de reportar, no después.*

---

# 📌 LOS LITERALES DE LAS FIRMAS — depositados antes de compactar

🔴 **Por qué existe esta sección:** todo lo de arriba cita las firmas **en mi
paráfrasis**. Sus palabras exactas vivían sólo en la conversación, y una
compactación las convierte en *mi resumen de lo que se autorizó* — que es
exactamente la fuente que `L-142` no acepta. **Se transcriben acá mientras
todavía se tienen.**

⚠️ **Marca de procedencia, y es parte del dato:** llegaron **relayadas por la
conductora**, no leídas por B de un acta. *Un verbatim de segunda mano se
declara como tal; quien las necesite como fuente, las coteja contra la mesa.*

## Sobre las piezas
> **La ficha expandible:** *«el fondo pasa de magenta a VERDE SUAVE, en todas las
> fichas que usen ese componente. El magenta es color de marca y ahí no
> corresponde.»*
> *«pared de fondo blanco, etiqueta a la izquierda, toggle a la derecha — y el
> toggle es el que ENCIENDE Y EXPANDE en el mismo acto. Un solo gesto que dice
> las dos cosas.»*

> **El slider:** *«BAJAR LA SENSIBILIDAD, y vale para todos los sliders de
> precio de la casa… Es una pieza que decide plata: que se mueva cuando se
> quiere mover.»*

## Sobre la baldosa
> *«FUERA EL SUBTÍTULO. La baldosa queda con glifo y nombre del servicio, nada
> más.»*
> *«El aspectRatio 0.8 estaba dimensionado para alojar el subtítulo… re-derivalo
> MEDIDO para que entren glifo y nombre en DOS RENGLONES y ni un píxel más.»*
> *«apagado NO puede leerse como deshabilitado. Esa baldosa es exactamente la
> que el prestador tiene que tocar… Un gris de «disabled» le dice que no puede
> entrar.»* · *«atenuar el GLIFO, que es lo decorativo, y dejar el NOMBRE en su
> color pleno; el canto de capa, más suave.»* · *«el estado tiene que viajar
> también en la etiqueta accesible, porque un lector de pantalla no ve el
> color.»*
> **Sobre el label:** *«a tres columnas el label baja de escala Y admite corte
> de palabra»* · *«Medí contra el render, no contra el modelo de ancho»* · *«El
> caso que decide es «Adiestramiento» con glifo, no la palabra sola.»*
> **El fallo de carga:** *«Una baldosa que FALLÓ AL LEER no se pinta apagada —
> queda neutra, como durante la carga… null no es cero.»*

## Sobre los glifos
> *«Los cuatro glifos, la baldosa a tres columnas y D-973: los que están puestos
> quedan. El perdedor de cada par muere con su lápida y D-973 se cierra sin
> cambio.»*
> **Sus dos vetos, del pedido de C (ése SÍ está en el repo):** *«NO es una
> medalla de negocio ni un diploma de profesional… acredita a la persona
> equivocada»* · *«JAMÁS un corazón médico. Un corazón dice consulta… esto es lo
> contrario: algo puesto encima que mide todo el tiempo.»*

## Sobre las cadenas y el gate
> *«"La vitrina" y "Muy pronto": los textos SE VAN.»*
> *«El glifo apagado se ve bien en el aparato: queda como está. Tu medición de
> contraste queda como dato declarado, no como pendiente de cura.»*

## Sobre el método
> **La galería:** *«el gate visual va sobre la app real cuando C monte — la
> galería vale como consumidor mínimo del typecheck, no como sala de revisión.
> El founder corrige en el aparato.»*
> **La enmienda de numeración, FIRMADA:** *«El número se toma en el acto de
> depositar en el archivo canónico: releer el máximo y escribir el depósito son
> UN solo acto, en el mismo commit — quien deposita re-verifica ahí, no antes.
> Una ficha sin depositar no tiene número: las reservas fuera del canónico no
> reservan. Todo censo de numeración excluye el parte propio, y D-999 no es
> ficha.»*
> **La prop obligatoria:** *«Tres call sites en territorio ajeno es barato contra
> un defecto invisible: que rompa el typecheck es el punto.»*
