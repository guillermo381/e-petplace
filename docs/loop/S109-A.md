# S109-A · EL COBRO REAL DE LOS CUATRO SUJETOS — parte de pista

> **Se lee ANTES que cualquier backlog.** Continúa `docs/loop/S108-A.md`, que
> tiene el censo de apertura y el arco de guardería. Este cubre lo que siguió.
>
> **Regla que rige acá:** hasta el **30-sep-2026** todo es ambiente de pruebas y
> ninguna fila pagada es plata real ⇒ **cero backfill**: el comportamiento cambia
> hacia adelante y lo viejo queda donde está.

---

## ① EL ESTADO, en una línea

**Los CUATRO comprables nacen `pendiente` y ninguno otorga antes de que la plata
entre.** El XOR tiene **siete sujetos**, los siete catalogados, y el actuador y
el reverso los mueven. **Ninguna llave está encendida** — medido: `guarderia_recurrente_vivo=false` · `cierre_cuenta_vivo=false` · `pagos_actuador_vivo=true` (preexistente, es el actuador).

| sujeto | tabla | nace pendiente | hold | confirma | reverso | compuerta |
|---|---|---|---|---|---|---|
| bono guardería | `bonos` | ✅ | 15 min | ✅ | ✅ | n/a |
| paquete paseo | `bonos` (mismo) | ✅ | 15 min | ✅ | ✅ | n/a |
| programa | `programas_contratados` | ✅ | 15 min | ✅ | ✅ | ✅ ensaya |
| plan paseo | `suscripciones_servicio` | ✅ | 15 min | ✅ | ✅ | ✅ ensaya |
| mensualidad guardería | `guarderia_suscripciones` | n/a (mandato) | — | ✅ | ✅ | ✅ (B) |

---

## ①bis LO QUE CERRÓ DESPUÉS — el riel del plan y el reloj del link

> **Enmienda del 07-09 (migración `20260907140000`).** Se agrega, no se reescribe.

Al ir a confirmar los tres ítems del founder (① `contratar_plan_paseo` nace
pendiente · ② la rama del CASE por riel · ③ la máquina del link) **el objeto dijo
que ② y ③ estaban a medias**, y el censo destapó **cuatro** cosas:

1. 🔴 **El riel estaba en la tabla y no viajaba al que decide.**
   `planes_vencidos_pendientes` barría TODO plan y estampaba
   `proveedor='nuvei' forma='tokenizacion'`. **El vecino ya tenía la rama** —
   `mensualidades_vencidas_pendientes` frena con `riel_no_cobrable_por_token`.
   *El patrón se copia, no se reinventa.* **Curado: el riel viaja en
   `para_cobrar`, y lo que no puede cobrarse por token se PIDE por link.**
2. 🔴 **La máquina del link no tenía ni un llamador ni un reloj.** Censo:
   `emitir_link_mensual` con **CERO** llamadores en la base y **CERO** en el repo;
   `vencer_links_mensuales` sin cron. `L-318` otra vez.
   **Curado: el selector la llama, y nace el cron `vencer-links-mensuales 30 8 * * *`.**
3. 🔴 **Los dos rieles iban a pedir DOS PRECIOS distintos por el mismo mes** (la
   tarjeta con crédito fresco, el link con un snapshot sin crédito).
   **Curado: el monto sale del DESGLOSE — una sola cuenta — y sin desglose
   congelado el emisor falla cerrado (`sin_desglose_congelado`).**
4. 🔴 **El riel tiene TRES estados y el tercero no se adivina** — lo destapó el
   propio cinturón al abortar contra `chk_susc_riel_valido`. El único plan vivo
   tiene `riel` NULL **y** `tarjeta_id` NULL. **Curado: `riel_no_declarado`,
   fail-closed.** ⇒ **`L-445`**.

**CINTURÓN VERDE, 4 brazos + el reloj**, sobre el plan REAL en subtransacción que
se deshace sola; **residuo verificado en 0** y el plan intacto.
⚠️ **NO EJERCIDO Y DECLARADO:** la emisión feliz del link — `guarderia_recurrente_vivo`
está en `false` **y es del founder**. *Fabricar la llave en un arnés sería simular
el veredicto que la llave existe para dar.*

⚠️ **CONSECUENCIA DECLARADA, reversible con una línea:** el único plan vivo deja de
renovar hasta que alguien declare su `riel`. **Hoy renovaría cobrando mal.** Cero
backfill: no se le rellenó el riel a nadie.

🔴 **Y LO QUE SIGUE SIN LLAMADOR, con dueño: `marcar_link_mensual_pagado`.** Su
llamador natural es el **webhook de DeUna**, que es de **B**. Sin él, un link se
paga y la máquina no se entera.

---

## ①ter LA CORRECCIÓN DEL FOUNDER SOBRE DEUNA — y lo que dejó sin efecto

> **Medida contra el proveedor, 07-09. Cambia el alcance, no la firma.**

🔴 **DEUNA NO EMITE LINKS.** El «link mensual» es **NUESTRO**: una ruta de la app
que abre la pantalla donde se genera **el código DeUna de 6 dígitos**, el mismo
mecanismo ya desplegado para las compras sueltas. **No hay integración nueva.**

**QUEDA SIN EFECTO** todo lo previsto sobre generar links por API, su vida, su
referencia y su regeneración — y también **la separación entre el aviso y el
link**: una ruta propia no caduca, así que **viajan juntos en el mismo correo**.

**LOS DOS RELOJES, y no se mezclan:**
· **el CÓDIGO** vence con la ventana de siempre de DeUna (`codigo_expira_en`);
· **el MES** vence al terminar el período pagado ⇒ el plan **no renueva y queda
  REACTIVABLE**, jamás cancelado (`cobro_link_mensual.vence_en`).

**Lo que falta del cron, cuando se toque: en la rama DeUna NO COBRA** — marca el
mes pendiente y dispara el correo con el link a esa ruta. **Dueño: S109-B.**

### La medición que el founder pidió, y salió peor que la pregunta

Pedía verificar que pedir cinco códigos **no cree cinco intentos**. Medido entre
A y B: **`clave_idempotencia = 'deuna:<sujeto>:<ref>'` con la `ref` generada por
pedido** ⇒ nunca colisiona ⇒ **la constraint existe y el mecanismo no**. Y el
freno de intento-en-vuelo **vivía en UNA de las cinco ramas**. ⇒ **`L-446`**.

**LA LETRA QUE RIGE (firmada por esta pista, a pedido de B):** el código es un
**ATRIBUTO del intento**, no un intento. Código vivo ⇒ se devuelve el MISMO;
código vencido ⇒ **se acuña uno nuevo SOBRE EL MISMO intento**; intento terminal
⇒ uno nuevo, que **no es duplicado sino segundo intento tras un desenlace real**.
Clave: `deuna:<tipo>:<sujeto_id>[:<periodo>]`, con **`ON CONFLICT DO UPDATE`**.
*Frenar cuando el código venció deja a la familia sin poder pagar un mes que sí
quiere pagar.* **Construcción: S109-B, edge sin desplegar (v15).**

---

## ①quater LA SEXTA PUERTA CIERRA EL ÚLTIMO GATE ROJO

> Enmienda del 07-09 (S109-B, `ec56b9cf`). **Se agrega, no se reescribe.**

**`suscripcion_servicio` ganó su puerta** —`{tipo:'plan'}` + `suscripcion_servicio_id`
en la edge, monto de `precio_mensual` **sin exigir desglose en el primer cobro**
(precedente de la mensualidad), compuerta **antes** de mover plata—.
⇒ **`verify-compuertas-por-sujeto` pasa a VERDE por la CURA**, no por ajuste del
gate. Era el único gate rojo de la casa.

**Dos decisiones que quedaron declaradas y NO resueltas:**
· 🔴 **La pertenencia del plan es del CONTRATANTE, no de la familia** —
  `suscripciones_servicio` no tiene `familia_id`. Fail-closed, con precedente
  firmado: **`D-981`** (*ensanchar quién paga por quién no es una equivalencia,
  es una decisión*). **La asimetría con el bono —que SÍ es del hogar por firma—
  queda declarada. Ensancharla es del founder.**
· 🔴 **`ON CONFLICT DO UPDATE` en la clave de DeUna sigue pendiente.** El reuso
  hoy se resuelve leyendo antes de insertar, **y eso es una carrera**. Lo agudo:
  **arreglar la clave volvió la colisión ALCANZABLE** —con `…:<ref>` era
  imposible— y su modo de falla es un `23505` crudo en la cara de quien paga
  (`L-424`). **Dueño: S109-B.**

🔴 **LAS DOS EDGES ESTÁN ESCRITAS Y SIN DESPLEGAR** — `pagos-cobro` **v30**,
`pagos-deuna-solicitud` **v15**. B espera confirmación del founder y **hace bien**:
*un peer no traslada una firma, traslada la noticia de que existe.*
⚠️ **Consecuencia para la superficie:** `checkout.simuladoAviso` puede retirarse
—su último consumidor cae con esta puerta— **pero no antes del deploy**, o la
pantalla promete un cobro que la edge arriba no sabe recibir.

---

## ①quinquies LOS GATES DEL OTA, FIRMADOS — y el número deja de elegirse

> Firma del founder, 07-09. **Se agrega, no se reescribe.**

🔴 **ANTES DE PUBLICAR CORREN LOS TRES, y ninguno pasa a verde por ajuste del
gate:** `verify:frenos` · `verify:migraciones` · `verify-selectores-recurrentes`.
`verify:frenos` deja de ser cinturón de pista y **entra a los verdes del OTA**.

**`pnpm proximo:migracion`** — el número **se pide, no se elige**. Máximo entre
directorio y ledger, más un paso.
· **La medición que lo justifica:** de las migraciones del repo, el **99 % tiene
  los segundos en `00`** y el **98 % los minutos redondos**. *Dos pistas no
  chocan por mala suerte: chocan porque eligen del mismo cajón.*
· ⚠️ **Su punto ciego vive EN el comando, no en un documento:** un número que
  otra pista **escribió y todavía no aplicó** no está ni en el directorio ni en
  el ledger. Sólo lo cierra avisarle a esa pista. *Las cinco colisiones del arco
  fueron contra números ya APLICADOS ⇒ el comando las habría cazado a las cinco.*

**Y `verify:frenos` (`scripts/verify-frenos-por-sujeto.mjs`) tiene dos brazos:**
① el guard de intento-en-vuelo alcanza la `columna_intento` de cada sujeto
cobrable — **no exige un guard por sujeto**, porque el guard vive una vez en la
puerta y exigir seis daría rojo sobre código correcto ·
② **el censo de CONSTRAINTS**: cada rama satisface los CHECK de pareja
`(a IS NULL) = (b IS NULL)` que su fila va a tocar, leídos de `pg_constraint`.
*Un censo de ramas no ve un campo que falta; un censo de constraints sí.*
**Probado que discrimina sobre los defectos REALES:** `714881c8^` rojo en los
dos brazos · `067acacb^` —el estado exacto del 500 de B— rojo en ② · hoy verde.

---

## ①sexies LOS SEIS SUJETOS COBRAN — los números del cierre

> Ejercidos por S109-B contra `pagos-cobro` **v33**, leídos del objeto.
> **`verify:frenos` corrido ANTES del deploy: verde los dos brazos.**

| sujeto | cobro real | comprobante |
|---|---|---|
| plan de paseo | `DF-2108362` · `DF-2108373` · $138 · `acto2=true` | «Plan mensual de paseos» ✅ |
| bono guardería | `364` · `367` · `370` | ✅ |
| bono paseo | `366` · `368` · `371` | «Paquete de 5 salidas de paseo» ✅ |
| mensualidad | `372` | ✅ |
| programa | `DF-2108283` · $160 | «Programa de 6 sesiones» ⚠️ **de v32** |

🔴 **EL PROGRAMA NO SE RE-MIDIÓ CONTRA v33, y se declara en vez de concluirse.**
Su verde es de **v32**. B no pudo repetirlo: los cinco perros del hogar de prueba
tienen programa vivo, y liberar uno falló porque **hay tres mascotas llamadas
«Kira»** y un `limit(1)` por nombre toma la que no es — `L-443` otra vez.
> *El delta v32→v33 es el período del PLAN y no puede tocar la rama del programa.
> **Pero eso es un razonamiento, no una medición**, y la instrucción era re-medir.*

**EL DOBLE TOQUE, caso fabricado — ninguno agrega un segundo intento:**
bono·guardería · bono·paseo · mensualidad · plan → `200` y después
**`409 pago_en_proceso`**, con 1 intento en los dos momentos. Programa → `409
sesiones_no_agendables` las dos veces, **0 intentos, y eso es lo correcto**.

🔴 **Y el arnés se corrigió ANTES de acusar al producto.** Su primera aserción era
`intentos === 1` y **daba rojo sobre el programa**, cuya compuerta había rebotado
**antes de cobrar**. *Confundía «el guard funcionó» con «el cobro ocurrió», y
acusaba al producto de un acierto.* **La pregunta correcta no es «¿hay exactamente
uno?» sino «¿el segundo toque AGREGÓ uno?»** — se compara contra el estado propio,
que es lo único que el segundo toque puede mover.

⚠️ **`cita` y `pedido` NO ejercidos, declarado:** su creación exige el recorrido de
reserva y el de carrito. *El guard los cubre por construcción — y «por
construcción» es la clase de afirmación que esta sesión falsó cinco veces.*

---

## ①septies LO ÚLTIMO ABIERTO, CON DUEÑO — el plan de paseo por DeUna

> Medido entre S109-B y S109-C, **sin empezar**.

🔴 **NO son dos líneas: es la rama entera.** `pagos-cobro` tiene **0 ocurrencias**
de `suscripcion_servicio_id` en su pata DeUna, y `SujetoDeuna` no tiene `plan`.

🟢 **Del lado motor ya está todo:** `cobro_link_mensual` tiene
`suscripcion_servicio_id` en su XOR y `emitir_link_mensual` conoce
`'suscripcion_servicio'`. **El destino existe; falta la puerta.**

⚠️ **Y viaja con la pantalla del link de C, que hoy sólo resuelve guardería.
Las dos piezas juntas, o se emite un sujeto sin destino** — la forma exacta que
esta sesión pagó cinco veces. **Dueño compartido: S109-B + S109-C.**

**Y por qué la fila de DeUna se apagó por SUJETO y no por categoría** (cura de C,
y su razón es la que hay que conservar): *no puede ser «porque es recurrente» —
la mensualidad de guardería también lo es y SÍ se paga por DeUna.* Apagar por
categoría habría sido el mismo `ELSE` que capturó de más cuatro veces hoy, en la
superficie en vez de en el motor.

---

## ⓪ LO PRIMERO DE S110 — el hueco que hoy son pruebas y el 30-sep es plata

> **No es deuda de fondo. Es el primer ítem.**

**`pagos_aprobados_sin_sujeto_movido()`** lista los intentos **`aprobado` cuyo
sujeto NO se movió**: **plata tomada, nada entregado.** Su primera corrida:

| | |
|---|---|
| el barrido **cubre** | **6** |
| **fuera de su alcance** | **37 · USD 1.490,39** |

🔴 **El hueco es SEIS VECES lo cubierto, y hasta hoy no se veía.** El barrido
mira intentos **no terminales**; un `aprobado` sin mover **es terminal**, así que
pasaba de largo **en silencio**. Ahora `pagos_conciliacion_cobertura()` devuelve
**las dos cifras en la misma respuesta** y el barrido lleva su `COMMENT`.

⚠️ **Y la fecha es lo que lo vuelve urgente: HOY son pruebas; el 30-sep son
PLATA.** De los 37, **35 son pedidos del sandbox** (12→26 ago) y **dos son de
hoy** — la mensualidad de prueba y `DF-2108181`, el programa de $90 que S109-B
declaró. *El lector encontró, en su primera corrida, el caso que motivó
construirlo.*

**No tiene UI y no la necesita: contable alcanza.** Lo que falta es **quién lo
mira y cuándo** — el caso «acto 2 falló» **necesita una persona**, y lo único que
faltaba era que una persona pudiera encontrarlo.

---

## ⓪bis `L-459` Y SU CORRECTIVO — el hallazgo de la sesión

**Las dos mitades, y la segunda es la que le da el peso:**

> **① La primera prueba de un guard nuevo no es que dé VERDE: es que dé ROJO
> sobre el primer caso real que aparezca.**
>
> **② Su fixture lo escribió el mismo que lo escribió a él, y comparte sus
> supuestos.** *Un control negativo prueba que el mecanismo PUEDE gritar; sólo
> un caso real prueba que grita CUANDO LE TOCA.*

`verify:huellas` nació para registrar rotaciones. **Horas después la key de Maps
rotó de verdad — la primera rotación real posterior a su nacimiento — y salió
VERDE** (declarada `e68088b4`, en disco `353a0fbf`). Tenía la ruta hardcodeada a
una app y la credencial vivía en la otra. **Y la tabla que el guard lee ya
declaraba la ruta correcta en una columna que el guard no leía.**

### 🔴 EL CORRECTIVO MECANIZABLE, que sale de curarlo

**Contar «no aplica» como verde fue el bug de nacimiento.**
⇒ **Todo gate de la casa dice cuánto NO midió, no sólo cuánto pasó.**

**EL CENSO, medido:**

| | |
|---|---|
| gates totales | **140** |
| **declaran** lo no medido | **29 · 20 %** |
| **NO lo declaran** | **111 · 79 %** |

🔴 **Y el número que decide la prioridad no es el 111: son TRES.** De los **siete
que corren en el hook o antes del OTA**, tres no declaraban su recorte —
`verify:frenos-por-sujeto` (**curado acá**), `verify:selectores-recurrentes` y
`verify:compuertas-por-sujeto` (**los dos de S109-B**).

*Los 111 restantes son gates de sesión que corren cuando alguien los llama; los
tres del hook corren en CADA commit, y su silencio se lee como salud cada vez.*

✅ **`verify:frenos-por-sujeto` ya lo dice**, y los números aparecieron solos:
**1 sujeto del catálogo fuera de la medición · 12 CHECK de la tabla que no mira.**
Su verde ahora cierra con *«lo que mide, está — jamás no hay nada más que
mirar»*.

---

## ② LO QUE FALTA — RE-MEDIDO AL CIERRE

> ⚠️ **Este bloque decía otra cosa hace una hora, y estaba VIEJO.** Afirmaba que
> la puerta del programa no existía y que el gate de selectores seguía rojo: B y
> C cerraron las dos mientras se escribía. **Se re-midió contra el objeto antes
> de cerrar.** *Un hecho vencido en un traspaso manda a la próxima sesión a
> arreglar lo ya arreglado — y a creer que hay un bloqueo que no hay.*

**✅ CERRADO — la puerta de entrada existe.** Medido: `SujetoDeCobro` tiene
**cinco** valores (`compra · cita · bono · mensualidad · programa`) y la edge
`pagos-cobro` crea intentos con `pedido_id · cita_id · bono_id ·
guarderia_suscripcion_id · programa_contratado_id`.

**✅ CERRADO — `D-984`.** `verify-selectores-recurrentes` sale **VERDE**, y —lo
que importa— **pasó a verde POR LA CURA, no por ajuste del gate**: el lazo itera
el catálogo y ninguno quedó cableado a mano.

**🔴 LO QUE SIGUE FALTANDO, medido:**
- **`suscripcion_servicio` NO tiene valor en `SujetoDeCobro`** ni columna en la
  edge ⇒ **el plan de paseo se puede contratar y NO se puede pagar.** Es el único
  de los cinco sujetos sin puerta de entrada. Dueño: **S108-B**.
- **El riel DeUna NO está ejercido**: `por_deuna = 0` intentos — bono y
  mensualidad cobraron por **Nuvei**. Todo el camino del link mensual, incluida
  su vuelta, es terreno no pisado.

> *Un sujeto que se aplica bien y no se puede crear nunca va a cobrar — y hoy se
> lee como completo.* (S108-B)

---

## ③ 🔴 LO QUE ESPERA FIRMA — no se modela hasta que llegue

**Reactivar un plan pausado por link impago, ¿RE-ANCLA el día de cobro?**
Lectura de la mesa, servida y **no aplicada**: fuera del período pagado es
contratar de nuevo (firma 8), y el link impago deja a la familia ahí ⇒ el link
vencido estaría muerto y se contrataría otra vez con ancla nueva. *Lo contrario
revive un plan que cobra el 31 sin que nadie haya contratado un 31.*
⚠️ Hoy `marcar_link_mensual_pagado` **nombra** el pago tardío
(`pago_tardio_link_vencido`) y **no lo aplica**. Es el único lugar donde esa
frontera toca código.

**El comprobante del paquete** espera al contador: dice *«Pago en e-PetPlace»*
sobre una compra de SALDO, y si eso tributa igual que un servicio no está medido.

---

## ④ LAS DECISIONES FIRMADAS QUE GOBIERNAN ESTO

- **PAGAR ES ARRANCAR** — la mensualidad cobra al contratar; el período ancla en
  la fecha del **intento confirmado**, no en el reloj. *Deroga la firma ③ del
  `PLAN_S108`.*
- **El ancla RECUPERA el día original**: `31-ene → 28-feb → 31-mar`. Vale para
  **todos** los recurrentes. El encadenado viejo bajaba el día y no volvía nunca.
- **El recurrente va sólo con tarjeta.** DeUna emite un link mensual y la familia
  paga a mano. La suscripción **sabe con qué riel se contrató**.
- **Si el link no se paga: el plan NO RENUEVA y queda REACTIVABLE.** Sin gracia,
  sin suspensión parcial, sin reintentos. **El mes ya pagado corre hasta su fin.**
- **Una sola llave** (`app_config.guarderia_recurrente_vivo`) gobierna **tres
  actos**: cobro por tarjeta · emisión del link · aviso de 3 días. **Nace en
  `false` explícito** y `guarderia_recurrente_vivo()` es su **único** lector.

---

## ⑤ LAS LECCIONES QUE COSTARON ALGO — cada una con lo que la produjo

**🔴 EL LEDGER NO ES PRUEBA.** `db push` dijo *«up to date»* y **no aplicó una
línea**: el número ya estaba puesto por otra pista. Se cazó preguntándole al
objeto si la función existía. ⇒ **`pnpm verify:migraciones`** mide duplicados
**y** que cada versión registrada lo esté **con el nombre de su archivo**.
*El número chocó CUATRO veces en un día.*

**🔴 UN `else` CON SIETE SUJETOS YA NO ES UN XOR.** `mover_sujeto_por_reverso`
cerraba con un `ELSE` que significaba `recurrencia`: un sujeto sin rama
**cancelaba una recurrencia de despensa ajena, con `ok:true`**.

**🔴 QUITAR UN CAMPO DE UN RETORNO NO ROMPE EL SQL — rompe al validador del
consumidor**, que sigue exigiéndolo y **convierte un éxito en error**. Ningún
typecheck lo ve: el campo era `unknown` de un `jsonb`. ⇒ se cambia el **valor**,
no se quita la clave. *Lo midió S108-C sobre `contratar_programa`.*

**🔴 UN `ok:false` CORRECTO TIRADO A LA BASURA ES INDISTINGUIBLE DE UN `ok:true`
MENTIROSO** (`L-440`). Y su filtro discriminador, de S108-B: el patrón `PERFORM`
da 26 candidatos; lo que decide es **¿el llamado avisa DEVOLVIENDO o LANZANDO?**
✅ **Corrido sobre mis 14 funciones de este arco: CERO.** *Es un resultado, no una
ausencia.*

**🔴 UN CENSO CORRECTO ENVEJECE EN CUARENTA MINUTOS** con otra pista en vuelo
(`L-441`). Lo hace visible el **control positivo** del cinturón, no leer con
cuidado.

**🔴 EL CASO SE FABRICA, NO SE BUSCA.** Un fixture que busca su rojo en el estado
de la base deja de discriminar el día que alguien cura ese estado — **y no avisa:
acusa.** Y un `LIMIT 1` sin `ORDER BY` devuelve una fila que cambia sin que nadie
toque nada.

**🔴 `pg_get_functiondef` LEE LOS COMENTARIOS COMO CÓDIGO** (`L-170`). Tres falsos
positivos en una jornada, uno de ellos abortando una migración correcta. Se mide
sobre el cuerpo **sin comentarios**.

**🔴 UN NOMBRE QUE MIENTE SE ARREGLA EN EL ACTO.** `confirmar_pago_paquete_guarderia`
→ `confirmar_pago_bono` · `guarderia_proximo_cobro` → `proximo_cobro_mensual`.
*Una función `…_guarderia` aplicada a otro oficio es cómo el próximo la lee como
si fuera de uno solo.*

**🔴 UN DEFAULT NO ES UNA DECISIÓN.** Una fila ausente y una en `false` se
comportan igual y significan cosas distintas: la primera no distingue *«está
apagado»* de *«nadie lo configuró»*.

---

## ⑥ PIEZAS QUE CONVIENE NO REDESCUBRIR

- `proximo_cobro_mensual(dia, periodo_desde)` — IMMUTABLE, pura, **recupera el
  día**. Parte SIEMPRE del día original, jamás del período anterior: es lo único
  que hace posible recuperarlo.
- `verificar_compuerta_programa` / `verificar_compuerta_plan` — **ensayan el acto
  REAL** en subtransacción que se deshace. *Una validación que reimplementa las
  razones del acto se separa de él en el tercer cambio.*
- `_trg_bono_congela_desglose` y `_trg_programa_congela_desglose` — **fail-closed**:
  si no pueden congelar, la fila **no entra**. El desglose lo garantiza la TABLA,
  no cada productor.
- `bonoNoPagadoATiempo` (`packages/api`) — **una sola derivación** compartida:
  dos derivadas de la misma marca podrían discrepar sobre por qué murió un
  paquete.
- `cobro_link_mensual` — la frontera con DeUna (`url_proveedor`,
  `referencia_proveedor`) vive **en un solo lugar** y nace NULL.

---

## ⑦ OPERATIVO

**Sin OTA**, por orden del founder: el motor nuevo no llega al aparato hasta que
los checkouts de C estén. **Las llaves apagadas.** **Los 12 `pago_simulado` de
paseo/adiestramiento siguen vivos a propósito** — son ciertos mientras esos
oficios simulen, y retirarlos antes es la mentira al revés.

**Antes de cada push:** `pnpm verify:migraciones` · contar local contra remoto
**leído del objeto** · censar la clase sobre el cuerpo **sin comentarios** ·
censar **LLAMADORES** además de consumidores.
