# S81-B4 · M1 — EL BOCETO DEL PEOR CASO (vocabulario de bitácora, N=23)

**Sesión B · 29-jul-2026 · CERO construcción.** Tres láminas del MISMO set en
`2026-07-29-s81-B4-laminas-peor-caso.html` (abrir en navegador). **Nada se
prescribe: este documento no recomienda — el founder firma VIENDO.**

---

## L-186 — el componente vivo sobre el que se monta

**`SelectorOpcion` con `multiple` (chips de valor, disposición envuelta)** en
`apps/cliente/src/app/(tabs)/hogar/adiestramiento.tsx:129` — el registro de
la bitácora de la familia (P5, S63 §7): "¿Qué trabajaron hoy?" con el filtro
rápido encima (matching compartido, S65; el filtro ORDENA — mostrar primero,
no esconder: los 23 siguen alcanzables). El mismo vocabulario lo consume el
Durante del prestador (chips trabajado≠alcanzado, S63) y las Insignias de las
entradas ya registradas — **lo que se firme acá es la intensidad de REPOSO
del chip de catálogo; la construcción sería enmienda de `SelectorOpcion`
(una intensidad nueva), jamás un componente paralelo.**

## El set — N=23, medido en vivo

`cat_objetivos_adiestramiento` (activo IS NOT FALSE) = **23 filas**, medido
en DB el 29-jul. **El más cargado del censo.** Las láminas usan los 23
`nombre_familia` LITERALES (de "Te mira cuando le hablas" a "Rueda cuando se
lo pides"), orden `orden_display` — cero texto inventado (L-139).

## Las tres láminas — UNA variable

Mismo marco (390 px, papel `#FAF9F7`), mismo set, mismos 3 elegidos
(tratamiento TONAL del acento de control `#8E1F68`, Ley 22 — **constante**
entre láminas). Lo ÚNICO que cambia es el reposo:

| Lámina | Reposo del chip | Tokens literales | Procedencia |
|---|---|---|---|
| **(a) CONTORNO** | borde, sin relleno | card `#FFFFFF` + borde 1.5 `#E3E0EF` + texto `#1D1A2E` | el reposo de `SelectorOpcion` HOY — la pantalla viva |
| **(b) INTERMEDIO** | tinte de capa, sin borde | `rgba(40,232,218,.16)` (`tealAlpha16`) + texto `#0A7268` (`capaText.cuidado`) | el color-completo-sin-borde (dirección §7, S73) |
| **(c) PLENO** | sólido de capa | `#0A7268` (tealDark AA) + contenido blanco | el sólido de Ley 22 llevado al catálogo |

**Registro honesto dentro de la lámina (c):** sobre cama sólida, el elegido
TONAL queda más callado que el reposo — se muestra, no se maquilla. Si el
founder firma (c), la señal de elección necesita su propia decisión (no viene
gratis); eso también es dato del peor caso.

## La regla de decisión (de la mesa, citada — no mía)

*"Si el intermedio sobrevive a 23, sobrevive a todo; si no, la respuesta es
el árbitro."* El árbitro nombrado por 19.8 (PROPUESTA, sin firma) es **el
CATÁLOGO** — fila de catálogo = existe = relleno · parámetro = se fija =
contorno — con su confirmación práctica: *"lo que existe viene de a pocos"*…
que este vocabulario de 23 desafía a propósito. La firma del founder sobre
estas láminas ES el dato que a 19.8 le faltaba para firmarse, recortarse o
caerse.

## Salvedades declaradas

- Lámina web hecha a mano: **CRITERIO, NO EVIDENCIA** (§10 directiva) — la
  traducción a RN y el gate en dispositivo vienen después de la firma, no
  antes. DM Sans sustituida por la sans del sistema (la lámina juzga
  tratamiento, no tipografía).
- El acento de los elegidos es `control` (cliente). El lado prestador del
  mismo vocabulario (Durante) vive en dosis baja con acento de oficio — si la
  firma cambia el reposo, la dosis modula color, jamás gramática (Ley 19).
- Los 3 temas: la lámina es claro-solo a propósito (el peor caso es de
  DENSIDAD, no de tema); dark/memorial se resuelven en la construcción con
  sus registros, si algo se firma.

## Qué firma el founder

Una de tres (o el recorte que dicte viendo): (a) · (b) · (c) para el REPOSO
del chip de catálogo en el peor caso. Con la firma, la mesa decide si 19.8
se firma con el árbitro, se recorta, o cae. **Sin firma, nada se toca —
`SelectorOpcion` queda como está.**
