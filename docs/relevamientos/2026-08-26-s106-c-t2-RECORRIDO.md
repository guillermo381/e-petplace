# S106-C · TANDA 2 · OBRA 0 — EL RECORRIDO, ANTES DE CONSTRUIR

> **Pista C** · 26-ago-2026 · rama `pista/s106-c-t2` · base `main` `6bf81d12`.
> **Cero código.** Se deposita y se reporta **antes** de construir, como manda
> el encargo.
>
> **En voz de usuario**, tres pantallas nuevas: pre-join del dueño · in-call
> del dueño · in-call del profesional. Cada una con **qué ve · qué toca · qué
> pasa después · qué lo confundiría**, y los cuatro caminos tristes.
>
> 🔴 **Freno declarado en §0.** La dirección de arte que el encargo dice
> adjunta **no llegó**. Todo lo visual de acá es **provisional**.

---

## §0 · 🔴 LO QUE FRENÉ ANTES DE EMPEZAR

### ① La dirección de arte §1-§3 no viajó

El encargo dice *«LA DIRECCIÓN DE ARTE FIRMADA va adjunta (§1-§3). Manda sobre
cualquier criterio propio»*. **No vino en el mensaje, y no está en el repo.**
Medido:

- `docs/DIRECCION_ARTE.md` → **cero** menciones de `videoconsulta`,
  `teleconsulta`, `in-call` o `pre-join`. Su último commit es `81ec58db`
  (S105-A, la ley de marca ajena) — es la general de S53, no una de estas
  pantallas.
- `docs/laminas/` → **ninguna** lámina de video.

**Por qué esto frena de verdad y no es formalismo:** el encargo dice que esa
letra **manda sobre cualquier criterio propio**. Si diseño la jerarquía visual
ahora, estoy construyendo contra un documento que después me pisa. *Es el
precedente 76(b) exacto —anunciada «adjunta» sin literal— y ya se pagó dos
veces (S68, S74).*

⇒ **Este recorrido decide FLUJO y VOZ, no craft.** Donde digo «grande» o
«discreto» es intención de comportamiento, no especificación visual. **La
dirección de arte, cuando llegue, la pisa sin discusión.**

### ② De las piezas de B existe UNA, y la in-call necesita tres

| pieza | estado |
|---|---|
| `AvisoTeleconsulta` | ✅ publicada (`packages/ui`), con el texto firmado por props |
| `InsigniaModalidad` | ✅ publicada |
| **el modal de dos alturas** (Obra 4) | ❌ **no existe** |
| controles de llamada · cuadro congelado · pre-join | ❌ **no existen** |

**Ley 11: no las monto inline. Se piden.** El pedido va en §5.

### ③ La casilla de consentimiento (Obra 6) es de B, no mía

`AvisoTeleconsulta` **no tiene casilla** (medido: cero menciones de
casilla/checkbox/acepto). Su contrato recibe el texto por props y expone tres
acciones. **La casilla cambia el contrato de la pieza** ⇒ es de B.
*Yo cableo el efecto: sin casilla marcada, «Continuar» no habilita.*

---

## §1 · LOS OCHO MOTIVOS — la tabla que gobierna las tres pantallas

Medidos del objeto (`20260826260000_s106a_puede_entrar_a_videollamada.sql`).
🔴 **No se colapsan en «no podés entrar».** Cada uno dice **qué pasó** y, donde
existe, **qué hacer**.

| # | motivo | qué le decimos | por qué así |
|---|---|---|---|
| 1 | `fuera_de_ventana` | **«La sala abre a las {{hora}}.»** | 🔴 **Es EL caso**, y trae `abre_en` del servidor. *Decirle «no podés entrar» a alguien que llegó veinte minutos antes lo manda a llamar por teléfono.* Con la hora, espera. |
| 2 | `cita_no_pagada` | «Esta videoconsulta todavía no está pagada.» + camino a pagar | Tiene salida, y la salida existe hoy |
| 3 | `cita_cancelada` | «Esta videoconsulta está cancelada.» | Hecho, sin camino: reservar de nuevo es otra pantalla |
| 4 | `cita_no_realizable` | «El profesional marcó que no se pudo realizar. El pago vuelve a tu medio de pago.» | §5 — **y no nombra culpa** |
| 5 | `cita_finalizada` | «Esta videoconsulta ya terminó.» | |
| 6 | `no_es_teleconsulta` | *no se dibuja el botón* | No es un error del usuario: es una cita presencial. **Nada que decir** |
| 7 | `ajeno_a_la_cita` | *no se dibuja el botón* | 🔴 **Jamás «no tenés permiso»**: confirmaría que la cita existe. Se comporta como si no hubiera nada |
| 8 | `cita_inexistente` | «No encontramos esta videoconsulta.» | |

**La regla que los ordena:** **el botón EXISTE sólo cuando el servidor dice que
sí.** Los motivos 6 y 7 **no pintan nada**; el resto pinta **el motivo, no el
botón**. *Un botón apagado con un cartel al lado es una puerta que promete y no
cumple — la casa ya lo tiene escrito (Ley 23).*

---

## §2 · PRE-JOIN DEL DUEÑO

### Qué ve

Se ve **a sí misma** en pantalla, grande, y arriba dice con quién va: *«Vas a
entrar con Dr. Ramiro Salas · Clínica Aurora»*. Abajo, dos controles con el
estado en que va a entrar —micrófono y cámara— y un botón para entrar.

**El preview propio es lo primero que carga**, antes que cualquier texto: *lo
primero que hace cualquiera antes de una videollamada es mirarse.*

### Qué toca

Apaga el micrófono si quiere entrar en silencio · apaga la cámara · **entra**.

### Qué pasa después

Entra a la sala. **Con el mic y la cámara exactamente como los dejó acá** — sin
sorpresas.

### 🔴 Qué la confundiría

- **Que le pidamos permisos adentro de la llamada.** Por eso el permiso se pide
  **acá**, con la pantalla explicando para qué. *Un diálogo del sistema
  apareciendo sobre la cara del veterinario es la peor interrupción posible.*
- **Que el preview esté en negro sin decir por qué.** Si negó el permiso o la
  cámara no está, **la pantalla lo dice** — no muestra un rectángulo negro.
- **No saber si el otro ya está.** v1 **no lo dice**, y es una decisión: el
  servidor no lo informa hoy. **Antes que inventarlo, se calla.** ⇒ va a §5.

---

## §3 · IN-CALL DEL DUEÑO

### Qué ve

**Al profesional a pantalla completa.** Ella, en chico, en una esquina. Cuatro
controles: **micrófono · cámara · girar cámara · colgar**.

### Qué toca

🔴 **Girar cámara, y va a ser el más usado.** Las dos cámaras arrancan
frontales por firma del founder, y **toda consulta llega al momento de mostrar
al animal**. Ese botón es **obvio, grande, y NO se esconde cuando el resto del
chrome se desvanece.** *Si para mostrar al perro hay que tocar la pantalla
primero para que aparezcan los controles, el momento se pierde.*

### Qué pasa después

Cuando el profesional escribe, aparece **una señal discreta y se desvanece sola
a los 3 segundos**. 🔴 **Es una señal, no un texto:** sin contenido, sin scroll,
sin permanencia. *Sólo dice «está anotando». Mostrar lo que escribe convertiría
la consulta en una lectura, y el dueño tiene que estar mirando a su animal.*

**Colgar pregunta una vez.** Una sola.

### 🔴 Qué la confundiría

- **Ver tarjetas con datos de su mascota.** No las ve — **firma del founder**.
  *El expediente es de la consulta, no de la pantalla de video.*
- **Que se le vaya el botón de girar** justo cuando lo necesita (arriba).
- **Un silencio sin explicación.** Si el otro no tiene cámara o se corta el
  video, **la pantalla habla** — no deja un negro mudo.

---

## §4 · IN-CALL DEL PROFESIONAL

### Qué ve

El video, y **el modal de dos alturas**:

- **En medio** — la **nota clínica**: dictado y teclado, **el mismo registro
  del Durante que ya existe**. 🔴 **Su regla viaja intacta: la plataforma
  JAMÁS sugiere medicamentos, tratamientos ni posologías** (auditado en tanda
  1: el muro §8.3 está en el prompt y en el código, y la posología no parseable
  cae a `null`).
- **En completo** — la **historia clínica en LECTURA**, con **filtros por fecha
  y por tipo de caso**.

### Qué toca

Dicta o escribe · sube el modal para consultar la historia · **saca un cuadro
congelado**.

### Qué pasa después

**El cuadro congelado toma el cuadro actual del video remoto**, confirma en
**una línea**, y lo adjunta por la puerta de A. **Flash blanco de 120 ms y nada
más: la llamada no se interrumpe.**

🔴 **Si el adjunto falla, lo dice y no promete.** *Un «guardado» sobre algo que
no se guardó es peor que el fallo: el profesional sigue la consulta creyendo
que tiene la foto.*

### 🔴 Qué lo confundiría

- **Que el modal le tape al paciente** mientras dicta.
- **Que el cuadro congelado parezca haber salido y no** (arriba).
- **No saber si su nota se guardó al colgar.** El cierre es el del Durante que
  ya conoce — no se inventa uno nuevo para esta pantalla.

---

## §5 · LOS CUATRO CAMINOS TRISTES

| camino | qué ve | la regla |
|---|---|---|
| **Llegó y el otro no está** | Se queda en la sala, y la pantalla lo DICE: *«Todavía no entró nadie más»*. **No hay cuenta regresiva ni «esperá un momento»** | *Un temporizador promete una llegada que nadie garantizó.* Con la hora de la cita a la vista, esperar es una decisión informada |
| **Se cayó la conexión** | La pantalla lo dice y **reintenta sola**; si no vuelve, **el profesional tiene «no se pudo realizar»** (§5, ya construido en tanda 1) | 🔴 **No se nombra culpa**: el sistema no mide la conexión de nadie. *Un diagnóstico que el sistema no puede hacer, no lo hace* |
| **Negó el permiso** | **En el pre-join**, con su porqué y el camino a Ajustes. **Nunca adentro de la llamada** | Si entró sin cámara, **la in-call lo dice** — no muestra su propio cuadro en negro como si fuera normal |
| **Entró antes de tiempo** | 🔴 **«La sala abre a las {{hora}}»** — con la hora, del `abre_en` del servidor | El caso que más veces va a pasar. *Sin la hora, se va a llamar por teléfono* |

---

## §6 · LO QUE PIDO ANTES DE CONSTRUIR

**A la mesa / al founder:**

1. 🔴 **La dirección de arte §1-§3.** Sin ella lo visual es provisional.
2. **¿El pre-join dice si el otro ya está?** Hoy el servidor no lo informa.
   **Propongo que v1 NO lo diga** —antes que inventarlo, callarse— pero es
   decisión de producto.

**A B — tres piezas (Ley 11):**

3. **El modal de dos alturas** (Obra 4): medio = nota · completo = historia con
   filtros. Necesito su API y **quién posee la altura**.
4. **Los controles de llamada** con la condición dura: **«girar cámara» no se
   esconde con el resto del chrome**.
5. **La casilla de consentimiento en `AvisoTeleconsulta`** (Obra 6):
   desmarcada por defecto, **habilita SOLO «Continuar»**; las otras dos
   acciones **siempre disponibles**. *Cambia el contrato de su pieza.*

**A D:**

6. **La forma exacta de la respuesta de `video-token`** en el caso bueno —
   necesito el nombre del campo del token, la sala y la URL para armar el
   `LiveKitRoom`. *No lo deduzco del `index.ts`: lo pido, como el cable.*

**A A:**

7. **La puerta del cuadro congelado**: qué wrapper adjunta una imagen a la
   consulta, y qué rechazos tipados tiene.
8. **La modalidad en el detalle de cita**: `leerCitaResuelta` **no trae
   `modalidad`** — la insignia de B la necesita resuelta.


---

## §7 · LO QUE SE RESCATA DEL CABLE ANTES DE BORRARLO (Obra 8)

**La pantalla del cable se retira** (cumplió: gate verde 26-ago, LiveKit
firmado sin condición). **Pero sabía dos cosas medidas, y borrarla se las
llevaría** — se mudan acá, que es donde tanda 2 va a leer.

### ① La vista propia NO sale de `useParticipantTracks`

Medido en el gate: el tile «Vos» **no renderizó**, y el remoto —con el **mismo**
cast `'camera' as never`— **sí**. ⇒ el cast no es la causa; la fuente lo es:
`useParticipantTracks(...)` sobre la identidad del participante **local** no
devuelve su publicación de cámara.

⇒ **El pre-join (Obra 2) y el propio-en-chico de la in-call (Obra 3) leen
`useLocalParticipant().cameraTrack`** (`TrackPublication | undefined`, medido en
`@livekit/components-react`). *La medición que descarta la hipótesis obvia es el
propio verde de ③ del gate.*

### ② `registerGlobals()` se va con el cable — y hay que reponerlo

Vivía en `lib/livekit-cable.ts` y **se borra con él**. Nada usa LiveKit hasta
que existan las pantallas reales, así que hoy no rompe nada — **pero la in-call
no funciona sin él**.

⇒ **En tanda 2 va en el RAÍZ de cada app**, no en el módulo de una pantalla.
*Acá va la razón del cambio de lugar: en el cable lo puse en la ruta porque era
dev-only y no correspondía meter un módulo nativo en el arranque de la app de
una familia. Con superficie real de producto, el arranque ES su lugar.*
**Con `require` en try/catch igual** (precedente D-456): un binario horneado
antes de esta build no tiene el módulo, y sin el guard **se cae la app entera**
en vez de degradar.

### ③ Lo que NO se borra

**Los cuatro paquetes de LiveKit y el plugin quedan** — son la base de tanda 2.
Se retiran **las dos pantallas y sus dos libs**, nada más.


---

## §8 · CONSTRUIDO EN ESTE TURNO (obras 8, 7 y 1-parcial)

### ✅ Obra 8 — el cable retirado

Las **dos pantallas y sus dos libs** borradas. **Los cuatro paquetes de LiveKit
y el plugin QUEDAN** — son la base de tanda 2 (verificado:
`@livekit/react-native@2.12.0` sigue en `package.json`). Cero referencias vivas
en código. **Lo que sabían se rescató en §7 antes de borrar.**

### ✅ Obra 7 — las cadenas de permiso, y apareció algo peor de lo esperado

El encargo pedía la cadena de cámara del cliente. **Medido, el problema era
mayor: el PRESTADOR tenía DOS cadenas de cámara y DOS de micrófono**, escritas
por plugins distintos (`expo-image-picker` + `expo-camera` +
`expo-speech-recognition`), **e iOS admite UNA de cada**.

> **Gana la del último plugin.** Hoy, al sacar una foto de evidencia, iOS decía
> *«para grabar clips cortos del progreso en las sesiones de adiestramiento»*.
> *No es un defecto que introdujo la videoconsulta: lo destapó.*

**La cura no pelea con el orden de los plugins — las unifica.** Si todas dicen
lo mismo y es verdad, gane la que gane el resultado es correcto. Y además se
fijan en `ios.infoPlist`, que es la fuente autoritativa.

| app | cámara | micrófono |
|---|---|---|
| cliente | «Para la foto de tu mascota y para tus videoconsultas con el veterinario.» | «Para que el veterinario pueda escucharte durante tu videoconsulta.» |
| prestador | «Para las fotos de cada atención, los clips de adiestramiento y tus videoconsultas.» | «Para dictar la nota de la consulta, el audio de los clips de adiestramiento y tus videoconsultas.» |

**Verificado contra el objeto:** cadenas distintas por app **= 1** (cero
colisión) y `expo config --type prebuild` resuelve las cuatro.

### ✅ Obra 1 — las voces y la decisión. ❌ Falta la puerta

**Hecho:** los **ocho motivos con voz propia en las dos apps** (se leen distinto
para la familia y para el profesional) + **la lógica pura del veredicto**
(`lib/telemedicina/veredicto-entrada.ts`, en las dos).

**Ejercida con sus casos borde**, no sólo compilada:

| caso | resultado |
|---|---|
| puede | **botón** |
| `fuera_de_ventana` con `abre_en` | **la hora** («03:30 p. m.») |
| `fuera_de_ventana` **sin** hora / con hora rota | **«no pudimos verificar»** |
| `ajeno_a_la_cita` | **silencio** (`claveVoz: null`) |
| `no_es_teleconsulta` | **silencio** |
| `cita_no_pagada` | su voz |

🔴 **La degradación de `fuera_de_ventana` sin hora es decisión, no descuido:**
cae a *«no pudimos verificar»* y **no** a un estado inventado. *Decir un estado
equivocado es peor que admitir que no se pudo consultar* — y sin la hora, ese
motivo pierde lo único que lo hacía útil.

**Nace un NOVENO código que no es del motor:** `no_se_pudo_consultar`.
*Confundir «no pude preguntar» con «no puedes entrar» es afirmar algo que no
sabemos.*

**Lo que falta, y es de A:** un wrapper que llame a la edge `video-token` — la
casa prohíbe que una app invoque una function directo. **El contrato ya está
medido**, así que el pedido es corto:

```
pedirTokenVideollamada(citaId: string)
  OK    → { token, url, sala, rol, expira_en }
  error → { codigo: <uno de los ocho>, abre_en?: string }
```

**Sin eso no monto UI**: un botón sin veredicto sería exactamente el botón
apagado que la Ley 23 prohíbe.


---

## §9 · SEGUNDO TURNO DE CONSTRUCCIÓN (26-ago, `main` `1b1fb6e8`)

### ✅ Llegó lo de A, y su contrato es MEJOR que el mío

`pedirTokenVideollamada` · `adjuntarCuadroTeleconsulta` · `obtenerConfigVideo`,
los tres presentes. **Y `ResultadoVideollamada` hace `abreEn` OBLIGATORIO** en
el brazo `fuera_de_ventana` —A midió que la RPC lo devuelve
incondicionalmente— con lo que *«fuera de ventana sin decir cuándo abre» quedó
inexpresable*.

⇒ **Reescribí mi lógica pura para consumir su tipo, y retiré mi rama de
degradación**: ya no tiene caso que la produzca. *Un tipo local más flojo que
el del wrapper mantiene vivo un estado que el sistema volvió imposible, y el
próximo lector no sabe cuál de los dos manda.*

**Y aparecieron códigos que no son del veredicto** (`sin_sesion`,
`veredicto_no_disponible`, `servidor_sin_configurar`…). **Todos caen a UNA voz:
«no pudimos verificar».** *Ninguno significa «no puedes entrar»: significan «no
pudimos preguntar».* Mi noveno código de la v1 quedó absorbido por los suyos.

**Re-ejercida contra el tipo real, nueve casos:** el `ok` da botón · la hora
formatea · el ISO roto cae a «no pudimos verificar» · **los dos silencios
callan** · los tres de transporte caen a la voz única.

### 🔴 LAS SIETE PIEZAS DE B NO ESTÁN EN `main`

Medido: `packages/ui` tiene **sólo** `AvisoTeleconsulta` (aún **sin casilla**)
e `InsigniaModalidad` — las mismas de ayer.

**Existen, pero en `pista/s106-b2` (`3b1bab6e`)**, sin mergear:
`ControlLlamada` · `EncabezadoLlamada` · `SuperficieLlamada` ·
`TemporizadorLlamada` · `TileVideoPropio`.

> **`L-217`: un commit pusheado a su rama de pista está en origin sin estar en
> el canon.** B entregó; **`main` no lo tiene**, y yo construyo contra `main`.

⇒ **Obras 2, 3, 4 y 6 esperan ese merge.** No las monto inline (Ley 11).

### 🔴 Y EL GATE ME FRENÓ UNA CADENA DE PROMESAS — le hice caso

Monté el botón de Obra 1 apuntando a `/videollamada/[citaId]`. **`R63` lo
rebotó**: *«declara `pathname: '/videollamada/[citaId]'` y ningún archivo de
`src/app` la sirve»*. Es cierto — esa ruta **es** Obra 3, que espera a B.

**Retiré el montaje en vez de trabajar alrededor del gate.** *Un botón que
existe porque el servidor dijo que sí y lleva a una pantalla que no existe es
peor que no tener botón: la promesa se cumple hasta el último toque.*

**Obra 1 queda: voces ✅ · lógica ✅ ejercida · pieza ✅ construida ·
montaje ❌ — y su montaje es UNA línea en cada pantalla**, que entra junto con
la ruta de Obra 3.

### ⚠️ Obra 5 necesita un paquete nativo que no está

`FLAG_SECURE` en Expo es `expo-screen-capture`. **Medido: no está instalado ni
en el store.** Instalarlo es módulo nativo ⇒ **build nueva**. *No lo agrego sin
avisar: A acaba de hacer dos builds y una dependencia nativa nueva las
invalida.* **Decisión de la mesa: ¿entra ahora en el mismo tren que las
pantallas de video, o después?**


---

## §10 · TERCER TURNO — DOS FRENOS DUROS, Y UNO ES DE LETRA FIRMADA

**Verificado por contenido** (no por el anuncio): las **cinco piezas de B** en
`packages/ui` con su export · la **casilla** en `AvisoTeleconsulta` (22
menciones) · los **tres wrappers** de A · `expo-screen-capture ~57.0.2` ·
**el bump de versión SÍ está** (cliente 1.0.5→1.0.6 · prestador 1.0.6→1.0.7;
mi primera lectura decía 1.0.0 porque miré `package.json` en vez de
`app.json` — instrumento equivocado, corregido).
⚠️ **`registrarHechoDeSala` NO tiene wrapper** (0 en `packages/api`). Mis obras
no lo necesitan; se declara para que nadie lo dé por hecho.

### ✅ Lo construido y verde: el arranque nativo

`registerGlobals()` **en el raíz de las dos apps**, con `require` en try/catch.
**Cambio deliberado de lugar** respecto del cable: ahí vivía en la ruta porque
era dev-only; con superficie de producto el arranque es su lugar, y quedó
escrito en §7 para que no se leyera como descuido. Sin el guard, un binario
horneado antes de esta build **se cae entero al abrir**.

### 🔴 FRENO ① — la API de transporte que no pude verificar

Las obras 2, 3 y 4 necesitan **dos cosas del SDK que no pude medir**:

1. **La fuente del preview del pre-join** *sin* estar en la sala.
   `expo-camera` **no está en el cliente** (sí en prestador), y agregarlo es
   módulo nativo ⇒ invalidaría el bump que A acaba de hacer.
2. **`switchCamera`** — 🔴 **el botón más usado de la pantalla** según §2.

**Por qué no las adiviné:** los typings de `@livekit/react-native-webrtc`
vienen **manglados** (`n()`, `_n()` en `MediaStreamTrack.d.ts`), y
`livekit-client` no expone un `.d.ts` legible en `dist`. *Escribir una llamada
que compila contra un tipo que no leí es la clase de verde flojo que esta
sesión evitó siete veces.*

⇒ **Pedido a D**, que es quien tiene el conocimiento de transporte y ya
entregó la spec del cable: **las dos líneas exactas** — de dónde sale el track
local antes de conectar, y cómo se gira la cámara con estas versiones.
*Igual que con el cable: se pide, no se deduce.*

### 🔴 FRENO ② — LA LETRA ENUMERA SEIS SIGNOS Y DICE «CINCO»

**Medido en `LETRA_TELEMEDICINA` v1.1 §3**, líneas 42-45. El texto firmado
enumera:

> «—dificultad para respirar, **sangrado**, convulsiones, golpe fuerte, dolor
> intenso **o decaimiento repentino**—»

**Son SEIS:** ① dificultad para respirar · ② sangrado · ③ convulsiones ·
④ golpe fuerte · ⑤ dolor intenso · ⑥ decaimiento repentino.

**Pero el propio comentario de la letra dice «los cinco signos»** (línea 62, y
la nota 🔴 original: *«nombrar cinco signos le da un criterio»*), **y B
construyó la tupla con cinco** — con su candado explícito: *«Tupla: cuatro no
compila»*.

> ### El candado de B es correcto y por eso el choque no se puede esquivar.
> Una tupla de cinco **no acepta seis**. Para que compile hay que **borrar un
> signo clínico** o **fundir dos en uno** — y §3 prohíbe las dos cosas con
> todas las letras: *«No se resume, no se acorta.»*
>
> *El candado de B hizo exactamente lo que un candado debe hacer: convirtió
> una discrepancia de prosa en un error de compilación. Sin él, alguien
> pasaba cinco de seis y el aviso salía a producción con un signo menos.*

**No elijo cuál se cae.** Es letra firmada y es contenido clínico: lo resuelve
la mesa, y son dos caminos —**la prosa dice mal y son seis** (⇒ B pasa la tupla
a seis) o **la enumeración tiene uno de más** (⇒ el founder dice cuál sale)—.

⇒ **Obra 6 revertida** para dejar el árbol verde. Su cableado está probado:
entra en cuanto la tupla y la letra coincidan.

### Estado al cierre de este turno

| obra | estado |
|---|---|
| 1 | voces ✅ · lógica ✅ · pieza ✅ · **montaje espera la ruta de Obra 3** |
| 2, 3, 4 | 🔴 **freno ①** — la API de transporte |
| 5 | el paquete ya está; **entra con la pantalla de consulta**, que es de Obra 4 |
| 6 | 🔴 **freno ②** — seis signos contra una tupla de cinco |
| 7, 8 | ✅ entregadas en el turno anterior |

**4 typechecks en 0 · `verify:diseno` VERDE con 58 reglas.**


---

## §11 · CIERRE DE LA TANDA 2 — todo verde, sin gates salteados

`main` `26ba4043`. **R67 en verde**: lee los seis por la forma de lista.
*(Nota menor para B: el comentario de su línea 36 sigue diciendo cinco mientras
el tipo de la 151 ya dice seis — prosa vencida al lado del código correcto.)*

| obra | estado |
|---|---|
| 1 · el botón «entrar» | ✅ montado (la ruta ya existe) |
| 2 · pre-join | ✅ |
| 3 · in-call del dueño | ✅ |
| 4 · in-call del profesional | ✅ **salvo el cuadro congelado** (abajo) |
| 5 · `FLAG_SECURE` | ✅ `usePreventScreenCapture()` |
| 6 · la casilla | ✅ repuesta |
| 7 · cadenas de permiso | ✅ (turno anterior) |
| 8 · retirar el cable | ✅ (turno anterior) |

**Obra 5** usa `usePreventScreenCapture()` y no el par manual: **enciende al
montar y apaga al desmontar por sí solo**, así que no puede quedar encendido
por un camino de salida que nadie previó — que es exactamente el riesgo que la
letra nombra («si queda encendido de más, el founder no puede sacar
capturas»). Su límite honesto quedó escrito en el archivo.

### 🔴 EL CUADRO CONGELADO NO TIENE CAMINO VERIFICADO — y no lo inventé

Medido, con control positivo sobre el mismo directorio:

- **Ni `@livekit/react-native` ni el fork de webrtc exponen captura de cuadro**
  (`snapshot` · `captureFrame` · `grabFrame` → **cero**; el control con
  `VideoTrack` sobre ese mismo directorio **sí** imprime ⇒ el cero es real).
- **`react-native-view-shot` no está instalado** en ninguna de las dos apps.

**Y el candidato obvio tiene un riesgo conocido que conviene decir antes de
gastar un build:** `view-shot` captura la jerarquía de vistas, y un video
nativo se dibuja en `SurfaceView`/`TextureView` — **en varios casos sale
NEGRO**. *O sea que agregar una dependencia nativa, invalidar las builds y
rehornear puede terminar en un rectángulo negro adjuntado a una historia
clínica.*

⇒ **Decisión de la mesa, con dos caminos y su costo:**
① `view-shot` + una prueba en aparato **antes** de prometerlo · ~~② pedirle a D
una vía del lado del SDK~~.

> ### ☠️ EL CAMINO ② YA SE CERRÓ — 26-ago, y con una razón mejor que «no hay»
>
> **D lo midió por su cuenta y desde el otro lado, y coincidió:** no hay vía en
> el cliente. **Y la del servidor es Egress — o sea GRABACIÓN — que nuestro
> propio token ya cierra con `roomRecord: false`.**
>
> *No es que no encontramos cómo: es que la única vía del servidor es
> exactamente lo que la letra decidió no hacer.* **La ausencia dejó de ser un
> hueco y pasó a ser una consecuencia de algo firmado**, que es una respuesta
> mucho más estable — nadie tiene que volver a buscar.
>
> **Queda vivo sólo el ①**, y con la vara puesta antes: *un rectángulo negro en
> una historia clínica no es un bug de UI, es un dato clínico falso.* **Lo que sí está listo es todo lo que sigue al
cuadro**: `adjuntarCuadroTeleconsulta` espera un objeto **ya subido**
(`bucket` + `storagePath`), y ese camino la casa ya lo tiene.

### ⚠️ Y dos cosas del modal que declaro en vez de simular

- **La nota del modal escribe en estado local, no sedimenta todavía.** El
  Durante real pasa por `sedimentar_nota_clinica` con su confirmación campo por
  campo — **meter media sedimentación adentro de una llamada sería peor que no
  tenerla**. *Pregunta a la mesa: ¿la teleconsulta sedimenta al colgar por el
  Durante que ya existe, o la nota del modal es su propio acto?*
- **La historia clínica en `completo` muestra su encabezado y su «solo
  lectura», no la lista con filtros.** El lector con filtros por fecha y por
  tipo de caso **no existe todavía** — va como pedido a A, y prefiero un
  encabezado honesto a una lista inventada.


---

## §12 · LA FIRMA DE LA NOTA, CABLEADA — el borrador no muere al colgar

**Firma del founder (26-ago): el modal es BORRADOR VIVO; la nota sedimenta por
el Durante, al colgar.**

🔴 **Y eso era trabajo, no ratificación: hasta este cambio el borrador SE
PERDÍA.** `colgar` hacía `router.back()` y el texto vivía en un `useState` que
moría con la pantalla. *Un vet que escribió media consulta mirando al animal y
cuelga con la nota en blanco no ve un error: ve que su trabajo no estaba.*

**Cómo quedó:**

- `colgar` **entrega el borrador** en vez de descartarlo.
- **Con texto** → `router.replace` al Durante con `?borrador=`, que arranca en
  fase `dictado` y con el texto puesto. Ahí sedimenta **por el flujo que el vet
  ya conoce de las presenciales**, con su confirmación campo por campo.
- **Sin texto** → sale y ya. *Obligar a pasar por el Durante a quien no
  escribió nada sería cobrarle un trámite por no haber usado una función.*
- **Arranca en `dictado` y no en `antes`**: el vet **acaba de ver al animal en
  vivo**; hacerlo pasar de nuevo por la ficha de 30 segundos sería repetirle lo
  que está mirando.
- `replace` y no `push`: volver con atrás a una sala que ya se dejó no tiene
  sentido.

### Lo que sale de la tanda, por decisión de la mesa

**El cuadro congelado** — sin vía verificada no entra. *El resto de su camino
(`adjuntarCuadroTeleconsulta` sobre un objeto ya subido) está listo y no se
tocó: cuando aparezca la captura, engancha.* **El lector de historia con
filtros** queda como pedido a A.

**La tanda cierra con 4 typechecks en 0, `verify:diseno` VERDE con 58 reglas y
CERO gates salteados.**


---

## §13 · LOS DOS HALLAZGOS DEL GATE — uno curado, el otro NO se reproduce

### ⚠️ ② EL ÁREA SEGURA INFERIOR — CURADO, y era mío

El CTA «Entrar a la consulta» quedaba **debajo de los botones del sistema**.
**Causa medida:** las pantallas de videollamada declaraban `edges={['top']}`
⇒ **el inset de abajo no se aplicaba nunca**. Curadas **4 en el cliente y 3 en
el prestador**.

*Un CTA que el pulgar no alcanza no es un problema de estilo: es una pantalla
sin salida.*

⚠️ **Y la in-call NO se toca, a propósito:** no lleva `SafeAreaView` porque el
video va **a sangre**, y los insets viajan como props a `SuperficieLlamada`,
que **ya los aplica** en su barra (`paddingBottom: insetBottom + spacing[4]`,
medido en su línea 211). *Envolverla la recortaría con dos franjas donde tiene
que haber imagen.* ⇒ **el «revisá lo mismo en los controles» está verificado y
NO hace falta cambio.**

### 🔴 ① GIRAR CÁMARA — NO SE REPRODUCE CONTRA `main`

A midió *«la pieza está en el binario y tu pantalla no la invoca con ese
control»*. **Medido contra `main` `5553c3ab`, eso no da:**

| medición | resultado |
|---|---|
| `onGirarCamara` en la in-call del cliente **en `main`** | **línea 373** ✅ |
| `vozControles.girarCamara` | **línea 382**, con `vcVozGirar` ✅ |
| `24e1f4cc` (mi Obra 3) ancestro de `main` | **sí** ✅ |
| superficies de llamada en el cliente | **UNA sola** ⇒ no hay otra pantalla que medir |
| la pieza de B | dibuja el control con `{onGirarCamara != null && …}` ✅ |

> **Las dos mediciones no pueden ser ciertas sobre el mismo objeto ⇒ una de las
> dos mide contra otro.** Y la explicación más probable no es un error de A:
> **es que el binario que corrió el gate sea ANTERIOR a mi commit.**

**El discriminador, para cerrarlo en un minuto y sin discutir:** la etiqueta de
accesibilidad del control es **«Girar cámara»** (`vcVozGirar`).
**Si esa cadena no está en el bundle instalado, ese binario no tiene mi Obra 3**
— y entonces el hallazgo es de tren, no de código.
*Si SÍ está y el botón igual no aparece, entonces es de la pieza y es de B —
pero eso hay que medirlo antes de moverle una línea a nadie.*

⇒ **No toco nada de girar cámara hasta que ese discriminador conteste.**
*Mover código que la medición dice que está bien es la forma más rápida de
romper lo único que anda.*


---

## §14 · EL SILENCIO NO TENÍA DÓNDE VIVIR — curado

**Hallazgo de A en el gate, y es mío.** `queDibujar` devolvía `claveVoz: null`
para los dos silencios, y la pantalla hacía:

```
titulo={vozSinEntrada ?? t('…vcSinEntrada')}
```

⇒ **el silencio caía al genérico** *«No podemos abrir esta videoconsulta»*.
**Doble costo, y el segundo es el grave:** colapsa los ocho motivos **y ese
genérico también confirma que la cita existe** — o sea que la protección de
`ajeno_a_la_cita` se perdía **en el último tramo**.

> ### La decisión estaba bien en la lógica y no tenía dónde vivir en la superficie.
> `string | null` hacía que `null` significara **dos cosas** —«no hay voz» y
> «no pintes nada»— y la pantalla las trataba igual.
> ***Un estado que no se puede expresar se convierte en el estado de al lado.***
> Es la misma familia que el criterio ② que la mesa ya registró (`null` no se
> degrada a `false`), aparecida del otro lado: **no en el lector, en el
> renderizador.**

**La cura:** el silencio pasa a ser **su propio caso**
(`{ tipo: 'silencio' } | { tipo: 'voz'; texto }`), y **no dibuja: SALE**. Una
pantalla en blanco sería un callejón, y cualquier texto —hasta el genérico—
confirmaría la cita.

**Sale con `canGoBack()`**, no con `back()` a secas: a esta pantalla se llega
**por deep link**, donde `back()` no tiene a dónde volver. *Es exactamente el
defecto que el gate encontró en «Probar de nuevo» — se cura acá antes de que
lo encuentre otra vez.*

### ⚠️ Lo que NO hacía falta tocar, y por qué

**El componente inline de la Obra 1 ya estaba bien.** Ahí `return null` **no
pinta nada y no hay callejón**, porque vive dentro de una pantalla que tiene
otro contenido.

> **La misma decisión era correcta en un lugar y defectuosa en el otro**, y la
> diferencia no está en la lógica: está en que **una pantalla completa no tiene
> «nada» — siempre tiene un default.** *Por eso el defecto sobrevivió a que la
> lógica se ejerciera con sus nueve casos: los nueve pasaban.*

### ✅ ② confirmado — coincide con mi diseño

4 `edges` en el cliente y 3 en el prestador, y **la in-call sin `SafeAreaView`
a propósito**: el video va a sangre y los insets viajan como props a
`SuperficieLlamada`, que los aplica en su barra. Medido después del merge:
idéntico a lo que había hecho.
