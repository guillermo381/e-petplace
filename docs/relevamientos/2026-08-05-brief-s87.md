# BRIEF S87

> **Estado de entrada: `9e83b6d`** (cierre de S86). **El acta del método
> de S86 es lectura de arranque** — su §4 (los cuatro frenos) y su §8 (la
> ley de los guards) explican por qué varias cosas de acá están escritas
> como están.
>
> **Arranque obligatorio, sin excepción:** `docs/METODO_TRES_PISTAS.md`
> (territorios · paso ⓪ **con sus dos mitades** · cómo se pide algo de
> otro territorio) y `docs/CONTRATO_TRABAJO.md` **v1.28**.

---

## 1 · LA PRIMERA ORDEN — `D-651` 🔴 LA BARRA DE TRES

**Cinco personas ven HOY una superficie que nunca se diseñó**, y el
código declara **por escrito** que eso es imposible:
`(tabs)/_layout.tsx:305` dice *«INERTE hoy: solo el titular llega»*.
**La DB dice cinco filas activas no-titulares en negocios `activo`**
(4 con chips + 1 recepción), y la puerta resuelve por **titularidad O
vínculo activo** desde S75.

**⇒ arranca por acá porque es lo único de la lista que YA está
pasando.** *No es una deuda de futuro: es una pantalla sin diseño que
alguien está usando.*

**Sus dos mitades:**
1. **El DISEÑO de la barra de tres** — hoy es un `.filter()` sin lámina.
2. **EL GUARD, que es la parte que trasciende el caso** *(instrumento de
   B)*:
   > **UNA CONDICIÓN QUE EL CÓDIGO DECLARA «INERTE» SE MIDE CONTRA EL
   > MOTOR, NO SE DESCRIBE. SI LA PREMISA CADUCA, ROJO.**
   *Precedente vivo: el trigger de la vitrina (S78) ya se abre solo
   verificando `to_regprocedure`; falta la versión del lado del código.*

**⚠️ Y su trampa de gate, que hay que resolver ANTES de firmar nada:**
**el founder NO PUEDE verla con su dedo** — su cuenta es titular. *Es una
de las cuatro superficies inalcanzables de `L-161`.* **Sin un camino de
gate, esta pantalla se construye a ciegas.**

---

## 2 · `D-654` 🔴 — EL MOSTRADOR DE TODOS LOS OFICIOS · **el orden ya está escrito**

**No queda nada que re-decidir** — las tres firmas están en la ficha. **Se
construye en este orden y el orden NO admite partirse:**

1. **`registrar_atencion_mostrador`** — el gate deja de preguntar
   `es_medico` y pasa a contemplar los cuatro oficios.
2. **La resolución de la oferta pasa a `(tipo_servicio, duracion_minutos)`**
   — muere el `LIMIT 1`; la duración deja de ser derivada y pasa a ser
   **parámetro**.
3. **Wrapper y superficie** reciben la duración elegida *(C ya tiene la
   pieza: el menú llega a 300')*.

> **ABRIR EL GATE SIN CURAR EL `LIMIT 1` SERÍA ENCENDER EL DEFECTO** —
> hoy es inofensivo **porque** el gate no deja pasar oficios con
> duraciones múltiples.

**⚠️ EL «ANTES» SE CAPTURA PRIMERO (L-199):** hoy ese registro rebota
`tipo_no_medico`. **Ése es el rojo que hay que producir ANTES de abrir el
gate**, o la cura queda sin evidencia para siempre.

---

## 3 · LO QUE S86 NO ABRIÓ — y lleva dos sesiones de arrastre

**Se escribe como HEREDADO y no como nuevo, a propósito.**

- **EL REPARTO DE ROLES.** *Era el foco declarado de S86.* **Se midió
  —el censo existe y parió `D-651` y `D-652`— y no se construyó nada.**
  El censo vive en `2026-08-04-s86a-CENSO-DE-ROLES-prestador.md`: **es el
  insumo, no hay que re-medir.**
- **NOTIFICACIONES — nunca se abrió.** Sigue siendo **precondición** de
  `A3.5bis-b` (la puerta del permiso, **letra sin motor**) y del **correo
  de recuperación que hoy lleva al portal viejo**.

---

## 4 · «PIDE TU OJO» — recortada, esperando **dos lectores**

**Firma de mesa: sin fuente no se dibuja.** *Una sección que inventa su
contenido es peor que una que no está.*

Los dos que faltan: **vacunas vencidas entre las vidas que cuida** ·
**documentos/registro faltante del equipo**. **Cada línea es una puerta**
⇒ el lector devuelve **el destino**, no solo el texto. *Lo mismo aplica
al «1 aviso» del resumen de equipo.*

---

## 5 · LAS OTRAS VIVAS

| ficha | qué decidir o hacer |
|---|---|
| **`D-653`** 🟡 | **decisión de PRODUCTO antes que de código:** *¿el mostrador puede agendarle a una mascota que hace 7 meses no pisa la clínica?* Si sí, la caducidad sobra en el lector; si no, falta en el escritor. **Hoy cada puerta contesta distinto.** 8 filas vivas, 0 caducadas — **envejecen solas** |
| **`D-652`** 🟠 | **su primera prueba es CREAR UN ADMIN REAL** y caminar el censo con él. **Trampa declarada: el alta no tiene superficie** (`D-513`/`D-517`) ⇒ hoy es un acto de SQL |
| **`D-649`** 🔴 | vive, **y su cura viaja por el canal roto**. *Toda cura que viaje por el canal que está roto necesita además un camino que no dependa de él* |
| **`D-539`** 🟠 | **que pintar `mensaje` sea IMPOSIBLE o RUIDOSO.** Tres formas, ninguna firmada: lint que prohíba `.mensaje` en JSX *(de B)* · renombrar a `mensajeDev` *(rompe en compilación, caro y honesto)* · la capa de idioma *(la única que sirve para un tercer idioma)* |
| **`D-650`** 🔴 | **la cura de raíz NO está diseñada** — que la tarea de background no pinche el chequeo. *No se inventa acá* |

---

## 6 · LOS PENDIENTES **SIN DUEÑO** — a repartir al abrir

*Se listan juntos porque comparten defecto: **nadie los va a tomar solo**.*

- **`apps/cliente` NO FIGURA EN LA TABLA DE TERRITORIOS.** *A no escribe
  apps; B es `packages/ui`; C es `apps/prestador`.* **En S86 hubo que
  cederlo ad-hoc dos veces.** ⇒ **se resuelve al abrir, no a mitad.**
- **Las 9 rutas de `M2`** *(`Campo` sin `EvitaTeclado`)* y **las 17 de
  `R31`** — con su alcance ya declarado por el guard.
- **EL ÁRBOL DE MONTAJE como cura real del discriminador:** hoy `M2`
  resuelve anfitrionas **por archivo**; la cura correcta es evaluar **por
  árbol de montaje** *(un `<Campo>` está cubierto si su pantalla raíz
  porta la pieza)*. **Mientras no exista, los falsos positivos vuelven.**
- **Las 83 imperativas FUERA del alcance de `R31`** — *el mismo defecto
  de alcance que `M2` tuvo: el número es correcto para lo que mide y más
  chico que la ley.*

---

## 7 · LA LÍNEA DE TIEMPO DEL CLIENTE — *si aplica*

El HOY del prestador **dejó de contar el día dos veces** (arriba lo vivo,
el día abajo y en orden). **Si el Hogar del cliente tiene el mismo doble
conteo, la forma ya está firmada y probada de un lado** — **pero eso NO
está medido**, y esta línea **no lo afirma**: *se mide antes de
proponerlo.*

---

## 8 · LO QUE NO SE HEREDA COMO HECHO

- **`verify-ota` está enganchado al paso ⓪** (regla 84 ③): **un publish
  sin su verde NO se declara cerrado.** *Su costo está aceptado por
  escrito: por L-197 sale ROJO cuando no puede medir ⇒ una caída de EAS
  frena un cierre. El escape es `--sin-builds`, ruidoso, y quien lo use
  lo declara con el ancla.*
- **El pedido a B y C de S86 quedó COMMITEADO Y SIN ENVIAR**
  (`2026-08-04-s86a-PEDIDO-a-B-y-C-teclado-y-mudanza.md`). **Su contenido
  sigue vigente** — incluida la corrección visible de la lista de A por
  el ensanche de B.
- **`paseo_30min` / `paseo_60min` son TAXONOMÍA MUERTA declarada** (cero
  ofertas, cero consumidores). **Su jubilación se decide APARTE** y **no
  se les agregan hermanos**.
