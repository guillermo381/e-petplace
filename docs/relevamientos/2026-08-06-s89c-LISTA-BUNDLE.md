# S89-C · LA LISTA DEL BUNDLE — dedos pendientes de la pista C

> **Vehículo: el bundle de la próxima veda.** Régimen de aparato S89: el
> único teléfono es del founder — cero dedos de la pista; cada ítem trae
> su paso a paso. Consolida los dedos de S88 (estado de cierre §5, que
> queda como historia) + lo nuevo de S89. Las notas operativas (emulador,
> `adb -s`, la sonda de credenciales, `eas-cli` desde `apps/prestador/`)
> viven en el estado S88-C §6 y NO se copian: se leen allá.

## A · Herencia S88 (sin correr — ninguno de los 5 commits estaba en bundle)

1. **Preferencias del prestador** (`/cuenta/preferencias`): 6 filas con
   sus líneas firmadas · `resumen` ausente · rebote
   `categoria_no_apagable` al intentar apagar una no-apagable · la Hoja
   de WhatsApp con su literal y el rebote `opt_in_sin_evidencia`
   (construido y JAMÁS ejercitado) · permiso del SO negado, dicho.
2. **La campana** (`/avisos` + el techo del HOY): huella presente con el
   fixture D-671 → desaparece al leer todos · la lista con los 4
   estados · el toque lleva al lugar del hecho · vacío honesto · nombre
   largo y en inglés (la línea de la lámina) · el toque en la banda de
   20dp no abre lo que no era.
   ⚠️ Caveat conocido (censo a B, sin cura): la huella del Badge pinta
   `accent.active` = el hex del muro del techo — puede ser invisible
   justo donde la lámina la manda. El ojo del founder lo va a ver;
   no es fallo del fixture.
3. **Viejo:** la voz nueva de GateAjeno vía deep link con el admin.

## B · Apertura S89 (las dos voces — commits `96fd21e` y `b93b310`)

4. **La voz del hueco de plata** (tab Datos): entrar como **profesional
   con chips** (p. ej. `+s87prof` si tiene chips en un negocio activo) →
   tab Datos → la tarjeta del hueco dice **«Los ingresos los ve quien
   está en el mostrador»** (ya no «Solo el titular»). Con titular, admin
   o recepción: la plata SE VE (números, no el hueco).
5. **La voz del empleado de negocio no-activo** (`sesion.empleadoDetalle`)
   — **SIN DEDO POSIBLE, declarado:** la rama espera su lector (hoy
   degrada a `sinRol`); no hay camino de pantalla que la alcance. Se
   verificó por código (tsc + Espejo es/en). Entra al bundle solo como
   registro: si algún día un dedo la ve, la voz correcta es «El negocio
   todavía no está activo en e-PetPlace…».

## C · Orden 3 ① — el ocultamiento derivado (commit de esta orden)

6. **Preferencias como PRESTADOR — el ojo del founder:** entrar a
   `/cuenta/preferencias` con una cuenta de prestador y verificar DOS
   ausencias y UNA presencia:
   - la fila **«Lo que ya pagaste» NO está** (derivada: 0 tipos
     `saldo_pagado` para prestador — la firma de mesa S88, cumplida por
     el catálogo);
   - la fila **«Resúmenes» NO está** (0 tipos vivos, igual que S88);
   - la fila **«Salud y seguridad» SÍ está** (2 tipos `ambas` — la
     pre-adjudicación VISIBLE de la mesa).
   Contra-ojo en el cliente: «Lo que ya pagaste» SÍ se muestra allá.

## D · Cuentas para los dedos (heredado S88, con su advertencia)

- (a) `+s87prof` · (b) `+s87recep` · (c) `demo-prestador` —
  `S87prueba!2026` · admin `+s88rolpuro` — `S88puro!2026`.
- **Todo recuento excluye `+s87`/`+s88` o miente.**
- ⚠️ (c) `demo-prestador` sigue **bajo investigación de A** (falló login
  el 6-ago): la sonda funcional de credenciales (estado S88-C §6) corre
  ANTES de gastar cualquier dedo con esa cuenta.
