# S79-A · Tanda 1 — LAS LECTURAS (27 Jul 2026)

Relevamiento de arranque del arco EL PERFIL DEL PRESTADOR + PLACES.
**Cero escritura**: ni DB, ni docs maestros, ni commits. 76(g) declarada: NO RIGE
(no hay migración). Toda afirmación de abajo sale de literal contra la DB viva
(`information_schema` · `pg_get_functiondef` · `pg_policies` · `pg_constraint` ·
filas reales) o de grep del árbol en `fb5ee24`. Lo no medido está marcado.

**Caveat L-170 vigente en todo censo por `prosrc`:** el match lee comentarios como
código; donde el body no se leyó entero, se declara.

---

## 0. Paquete de arranque — qué se leyó, por sección

| Doc | Secciones leídas |
|---|---|
| `MODELO_PRODUCTO.md` | §2 Tesis del producto ENTERA (superapp, insight estructural, 3 capas, modelo humano, multi-especie, vara wow, filtro amor al oficio) + índice completo del doc |
| `MODELO_FINANCIERO.md` | §2.6 (una entidad fiscal = una cuenta multi-rol; `tipo_actor` en `cuenta_roles`; coherencia 4 tablas operativas sin fiscal) · §2.7 (multi-sede/multi-persona reescrito S66: `uq_prestadores_user_id`, multiplicidad en cuenta→sedes) · §4.2 (tabla de las 4 operativas + constraints de `prestadores`) · Decisión K (multi-sede) · Decisión L (fiscal SOLO en `cuentas_comerciales`) |
| `MODELO_VETERINARIA.md` | PARTE I §1 (negocio contenedor, persona = negocio de 1) · §2 (vitrina/ocupación + enmiendas S78: la cita dice quién atiende; vitrina con gate mecánico) · §5 (multi-rol/multi-sede: el índice ganó) · §14 entero, en particular §14.2 (verificación del vet: "`requiere_validacion_admin` existe; el PROCESO se construye") |
| `PORTAL_PRESTADOR.md` | §2 ENTERO (2.1–2.8: selección, llegada, bienvenida, home, aspiracional, revelación progresiva, hitos, resumen) · §6.5 ENTERO (6.5.1 Mi perfil · 6.5.2 Documentos · 6.5.3 Servicios · 6.5.4 Horarios · 6.5.5 Equipo) |
| `DEUDAS_CANONICAS.md` | D-518 con sus DOS enmiendas S77 (calendario-reclutamiento + L26 "no es un filtro") · D-367 (+enmienda S75 sistémica) · D-331 · D-332 · D-293 ✅ · D-289 ✅ · D-310 ✅ · D-517 (con adjudicación S75) |

---

# BLOQUE GEO

## A1 — Las keys de Google en el árbol

**Una sola variable viva: `GOOGLE_MAPS_API_KEY`. Cero camino server-side a ninguna
API de Google. El canon (D-289/D-293) se sostiene contra el árbol.**

Consumidores (grep completo de `GOOGLE_|googleapis|PLACES` en apps/packages/supabase,
excluido `database.types`):

| Archivo | Línea | Qué hace |
|---|---|---|
| `apps/cliente/app.config.ts` | 19 | `apiKey: process.env.GOOGLE_MAPS_API_KEY ?? ''` → `android.config.googleMaps.apiKey` (react-native-maps) |
| `apps/prestador/app.config.ts` | 22 | Idéntico (mecánica calcada, header lo declara: env secret de EAS environment development; prebuild local = exportar en shell; sin variable → key vacía, tiles vacíos) |
| `packages/ui/src/gallery/TokenGallery.tsx` | 1896 | Falso positivo: URL de un video sample en `commondatastorage.googleapis.com` — no es API ni key |

- **`.env.local` de ambas apps**: solo `EXPO_PUBLIC_SUPABASE_URL/ANON_KEY` + `EXPO_PUBLIC_DEMO_EMAIL/PASSWORD`. Cero Google.
- **Server-side**: grep de `googleapis|maps/api|places` en `supabase/functions/` y `packages/api/src/` = **0 hits**. Ninguna Edge Function ni wrapper llama hoy a una API de Google.
- **Implicancia de hecho (no propuesta):** la única key viva es de CLIENTE Android (restringida por package+SHA-1 según D-289 — la restricción vive en la consola de Google y **no se midió desde acá**, es del founder). Hoy NO existe infraestructura de key server-side; si Places se llamara desde una Edge Function, hoy no habría con qué.

## A2 — El chasis de dirección

**SON DOS PUNTAS DISTINTAS, no una tabla compartida:**

**Punta hogar = tabla `direcciones_guardadas`** (columnas literales, `information_schema`):

| Columna | Tipo | Null | Default |
|---|---|---|---|
| id | uuid | NO | gen_random_uuid() |
| user_id | uuid | NO | — (FK → `profiles(id)` ON DELETE CASCADE) |
| country_code | text | NO | 'EC' |
| alias | text | NO | — |
| nombre_receptor / telefono | text | YES | — (CHECK `telefono !~ '^\+'`) |
| direccion | text | NO | — |
| **ciudad** | **text** | **NO** | — (**texto libre, SIN FK, SIN columna ciudad_id**) |
| sector / referencias | text | YES | — |
| **lat / lon** | **double precision** | YES | — |
| es_principal | boolean | NO | false |
| created_at | timestamptz | NO | now() |

**Punta prestador = columnas EN `prestadores`** (no usa `direcciones_guardadas`):
`direccion` text NULL · `ciudad` text NULL (texto libre, sin FK) · `sector` text NULL ·
`lat`/`lon` double precision NULL · `radio_cobertura_km` integer NULL DEFAULT 5 (ver A4).

**Las 2 filas de hogar — ambas con lat/lon NULL (literal):**

| direccion | ciudad | sector | lat/lon | nota |
|---|---|---|---|---|
| `[DEMO S56] Av. Siempreviva 742` | Quito | — | NULL/NULL | seed S56, user `c5d54e3a…` |
| `Shyris y Suecia` (ref: "Shyris y suecia Edificio Iqon") | Quito | Carolina | NULL/NULL | alta real, user `dd024680…` |

**Las 5 filas de prestador — lat/lon en 2 de 5 (literal):**

| prestador | estado | ciudad | lat / lon | radio |
|---|---|---|---|---|
| `[DEMO S68] Clínica Aurora` | activo | Quito | NULL / NULL | 5 |
| **Carlos** (`d73347ba`) | en_revision | Quito | **-0.1161442 / -78.4234999** | 5 |
| **Satori Latam sas** (`2052f109`) | activo | Quito | **-0.1794464 / -78.4814217** | 5 |
| `[DEMO S58] Wizard` | activo | NULL | NULL / NULL | 5 |
| `[DEMO S44] Paseos Andres` | activo | Quito | NULL / NULL | 5 |

> **Corrección de dato al canon (enmienda 2ª S77 de D-518 decía "2 de 5 y son
> seed"):** las 2 filas CON lat/lon son **Carlos y Satori — las dos filas del
> portal v2/legado, las únicas SIN marca `[DEMO]`**. Las 3 seeds DEMO tienen
> lat/lon NULL. La conclusión de la enmienda (nadie geocodifica hoy) queda
> INTACTA — solo la atribución "son seed" era imprecisa: las coordenadas vivas
> las escribió el wizard del portal viejo, no un seed nuestro.

**Puente ciudad → `cat_ciudades`: NO EXISTE.** Ni FK ni columna `ciudad_id` en
ninguna de las dos puntas — `ciudad` es text libre en ambas. La ÚNICA FK entrante
a `cat_ciudades` en toda la DB es `prestador_zonas.ciudad_id` (RESTRICT).

**`cat_ciudades`: 9 filas** (todas activas, creadas 2026-07-13/S58): Ambato, Cuenca,
Guayaquil, Ibarra, Loja, Manta, Quito, Santo Domingo (EC) + Bogotá (CO).

Escritores/lectores DB de `direcciones_guardadas` (prosrc): `guardar_direccion_hogar`
(alta — captura texto, no geocodifica, coherente con L26) y `_direccion_hogar_snapshot`
(el snapshot D-339 hacia la cita).

## A3 — Las lectoras de oferta (bodies literales leídos con `pg_get_functiondef`)

Los tres bodies completos quedaron archivados en el scratchpad de la sesión; acá lo
que decide la pregunta:

- **`obtener_paseadores_disponibles(p_fecha, p_hora, p_duracion)`** — SECURITY DEFINER.
  Es la lectora que RECORRE prestadores: `FROM prestador_servicios ps JOIN prestadores pr
  ON … pr.estado='activo' JOIN cuentas_comerciales cc ON … cc.estado='activa' JOIN
  tipos_servicio ts …` + `NOT _prestador_bloqueado(pr.id, p_fecha)` + EXISTS de franja
  por persona con `_agenda_ocupacion(pe.id, …) < LEAST(franja, cupo_techo)`. **Cero
  geografía en el WHERE** (ratifica D-518: ni nombra `ciudad`).
- **`obtener_slots_disponibles(p_prestador_id, p_servicio_id, p_desde, p_hasta)`** —
  recibe `p_prestador_id`: es POR-prestador. Genera slots por persona habilitada y
  resta `_agenda_ocupacion`; respeta `_prestador_bloqueado`.
- **`_inicios_disponibles_prestador(p_prestador_id, p_servicio_id, p_fecha, p_duracion,
  p_empleado_id DEFAULT NULL)`** — ídem POR-prestador; el patrón S78 del
  `p_empleado_id DEFAULT NULL` está a la vista: *"la elección de persona es UN AND
  MÁS … por eso los 9 callers existentes no se tocan"*.

**Respuesta a la pregunta concreta: SÍ alcanza con UN WHERE más, y solo en las
lectoras de OFERTA — el motor de ocupación NO se toca.** Razones medidas:

1. `_agenda_ocupacion` opera por (persona, fecha, hora, duración, tipo): no sabe de
   lugares. La geografía decide QUÉ prestadores entran al listado, no cuántas citas
   caben — son ejes ortogonales.
2. `obtener_slots_disponibles` y `_inicios_disponibles_prestador` reciben
   `p_prestador_id`: cuando se llaman, la geografía ya quedó decidida en el paso
   QUIÉN. El filtro pertenece a `obtener_paseadores_disponibles` y sus hermanas por
   oficio (`obtener_groomers_disponibles`, `obtener_adiestradores_disponibles`,
   `obtener_veterinarios_disponibles`/`_vet_ofertas_cobrables` — censadas en D-518,
   no re-leídas acá).

**El fragmento** (patrón exacto de S78: parámetros nuevos con DEFAULT NULL, callers
intactos; haversine que la casa ya calcula — Vitales S53):

```sql
-- firma: … , p_lat double precision DEFAULT NULL, p_lon double precision DEFAULT NULL
-- (o resolver el hogar server-side desde direcciones_guardadas del auth.uid())
-- en el WHERE de obtener_paseadores_disponibles, tras NOT _prestador_bloqueado(…):
AND (
  p_lat IS NULL OR p_lon IS NULL
  OR (
    pr.lat IS NOT NULL AND pr.lon IS NOT NULL
    AND 2 * 6371 * asin(sqrt(
          power(sin(radians((pr.lat - p_lat) / 2)), 2)
          + cos(radians(p_lat)) * cos(radians(pr.lat))
            * power(sin(radians((pr.lon - p_lon) / 2)), 2)
        )) <= COALESCE(pr.radio_cobertura_km, 5)
  )
)
```

**Tres avisos que el fragmento NO decide (van a la mesa, no acá):**
- **La semántica del NULL es decisión de producto:** tal como está escrito, el
  prestador SIN coordenadas pasa el filtro cuando hay lat del cliente… no — tal como
  está escrito queda EXCLUIDO (la rama exige `pr.lat IS NOT NULL`). Mostrar u ocultar
  al no-geocodificado es letra, no técnica; el fragmento se escribe en ambos sentidos
  con una línea.
- **La puerta (`crear_bloqueo_agenda`) hoy tampoco valida geografía** — si la regla
  es "la DB manda" (patrón `mascota_no_elegible`/`paseo_social_no`), el mismo guard
  va también ahí; el body está leído y tiene el lugar natural (junto a
  `_prestador_bloqueado`).
- **Hoy ese WHERE filtra contra NADA**: 0 hogares con lat/lon, 2/5 prestadores (los
  dos legacy). Confirma la enmienda L26: la CAPTURA es la cara; el filtro es la mitad
  barata. Y sin índice geoespacial es scan — irrelevante con 5 filas, declarado.

## A4 — `radio_cobertura_km` y `prestador_zonas`

**LA COLUMNA EXISTE — y su default es 5, no 15.**

- `prestadores.radio_cobertura_km` · integer · NULL · **DEFAULT 5** (literal de
  `information_schema.columns`). También expuesta en la vista `v_prestadores_publicos`.
- **Filas: 5/5 con valor 5** (tabla de A2). Ninguna fila NULL, ninguna distinta de 5.
- **Quién la escribe:** `crear_prestador_inicial` (body leído entero): parámetro
  `p_radio_cobertura_km integer DEFAULT NULL` → INSERT con **`COALESCE(p_radio_cobertura_km, 5)`**.
  `wizard_crear_cuenta_y_rol` también matchea por prosrc — **su body NO se leyó
  entero** (no medido cómo la usa; caveat L-170: puede ser passthrough o comentario).
- **Quién la LEE: NADIE.** Cero hits de `radio_cobertura` en apps/packages (fuera de
  `database.types`); cero funciones DB la leen para filtrar (solo las 2 escritoras la
  nombran). Es una columna que se escribe al nacer y jamás se consulta.

> **Para la PRIMERA PREGUNTA DEL BRIEF (la del founder):** la pregunta "¿el default
> de 15 km migra las filas vivas o rige solo para las nuevas?" tiene una tercera pata
> medida — **el 15 firmado NO está en la DB en ningún sitio**. Hoy el default dice 5
> en DOS lugares (default de columna + `COALESCE(…, 5)` de `crear_prestador_inicial`).
> Cualquiera sea la respuesta del founder, ejecutar "15" toca esos dos sitios; y las
> 5 filas vivas valen 5, así que "migrar las vivas" = UPDATE de 5 filas (todas
> demo/legacy — hoy no hay prestador real que pierda nada).

**`prestador_zonas`** (shape literal): `id` uuid PK · `prestador_id` uuid FK→prestadores
CASCADE · `ciudad_id` uuid FK→cat_ciudades RESTRICT · `created_at` · `UNIQUE(prestador_id,
ciudad_id)`. **1 fila**: `[DEMO S44] Paseos Andres` → Quito (2026-07-13, el seed S58).

**Confirmación D-331/D-367: NINGUNA función de la DB la consulta** (prosrc de todo
`public` = 0 matches). En el árbol solo la toca `packages/api/src/wrappers/zonas.ts`
(CRUD+lectura S58, declarativo — sus comentarios nombran policies `prestador_zonas_own`
y `prestador_zonas_public`; **las policies de la tabla no se releyeron en esta tanda**,
lo dicho sale del wrapper y del canon S58, marcado como no medido).

---

# BLOQUE DOCUMENTOS

## A5 — `requiere_validacion_admin`

**Dónde vive:** `tipos_servicio.requiere_validacion_admin` · boolean · NOT NULL ·
DEFAULT false. Única columna con ese nombre en la DB.

**Filas por valor — 15 y 15:**
- `true` (15): certificado_apoyo, certificado_viaje, cirugia, consulta_especializada,
  consulta_general, ecografia, emergencia, laboratorio, radiografia, servicio_exequial,
  telemedicina, urgencia_domicilio, urgencia_local, vacunacion, vacunacion_internacional.
- `false` (15): adiestramiento, grooming, grooming_completo, guarderia_dia,
  guarderia_mensual, hotel, hotel_dia, hotel_noche, paseo, paseo_30min, paseo_60min,
  paseo_mensual, paseo_paquete, procedimiento, registro_evento.

**Quién la lee — exactamente UN lector, y está VIVO:**
- Función `_trg_ps_verificacion_profesional` vía trigger
  `trg_ps_verificacion_profesional` sobre `prestador_servicios` (el "primer lector
  vivo" de S68). Body literal: al activar una oferta cuyo tipo tiene
  `requiere_validacion_admin=true` SIN una fila de `prestador_documentos` con
  `tipo IN ('titulo_profesional','registro_senescyt')` y `estado='aprobado'` →
  `RAISE 'verificacion_profesional_pendiente'` (23514).
- **Cero policies** la nombran (`pg_policies` sobre qual+with_check = 0).
- **Cero lectura directa en el árbol**: el único hit es un COMENTARIO en
  `packages/api/src/wrappers/veterinaria-oferta.ts:48` que documenta el rebote del
  trigger (el error tipado `verificacion_profesional_pendiente` está mapeado en ese
  wrapper). Ninguna pantalla consulta la columna.

**Qué tan cierto es el "existe" de §14.2: MÁS cierto que la letra.** No solo existe
la columna — existe el gate vivo (trigger S68) Y el circuito del prestador para
satisfacerlo (wrapper + pantalla + bucket, ver A6). Lo que del PROCESO sigue sin
construir en el monorepo: **el lado ADMIN** (quién aprueba: `revisado_por/revisado_en/
notas_revision` existen como columnas; ninguna superficie del monorepo las escribe —
las filas aprobadas de mayo salieron del portal admin legado, no medido desde acá) y
**los vencimientos** (`fecha_vencimiento` NULL en 7/7 filas; el estado `'vencido'`
existe en el CHECK sin productor — el "vencimiento y renovación visibles" de §6.5.2
es letra sin motor).

## A6 — Documentos del prestador: tablas, buckets, y el molde D-310

### Lo que EXISTE (y es más de lo que el arranque presuponía)

**`prestador_documentos` — tabla viva con modelo de revisión completo:**

| Columna | Tipo | Detalle |
|---|---|---|
| id / prestador_id | uuid | FK → prestadores CASCADE |
| **tipo** | text NOT NULL | CHECK: `cedula · ruc · titulo_profesional · registro_senescyt · permiso_funcionamiento · certificado_vacunas · seguro · otro` |
| nombre / archivo_url | text NOT NULL | archivo_url guarda PATH del bucket (verificado en las 7 filas) |
| fecha_emision / fecha_vencimiento | date NULL | 7/7 con vencimiento NULL |
| **estado** | text NOT NULL DEFAULT 'pendiente' | CHECK: `pendiente · aprobado · rechazado · vencido` |
| revisado_por / revisado_en / notas_revision | — | FK revisado_por → profiles |

**7 filas vivas:** 5 del portal v2 (mayo 2026 — Satori: ruc aprobado, título aprobado,
cédula aprobada, permiso RECHAZADO; Carlos: cédula pendiente) + 2 del circuito S68
(Clínica Aurora: `[DEMO S68] Título profesional` aprobado en path `demo/…`, Registro
SENESCYT pendiente en carpeta real del titular).

**RLS:** DOS policies ALL **duplicadas** con el mismo predicado (`pd_own` y
`prestador_documentos_own`): own-or-admin, donde own = `prestador_id IN (SELECT id
FROM prestadores WHERE user_id = auth.uid())` — es decir **TITULAR-only** (un
empleado no ve ni escribe documentos del negocio; hoy coherente con §6.5.2
"defensiva"). La duplicación es familia D-342 (higiene), se registra.

**Escritores:** RPC legada `insertar_documentos_batch` (SECURITY DEFINER, ownership
por `user_id`, inserta con estado 'pendiente'; **proacl limpio: sin `anon`** —
`postgres/authenticated/service_role`) + el camino S68 del monorepo:
`packages/api/src/wrappers/prestador-documentos.ts` (`obtenerDocumentosVerificacion`,
`registrarDocumentoVerificacion` — insert directo por RLS —, `resolverUrlDocumento`
con signed URL) + pantalla `apps/prestador/src/app/veterinaria/verificacion.tsx` +
`apps/prestador/src/lib/subir-documento.ts` (sube al bucket).

**Las hermanas del barrido** (`information_schema`, patrón
`documento|archivo|adjunto|titulo|certificad|licencia|permiso`):
tablas `criadero_documentos` · `seller_documentos` · `certificaciones` (con
`archivo_url`) · `cat_tipos_documento_titular` (bancario, Decisión O) ·
`cat_categorias_archivo` · `evento_archivo_adjunto` · `evento_certificado_emitido` ·
`evento_grooming_archivos` · `admin_permisos`; columnas sueltas relevantes:
`refugios.permisos_legales`, `servicios_exequiales.certificado_url`,
`evento_examen_diagnostico.archivo_resultado_path`, `liquidaciones.archivo_url`.

### Inventario COMPLETO de buckets (storage.buckets, literal)

| Bucket | public | Límite | MIME | Policies |
|---|---|---|---|---|
| **avatars** | **true** | — | — | SELECT público · INSERT any-authenticated · UPDATE por carpeta |
| **adopcion-fotos** | **true** | — | — | INSERT/DELETE "admin" **pero el predicado es solo `bucket_id=…` a `authenticated`** — cualquier autenticado sube/borra (ver hallazgos) |
| **productos-fotos** | **true** | — | — | SELECT público · INSERT/DELETE gateados por `is_admin()` |
| prestador-documentos | false | 5MB | — | `prestador_archivos_propios` (ALL por carpeta `auth.uid()`) + `admin_lee_documentos_prestadores` (SELECT `is_admin()`) |
| cita-archivos | false | 10MB | — | familia completa por `user_puede_acceder_prestador` + 2 SELECT por acceso-a-mascota |
| grooming-archivos | false | 10MB | jpeg/png/webp/heic/pdf | ídem patrón |
| mascotas | false | 5MB | jpeg/png/webp | el saneo D-310 (SELECT dos patas, DELETE carpeta, INSERT estricta) |
| adiestramiento-clips | false | 50MB | mp4/mov/webm | patrón prestador + SELECT por acceso-a-mascota |

### El PATRÓN de `20260708220000` (D-310) — el molde, extraído del literal

1. **Bucket privado + resistencia server-side**: `UPDATE storage.buckets SET public=false,
   file_size_limit=…, allowed_mime_types=ARRAY[…]` — los límites viven en el server,
   el resize del app es cortesía.
2. **DROP explícito de las policies laxas heredadas** (por nombre literal relevado).
3. **SELECT de dos patas**: carpeta propia (`(storage.foldername(name))[1] =
   auth.uid()::text`) OR join inverso a la tabla que referencia el objeto + helper de
   acceso de la casa (`user_tiene_acceso_a_mascota`), **con índice parcial** para que
   la pata B sea un probe.
4. **DELETE por carpeta propia; UPDATE NO EXISTE** — archivos inmutables, reemplazar
   = subir + borrar.
5. **La columna guarda PATH, jamás URL**, garantizado por CHECK en migración hermana
   (`20260708233000`, `mascotas_foto_url_es_path`); la lectura firma signed URLs por
   frontera única (`resolverUrlFoto`, cache TTL + batch).

**Delta de `prestador-documentos` contra el molde (medido):** ya es privado con
carpeta propia y 5MB ✓; le faltan `allowed_mime_types` (NULL), el CHECK de
path-no-URL en `prestador_documentos.archivo_url` (hoy las 7 filas SON paths, pero
nada lo garantiza), y la pata de vencimientos no tiene motor. La lectura firmada ya
existe (`resolverUrlDocumento`).

---

# BLOQUE PERFIL

## A7 — `prestadores` vs `cuentas_comerciales`, y qué lee la pantalla de perfil

**Columnas lado a lado (literal, `information_schema`):**

`prestadores` (35 cols): id · user_id (UNIQUE) · country_code ('EC') · **tipo** (NOT
NULL — el eje muerto D-487, sigue ahí) · nombre_comercial · descripcion · foto_url ·
fotos_galeria jsonb · telefono · **whatsapp NOT NULL** (legacy) · email_contacto ·
sitio_web · direccion · ciudad · sector · lat · lon · estado ('pendiente') ·
aprobado_por/aprobado_en/motivo_rechazo · calificacion_promedio/total_citas/
total_resenas · acepta_emergencias · acepta_telemedicina · **radio_cobertura_km
(DEFAULT 5)** · created_at/updated_at · **matricula_profesional** · cuenta_comercial_id
NOT NULL · metadata · grooming_extra_pelaje_largo · grooming_recargo_domicilio ·
modo_horarios ('universal') · expone_personas (false).

`cuentas_comerciales` (20 cols): id · owner_profile_id · **tipo_fiscal** ·
**identificacion_fiscal** · **razon_social** · nombre_comercial · country_code ·
moneda ('USD') · modelo_comercial · **datos_bancarios jsonb** · kushki_subaccount_id ·
estado ('pendiente_validacion') · activado_en/suspendido_en/suspension_motivo/
cerrado_en · saldo_arrastre · metadata · created_at/updated_at.

**Regla 25 / Decisión L: SE CUMPLE EN LA DB.** Cero columnas fiscales en
`prestadores` — ni RUC, ni razón social, ni bancarios. Nota de borde:
`matricula_profesional` es habilitación de oficio (correcta en la operativa), no
dato fiscal.

**Qué lee HOY la pantalla de perfil** (`apps/prestador/src/app/(tabs)/cuenta/perfil.tsx`,
leído; territorio B — solo lectura, cero toque):

1. `obtenerMiPerfil()` (user: nombre/teléfono/email) + `obtenerMiPrestador()` (**R1**,
   `packages/api/src/wrappers/prestador.ts`) en paralelo — R1 devuelve el `Pick` de
   **17 columnas**: id, nombre_comercial, tipo, country_code, cuenta_comercial_id,
   direccion, ciudad, grooming_*, descripcion, telefono, whatsapp, email_contacto,
   sitio_web, estado, foto_url, expone_personas. **NO trae lat/lon, ni sector, ni
   radio_cobertura_km** — la pantalla de perfil de hoy no ve nada geo salvo
   direccion/ciudad texto, en solo-lectura digno.
2. **SÍ cruza ya a `cuentas_comerciales`: `obtenerMiCuentaComercial()` (R2)** en la
   línea 119 — pero SOLO para el cómputo del estado 7.13 (`estado==='activa'` + ≥1
   oferta activa + franjas → "visible"). Si cualquiera de las 4 patas falla, degrada
   a `setVisible(null)` (null honesto, la fila no se dibuja) — **el muro D-517 está
   en el camino de esta pantalla pero hoy degrada, no rompe** (R2 null para un
   empleado ⇒ `rCuenta.ok` false ⇒ visible=null; medido en el código, no en
   dispositivo).
3. Escritura: `actualizarMiPerfil` (user) + `actualizarPerfilPrestador` con
   **whitelist clave por clave** (descripcion, telefono, whatsapp, email_contacto,
   sitio_web, foto_url/logo — D-505). direccion/ciudad NO son editables desde
   ninguna superficie del prestador hoy.

## A8 — D-332: `crear_bloqueo_agenda` y los bloqueos

**Body leído entero** (archivado con los de A3). **SÍ consulta bloqueos programados:**
`IF _prestador_bloqueado(p_prestador_id, p_fecha) THEN RAISE 'prestador_no_disponible'`
(comentario in-body: D-341) — y las tres lectoras de A3 también lo llaman.

`_prestador_bloqueado(p_prestador_id, p_fecha)` (body literal): `EXISTS (SELECT 1
FROM prestador_bloqueos b WHERE b.prestador_id=… AND p_fecha BETWEEN b.fecha_inicio
AND b.fecha_fin)`. Tabla `prestador_bloqueos`: {id, prestador_id, fecha_inicio date,
fecha_fin date, motivo, created_at} · **0 filas hoy** · pantalla
`apps/prestador/src/app/vacaciones.tsx` existe.

**Veredicto sobre D-332 — la deuda NO sigue entera; sigue por la MITAD:**
- **Vacaciones y bloqueos puntuales: CUBIERTOS de motor a pantalla** (D-341 pagada
  en S62: helper único en las cuatro puertas + /vacaciones). La frase de D-332 *"no
  existen feriados, vacaciones ni bloqueos puntuales"* quedó vieja en sus dos
  últimos tercios.
- **Feriados: SIGUEN SIN EXISTIR** — ninguna tabla de calendario público, cero
  automatismo; un feriado ecuatoriano oferta normal salvo que el prestador se
  bloquee a mano. Esa mitad de D-332 sigue viva, con su disparo original.

---

# HALLAZGOS LATERALES (medidos de paso; se registran, NO se curan en esta tanda)

1. **`v_prestadores_publicos` — vista SIN `security_invoker` (reloptions NULL) y con
   ACL total para `anon`** (`anon=arwdDxtm`): un anónimo puede SELECTear de todos los
   prestadores activos **user_id, lat, lon, ciudad, sector** y el catálogo con
   precios. Cero consumidores en el monorepo y cero funciones DB la nombran —
   herencia v2 viva. Familia L-140/D-390; expone además las columnas que este arco
   va a poblar (coordenadas). Queda para la mesa.
2. **Policies duplicadas `pd_own` / `prestador_documentos_own`** (mismo predicado,
   ambas ALL) — familia D-342.
3. **`adopcion-fotos`: las policies dicen "Admin" en el nombre pero el predicado es
   solo `bucket_id='adopcion-fotos'` a `authenticated`** — cualquier usuario logueado
   sube y borra fotos de adopción. Bucket además público y sin límites. Pariente
   directo del relevamiento que parió D-310.
4. **Los 3 buckets públicos (avatars, adopcion-fotos, productos-fotos) no tienen
   file_size_limit ni allowed_mime_types** — fuera del molde D-310.
5. **Corrección de dato al canon** (detallada en A2): las 2 filas de prestador con
   lat/lon son las legacy del portal v2 (Carlos, Satori), no seeds DEMO.
6. **El default vivo de `radio_cobertura_km` es 5** — el "15 km firmado" de S77/S79
   nunca aterrizó en la DB (detalle en A4).

---

**FIN DE LA TANDA 1.** Cero escritura ejecutada (ni DB ni docs maestros ni commits;
este archivo de relevamiento es la única salida, según el mandato). La mesa
presupuesta con esto en la mano.
