# S89-A · LETRA — EL LADO A LADO DEL TUTEO (firma founder, 6-ago-2026)

**La firma:** tuteo neutro ÚNICO — resuelve la divergencia de acentos que D
midió (relevamiento D-539 §los-dos-acentos). Migración `20260806200000`;
cinturón: cero voseo residual en `_voz_notificacion`. **Solo cambia el
acento** — ni datos, ni estructura, ni destinatarios. Reversa depositada
(des-hace una firma: solo con firma nueva encima).

| voz | decía (voseo, lote S88) | dice (tuteo, firmado) |
|---|---|---|
| `plan_renovado` | «**Podés** ver el detalle en la app.» | «**Puedes** ver el detalle en la app.» |
| `plan_renovacion_proxima` | «Si no **querés** que siga, **podés** pausarlo desde la app antes de esa fecha.» | «Si no **quieres** que siga, **puedes** pausarlo desde la app antes de esa fecha.» |
| `paquete_vence` | «**Podés** reservarlas desde la app.» | «**Puedes** reservarlas desde la app.» |
| `programa_vence` | «**Coordiná** las sesiones que faltan desde la app.» | «**Coordina** las sesiones que faltan desde la app.» |
| `procedimiento_agendado` | «**Podés** ver el detalle en la app.» | «**Puedes** ver el detalle en la app.» |

*(Las voces sin voseo — `programa_vencido_reembolso`, `plan_vencido_reembolso`,
`registro_completado_*` — no se tocaron: ya eran neutras. Las nuevas de S89 —
las tres de cita y `plan_renovacion_fallida` — nacieron tuteando.)*
