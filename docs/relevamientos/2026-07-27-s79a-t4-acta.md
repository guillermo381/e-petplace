# S79-A · Tanda 4 — EL ALTA, Y EL CIERRE DEL ARCO (acta, 27 Jul 2026)

Escritor único de DB y docs. 76(f2) rigió en cada commit. CERO push.
Proyecto: `zyltipqscdsdsxnjclhp`. 76(g) declarada en la única migración de
la tanda (NO RIGE — DDL de funciones, cero backfill, cero anclas).
Reversa escrita ANTES de aplicar. **FRENO ejecutado antes de T4.6.**

## LAS NOVEDADES DEL FOUNDER, EJECUTADAS

- **D-557 CERRADA COMO ROTADA**: key server-side nueva creada, cargada
  como secret, la vieja BORRADA; la sonda T3.1 verde de punta a punta lo
  respalda. La lección de método queda firme en la deuda (credenciales
  navegador→terminal, jamás al chat).
- **D-558 DEPOSITADA**: la key de NAVEGADOR del legado — comodín
  `*.vercel.app` (cualquiera factura contra el billing) + Geocoding API
  habilitada (**cierra la pregunta de la Tanda 1**: las coordenadas de
  Carlos y Satori las escribió el wizard legado con esa key). Candidata a
  jubilarse ENTERA; disparo: próxima visita a la consola — mínimo
  inmediato, quitar el comodín. No se ejecuta desde acá.
- **LETRA_PERFIL_S79 v1.1: FIRMADA** (estado cambiado en el doc, con las
  DOS excepciones explícitas de la misma firma: §7 sigue PROPUESTA con
  gate propio; el contrato espera T4.6). Precisión del mismo acto:
  `registrar_primer_ingreso` estampa SOLO con `estado='activo'` (la
  ceremonia es de la fase 4 — el CONTRATO §9.4 ya lleva la condición).

## T4.1 — LA WHITELIST DE LA SEDE (hecho PRIMERO — EL DESBLOQUEO DE B)

Commit propio, primera pieza de la tanda: `actualizarPerfilPrestador`
gana `direccion · ciudad · sector · lat · lon · radio_cobertura_km`
(clave por clave, como todo lo demás), con las dos leyes de A4 rigiendo:
**par-o-rebota** (`coordenadas_invalidas`, rango validado; radio entero
1–500 o `radio_invalido`) y **la coordenada muere con el texto** (viaja
`direccion` sin lat/lon ⇒ el wrapper escribe NULL/NULL). Además el shape
de R1 ganó `sector/lat/lon/radio_cobertura_km` — el editor de B necesita
LEER lo que la whitelist deja escribir. Los códigos nuevos viven en un
union propio del camino de escritura (`CodigoErrorPerfilPrestador`) para
no ensanchar el de lectura que `fees.ts` subsume. Typecheck
api/prestador/cliente VERDES. **B: tu desbloqueo está en `main`, tipos
exportados desde `@epetplace/api`.**

## T4.2 — LAS TRES MEDICIONES (literal)

**a) `prestadores.estado`:** CHECK `pendiente · en_revision · activo ·
suspendido · rechazado` (5 valores). Filas: activo=4 (Aurora, Satori,
Wizard, Paseos Andres) · en_revision=1 (Carlos). **Transiciones de
estado: NINGUNA función viva las escribe** (censo por prosrc, caveat
L-170 declarado — los matches encontrados tocan otras columnas:
calificación, modo_horarios, INSERT inicial). Los `aprobado_por/en` de
las filas viejas salieron del portal admin legado. `activar_prestador`
(T4.4) es el primer escritor del monorepo.

**b) ¿El titular 'pendiente' puede editar su fila? SÍ — la sala de
espera FUNCIONA de motor HOY.** `prestador_own_profile` (ALL,
`user_id=auth.uid() OR is_admin()`) NO condiciona por estado; y el
trigger D-389 (`_prestadores_protege_columnas`, body leído) le VEDA
exactamente `estado/aprobado_*/motivo_rechazo/métricas/identidad` — el
pendiente edita su sede y NO puede auto-activarse. El gobierno ya estaba
sano; nada que curar.

**c) `aceptar_invitacion_pendiente_login` (body entero leído) — lo
MINADO:** resolución de identidad por EMAIL contra `auth.users` ·
defensa self-only · concesiones idempotentes con `ON CONFLICT`
(empleado_roles) · puerta única declarada. Ese patrón es el esqueleto de
`invitar_prestador`; no nació otro camino de invitación.

**Extra medido para el motor:** `uq_cuentas_owner_profile` (UNA cuenta
por humano) · `uq_cuenta_identificacion_pais` · `tipo_fiscal_enum` (4
valores) · `chk_datos_bancarios_validos` — **el motor ya dice la frase
de §4 de la letra**: `pendiente_validacion` vive con `{}`; `activa`
exige las 7 claves bancarias.

## T4.3 — `LETRA_ALTA_S79` v1.0 DEPOSITADA

Contraste MODELO_PRODUCTO/PORTAL adentro (§0). Registro por INVITACIÓN ·
las CUATRO FASES con su motor medido por fila · el checklist corregido
(lo que exige con sus rebotes tipados / lo que NO exige con su porqué —
"un checklist que pidiera lo que la sala de espera no puede dar sería
una puerta que nunca abre" — y la consecuencia declarada CORRECTA) ·
bancarios = requisito de COBRO con el literal del CHECK que ya lo dice ·
el propósito y la dirección de envío entran por la APLICACIÓN (§5, con
la pieza 5 del contrato cubriéndolos y el aviso de que `invitar` gana
los dos params en T4.6) · **el portal admin NO nace** (§6, disparo
verbatim del founder: la TERCERA operación admin, o el día que las opere
alguien que no sea el founder).

## T4.4 — EL MOTOR DEL ALTA (migración `20260727180000`, APLICADA)

`invitar_prestador` (7 params; email→auth.users o `usuario_no_registrado`;
espejos hablados de los únicos: `ya_es_prestador`/`ya_tiene_cuenta`/
`identificacion_en_uso`; **radio NULL EXPLÍCITO** — pisa el DEFAULT 5
todavía vivo; whatsapp `''` legacy relevado; cuenta_roles + user_roles
idempotentes, espejo del wizard) · `activar_prestador` (veredictos
`activo`/`rechazado`; el checklist §3 en orden: `direccion_sin_coordenadas`
→ `radio_no_declarado` → credencial médica `verificacion_profesional_
pendiente` con el MISMO nombre del trigger S68; rechazo exige
`motivo_requerido`; activo escribe `aprobado_por/aprobado_en`). L-140:
REVOKE/GRANT + proacl verificado en la migración. Cero wrapper TS, por
diseño (§6).

**Fixture 10/10 con ROLLBACK y residuo 0** (5 prestadores, 5 cuentas,
cero filas de los invitados del fixture): no-admin rebota · invitado nace
`pendiente` con radio NULL y cuenta creada · sin geo rebota · sin radio
rebota · checklist completo ACTIVA con `aprobado_por`=founder · médico
sin credencial rebota · rechazo sin motivo rebota · rechazo con motivo
escribe · veredicto ilegal rebota · re-invitar rebota.

## T4.5 — LA REGLA DEL CANON

En la skill `epetplace-db` (sección nueva, antes de SECURITY DEFINER):
**`prestadores` pasa a privilegios por COLUMNA ⇒ toda columna nueva nace
SIN grant y es invisible para `authenticated` hasta que la migración que
la crea se lo dé — o decida a propósito no dárselo.** Con el síntoma
("la columna nueva no lee" = falta grant, no RLS), el diagnóstico
(`has_column_privilege`) y la nota de vigencia (rige desde que el
CONTRATO aplique). `gen:types` en sync (las 2 RPCs en los tipos).

## T4.6 — EL FRENO (acá termina la tanda)

**Condición única restante: el GATE EN DISPOSITIVO de la captura de la
sede de B con el founder.** Cuando la mesa avise que pasó, la secuencia
ya escrita: ① re-leer los bodies vivos de `crear_prestador_inicial` y
`obtener_paseadores_disponibles` (el preámbulo del contrato lo manda) ·
② aplicar el CONTRATO entero (las 5 piezas, columna incluida) · ③
`gen:types` + fixture §9.6 · ④ **las TRES lectoras hermanas ganan el
mismo AND EN LA MISMA TANDA** (leyendo cada body antes, L-141 — sin
ventana en que paseo filtre y los otros tres no) · ⑤ `invitar_prestador`
gana `p_proposito`/`p_direccion_envio` (LETRA_ALTA §5, DROP+CREATE).
