# S79-A · Tanda 2 — MOTOR Y LETRAS (acta, 27 Jul 2026)

Escritor único de DB y docs. 76(f2) rigió (todo commit con `add` acotado +
`--only` por ruta). CERO push. 76(g) declarada ANTES de cada migración
(las tres: NO RIGE — DDL sin backfill, sin anclas; declarado en cada
header). **Toda reversa escrita ANTES de aplicar.**

## EL GATE, HONRADO EN SU LETRA

*"La letra se firma antes de que exista una columna."* Al cierre de esta
tanda, **cero columnas nuevas existen** — medible contra
`information_schema`. Todo el DDL de la letra (proposito · direccion_envio ·
primer_ingreso_en · caída del DEFAULT 5 · el AND geográfico de la lectora ·
`registrar_primer_ingreso`) vive como **CONTRATO fuera de
`supabase/migrations/`** (precedente S78), con su reversa al lado:
- `2026-07-27-s79a-CONTRATO-letra-perfil.sql` · `…-REVERSA-letra-perfil.sql`

**UNA decisión de mesa declarada para revisión del founder:** el ensanche
de `guardar_direccion_hogar` (p_lat/p_lon) SÍ se aplicó pre-firma
(migración `20260727170000`) — crea CERO columnas (lat/lon existen desde
S56) y era condición de que A4 (mandatada esta tanda) funcionara. Su
corolario ("la coordenada muere con el texto") es letra §2.2; si la firma
lo enmienda, la reversa está escrita y revertir no pierde datos.

## LO HECHO, POR PUNTO

**A0 (`85ef2a1`)** — `v_prestadores_publicos`: `security_invoker=on` +
REVOKE ALL a `anon` (migración `20260727150000`, verificación imperativa
adentro). Sonda en ambos sentidos: **anon `permission denied` ·
authenticated ve las MISMAS 4 filas de activos** (la RLS
`prestadores_public` coincide con el WHERE de la vista — cero regresión).
NO se dropeó. **D-471 ENMENDADA:** el legado NO está desplegado (palabra
del founder) — el disparo "apagado definitivo" queda satisfecho por
declaración; el DROP sigue siendo decisión aparte. **Censo admin: el
founder ES admin** (`admin_users`, uid `75d0798a…`, activo) — A3 tiene
ejecutor.

**A1 — `docs/LETRA_PERFIL_S79.md` v1.0 DEPOSITADA, ESPERA FIRMA.**
Contraste MODELO_PRODUCTO adentro (§0 y §8). Contenido: los CUATRO
registros con su régimen (el fiscal se ENLAZA jamás inline — D-517) ·
cae el DEFAULT 5 y el COALESCE (el 15 vive en el formulario) · LA FIRMA
sin-datos-sin-oferta con el fragmento SIN COALESCE (L-139) y la
transición del cliente declarada con disparo · `proposito` +
`direccion_envio` (en `prestadores`, con sus tres NO) · **el primer
ingreso diseñado para B** (§4: `primer_ingreso_en` + RPC idempotente que
devuelve `es_primer_ingreso` — la carrera se resuelve en la fila; solo el
TITULAR estampa) · **el puente ciudad→cat_ciudades NO NACE** (§5, tres
porqués + disparo) · credencial-de-persona como gate (§6) · vencimientos
como PROPUESTA (§7, cero código).

**A2 — Places, contrato primero.** `packages/api/src/wrappers/lugares.ts`
publicado ANTES de la implementación server (commit propio — B no espera):
`crearSesionLugares` · `buscarLugares` (predicciones SIN coordenadas a
propósito) · `resolverLugar` (el ÚNICO productor de lat/lon — forma
incompleta = `lugar_invalido`, jamás ok a medias). Edge Function
`lugares` DESPLEGADA (proxy server-side, sesión que cierra SIEMPRE con
Details, field mask mínimo `id,location,formattedAddress,addressComponents`,
ciudad de `locality`). **Verificado en vivo: 401 sin JWT · `503
sin_configuracion` con JWT** — el preparado-apagado dice su nombre.
**PRECONDICIÓN DEL FOUNDER (consola suya, D-289):** crear la key
server-side de Places y cargarla como secret
(`npx supabase secrets set GOOGLE_PLACES_API_KEY=…`). Hasta entonces las
dos capturas degradan a manual en silencio.

**A3 — el remate de documentos (migración `20260727160000`).** Mime types
en el bucket (jpeg/png/webp/heic/pdf) · CHECK `archivo_url` path-no-URL
(pre-verificado 0 URLs en las 7 filas) · **RPC
`revisar_documento_prestador`** gateada por `is_admin()` (veredictos
aprobado/rechazado; 'vencido' NO es veredicto — es del motor §7 propuesto;
sin deshacer a 'pendiente'; re-veredictar sí) · **higiene D-342 pagada en
esta tabla**: murió `pd_own`, queda UNA policy. **Fixture 6/6 con
ROLLBACK y residuo 0**: admin veredicta ✓ · fila con revisado_por/en ✓ ·
no-admin rebota `solo_admin` ✓ · `veredicto_invalido` ✓ · URL rebota en
el CHECK ✓ · el titular sigue leyendo SUS 2 filas post-higiene ✓.
Invocación para el founder (sin pantalla, por diseño): fixture-style en
el SQL editor — `set_config('request.jwt.claims', …su uid…)` + `SET LOCAL
ROLE authenticated` + `SELECT revisar_documento_prestador(id, 'aprobado',
null);` (el snippet exacto está en el patrón de memoria S78).

**A4 — la captura del hogar.** Motor (migración `20260727170000`):
`guardar_direccion_hogar` gana `p_lat/p_lon` — par-o-rebota
(`coordenadas_invalidas`), rango validado, y **NULL PISA** (§2.2).
Cambio de firma ⇒ DROP explícito (L-119) + ACL re-establecida (L-140,
sobrecargas=1 verificado). **Fixture 4/4, residuo 0** (la fila real del
founder quedó intacta: "Shyris y Suecia", lat/lon NULL). `gen:types` en
sync. Wrapper `direcciones.ts` ensanchado (lectura devuelve lat/lon;
código nuevo `coordenadas_invalidas`). UI: `DireccionHogarForm` (el
compartido — Cuenta y checkout ganan juntos) con predicciones en `Celda`
interactiva bajo el campo, resolución que llena dirección+ciudad, ayuda
"Ubicada en el mapa" SOLO cuando es verdad, y degradación silenciosa sin
key (Ley 23: la puerta no promete lo que no puede cumplir). Typecheck
`@epetplace/api` y `cliente` VERDES.

## OPERATIVO

- **3 migraciones aplicadas** (`20260727150000` vista · `160000`
  documentos · `170000` dirección hogar) — 76(g) NO RIGE declarada en
  las tres; reversas en `docs/relevamientos/2026-07-27-s79a-REVERSA-*.sql`
  (3 aplicables + 1 del contrato gated).
- **Deudas:** D-471 enmendada (legado no desplegado) · D-342 pagada en
  `prestador_documentos` · **D-556 NUEVA** (buckets públicos heredados +
  policies "Admin" de adopcion-fotos que no gatean admin).
- **Commits A de la tanda:** `a34d707` (reporte T1) · `85ef2a1` (A0) ·
  letra+contrato · contrato lugares · edge function · A3 · A4-motor ·
  fix tipos · A4-UI · este acta. CERO push.

## LO QUE ESPERA (en orden)

1. **La FIRMA del founder sobre LETRA_PERFIL_S79** → se aplica el
   CONTRATO (columnas + radio + lectora de paseo + primer ingreso),
   `gen:types`, fixture §9.5, y las TRES lectoras hermanas
   (grooming/adiestramiento/vet) ganan el mismo AND leyendo cada body
   antes (L-141).
2. **El secret `GOOGLE_PLACES_API_KEY`** (founder, consola + CLI) → las
   capturas se encienden solas, cero deploy adicional.
3. **La propuesta de vencimientos** (letra §7) espera su gate.
4. **Para B:** el contrato `lugares.ts` está publicado y estable; el
   shape de `registrar_primer_ingreso` está declarado en la letra §4 —
   la RPC nace CON la firma, B puede construir contra el shape.
