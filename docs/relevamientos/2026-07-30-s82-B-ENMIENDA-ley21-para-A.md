# ENMIENDA A LA LEY 21 — para que A la deposite al cierre

**Firmada por el founder en galería (S82-B, 30-jul-2026).** Texto listo para
copiar a la skill `epetplace-design-system` (Ley 21) y a `DIRECCION_ARTE`.

## El texto de la enmienda

> **ENMIENDA S82 (FIRMADA en galería): el CTA primario del CLIENTE deja de
> anclar en tinta y pasa al ORO — `palette.ctaOro` `#FCBC1D`, con el label
> en TINTA.** Es **un solo color para los DOS temas**: el par que manda
> —label sobre fill— da **9.96 idéntico en claro y en oscuro**, y esa
> igualdad es lo que hace posible un color único. La mitad del PRESTADOR de
> la Ley 21 queda **INTACTA** (su CTA sigue anclado al oficio, tealDark, por
> `lightOficio`/`darkOficio`) y **memorial sigue SIEMPRE en tinta** por
> `getTheme` — las dos garantías viven en la fuente, no en la disciplina de
> cada pantalla. **Muere la frase "cada CTA nuevo del cliente nace negro".**

## Lo que la firma cierra, además

- **`#fff645` RETIRADO** como CTA oscuro: con un color para los dos temas
  dejó de hacer falta. **El estatuto solo-marca de la rampa del isotipo
  queda INTACTO — sin enmienda.**
- El oro vive en el hueco de **cuatro grados** entre el ámbar de alerta
  (H41.0) y la ventana prohibida del amarillo de marca (H45.2): H43 · S97%
  · L55%. **Lo que lo mantiene distinguible del alerta NO es el matiz** (a
  2-4° el ojo no separa) sino que el alerta vive como **tinte** y el CTA
  como **fill saturado** — eso lo protege **R20** en `verify:diseno`.
- Fill vs papel **1.55** · vs negro **11.97**: en oscuro el color carga la
  separación, en claro no. **La elevación del primario NO viaja con esta
  firma** — es firma aparte (cambiaría la anatomía del botón en las DOS
  apps, porque el prestador usa la misma variante con su teal).

## Alcance medido (lo que cambia al encenderlo)

**43 instancias de `Boton variante="primario"` en ~20 pantallas del
cliente** — Agendar un paseo · Ver quién puede · los checkouts (el pago
simulado) de los cuatro oficios · index · login · registro · carnet · el
detalle del paseo en vivo · las portadas y `disponibles` de cada oficio ·
las Hojas de decisión. **Prestador: CERO.**

## Reversa (una línea)

`accent.cta` a `palette.textLight0` en claro y `palette.textDark0` en
oscuro, con sus `ctaTexto` respectivos.
