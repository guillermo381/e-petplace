# S90 · PISTA D — CIERRE Y VOLCADO

> **Qué es este archivo:** lo que vivía SOLO en el contexto de la pista D antes
> de compactar. Está escrito para que S91 no re-mida nada de lo que ya se midió,
> y para que nadie cure un hueco que no existe.
>
> **Estado al escribir:** rama `pista/s90-d` **fast-forwardeada a `origin/main`
> (`eecfe559`)**. Árbol limpio. **Todo el trabajo de D ya está mergeado y
> APLICADO por A** — este archivo NO pide nada de código.

---

## ① EL ESTADO REAL DE LA RAMA — verificado, no recordado

**UN commit propio: `074ba693`**
`certificado(S90-D): EL PAPEL QUE CERTIFICA UN JUICIO, NO UN DATO — …`

```
git merge-base --is-ancestor 074ba693 origin/main   → 0 (SÍ está en origin/main)
git log main..HEAD                                   → vacío
git status --porcelain                               → vacío
```

**Lo trajo A** en `aaab1329 merge(S90): pista D`. Después lo completó en
`eef1e67f` (EF desplegada + cura del gate roto) y `b9c26f0a` (folio + marca de
agua montada en mi punto de montaje).

### Mi lista real de archivos (76h) — 17, con lo que A hizo con cada uno

| archivo | mío | estado hoy |
|---|---|---|
| `apps/prestador/src/app/veterinaria/certificado/[citaId].tsx` | A (nuevo) | **verbatim** |
| `apps/prestador/src/app/veterinaria/cita/[citaId].tsx` | M | verbatim (entrada) |
| `apps/prestador/src/app/veterinaria/consulta/[citaId].tsx` | M | verbatim (entrada) |
| `apps/prestador/src/i18n/es.ts` · `en.ts` | M | verbatim (namespace `certificado.*`) |
| `packages/api/src/wrappers/certificados.ts` | A (nuevo) | **A lo ratificó** (era pedido) |
| `packages/api/src/wrappers/documentos.ts` | M | **A lo reescribió** — ver ③ |
| `packages/api/src/index.ts` | M | A lo mantuvo + sumó lo suyo |
| `docs/relevamientos/2026-08-07-s90d-SQL-PARA-A-certificado.sql` | A (nuevo) | **consumido y adaptado** por A |
| `docs/relevamientos/2026-08-07-s90d-REVERSA-certificado.sql` | A (nuevo) | A la adaptó, nota de datos intacta |
| `supabase/functions/documento-certificado/{index,render,deps}.ts` | A (nuevo) | **desplegada**; A montó `marcaDeAgua` |
| `scripts/verify-certificado-render.mjs` | A (nuevo) | **verde 3/3 hoy**, con la marca de agua de A adentro |
| `scripts/capturas/s90-d-certificado-{viaje,constancia-memorial,datos-minimos}.pdf` | A (nuevo) | evidencia del gate impreso |

---

## ② SQL QUE LE DEBA A A: **NINGUNO. Ya viajó y está aplicado.**

`2026-08-07-s90d-SQL-PARA-A-certificado.sql` está en el repo completo y A lo
aplicó **adaptado** en la migración `20260807110000`. **No queda SQL en mi
cabeza.** Lo que A cambió de mi original, y por qué importa saberlo:

| lo que yo escribí | lo que rige hoy |
|---|---|
| `documento_token.referencia_id` | **`ref_id`** — el catálogo de A ya usaba ese nombre |
| CHECK enumerado de tipos | **FK a `cat_documentos_mascota`** (`requiere_ref`) |
| `emitir_token_documento(p_mascota_id, p_tipo, p_referencia_id)` | **`p_ref`**, y devuelve además `funcion` |
| `PAPELES_POR_ACTO` (Set local) | **`TipoDocumentoExpediente` / `TipoDocumentoActo`** — mi letra, mejor forma |

**Mi semántica sobrevivió entera**, incluido el rebote
`referencia_no_es_de_la_mascota`. La tabla `certificado_salud`, sus tres gates
y sus cinturones entraron verbatim.

> **⚠️ Y el atrape que dejó A, que es una lección en carne:** mi cinturón (d)
> —el que prohíbe usar `_empleado_matricula_ok` en la emisión— lee `prosrc` con
> `LIKE` y **se disparó contra MI PROPIO COMENTARIO** que nombraba el helper
> vetado. **L-170 exacta: `prosrc` lee los comentarios como código.** A
> reescribió el comentario, **jamás el cinturón** — que es la resolución
> correcta. Si S91 escribe otro cinturón por `prosrc`, que no nombre en un
> comentario lo que el cinturón prohíbe.

---

## ③ LO QUE QUEDA A MEDIAS, NOMBRADO (regla 77)

### 3.1 · El gate impreso del certificado — **de FOUNDER**
Los tres PDFs están en `scripts/capturas/s90-d-certificado-*.pdf`. **Lo que
falta:** imprimirlos en papel real y mirarlos. **Dos cosas que solo el papel
contesta:** ① la opacidad de la marca de agua (6%, A la declaró **no verificada
en papel** — ajuste de UNA línea en `_shared/papel.ts`) · ② si el folio
`F-2026-NNNNNN` en mono da confianza o estorba.
⚠️ **Los tres PDFs del repo son PRE-folio y PRE-marca de agua** — se
regeneran con `node scripts/verify-certificado-render.mjs` (ver ④).

### 3.2 · El QR de verificación — **de MESA, y es lo más urgente del papel**
El certificado sale **sin verificación pública, y lo dice impreso**. Fase 1
(folio) la construyó A; **fase 2 (la landing que responde por folio) no
existe**. **Este es el más falsificable de los cinco papeles**: lleva nombre y
matrícula de un profesional real y nada que lo compruebe. Se decide **con el
papel impreso en la mano**, no antes.

### 3.3 · La receta en la lista del cliente — **de C** (declarado por A)
`receta` quedó en `TipoDocumentoExpediente` porque su fila ya vivía en la lista
de botones, **pero descargarla exige elegir consulta**. Tensión de superficie
declarada, sin cura.

### 3.4 · D-678 — **sin dueño adjudicado** (ver ⑤.1)

---

## ④ LO QUE S91 NO DEBE RE-MEDIR

### 4.1 · El render del certificado corre HOY, sin DB y sin desplegar
```
PDFLIB_DIR=<ruta>/node_modules node scripts/verify-certificado-render.mjs
```
**Verde 3/3 medido hoy**, ya con la marca de agua de A adentro. `pdf-lib` **no
es dependencia del monorepo** (vive en el runtime de la EF) — se instala fuera
del repo y se pasa por `PDFLIB_DIR`; el script lo dice en su error.
**Corre el archivo REAL** (`render.ts`) vía un hook de módulos que mapea
`npm:pdf-lib@…` → `pdf-lib`. **No es una copia:** si alguien cambia el papel,
esto lo mide.

### 4.2 · Ver un PDF sin abrir la app
`qlmanage -t -s 1400 -o <dir> <archivo>.pdf` → PNG mirable. Así encontré que el
filete magenta cortaba las mayúsculas del título a +16pt (hoy +28).

### 4.3 · D-379 — **la parte que la lámina de A NO tiene**
> La lámina `docs/laminas/LAMINA_ALTA_MASCOTA_S91.md` ya cubre: cero tablas de
> raza · `mascotas.raza` text · las dos RPCs del dueño sin raza · el bucket
> `especies-razas` con 111 objetos. **Todo eso es correcto y coincide con lo que
> medí.** Lo de abajo es lo que **no** está ahí.

**(a) Las SIETE columnas «raza» de la DB** — no es solo `mascotas`:
`mascotas.raza` (text) · `mascotas_adopcion.raza` (text) ·
`criadero_camadas.raza` (text) · **`criaderos.razas` (jsonb)** ·
`v_criaderos_publicos.razas` (jsonb) · `lista_espera.raza_preferida` (text) ·
`prestador_atencion_log.pet_raza` (text).
*Un catálogo de razas tiene siete consumidores potenciales, no uno.*

**(b) EL PRESTADOR SÍ CAPTURA RAZA; EL DUEÑO NO.** Las TRES RPCs que escriben
`mascotas.raza` (medido por `prosrc`):
`crear_mascota_walkin` · `crear_alta_asistida_pendiente` ·
`crear_alta_asistida_existente` — las tres con `p_raza text DEFAULT NULL`.
⇒ *el hueco de D-379 es del alta del DUEÑO, no del alta en general.*

**(c) Datos vivos:** 20 mascotas · **5 con raza** · 4 distintas
(`Beagle · Labrador · Mestizo · Schnauzer miniatura`) · **las 20 son `perro`**.
Sin FK y **sin CHECK** sobre `raza`.

**(d) Talla y pelaje — quién los escribe, medido:**
`declarar_talla_pelaje(p_mascota_id, p_talla, p_pelaje)`, wrapper
`declararTallaPelaje` (`grooming-reserva.ts:449`), **única superficie**
`apps/cliente/src/components/talla-pelaje-hoja.tsx:71`. **Los declara la
FAMILIA**, al elegir mascota en la reserva de grooming. **7 de 20** con talla,
**7 de 20** con pelaje. Sus CHECKs:
`talla IS NULL OR talla IN ('S','M','L')` · `pelaje IS NULL OR pelaje IN ('normal','largo')`

**(e) Hay un catálogo de pelaje que el perfil NO consume:** `cat_tipos_pelaje`
tiene **7 filas** (`corto_liso · corto_duro · mediano · largo · doble_capa ·
rizado · mixto`) y `mascotas.pelaje` acepta **dos** valores. *Si D-379 pre-llena
pelaje desde la raza, la pregunta «¿contra cuál de los dos vocabularios?» ya
está planteada por el objeto.*

**(f) `cat_especies` — 11 filas.** Activas 6: `perro`(A) `gato`(A) `conejo`(B)
`ave`(B) `roedor`(C) `pez`(C). **Inactivas 5: `reptil`(B) `huron`(B)
`cobaya`(B) `otro`(D) `equino`.**
⚠️ **`reptil` es el único cruce divergente** con las siete carpetas nombradas
por la mesa: existe como carpeta y está `activo=false` en DB. *(A lo confirmó
por otro camino: «reptil sin genérico VERIFICADO 400 = firma y no olvido».)*

**(g) ⚠️ EL TIPEO PREDICTIVO QUE LA LÁMINA PIDE **NO EXISTE COMO COMPONENTE**.**
Cero matches de `autocomplet|sugerencia|predictiv|typeahead` en
`packages/ui/src/` (el único es un comentario en `Hoja.tsx`). Los selectores
que hay: `Campo · CampoCodigo · CampoFecha · FiltroPills · SelectorAvatar ·
SelectorDia · SelectorEspecie · SelectorOpcion · SelectorSegmentado`.
**El patrón existe y está probado, pero INLINE** en
`apps/cliente/src/app/(tabs)/hogar/bitacora.tsx` (350 líneas):
`palabrasDe()` normaliza (líneas 62-63) · `const sugerencias = useMemo` (145) ·
render = `Campo` (316) + `SelectorOpcion opciones={sugerencias…}` (331).
⇒ **quien construya el paso 2/4 de la lámina o promueve esa pieza a
`packages/ui`, o la clona. No hay tercera opción, y clonarla es lo que §6
prohíbe.**

**(h) Ninguna superficie pinta imagen por especie o raza — medido HOY, post-A:**
`grep especies-razas apps packages` = **0** (la galería está sembrada y **sin
consumidor**). `AvatarMascota` recibe `especie` y su JSDoc línea 59 sigue
diciendo *«Hoy no cambia el render; el set ilustrado por especie la consumirá
(D-288)»*. `SelectorEspecie.tsx:123` monta `AvatarMascota` — **mismo render para
las seis**.

**Comandos, para re-correr cualquiera de estos:**
```sql
-- columnas raza
SELECT table_name||'.'||column_name||':'||data_type FROM information_schema.columns
WHERE table_schema='public' AND column_name ILIKE '%raza%';
-- quién escribe raza
SELECT proname FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
WHERE n.nspname='public' AND p.prosrc ILIKE '%raza%'
  AND (p.prosrc ILIKE '%INSERT INTO mascotas%' OR p.prosrc ILIKE '%UPDATE mascotas%');
```

---

## ⑤ HALLAZGOS SIN DUEÑO — con su etiqueta

### 5.1 · **HALLAZGO PROBADO** — D-678, las dos mediciones que la ficha no tiene
La ficha `D-678` (línea 8989 de DEUDAS) dice el QUÉ y el disparo, **pero no
lleva los números de la enmienda del founder del 7-ago** (nombre + fondo verde
oscuro + imagen de fondo). Los mido acá para que no se pierdan:

**El fondo** — contraste contra `#050508` (el negro del cliente) es el eje real:

| hex | luminancia | vs blanco | **vs #050508** |
|---|---|---|---|
| `#0F5E56` *(«muy claro» — founder)* | .0878 | 7.62 | **2.67** |
| `#0C4F48` | .0614 | 9.43 | 2.16 |
| `#0A4A44` | .0538 | 10.12 | 2.01 |
| **`#0A3F38`** *(base propuesta)* | .0391 | 11.79 | **1.73** |
| `#083530` | .0281 | 13.44 | 1.51 |
| `#050508` *(«lo veo negro»)* | .0016 | 20.36 | 1.00 |

**El mecanismo — verificado contra el objeto**, no contra la doc:
`node_modules/@expo/config-types/build/ExpoConfig.d.ts:521` →
`android.adaptiveIcon.backgroundImage`, y su JSDoc (línea 519) dice literal:
**«overrides the `backgroundColor` key. Must have the same dimensions as
`foregroundImage`»**. Con el isotipo a 168 de 432 firmado en S89, **el fondo va
a 432×432**. Sin imagen, `backgroundColor` solo admite un plano — el founder
tiene razón en pedirlo así o vuelve plano.

**El nombre:** `apps/prestador/app.json:3` dice `"ePetPlace Pro"` (13 chars).
`ePetPlace Care` son 14. *No tengo veredicto sobre «Care» a secas — es decisión
de marca.*

**Sin dueño:** toca `app.json` + `assets/images/`. La identidad del launcher la
construyó A (S89-A orden 17, `e94b153a`). **Lo adjudica la mesa.**

### 5.2 · **HALLAZGO PROBADO** — `.rpc()` NO valida los nombres de parámetro
Probado con sonda y su rojo producido: agregué
`p_parametro_inventado_que_no_existe: 42` a una llamada a
`emitir_token_documento` (cuyos `Args` generados son `{p_mascota_id, p_tipo}`)
y **`tsc` no dijo nada** — con cast y sin cast (grep del archivo sonda en la
salida: 0 matches, dos corridas).
**Consecuencia:** un wrapper puede mandar un `p_` inexistente o mal escrito y
el typecheck lo deja pasar; el error aparece en runtime o —peor— resuelve otra
sobrecarga. **Familia L-192: una verificación cuyo modo de falla es el
silencio.** Nadie lo tiene fichado. *(Un guard barato sería cruzar los
literales `p_*` de los wrappers contra `database.types.ts`.)*

### 5.3 · **HALLAZGO PROBADO** — el lint no corre en un worktree con `node_modules` prestado
`scripts/verify-diseno.mjs` invoca su volcador con **`pnpm exec tsx`**, y en un
worktree cuyo `node_modules` apunta al del repo principal pnpm imprime su
banner `Scope: all 7 workspace projects` **en stdout**, que rompe el `JSON.parse`
del volcador → **R12 y R15 salen en ROJO por no poder medir** (correcto por
L-197, pero la razón no es la suya).
**Baseline medido:** `main` = **VERDE 25/25**; mi worktree = 23 verdes + esos 2.
**Cura de mi lado que SÍ funcionó** (y que S91 debería hacer al abrir worktree):
el `node_modules` de la raíz **no puede ser un symlink** — tiene que ser un
directorio real de symlinks a las entradas del principal (`.bin`, `.pnpm`,
`.modules.yaml` incluidos), porque `.gitignore` usa `node_modules/` con barra y
**un symlink no matchea esa forma** ⇒ el árbol sale sucio y el ancla saldría con
asterisco.
*(A/B: `31422591` menciona «la cascada del volcador con tres runners
declarados» — puede que ya esté curado; **no lo re-medí**.)*

**⇒ CUARTA PIEZA del arranque de worktree**, junto a `node_modules`,
`.env.local` y `supabase/.temp` (candidata ① del método): **los
`node_modules` por paquete también hay que rehacerlos**, porque el symlink
`@epetplace/api → ../../../../packages/api` es RELATIVO y desde un
`node_modules` prestado **resuelve al repo principal** — el worktree
typechequea contra el código de otro. Lo pagué: 6 errores fantasma de
«has no exported member» sobre símbolos que sí existían.

### 5.4 · **SOSPECHA, NO PROBADA** — el `cat_documentos_mascota` que apareció solo
A las ~08:10 medí `to_regclass('public.cat_documentos_mascota')` → **NO EXISTE**.
A las ~08:50, en la misma sesión, → **existe con 5 filas**. **La explicación
obvia es que A aplicó su migración en el medio, y es casi seguro** — pero **no
lo verifiqué contra `supabase migration list`**. Lo anoto como **L-166 en vivo**:
*la ventana de invalidación de un dato de DB, en sesión paralela, es de minutos.*

### 5.5 · **HALLAZGO PROBADO, YA CURADO POR A** — el gate roto de `asignar_cita_a_persona`
Reporté que la función referenciaba `v_cita.tipo_servicio` sin declarar `v_cita`
(usa `v_tipo`). **A lo confirmó y curó** (`20260807120000`): *«plpgsql resuelve
al ejecutar, así que se creó verde y CRASHEABA TODA ASIGNACIÓN que pasara los
gates 1-5»*. **Se registra acá solo para que nadie lo vuelva a "encontrar".**

---

## ⑥ DISCRIMINADORES Y DATOS DE FIXTURE — reales, hoy

Reconstruirlos cuesta más que escribirlos.

**El discriminador del gate de matrícula (el que prueba que NO es el de citas):**
al 7-ago hay **0 empleados con `matricula_profesional`** y **3 con chip médico**
(los de Clínica Aurora). ⇒ si la emisión del certificado usara
`_empleado_matricula_ok`, **los tres pasarían por gracia** y el papel saldría con
la línea de matrícula vacía. *Ese es el par: mismos datos, un helper vs el otro.*

**Las 4 historias clínicas vivas** (todas de la MISMA mascota
`d2e31d70-54fc-4d47-b425-1617239257eb`), útiles para cualquier papel clínico:

| cita_id | empleado_id | veterinario_user_id |
|---|---|---|
| `5b701b81-9b00-4808-94a8-ae2e55187029` | `c59827f4-31f5-4558-a654-53cddef116d7` | `c5d54e3a-cf1a-45c6-8605-dfd826b022ee` |
| `bd5f2e19-17c5-40e5-b371-c6dcb92cc4b1` | `de680000-0000-4000-8000-0000000000ee` | `4f572081-26a5-4d3b-9d80-25ea751fdc9c` |
| `435e2538-e3cf-40a9-aaad-41a230f7ed0f` | `afdc7fb9-56f3-44cc-9432-7e49880f6ff5` | `c8429100-82a2-4430-ba4b-0e316e2b5374` |
| `e184d752-2fd4-4297-af17-436ba6a1099e` | `afdc7fb9-56f3-44cc-9432-7e49880f6ff5` | `c8429100-82a2-4430-ba4b-0e316e2b5374` |

**El emisor real, con su hueco:** Clínica Aurora →
`direccion='Av. de los Andes 123'` · **`telefono=NULL`** · **`matricula_profesional=NULL`**.
*El papel omite la línea del teléfono en vez de imprimir un guion — pero un
certificado real de esa clínica hoy sale sin teléfono de contacto.*

**El memorial no se puede probar con datos vivos:** las 20 mascotas están en
`estado_vida='activa'`. El gate (`<> 'activa'` ⇒ solo `constancia`) **solo tiene
fixture in-txn**, nunca un caso real. Es el brazo (3) del fixture del SQL.

---

## ⑦ HIPÓTESIS QUE DEJÉ ABIERTAS, y por qué

1. **¿El SQL parsea?** No lo corrí contra ningún servidor: correrlo in-txn habría
   tomado un `ACCESS EXCLUSIVE` sobre `documento_token` **en producción con A
   trabajando**. **Resuelta por A al aplicarlo.**
2. **¿La EF compila en Deno?** No hay `deno` instalado en esta máquina. Verifiqué
   la composición del PDF en Node con el hook; el `Deno.serve` y el cliente de
   Supabase quedaron sin ejecutar. **Resuelta por A: desplegada y E2E real 200.**
3. **¿«Care» a secas es el nombre correcto?** No la cerré **a propósito**:
   es decisión de marca y mi orden dice hallazgos, nunca veredictos.
4. **¿Las siete carpetas de la galería casan una a una con los códigos?**
   Cerrada en parte: los siete conceptos casan, `reptil` diverge en estado.
   **Lo que NO verifiqué es el CONTENIDO** de las carpetas — no estaban en el
   workspace cuando medí (`find` a 6 niveles sobre `/proyectos/ePetPlace` = 0
   directorios). **A las encontró después, en el bucket `especies-razas`.**
   ⇒ *mi «no existen» era cierto del DISCO y falso del BUCKET. Si S91 lee mi
   medición sola, va a concluir mal: la de A manda.*

---

## ⑧ LO QUE NECESITO DE A

**Nada de código. Todo lo mío ya está mergeado, aplicado y desplegado.**

- **SQL a aplicar:** ninguno.
- **Commits a mergear:** ninguno — `074ba693` ya es ancestro de `origin/main`.
- **Texto a depositar en `docs/`:** **este archivo**, y de él solo dos cosas
  piden ficha propia, si la mesa las quiere:
  ① **⑤.2** (`.rpc()` no valida nombres de parámetro) — no tiene número.
  ② **⑤.3 + la 4ª pieza del worktree** — al `METODO_TRES_PISTAS` o a B, que
  es la dueña del lint.
- **Y una enmienda barata, si A la quiere:** sumarle a la ficha **D-678** las
  dos mediciones de ⑤.1 (la tabla de hexes y la restricción de
  `backgroundImage`), para que la próxima build no las re-mida.
