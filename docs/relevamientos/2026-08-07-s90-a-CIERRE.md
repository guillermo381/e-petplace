# S90 · PISTA A — CIERRE Y VOLCADO (7-ago-2026)

> Volcado pre-compact. **Nada de lo de abajo es construcción nueva**: es lo
> que solo vivía en el contexto de A y ahora vive acá. A ES la escritora —
> los depósitos grandes ya están en disco (migraciones · reversas · reportes
> · lámina · deudas); esto captura el RESTO.

---

## ① LO QUE SOLO ESTABA EN LA CABEZA DE A

### El arco del push, con sus literales (no está en ningún otro doc)

**La prueba dirigida del CLIENTE (SQL de C, aplicado verbatim):** intención
`paquete_vence` sobre el bono vivo de Thor → `nacida`/`push`, id
`9203d56c-f98f-4a1c-8277-9b16851de85c`, voz «Te quedan 8 salidas por usar».
El founder la despachó a mano y **el teléfono VIBRÓ** (gate:
`{"modo":"transporte_vivo","entregadas":1,"fallidas":0}`).
**La fila `clave_dedup='prueba-push-s90c-1'` SE QUEDA — firma founder,
cuarta vez pedido: NO SE BORRA, y no se vuelve a proponer.**

**El cron del vagón (literal de C, aplicado):** `despachar-push-tick`,
**job 8**, `* * * * *`, `net.http_post` a `despachar-push` con el Bearer
anon. Salud medida: 65/65 `succeeded` en la primera hora.

**El flip:** `cat_notificacion_canales.transporte_vivo` → push **true**
(~19:35 UTC). Primera hora medida: **0 intenciones push orgánicas, 0
diferidas por techo, 0 intenciones de cualquier canal** — cero de salud
(tick vivo), no de silencio roto.

**La prueba dirigida del PRESTADOR (SQL de A, patrón C):**
- Token del prestador llegó SOLO al aplicar el OTA `019fddbf`:
  `push_tokens` pasó de 1 a 2 a las 21:40:30. Fila nueva:
  `09c496b6-9dc8-48ca-b002-c2cc748529fa`, user `c5d54e3a-cf1a-45c6-8605-dfd826b022ee`
  (titular de **Paseos Andres**, prestador `de300000-0000-4000-8000-0000000000e5`).
  El token del cliente: `5edfa48f-2621-4b06-a4f4-3f3dd7d181d4` (user
  `dd024680-…`).
- Siembra: tipo `cita_solicitada` (audiencia prestador, fuera de sombra,
  consentimiento `preferencia_efectiva=true`, techo 3/20 en ventana) con la
  voz de `_voz_notificacion` alimentada de la cita VIVA
  `04667628-9590-462f-8cc4-7a168868746e` (Thor, paseo, confirmada) —
  «Te llegó una nueva reserva — Reservaron para Thor: 6 de agosto a las
  21:00». Canal forzado SOLO en esa fila
  (`forzado_por='prueba-dirigida-S90-A-prestador'`,
  `clave_dedup='prueba-push-s90a-prestador-1'`), intención
  `7282b9f1-a619-448b-93e5-4e0da803e454`.
- **Resultado del despacho: `estado=entregada`**, `despacho_en`
  2026-08-07T22:08:00Z, `gate_que_corto: null`, vía el tick (no a mano).
- **PENDIENTE (founder): confirmar la VIBRACIÓN del aparato del prestador.**

### El lote de papeles del gate (folios consumidos, no re-medir raro)

Los cinco entregados en `~/Desktop/papeles-s90-gate/` por el camino real:
folios **F-2026-000003..000006** (carnet Thor · HC Thor · receta Thor ·
ficha Zeus) + el certificado MUESTRA (F-2026-000000, datos ficticios,
copiado de `scripts/capturas/`). Los folios 000001/000002/000012 fueron
E2E de A y sus filas se limpiaron por id (huecos legales en la secuencia:
**los números no se reusan**). La secuencia `documento_folio_seq` va por ~12.

### GATE IMPRESO: **APROBADO, firma founder** (con dos correcciones YA ejecutadas)

- FIRMADO y no se toca: marca de agua al 6% (anotado en `papel.ts`) ·
  filete magenta · folio en mono · banda de emisor · procedencia fila por
  fila · título del certificado a 22pt.
- Corrección ① ejecutada: `AIRE_BAJO_FILETE = 10` en `_shared/papel.ts`
  (un solo lugar; el certificado ya traía su aire, no se tocó).
- Corrección ② ejecutada: el carnet ANEXA la foto del carnet escaneado AL
  FINAL, página propia por archivo, rótulo verbatim «Documento aportado por
  la familia — no verificado por e-PetPlace». Medido antes: la imagen SÍ se
  guardaba (`evento_vacuna_aplicada.archivo_url`, 32/32 filas, 5 archivos
  distintos, bucket `mascotas`; Thor tiene 1:
  `dd024680-…/carnet-1783633828265.jpg`). E2E: carnet de Thor a 2 páginas,
  202 KB, verificado visualmente.
- **D-677/D-681: el criterio de muerte está esencialmente cumplido** (gate
  impreso pasado + correcciones hechas); la muerte FORMAL espera el ojo del
  founder sobre la reimpresión con el aire — lo declara la mesa en la
  transposición, no esta pista sola.

### Discriminadores de fixture vivos (IDs reales que cuestan re-encontrar)

- Thor `d2e31d70-54fc-4d47-b425-1617239257eb` · Zeus
  `a3332037-c487-45c1-875f-83caf342f59e` · familia
  `ce057f90-82d8-40f8-a816-796c0f2b5b2a` · titular
  `dd024680-3d1c-4465-b38b-dedab45da037`.
- Citas CON medicación (las 2 recetas emitibles): `5b701b81-9b00-4808-94a8-ae2e55187029`
  (Paseos Andres · demo-prestador · 2 meds) y
  `bd5f2e19-17c5-40e5-b371-c6dcb92cc4b1` (Clínica Aurora · [DEMO S68] Dra.
  Aurora · 2 meds). **2/2 salen con fallback de negocio: 0 de 16 empleados
  con matrícula** (la captura de B viaja en el OTA 019fddbf).
- El arnés de `db query`: **auto-commita el batch** (el fixture SIN
  BEGIN/ROLLBACK escribe de verdad — el primer lote de tokens E2E venció a
  los 10 min entre turnos y pareció rollback: FALSO); **muestra solo las
  filas del ÚLTIMO statement**; una excepción en un DO **aborta el batch
  ruidosamente** (rojo producido y verificado dos veces).

## ② EL ESTADO DE LA RAMA — verificado por contenido

**Árbol LIMPIO. Los 15 commits de la tanda son ancestros de `origin/main`**
(verificado con `merge-base --is-ancestor`, hoy):
`71d61bd8` migración receta+catálogo · `4e81c179` plantilla+4 EFs ·
`eba533c9` wrapper catálogo · `e5f5e388` reporte cierre A ·
`22744ae4` voces firmadas (tripwire) · `90f32833` merge B (matrícula+invitación) ·
`aaab1329` merge D (certificado, adaptado) · `eef1e67f` EF certificado ref_id + cura asignar ·
`31422591` merge B2 (glifo cápsula + volcador + guardas cliente) ·
`b9c26f0a` orden 9 (fechas+folio+marca de agua en certificado) ·
`74cc8984` lámina S91 + D-288 origen-IA · `d2c0c051` D-684 koi ·
`44ae0ad6` lista derivada del catálogo vivo · `eecfe559` gate impreso ①②+lector C ·
`9707485e` wrapper obtenerConsultasConReceta.

**Migraciones: 209 locales, TODAS aplicadas y registradas** (la última:
`20260807150000`; verificar con `npx supabase migration list --linked`).
Deploys vivos: las 5 `documento-*` re-desplegadas hoy (carnet la más nueva,
con anexo).

**OTAs publicados por A hoy (guard verde los tres):**
- cliente `558ac817` / android `019fddbe…` (D-682 + guardas + voces) —
  SUPERSEDIDO por → cliente `a8745c47` / android `019fde4c-41fe-7af7-989e-2caa39e53fd7`
  (lista derivada del catálogo). Runtime 1.0.3. **El founder debe ver
  `update 019fde4c` en el pie.**
- prestador `67952f31` / android `019fddbf-0062-7e62-aa5d-f47db56012f6`
  (captura de matrícula + invitación de avisos + pantalla certificado).
  Runtime **1.0.4** — el APK 1.0.3 NO lo recibe (huérfanos declarados por
  el guard).

## ③ LO QUE QUEDA A MEDIAS, con dueño

| pendiente | qué falta | próximo paso de |
|---|---|---|
| Vibración del prestador | confirmar que sonó la dirigida `entregada` de las 22:08 | **founder** |
| Los 4 papeles en pantalla | pie `update 019fde4c` + ver las 4 filas | **founder** |
| Picker de consulta de la receta | superficie que elige la cita (lector + wrapper LISTOS) | **C** |
| Verificar/firmar adaptaciones de su SQL (lector §① y ⑤ del certificado) | «se verifica y se firma, o se revierte» | **C y D** |
| Glifo cápsula 'receta' | gate por ícono a 21px (construido, sin firma; el préstamo 'caso' rige) | **founder** |
| El glifo que FALTA (pigeonhole 4 filas/3 dibujos en cliente) | B lo dibuja (hoja de contacto) — la cápsula puede ser la mitad de la solución | **B + founder** |
| Superficie de certificados de la familia | propuesta servida (lista de N actos en el perfil) — candidato S91 | **mesa** |
| Primera receta con firmante REAL y primer certificado real | exigen matrícula cargada (la captura ya viaja en 019fddbf) | **prestadores** |
| Muerte formal D-677/D-681 | reimpresión con el aire + palabra founder; la mesa transpone | **mesa** |
| D-684 reemplazo del koi | dispara con el próximo lote de regeneración de la galería | (disparo) |
| Cron del correo vs push | los DOS ticks corren por minuto; ¿consolidar? — NO se midió, solo anotado | **mesa (S91)** |

## ④ LO QUE S91 NO DEBE RE-MEDIR (medido hoy, con su comando)

- **Catálogo de papeles**: 5 filas (`SELECT codigo, requiere_ref, orden FROM cat_documentos_mascota ORDER BY orden`) — carnet 10 · hc 20 · receta 30 (ref) · ficha 40 · certificado 50 (ref). **SIN columna categoria** (nace con el agrupador, D-683, papel 8).
- **`documento_token`**: FK al catálogo · `ref_id` · `folio` UNIQUE (F-YYYY-NNNNNN, secuencia global) · ilegible por PostgREST.
- **Las fechas de matrícula**: UNA — `_corte_matricula()` = 2026-08-15; exigido ⟺ nació desde el corte; los 16 existentes exentos PARA CITAS para siempre; papeles con gate propio. Cinturón anti-fecha-propia vivo.
- **Canales**: push/email/in_app `transporte_vivo=true`, whatsapp false. Cron jobs de push=8, correo existente aparte.
- **Galería especies-razas**: 111 objetos medidos (anexo de la lámina `docs/laminas/LAMINA_ALTA_MASCOTA_S91.md` — TODO el estado del alta está ahí: RPCs sin raza/origen, CHECK de 9 valores, `_mapeo.json` AUSENTE del repo).
- **Push**: 2 tokens (cliente + prestador titular de Paseos Andres).
- La app cliente deriva papeles del catálogo con respaldo de compilación (`apps/cliente/src/lib/papeles.ts::componerPapeles`).

## ⑤ HALLAZGOS SIN DUEÑO — probados vs sospechas

**PROBADO:**
- El arnés `db query` (semántica completa en ① — auto-commit, último
  statement, DO aborta). Vale para toda pista que lo use.
- `eas update:view` SIN `--json` calla `gitCommitHash` (reconfirmado hoy;
  es la candidata S88 ②). Con `--json` el shape no es el esperado por un
  `json.load` directo (mi parse devolvió `{}`) — **instrumentación pendiente**.
- El TTL de 10 min de los tokens puede vencer ENTRE TURNOS de una sesión de
  agente — un E2E de documentos emite y descarga EN EL MISMO turno.
- La cara del certificado (D) tiene título 22pt vs 16 de la espec — VISTO Y
  FIRMADO por el founder en el gate impreso: no se toca.
- `prestador_empleados` tiene `matricula_pais_emisor` (además de
  `matricula_profesional`).
- El pie de la plantilla corta por ancho (con folio desbordaba — curado).

**SOSPECHA (no leer como hallazgo):**
- Por qué el founder vio DOS papeles reportando `019fddbe`: nunca se probó;
  la explicación más plausible es mirada pre-aplicación del update. El
  código de ese bundle tenía las 4 filas (verificado por contenido en el
  árbol del ancla). Si con `019fde4c` siguiera viendo 2 → bug vivo real y
  se caza con captura de pantalla + forense.
- `net.http_post` del tick con el Bearer anon: funciona (65/65) — si la
  anon key rota, el tick muere en silencio HTTP. Nadie lo midió como
  riesgo; queda dicho.

## ⑥ AVISO A A (esta pista ES A — el aviso es para su próximo contexto)

**Nada quedó sin aplicar ni sin depositar: cero SQL en vuelo, cero texto
debido, cero commits sin mergear.** Lo que la A del próximo contexto hereda
como DEBERES: ① transponer el cierre S90 al canon (CLAUDE.md) con la mesa —
incluye la muerte formal de D-677/D-681, el estado del tren de push
(CERRADO salvo vibración del prestador), y el arco de papeles (gate impreso
PASADO) · ② los merges a demanda de B/C/D siguen siendo suyos · ③ la fila
`prueba-push-s90c-1` NO SE BORRA (cuarta vez: firmado).
