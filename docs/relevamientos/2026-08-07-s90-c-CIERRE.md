# S90 · PISTA C — CIERRE Y VOLCADO

> **Escrito el 7-ago-2026 antes del compact de C.** Todo lo de acá vivía SOLO
> en el contexto de C. **Cada número trae su comando o su ruta**, para que S91
> pueda re-medir en vez de creerme.
>
> ⚠️ **Este archivo está en `docs/`, que es territorio de A** (regla 76d). Se
> escribe acá **por orden expresa de la mesa** para el volcado de cierre — no
> es C tomando el territorio. **Todo lo demás que C le debe a `docs/` viaja
> como TEXTO en §1.5 y §1.6, para que lo deposite A.**

---

## 0 · ESTADO DE LA RAMA — medido, no recordado

```
rama            : main
árbol           : LIMPIO (git status --porcelain vacío)
HEAD            : eecfe559
origin/main     : eecfe559
pendiente push  : NADA (git log origin/main..HEAD vacío)
```

**Los DOS commits de C, verificados con `git merge-base --is-ancestor <sha> origin/main`:**

| hash | asunto | en origin/main |
|---|---|---|
| `a8c7758d` | `tren(S90-C): EL VAGÓN DEL TRANSPORTE — despachar-push contra FCM v1, la gemela del correo` | **✅ SÍ** |
| `14226835` | `craft(S90-C · D-682): LOS PAPELES GANAN SU CHEVRON — y la fila deja de dibujar su propia anatomía` | **✅ SÍ** |

**Lista real de archivos (76h), archivo por archivo:**

```
a8c7758d  supabase/functions/despachar-push/index.ts
          supabase/functions/despachar-push/fcm-oauth.ts
          supabase/functions/despachar-push/fixture-oauth.ts
14226835  apps/cliente/src/app/(tabs)/hogar/mascota/[mascotaId].tsx
          apps/cliente/src/components/fila-documento.tsx
```

**Nada más tocó C en el repo.** Cero binarios; la galería vive en Storage.

---

## 1 · EL SQL Y LOS TEXTOS QUE C LE DEBE A A — completos, sin referencias

### 1.1 · El CRON del vagón de push — **va ANTES del flip**

```sql
SELECT cron.schedule('despachar-push-tick', '* * * * *', $$
  SELECT net.http_post(
    url     := 'https://zyltipqscdsdsxnjclhp.supabase.co/functions/v1/despachar-push',
    headers := jsonb_build_object(
      'Content-Type', 'application/json',
      'Authorization', 'Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inp5bHRpcHFzY2RzZHN4bmpjbGhwIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzY4MDMxMDYsImV4cCI6MjA5MjM3OTEwNn0.kvHD9-JvaGytu0a7kAwgTyVXExrhIaGg1Z8_-99SOxA'),
    body    := '{"origen":"pg_cron"}'::jsonb
  );
$$);
```

> **POR QUÉ EXISTE, y es lo que no se puede perder:** `despachar-push`
> **NO llama** a `despachar_notificaciones` a propósito — dos llamadores compiten
> por el FUSIBLE del techo duro (un `UPDATE` que apaga el despacho global).
> **Consecuencia declarada: hoy push depende del tick del correo** (`cron.job`
> jobid 6). **Si ese tick se apaga, push deja de entregar sin dar rojo.** Este
> cron lo independiza.

### 1.2 · El FLIP — el ÚLTIMO acto, después del gate del founder

```sql
UPDATE public.cat_notificacion_canales SET transporte_vivo = true WHERE codigo = 'push';
-- REVERSA: UPDATE public.cat_notificacion_canales SET transporte_vivo = false WHERE codigo = 'push';
```

### 1.3 · La PRUEBA DIRIGIDA de push — **no se aplicó**

Las dos sentencias van en **UNA transacción**: entre ① y ② la fila vive con
`canal_elegido='email'` y el tick del minuto podría **mandar un correo**.

```sql
BEGIN;

SELECT public.registrar_intencion_notificacion(
  p_tipo                 := 'paquete_vence',
  p_destinatario_user_id := 'dd024680-3d1c-4465-b38b-dedab45da037'::uuid,
  p_mascota_id           := (SELECT b.mascota_id FROM public.bonos b
                              WHERE b.id = 'fa562b2a-0125-4b8c-9557-846eb1e7dfb6'),
  p_datos                := (
      SELECT public._voz_notificacion(
        'paquete_vence',
        'dd024680-3d1c-4465-b38b-dedab45da037'::uuid,
        b.mascota_id,
        jsonb_build_object(
          'restantes', (b.unidades_total - b.unidades_usadas)::text,
          'vence',     to_char(b.fecha_vencimiento,'FMDD') || ' de ' ||
                       (ARRAY['enero','febrero','marzo','abril','mayo','junio','julio',
                              'agosto','septiembre','octubre','noviembre','diciembre'])
                         [extract(month from b.fecha_vencimiento)::int]))
      FROM public.bonos b WHERE b.id = 'fa562b2a-0125-4b8c-9557-846eb1e7dfb6'),
  p_clave_dedup          := 'prueba-push-s90c-1'
) AS intencion_id;

UPDATE public.notificacion_intencion
   SET resuelto_como = coalesce(resuelto_como,'{}'::jsonb)
                     || jsonb_build_object('canal_elegido','push',
                                           'forzado_por','prueba-dirigida-S90-C'),
       updated_at = now()
 WHERE clave_dedup = 'prueba-push-s90c-1'
RETURNING id, estado, motivo, resuelto_como->>'canal_elegido' AS canal,
          datos->>'titulo' AS titulo, datos->>'mensaje' AS mensaje;

COMMIT;
```

**Reversa:** `DELETE FROM public.notificacion_intencion WHERE clave_dedup = 'prueba-push-s90c-1';`
**⚠️ La reversa NO deshace el aviso** — un push entregado no se retira (§10).
**Para repetir:** bumpear la clave a `-2` en los DOS lugares (`ON CONFLICT DO NOTHING`).

**Lectura del `RETURNING`:** `nacida`+`push` = lista · `diferida`/`diferida_techo` = el techo cortó, **se reporta y NO se sube** · cero filas = la clave ya existía.

**Después:** `SELECT public.despachar_notificaciones(false);` y
`curl -s -X POST 'https://zyltipqscdsdsxnjclhp.supabase.co/functions/v1/despachar-push' -H 'Content-Type: application/json' -d '{"origen":"gate-s90c"}'`
→ esperado `{"modo":"transporte_vivo","proyecto":"e-petplace-7854e","entregadas":1,...}`

### 1.4 · El LECTOR de consultas con receta — ✅ **YA ENTREGADO POR A, no hay nada que aplicar**

> **✅ RESUELTO MIENTRAS C ESCRIBÍA ESTE VOLCADO.** Verificado contra el objeto
> al cerrar: **`obtener_consultas_con_receta(p_mascota_id uuid)` EXISTE**, con la
> firma exacta —`TABLE(cita_id uuid, fecha date, hora time, negocio text,
> profesional text, medicamentos integer)`— y **su wrapper también**:
> `obtenerConsultasConReceta` en `packages/api/src/wrappers/documentos.ts:115`.
> Commits de A: `eecfe559` (el lector) y `9707485e` (el wrapper).
>
> **⇒ El 🔴 del gate está DESTRABADO. Lo que falta es la Hoja de elección, y es
> de C.** El SQL de abajo queda como registro de lo que C pidió — **no se
> aplica**, ya está.

Predicado **copiado del motor** (`emitir_token_documento`) para que lector y emisor no diverjan:

```sql
CREATE OR REPLACE FUNCTION public.obtener_consultas_con_receta(p_mascota_id uuid)
RETURNS TABLE (cita_id uuid, fecha date, hora time, negocio text,
               profesional text, medicamentos integer)
LANGUAGE plpgsql STABLE SECURITY DEFINER
SET search_path TO 'public','pg_temp' AS $$
BEGIN
  IF auth.uid() IS NULL THEN
    RAISE EXCEPTION 'auth_required' USING ERRCODE='42501';
  END IF;
  IF NOT user_tiene_acceso_a_mascota(p_mascota_id) THEN
    RAISE EXCEPTION 'no_access_to_mascota' USING ERRCODE='42501';
  END IF;
  RETURN QUERY
  SELECT c.id, c.fecha, c.hora,
         cc.nombre_comercial,
         pr.nombre_completo,          -- NULL honesto si no consta
         count(*)::integer
    FROM evento_medicacion_prescrita m
    JOIN evento_cita_servicio c      ON c.id = m.cita_id
    LEFT JOIN prestadores p          ON p.id = c.prestador_id
    LEFT JOIN cuentas_comerciales cc ON cc.id = p.cuenta_comercial_id
    LEFT JOIN prestador_empleados pe ON pe.id = c.empleado_id
    LEFT JOIN profiles pr            ON pr.id = pe.user_id
   WHERE m.mascota_id = p_mascota_id
   GROUP BY c.id, c.fecha, c.hora, cc.nombre_comercial, pr.nombre_completo
   ORDER BY c.fecha DESC, c.hora DESC;
END $$;
```

> **⚠️ C NO verificó los nombres de columna del profesional** (`profiles.nombre_completo`,
> `prestador_empleados.user_id`). **A los coteja contra el objeto antes de aplicar.**

**El predicado literal del motor, para no re-buscarlo:**
```sql
-- dentro de emitir_token_documento, rama 'receta':
IF NOT EXISTS (SELECT 1 FROM evento_medicacion_prescrita m
                WHERE m.cita_id = p_ref AND m.mascota_id = p_mascota_id)
THEN RAISE EXCEPTION 'receta_sin_medicacion' USING ERRCODE = '22023';
```

### 1.5 · TEXTO para `DEUDAS_CANONICAS.md` — la deuda del koi

**Número verificado por grep el 7-ago:** la más alta es **D-683**; `grep -c "D-684"` en
`docs/` y `CLAUDE.md` = **0**. *(A re-verifica al depositar — regla 66.)*

```markdown
#### D-684 — `pez/koi` de la galería no corresponde a su rótulo ⚪ REGISTRO
⚪ BAJA. **La imagen publicada como `pez/koi.webp` no es un koi:** el koi es
una carpa alargada de patrón blanco/naranja/negro, y la imagen lee como
CÍCLIDO (cuerpo corto, iridiscencia azul/roja). **Origen: hallazgo de C al
publicar la galería (S90)** — salió de mirar 6 de las 105, no de una
auditoría; **las otras 99 NO están verificadas contra su rótulo**.
**El founder la deja hoy y firma su reemplazo como deuda.**
**Atenuante MEDIDO:** el archivo de origen era un **UUID crudo**
(`21146D49-C35D-49D5-AD38-B0A7FA32A746.webp`), así que **el nombre de CARPETA
es la única afirmación de qué es**. Con imágenes **generadas por IA**, un
rótulo que no corresponde al dibujo es **modo de falla esperable**.
**Disparo: cuando se regenere cualquier imagen de la galería.**
Origen: S90-C (publicación de la galería especie/raza).
```

### 1.6 · TEXTO para `DEUDAS_CANONICAS.md` — el origen IA (enmienda a D-288)

**D-288 dice hoy «Assets con licencia comercial verificada (LICENCIA.txt en el
repo)». Ese archivo NO EXISTE y NO CORRESPONDE.** Enmienda a depositar:

```markdown
> **ENMIENDA S90 (firma del founder, 7-ago-2026) — EL BLOQUEO SE SATISFACE POR
> ORIGEN, NO POR LICENCIA.** Las 106 imágenes de la galería de especie/raza
> **son GENERADAS CON IA**: no hay derechos de terceros que verificar, y por lo
> tanto **no hay `LICENCIA.txt` que escribir** — el requisito de «licencia
> comercial verificada» queda cumplido porque no se licenciaron: se generaron.
> **Se escribe con su fecha porque el día que alguien pregunte por qué un
> labrador se ve así, la respuesta tiene que estar escrita, no recordada.**
> Publicadas en el bucket `especies-razas` (S90-C). Hermana: **D-684**.
```

---

## 2 · MEDICIONES DEPOSITADAS — con su comando

### 2.1 · El canal PUSH

| pieza | estado 7-ago |
|---|---|
| `cat_notificacion_canales` push | `transporte_vivo=false` · `es_piso=false` · `orden=2` (**antes** que email en 3) |
| `push_tokens` | **1 fila** · android · activo · 142 chars · id `5edfa48f-2621-4b06-a4f4-3f3dd7d181d4` · user `dd024680-…` |
| `FCM_SERVICE_ACCOUNT` | **CARGADO** (11 secrets) — `npx supabase secrets list --project-ref zyltipqscdsdsxnjclhp` |
| `despachar-push` | **desplegada**; responde `{"modo":"transporte_vivo","proyecto":"e-petplace-7854e","entregadas":0,...}` |

**QUIÉN ELIGE EL CANAL — el hallazgo que ordena todo el arco:**
**`registrar_intencion_notificacion`, AL NACER la intención**, no el despachador.
`despachar_notificaciones` solo aplica vigencia/kill-switch/techo y marca
`para_transporte`. **Por eso el flip cambia el futuro y no la cola.**

**El par de prueba de tres casos, medido en frío contra el catálogo y las
preferencias reales del founder (sin escribir nada):**
```
A · hoy — push SIN tren        → canal_elegido = email
B · push CON tren (post-flip)  → canal_elegido = push
C · sin trenes                 → canal_elegido = in_app
   (habilitados en los tres: in_app,push,email)
```

**Token nativo, no de Expo:** la app llama `getDevicePushTokenAsync()`
(`apps/cliente/src/components/invitacion-avisos.tsx:49`) ⇒ en Android es el
token **de FCM** ⇒ **FCM v1 directo es el transporte correcto**.

### 2.2 · El TECHO de `operacion` — 🔴 bloqueaba el gate de la cita

```
categoria         techo    usadas(founder)  LIBRE
operacion         20/24h        20            0     ← EN EL TECHO
saldo_pagado     200/24h        13          187
salud_seguridad  200/24h         0          200
relacional        20/24h         0           20
comercial          5/24h         0            5
```
**Los 4 tipos abiertos de cita son `operacion`** ⇒ una `cita_confirmada` para el
founder nacía **`diferida`** y no llegaba al transporte. *(Medido 14:41 UTC; se
liberaba una plaza a las 15:45 UTC. **S91 re-mide: la ventana es móvil de 24 h.**)*

### 2.3 · El ALTA DE MASCOTA

**Son DOS flujos calcados de 4 pantallas** — divergen **42 · 27 · 24 · 50** líneas:

```
1ª mascota : app/onboarding/{mascota,fecha,foto,cierre}.tsx
             RPC crear_familia_con_primera_mascota
2ª+        : app/(tabs)/hogar/agregar/{index,fecha,foto,cierre}.tsx
             RPC agregar_mascota_a_familia
```

**Campos:** paso 1 `nombre`+`especie` (**los únicos obligatorios**:
`nombre.trim().length>0 && especie!==undefined`) · paso 2 `fecha`+`sexo`
(opcionales, **sin guard**) · paso 3 `foto`+encuadre `cx/cy/z` · paso 4 RPC.

**`mascotas` tiene 27 columnas. El alta del dueño llena 9:**
`nombre · especie · origen · familia_id · user_id · fecha_nacimiento ·
fecha_nacimiento_precision · sexo · foto_url` (+ `foto_cx/cy/z` por
`declararFotoMascota`; `pet_hash` es GENERATED ALWAYS).

**Nulos sobre 20 mascotas vivas:**
```
criadero_id 20/20 · refugio_id 20/20 · microchip 20/20   ← nadie los llenó NUNCA
paseo_social_ok 16/20 · raza 15/20 · talla/pelaje/foto_url 13/20
fecha_nac_precision 9/20 · fecha_nacimiento 8/20 · sexo 4/20 · user_id 2/20
especie 20/20 = 'perro' · estado_vida 20/20 = 'activa'
```

**7 funciones insertan en `mascotas`** — y **el prestador pide MÁS que el dueño**:
`crear_mascota_walkin`, `crear_alta_asistida_existente` y
`crear_alta_asistida_pendiente` traen **`raza` y `microchip`**; las dos del dueño
**no tienen esos parámetros**. Al revés con `precision_fecha`.
`simular_cliente_crea_mascota` es la ÚNICA que acepta `p_origen`.

**`origen` está HARDCODEADO** en el `INSERT` de las dos RPCs del dueño:
```sql
values (btrim(p_nombre_mascota), p_especie, 'desconocido', v_familia_id, v_uid, …)
```
CHECK admite 9 valores; distribución real: `desconocido 15 · alta_asistida 3 ·
comprado_particular 1 · nacido_en_casa 1`. Y `mascotas_origen_coherencia_check`
ata `criadero_id`/`refugio_id` a `origen` ⇒ **ambos son 100% nulos POR
CONSTRUCCIÓN**, no por olvido.

**La fecha aproximada EXISTE y NUNCA se usó:**
`chk_mascotas_fecha_nacimiento_precision` admite `exacta|aproximada|estimada`
**y exige `precision NOT NULL ⇒ fecha NOT NULL`** ⇒ **una edad sin fecha es
inexpresable**. `CampoFecha` tiene el botón «No sé la fecha» → 4 etapas
(`cachorro 0 · joven 2 · adulto 5 · mayor 9` años atrás) y `elegirEtapa`
(`packages/ui/src/components/CampoFecha.tsx:252`) escribe
`${anioActual-aniosAtras}-01-01` + `'estimada'`. **Distribución real: `exacta` 11
· null 9 · `aproximada` 0 · `estimada` 0.**

**La 2ª mascota re-pregunta TODO** (`useState('')` / `useState(undefined)`, cero
pre-fill). Lo único que NO re-pregunta es `familia_id`, que la RPC deriva del
caller. `country_code` entra por default `'EC'`.

**`evento_hito_narrativo` NO EXISTE:** `to_regclass` = `null`, 0 tablas y 0
columnas con «hito». De las **45 tablas `evento_*`**, ninguna es de hito narrativo.

### 2.4 · La BITÁCORA

**Ni falta la puerta ni falta el motor: los dos están enteros.** Lo que bloquea
a un pet parent sin adiestrador es **UN guard deliberado** en
`registrar_bitacora_familia`:

```sql
IF v_programa IS NULL AND NOT EXISTS (
  SELECT 1 FROM evento_cita_servicio c
  JOIN tipos_servicio ts ON ts.codigo=c.tipo_servicio AND ts.categoria='adiestramiento'
  WHERE c.mascota_id=p_mascota_id AND c.estado IN ('confirmada','en_curso')
    AND c.estado_reserva='pagada')
THEN  -- el propio comentario dice: «la bitácora UNIVERSAL es diferido declarado (§7)»
  RAISE EXCEPTION 'sin_contexto_activo' USING ERRCODE='22023';
```

```
evento_bitacora_familia : 9 cols · 2 filas · 1 escritor (registrar_bitacora_familia)
   RLS: SELECT por user_tiene_acceso_a_mascota · DELETE admin · SIN policy de INSERT
   aportado_por_menor: NOT NULL y CABLEADO — derivado del rol en familia_miembro
   las 2 filas vivas: programa_contratado_id NULL en ambas, 1 chip, sin texto
evento_temperamento_observacion : 12 cols · 0 filas · 0 productores · 0 wrappers
   · 0 superficies — PERO con policy de INSERT abierta a authenticated
   sus columnas prestador_id/empleado_id la ubican del lado PRESTADOR
cat_conductas_bitacora        : 10 (todas activas, es_seed_preliminar=TRUE,
                                pais_codigo NULL, SIN columna especie)
cat_objetivos_adiestramiento  : 23 (idem) — los dos con nombre_familia es+en
pantalla cliente : app/(tabs)/hogar/bitacora.tsx — 350 líneas, LEE Y ESCRIBE
wrapper          : packages/api/src/wrappers/adiestramiento-bitacora.ts (3 exports)
navegación       : UN solo acceso → hogar/adiestramiento.tsx:218
rebote con voz   : sin_contexto_activo → «La bitácora se abre con un programa o
                   una sesión de adiestramiento activa.»
```

**8 de 10 conductas son neutras de especie; 2 son caninas por texto:**
`ladridos_excesivos` («Ladró más de lo normal») y `jugo_con_otros_perros`.

**⚠️ ACOPLAMIENTO MEDIDO que nadie más mira:** `registrar_intencion_notificacion`
**lee `evento_bitacora_familia` para su GATE 2 (menores, P5)**. Hoy cubre 2 filas;
con bitácora universal pasa a cubrir la corriente diaria de cada hogar.

**⚠️ CHOQUE CON LETRA FIRMADA — para que no se descubra chocando:** la alerta de
seguridad de Eje 6 que ve recepción (`BIO_EXPEDIENTE:265`) **NO transporta
conductas**: su objeto lo fijó `LETRA_CUIDADO_ESPECIAL_S74` (FIRMADA) —
**«Voz de MANEJO, jamás de carácter»**, código de catálogo, jamás texto libre.
Las 10 conductas están en voz de HECHO («Rompió algo en casa») — **es la voz que
esa letra prohíbe para ese canal**. La pregunta abierta correcta es si una
conducta puede **DISPARAR** un cuidado especial (eso es **D-500**, umbral firmado
>5, bordes sin firma). **Y baja la urgencia: `cuidado_especial` no existe en el
schema — 0 tablas. D-469 sigue 🔴.**

**Y `BIO_EXPEDIENTE:884` Eje 6 dice al literal:** *«observaciones del dueño …
Timeline + perfil vigente (temperamento, miedos)»* ⇒ **universalizar la bitácora
no agrega contenido al eje: lo cumple.**

### 2.5 · La GALERÍA — publicada

```
bucket   : especies-razas · PÚBLICO · mime único image/webp · límite 256 KB
URL base : https://zyltipqscdsdsxnjclhp.supabase.co/storage/v1/object/public/especies-razas/
ruta     : <codigo_especie>/<slug-raza>.webp   ·   fallback <especie>/generico.webp
objetos  : 111 (105 razas + 6 genéricos)
peso     : 739 KB desde 38.3 MB  →  −98.1 %  ·  prom 7.0 KB  ·  máx 14.0 KB
proceso  : 256×256 webp q80 · aplanado sobre BLANCO · recorte CENTRADO a cuadrado
```

**Genéricos (copiados dentro del bucket, mismo objeto):** `perro←criollo` ·
`gato←gato-comun` · `ave←periquito-australiano` · `pez←pez-dorado` ·
`conejo←californian` · `roedor←hamster-sirio`.
**`reptil` NO tiene genérico** — verificado: `reptil/generico.webp` → **400**.

**⚠️ REPTIL: sus 5 imágenes ESTÁN en el bucket público** (`reptil/…`), **sin fila
de catálogo y sin superficie**. `cat_especies.reptil` sigue `activo=false`.
Son alcanzables por URL si alguien la adivina — **declarado, no escondido**.

**Códigos reales de `cat_especies`** (las carpetas venían en plural):
`Perros→perro · Gatos→gato · Aves→ave · Peces→pez · Conejos→conejo ·
Roedores→roedor · Reptiles→reptil`. Activas: las 6 primeras; `reptil`, `huron`,
`cobaya`, `otro`, `equino` inactivas.

**Anomalías de la fuente:** `Perros/Cocker Spaniel` traía **2** archivos
(desempate: gana el que se llama como su carpeta) · **28 archivos con nombre ≠
carpeta**, incluidos **9 de los 10 peces con UUID crudo** ⇒ **la clave confiable
es el nombre de CARPETA**.

#### EL MAPEO COMPLETO — insumo directo de D-379

> **Vivía SOLO en el scratchpad (`_mapeo.json`), que muere con la sesión.**
> Formato: `especie ⟶ slug ⟶ carpeta de origen ⟶ bytes finales`.

```tsv
perro	akita-inu	Akita Inu	4640
perro	american-bully	American Bully	7112
perro	beagle	Beagle	7416
perro	bernese	Bernese	9404
perro	bichon-frise	Bichon Frise	4118
perro	border-collie	Border Collie	8022
perro	boston-terrier	Boston terrier	5626
perro	boxer	Boxer	7488
perro	bull-terrier	Bull Terrier	4682
perro	bulldog-frances	Bulldog_Frances	7474
perro	bulldog-ingles	Bulldog_Ingles	8730
perro	charles-spaniel	Charles Spaniel	8436
perro	chihuahua	Chihuahua	4696
perro	chow-chow	Chow Chow	8802
perro	cocker-spaniel	Cocker Spaniel	7568
perro	collie	Collie	8752
perro	criollo	Criollo	10858
perro	dalmata	Dalmata	8794
perro	doberman	Doberman	4664
perro	golden-retriever	Golden retriever	7568
perro	gran-danes	Gran Danes	7144
perro	husky-siberiano	Husky Siberiano	6676
perro	jack-rusell	Jack_Rusell	5826
perro	labrador-retriever	Labrador_Retriever	7204
perro	maltes	Maltes	3306
perro	pastor-belga	Pastor Belga	6980
perro	pastor-aleman	Pastor_Aleman	7858
perro	pekines	Pekines	9232
perro	pinscher	Pinscher	5208
perro	pitbul-terrier	Pitbul Terrier	7358
perro	pomerania	Pomerania	7274
perro	poodle	Poodle	10558
perro	pug	Pug	5766
perro	rottweiler	Rottweiler	6478
perro	salchicha	Salchicha	5754
perro	samoyedo	Samoyedo	3268
perro	san-bernardo	San Bernardo	9776
perro	shiba-inu	Shiba Inu	5434
perro	shih-tzu	Shih_Tzu	6582
perro	shnauzer	Shnauzer	7474
perro	springer-spaniel	Springer Spaniel	10826
perro	stanffordshire-bull-terrier	Stanffordshire Bull Terrier	9086
perro	weimaraner	Weimaraner	6238
perro	yorkshire-terrier	Yorkshire_Terrier	10308
gato	abisinio	Abisinio	5912
gato	american-shorthair	American Shorthair	9542
gato	azul-ruso	Azul Ruso	7256
gato	bengali	Bengalí	10590
gato	birmano	Birmano (Birman)	4146
gato	bosque-de-noruega	Bosque de Noruega	5492
gato	british-shorthair	British Shorthair	8754
gato	cornish-rex	Cornish Rex	6936
gato	devon-rex	Devon Rex	8886
gato	exotic-shorthair	Exotic Shorthair	7812
gato	gato-comun	Gato Común	10026
gato	maine-coon	Maine Coon	9738
gato	oriental-de-pelo-corto	Oriental de Pelo Corto	7994
gato	persa	Persa	6010
gato	ragdoll	Ragdoll	5008
gato	scottish-fold	Scottish Fold	8772
gato	siames	Siamés	5742
gato	siberiano	Siberiano	7066
gato	sphynx	Sphynx (Esfinge)	4932
gato	turkish-angora	Turkish Angora (Angora Turco)	3920
ave	agapornis	Agapornis (Lovebird)	4926
ave	cacatua-alba	Cacatúa Alba	2962
ave	canario	Canario	4612
ave	conuro-mejillas-verdes	Conuro Mejillas Verdes	6998
ave	diamante-mandarin	Diamante Mandarín	7072
ave	eclectus	Eclectus	6288
ave	guacamayo-azul-y-amarillo	Guacamayo Azul y Amarillo	8098
ave	loro-yaco-africano	Loro Yaco Africano	7448
ave	ninfa	Ninfa (Cockatiel)	4228
ave	periquito-australiano	Periquito Australiano	6582
pez	corydora	Corydora	7406
pez	disco	Disco	14386
pez	guppy	Guppy	10018
pez	koi	Koi	13514
pez	molly	Molly	8796
pez	pez-betta	Pez Betta (Betta splendens)	10752
pez	pez-dorado	Pez Dorado (Goldfish)	5672
pez	pez-angel-de-agua-dulce	Pez Ángel de Agua Dulce (Escalar)	7202
pez	platy	Platy	6280
pez	tetra-neon	Tetra Neón	4870
conejo	californian	Californian	6112
conejo	english-angora	English Angora	3814
conejo	holland-lop	Holland Lop	5972
conejo	lionhead	Lionhead	5642
conejo	mini-lop	Mini Lop	6856
conejo	mini-rex	Mini Rex	6626
conejo	netherland-dwarf	Netherland Dwarf	4930
conejo	rex	Rex	6226
roedor	chinchilla	Chinchilla (Chinchilla lanigera)	8798
roedor	cobaya-o-cuy	Cobaya o Cuy	8186
roedor	degu	Degu	8110
roedor	hamster-ruso	Hámster Ruso	5862
roedor	hamster-sirio	Hámster Sirio	6292
roedor	jerbo-o-gerbil-mongol	Jerbo o Gerbil Mongol	5968
roedor	rata-domestica	Rata Doméstica (Rattus norvegicus domestica)	6624
roedor	raton-domestico	Ratón Doméstico (Mus musculus domesticus)	5410
reptil	dragon-barbudo	Dragón Barbudo	9990
reptil	gecko-leopardo	Gecko Leopardo	7954
reptil	piton-bola	Pitón Bola	10046
reptil	serpiente-del-maiz	Serpiente del Maíz	10320
reptil	tortuga-rusa	Tortuga Rusa	8344
```

**Regla del slug:** del nombre de CARPETA · se descarta **entero** lo que va entre
paréntesis (nombre alterno o científico) · sin tildes · minúsculas · guiones.

### 2.6 · El CARNET escaneado

```
columna  : evento_vacuna_aplicada.archivo_url (text)
bucket   : mascotas — ⚠️ PRIVADO (5 MB · image/jpeg|png|webp)
ruta     : {uuid}/carnet-{timestamp}.jpg
filas    : 32 vacunas · 32 con archivo_url (100%)
imágenes : 5 distintas · 5 mascotas
```
> ### **UNA imagen por ESCANEO, compartida por las N vacunas de esa tanda.**
> 32 filas ↔ 5 imágenes. **La imagen NO pertenece a ninguna fila.**

**Procedencia: 32 de 32 = `declarado_por_familia`.** Leer por cámara **no eleva la
procedencia**. Cero `verificado_por_prestador`.

### 2.7 · La RECETA

```
evento_medicacion_prescrita : 4 filas · 2 citas · 1 mascota
urlDocumento(mascotaId, tipo, refId?)  — packages/api/src/wrappers/documentos.ts:67
PAPELES_CON_REF = { 'receta', 'certificado_salud' }
códigos: ref_requerida · receta_sin_medicacion · sin_acceso · sin_sesion
⚠️ EL TOKEN ES DE UN SOLO USO — sondear para descubrir quema el papel
```

---

## 3 · DISCRIMINADORES VIVOS — IDs reales, para no reconstruirlos

```
founder (cliente)  dd024680-3d1c-4465-b38b-dedab45da037
Thor               d2e31d70-54fc-4d47-b425-1617239257eb   (activa)
Zeus               a3332037-c487-45c1-875f-83caf342f59e   (activa)
push_token         5edfa48f-2621-4b06-a4f4-3f3dd7d181d4   android · activo

BONO REAL Y VERDADERO (la prueba de push dice la verdad con estos números):
  fa562b2a-0125-4b8c-9557-846eb1e7dfb6 · Thor · 10 totales − 2 usadas = 8
  vence 2026-08-12 · estado activo
  → voz generada: «Te quedan 8 salidas por usar / El paquete de paseos de Thor
     vence el 12 de agosto y todavía te quedan 8 salidas.»

RECETA — el caso ambiguo que el founder chocó, reproducible:
  Thor tiene DOS consultas con medicación
  5b701b81-9b00-4808-94a8-ae2e55187029 · 2026-07-19 09:30 · 2 meds · Paseos Andres
  bd5f2e19-17c5-40e5-b371-c6dcb92cc4b1 · 2026-07-21 12:30 · 2 meds · Clínica Aurora
  → prueba que «traer la última» habría MENTIDO

CITA CONFIRMADA REAL (para el gate literal del founder, cuando el techo abra):
  f36f8ee2-f756-46c8-82cd-59514d00ec30 · Thor · 10-ago 17:30 · Paseos Andres
  confirmada · pagada
```

---

## 4 · LO QUE QUEDA A MEDIAS — regla 77

| # | qué falta exactamente | próximo paso, de quién |
|---|---|---|
| 1 | **🔴 El teléfono no vibró.** El transporte está vivo y responde, pero **la prueba dirigida no se aplicó**: el SQL de §1.3 no corrió. | **A** aplica §1.3 · **founder** corre el runbook |
| 2 | **El cron del vagón (§1.1) no existe.** Hoy push depende del tick del correo. | **A** |
| 3 | **El flip (§1.2) NO se ejecutó** — correcto por la ley de secuencia §9. | **founder** gatea, **A** aplica |
| 4 | **La receta sin salida (🔴 del gate) NO se construyó.** ✅ **El lector y su wrapper YA EXISTEN** (A, `eecfe559`+`9707485e`) — **la dependencia cayó**. Falta SOLO la Hoja de elección: tocar «Receta» → lista de consultas medicadas → `refId` por fila · una sola → descarga directa · ninguna → voz, no rebote. | **C** — sin bloqueo |
| 5 | **El carnet anexo NO se construyó** — espera **firma de ubicación** del founder. Voto de C y del arquitecto: **página propia al final, rotulada**. | **founder** firma |
| 6 | **D-684 y la enmienda de D-288 no están en `docs/`** — textos en §1.5 y §1.6. | **A** deposita |
| 7 | **La enmienda de documentos (`14226835`) no está en ningún teléfono** — sin OTA. | **mesa** abre veda |
| 8 | **Dos ramas de `despachar-push` sin ejercitar:** el retiro de token muerto (exige un token muerto real) y el intercambio OAuth contra Google (se probó la FIRMA, no la respuesta). El paso 3 del runbook las prueba juntas. | **founder**, al correr la prueba |

---

## 5 · LO QUE S91 NO DEBE RE-MEDIR

Todo lo de **§2**, con estas salvedades de caducidad:

- **✅ Sigue cierto sin fecha de vencimiento:** el esquema de `mascotas` (27 cols
  y sus CHECKs) · los 7 escritores · `origen` hardcodeado · la mecánica de
  `CampoFecha` · el guard `sin_contexto_activo` · las RLS de bitácora y
  temperamento · que `evento_hito_narrativo` y `cuidado_especial` **no existen** ·
  el bucket `especies-razas` y su mapeo · el bucket privado del carnet · quién
  elige el canal de notificación.
- **⏱️ CADUCA — re-medir siempre:** los **conteos de filas** (mascotas, bitácora,
  medicación, vacunas) y sobre todo **el techo de `operacion`** (ventana móvil de
  24 h). **Ningún contador se toma de este archivo: se lee del objeto** (regla 66
  y la candidata ④ de `METODO_TRES_PISTAS`).

**Comandos que valen:**
```bash
npx supabase --experimental db query --linked --file <archivo.sql>   # inline falla
npx supabase secrets list --project-ref zyltipqscdsdsxnjclhp
node scripts/verify-diseno.mjs            # lint de B — VERDE, 25 reglas, al cierre
pnpm --filter cliente exec tsc --noEmit   # VERDE al cierre
npx --yes deno check supabase/functions/despachar-push/index.ts   # VERDE
```

---

## 6 · HALLAZGOS LATERALES — probado vs sospecha

### ✅ HALLAZGOS PROBADOS

1. **`chevron.ts` de `packages/ui` NO se exporta a propósito** y su cabecera lo
   dice: *«una pantalla que necesite un chevron usa la PIEZA que lo porta, jamás
   el path suelto»*. **Existe una violación viva:** `[mascotaId].tsx` dibuja un
   chevron artesanal en la sección de vacunas (`M9 5l7 7-7 7`, ~línea 966), con
   trazo **distinto** al canónico. **C no lo tocó** (fuera de alcance).
2. **La prop `direccion` de `CeldaNavegacion` (S83-B12) declaró su «condición de
   muerte del clon»** y C la ejecutó en la sección de documentos. **Puede haber
   otros encabezados-que-despliegan artesanales**; C no barrió.
3. **`eas-cli` scaffoldea un `app.json` stub si se corre desde la RAÍZ** — incluso
   en comandos de LECTURA. Siempre desde `apps/<app>/`.
4. **No hay encoder webp en la máquina del founder** (sin `cwebp`, sin
   ImageMagick, sin PIL; `sips` **lee** webp pero **no lo escribe**). C usó un
   venv con Pillow en scratchpad — **cero instalación en el sistema, y muere con
   la sesión**: S91 lo rehace si necesita procesar imágenes.
5. **`_voz_notificacion(p_tipo, p_user_id, p_mascota_id, p_extra)` es la voz
   firmada** que usan los productores. Sin `p_mascota_id` ni `p_extra` los textos
   salen **con huecos** («La cita  con el prestador quedó confirmada para el .»).

### 🟡 SOSPECHAS — NO probadas, no curar sobre esto

1. **`pez/koi` no parece un koi** → es **D-684**. C miró **6 de 105**: **no hay
   evidencia sobre las otras 99 en ninguna dirección**.
2. **Una consulta con medicación está atribuida a «Paseos Andres»**, que es un
   paseador (cita `5b701b81…`). **Puede ser dato de prueba o un hueco de
   atribución. C no lo investigó.**
3. **El carnet multi-página puede quedar incompleto en el anexo**: hoy hay UNA
   imagen por escaneo. **C no midió si algún carnet real tenía más de una página.**
4. **`raza` es `text` libre y solo la escribe el prestador**; el dueño no puede
   declararla. Que eso sea la causa del 75% de nulos es **inferencia razonable,
   no medición.**

---

## 7 · LO QUE C NECESITA DE A — el aviso

1. **APLICAR SQL, en este orden:** §1.1 (cron del vagón) → §1.3 (prueba dirigida)
   → §1.2 (flip, **solo después del gate del founder**).
   **§1.4 YA NO SE APLICA: A lo entregó durante esta misma sesión** (`eecfe559`
   el lector + `9707485e` el wrapper) — queda como registro, no como pedido.
2. **DEPOSITAR EN `docs/`:** §1.5 (ficha **D-684**) y §1.6 (enmienda a **D-288**,
   el origen IA). **Re-verificar que D-684 siga libre al depositar** (regla 66).
3. **MERGEAR: nada.** Los dos commits de C (`a8c7758d`, `14226835`) **ya están en
   `origin/main`** — verificado con `merge-base --is-ancestor`. HEAD ==
   origin/main == `eecfe559`, árbol limpio, cero pendientes.
4. **Y para la mesa, no para A:** la enmienda de documentos **no viajó a ningún
   teléfono** — necesita OTA, y la veda es de la mesa.
